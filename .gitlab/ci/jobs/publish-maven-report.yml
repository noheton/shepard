publish_maven_report:
  stage: upload
  needs:
    - job: build_maven_report
      artifacts: true
  script:
    - ls -hal backend/target/site
    - mkdir public
    - mv backend/target/site/* public/
    - echo ${CI_PAGES_DOMAIN}
  artifacts:
    expire_in: 2 days
    paths:
      - public
