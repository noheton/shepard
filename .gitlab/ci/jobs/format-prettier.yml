format_prettier:
  image: node:20-slim
  stage: check
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - .npm/
  needs: []
  before_script:
    - npm ci --cache .npm --prefer-offline
  script:
    - npm run check-format
