format_prettier:
  image: node:21-slim
  stage: check
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - .npm/
  before_script:
    - npm ci --cache .npm --prefer-offline
  needs: []
  script:
    - npm run check-format
