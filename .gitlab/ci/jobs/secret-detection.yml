include:
  - template: Jobs/Secret-Detection.latest.gitlab-ci.yml

secret_detection:
  stage: check
  needs: []
  before_script:
    - !reference [.version_number, script]
  script:
    - echo ${CI_COMMIT_TAG}
    - echo ${VERSION_NUMBER}
