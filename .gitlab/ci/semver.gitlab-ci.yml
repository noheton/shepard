# For commits of pushed tags
stages:
  - tag

fetch_tag:
  stage: tag
  before_script:
    - !reference [.version_number, script]
  script:
    - ls -la
    - echo ${CI_COMMIT_TAG}
    - echo ${LATEST_TAG}
