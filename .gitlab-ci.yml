image: python:3

before_script:
  - pip install poetry
  - poetry config virtualenvs.create false
  - poetry install

black:
  script:
    - python -m black main.py --check

lint:
  script:
    - python -m ruff check .

format:
  script:
    - python -m ruff format .
