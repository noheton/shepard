"""ANTLR4-generated lexer + parser for KRL tier-1.

These files are produced by running
``java -jar antlr-4.13.2-complete.jar -Dlanguage=Python3
       -o generated -Xexact-output-dir KrlLexer.g4 KrlParser.g4``
from the ``grammar/`` directory. They are committed to the repo
so that ``pip install -e .`` does not require ANTLR's Java
toolchain. Regenerate when the ``.g4`` grammar changes.
"""
