version: "3"

services:
  neo4j:
    image: neo4j:5.24
    environment:
      - NEO4J_AUTH=${NEO4J_AUTH}
    ports:
      - 7474:7474
      - 7687:7687
    restart: unless-stopped

  mongodb:
    image: mongo:8.0
    ports:
      - 27017:27017
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD}
      - MONGO_USERNAME=${MONGO_USERNAME}
      - MONGO_PASSWORD=${MONGO_PASSWORD}
      - MONGO_INITDB_DATABASE=${MONGO_INITDB_DATABASE}
    restart: unless-stopped
    volumes:
      - ../infrastructure/docker-entrypoint-initdb.d/00-create-mongo-user.js:/docker-entrypoint-initdb.d/00-create-mongo-user.js

  mongoexpress:
    image: mongo-express:latest
    ports:
      - 8084:8081
    environment:
      - ME_CONFIG_MONGODB_SERVER=mongodb
      - ME_CONFIG_BASICAUTH_USERNAME=${MONGO_INITDB_ROOT_USERNAME}
      - ME_CONFIG_BASICAUTH_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD}
      - ME_CONFIG_MONGODB_ENABLE_ADMIN=true
      - ME_CONFIG_MONGODB_ADMINUSERNAME=${MONGO_INITDB_ROOT_USERNAME}
      - ME_CONFIG_MONGODB_ADMINPASSWORD=${MONGO_INITDB_ROOT_PASSWORD}
    restart: unless-stopped
    depends_on:
      - mongodb

  timescaledb:
    image: timescale/timescaledb:latest-pg16
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_SHEPARD_USER: ${POSTGRES_SHEPARD_USER}
      POSTGRES_SHEPARD_USER_PW: ${POSTGRES_SHEPARD_USER_PW}
    restart: unless-stopped
    volumes:
      - ../infrastructure/docker-entrypoint-initdb.d/01-init-postgres-db.sh:/docker-entrypoint-initdb.d/01-init-postgres-db.sh

  influxdb:
    image: influxdb:1.8
    environment:
      - INFLUXDB_ADMIN_USER=${INFLUXDB_ADMIN_USER}
      - INFLUXDB_ADMIN_PASSWORD=${INFLUXDB_ADMIN_PASSWORD}
    ports:
      - 8086:8086
      - 8088:8088
    restart: unless-stopped

  chronograf:
    image: chronograf:latest
    environment:
      - INFLUXDB_URL=http://influxdb:8086
      - INFLUXDB_USERNAME=${INFLUXDB_ADMIN_USER}
      - INFLUXDB_PASSWORD=${INFLUXDB_ADMIN_PASSWORD}
    ports:
      - 8888:8888
    restart: unless-stopped
    depends_on:
      - influxdb

  grafana:
    image: grafana/otel-lgtm:latest
    ports:
      - 3000:3000
      - 4317:4317
      - 4318:4318
    restart: unless-stopped
