# V2CONV-B3 — built-in local-default TransformExecutor (identity transform).
# Plugins add their own entries (e.g. scene-graph play) via their own
# META-INF/services file on the classpath; ServiceLoader unions them.
de.dlr.shepard.spi.transform.NoOpTransformExecutor
# URSCRIPT-TRAJECTORY-1 — URScript (.urscript/.script) interpret as a MAPPING_RECIPE
# transform (in-tree). Resolves .urscript + URDF FileReferences, invokes the URScript
# sidecar, mints a derived joint-trajectory TimeseriesReference (REFERENCE result).
de.dlr.shepard.v2.transform.urscript.UrScriptTrajectoryTransformExecutor
