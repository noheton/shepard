/**
 * Reserved namespace for {@code /v2/...} JAX-RS resources. Per
 * {@code aidocs/47 §2} and {@code CLAUDE.md}'s API-version policy, every
 * resource in this package (or sub-packages) MUST have a {@code @Path}
 * value starting with {@code /v2/}. Fenced by
 * {@code de.dlr.shepard.architecture.V2NamespaceTest}.
 *
 * <p>Resources outside this package MUST NOT have a {@code @Path} value
 * starting with {@code /v2/}. Same fence.
 *
 * <p>No resource lives here yet — L2d ({@code aidocs/25 §4 Phase 4}) will
 * populate it.
 */
package de.dlr.shepard.v2;
