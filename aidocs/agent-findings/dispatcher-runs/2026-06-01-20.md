---
stage: deployed
last-stage-change: 2026-06-01
---

# Dispatcher run — 2026-06-01-20 UTC

**Fire time:** 2026-06-01 20:19 UTC  
**Recent commits (last 2h):** 2 (b2180fe — fire 19 log, 8b48329 — fire 18 log)  
**Open PRs at fire time:** 30 (mostly dependabot; feature PRs: #1701–1705)

## Rows considered

Scanned `aidocs/16-dispatcher-backlog.md` for `queued` items. Cross-referenced against open PRs and recent commits.

| Row ID | Description | Size | Decision |
|--------|-------------|------|----------|
| UX-WALK-2026-05-29-08 | Fix Access column null → chip on `/collections` list | XS/S | **DISPATCHED** |
| UX-WALK-2026-05-29-07 | Add Trace3D CTA button on TS container page | S | **DISPATCHED** |
| PERF13 | Deduplicate collection-metadata fetch between page and sidebar | S | **SKIPPED** — gated on PERF11 (PERF11 still queued; the dedup relies on `useAsyncData` key consolidation that PERF11 provides) |
| SCENEGRAPH-PERMS-1 | Per-scene permission anchor for `:DigitalTwinScene` | M | **SKIPPED** — touches auth/permissions surface (CLAUDE.md constraint) |
| RDM-002 | ORCID input on `/me` Profile card + ORCID badge | S | **SKIPPED** — already done (task #195) |
| RDM-005a(d) | Server-side `GET /v2/collections/{appId}/completeness` | S–M | **SKIPPED** — requires porting completeness scoring logic from TypeScript to Java; S-M effort, not clearly XS/S |
| CP1c | Wire `webdavVisible` into MNT1 WebDAV mount path | S | **SKIPPED** — gated on MNT1 (status unclear without verifying MNT1 shipped) |
| TERM3 | Frontend autocomplete for glossary-aware annotation entry | S | **SKIPPED** — gated on TERM2 glossary REST endpoint (TERM2 status not verified as shipped) |
| L2e | Drop legacy `/v1/` long-id paths | M | **SKIPPED** — explicitly BREAKING |
| P6 | Fold `X-API-KEY` into `Authorization: Bearer` | S | **SKIPPED** — explicitly BREAKING |

## Dispatched (PRs opened)

| Task | Description | PR |
|------|-------------|-----|
| UX-WALK-2026-05-29-08 | Extend `AccessRightsChip` to accept null/undefined; render "Not set" chip instead of bare `—` on `/collections` Access column | [#1706](https://github.com/noheton/shepard/pull/1706) |
| UX-WALK-2026-05-29-07 | Add "Visualize in 3D" button (with `ViewRecipeBuilderDialog`) to TS container page `#buttons` slot; shows when ≥3 channels | [#1707](https://github.com/noheton/shepard/pull/1707) |

## Failures

None.

## Notes

- UX-WALK-08 agent chose Option B (extend `AccessRightsChip` to handle null internally) over Option A (separate inline chip). 8 new Vitest tests added. All 3 frontend gates passed.
- UX-WALK-07 agent found the remote branch already had a partial implementation (`canVisualize3D` computed, minimal dialog). Merged both: kept ≥3-channel threshold from remote + added `channelsV2` fetch + `start-ns`/`end-ns` props for full annotation-save parity. All 3 frontend gates passed (5 pre-existing test failures, no new ones).
- PERF13 re-evaluated: the `useFetchCollection` composable uses plain `fetch()` (not `useAsyncData`), so key-based dedup requires PERF11 first. Deferring until PERF11 is dispatched.
