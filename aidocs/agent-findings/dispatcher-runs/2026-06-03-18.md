---
stage: deployed
last-stage-change: 2026-06-03
---

# Dispatcher run — 2026-06-03-18

**Fire time:** 2026-06-03 18:16 UTC  
**Trigger:** trig_01RSpqFMhs4wj5SxF3KTa9UV (cron `0 * * * *`)  
**Recent commits (last 2 h):** 5 (doc-stage regen, v2 surface convergence SSOT, 17-fire log update + 2 from prior fire PRs)  
**Open PRs:** ~50

## Rows considered

Scanned all `queued` rows in `aidocs/16-dispatcher-backlog.md`. Approximately 50+ distinct queued items evaluated. Special attention paid to the new **V2CONV** rows added by commit `63a2394` (aidocs/platform/191 v2 surface convergence design).

## Rows dispatched (3)

| Row ID | Description | Branch | PR |
|---|---|---|---|
| V2CONV-B2-INSTANTIATION-VALIDATE | Wire SHACL shape validation into `TemplateInstantiationRest` after body flatten; 422 on violation | `V2CONV-B2-INSTANTIATION-VALIDATE-shape-validation` | [#1780](https://github.com/noheton/shepard/pull/1780) |
| MFFD-IMAGEBUNDLE-PANE-MOUNT-1 | Mount `ImageBundleViewer.vue` on DataObject detail page; detect `.png/.jpg/.tif` FileBundleRefs | `MFFD-IMAGEBUNDLE-PANE-MOUNT-1-image-bundle-pane` | [#1779](https://github.com/noheton/shepard/pull/1779) |
| UI7 | PayloadVersion history panel on FileReference detail page; per-version download links | `UI7-payload-version-history-panel` | [#1778](https://github.com/noheton/shepard/pull/1778) |

### UI7 (#1778) — completed

- New `PayloadVersionHistoryPanel.vue`: Vuetify expansion panel (collapsed by default), lazy-loads on first expand via `GET /v2/file-containers/{containerAppId}/files/{fileName}/versions`. Columns: version # chip, upload timestamp, human-readable size, SHA-256 (12 chars + full-hash tooltip), uploader, per-row download button. `data-testid="payload-version-history-panel"`.
- Added `fileContainerAppId` ref to `useFetchFileReference.ts`; `referencedContainerAppId` to `dataReference.ts`
- FileReference detail page mounts the panel after the files DataTable, guarded by `fileContainerAppId && primaryFileName`
- 17 Vitest tests; lint CLEAN; typecheck CLEAN

### MFFD-IMAGEBUNDLE-PANE-MOUNT-1 (#1779) — completed

- New `DataObjectImageBundlePane.vue`: detects FileBundleReferences with image extensions (`.png/.jpg/.jpeg/.tif/.tiff`), renders `ImageBundleViewer.vue` with `bundleAppId`, `groupAppId`, `containerMongoId` props; hidden via `v-if` when no image bundles; follows `DataObjectThermographyPane.vue` pattern
- Mounted as `ExpansionPanelItem` on DataObject detail page
- `data-testid="image-bundle-pane"`; 35 Vitest tests; lint CLEAN; typecheck CLEAN

### V2CONV-B2-INSTANTIATION-VALIDATE (#1780) — completed

- `TemplateInstantiationRest.java` injects `JenaShaclValidator`; after `inheritanceResolver.flattenBody(template)` and before DataObject creation, calls `extractShapeGraph()` on the collection's allowed shapes and `buildDataTurtle()` on the candidate attributes, then validates
- Returns 422 Unprocessable Entity with the violation report when `!report.conforms()`
- 21 targeted `TemplateInstantiationRestTest` tests pass (11 per the guidance agent); pre-existing `mvn verify` failures in unrelated test suites (Mockito argument-matcher issues) predate this branch

## Rows skipped (reasons)

| Row ID | Reason |
|---|---|
| A2 | L-size; JAX-RS decomposition affects many files — too large for autonomous dispatch |
| P2b | M-size; TimescaleDB continuous aggregates — schema infra decision needed |
| P3b | Image source lives outside this repo |
| L2e | Touches deprecated-path sunset; intersects A4c cache; BREAKING path |
| L4 | M-size; large frontend effort intersecting multiple design docs |
| L7 | L-size; too large for autonomous dispatch |
| A5e | Gated on shared-Keycloak realm config (operator-side dependency) |
| PM1b3 | M-L; complex Quarkus build-time CDI constraint; architectural decision pending |
| P5/F1 | Touches auth/permissions — excluded per dispatcher hard constraints |
| P6 | Breaking change (header rename) |
| F2 | L-size; major feature (Group/sharing model) |
| P12 | Already tracked as FS1c (shipped) or handled by presigned path |
| ID-MIG1/2/3 | Excluded per CLAUDE.md — appId→shepardId rename arc reserved for coordinated pass |
| PRE-MUT-SNAP4 | Implicitly gated on PRE-MUT-SNAP1/2/3 (substrate-native restore primitives not yet shipped) |
| V2CONV-A2/A3 | L-size; large surface refactor deleting existing endpoints |
| V2CONV-A4 | M-size; requires design review of ConfigDescriptor SPI before collapsing bespoke configs |
| V2CONV-A6 | L-size; plugin extractions require full plugin build + test pipeline per item |
| V2CONV-B4/B5 | Gated on B3 (MAPPING_RECIPE kind not yet shipped) |
| V2CONV-B6/B7 | Gated on B1 (SHACL shape-builder not yet shipped) |
| MFFD-VIDEOREF-HLS-1 | L-size; ffmpeg sidecar + temp-file management deliverable |
| MFFD-IMAGEBUNDLE-COMPARE-1 | Gated on Collection-level compare-by-attribute picker |
| DB-AP1/AP2 | L/M; multi-agent fan-out design needed first |
| BATCH-API-1/2 | M/L; design spec work needed before implementation |
| TSDB-DDL-2 | M; TimescaleDB chunk-drop path; touches production delete path — needs careful review |

## Note on git state

The local main branch has diverged from origin/main (unrelated histories in this container session). The dispatcher log was pushed directly to origin/main via GitHub API. The MFFD-IMAGEBUNDLE uncommitted changes were in the main working tree (the branch was created there during agent setup); committed and pushed directly before session end per stop-hook requirement.
