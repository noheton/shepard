---
stage: deployed
last-stage-change: 2026-05-31
fire: 2026-05-31-14
---

# Dispatcher fire 2026-05-31-14

**Fired:** 2026-05-31 ~14:xx UTC  
**Backlog SSOT:** `aidocs/16-dispatcher-backlog.md`  
**Open PRs at fire:** 30  
**Recent fires:** fire-04 dispatched PERF11 + UI21-BACKEND-Q; fire-03 dispatched GH-PM2

## Scan summary

- Read `aidocs/16-dispatcher-backlog.md` end-to-end (3053+ lines, read in 15 chunks).
- Catalogued all 30 open PR branches to build in-flight exclusion list.
- Identified and discarded stale backlog entries before dispatching (see below).

## Stale entries identified (skipped, not dispatched)

| Row | Reason stale |
|---|---|
| AI1c-UI | QualityScoreChip already wired in `ShowTimeseriesReferenceDialog.vue` + `DataObjectDataMetaCell.vue` |
| J1d-UI | `LabJournalHistoryDialog.vue` + `useFetchLabJournalHistory.ts` already exist and wired in `LabJournalExistingEntry.vue` |
| J1e-PR-08 | Already in-flight as PR #1652 |
| J1e-PR-09 | Already in-flight as PR #1653 |
| CHOKE-03, CHOKE-06 | Already done 2026-05-29 |
| FRONTEND-LINT-DEBT-04 | Only 1 bare `@ts-expect-error` instance found (`useCollectionEvents.test.ts:196`); not worth a dedicated dispatch |

## In-flight exclusions (sampled)

`perf11-ssr-payload-preloading`, `ui21-backend-q-created-by-filter`, `gh-pm2-traceability-pages-workflow`, `mcp-cov-05-semantic-bulk-concurrent`, `mcp-cov-11-prov-query-activity-list`, `scenegraph-create-from-urdf-2-fe-wire-button`, `vis-s1b-signed-url-minter`, `tsdb-ddl-2-chunk-aware-delete`, and 22 others.

## Candidates evaluated

| Row | Size | Ruling |
|---|---|---|
| AI1c-UI | XS | **Skip** — stale, already implemented |
| J1d-UI | XS | **Skip** — stale, already implemented |
| J1e-PR-08/09 | S | **Skip** — already in-flight (PR #1652, #1653) |
| FRONTEND-LINT-DEBT-04 | XS | **Skip** — only 1 instance; not worth full PR |
| **UX-WALK-2026-05-29-08** | S | **Dispatch** — frontend-only, single component change, clear criterion |
| **UX-WALK-2026-05-29-07** | S | **Dispatch** — frontend-only, adds CTA button, no API change |
| **BTKVS-A2-OPS** | XS | **Dispatch** — infra YAML only, operator-approved, unblocks BTKVS showcase |

## Dispatched (3 agents, parallel)

### UX-WALK-2026-05-29-08 — Collections list: "Not specified" chip for null accessRights
- **Branch:** `ux-walk-2026-05-29-08-access-not-specified-chip`
- **Acceptance:** When `accessRights` is null in the collections list, render a `v-chip` (size=small, variant=tonal, default colour) reading "Not specified" instead of bare `—`.
- **File:** `frontend/components/context/collection/list/CollectionList.vue`
- **Risk:** Low — single template slot change, no logic, no API touch
- **PR:** pending

### UX-WALK-2026-05-29-07 — TS container page: add "Visualize in 3D" CTA button
- **Branch:** `ux-walk-2026-05-29-07-trace3d-cta`
- **Acceptance:** TS container detail page has a "Visualize in 3D" button navigating to `/shapes/render?renderer=trace-3d&containerId=<numericId>`.
- **File:** `frontend/pages/containers/timeseries/[containerId]/index.vue`
- **Risk:** Low — adds a nav button, no logic, no API change
- **PR:** pending

### BTKVS-A2-OPS — Add writable bind-mount for semantic user-bundles dir
- **Branch:** `btkvs-a2-ops-semantic-bundles-mount`
- **Acceptance:** `infrastructure/docker-compose.yml` backend service gets `- /opt/shepard/semantic-bundles:/var/lib/shepard/ontologies` in its volumes block.
- **File:** `infrastructure/docker-compose.yml`
- **Risk:** XS — additive YAML volume entry, no code change
- **PR:** pending

## Not dispatched this fire

- **GH-PM3** (M, queued) — explicitly "After GH-PM2"; GH-PM2 still in-flight
- **PERF12** (M, queued) — held for same review window as PERF11
- **L4** (M, queued) — search-as-you-type ontology tree; needs design review first
- **BTKVS-A2-CONTENT** (S, queued) — depends on BTKVS-A2-OPS landing first

## Status

Sub-agents running. Will update with PR URLs when complete.
