---
stage: deployed
fire: 2026-06-02-21
fired-at: "2026-06-02T21:26Z"
dispatcher: claude-sonnet-4-6
---

# Dispatcher fire 2026-06-02-21

**Fired:** 2026-06-02 21:26 UTC  
**Session:** continuation of 2026-06-02-20 (context compaction occurred between fires)

## Rows considered

| Row ID | Size | Decision | Reason |
|--------|------|----------|---------|
| SHAPES-V-PREFILL-2-RDF-ENDPOINT | M | ✅ dispatched | Backend `GET /v2/data-objects/{appId}/rdf`; frontend already wired in `validate.vue`; clear scope |
| COLL-TIMELINE-ANNOTATE-1 | S | ✅ dispatched | Frontend-only: add Annotate button to `CollectionTimelinePane.vue` using `AnnotationDialog` v2-mode |
| MCP-COV-05-SEMANTIC-BULK-CONCURRENT | S | ⏭ skipped | CDI request scope doesn't propagate across virtual threads; `contextBridge.bind()` is thread-local; concurrent fan-out would corrupt principal isolation |
| TEMPLATE-ICONS-2-FE-RENDER-POINTS-EXPAND | S | ⏭ skipped | Gated on `DataObjectListItemV2IO.attachedTemplateAppId` absent from list response; backend prerequisite not yet landed |
| PLACEHOLDER-REPLACE-P10c | XS | ⏭ already done | `AdminSqlTimeseriesPane.vue` already mounted at `sql-timeseries` slug; backlog stale |
| KRL-INTEGRATION-MFFD-REAL-01-ENCODING-LATIN1 | S | ⏭ already done | Latin-1/CP1252 fallback already in `plugins/krl-interpreter/krl_interpreter/sidecar/schemas.py` line 106 |
| PLACEHOLDER-REPLACE-TS-SEMANTIC-REST | XS | ⏭ already done | `ChannelAnnotationsPane.vue` already replaces this placeholder |
| MFFD-WIKI-TO-* | M | ⏭ blocked | Needs HTML files at `/mnt/pve/unas/dump/dataset/wiki/MFFD/*.html` — not in repo checkout |

## Dispatched

### 1. SHAPES-V-PREFILL-2-RDF-ENDPOINT → PR #1750
- **Branch:** `SHAPES-V-PREFILL-2-RDF-ENDPOINT-rdf-endpoint`
- **PR:** https://github.com/noheton/shepard/pull/1750
- **What:** New `DataObjectRdfRest.java` at `GET /v2/data-objects/{appId}/rdf` → `text/turtle`. Calls `SemanticAnnotationV2DAO.findBySubjectAppId(appId)` (method added to v2 DAO). Turtle serialisation replicates `buildTurtle()` helpers from `SemanticAnnotationV2Rest` as package-private static helpers for independent testability. Auth mirrors `isAccessAllowedForDataObjectAppId` pattern. 8 unit tests (200 with annotations, 200 empty, 404, 401, 403, multi-annotation, IRI value, Content-Type).
- **Frontend note:** `validate.vue` already calls this endpoint when `?focusAppId=` is present; no frontend change needed.

### 2. COLL-TIMELINE-ANNOTATE-1 → PR #1749
- **Branch:** `COLL-TIMELINE-ANNOTATE-1-annotate-bin`
- **PR:** https://github.com/noheton/shepard/pull/1749
- **What:** Creates `frontend/components/context/collection/CollectionTimelinePane.vue` with Annotate button in the timeline-header toolbar. Uses `AnnotationDialog` v2 (appId) mode with `subjectKind="Collection"`. One dialog instance outside the `v-for` swimlane loop.
- **Path note:** The component was created at `collection/` (per remote `main`'s org structure) rather than the old top-level path (which exists only in the ~57-commits-ahead local HEAD). A PR comment (#4607216437) explains the mounting gap: the collection page (`index.vue`) on remote `main` doesn't yet mount `CollectionTimelinePane`. Options for reviewer: extend PR to add the mount point, or merge and reconcile when the local-head commits land.
- **Validation:** `npm run lint` (0 new errors), `npm run typecheck` (clean), `npm run test` (1053 passed, 5 pre-existing failures unchanged).

## Stale backlog notes (for next cleanup pass)

- Lines 3100+ contain duplicate queued rows already implemented
- `PLACEHOLDER-REPLACE-P10c`, `KRL-INTEGRATION-MFFD-REAL-01-ENCODING-LATIN1`, `PLACEHOLDER-REPLACE-TS-SEMANTIC-REST` should be marked `done`
- Previous fire (2026-06-02-20) dispatched: TEMPLATE-ICONS-1 (#1748), BE-USERS-ME-ROLES-1 (#1720), TPL-ME-USE-FROM-BROWSE (#1719)
