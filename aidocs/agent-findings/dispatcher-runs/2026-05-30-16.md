# Dispatcher run — 2026-05-30-16

**Fire time:** 2026-05-30 ~16:10 UTC  
**Considered:** 4 rows  
**Dispatched:** 2  
**Noted done (status update only):** 2  
**Skipped:** 0  
**Recent commits:** 8 (PRs #1626/#1627/#1628 merged; MFFD IOT1 + scenegraph bootstrap + KRL Latin-1 fix)

## Rows dispatched

| Row ID | Title | Branch | PR |
|--------|-------|--------|----|
| KRL-INTEGRATION-MFFD-REAL-03-EKRL-CHANNEL | Surface `;EKRL:` comments as structured `unsupportedConstructs` | `krl-integration-mffd-real-03-ekrl-channel` | [#1629](https://github.com/noheton/shepard/pull/1629) |
| KRL-COMPARE-01-DESIGN | KRL nominal vs actual trajectory comparison design doc | `krl-compare-01-design` | [#1630](https://github.com/noheton/shepard/pull/1630) |

## Rows noted done (already implemented, backlog updated)

| Row ID | Status | Evidence |
|--------|--------|----------|
| KRL-INTEGRATION-MFFD-REAL-01-ENCODING-LATIN1 | **done** | Codec-cascade (`utf-8 → iso-8859-1 → cp1252 → errors=replace`) already in `schemas.py _normalise_src_and_urdf` (commit `71e0f2dc`). 4 regression tests added in PR #1629. |
| KRL-INTEGRATION-MFFD-REAL-02-IF-COMPOUND-BOOLEAN | **done** | Grammar already handles `(cond) OR (cond) THEN` via `atom → LPAREN expr RPAREN`. Verified 2026-05-30; no grammar changes needed. |

## Selection rationale

- **KRL-INTEGRATION-MFFD-REAL-03-EKRL-CHANNEL** (S): The EKRL pre-scan is a pure Python
  addition to `parse()` with zero grammar changes and zero schema impact. It directly
  addresses the MFFD real-file integration gap (5 of 7 AFP callback hooks use `;EKRL:`).
  Implementation: regex pre-scan before ANTLR tokenisation; ANTLR silently drops `;` lines
  as `LINE_COMMENT`, so no grammar modification needed.

- **KRL-COMPARE-01-DESIGN** (S): Design-only PR — no code, no schema, no migration.
  Unblocks the KRL-COMPARE-01 through -09 implementation arc which has direct value for
  MFFD AFP QA (replacing 30–60 min manual Python loop with a 90 s traceable comparison).

## PR summaries

### PR #1629 — KRL-INTEGRATION-MFFD-REAL-03-EKRL-CHANNEL
- **`plugins/krl-interpreter/krl_interpreter/parser/__init__.py`**: `_scan_ekrl()` + `_EKRL_RE` regex; called in `parse()` before ANTLR, results merged into `ParseResult.unsupported`.
- **`tests/test_parser_flow.py`**: 3 EKRL tests (single line detected, two lines both detected, absent produces no false positive).
- **`tests/test_sidecar_schemas.py`**: 4 encoding-cascade tests (UTF-8, Latin-1 `ü`, CP1252 smart-quote, truly-invalid bytes → replace mode).
- **`aidocs/16`**: Rows -01/-02/-03 updated to `done (2026-05-30)`.
- **Gate:** 95 tests pass.

### PR #1630 — KRL-COMPARE-01-DESIGN
- **`aidocs/integrations/118-krl-comparison.md`**: Full 14-section design doc (`stage: fragment`). Covers: purpose, scope, architecture, `AlignmentStrategy` SPI (xcorr/manual/event-marker; DTW fits as 1-class addition), FK module (`krl_interpreter/fk/fk_chain.py`), `POST /v2/krl/compare` REST API, `:KrlReplayComparison` Activity entity, sidecar `POST /compare` extension, frontend compare dialog + dual-trail Trace3D, MCP tools, 9-row phasing table (KRL-COMPARE-01 through -09), 6 open questions.
- **`aidocs/16`**: KRL-COMPARE section added with all 9 sub-rows; KRL-COMPARE-01-DESIGN marked `in-flight`.
- **`aidocs/01`**: Stage index regenerated (395 docs).

## Context carried forward

- KRL-INTERPRETER-05-FOLLOWUP-SCENE: still blocked on KRL-CONFIG-1 (#1627) merging first.
- 30 open PRs total (heavy queue; dispatcher will skip already-in-flight items on next fire).
- KRL-COMPARE implementation arc (KRL-COMPARE-02 through -09): now unblocked; KRL-COMPARE-02-FK-MODULE is the natural next dispatch once #1630 merges.
