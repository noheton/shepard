---
fire: 2026-06-02-00
date: 2026-06-02
hour: "00"
status: dispatched
rows_dispatched: 3
tracker_issue: 1576
---

# Dispatcher fire 2026-06-02-00

## Fire summary

| Field | Value |
|---|---|
| Fire time | 2026-06-02 ~00:00 UTC |
| Backlog read | `aidocs/16-dispatcher-backlog.md` (3126 lines, 7 chunks) |
| Git window | `--since='2 hours ago'` → 4 commits (2× log updates + 2× run logs) |
| Open PRs scanned | 50 |
| Candidates evaluated | ~12 |
| Rows dispatched | 3 |

## Recent commits (last 2 hours)

- `b6377f4` chore(dispatcher): update 2026-06-01-23 log — all 3 PRs landed (#1716, #1717, #1718)
- `e26c42f` docs(UI-PATHS-01-AUDIT): audit frontend for free-form URL/path inputs
- `962e446` chore(dispatcher): log fire 2026-06-01-23 — FRONTEND-LINT-DEBT-04, SINGLETON-FILE-02-SEED-MIGRATION, UI-PATHS-01-AUDIT
- `ace2702` chore(dispatcher): log fire 2026-06-01-22 — DT1-DAO-FRESH-SESSION #1715, SCENEGRAPH-LIST-1 FE #1714, DOC-STAGE3 #1713

## In-flight rows (open PRs, excluded from dispatch)

SINGLETON-FILE-02-SEED-MIGRATION (#1717), FRONTEND-LINT-DEBT-04 (#1716), DT1-DAO-FRESH-SESSION (#1715), SCENEGRAPH-LIST-1 FE (#1714), DOC-STAGE3 (#1713), PLACEHOLDER-REPLACE-P10c (#1710), SNAPSHOTS-DIFF-NAV-01 (#1709), J1e-PR-06 (#1708), UX-WALK-2026-05-29-07 (#1707), UX-WALK-2026-05-29-08 (#1706), PROV-CAPTURE-READS-FLIP (#1705), DB-BP1 (#1704), J1e-PR-08/J1e-PR-09 (#1703), SCENEGRAPH-CREATE-FROM-URDF-2-FE (#1702), AI1c-UI/J1d-UI (#1701)

## Eliminated candidates

| Row ID | Reason eliminated |
|---|---|
| SINGLETON-FILE-01-AUDIT | Requires live Neo4j DB access for the audit query |
| SCENEGRAPH-NAV-02 | Gated on SCENEGRAPH-CREATE-FROM-URDF-2 (in-flight #1702) |
| J1e-PR-07 | Gated on J1e-PR-06 (in-flight #1708) |
| AI1c-TESTS | Gated on AI1c-UI (in-flight #1701) |
| TPL-TEMPLATE-CREATE-1 | Touches template creation schema — medium-risk, needs design review |
| UI-PATHS-FROM-REFERENCES | Large refactor, multiple substrates, explicitly high-risk |

## Dispatched rows

### 1. BE-USERS-ME-ROLES-1 — `effectiveRoles[]` in UserIO

| Field | Value |
|---|---|
| Row size | S |
| Risk | Low |
| Branch | `BE-USERS-ME-ROLES-1-effective-roles` |
| Scope | Backend only — additive nullable field on UserIO |
| Rationale | Pure additive response-field addition. Fixes ROLE-GRANT-STALE-SESSION where JWT-only role checks miss post-login Neo4j `:HAS_ROLE` grants. One field + one test. |
| Gate | `mvn verify -pl backend` |

**Acceptance criterion:** `GET /shepard/api/users/me` returns `effectiveRoles: ["instance-admin", ...]` populated from live Neo4j role resolution, not the JWT claim alone.

### 2. KRL-CONFIG-1 — `:KrlInterpreterConfig` admin singleton

| Field | Value |
|---|---|
| Row size | S |
| Risk | Low |
| Branch | `KRL-CONFIG-1-krl-admin-config` |
| Scope | Backend — mirrors JupyterConfig/UnhideConfig pattern exactly |
| Rationale | Mechanical clone of an established pattern (A3b/UH1a/N1c2). New Neo4j entity + migration + `GET/PATCH /v2/admin/krl/config` + CLI parity. No schema mutations beyond a new singleton. Enables operators to flip KRL sidecar URL/timeout at runtime. |
| Gate | `mvn verify -pl backend` |

**Acceptance criterion:** `GET /v2/admin/krl/config` returns `{sidecarUrl, timeoutSeconds, maxBodySizeMb, enabled}`; `PATCH /v2/admin/krl/config` updates at runtime without restart.

### 3. TPL-ME-USE-FROM-BROWSE — "Use here…" in `/me#templates`

| Field | Value |
|---|---|
| Row size | S |
| Risk | Low |
| Branch | `TPL-ME-USE-FROM-BROWSE-collection-picker` |
| Scope | Frontend only — `/me` templates Details dialog |
| Rationale | Pure frontend. Backend endpoint already exists (`POST /v2/collections/{collectionAppId}/templates/{templateAppId}/data-object`). Adds Collection autocomplete picker + one API call. Uses `useWatchedCollections` + permission filter pattern already in the codebase. |
| Gate | `npm run lint && npm run test && npm run typecheck` (in `frontend/`) |

**Acceptance criterion:** In `/me#templates`, opening the Details dialog for a template shows a "Use here…" button; clicking it prompts for a (write-accessible) Collection; on confirm, a DataObject is created and the user gets confirmation.

## Sub-agent status

| Row | Branch | Agent | Status at log time |
|---|---|---|---|
| BE-USERS-ME-ROLES-1 | `BE-USERS-ME-ROLES-1-effective-roles` | a1f4ad0ac7494b542 | in-progress |
| KRL-CONFIG-1 | `KRL-CONFIG-1-krl-admin-config` | a6d10b7a9c7e93919 | in-progress |
| TPL-ME-USE-FROM-BROWSE | `TPL-ME-USE-FROM-BROWSE-collection-picker` | a9c81887be99f3755 | in-progress |

## Notes

- Previous fire (2026-06-01-23) dispatched: FRONTEND-LINT-DEBT-04 (#1716), SINGLETON-FILE-02-SEED-MIGRATION (#1717), UI-PATHS-01-AUDIT. All 3 landed per the log update commit.
- Candidate pool is healthy — queue depth remains deep. Next fire should find ready candidates quickly.
- KRL-CONFIG-1 was spawned 2026-05-30 by KRL-INTERPRETER-05-REST and has been queued since; this fire closes it.
