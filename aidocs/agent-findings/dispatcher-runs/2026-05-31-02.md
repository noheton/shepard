---
stage: deployed
last-stage-change: 2026-05-31
---

# Dispatcher run 2026-05-31-02

**Fire time:** 2026-05-31 ~02:xx UTC  
**Trigger:** hourly cron `0 * * * *`

## Inventory

- Backlog SSOT: `aidocs/16-dispatcher-backlog.md`
- Recent commits (last 2h): 121 commits pulled from origin/main at fire time (local main was stale; fast-forwarded)
- Open PRs: ~35 dispatcher-opened items (above informal 30-item budget)

## Rows considered

| ID | Description | Decision | Reason |
|---|---|---|---|
| MCP-COV-05-SEMANTIC-BULK-CONCURRENT | Add Semaphore(10) fan-out to `semanticAnnotateBulk` in AnnotationMcpTools | **DISPATCHED** | S, backend-only, no interface change, v0 sequential commit already on branch |
| MCP-COV-11-PROV | Add `prov_query` + `activity_list` MCP tools (ActivityDAO-backed) | **DISPATCHED** | S, backend-additive, new read-only tools, no schema mutation |
| KRL-INTEGRATION-MFFD-REAL-02-IF-COMPOUND-BOOLEAN | Compound parenthesized boolean in KRL IF statements | SKIPPED | 3rd consecutive skip — grammar inspection across fires suggests `atom → LPAREN expr RPAREN` may already be present in `KrlParser.g4`; blocked on manual verification |
| UX-WALK-2026-05-29-08 | Access column `—` → controlled vocabulary chip | SKIPPED | PR #1580 already in-flight (contrary to -01 log; confirmed by branch scan) |

## Dispatched

| ID | PR | Status |
|---|---|---|
| MCP-COV-05-SEMANTIC-BULK-CONCURRENT | [#1655](https://github.com/noheton/shepard/pull/1655) | ✓ open |
| MCP-COV-11-PROV | [#1654](https://github.com/noheton/shepard/pull/1654) | ✓ open |

### MCP-COV-05-SEMANTIC-BULK-CONCURRENT details (PR #1655)

- `AnnotationMcpTools.java`: replaced sequential `for` loop with `CompletableFuture[]` fan-out using `Thread.ofVirtual()` as executor, gated by `Semaphore(SEMANTIC_ANNOTATE_BULK_CONCURRENCY)` (= 10)
- `processAnnotationRow` helper called inside each virtual thread; result ordering preserved by joining `futures[i]` in input-index sequence
- Added `SEMANTIC_ANNOTATE_BULK_CONCURRENCY = 10` constant; removed `TODO` note from tool description
- `AnnotationMcpToolsTest`: 5 new tests including `semanticAnnotateBulkConcurrentPathAllProcessedInOrder` (25 rows > concurrency limit); 23/23 tests pass
- Note: pre-existing test failures in other suites (78 failures, 44 errors) unrelated to this change

### MCP-COV-11-PROV details (PR #1654)

- New `ActivityMcpTools.java`: `@ApplicationScoped` MCP tool class with `prov_query` and `activity_list` tools
- `ActivityDAO.java`: added `findByEntityAppId(String appId, int depth)`, `listForMcp(String userId, String resourcePath, String httpMethod, int limit)`, and `ActivityEdgeRow` record
- `prov_query`: queries `MATCH (a:Activity)-[r:GENERATED|USED]->(e {appId: $appId})` up to depth*50 result cap; returns activities with edge relation label
- `activity_list`: filters by `userId`, `resourcePath` (STARTS WITH), `httpMethod`; limit capped at 100; newest first
- `ActivityMcpToolsTest`: 11 tests, all pass
- Note: same pre-existing suite failures as above; unrelated to this change

## Notes

- PR count is at/above the 30-item dispatcher budget; next fire should prefer XS/docs-only items or wait for merges
- KRL-02-IF-COMPOUND-BOOLEAN has been skipped 3 consecutive fires — needs a human to open `KrlParser.g4` and confirm whether `atom → LPAREN expr RPAREN` is present before any further dispatch attempt; if present, mark the row `done (verified-stale)`
- Local main was 121 commits behind origin at fire start; fast-forwarded before writing this log
- Sub-agent for MCP-COV-11 left a stale local branch pointing to origin/main HEAD — cleaned up during fire
