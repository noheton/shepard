---
stage: deployed
last-stage-change: 2026-05-29
---

# Dispatcher run 2026-05-29-22

**Fire time:** 2026-05-29 ~22:xx UTC  
**Trigger:** trig_01RSpqFMhs4wj5SxF3KTa9UV (hourly cron `0 * * * *`)

## Inventory

- Backlog SSOT: `aidocs/16-dispatcher-backlog.md` (2735 lines)
- Recent commits (last 2h): 9 — including SCENEGRAPH-REST-1 merge (#1601 `7853b1e`), DT1-DAO-FRESH-SESSION + FRONTEND-LINT-DEBT-02 dispatch log updates from previous fire
- Open PRs: 30 items reviewed

## Rows considered

| ID | Description | Decision | Reason |
|---|---|---|---|
| PERF4d | Nightly CI job `perf-endpoints.yml` — boots compose stack, runs `k6-endpoints.js` steady + ramp, uploads artifact, files issue on breach | **DISPATCHED** | S, not gated (PERF4a shipped, design at aidocs/77 §9.2), pure YAML addition, not in-flight |
| D1c (partial) | Task-shaped help pages — `share-collection.md`, `export-rocrate.md`, `process-step.md` (upload-data.md already exists) | **DISPATCHED** | S, not gated (D1a shipped), pure markdown, not in-flight |
| J1e-PR-07 | aidocs/34 row + wire-compat REST shim for J1e plugin relocation | SKIPPED | Codebase state unclear — carried from previous fire, no new information |
| PERF13 | Deduplicate collection-metadata fetch between page and sidebar | SKIPPED | Gated on PERF11 (open PR #1586) |
| BTKVS-D1 | Nominate shepard-plugin-btkvs-docket in ecosystem partner list | SKIPPED | `aidocs/strategy/40-ecosystem.md` does not exist |
| D1b | Playwright spec + screenshot pipeline | SKIPPED | Gated on a CI-booted stack — M-sized, too large for this fire |
| Any auth/permissions rows | Various | SKIPPED | Standing exclusion — never touch auth/permissions |

## Dispatched

| ID | PR | Status |
|---|---|---|
| PERF4d | #1603 | ✓ open |
| D1c (partial) | #1604 | ✓ open |

### PERF4d details (PR #1603)
- Created `.github/workflows/perf-endpoints.yml` from canonical YAML in `aidocs/ops/77 §9.2`
- Cron `37 2 * * *` daily, `workflow_dispatch` with `post_issue` boolean input
- Steps: checkout → compose up → wait backend → seed fixtures → k6 via `docker run grafana/k6 --network host` → upload artifacts → dump logs on failure → file issue via `github-script@v7`
- **⚠️ Runtime note:** `scripts/perf/k6-endpoints.js` does not yet exist (PERF4a still `📐 designed`); the workflow file is valid and can merge, but will fail at runtime until PERF4a ships the script

### D1c details (PR #1604)
- `share-collection.md` — roles (Reader/Writer/Manager/Owner), UI flow to add collaborators, visibility modes, REST snippet for `PUT /shepard/api/collections/{id}/permissions`; grounded in `Roles.java`, `PermissionsIO.java`, `PermissionType.java`
- `export-rocrate.md` — three download paths (UI button, `POST /v2/collections/{appId}/export-url` presigned URL, legacy streaming GET), `ExportSelection` body, ZIP layout including `ro-crate-metadata.json`, redaction options; feature fully shipped (`CollectionExportUrlRest.java`)
- `process-step.md` — creating DataObjects, Predecessor/Successor linking, containers, annotation, rework loop modelling without destroying lineage, lineage graph navigation; REST snippet using `/v2/collections/{collectionAppId}/data-objects` with `predecessorIds`
- All pages: Jekyll frontmatter with `stage: deployed`, casual-task voice, REST snippets, cross-links

## Notes

- SCENEGRAPH-REST-1 confirmed merged this cycle (commit `7853b1e`) — the PR #1598 reference in the 21:xx log was superseded by #1601
- 30 open PRs → now 32 with these two; budget is at capacity — next fire should be XS-only or skip dispatch entirely
- Previous fire (2026-05-29-21) dispatched DT1-DAO-FRESH-SESSION (#1602) and FRONTEND-LINT-DEBT-02 (#1600) — both still open
- Dispatcher dispatch decision error: PERF4d was listed as "Blocked on PERF4a" in the backlog; the backlog description was correct — PERF4a has not shipped. The PR can still merge as a forward reference; the workflow will be inert until PERF4a lands.
