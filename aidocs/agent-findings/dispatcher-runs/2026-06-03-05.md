---
stage: deployed
fire: 2026-06-03-05
fired-at: "2026-06-03T05:18Z"
dispatcher: claude-sonnet-4-6
---

# Dispatcher run 2026-06-03-05

**Fired:** 2026-06-03 05:18 UTC  
**Recent commits (last 2 h):** 4 — `f8d1fad` (doc-stage regen 04-fire), `23d5303` (run log 2026-06-03-04), `c18916e` (doc-stage regen 02-fire), `5ecc0f2` (run log 2026-06-03-02)  
**Open PRs at fire time:** 30 (including DB-BP1 #1768, PRE-MUT-SNAP1 #1767, PRE-MUT-SNAP4 #1766, RDM-005a(d) #1765, UI21-SIZEBAR-DATA #1764, DT1-DAO-FRESH-SESSION #1763 from prior fires)

---

## Rows considered

Scanned full `aidocs/16-dispatcher-backlog.md` (415 queued rows identified). Filtered for low/medium risk per dispatch rules.

Key exclusions applied:
- Already in-flight (30 open PRs with row-ID branch names)
- Auth/security perimeter items (F1, F2, F8, BE-USERS-ME-ROLES-1, PERM-SYSTEM-REVIEW)
- BREAKING or multi-substrate schema mutations (L2e, P6, V2S1b cross-cutting)
- Large scope (L size: A2, L7, ENT1, P7)
- Gated on unshipped items (CP1c on MNT1a, A5e on Keycloak infra, PRE-MUT-SNAP5 on SNAP1-4)

Verified "already done" items (backlog stale):
- `AI1c-UI` — `qualityScore` badge already in `DataObjectDataMetaCell.vue` + timeseries detail page
- `PLACEHOLDER-REPLACE-P10c` — `AdminSqlTimeseriesPane.vue` already mounted in `/admin`
- `SCENEGRAPH-CREATE-FROM-URDF-2-FE` — `useScenegraphFromUrdf.createFromUrdf` wired in `OpenInSceneGraphButton.vue`
- `J1e-PR-08` + `J1e-PR-09` — J1e already marked as plugin (not core) in aidocs/44 + aidocs/42
- `SNAPSHOTS-DIFF-NAV-01` — `compareInSnapshotDiff` already in `SnapshotsPane.vue`

Candidates evaluated:

| ID | Size | Evaluation |
|---|---|---|
| MFFD-IMAGEBUNDLE-PANE-MOUNT-1 | S | ✅ dispatched — `ImageBundleViewer.vue` exists, clear mount pattern from thermography pane |
| MFFD-NDT-HEATMAP-MOUNT-TEST-1 | S | ✅ dispatched — test-only, 3 branches clearly specified |
| TPL-ME-USE-FROM-BROWSE | S | ✅ dispatched — backend exists, `instantiateDataObject` callable, clear UX spec |
| A2 | L | skip — large decompose task, many conflicts possible |
| P2b | M | skip — TimescaleDB aggregate schema change, needs DBA review |
| P3b | M | skip — external-repo dependency |
| L4 | M | skip — frontend+backend, intersects open search PRs |
| A5e | S–M | skip — gated on operator-side Keycloak realm config |
| DX5 | M | skip — Quarkus Dev Services plumbing |
| MFFD-IMAGEBUNDLE-COMPARE-1 | M | skip — gated on Collection compare-by-attribute picker |
| FAIR4-BRIDGE-ONTOLOGY | M | skip — M-sized ontology file requiring RDF domain knowledge |
| FAIR4-SHACL-COMPLIANCE | M | skip — M-sized, pairs with unshipped #127 SHACL changeover |
| BUG-PREDECESSOR-IDS-NUMERIC-IN-V2-PATCH | S | skip — touches DataObjectIO on backend; risk of conflict with 5 open DO-related PRs |

---

## Rows dispatched (3)

| ID | Description | PR |
|---|---|---|
| MFFD-IMAGEBUNDLE-PANE-MOUNT-1 | Mount `ImageBundleViewer.vue` on DataObject detail page via new `DataObjectImageBundlePane.vue` wrapper; 16 Vitest cases; `isImageBundleRef` helper excludes `.tif`/thermo/ndt | #1770 |
| MFFD-NDT-HEATMAP-MOUNT-TEST-1 | 26 Vitest cases covering 404 / success / re-analyze branches as pure async logic tests (no `@vue/test-utils` mount — project uses `environment: "node"`) | #1769 |
| TPL-ME-USE-FROM-BROWSE | "Use in Collection…" button + `useCollectionSearch`-backed autocomplete in `MyTemplatesPane.vue` details dialog; 10 logic-unit tests | #1719 |

---

## Notes

- DISPATCHER-REGEN-INDEX-1 (XS, queued): This fire intentionally runs `python3 scripts/regenerate-doc-stage-index.py` after committing this log, addressing the three-fire pattern of doc-stage-index drift that necessitated manual catch-up commits. Pattern resolved in-routine without a separate PR.
- 30 open PRs is at the high end — risk of merge conflicts means conservative selection of additive, isolated frontend tasks this fire.
