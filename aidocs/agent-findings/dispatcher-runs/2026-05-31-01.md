---
stage: deployed
last-stage-change: 2026-05-31
---

# Dispatcher run 2026-05-31-01

**Fire time:** 2026-05-31 ~01:xx UTC  
**Trigger:** hourly cron `0 * * * *`

## Inventory

- Backlog SSOT: `aidocs/16-dispatcher-backlog.md`
- Recent commits (last 2h): 4 — latest `1bbbbb4` (ci/PERF4d branch commit)
- Open PRs: ~31–33 items (30 at last fire + 3 new dispatches this fire)

## Rows considered

| ID | Description | Decision | Reason |
|---|---|---|---|
| J1e-PR-08 | aidocs/44 + aidocs/42: mark J1e in plugin column instead of core | **DISPATCHED** | XS, docs-only, no code risk, plugins/jupyter/ confirmed shipped |
| J1e-PR-09 | Reconcile J1e = J2a cross-reference in aidocs/16 | **DISPATCHED** | XS, docs-only, no code risk, clear acceptance criterion |
| PERF4d | Nightly CI job `perf-endpoints.yml` — per-endpoint SLO check | **DISPATCHED** | S, CI-only, no code risk, PERF4a prerequisite shipped, full design in aidocs/ops/77 §9.2 |
| KRL-INTEGRATION-MFFD-REAL-02-IF-COMPOUND-BOOLEAN | Compound parenthesized boolean in KRL IF statements | SKIPPED | Manual grammar verification required — flagged by 2026-05-31-00 fire; `atom → LPAREN expr RPAREN` may already be present in KrlParser.g4 |
| UX-WALK-2026-05-29-08 | Access column ambiguous `—` → controlled vocabulary chip (Open/Shared/Restricted/Closed) | SKIPPED | PR count at upper bound (~30+); deferred to next fire once PRs merge |

## Dispatched

| ID | PR | Status |
|---|---|---|
| J1e-PR-08 | [#1652](https://github.com/noheton/shepard/pull/1652) | ✓ open |
| J1e-PR-09 | [#1653](https://github.com/noheton/shepard/pull/1653) | ✓ open |
| PERF4d | [#1603](https://github.com/noheton/shepard/pull/1603) | ✓ open (updated) |

### J1e-PR-08 details (PR #1652)

- `aidocs/44` line ~264: J1e row symbol corrected to `**✓ ↑** (J1e — plugin: \`plugins/jupyter/\`)` with refs pointing to `plugins/jupyter/docs/`
- `aidocs/42` §"What's in the box" and §"Where it's going": added `(plugin: \`plugins/jupyter/\`)` parentheticals; corrected admin path from `/v2/admin/jupyter/config` → `/v2/admin/plugins/jupyter/config`
- `aidocs/16` J1e-PR-08 row flipped to `done (2026-05-31)`
- `aidocs/01-doc-stage-index.md` regenerated (413 docs)

### J1e-PR-09 details (PR #1653)

- `aidocs/16` J1e section: added `J1e ↔ J2 cross-reference` paragraph — J1e maps to J2a (shipped as `plugins/jupyter/`); J2b/c/d retain future-phase meanings
- `aidocs/16` J2a row: added parenthetical `(J2a = J1e, already shipped)`
- J1e-PR-09 status flipped to `done (2026-05-31)`

### PERF4d details (PR #1603)

- `.github/workflows/perf-endpoints.yml`: nightly per-endpoint SLO workflow created (cron `37 2 * * *` daily per aidocs/ops/77 §9.2); mirrors `perf-smoke.yml` structure (compose boot → `/versionz` poll → k6 via Docker → artifact upload → issue-filing on breach → teardown)
- `K6_SCENARIO=steady,ramp` env var added — limits nightly run to ~8 min; spike scenario excluded from automated runs (first-alarm only)
- `aidocs/16` PERF4d row flipped from `📐 designed` to `done (2026-05-31)`
- `aidocs/44` per-endpoint SLO row flipped from `📐 ↑` to `✓ ↑`; notes updated to "PERF4a–d shipped"

## Notes

- PR count is at/above the 30-item concurrent-work budget after this fire; next dispatcher should only pick XS/S doc-risk or CI-only items
- KRL-02-IF-COMPOUND-BOOLEAN remains blocked on manual grammar verification (`KrlParser.g4` may already handle the case — needs a human look before dispatch)
- UX-WALK-2026-05-29-08 is queued; prioritize once a PR slot opens
- PERF4d found an existing branch/PR (#1603) from a prior partial session — agent added missing `K6_SCENARIO` env var and updated aidocs/16 + aidocs/44 on that branch
