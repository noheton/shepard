---
fire: 2026-06-02-04
fired-at: 2026-06-02T04:00Z
dispatcher-version: hourly-v1
---

# Dispatcher run 2026-06-02-04

## Inventory snapshot

- Open PRs at fire time: 30 (#1700–#1730, all open)
- In-flight items (PRs already open): MCP-COV-07, UX-WALK-2026-05-29-05, PLACEHOLDER-REPLACE-FS1e1, SEMANTIC-ANNOTATE-BULK-REST-1, MCP-COV-01-AUDIT, UI21-BACKEND-Q, SNAPSHOTS-DIFF-NAV-01, UI7, PROV-CAPTURE-READS-FLIP, DOC-STAGE3, SCENEGRAPH-LIST-1, DB-BP1 (and others)
- Prior fires today: 2026-06-02-01, 2026-06-02-02, 2026-06-02-03

## Items dispatched

### 1. SCENEGRAPH-CREATE-FROM-URDF-2-FE — XS, frontend
**PR:** https://github.com/noheton/shepard/pull/1732
**Branch:** `SCENEGRAPH-CREATE-FROM-URDF-2-FE-wire-button`
**Commit:** `71813eb`

Wired `OpenInSceneGraphButton.vue` confirm button to `POST /v2/scene-graphs/from-urdf/{fileReferenceAppId}`.

- 201 → navigate to new scene graph page
- 409 → navigate to existing scene via `existingSceneAppId` in response body
- 4xx/5xx → modal error alert
- Loading spinner while in-flight

New files: `useScenegraphFromUrdf.ts`, `useSceneGraph.ts`, `openInSceneGraphButtonHelpers.ts`, 33 Vitest tests.
Gates: `npm run lint` clean (no new errors), `npm run typecheck` clean, `npm run test` 33/33 new tests pass.

### 2. PERF4d — S, CI YAML
**PR:** https://github.com/noheton/shepard/pull/1731
**Branch:** `PERF4d-nightly-k6-endpoint-workflow`
**Commit:** `208425d`

Created `.github/workflows/perf-endpoints.yml` — nightly k6 endpoint performance workflow.

- Schedule: `42 3 * * 1-5` + `workflow_dispatch`
- Boots compose stack, waits for backend health, seeds fixtures
- Runs `scripts/perf/k6-endpoints.js` (file already exists)
- Uploads output + `last-run.json` as 30-day artifacts
- Files GitHub issue with `performance` + `automated` labels on threshold breach
- Teardown always runs; closes #1552

Gate: YAML validated via `python3 yaml.safe_load` — clean.

### 3. M4I-d-3-followup — M, backend renderer
**PR:** https://github.com/noheton/shepard/pull/1733
**Branch:** `M4I-d-3-followup-numerical-variable`
**Commit:** `500b301`

Extended `M4iDataObjectRenderer` to emit channel-level `m4i:NumericalVariable` blank nodes for timeseries channels with `urn:shepard:unit` semantic annotations.

- New `SemanticAnnotationDAO.findChannelUnitsByDataObjectAppId()` — two-hop Cypher traversal
- `addChannelNumericalVariables()` in renderer: per-channel blank node with `rdfs:label` + `m4i:hasUnit` QUDT IRI
- DAO failure is fail-soft (caught, WARN-logged, never propagated)
- 5 new tests; 31/31 `M4iDataObjectRendererTest` pass

Design ref: `aidocs/semantics/94 §4.4 d-3`.

## Skipped / not dispatched

No items were skipped due to conflicts. The three candidates were chosen as the highest-value queued items with no in-flight blockers and no constraint violations (no auth/security/schema-multi-substrate/BREAKING items).

## Failures

None.

## Summary

| Item | Size | PR | Status |
|---|---|---|---|
| SCENEGRAPH-CREATE-FROM-URDF-2-FE | XS | #1732 | ✓ opened |
| PERF4d | S | #1731 | ✓ opened |
| M4I-d-3-followup | M | #1733 | ✓ opened |
