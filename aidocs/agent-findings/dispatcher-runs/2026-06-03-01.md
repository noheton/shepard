---
stage: fragment
last-stage-change: 2026-06-03
---

# Dispatcher run — 2026-06-03-01

**Fire time:** 2026-06-03 ~01:00 UTC (context-resumed from previous session)
**Tracker:** [issue #1576](https://github.com/noheton/shepard/issues/1576)

## Summary

| Stat | Count |
|------|-------|
| Rows considered | 28 |
| In-flight (skip) | 17 |
| Already done (skip) | 3 |
| Blocked / too large (skip) | 5 |
| **Dispatched** | **3** |

## Already-done discoveries (backlog stale)

These rows are marked `queued` in aidocs/16 but are already shipped:

| Row | Evidence |
|-----|----------|
| PERF4e | All 4 k6 scripts already use `apikey` header; `k6-endpoints.js` comment: "All four k6 scripts now use apikey (PERF4e unification, 2026-05-27)" |
| SNAPSHOTS-DIFF-NAV-01 | `SnapshotsPane.vue` lines 39/243/261 already have `compareInSnapshotDiff` + `mdi-compare` Compare action |
| SCENEGRAPH-NAV-01 | Previous run (2026-06-03-00) noted `frontend/pages/scene-graphs/index.vue` already has a full `v-data-table-server` implementation |

## Dispatches

### 1. SINGLETON-FILE-02-SEED-MIGRATION (S)

**Row:** `examples/mffd-rdk-urdf-showcase/seed.py` switches every one-file `upload_file_reference()` to `POST /v2/files` (singleton). Idempotent.

**Branch:** `SINGLETON-FILE-02-SEED-MIGRATION-mffd-urdf-singleton`
**Status:** dispatched — PR pending

**Why:** The showcase seed still calls the v1 bundle-path (`apis.file_reference.create_file_reference()` with `fileOids=[oid]`) for single-file uploads at lines 723, 743, 745, 775. The FR1b singleton rule (`POST /v2/files`) is the correct shape. The SHOWCASE.md already documents the expected singleton appId form.

---

### 2. SEMANTIC-ANNOTATE-BULK-REST-1 (S)

**Row:** `POST /v2/semantic-annotations/bulk` — bulk-create REST endpoint mirroring the MCP bulk tool. v0's MCP tool fans out to the single DAO; this REST row lets non-MCP callers (UI mass-annotation, CLI sweeps) share the same semantics.

**Branch:** `SEMANTIC-ANNOTATE-BULK-REST-1-bulk-annotation-rest`
**Status:** dispatched — PR pending

**Why:** `AnnotationMcpTools.semanticAnnotateBulk` shipped 2026-05-30 (MCP-COV-05 v0). The identical semantics (max 100 entries, per-row error isolation) belong on the REST surface so UI mass-annotation and CLI sweeps don't need the MCP layer. `SemanticAnnotationV2Rest.java` currently has no `/bulk` endpoint.

---

### 3. RDM-005a(e) (XS)

**Row:** "Show only failing" filter on the per-check list (sub-item (e) of RDM-005a).

**Branch:** `RDM-005a-e-show-only-failing`
**Status:** dispatched — PR pending

**Why:** `MetadataCompletenessCard.vue` renders all checks including passing ones. When a Collection has 7/9 checks passing, a researcher looking for what to fix must visually scan the full list. The filter is a computed + reactive boolean + one button — XS, self-contained, high UX value.

---

## Skipped (notable)

| Row | Reason |
|-----|--------|
| SINGLETON-FILE-05-SHACL-GUARD | Pairs with RDM-005 widget — wait for RDM-005a(d) server-side score first |
| GH-INFRA2 | Manual GitHub board setup — not a code task |
| MCP-COV-05-SEMANTIC-BULK-CONCURRENT | Concurrent fan-out upgrade for the MCP bulk tool — M-sized, needs benchmarking |
| RDM-005a(a)(b)(c)(d) | Each is S–M and has ordering dependencies or needs new fields; only (e) is independently XS-shippable |
| PROV-CAPTURE-READS-FLIP | In-flight (PR #1739) |
| UI7, UI21-BACKEND-Q | In-flight (#1722, #1726) |
| MCP-COV-07-SEMANTIC-SPARQL | In-flight (#1730) |
| PLACEHOLDER-REPLACE-FS1e1 | In-flight (#1729) |
| SHAPES-V-PREFILL-2 | In-flight (#1750) |
| UX-WALK-2026-05-29-07 | In-flight (#1755) |
| MFFD-IMAGEBUNDLE-PANE-MOUNT-1 | In-flight (#1737) |

## PRs opened this fire

*(to be updated when sub-agents report back)*

| Row | PR |
|-----|----|
| SINGLETON-FILE-02-SEED-MIGRATION | TBD |
| SEMANTIC-ANNOTATE-BULK-REST-1 | TBD |
| RDM-005a(e) | TBD |
