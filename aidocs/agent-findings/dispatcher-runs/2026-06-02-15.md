---
stage: fragment
last-stage-change: 2026-06-02
---

# Dispatcher run — 2026-06-02 15:xx UTC

## Summary

| | Count |
|---|---|
| Backlog rows considered | ~130 queued rows scanned |
| Already in-flight (skipped) | 31 open PRs de-duped |
| Pre-implemented (code exists, row stale) | 4 rows flagged |
| Dispatched this run | 3 |
| Failures | 0 (agents still running at log-commit time) |

---

## Recent commits (last 24 h)

- `72c8eba` merge: AAH1 — TcScopeCsvParser + SvdxIngestRest scaffolding (#305)
- `86d33ad` WIP(svdx): AAH1 partial TcScopeCsvParser + v2 svdx REST scaffolding

---

## In-flight PRs sampled (31 total)

Key in-flight rows that were excluded from dispatch:
`DB-BP1` (#1704), `PERF4d` (#1731), `PROV-CAPTURE-READS-FLIP` (#1705),
`DT1-DAO-FRESH-SESSION` (#1715), `UI7` (#1722), `FRONTEND-LINT-DEBT-04` (#1716),
`KRL-CONFIG-1` (#1721), `SINGLETON-FILE-02-SEED-MIGRATION` (#1717),
`MCP-COV-07-SEMANTIC-SPARQL` (#1730), `MCP-COV-01-AUDIT` (#1725),
`SEMANTIC-ANNOTATE-BULK-REST-1` (#1727), `J1e-PR-06-AUTOFETCH-02` (#1723),
`SNAPSHOTS-DIFF-NAV-01` (#1709), `TPL-ME-USE-FROM-BROWSE` (#1719),
`BE-USERS-ME-ROLES-1` (#1720), `PLACEHOLDER-REPLACE-P10c` (#1710),
`PLACEHOLDER-REPLACE-FS1e1` (#1729), `DOC-STAGE3` (#1713),
`SCENEGRAPH-LIST-1` (#1714), `SCENEGRAPH-CREATE-FROM-URDF-2-FE` (#1732),
`M4I-d-3-followup-numerical-variable` (#1733), `UI21-BACKEND-Q` (#1726).

---

## Pre-implemented rows (stale backlog, need doc updates only)

These rows are still marked `queued` in the backlog but the code is already on `main`.
They are handled in dispatch item 1 below.

| Row | Evidence |
|---|---|
| `KRL-INTEGRATION-MFFD-REAL-01-ENCODING-LATIN1` | Fix in `plugins/krl-interpreter/krl_interpreter/sidecar/schemas.py` lines 94–115 (utf-8 → iso-8859-1 → cp1252 codec ladder + comment citing the row). |
| `SCENEGRAPH-NAV-01` | `frontend/pages/scene-graphs/index.vue` fully implemented with `v-data-table-server`, pagination, open-by-appId form. |
| `SCENEGRAPH-NAV-02` | `OpenInSceneGraphButton` mounted in `frontend/pages/…/filereferences/[fileReferenceId]/index.vue` line 202. |
| `TOOLS-CONTEXT-FILEREF-SCENE` | Cross-ref of SCENEGRAPH-NAV-02; same evidence. |

---

## Items skipped and why

| Row | Reason |
|---|---|
| `DB-OPT4` | L-sized, too large for autonomous dispatch |
| `TSDB-DDL-2` | M-sized, touches TimescaleDB + service layer — medium-high risk |
| `V16-IMPL-*` | Complex Python importer multi-step |
| `FRONTEND-LINT-DEBT-02` | M-sized, needs real type decisions |
| `KRL-COMPARE-*` | Whole family gated on `-01-DESIGN` doc first; XL total effort |
| `LINEAGE-EDIT-1` | Blocked on `BUG-PREDECESSOR-IDS-NUMERIC-IN-V2-PATCH` (dispatched) |
| `TOOLS-NAV-01` | M-sized, touches HeaderBar navigation globally |
| `PROV1e` | M-sized; gated on PROV1d + admin-page strip |
| `BUG-COLL-APPID-ROUTE-004` | M-sized, requires generated client work |
| `BUG-COLL-APPID-ROUTE-006` | M-sized, backend + frontend list endpoint extension |
| All XL/L items | Too large for autonomous dispatch |

---

## Dispatched

### 1. J1e-PR-08 + J1e-PR-09 + backlog housekeeping

| Field | Value |
|---|---|
| Row IDs | `J1e-PR-08`, `J1e-PR-09` + 3 stale-row updates |
| Risk | low |
| Size | XS (doc-only) |
| Branch | `J1e-PR-08-09-doc-cleanup` |
| Scope | `aidocs/16`, `aidocs/42`, `aidocs/44` |

Updates:
- `J1e-PR-08`: Move J1e from "core" → "plugin" column in `aidocs/44` + `aidocs/42`.
- `J1e-PR-09`: Mark J1e-PR-09 done; add reconciliation note that J1e = J2a.
- Mark `KRL-INTEGRATION-MFFD-REAL-01-ENCODING-LATIN1` done (fix already in schemas.py). Spawn test sub-row.
- Mark `SCENEGRAPH-NAV-01` done (page already exists).
- Mark `SCENEGRAPH-NAV-02` done (button already exists).

---

### 2. MFFD-IMAGEBUNDLE-PANE-MOUNT-1

| Field | Value |
|---|---|
| Row ID | `MFFD-IMAGEBUNDLE-PANE-MOUNT-1` |
| Risk | low |
| Size | S (frontend-only) |
| Branch | `MFFD-IMAGEBUNDLE-PANE-MOUNT-1-fe-pane` |
| Scope | `frontend/components/context/file/DataObjectImageBundlePane.vue` (new), `frontend/composables/context/useImageBundleGroups.ts` (new), `frontend/pages/…/dataobjects/[dataObjectId]/index.vue` (mount panel) |

Detects FileBundleReferences with image-extension files and mounts `ImageBundleViewer.vue` as an "Image Frames" `ExpansionPanelItem`. Multi-bundle picker when >1 image bundle present. Follows `DataObjectThermographyPane.vue` mount pattern.

---

### 3. BUG-PREDECESSOR-IDS-NUMERIC-IN-V2-PATCH

| Field | Value |
|---|---|
| Row ID | `BUG-PREDECESSOR-IDS-NUMERIC-IN-V2-PATCH` |
| Risk | medium (backend DataObjectIO change + frontend composable) |
| Size | S |
| Branch | `BUG-PREDECESSOR-IDS-NUMERIC-IN-V2-PATCH-appid-field` |
| Scope | `DataObjectIO.java` (add `predecessorAppIds: List<String>` companion field), DataObject PATCH handler (resolve appIds → numeric IDs, combine), `useUpdateDataObjectPredecessor.ts` (send both fields) |

Fixes the broken rework-loop UI for post-Neo4j-reset DataObjects that have UUID v7 appId only and no numeric Neo4j internal id.

---

## Notes for operator

- 31 open PRs is a heavy in-flight load; recommend reviewing the oldest PRs before the next dispatcher fire to reduce queue depth.
- The KRL encoding fix was pre-implemented (code merged without backlog update) — this is a pattern to watch; run `scripts/trace-feature.sh` after each ship to catch row staleness early.
