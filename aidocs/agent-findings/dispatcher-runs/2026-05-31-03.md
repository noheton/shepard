---
stage: deployed
last-stage-change: 2026-05-31
---

# Dispatcher run 2026-05-31-03

**Fire time:** 2026-05-31 ~03:xx UTC  
**Trigger:** hourly cron `0 * * * *`

## Inventory

- Backlog SSOT: `aidocs/16-dispatcher-backlog.md`
- Recent commits (last 2h): 3 — `214a675` (ops/dispatcher 02 fire log), `1594d52` (ops/dispatcher 01 fire log), sub-agent commit on GH-PM2 branch
- Open PRs at fire start: ~52 items (well above the 30-item budget; prior fires flagged this; note carried forward)

## Rows considered

| ID | Description | Decision | Reason |
|---|---|---|---|
| GH-PM2 | Wire `scripts/build-traceability-index.py` into `.github/workflows/pages.yml` | **DISPATCHED** | S/CI-only; script on main; stdlib-only; single workflow file edit; satisfies "XS/S CI-only" constraint from -01/-02 fire notes |
| AI1c-UI | `qualityScore` badge on TimeseriesReference cards + detail page | SKIPPED — likely already done | `QualityScoreChip.vue` exists and is used in `DataObjectDataMetaCell.vue` (table rows), `ShowTimeseriesReferenceDialog.vue`, and the TS reference detail page. Recommend human verify and mark row `done` |
| PERF13 | Deduplicate collection-metadata fetch between page and sidebar | SKIPPED | Gated on PERF11 (PR #1586 still open, not yet merged to main) |
| UI21-BACKEND-Q | Add `createdBy` filter to `GET /v2/containers` | SKIPPED | PR budget at ~52; M-effort backend change; defer to next fire once PRs drain |
| A2 | Decompose monolithic TimeseriesRest / FileRest / CollectionRest | SKIPPED | L size; too large for autonomous dispatch |
| P7 | Ship unified `POST /search/v2` | SKIPPED | L size; gated on aidocs/13 |
| F1 / P5 | `@Authz` annotation seam replacing `isAllowed` path switch | SKIPPED | Touches auth/permissions — hard constraint |
| F2 | Group / sharing model | SKIPPED | L size; touches permissions |
| L2e | Drop legacy /v1/ long-id paths + flip cache key | SKIPPED | BREAKING; touches permissions cache |
| P6 | Fold X-API-KEY into Authorization: Bearer | SKIPPED | Breaking |
| PM1b3 | True runtime CDI integration via Vert.x router | SKIPPED | M-L; complex Quarkus internals |
| All other queued items | Various | SKIPPED | Either: (a) PR budget constraint, (b) gated on in-flight PRs, (c) operator-side action only, (d) requires user decision |

## Dispatched

| ID | PR | Status |
|---|---|---|
| GH-PM2 | [#1656](https://github.com/noheton/shepard/pull/1656) | ✓ open |

### GH-PM2 details (PR #1656)

- `.github/workflows/pages.yml`: added `scripts/build-traceability-index.py` to `paths:` trigger; inserted "Generate requirements traceability index" step (`python3 scripts/build-traceability-index.py`) between the bib2yml step and the Jekyll build.
- `aidocs/16-dispatcher-backlog.md`: GH-PM2 row status flipped `queued` → `in-progress`; notes branch name.
- Script outputs `docs/reference/traceability.json` + `docs/reference/traceability.md`; both are under `docs/` so Jekyll picks them up automatically on every Pages build.

## Notes

- PR count remains at ~52, well above the 30-item budget. Next fires should continue the "XS/S CI-only" posture until the queue drains.
- **AI1c-UI appears already implemented** (quality score chip in table + detail page + dialog). Human should verify and mark done in `aidocs/16`.
- **KRL-INTEGRATION-MFFD-REAL-02-IF-COMPOUND-BOOLEAN** continues to require manual `KrlParser.g4` grammar verification before any dispatch attempt; skipped for the 4th consecutive fire.
- **PERF13** becomes dispatchable the moment PERF11 (PR #1586) merges to main.
