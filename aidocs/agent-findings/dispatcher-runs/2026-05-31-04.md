---
stage: deployed
last-stage-change: 2026-05-31
fire: 2026-05-31-04
---

# Dispatcher fire 2026-05-31-04

**Fired:** 2026-05-31 ~04:xx UTC  
**Backlog SSOT:** `aidocs/16-dispatcher-backlog.md`  
**Open PRs at fire:** 30  
**Recent fires:** fire-03 dispatched GH-PM2; fire-02 dispatched MCP-COV-05 + MCP-COV-11

## Scan summary

- Read `aidocs/16-dispatcher-backlog.md` end-to-end (3053 lines).
- Catalogued all 30 open PR branches to build in-flight exclusion list.
- Identified stale backlog entries: lines 3008-3010 still list TOOLS-NAV-01, SCENEGRAPH-NAV-01, SCENEGRAPH-NAV-02 as "queued" but they shipped 2026-05-30. Low-priority cleanup deferred.

## In-flight exclusions (sampled)

`gh-pm2-traceability-pages-workflow`, `mcp-cov-05-semantic-bulk-concurrent`, `mcp-cov-11-prov-query-activity-list`, `scenegraph-create-from-urdf-2-fe-wire-button`, `vis-s1b-signed-url-minter`, `tsdb-ddl-2-chunk-aware-delete`, and 24 others.

## Candidates evaluated

| Row | Size | Ruling |
|---|---|---|
| GH-PM3 | M | **Skip** — explicitly "After GH-PM2"; GH-PM2 still in-flight (PR #1656) |
| PERF13 | S | **Skip** — "XS after PERF11"; PERF11 not yet landed |
| SCENEGRAPH-PERMS-1 | S | **Skip** — touches auth/permissions (CLAUDE.md hard constraint) |
| SCENEGRAPH-CREATE-FROM-URDF-2-FE | XS | **Skip** — already in-flight (branch `scenegraph-create-from-urdf-2-fe-wire-button`) |
| VIS-S1b | S | **Skip** — already in-flight (branch `vis-s1b-signed-url-minter`) |
| **PERF11** | M | **Dispatch** — frontend-only SSR preloading, no deps, clear acceptance criterion |
| **UI21-BACKEND-Q** | S | **Dispatch** — additive nullable field on container search, clear acceptance criterion |
| **DT1-DAO-FRESH-SESSION** | XS | **Dispatch** — mirrors JupyterConfigDAO pattern, gate satisfied (SCENEGRAPH-REST-1 shipped), MINOR risk |

## Dispatched (3 agents, parallel)

### PERF11 — SSR payload preloading for collection composables
- **Branch:** `perf11-ssr-payload-preloading`
- **Acceptance:** Replace `$fetch` with `useAsyncData` in `useFetchRecentCollections`, `useFetchCollectionById`, `useFetchDataObjectMapByCollection`; dedup keys: `'recent-collections'`, `` `collection-${id}` ``, `` `collection-dataobjects-${collectionId}` ``; expose `refresh()` for post-mutation invalidation. No backend change.
- **Risk:** Low — pure Nuxt 3 frontend pattern, no API surface change
- **Gates:** `npm run lint`, `npm run test`, `npm run typecheck`
- **PR:** pending sub-agent

### UI21-BACKEND-Q — server-side `createdBy` filter on `GET /v2/containers`
- **Branch:** `ui21-backend-q-created-by-filter`
- **Acceptance:** Extend `ContainerSearchBody.searchParams` with nullable `createdBy` field + SearchApi predicate; frontend swap removes client-side `visibleServerItems` filter, routes `owner` via `useSearchContainers`.
- **Risk:** Low-medium — additive nullable field, no existing callers affected
- **Gates:** `mvn verify -pl backend`, `npm run lint`, `npm run test`, `npm run typecheck`
- **PR:** pending sub-agent

### DT1-DAO-FRESH-SESSION — fresh-session override on scene-graph DAOs
- **Branch:** `dt1-dao-fresh-session`
- **Acceptance:** Override `createOrUpdate` + finders on `DigitalTwinSceneDAO`, `CoordinateFrameDAO`, `JointDAO` with `NeoConnector.getInstance().getNeo4jSession()` + null-guard + `AppIdGenerator.next()` mint + `live.save(entity, 1)`. Mirror `JupyterConfigDAO`. Add JUnit coverage.
- **Risk:** Low — internal Java implementation fix, no API/schema change
- **Gates:** `mvn verify -pl backend`
- **PR:** pending sub-agent

## Not dispatched this fire

- **GH-PM2** (in-flight), **MCP-COV-05/11** (in-flight, fire-02)
- **PERF12** (M, queued) — paginate `useFetchDataObjectMapByCollection`; held for same fire as PERF11 review
- **L4** (M, queued) — search-as-you-type ontology tree; large enough to warrant a design review first
- **BATCH-API-1** (M, queued) — needs design spec first

## Status

Sub-agents running in parallel. Dispatcher log committed directly to `main`.
