---
name: 2026-06-02-02
description: Dispatcher run log auto-generated by cron
type: project
stage: fragment
last-stage-change: 2026-06-02
---

---
fire: 2026-06-02-02
date: 2026-06-02
hour: "02"
status: complete
rows_dispatched: 3
tracker_issue: 1576
---

# Dispatcher fire 2026-06-02-02

## Fire summary

| Field | Value |
|---|---|
| Fire time | 2026-06-02 ~02:30 UTC |
| Backlog read | `aidocs/16-dispatcher-backlog.md` (3126 lines, read in chunks) |
| Git window | `--since='3 hours ago'` → 4 commits (2× log updates, 2× log commits) |
| Open PRs scanned | ~21 in-flight from prior fires |
| Candidates evaluated | ~10 |
| Rows dispatched | 3 |

## Recent commits (last 3 hours)

- `b068f8c` chore(dispatcher): update 2026-06-02-01 log — all 3 PRs opened (#1722, #1723, #1724)
- `1d98683` chore(dispatcher): log fire 2026-06-02-01 — UI7, J1e-PR-06-AUTOFETCH-02, REF-EDIT-4
- `3484fbe` chore(dispatcher): update 2026-06-02-00 log — all 3 PRs opened (#1719, #1720, #1721)
- `a8608bd` chore(dispatcher): log fire 2026-06-02-00 — BE-USERS-ME-ROLES-1, KRL-CONFIG-1, TPL-ME-USE-FROM-BROWSE

## In-flight rows (open PRs, excluded from dispatch)

UI7 (#1722), J1e-PR-06-AUTOFETCH-02 (#1723), REF-EDIT-4 (#1724), KRL-CONFIG-1 (#1721), BE-USERS-ME-ROLES-1 (#1720), TPL-ME-USE-FROM-BROWSE (#1719), SINGLETON-FILE-02-SEED-MIGRATION (#1717), FRONTEND-LINT-DEBT-04 (#1716), DT1-DAO-FRESH-SESSION (#1715), SCENEGRAPH-LIST-1-FE (#1714), DOC-STAGE3 (#1713), PLACEHOLDER-REPLACE-P10c (#1710), SNAPSHOTS-DIFF-NAV-01 (#1709), J1e-PR-06-index-crosslink (#1708), UX-WALK-2026-05-29-07 (#1707), UX-WALK-2026-05-29-08 (#1706), PROV-CAPTURE-READS-FLIP (#1705), DB-BP1 (#1704), J1e-PR-08/J1e-PR-09 (#1703), SCENEGRAPH-CREATE-FROM-URDF-2-FE-backlog-flip (#1702), AI1c-UI/J1d-UI-backlog-flip (#1701)

## Eliminated candidates

| Row ID | Reason eliminated |
|---|---|
| SEMA-V6-PRED-UI | Already fully shipped — `SemanticPredicateStatsRest.java` implements `GET /v2/semantic/predicates/{predicateIriBase64}/stats`; `[predicateIri].vue` renders total-annotations card, top-values table, sample-entities table. Backlog row stale. |
| SHAPES-V-PREFILL-2-RDF-ENDPOINT | Already implemented — `DataObjectRdfRest.java` at `GET /v2/data-objects/{appId}/rdf` returns Turtle subgraph. `validate.vue` calls `fetchDataObjectRdf()` on mount with `?focusAppId`. Backlog row stale. |
| SHAPES-V-PREFILL-1 (frontend auto-load) | Already implemented — `validate.vue` has `fetchAttachedTemplate()` + `fetchDataObjectRdf()` on `onMounted`; both SHAPES-V-PREFILL-1 and SHAPES-V-PREFILL-3-EXTRACT-SHACL comments say "this commit". Backlog row stale. |
| SCENEGRAPH-CREATE-FROM-URDF-2-FE | In-flight as PR #1702 (`SCENEGRAPH-CREATE-FROM-URDF-2-FE-backlog-flip`). |
| MCP-COV-05-SEMANTIC-BULK-CONCURRENT | `SemanticAnnotationDAO` is `@RequestScoped`; fan-out via `CompletableFuture` from `@ApplicationScoped` MCP bean would break CDI proxy without explicit Quarkus context propagation. Deferred — risk higher than expected. |

## Dispatched rows

| Row ID | Description | Branch | PR |
|---|---|---|---|
| SEMANTIC-ANNOTATE-BULK-REST-1 | `POST /v2/annotations/bulk` REST endpoint — batch-upsert up to 100 semantic annotations in one call; `BulkAnnotationResultIO` per-row success/error envelope | `SEMANTIC-ANNOTATE-BULK-REST-1-bulk-endpoint` | #1727 |
| MCP-COV-01-AUDIT | MCP coverage inventory doc — catalogue every shipped MCP tool against the backlog, identify gaps | `mcp-cov-01-audit-inventory` | #1725 |
| UI21-BACKEND-Q | Add `createdBy` server-side filter to container search — extend `ContainerSearchParams` + `Neo4jQueryBuilder`; drop client-side owner filter from `ContainerListPage.vue` | `UI21-BACKEND-Q-container-owner-filter` | #1726 |

## Agent outcomes

### SEMANTIC-ANNOTATE-BULK-REST-1 — PR #1727
- New `BulkAnnotationResultIO.java` — per-row `{ok, appId, subjectAppId, error}` envelope with convenience constructors
- `POST /v2/annotations/bulk` in `SemanticAnnotationV2Rest.java`: 400 on null/empty/oversized (>100); per-row isolation (one failure doesn't abort batch); per-row permission gate returns failure row (not 403); per-row provenance via `ProvenanceService.record()`; `PROP_SKIP_CAPTURE` set after loop to suppress duplicate Activity from filter; returns 200 (mixed-success array)
- 8 new test cases (happy path, partial failure, empty/null → 400, oversized → 400, 401, ambiguous object type, permission denied); total 50 tests, 0 failures
- `aidocs/16` SEMANTIC-ANNOTATE-BULK-REST-1 marked done; `aidocs/34` row added

### MCP-COV-01-AUDIT — PR #1725
- New `aidocs/agent-findings/mcp-coverage-audit.md` — full catalogue of 197 REST endpoints (97 `*Rest.java` files) vs. 52 MCP tools (12 tool classes)
- Coverage of non-admin REST surface: ~23% (37 / 162 endpoints)
- 12 MCP tools operate direct to service layer with no REST counterpart
- Top 5 gaps: DataObject CRUD, Snapshots (8 endpoints 0 coverage), temporal annotations on timeseries, SPARQL query (MCP-COV-07 now unblocked), Import pipeline context endpoint
- `aidocs/16` MCP-COV-01 marked done; MCP-COV-07 notes updated to "unblocked"

### UI21-BACKEND-Q — PR #1726
- Backend: nullable `createdBy` field on `ContainerSearchParams`; `Neo4jQueryBuilder` 5-arg overload appending `AND toLower(c.createdBy) CONTAINS $pN` when non-blank (4-arg delegate preserves compat); `ContainerSearchService` passes `getCreatedBy()` as 5th arg
- Tests: 4 new cases (happy path, null filter absent, blank filter absent, injection safety — literal value in params map not Cypher string); all 8 tests pass
- Frontend: `containerListQueryParams.ts` + `useSearchContainers.ts` forward `createdBy`; `ContainerListPage.vue` client-side `ownerFilter` computed removed; server-side filter fixes pagination counts
- `backend-client/src/models/ContainerSearchParams.ts` updated; `aidocs/16` UI21-BACKEND-Q marked done
