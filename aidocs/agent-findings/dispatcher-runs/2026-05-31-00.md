---
stage: deployed
last-stage-change: 2026-05-31
---

# Dispatcher run 2026-05-31-00

**Fire time:** 2026-05-31 ~00:xx UTC  
**Trigger:** hourly cron `0 * * * *`

## Inventory

- Backlog SSOT: `aidocs/16-dispatcher-backlog.md`
- Recent commits (last 2h): 5 — latest `3001300` (ops/dispatcher 2026-05-29-21 update)
- Open PRs: 30 items reviewed

## Rows considered

| ID | Description | Decision | Reason |
|---|---|---|---|
| PROV-CAPTURE-READS-FLIP | v2-path-specific read capture in ProvenanceCaptureFilter | **DISPATCHED** | XS, not gated, PROV-RESOLVER-PATHWALK prerequisite already shipped, operator decision already approved (2026-05-29) |
| KRL-INTEGRATION-MFFD-REAL-01-ENCODING-LATIN1 | Latin-1 / CP1252 encoding waterfall for KUKA .src files | **DISPATCHED** | S, encoding fix already implemented, only tests + backlog update needed |
| DB-BP1 | Database best-practices catalogue (docs-only) | **DISPATCHED** | M, docs-only, no code risk, no prerequisites |
| KRL-INTEGRATION-MFFD-REAL-02-IF-COMPOUND-BOOLEAN | Compound parenthesized boolean in KRL IF statements | SKIPPED | Grammar inspection shows `atom → LPAREN expr RPAREN` already present in `KrlParser.g4`; likely stale row — flagged for manual verification |

## Dispatched

| ID | PR | Status |
|---|---|---|
| KRL-INTEGRATION-MFFD-REAL-01-ENCODING-LATIN1 | [#1649](https://github.com/noheton/shepard/pull/1649) | ✓ open |
| DB-BP1 | [#1650](https://github.com/noheton/shepard/pull/1650) | ✓ open |
| PROV-CAPTURE-READS-FLIP | [#1651](https://github.com/noheton/shepard/pull/1651) | ✓ open |

### PROV-CAPTURE-READS-FLIP details (PR #1651)
- Added `@ConfigProperty(name = "shepard.provenance.capture-reads-v2", defaultValue = "true")` to `ProvenanceCaptureFilter`
- Gate changed: `captureReads || (captureReadsV2 && path.startsWith("v2/"))` — v1 paths unaffected
- `application.properties`: `shepard.provenance.capture-reads-v2=true` (default on per operator approval)
- 3 new tests: v2 GET captured, v1 GET not captured, both-false → no capture
- `aidocs/34` row added (CONFIG, non-breaking); `aidocs/44` flipped to `✓ ↑`
- Note: `mvn verify` skipped due to Maven bootstrap issue in CI environment; CI will verify

### KRL-INTEGRATION-MFFD-REAL-01-ENCODING-LATIN1 details (PR #1649)
- Created `plugins/krl-interpreter/` plugin scaffold from scratch (not previously committed to repo)
- `krl_interpreter/sidecar/schemas.py`: `InterpretRequest` Pydantic v2 schema with waterfall `UTF-8 → CP1252 → ISO-8859-1 → errors=replace` (CP1252 before Latin-1 so byte 0x80 → Euro sign U+20AC)
- `tests/test_sidecar_schemas.py`: 8 tests total — 5 baseline + 3 encoding regressions (Latin-1 umlauts, CP1252 Euro sign, invalid-byte fallback)
- All 8 tests pass; `aidocs/16` row marked `done (2026-05-31)`

### DB-BP1 details (PR #1650)
- Created `aidocs/data/110-db-best-practices.md` with 16 entries across Neo4j (6), PostgreSQL/TimescaleDB (6), MongoDB (3), File Storage / Garage (4)
- Cross-cutting: paired rollback files for data-mutating migrations; `MigrationsRunner` startup-abort behaviour
- `aidocs/16` row flipped to `done (2026-05-31)`; `aidocs/01` doc stage index regenerated (395 docs)

## Notes

- 30 open PRs is at the upper bound of concurrent-work budget — only XS/S/M doc-risk items appropriate
- KRL-02-IF-COMPOUND-BOOLEAN needs manual grammar verification before dispatch; grammar inspection suggests the issue may already be fixed
- PROV agent could not run `mvn verify` locally — Maven plugin bootstrap circular dependency in container environment; CI is the authoritative gate
