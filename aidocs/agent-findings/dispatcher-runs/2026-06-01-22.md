---
fire: "2026-06-01-22"
dispatcher: claude-sonnet-4-6
fired_at: "2026-06-01T22:15Z"
---

# Dispatcher run 2026-06-01-22

## Rows considered

Scanned `aidocs/16-dispatcher-backlog.md` for `status: queued` rows with `risk: low` or `risk: medium`. Cross-referenced open PRs (47 open) to identify in-flight rows.

**Recent commits (last 2 hours):**
- `3a4cf69` chore(dispatcher): update 2026-06-01-21 log — PLACEHOLDER-REPLACE-P10c PR #1710
- `1c34c10` chore(dispatcher): update 2026-06-01-21 log — SNAPSHOTS-DIFF-NAV-01 PR #1709
- `2f0690d` chore(dispatcher): log fire 2026-06-01-21 — J1e-PR-06, PLACEHOLDER-REPLACE-P10c, SNAPSHOTS-DIFF-NAV-01
- `89958b9` ops(dispatcher): fire 2026-06-01-20 — UX-WALK-08, UX-WALK-07

**In-flight rows confirmed (open PRs, skipped):**
- PLACEHOLDER-REPLACE-P10c → PR #1710
- SNAPSHOTS-DIFF-NAV-01 → PR #1709
- J1e-PR-06, J1e-PR-08, J1e-PR-09 → PRs #1708, #1703
- UX-WALK-07, UX-WALK-08 → PRs #1707, #1706
- PROV-CAPTURE-READS-FLIP → PR #1705
- DB-BP1 → PR #1704
- SCENEGRAPH-CREATE-FROM-URDF-2-FE → PR #1702
- AI1c-UI, J1d-UI → PR #1701

## Rows dispatched

| Row ID | Branch | PR | Status |
|---|---|---|---|
| DT1-DAO-FRESH-SESSION | `DT1-DAO-FRESH-SESSION-choke03` | [#1715](https://github.com/noheton/shepard/pull/1715) | ✅ opened |
| SCENEGRAPH-LIST-1 (FE follow-up) | `SCENEGRAPH-LIST-1-fe-data-table` | [#1714](https://github.com/noheton/shepard/pull/1714) | ✅ opened |
| DOC-STAGE3 | `DOC-STAGE3-per-stage-filter` | [#1713](https://github.com/noheton/shepard/pull/1713) | ✅ opened |

## Rows skipped

- **P17b** — already done (status=done in backlog); the baseline-move note inside P17b is an inline follow-up without its own row ID
- **DB-BP1** — already in-flight (PR #1704)
- **SCENEGRAPH-CREATE-FROM-URDF-2-FE** — already in-flight (PR #1702)
- **PROV-CAPTURE-READS-FLIP** — already in-flight (PR #1705)
- **DB-OPT5-NESTED** — queued but backlog explicitly notes "Not blocking. Surface when a real caller asks." — deferred until a real caller request surfaces
- **RDM-003** — M complexity, requires both a new `GET /v2/admin/publications` backend endpoint and a frontend admin table; exceeds single-session sub-agent scope for this fire

## Notes

- **DT1-DAO-FRESH-SESSION (#1715):** Fixes the CHOKE-03 startup-ordering NPE risk across `DigitalTwinSceneDAO`, `CoordinateFrameDAO`, `JointDAO`. Mirrors `JupyterConfigDAO` / `SqlTimeseriesConfigDAO` pattern (`NeoConnector.getInstance().getNeo4jSession()` on every invocation). 18 new unit tests (6 per DAO) covering appId minting, null-factory guard, and finder coverage. Gating condition met: SCENEGRAPH-REST-1 backend is confirmed shipped.
- **SCENEGRAPH-LIST-1 FE (#1714):** Completes the frontend follow-up of the backend-shipped SCENEGRAPH-LIST-1 row. `frontend/pages/scene-graphs/index.vue` now shows a `v-data-table-server` with name, appId, frame count, joint count, and last-updated columns. New `useSceneGraph.list()` composable method, `isPlausibleAppId()` util, and 16 unit tests.
- **DOC-STAGE3 (#1713):** `scripts/regenerate-doc-stage-index.py` now emits `aidocs/01-doc-stage-index.json` in addition to the existing markdown index. A `frontend/public/doc-stage-index.json` mirror enables the new `frontend/pages/doc-stages/[stage].vue` dynamic route to `useFetch()` the data at runtime. Shows per-stage doc table with title, path, last-stage-change. Both typecheck and lint pass.
