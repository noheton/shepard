---
stage: fragment
last-stage-change: 2026-05-29
---

# Dispatcher run — 2026-05-29 17:xx UTC

**Trigger:** cron `0 * * * *`  
**Considered:** 18 rows  
**Dispatched:** 3  
**Skipped:** 15  
**Recent commits at fire time:** `06501aa`, `07a4d13`, `568cde5`, `1f0d3f5`, `d16fd8b`

---

## Dispatched

| Row | Title | PR | Branch | Size |
|-----|-------|----|--------|------|
| PERF11 | SSR payload preloading for collection + home composables | [#1586](https://github.com/noheton/shepard/pull/1586) | `PERF11-ssr-preload` | M |
| RDM-003 | `/admin/publications` list UI — read-only PID inventory | [#1587](https://github.com/noheton/shepard/pull/1587) | `RDM-003-admin-publications` | M |
| FE-BUILD-03-REGEN | Retire stale `as unknown as` cast in `usePagedDataObjects` | [#1585](https://github.com/noheton/shepard/pull/1585) | `FE-BUILD-03-REGEN-remove-cast` | S |

---

## Skipped

| Row | Reason |
|-----|--------|
| PERF8 | Already done — `CollectionLineageGraph.vue` already uses dagre layout (`layout: "none"`) with pre-computed positions; `NODE_CAP = 150`; semantic-zoom (Idea D) deferred. Row should be closed. |
| PERF13 | Gated on PERF11 (now dispatched as #1586); eligible for next fire once PERF11 merges. |
| L2e | BREAKING — drops `/v1/` long-id paths + permission-service cache key flip; excluded per hard constraint. |
| F1 / P5 | Auth/permissions refactor (`PermissionsService.isAllowed` annotation seam); excluded per hard constraint. |
| ID-MIG1 / ID-MIG2 / ID-MIG3 | `appId → shepardId` rename arc; excluded per hard constraint. |
| IOT1 | M-size; requires fetching + SHA-256-pinning external TTL bundles (SOSA/SSN, iot-lite, QUDT); needs network access to W3C/QUDT hosts — not safe for a headless agent fire. |
| DOC-STAGE3 | M-size Pages site route; lower priority than the three dispatched; no urgent need. |
| V16-IMPL-1/4/5/7 | L-size ingest script work; excluded (L-size rule). |
| BATCH-API-1 | M-size design + spec pass; lower value than the dispatched items for this fire. |
| PRE-MUT-SNAP1/2/4 | M-size; multi-substrate snapshot contract — high coordination cost, would benefit from a design review pass before dispatch. |
| TSDB-DDL-2 | M-size TimescaleDB chunk-aware delete path; gated on TSDB-DDL-1 being verified first. |
| DB-OPT5-NESTED | S-size; low urgency ("surface when a real caller asks"); deferred. |

---

## Notes

- PERF8 row should be flipped to `done` in a clean-up pass; the dagre migration already happened (part of an earlier PR that added `@dagrejs/dagre` import and `dagreLayout()` to `CollectionLineageGraph.vue`).
- FE-BUILD-03-REGEN agent found the cast did not exist in the current `main` state (was already removed/never added in this session's snapshot of main); agent instead properly updated the client interface (`ListDataObjectsV2Request` + `listDataObjectsWithCount`) and composable from scratch. PR #1585 is structurally correct.
- PERF11 agent fixed 5 pre-existing test failures in `useFetchRecentCollections.test.ts` as a side-effect; all 339 Vitest tests pass.
- RDM-003 shipped full backend + frontend + tracker updates + docs in one PR.
