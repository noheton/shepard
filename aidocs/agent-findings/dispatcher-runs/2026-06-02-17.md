---
fire: 2026-06-02-17
utc: "2026-06-02 17:21"
dispatched: 2
skipped: 7
recent_commits: 4
---

# Dispatcher run 2026-06-02-17

## Summary

| | Count |
|---|---|
| Backlog rows considered | ~3,301 lines surveyed |
| In-flight PRs excluded | 32 open PRs de-duped |
| Pre-implemented (stale rows) | 6 flagged (SCENEGRAPH-NAV-01/02, SHAPES-V-PREFILL-2-RDF-ENDPOINT, PLACEHOLDER-REPLACE-TPL3a-lite, PLACEHOLDER-REPLACE-FE-PROV-INSTANCE-REGISTRY, PLACEHOLDER-REPLACE-TS-SEMANTIC-REST) |
| Dispatched this run | 2 |
| Failures | 0 |

---

## Recent commits (last 2 h)

- `e2bba33` chore(dispatcher): run log 2026-06-02-16 — 3 dispatched (PROV-CAPTURE-READS-FLIP, D1c help pages, UX access chip)
- `920d9b0` docs(mffd): Collection layout + wiki transformation + Programme UI design bundle
- `1d84b61` docs(J1e-PR-08): correct J1e row in aidocs/44 — plugin, not in-core
- `0667d95` chore(dispatcher): run log 2026-06-02-15 — 3 dispatched

---

## In-flight PRs excluded (32 total, sampled)

Key rows excluded: `D1c` (#1740), `PROV-CAPTURE-READS-FLIP` (#1739),
`BUG-PREDECESSOR-IDS-NUMERIC-IN-V2-PATCH` (#1738), `MFFD-IMAGEBUNDLE-PANE-MOUNT-1` (#1737),
`J1e-PR-08-09-doc-cleanup` (#1736), `M4I-d-3-followup` (#1733),
`SCENEGRAPH-CREATE-FROM-URDF-2-FE` (#1732), `PERF4d` (#1731),
`MCP-COV-07-SEMANTIC-SPARQL` (#1730), `PLACEHOLDER-REPLACE-FS1e1` (#1729),
`UX-WALK-2026-05-29-05` (#1728), `SEMANTIC-ANNOTATE-BULK-REST-1` (#1727),
`UI21-BACKEND-Q` (#1726), `MCP-COV-01-AUDIT` (#1725), `REF-EDIT-4` (#1724),
`J1e-PR-06-AUTOFETCH-02` (#1723), `UI7` (#1722), `KRL-CONFIG-1` (#1721),
`BE-USERS-ME-ROLES-1` (#1720), `TPL-ME-USE-FROM-BROWSE` (#1719),
`SINGLETON-FILE-02-SEED-MIGRATION` (#1717), `FRONTEND-LINT-DEBT-04` (#1716),
`DT1-DAO-FRESH-SESSION` (#1715), `SCENEGRAPH-LIST-1` (#1714), `DOC-STAGE3` (#1713),
`PLACEHOLDER-REPLACE-P10c` (#1710), `SNAPSHOTS-DIFF-NAV-01` (#1709),
`UX-WALK-2026-05-29-08` (#1706, was dispatched last run).

---

## Rows dispatched

### 1. UX-WALK-2026-05-29-07

| Field | Value |
|---|---|
| Row ID | `UX-WALK-2026-05-29-07` |
| Risk | low |
| Size | S (frontend-only) |
| Branch | `UX-WALK-2026-05-29-07-trace3d-cta` |
| PR | [#1707](https://github.com/noheton/shepard/pull/1707) |
| Scope | `frontend/pages/containers/timeseries/[containerId]/index.vue` + new unit test |

Add **"Visualize in 3D"** button (`mdi-cube-outline`) to the Channel Overview
expansion panel's `#append` slot on the Timeseries container page. Only shown
when at least one channel is loaded and the panel is not in channel-edit mode.
Mounts `ViewRecipeBuilderDialog` pre-wired with `startNs=0` /
`endNs=Date.now() * 1_000_000` (full-history window; user refines inside the
dialog). 10 Vitest unit tests — all pass. `npm run typecheck` + `npm run lint`
clean. Note: the branch was a pre-existing stub from 2026-06-01; the agent
updated it with the working implementation.

---

### 2. BUG-COLL-APPID-ROUTE-006

| Field | Value |
|---|---|
| Row ID | `BUG-COLL-APPID-ROUTE-006` |
| Risk | medium |
| Size | M (backend + frontend) |
| Branch | `BUG-COLL-APPID-ROUTE-006-treeview-list-v2` |
| PR | [#1741](https://github.com/noheton/shepard/pull/1741) |
| Scope | `DataObjectV2Rest.java`, `DataObjectV2RestTest.java`, `useTreeviewItems.ts`, new unit test |

Fixes the broken treeview initial-load for UUID-v7-only Collections. Backend
adds `?parentAppId=<appId|NONE>`, `?predecessorAppId`, `?successorAppId` query
params to `GET /v2/collections/{collectionAppId}/data-objects`; the DAO's -1
sentinel for root-level items is preserved. Frontend swaps `getAllDataObjects`
(v1 numeric path, 400s for UUID Collections) for a raw-fetch to the v2
endpoint. 14 new frontend Vitest tests pass; frontend lint + typecheck clean.
⚠️ **Backend `mvn verify` was not run in the worktree** (no JVM in the
container) — CI will be the gate; 3 new `DataObjectV2RestTest` cases validate
the new params.

---

## Rows skipped

| Row ID | Reason |
|---|---|
| UX-WALK-2026-05-29-08 | In-flight from last fire (#1706) |
| COLL-TIMELINE-DRILLDOWN-FILTER-1 | S but requires server-side annotation filter + date range on DataObjectV2Rest.list — good follow-up candidate when the 32-PR queue shrinks |
| FE-BUILD-03-REGEN | Requires running backend server to regenerate OpenAPI client — cannot run without live instance |
| J1e-PR-06-AUTOFETCH-03 | Dependent on J1e-PR-06-AUTOFETCH-02 landing first (PR #1723 still open) |
| SHAPES-V-PREFILL-3-EXTRACT-SHACL | M, needs design doc update before implementation |
| BUG-COLL-APPID-ROUTE-PERMS-1 | M, requires backend v2 PermissionsRest scaffold — architectural |
| ONTOLOGY-BROWSE-NONADMIN-01 | M, queued but vocabulary browse already accessible at `/semantic/vocabularies` — may already be done; needs verification before dispatch |

## Stale-row observations (for backlog housekeeping)

The following rows appear to be pre-implemented but remain `queued` in
`aidocs/16`:
- `SCENEGRAPH-NAV-01` — scene-graphs index page exists at
  `frontend/pages/scene-graphs/index.vue`
- `SCENEGRAPH-NAV-02` — `OpenInSceneGraphButton.vue` ships the conditional
  URDF/RDK affordance
- `SHAPES-V-PREFILL-2-RDF-ENDPOINT` — `DataObjectRdfRest.java` exists
- `PLACEHOLDER-REPLACE-TPL3a-lite` — `AdminOntologyAlignmentPane.vue` shipped
  2026-05-31 (noted in registry comments)
- `PLACEHOLDER-REPLACE-FE-PROV-INSTANCE-REGISTRY` — shipped 2026-05-31
- `PLACEHOLDER-REPLACE-TS-SEMANTIC-REST` — `ChannelAnnotationsPane` is mounted;
  placeholder no longer active

Recommend a housekeeping pass to mark these done in `aidocs/16` and flip
`aidocs/44` status symbols accordingly.
