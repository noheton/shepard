---
stage: deployed
last-stage-change: 2026-06-03
---

# Single-file FileBundleReference audit — 2026-06-03

Audit of `:FileBundleReference` rows that hold exactly one file — the technical debt described in CLAUDE.md "Always: singleton FileReference for one-file uploads; FileBundleReference only when bundling >1".

Run at: `2026-06-03T04:59:46.973425+00:00` against `bolt://localhost:7687`.

## Top line

| Metric | Count |
|---|---|
| `:FileBundleReference` (total) | 53 |
| `:SingletonFileReference` (total) | 63 |
| Singleton-shaped bundles (debt) | **0** |
| Singletons minted by V23 (`legacyV23Singleton=true`) | 0 |

**No single-file FileBundleReferences found.** The instance is either fully migrated (V23 has already run, or every existing bundle genuinely holds 0 or >1 files) or has no FileBundle payload yet. No backfill action required.

## Per-collection breakdown

_No candidates — table omitted._

## Per-extension breakdown

_No candidates — table omitted._

## Collection × extension matrix

_No candidates — matrix omitted._

## Recommended next steps

1. Mark `SINGLETON-FILE-01-AUDIT` and `SINGLETON-FILE-03-BACKFILL-SCRIPT` as **no-op** for this instance (re-run the audit before the next backfill window).
2. Land `SINGLETON-FILE-04-UI-DEFAULT` so the singleton-shape default prevents new debt from forming.
3. Land `SINGLETON-FILE-05-SHACL-GUARD` so the completeness widget flags any debt that does appear.

---

Audit generated by `scripts/audit-single-file-bundles.py` (SINGLETON-FILE-01-AUDIT).
