---
stage: deployed
last-stage-change: 2026-05-23
---

# Fork vs Upstream — Feature Matrix

**Status.** **Live.** Updated whenever a feature ships, a design doc
lands, or upstream cuts a new release.
**Snapshot date.** 2026-05-19.
**Upstream baseline.** `gitlab.com/dlr-shepard/shepard 5.2.0`.

This is the **progress tracker** comparing what's available in this
fork (`noheton/shepard main`) against the upstream 5.2.0 surface,
broken down by feature area. **Different from `aidocs/34`** — that
doc is admin-facing ("what an upgrader needs to know about each
landed change"); this doc is **contributor / PI-facing** ("how does
this fork compare across the whole feature surface, including
designed-not-yet-shipped work").

## Status legend

| Symbol | Meaning |
|---|---|
| **✓** | **Feature-complete** — backend logic shipped AND UI accepted on `main` |
| **⚙** | **Dev-track** — backend shipped; UI missing or not yet accepted. Not feature-complete. |
| **🅰️** | **Alpha (UI placeholder)** — backend shipped; UI surface is a placeholder stub from `frontend/components/common/placeholder/` (per CLAUDE.md "a feature with a placeholder UI is `alpha`, not `beta` and not `shipped`", codified 2026-05-30). Distinct from **⚙** because there IS a UI present — it's a stub mount, not a missing surface; the work is to replace the stub with a real implementation. Tracked under `PLACEHOLDER-REPLACE-<ID>` in `aidocs/16`. |
| **📐** | Designed (design doc landed; implementation queued) |
| **🚧** | Implementation in flight (agent dispatched / PR open) |
| **=** | Parity with upstream (same shape on both sides) |
| **↑** | This fork extends upstream (we ship more) |
| **—** | Not implemented anywhere |
| **⚠** | Diverges deliberately — see notes |

**Feature-complete definition:** A feature reaches **✓** only when both (a) the backend logic
is on `main` and (b) the UI surface is accepted — the full user-facing flow works end to end.
Backend-only rows stay at **⚙** until the UI ships and is verified. This prevents the matrix
from over-counting progress.

## Dev track

Features with complete backend logic but no accepted UI. These are **not** feature-complete
and should be prioritised for UI work before being promoted to **✓**.

| Feature | Backend slice | UI gap | Refs |
|---|---|---|---|
| **AI1c** Channel quality scoring | `qualityScore ∈ [0,1]` field computed + stored on `TimeseriesReference` | Score never displayed in the TS reference panel or chart header | `aidocs/43 §3.2` |
| **P21-V2-METADATA-EDIT** Metadata full-replace | `PUT /v2/containers/{appId}` — full-replace of `name`+`status`; `name` required, `status` null-cleared when absent. `PUT /v2/references/{appId}` — full-replace of `name` + kind-specific mutable fields; `name` required. Default `ReferenceKindHandler.put()` delegates to `patch()`; handlers may override for strict null-reset. 18 new JUnit tests across 4 test classes. | `EditContainerNameDialog.vue` — rename button + dialog wired into timeseries + file container pages (slice 2 in PR); calls `GET /v2/containers/{appId}` on open to preserve status before `PUT`. Reference rename dialog (`EditFileReferenceDialog`, REF-EDIT-3) already shipped. | `aidocs/16 P21-V2-METADATA-EDIT` |
| **TM1a** Time-reference model | `PATCH /v2/timeseries-references/{appId}` — `timeReference` + `wallClockOffset` + `wallClockOffsetSource` fields | No edit UI in the TS reference detail page; fields invisible to users | `aidocs/16 TM1a` |
| **N1f** SPARQL proxy | `GET/POST /v2/semantic/{repoAppId}/sparql` — read-only SELECT/ASK against n10s; 37+21 unit tests | No query interface anywhere in the frontend | `aidocs/16 N1f` |
| **A5a/A5b/A5c/A5d** HDF5 containers + per-DataObject references | HdfContainer CRUD + permission bridge + offline download + HdfReference per-DataObject anchor (`/v2/data-objects/{appId}/hdf-references` GET/POST/DELETE) | No HDF container UI (list, create, browse datasets, download); HdfReferencesPane wired to DataObject detail | `aidocs/35` + `aidocs/63 ADR-0020` |
| **A5c-annotation** HDF5 annotation affordance | `hdf` vocabulary (V87) + `hdf-shapes.ttl` SHACL shape + Annotate button in HdfReferencesPane | — | `aidocs/16` A5c-annotation |
| ~~**PV1a/PV1b** Payload version history~~ | ~~`GET /v2/{file,structured-data}-containers/{id}/files/{name}/versions` — per-upload SHA-256 + version counter~~ | **Promoted to ✓** — `PayloadVersionHistoryDialog.vue` + `useFetchPayloadVersions` + 8 Vitest tests shipped; history icon on every file row; >100 MB confirm guard | `aidocs/46` |
| **FS1e1/FS1e2** File storage migration | `POST /v2/admin/files/migrate` + status endpoint + auto-sweep scheduler | No migration trigger or progress UI in AdminStoragePane; CLI only | `aidocs/45 §6` |
| ~~**F3** Permission audit log~~ | ~~`GET /v2/admin/permission-audit/log` — GRANT/UPDATE/REVOKE events with actor/entityAppId/from/to filters~~ | **Promoted to ✓** — `PermissionAuditLogPane.vue` + `useFetchPermissionAuditLog` + `AdminPermissionAuditApi` shipped; filters for entityAppId/actor/from/to; paginated table with action chips | `aidocs/16 F3` |
| ~~**#68** Admin activity log viewer~~ | ~~`GET /v2/provenance/activities` — instance-wide provenance stream; admins see all rows unfiltered~~ | **Promoted to ✓** — `AdminActivityLogPane.vue` + `useFetchAdminActivities` composable shipped; server-side agent/targetKind/targetAppId filters; client-side text + action-kind chip filters; load-more pagination (100 batch); density-compact `v-table` with action-kind chips + status colour coding; `AdminFragments.ACTIVITY_LOG` + menu entry + landing card wired in `admin/index.vue` | `aidocs/16 #68` |
| **APISIMP-PROV-ISO8601-TIMESTAMPS** — `/v2/provenance/*` `since`/`until` params accept ISO 8601 instants (`2026-01-01T00:00:00Z`) alongside legacy epoch-ms | n/a (fork-only `/v2/` surface; no upstream provenance REST) | **✓ shipped (fire-313)** — `parseTimestamp()` static helper: `contains('T')` → `Instant.parse()`, else `Long.parseLong()`; invalid input → 400 `application/problem+json`; 9 endpoints updated; 11-test `ProvenanceRestTimestampTest` added. No wire break — epoch-ms callers unchanged. | **✓ ↑** | `aidocs/16` APISIMP-PROV-ISO8601-TIMESTAMPS |
| **APISIMP-ANNOTATION-MERGE-PATCH-HTTP** — `PATCH /v2/annotations/{appId}` (was `PUT`); method-level `@Consumes({"application/merge-patch+json", …})` added | n/a (fork-only `/v2/` annotation surface; upstream has no `/v2/annotations` REST) | **✓ shipped (fire-315)** — `@PUT` → `@PATCH` on `SemanticAnnotationV2Rest.update()`; method-level `@Consumes({"application/merge-patch+json", MediaType.APPLICATION_JSON})` added; activity summary updated from `"PUT /v2/annotations/…"` to `"PATCH /v2/annotations/…"`; 2 new reflection tests (`update_methodAnnotationIsPatch_notPut`, `update_consumesIncludesMergePatchJson`). `PUT /v2/annotations/{appId}` now returns 405; callers must use `PATCH`. Wire body shape unchanged. | **✓ ↑** | `aidocs/16` APISIMP-ANNOTATION-MERGE-PATCH-HTTP; `aidocs/34` APISIMP-ANNOTATION-MERGE-PATCH-HTTP row |
| **APISIMP-REFS-LIST-NO-XTOTALCOUNT** — `GET /v2/references` now includes `X-Total-Count` response header | n/a (fork-only `/v2/` surface) | **✓ shipped (fire-473)** — added `.header("X-Total-Count", total)` to `ReferencesV2Rest.list()` return; updated `@APIResponse` description to standard pattern; added `list_returnsXTotalCountHeader` unit test. Additive — no wire-shape break. | **✓ ↑** | `aidocs/16` APISIMP-REFS-LIST-NO-XTOTALCOUNT; `aidocs/34` APISIMP-REFS-LIST-NO-XTOTALCOUNT row |
| **APISIMP-XCOUNT-OPENAPI-HEADER** — 38 v2 list endpoints now formally declare `X-Total-Count` in the OpenAPI `@APIResponse` spec | n/a (fork-only `/v2/` surface; OpenAPI annotation only) | **✓ shipped (fire-479, PR #2411)** — added `headers = @Header(name="X-Total-Count", schema=@Schema(type=SchemaType.INTEGER))` to all 38 `@APIResponse(responseCode="200")` blocks for paginated list endpoints. All endpoints already emitted the header at runtime; the formal declaration makes it visible to OpenAPI-generated clients. Batch covered 37 in-tree REST classes + 2 bonus `ContainersV2Rest` endpoints missed by the 80-line backward search. Plugin files deferred to APISIMP-XCOUNT-OPENAPI-HEADER-2. | **✓ ↑** | `aidocs/16` APISIMP-XCOUNT-OPENAPI-HEADER; `aidocs/34` APISIMP-XCOUNT-OPENAPI-HEADER row |
| **APISIMP-XTOTALCOUNT-DUAL-EMIT-DROP** — `X-Total-Count` response header removed from all dual-emit v2 list endpoints; `PagedResponseIO.total` (body field) is the sole SSOT | n/a (fork-only `/v2/` surface; `DataObjectV2Rest.listDataObjects` excluded) | **✓ shipped (fire-546)** — removed `.header("X-Total-Count", ...)` from ~35 v2 REST source files (~51 call sites) that already returned `PagedResponseIO` body with `.total`; removed stale `@Header` OpenAPI annotations; removed unused `Header`/`SchemaType` imports; converted ~45 test assertions from header-checks to `body.total()` checks. `DataObjectV2Rest.listDataObjects` (plain-array, no `PagedResponseIO`) still emits the header. Follow-up `APISIMP-XTOTALCOUNT-LIST-DO` (queued) will close that final case. | **✓ ↑** | `aidocs/16` APISIMP-XTOTALCOUNT-DUAL-EMIT-DROP; `aidocs/34` APISIMP-XTOTALCOUNT-DUAL-EMIT-DROP row |
| **APISIMP-XTOTALCOUNT-LIST-DO** — `X-Total-Count` response header removed from `DataObjectV2Rest.listDataObjects` and 5 sibling endpoints; `Content-Range` is the sole pagination metadata header for the plain-array endpoint | n/a (fork-only `/v2/` surface) | **✓ shipped (fire-547)** — `usePagedDataObjects.ts` `parseTotalCount()` simplified to `Content-Range`-only; `.header("X-Total-Count", ...)` removed from `list()` (main + 3 early-return branches) and from `predecessors`, `successors`, `children`, `predecessorChain`, `successorChain`; stale `@Header` OpenAPI annotations removed from all 6 methods; `DataObjectV2RestTest` updated (now asserts header absent); `usePagedDataObjects.dbopt5.test.ts` mock switched to `Content-Range`. `grep -rn 'X-Total-Count' backend/src/main/java/de/dlr/shepard/v2/dataobject/` returns empty. | **✓ ↑** | `aidocs/16` APISIMP-XTOTALCOUNT-LIST-DO; `aidocs/34` APISIMP-XTOTALCOUNT-LIST-DO row |
| **APISIMP-DO-DEPRECATED-COUNT-FIELDS** — 3 deprecated count fields suppressed from `GET /v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}` response | n/a (fork-only `/v2/` surface) | **✓ shipped (fire-553)** — added `"timeseriesReferenceCount"`, `"fileBundleCount"`, `"structuredDataReferenceCount"` to `@JsonIgnoreProperties` on `DataObjectDetailV2IO`; these fields were declared `@Deprecated` on the parent `DataObjectIO` and replaced by v2-canonical fields on `DataObjectListItemV2IO`; no current frontend or known caller reads them from the detail endpoint. One-line annotation change; no migration. | **✓ ↑** | `aidocs/16` APISIMP-DO-DEPRECATED-COUNT-FIELDS; `aidocs/34` APISIMP-DO-DEPRECATED-COUNT-FIELDS row |
| **APISIMP-NOTEBOOK-LIST-PAGINATE** — `GET /v2/lab-journal/{appId}/notebooks` response wrapped in `PagedResponseIO` envelope; `?page` and `?pageSize` params added | n/a (fork-only `/v2/` surface) | **✓ shipped (fire-554)** — `NotebookRest.listNotebooks` now accepts `@QueryParam("page") @DefaultValue("0") @PositiveOrZero int page` + `@QueryParam("pageSize") @DefaultValue("50") @Min(1) @Max(200) int pageSize`; in-memory result list sliced and wrapped in `new PagedResponseIO<>(pageItems, total, page, pageSize)`; response body changes from bare array to `{"items":[…],"total":N,"page":0,"pageSize":50}`; `useFetchNotebooks.ts` was already reading `.items` from a `PagedResponse` type in anticipation. No DAO change needed — full list is assembled in-memory from FR1b singletons + FR1a bundles. No migration. | **✓ ↑** | `aidocs/16` APISIMP-NOTEBOOK-LIST-PAGINATE; `aidocs/34` APISIMP-NOTEBOOK-LIST-PAGINATE row |
| **APISIMP-PROBLEM-GENERIC-THROWS** — direct JAX-RS exception throws in `FileMigrationRest` (6) and `SqlTimeseriesRest` (2) replaced with `problem()` returning resource-specific RFC 7807 type URIs | n/a (fork-only `/v2/` surface; error-body enrichment only) | **✓ shipped (fire-555, PR #2493)** — replaced 8 throw sites with `return problem(PT_*, ...)` calls; added 6 `PT_*` constants to `FileMigrationRest` and 2 to `SqlTimeseriesRest`; removed now-unused `BadRequestException`/`NotFoundException`/`WebApplicationException` imports. HTTP status codes unchanged. `type` URI in error responses changes from generic (`/problems/bad_request`, `/problems/not_found_entity`) to resource-specific (e.g. `/problems/file-migration.rollback.not-found`). | **✓ ↑** | `aidocs/16` APISIMP-PROBLEM-GENERIC-THROWS; `aidocs/34` APISIMP-PROBLEM-GENERIC-THROWS row |
| **APISIMP-BASIC-ENTITY-ID-HIDE** — Neo4j internal node-id (`"id"`) suppressed from `FileGroupIO` v2 responses (`/v2/bundles/…/groups/…`) | n/a (fork-only `/v2/` surface) | **✓ shipped (fire-556, branch `APISIMP-BASIC-ENTITY-ID-HIDE`)** — `FileGroupIO extends BasicEntityIO` changed to `FileGroupIO extends BasicEntityV2IO` (which carries `@JsonIgnoreProperties({"id"})`). All other v2 IO classes already suppressed `id`; this closes the last gap. Unit test `fileGroupIO_serialisationOmitsNumericId` added. No v1 surface touched. | **✓ ↑** | `aidocs/16` APISIMP-BASIC-ENTITY-ID-HIDE; `aidocs/34` APISIMP-BASIC-ENTITY-ID-HIDE row |
| **APISIMP-SEMANNT-NUMERIC-IDS** — `SemanticAnnotationIO` numeric vocabulary IDs replaced with UUID v7 fields | n/a (fork-only `/v2/` surface; v1 `SemanticAnnotation` schema unchanged) | **✓ shipped (fire-558, PR #2495, sha `aea04658`)** — added `propertyVocabularyEntryAppId` + `valueVocabularyEntryAppId` (String, nullable) to `SemanticAnnotationIO`; populated from `SemanticRepository.getAppId()`. `propertyRepositoryId`/`valueRepositoryId` deprecated with `@Deprecated @Schema(deprecated=true)`. Frontend `AnnotationToAdd` type drops the numeric IDs; `AnnotatedChannel.addAnnotation` fills compat shim until next client regen. Tests updated. | **✓ ↑** | `aidocs/16` APISIMP-SEMANNT-NUMERIC-IDS; `aidocs/34` APISIMP-SEMANNT-NUMERIC-IDS row |
| **APISIMP-CHANNEL-SHEPARDID-NORMALIZE** — normalize `{shepardId}` → `{channelShepardId}` path-param in `ContainersV2Rest` data/ingest endpoints | n/a (pure Java variable rename; zero wire-shape change) | **🔄 PR open (fire-558, branch `APISIMP-CHANNEL-SHEPARDID-NORMALIZE`)** — renamed `@PathParam("shepardId")` on two data/ingest endpoints to match `{channelShepardId}` used on annotation endpoints in the same class. | **🔄** | `aidocs/16` APISIMP-CHANNEL-SHEPARDID-NORMALIZE; `aidocs/34` APISIMP-CHANNEL-SHEPARDID-NORMALIZE row |
| **APISIMP-ACTIVITY-EPOCH-MS-TO-ISO** — `ActivityIO.startedAtMillis`/`endedAtMillis` renamed to `startedAt`/`endedAt` (ISO 8601 `String`) | n/a (fork-only `/v2/provenance/activities` surface) | **✓ shipped (fire-577, PR #2536)** — `ActivityIO`: `Long startedAtMillis`→`String startedAt`, `Long endedAtMillis`→`String endedAt`; `toIso(Long)` helper via `DateTimeFormatter.ISO_INSTANT`; `from()`, `metadataOnly()`, `relationsOnly()` updated. `ProvenanceMcpTools.toActivityMaps()` updated to emit ISO strings + `toIso(Long)` helper + tool docstrings. `X-Next-Cursor` emit sites in `ProvenanceRest` fixed to parse ISO string back via `Instant.parse(...).toEpochMilli()`. Wire break on response field names (`startedAtMillis`→`startedAt`, `endedAtMillis`→`endedAt`) for `GET /v2/provenance/activities`. | **✓ ↑** | `aidocs/16` APISIMP-ACTIVITY-EPOCH-MS-TO-ISO; `aidocs/34` APISIMP-ACTIVITY-EPOCH-MS-TO-ISO row |
| **APISIMP-ONTOLOGY-ALIGNMENT-EPOCH-MS-TO-ISO** — `OntologyAlignmentIO.createdAt` changed from epoch-ms `Long` to ISO 8601 `String` | n/a (fork-only `/v2/semantic/ontology/alignment` admin surface) | **🔄 PR open (fire-578, branch `APISIMP-ONTOLOGY-ALIGNMENT-EPOCH-MS-TO-ISO`)** — changed field type to `String`; added `toIso(Long)` helper; `@Schema` description updated; `OntologyAlignmentRestTest` updated to assert ISO string values. Wire break on `createdAt` field type in `GET /v2/semantic/ontology/alignment` response (numeric ms → ISO string). | **🔄** | `aidocs/16` APISIMP-ONTOLOGY-ALIGNMENT-EPOCH-MS-TO-ISO; `aidocs/34` APISIMP-ONTOLOGY-ALIGNMENT-EPOCH-MS-TO-ISO row |
| **APISIMP-COLLECTION-EVENT-EPOCH-MS-TO-ISO** — `CollectionEventIO.timestamp` changed from epoch-ms `long` to ISO 8601 `String` in the SSE collection change-feed | n/a (fork-only `/v2/collections/{appId}/events` SSE surface; P13 feature) | **✓ shipped (fire-581, PR #2539 `8811b402`)** — changed field type to `String`; added public `toIso(long)` static helper; updated 6 call sites in `CollectionEventProducer` (4) and `CollectionEventBus` (2); `CollectionEventsRest` `@Operation` description updated; frontend `useCollectionEvents.ts` `timestamp: number` → `string`; test fixtures updated; 7 new JUnit tests in `CollectionEventIOTest`. Wire break on `timestamp` field type in each SSE data payload (numeric ms → ISO string). | **✓ ↑** | `aidocs/16` APISIMP-COLLECTION-EVENT-EPOCH-MS-TO-ISO; `aidocs/34` APISIMP-COLLECTION-EVENT-EPOCH-MS-TO-ISO row |
| **APISIMP-PROVENANCE-STATS-EPOCH-MS-TO-ISO** — `ProvenanceStatsIO.sinceMillis`/`untilMillis` renamed `since`/`until` (ISO 8601 `String`) | n/a (fork-only `/v2/provenance/stats` surface) | **🔄 PR open (fire-581, branch `APISIMP-PROVENANCE-STATS-EPOCH-MS-TO-ISO`)** — renamed `sinceMillis`→`since`, `untilMillis`→`until`; changed type `long`→`String`; added `public static String toIso(long)` helper; `ProvenanceStatsService.compute()` calls `toIso()`. Wire break on `since`/`until` field names + type (numeric ms → ISO string). `bucketMillis`, `buckets`, `cumulative` unchanged. `ProvenanceStatsIOTest` (2 tests) guards the helper. | **🔄** | `aidocs/16` APISIMP-PROVENANCE-STATS-EPOCH-MS-TO-ISO; `aidocs/34` APISIMP-PROVENANCE-STATS-EPOCH-MS-TO-ISO row |
| **APISIMP-PLUGIN-IO-SCHEMA-MISSING** — class-level `@Schema(name, description)` added to 34 plugin wire-IO classes | n/a (plugin OpenAPI annotation; no upstream surface affected) | **✓ shipped (fire-494, PR #2424, sha `ac87b33`)** — batch-added `@Schema(name=…, description=…)` to 34 wire IO types across 10 plugin modules. Pure OpenAPI metadata — no wire shape change. | **✓ ↑** | `aidocs/16` APISIMP-PLUGIN-IO-SCHEMA-MISSING; `aidocs/34` APISIMP-PLUGIN-IO-SCHEMA-MISSING row |
| **J1d** Lab journal edit history | `GET /v2/lab-journal/{entryAppId}/history` — ordered revision list with content snapshots | No history panel / diff viewer in `LabJournalExistingEntry.vue` | `aidocs/37` |
| ~~**PM1a–PM1e** Plugin admin~~ | ~~Full `GET/PATCH /v2/admin/plugins` REST surface + CLI~~| **Promoted to ✓** — `PluginsAdminPane.vue` shipped | `aidocs/16 PM1b` |
| ~~**P10 SQL admin config**~~ | ~~`GET/PATCH /v2/admin/config/sql-timeseries` — runtime row + duration caps (V2CONV-A4: moved from `/v2/admin/sql-timeseries/config`)~~ | **Promoted to ✓** — `AdminSqlTimeseriesPane.vue` + `useSqlTimeseriesConfig` composable + 8 Vitest tests shipped; read-only stats card + Edit dialog (maxRows / maxDuration); RFC 7396 null-to-clear supported; inline ISO-8601 duration + positive-integer validation; replaces `PlaceholderFragmentPane` in `/admin#sql-timeseries`; path collapsed onto generic `/v2/admin/config/{feature}` (V2CONV-A4, 2026-06-03) | `aidocs/29` |

## Standing rule

Per `CLAUDE.md`, this matrix updates in the same PR as any feature
landing or design-doc landing — keep it consistent with `aidocs/16`
backlog and `aidocs/00-index.md`. A row that's stale is the bug.

When a UI ships for a dev-track item: remove the row from the Dev Track table above,
promote the row in the relevant section to **✓**, and mark the matrix Snapshot date.

---

## 1. DB connectivity / health / migrations

| Capability | Upstream 5.2.0 | This fork | Status | Refs |
|---|---|---|---|---|
| Bounded `MigrationsRunner.waitForConnection` w/ exponential backoff | infinite-wait loop | configurable `shepard.migrations.connection-wait-timeout` (default `PT60S`) | **✓ ↑** | A1 / `aidocs/16` row A1 / `aidocs/17` |
| Per-DB health-check separation (startup vs runtime) | combined / coarse | per-DB `state` + `kind` in `/healthz` | **✓ ↑** | A1b |
| Graceful degradation when optional DBs (PostGIS) unavailable | endpoints hang | RFC-7807 503 + `Retry-After` when `@RequiresDatabase` not satisfied; 404 when toggle OFF | **✓ ↑** | A1c |
| `MigrationsRunner.apply()` fail-fast on `MigrationsException` | swallow + log | propagates as `RuntimeException` aborting startup | **✓ ↑** | A1e (commit `0f2f512`) |
| Automated DB recovery scheduler | none | `@Scheduled(every = "${shepard.health.recovery.interval}")` default `PT15S`; new `quarkus-scheduler` dep | **✓ ↑** | A1f |
| Parallel DB connectivity checks (startup + recovery) | sequential per-DB | `DbConnectivityWarmer` (startup) + `DbRecoveryScheduler` (recovery tick) both use Java 21 virtual threads; total wait = `max(latencies)` not `sum(latencies)` | **✓ ↑** | P1 |
| Flyway startup retry ceiling (Mongo/JDBC audit) | unbounded default (Flyway `connect-retries-interval=120s`) | `quarkus.flyway.connect-retries=10` + `connect-retries-interval=PT5S`; ≈50s ceiling aligned with Neo4j gate | **✓ ↑** | A1d (commit `e1c3635`) / `aidocs/17` |
| Migration progress monitoring endpoint | none | `GET /migrations/progress` (P3) | **✓ ↑** | P3 (commit `7cc74b8`) |
| Readiness check verifies Neo4j migration-chain integrity | none — readiness ping only checks connectivity | `neo4j-migration-chain-readiness` SmallRye check (`/shepard/api/healthz/ready`) asserts every classpath `V**` is APPLIED with a matching checksum on the live DB; delegates to `Migrations.validate()` + `Migrations.info(COMPARE)` (same code paths that gate `apply()`); cached per `shepard.health.readiness.max-staleness`; operator runbook `docs/admin/runbooks/migration-chain-integrity.md` with worked example for the UI-020 V61 splice | **✓ ↑** | OPS-MIGRATION-HEALTHCHECK (2026-05-24) / `aidocs/agent-findings/ops-migration-healthcheck-2026-05-24.md` |

## 2. Configuration / feature toggles

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| Build-time vs runtime feature toggle mechanism | `@IfBuildProperty` only | `@ConditionalOnFeature` + runtime-toggleable | **✓ ↑** | A3 |
| Runtime feature-toggle admin API (`GET /v2/admin/runtime-toggles`, `PATCH /v2/admin/runtime-toggles/{name}`) | none | `FeatureToggleRegistry` + `AdminFeaturesRest`; `@RolesAllowed("instance-admin")`; in-process override, not persisted across restart. **APISIMP-FEATURES-LIST-ENVELOPE (fire-299)** — list envelope migrated from bare JSON array to standard `PagedResponseIO<FeatureToggleIO>`; **APISIMP-FEATURES-ADMIN-BESPOKE (fire-356)** — path renamed from `/v2/admin/features` → `/v2/admin/runtime-toggles` to distinguish transient toggles from persisted `/v2/admin/config/{feature}` surface. | **✓ ↑** | A3b |
| Toggle `source` field (`"default"` / `"config"` / `"runtime"`) on each toggle entry returned by `GET /v2/admin/runtime-toggles` and `PATCH /v2/admin/runtime-toggles/{name}` | none | `FeatureToggleEntry.getSource()` — returns `"runtime"` when overridden by PATCH, `"config"` when an `application.properties` key is present, `"default"` otherwise. `FeatureToggleIO` carries the field; no new endpoints, no schema change. 3 new `AdminFeaturesRestTest` cases. | **✓ ↑** | DX7 |
| Spatial-data namespace alias (`shepard.spatial-data.*` → `shepard.infrastructure.spatial.*`) | only old names | both names resolve; old logs deprecation warning; removal v6.0 | **✓ ↑** | A3c / `aidocs/platform/A3c-namespace-migration.md` |
| Permission cache TTL/max-size config | hard-coded global defaults | `shepard.permissions.cache.ttl` (`PT5M`) + `.max-size` (`10000`) | **✓ ↑** | A4 |
| Runtime-mutable provenance config (`enabled`, `captureReads`, `retentionDays`) via `:ProvenanceConfig` singleton + `GET/PATCH /v2/admin/config/provenance` | deploy-time props only | `ProvenanceConfigDescriptor` in ConfigRegistry; `ProvenanceCaptureFilter` + `ProvenanceRetentionJob` read from service at runtime | **✓ ↑** | FTOGGLE-PROV-1 |

## 3. Auth / API keys / security

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| Semi-permanent API keys with expiry (`validUntil` + JWT `exp`) | none | shipped, distinguishable 401 on expiry | **✓ ↑** | L5 (commit `30c687a`) |
| `Bearer ` prefix mangle on JWTs containing the literal substring | mangles | safe `startsWith → substring(7)` | **✓ ↑** | M4 |
| Auth-header echo to warn-level logs (token-leak) | full echo | `present`/`absent` only | **✓ ↑** | M5 |
| `~/.shepard/keys/private.key` perms | umask default | `0600` via `Files.setPosixFilePermissions` (best-effort, POSIX only) | **✓ ↑** | M2 |
| Cypher injection on user-controlled property names + IRI types | injectable | parameterised + property-name allowlist; subsumes M9 | **✓ ↑** | C5 (cherry-pick `e90bfd8`) / `aidocs/07` C5 |
| Cypher injection — second wave (`*ReferenceDAO` family + `GenericDAO` + `VersionDAO` + `SemanticAnnotationDAO`) | injectable | parameterised + named params; **L2c precondition fully cleared** | **✓ ↑** | C5b (cherry-pick `c707e56`) / `aidocs/16` C5b |
| `PublicEndpointRegistry` path-match | `startsWith`, no normalisation (path-traversal vector) | exact-match against `Path.normalize()`-normalised path; 9 regression tests; post-P4 strips the `/shepard/api/` prefix via `RequestPathHelper` first | **✓ ↑** | H5 / `aidocs/07` H5 + P4 |
| `/v2/` JAX-RS routing scaffolding | n/a | shipped — `quarkus.http.root-path=/`; resources carry explicit `@Path("/shepard/api/...")`; `de.dlr.shepard.v2.*` package reserved; ArchUnit `V2NamespaceTest` fences (3 rules); `@Sunset` annotation skeleton ready for L2e | **✓ ↑** | P4 (cherry-pick `e0c5e32`) |
| OpenAPI spec splitting (`/shepard/doc/openapi/{v1,v2}.json`) | n/a | shipped — `V1OpenApiFilter` / `V2OpenApiFilter` (`OASFilter` implementations) classify paths through a shared `OpenApiShelfMembership` utility; `OpenApiPerShelfRest` (`@PermitAll`, `@Path("/shepard/doc/openapi")`) clones the live `OpenApiDocument` via `MergeUtil` then runs the shelf-appropriate filter; `?format=yaml` honoured; both paths added to `PublicEndpointRegistry`; combined `/shepard/doc/openapi.json` unchanged. 17 unit tests covering shelf membership, post-strip path handling, YAML output, singleton non-mutation, and the unset-doc 500 path. | **✓ ↑** | P4c (this branch) / `aidocs/16` P4c |
| **v2 surface convergence** — minimalist core: `?kind=` generic references/containers, `fileKind` subtypes on FileReference (krl/svdx/otvis/urdf/xit/video — video files play inline via `VideoPlayer`, MP4-PROMOTE-VIDEO 2026-07-10, `V119` backfill), templates-as-SHACL-shapes (JSON DSL→compiler), `MAPPING_RECIPE` kind (scene-graph + KRL dissolve), content-negotiated `/v2/shapes/render`, admin `ConfigRegistry`, plugin-namespace allowlist (Jupyter/AAS) with OpenAPI disable | n/a (fork-only architecture) | design doc landed; **B2 shipped** (`ccc90021c` — SHACL validation at template instantiation, 422 on violation); **B3 shipped 2026-06-03** (`MAPPING_RECIPE` kind + `TransformExecutor` SPI + `POST /v2/mappings/{appId}/materialize` + identity default); **A4 shipped 2026-06-03** (generic `GET\|PATCH /v2/admin/config/{feature}` + `ConfigDescriptor` SPI + `ConfigRegistry`; bespoke `*ConfigRest` for semantic/jupyter/sql-timeseries/ror deleted); **B4 shipped 2026-06-04** (scene-graph dissolved into a MAPPING_RECIPE view-shape — `SceneGraphPlayShape` IRI + `SceneGraphPlayTransformExecutor` in `plugins/vis-trace3d`; `/v2/scene-graphs/*` namespace + `:DigitalTwinScene`/`:CoordinateFrame`/`:Joint` graph + `scene_graph_*` MCP tools DELETED; migration V111; Collection hero link repointed to a MAPPING_RECIPE template appId); **A5 shipped 2026-06-04** (`RestNamespaceContributor` marker on `PluginManifest` + `RestNamespaceRegistry` + `DisabledNamespaceRequestFilter` 404s a disabled plugin/feature's `/v2/<prefix>/*` + `DisabledNamespaceOasFilter` strips it from `/shepard/doc/openapi/v2.json`; allowlist = AAS `/v2/aas` (plugin-gated) + Jupyter `/v2/jupyter` (feature-gated); data-driven, fail-soft); **B5 shipped 2026-06-04** (KRL interpret dissolved into a MAPPING_RECIPE transform-shape — `KrlTrajectoryShape` IRI + **in-tree** `KrlTrajectoryTransformExecutor` returning a `TransformResult.reference` derived TimeseriesReference; `POST /v2/krl/interpret` + `:KrlInterpretActivity` entity + bespoke FE DELETED; sidecar client reused/relocated; migration V112 relabels `:KrlInterpretActivity`→`:KrlTrajectoryActivity`; in-context "Interpret as joint trajectory" button on `.src`/`.krl` FileRef detail); **A2 shipped 2026-06-04** (unified `/v2/references?kind=…` create/get/patch/delete/list via a `ReferenceKindHandler` SPI — chosen over static delegation because video/git/hdf reference REST live in plugin modules core can't import; core file/timeseries/uri handlers in-tree; `ReferenceV2IO` = `kind`+`referenceShape`+`fileKind`+per-kind `payload`; nullable `fileKind` on FileReference; PATCH-only `timeseries-references`+`uri-references` DELETED; plugin reference handlers + their per-kind CRUD deletion deferred to `PLUGIN-REF-HANDLER-*`); **A3 shipped 2026-06-04** (unified `/v2/containers?kind=…` create/get/patch/delete/list via a `ContainerKindHandler` SPI — sibling of A2; core file/timeseries/structured-data handlers in-tree delegating to the existing container services; `ContainerV2IO` = `kind`+per-kind read-only `payload`; **purely additive — NO path deleted** because container CRUD only ever lived on the frozen v1 surface, so A3 ships the FIRST appId-keyed `/v2/` container CRUD alongside it; every `/v2/…-containers/…` special-op kept; hdf handler deferred to `PLUGIN-CONTAINER-HANDLER-HDF`); **CONTAINER-PERMS-V2 shipped 2026-06-04** (`GET\|PATCH /v2/containers/{appId}/permissions` on `ContainersV2Rest`, Manage-gated, RFC 7396 merge-patch; 10 new JUnit tests; frontend create composables repointed from v1 `*ContainerApi` to the new v2 path — closes the last v1 fallback in the container create flow); **B6 shipped 2026-06-04** (visual template editor — thin `POST /v2/shapes/build` wrapping the B1 `ShaclShapeBuilder` (`ShapesBuildRest` + `ShapeBuild{Request,Response}IO`); `TemplateShapeEditor.vue` composes a `ShepardTemplate` SHACL shape by picking predicates from the `/v2/shapes/predicates` + `/v2/semantic/terms/search` palette → property-shape rows → live Turtle preview → `/v2/shapes/validate` round-trip; wired into `AdminTemplateDialog` as a Visual/Raw toggle, stores `editorState`+compiled `shapeGraph` on the template body; `useShapePalette`/`useShapeBuilder` composables + pure `utils/templateShapeDsl.ts`; 9 backend tests + Vitest helper tests; polish → `V2CONV-B6-POLISH`); **B7 shipped 2026-06-04** (`PayloadKind.shapeDescriptor()` default method returning `ShapeSpec` (null = opt-out); `KindShapeSeeder` `@ApplicationScoped` StartupEvent observer iterates `ServiceLoader<PayloadKind>`, seeds idempotent `DATAOBJECT_RECIPE` templates tagged `system:kind-shape-seeder` with copy-on-write versioning and admin-edit protection; `UrdfKindShapeExample` in-tree POC; 7 JUnit in `KindShapeSeederTest`); **B8 shipped 2026-06-09** (`PayloadKind.viewShapeDescriptor()` default method returning `ViewRecipeSpec` (null = opt-out); `KindShapeSeeder.seedViewKind()` mirrors `seedKind()` for `VIEW_RECIPE` templates named `<kind>-view-shape`; `ViewRecipeSpec` + `ChannelBindingSpec` records in `de.dlr.shepard.v2.shapes.builder`; 6 new JUnit in `KindShapeSeederTest` (13 total)); **PLUGIN-PERKIND-CRUD-CLEANUP shipped 2026-06-09** (`HdfReferenceRest` deleted; `GitReferenceRest` stripped to preview+check-update; `VideoStreamReferenceV2Rest` stripped to upload+download — per-kind CRUD now fully handled by `ReferenceKindHandler` dispatch via `/v2/references?kind=…`); **APISIMP-KIND-DISCRIMINATOR-1 shipped 2026-06-16** (#1963 — `PUT /v2/references/{appId}/content` Option-C phase-2 endpoint + `FileReferenceKindHandler.create()` metadata phase + `SingletonFileReferenceService.createSingletonMetadata()` + `attachContent()`, 51 tests); **APISIMP-KIND-DISCRIMINATOR-2 PR open 2026-06-16** (fire-83 — `POST /v2/files` returns 410 Gone; `useCreateSingletonFileReference.ts` migrated to two-step pattern; 18 FE + 60 BE tests); **B6-SHACLPREFILL ✓ shipped 2026-06-09** (`2b93988a` — `TemplateShapeEditor.vue` `focusAppId` prop + `prefillDataGraph()` on mount + prefill chip/loading/error; `AdminTemplatesPane` reads `?newTemplate=1&targetEntityAppId=` query params on mount; `AdminTemplateDialog` passes `focusAppId` to editor; 9 new Vitest in `templateShapeEditorPrefill.test.ts`)); **A6 shipped 2026-06-04** (`216b61401` — `KrlTrajectoryTransformExecutor` extracted from in-tree into `plugins/krl-interpreter/`; svdx/thermography plugins already standalone; scene-graph dissolved B4; all bespoke core REST verticals gone); **V2-SWEEP-003 slice 1 shipped 2026-06-10** (`ContainerListPage.vue` bulk-delete FILE/TIMESERIES/STRUCTUREDDATA → `safeDeleteContainer`; removes v1 `FileContainerApi`/`TimeseriesContainerApi`/`StructuredDataContainerApi` from the bulk-delete path); **V2-SWEEP-003-2 in-flight 2026-06-10** (fetchPermissions/updatePermissions × 3 container accessors → `GET/PATCH /v2/containers/{appId}/permissions`; dead `useFetchTimeseriesContainer.ts` retired; `appId` forward-filled in `TimeseriesContainer`/`StructuredDataContainer` models; V2-SWEEP-003-3 fetchData deferred to route-appId migration); **A1b shipped 2026-06-10** (additive render-SPI extension: `RenderRequest.params` (E1) + file-rooted dispatch via `shapeIri`+`focusFileRefAppId` with `templateAppId` optional (E2) + `FocusPayloadResolver` SPI seam so ServiceLoader-POJO renderers reach focus bytes (E3) + describe via `params.mode=index` (E4); legacy 4-arg `RenderRequest`/3-arg `ShapesRenderRequestIO` constructors preserved → Trace3D byte-compatible); **A7-THERMO shipped 2026-06-10** (on A1b — `/v2/thermography/*` REST dissolved onto `POST /v2/shapes/render` via `OtvisFrameRenderer` (`OtvisFrameShape` → frame catalogue JSON + heatmap PNG) + `ThermographyHeatmapRenderer` (`ThermographyHeatmapShape` → `application/json` plate-heatmap grid); `ThermographyV2Rest` + `AnalyzeRequestIO` deleted; all 5 FE callers repointed; Re-analyze button removed — analysis is the upload-time file-parse side-effect); **V2-SWEEP-002-USERGROUP-V2 slice-1 in-flight 2026-06-10** (`UserGroupV2Rest` at `/v2/user-groups`, `UserGroupDAO.findByAppId`, `UserGroupV2IO`, 12 unit tests); **V2-SWEEP-002-2 shipped 2026-06-10** (frontend admin user-groups pane migrated to appId-keyed `/v2/user-groups` CRUD + membership via new `useUserGroupsV2`; numeric-id surfaces dropped from list/detail; permissions/roles remain V1-EXCEPTION → V2-SWEEP-002-PERMISSIONS; `tests/unit/useUserGroupsV2.test.ts` 9 tests); **A7-PLUGIN-ADMIN-CONFIG slice 1 shipped 2026-06-10** (`UnhideConfigDescriptor` migrates `GET|PATCH /v2/admin/unhide/config` onto generic `/v2/admin/config/unhide`; harvest-key credential sisters kept at `/v2/admin/unhide/harvest-key/*`; 9 `UnhideConfigDescriptorTest` unit tests; `useUnhideAdminConfig.ts` + CLI + feed error messages repointed; old path 404); **A7-PLUGIN-ADMIN-CONFIG slice 2 shipped 2026-06-10** (`VideoConfigDescriptor` migrates `GET|PATCH /v2/admin/video/config` onto generic `/v2/admin/config/video`; `VideoAdminRest` deleted (config-only class); `VideoAdminPaths.CONFIG` repointed; OGM bug fixed — `VideoPayloadKind.entityPackages()` now registers `de.dlr.shepard.plugins.video.entities` so `VideoConfigDAO` no longer 500s on startup; 8 `VideoConfigDescriptorTest` unit tests; old path 404); **A7-UNHIDE-NS-REVIEW shipped 2026-06-10** (verdict: `/v2/unhide/feed.jsonld` stays outside Tier-3 allowlist — 503-on-disable > 404-on-disable for harvesters; documented in `aidocs/platform/191 §7c`); **A7-PLUGIN-ADMIN-CONFIG slice 4 shipped 2026-06-11** (`AasConfigDescriptor` featureName=`"aas"` + `LegacyV1ConfigDescriptor` featureName=`"legacy-v1"` — `AasConfigAdminRest` + `LegacyV1ConfigAdminRest` deleted; `AdminLegacyV1Pane.vue` repointed from `/v2/admin/legacy/v1/config` to `/v2/admin/config/legacy-v1`; 13+9 unit tests; merged `dbe11f7`); **V2-SWEEP-004-1 in-flight 2026-06-11** (`CollectionReferenceKindHandler` kind=`collection` + `DataObjectReferenceKindHandler` kind=`dataobject` wired into `/v2/references?kind=…` dispatch; frontend migration V2-SWEEP-004-2 queued); **UI-GAP-3 shipped 2026-06-11** (AdminConfigOverviewPane.vue — config registry overview pane; CONFIG_OVERVIEW fragment wired in /admin; bespoke-pane links for semantic/jupyter/unhide/legacy-v1/sql-timeseries); **A7-SVDX shipped 2026-06-12** (bespoke `/v2/svdx/ingest` (`SvdxIngestRest` + `SvdxIngest{Request,Response}IO`) DELETED; the real CSV→TimescaleDB ingest rehomed as `SvdxCsvTransformExecutor` in `plugins/fileformat-svdx` claiming `SvdxCsvIngestShape` IRI, reusing `SvdxCsvIngestionService` verbatim via `CDI.current()`, materialized through `POST /v2/mappings/{appId}/materialize` — mirrors KRL/B5 + URScript; deterministic `svdx-ingest:<svdxId>+<csvId>` idempotency preserved; shape TTL `shapes/svdx-csv-ingest.shacl.ttl`; `SvdxManifestParser` annotate-on-upload untouched; `SvdxCsvTransformExecutorTest` 10 cases; no migration — svdx ingest had no typed `:*Activity`); **APISIMP-SHAPES-PREDICATES-BARE-LIST shipped 2026-07-02 fire-361** (`GET /v2/shapes/predicates` wrapped in `PagedResponseIO` + `?limit=` cap (max 500, default 200); `useShapePalette.ts` reads `.items`; PR #2236 merged `119a57e`); **APISIMP-INSTANCE-CAPABILITIES-DUPLICATE-200 shipped 2026-07-02 fire-361** (duplicate `@APIResponse(responseCode="200")` on `getCapabilities()` removed; PR #2235 merged `55ea4be`); **APISIMP-REF-ANNOTATION-LIST-NO-PAGINATION shipped 2026-07-02 fire-362** (`GET /v2/references/{appId}/annotations` now accepts `?page=`/`?limit=` (default 200, max 1000); list sliced server-side; `total` always reflects full count; additive — no wire break); **APISIMP-SHAPES-PREDICATES-PROBLEM-JSON in-flight fire-583** (`ShapesPredicatesRest.java:120-122` plain-String `text/plain` 400 on unknown `?substrate=` value converted to `ProblemResponse.problem(BAD_REQUEST, "…")` — `application/problem+json` with `urn:shepard:error:validation` type; 2 test methods strengthened to assert `ProblemJson` entity type); **APISIMP-PLUGINS-ADMIN-APPID-PATH in-flight fire-583** (`PluginsAdminRest.java:137,166` `@Path("/{id}")` / `@PathParam("id") String id` renamed to `@Path("/{appId}")` / `@PathParam("appId") String appId` throughout PATCH method — zero wire-shape change; pure Java variable rename; OpenAPI path-param name in generated stubs changes from `id` to `appId`); **APISIMP-COLLECTION-PERMISSIONS-PUT-VS-PATCH shipped fire-597** (`CollectionPermissionsRest.editPermissions` `@PUT → @PATCH` + method-level `@Consumes("application/merge-patch+json")`; generated TS client updated; 12 JUnit unit tests added); **APISIMP-PROVSTATS-BUCKET-MILLIS shipped fire-613** (`ProvenanceStatsIO.bucketMillis` → `bucketDuration: String` ISO 8601 duration, PR #2575 `b074bfe`); **APISIMP-TSREF-TIMEWINDOW-NANOS ✓ merged PR #2577 fire-614** (`TimeseriesReferenceKindHandler.toIO()` `start`/`end` → ISO 8601 strings); **APISIMP-SPATIAL-TIMEWINDOW-NANOS ✓ merged PR #2578 fire-614** (`SpatialDataReferenceKindHandler.toIO()` + `SpatialPromoteService.toIO()` `startTime`/`endTime` → ISO 8601 strings; `create()` accepts ISO or legacy nanos); **APISIMP-TSREF-ANNOT-MAP-NS-TO-ISO 🔄 fire-615** (PR #2579 — `TimeseriesReferenceKindHandler.annotationToMap()` emits ISO 8601 `start`/`end` instead of `startNs`/`endNs` nanosecond longs for the annotation sub-resource; backward-compat `isoOrLongToNanos()` on write); **APISIMP-METRICS-HTTP-MEAN-MILLIS 🔄 fire-615** (`AdminMetricsSummaryIO.httpMeanRequestMillis` (Double, raw ms) → `httpMeanRequestDuration` (String, ISO 8601 duration, e.g. `"PT0.2S"`); service converts via `Duration.ofNanos(Math.round(millis * 1_000_000)).toString()`; frontend `AdminMetricsCard.vue` updated) | ✓ **shipped** | `aidocs/platform/191` / `aidocs/16` V2CONV-* / V2-SWEEP-003 |
| **v2 conformance sweep** — audit that all frontend + plugin components address `/v2/` surface + appId (V2-SWEEP-AUDIT-2026-06-09); individual fix rows for 150 residual v1 call sites | n/a | **audit shipped 2026-06-09** (`1e246d3`; findings report: `aidocs/agent-findings/v2-conformance-sweep-2026-06-09.md`); **V2-SWEEP-002-USERGROUP-V2 slice-1 in-flight 2026-06-10** (`UserGroupV2Rest` at `/v2/user-groups`, `UserGroupDAO.findByAppId`, `UserGroupV2IO`, 12 unit tests); **V2-SWEEP-002-2 shipped 2026-06-10** (frontend user-groups pane → appId-keyed `/v2/user-groups` via `useUserGroupsV2`; permissions/roles → V2-SWEEP-002-PERMISSIONS); **V2-SWEEP-002-PERMISSIONS shipped 2026-06-10** (`GET/PATCH /v2/user-groups/{appId}/permissions` + `GET /v2/user-groups/{appId}/roles`; 9 new unit tests); **V2-SWEEP-002-3 shipped 2026-06-14** (frontend migrates 7 V1-EXCEPTION permission/role call sites in `useHandleUserGroupMembers` + `useCreateUserGroup` to v2 via `useUserGroupsV2`; removes `resolveNumericIdByName` bridge; V2-SWEEP-002-USERGROUP-V2 all slices done; one V1-EXCEPTION remains in `mapPermissions.fetchUserGroup` blocked on PermissionsIO carrying numeric readerGroupIds, filed V2-SWEEP-002-4); **V2-SWEEP-002-4 in-flight 2026-06-15** (`PermissionsIO` gains `readerGroupAppIds`/`writerGroupAppIds` String[] parallel to numeric arrays; `mapPermissions.ts` `mapPermissionRoleUserGroups` calls `GET /v2/user-groups/{appId}` via `useUserGroupsV2` when appId non-null; v1 `fetchUserGroup` is fallback-only for null-appId legacy nodes; 5 backend tests + 2045 FE tests green); **V2-SWEEP-003 slice 1 shipped 2026-06-10**; **V2-SWEEP-003-2 in-flight 2026-06-12** (dual-path `fetchData()` in FileContainerAccessor/TimeseriesContainerAccessor/StructuredDataAccessor — UUID → `GET /v2/containers/{appId}`, numeric → v1 V1-EXCEPTION; `ContainerRouteParams.containerId: string`; route builders use `c.appId`); **V2-SWEEP-004-1 in-flight 2026-06-11**; **V2-SWEEP-001-SPEC-FIX shipped 2026-06-11** (explicit unique `@Operation(operationId)` on 119 colliding `/v2/` methods → 0 duplicate operationIds in source; unblocks the client regen); V2-SWEEP-001 (client regen) host-only; **V2UI-CHANNEL-ANNO-CLIENT in-flight 2026-06-13** (`AnnotatedChannel` migrated from raw `fetch` to typed `TimeseriesChannelAnnotationsApi` via `useV2ShepardApi`; `_appIdMap` caches numeric id → UUID appId for typed delete; `SemanticAnnotationIO.java` gains `appId` field — additive, fixes latent channel-annotation delete bug; 6 new Vitest unit tests) | 🚧 in-flight | `aidocs/16` V2-SWEEP-* |
| RFC 7807 (`application/problem+json`) error responses + sanitised exception logging — **subsumes M7** | leaks `exception.getClass().getSimpleName()` + raw `getMessage()` for every 5xx; full stack at `error` for every 4xx | shipped — `ProblemJson` record with RFC 7807 §3.1 fields + flat `extensions` map; 10-entry `ShepardErrorCode` catalogue; known shepard exceptions surface their (controlled) message as `detail`; unknown-5xx returns generic with `traceId`; legacy `ApiError` preserved for `Accept: application/json` (upstream-client compat); full stack at `debug`, error line carries only `traceId + class + method + path` | **✓ ↑** | H4 (cherry-pick `e526183`) / `aidocs/07` H4 |
| CORS allowlist instead of `origins=*` | wildcard | TBD | 📐 (queued) | `aidocs/07` C2 |
| Default-credential placeholders that fail at startup if not changed | accept shipped defaults | TBD | 📐 (queued) | `aidocs/07` H8 |
| OIDC `realm_access.roles` claim path configurable (multi-IdP) | hard-coded Keycloak shape | TBD | 📐 (queued, F8) | `aidocs/22 §4.11a.4` |
| Permission system: declarative `@Authz` annotation | path-segment switch | TBD | 📐 (queued, F1) | `aidocs/24` F1 / P5 |
| **Instance-Admin role (`instance-admin`)** — single role tier for v1; gates `/v2/admin/*` endpoints + future `shepard-admin` CLI | none — `JWTPrincipal.roles` was always `new String[0]` so `@RolesAllowed("admin")` denied everyone | shipped — new `Role` Neo4j entity + `:HAS_ROLE` relationship; `V13` appId constraint; `JWTSecurityContext.isUserInRole` consults principal; `@RolesAllowed("instance-admin")` works | **✓ ↑** | A0 (this slice) / `aidocs/51` |
| **Dual-source role check** — IdP claim AND/OR Neo4j `:HAS_ROLE` edge, deduped on the principal | n/a (no role mechanism) | shipped — `JWTFilter.resolveDualSourceRoles`; principal carries one combined `roles` list | **✓ ↑** | A0 / `aidocs/51 §3.3` |
| **Bootstrap-token mechanism** — `/opt/shepard/.bootstrap-token` (mode 0600); `POST /v2/admin/bootstrap` consumes; replay-protected via `:BootstrapState` Neo4j flag node + token hash | none | shipped — `BootstrapTokenInitializer` runs after migrations; idempotent; configurable path via `shepard.bootstrap.token-path` | **✓ ↑** | A0 / `aidocs/51 §5` |
| **API-key roles claim** — `POST /apikeys` body grows `roles: [...]` field; minted JWT carries `roles` claim; cross-checked vs Neo4j-stored `roles` Set on read; allowlist + caller-must-have-each-role validation | none | shipped — `shepard.apikey.role-allowlist` (default `["instance-admin"]`); `InvalidAuthException` on escalation attempt; `InvalidRequestException` on out-of-allowlist | **✓ ↑** | A0 §4.2 / `aidocs/51 §4.2` |
| **C3: `getRoles` fail-closed** — orphan entities (no `:has_permissions` edge) now return `Roles(false,false,false,false)` instead of full read+write+manage to every authenticated user | full-access fallback (CRITICAL backdoor — `aidocs/07` C3) | shipped — paired with `OrphanPermissionsBackfillContext` pre-migration hook + V14 backfill | **✓ ↑** | C3 (bundled with A0) / `aidocs/07` C3 / `aidocs/51 §8` |
| **`GET /v2/admin/permission-audit`** — surfaces entities lacking `:has_permissions` edge (operational triage for the post-C3 fail-closed default) | none | shipped — `@RolesAllowed("instance-admin")`-gated; returns up to 1000 rows | **✓ ↑** | A0 / `aidocs/51 §10` |
| JWT `iat`-keyed permission cache (F4) — `isAccessTypeAllowedForUser` 4-arg `(entityId, accessType, username, jwtIat)`; prevents stale cache hits across JWT rotations (role-change, group-change, key rotation) | 3-arg cache key (no iat dimension; rotated JWTs can hit stale cache entries until TTL expires) | shipped — `JWTPrincipal` gains `iat` field; `JWTFilter` extracts `body.getIssuedAt()`; services with `AuthenticationContext` use `currentIat()`; REST resources and warmer pass `0L`. `CompositeCacheKey` is 4-tuple. 4 new `PermissionsServiceIatCacheKeyTest` cases. | **✓ ↑** | F4 |
| Fail-closed Neo4j guard (F5) — `PermissionsService.isAllowed()` returns `false` immediately when `DbHealthRegistry.isCurrentlyDown(NEO4J)` is `true`, without calling the DAO | degraded Neo4j → 500 or exception-propagated; no explicit deny | shipped — `dbHealthRegistry.isCurrentlyDown(DatabaseKind.NEO4J)` guard at top of `isAllowed()`; null-safe for test environments. 3 new `PermissionsServiceNeo4jGuardTest` cases. Requires A1b `DbHealthRegistry`. | **✓ ↑** | F5 |
| Group-based sharing model (`Group` node) | none | TBD | 📐 (queued, F2) | `aidocs/24` F2 |
| Permission audit log (Postgres) — `permission_audit_log` table, `PermissionAuditLogService` fire-and-forget hook in all three `PermissionsService` write paths (GRANT/UPDATE/REVOKE), `GET /v2/admin/permission-audit/log` with entityAppId / actor / from / to / page / size filters | none | shipped — `V1.10.0` migration, `PermissionAuditLogService`, `PermissionAuditLogQueryService`, `InstanceAdminRest` new endpoint; 13 unit tests. Frontend: `PermissionAuditLogPane.vue` + `useFetchPermissionAuditLog` composable + `AdminPermissionAuditApi` backend-client class. | **✓ ↑** | F3 |
| `GET /v2/admin/permission-audit/log` — response entries now carry `appId: string` instead of `id: number`; `V1.19.0` SQL migration adds `app_id UUID` column | none (fork-only endpoint) | 🔄 PR #1864 open (2026-06-12) — `PermissionAuditLogEntryIO` drops `long id` → `String appId`; wire break pre-prod only | **🔄 in-flight** | APISIMP-PERM-AUDIT-LOG-APPID |
| `GET /v2/admin/permission-audit/log` — response now wrapped in `PagedResponseIO` envelope (`{items, total, page, pageSize}`) with `X-Total-Count` header; `page` now `@PositiveOrZero`; `pageSize` now `@Min(1) @Max(500)` | none (fork-only endpoint) | shipped (fire-269) — wire break pre-prod: bare array → `PagedResponseIO` envelope; admin UI `PermissionAuditLogPane.vue` already reads `.items` + `X-Total-Count` | **✓ ↑** | APISIMP-PERMISSION-AUDIT-LOG-PAGINATION |
| `GET /v2/users?q=` — response changed from bare `List<UserIO>` to `PagedResponseIO<UserIO>` envelope; `page` (default 0) + `pageSize` (default 20, max 100) added; bounded `ORDER BY username ASC SKIP/LIMIT` Cypher; `SearchDAO.findUsers()` (v1 path) untouched | none (fork-only `/v2/` endpoint) | 🔄 in-flight fire-436 (PR pending) — wire break pre-prod: bare array → envelope; 5 unit tests in `UserSearchV2RestTest` | **🔄 in-flight** | APISIMP-USER-SEARCH-NO-PAGINATION |
| `/v2/` reference wire shapes (`ReferenceV2IO`, `FileReferenceV2IO`, `FileBundleReferenceIO`) — `id` and `dataObjectId` suppressed via `BasicReferenceV2IO @JsonIgnoreProperties` | none (fork-only `/v2/` surface) | in-flight — `BasicReferenceV2IO` introduced; 9-test suite verifies suppression; PR pending | **🔄 in-flight** | APISIMP-BASICREF-DATAOBJECTID |
| Nuclear instance reset — `POST /v2/admin/instance/nuke` (confirmation phrase `"yes drop everything"` required; wipes Neo4j data nodes, MongoDB collections, Postgres timeseries records; preserves users/API keys/config) | none | shipped — `NukeService` + `NukeRequestIO`/`NukeResultIO`; `NukeServiceTest` (7 unit tests) + `InstanceAdminNukeRestTest` (3 REST-layer tests) | **✓ ↑** | `aidocs/51 §10` |

## 4. Identifiers (the L2 chain)

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| Additive `appId` (UUID v7) on every Neo4j node-write | none | shipped via `HasAppId` mixin on 28 labels; minted by `GenericDAO` seam; `V11` per-label unique constraints | **✓ ↑** | L2a (commit `fec7979`) |
| Backfill `appId` for pre-L2a rows (`V12`) | n/a | shipped — chunked 10k rows per batch, idempotent, operator-run rollback file | **✓ ↑** | L2b (cherry-pick `796bc11`) |
| Read path uses `WHERE e.appId = $appId` | uses `id()` | shipped — `EntityIdResolver` request-scoped translates Long ↔ appId at the DAO boundary; 14 DAO files swapped (`PermissionsDAO`, `DataObjectDAO`, `GenericDAO`, the `*ReferenceDAO` family, `VersionDAO`, `ShepardFileDAO`, `StructuredDataDAO`, `SemanticAnnotationDAO`); public DAO signatures stay `long` for caller-compat; cache key stays `long` (per design §3.3); `Neo4jQueryBuilder`'s search-JSON predicates and `PermissionsService.isAllowed` segment dispatch deliberately untouched (those are L2d's job) | **✓ ↑** | L2c (cherry-pick `f3ca003`) / `aidocs/25 §4 Phase 3` |
| `/v2/` API exposes `appId` natively | n/a | **Phase A shipped** — `/v2/collections` CRUD (Phase A.1) and `/v2/collections/{collectionAppId}/data-objects` CRUD (Phase A.2: `GET` list, `GET` one, `POST`, `PATCH` merge, `DELETE`). Both use `EntityIdResolver` to translate `appId → ogmId` at the resource boundary; reuse existing `CollectionIO` / `DataObjectIO` (each already carries `appId` on `BasicEntityIO`); `[v2]` summary badge applied by `ShelfTagFilter`. **Phase A.2 enrichment shipped (L2d-RC37)**: `GET /v2/.../data-objects` list now returns `DataObjectListItemV2IO` items with `timeseriesCount`, `fileCount`, `structuredDataCount` (single Cypher round-trip, no N+1). **CC2 shipped**: `GET /v2/collections/{appId}/referenced-containers` lists all distinct containers reachable from the collection's DataObjects (type-discriminated `ContainerSummaryIO`; frontend "Referenced Containers" panel). **APISIMP-CONTAINERS-LIST-ENVELOPE (fire-300)**: response wrapped in `PagedResponseIO` envelope (was bare array). **REF-1 shipped**: `GET /v2/collections/{cid}/data-objects/{did}` now returns `DataObjectDetailV2IO` (extends `DataObjectIO`) with typed `containers.{timeseries,files,structuredData}` lists and `predecessorSummaries`/`successorSummaries`/`childSummaries`/`parentSummary` fields. **ANC-1 shipped**: 5 new sub-endpoints — `GET /predecessors`, `/successors`, `/children`, `/predecessor-chain?depth=N`, `/successor-chain?depth=N`; 8 unit tests. **APISIMP-DO-RELATIONSHIP-LISTS-UNBOUND (fire-322)**: `/predecessors`, `/successors`, `/children` now return `PagedResponseIO<DataObjectSummaryIO>` with `?page`/`?pageSize` params (defaults 0/50, max 200); total reflects full count; 3 new pagination tests. Phase A.3 (the three container kinds), Phase B (parallel `CollectionV2IO` / `DataObjectV2IO` with string-typed relations), and Phase C (`SubscriptionFilter` path-segment parser fix for subscription delivery on `/v2/` writes) still queued. | **✓ ↑ Phase A.1 + A.2 + A.2-RC37 + CC2 + REF-1 + ANC-1** / 📐 Phase A.3 + B + C | L2d (`aidocs/25 §4 Phase 4`) + `aidocs/34` L2d Phase A.1 / A.2 / RC37 + CC2 + REF-1 + ANC-1 |
| **MFFD-BATCH-01 — `POST /v2/data-objects/batch` (HTTP 207 bulk create)** | n/a | **shipped** — `DataObjectBatchV2Rest` at `@Path("/v2/data-objects/batch")`. Accepts a JSON array of 1–500 `DataObjectBatchCreateItemIO` items; returns HTTP 207 Multi-Status with per-item outcomes (`"created"` with minted `appId`, or `"error"` with `errorCode`). Sequential processing (no parallel Neo4j sessions); per-Collection permission check memoised within the batch loop. Error codes: `COLLECTION_NOT_FOUND`, `FORBIDDEN`, `PARENT_NOT_FOUND`, `INVALID_INPUT`, `INTERNAL_ERROR`. 15 unit tests in `DataObjectBatchV2RestTest`. No new Neo4j labels, no new config keys, no migration. | **✓ ↑** | `aidocs/34` MFFD-BATCH-01 |
| **MFFD-AF-TRACK-MAPPING — cross-process Predecessor edge infrastructure (`POST /v2/admin/mffd/process-chain-mapping`)** | n/a — upstream has no MFFD-aware cross-process linkage | **shipped 2026-06-02** — closes GAP-4 in `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md`. **V105** adds a range index on the `has_successor.transitionKind` Neo4j relationship property (idempotent CREATE INDEX IF NOT EXISTS + rollback twin). **REST `POST /v2/admin/mffd/process-chain-mapping`** (`@RolesAllowed("instance-admin")`) consumes `text/yaml`/`application/yaml`/`text/plain`, parses a schema-version-1 YAML payload, AND-matches source + target DataObjects via their `urn:shepard:mffd:*` SemanticAnnotations (predicates seeded by V100 templates), and idempotent-MERGEs `has_successor` edges with one of `normal | rework | re-test | concession` (same `transitionKind` vocabulary as AAA2's QM1b — the two PRs merge safely in either order). Response carries `{schemaVersion, entries, matched, unmatched, edgesCreated, unresolved[], warnings[]}`. Records a single richer `:Activity` (`EXECUTE` / `MffdProcessChainMapping`) with the counters in the summary; uses `PROP_SKIP_CAPTURE` so `ProvenanceCaptureFilter` does not double-capture. **Frontend** admin page at `/admin/mffd-process-chain` (file-upload + textarea + counters + unresolved checklist + warnings) linked from the `/admin` hub tile grid. **UI-GAP-2 polish (2026-06-10)**: `PlaceholderPageHeader` replaced with real `<h4>` heading + prose; "Download example YAML" button added (static asset at `/mffd-process-chain-mapping.example.yaml`). **Authoring**: `scripts/validate-mffd-process-chain-mapping.py` (Python, offline + live modes), worked example `scripts/mffd-process-chain-mapping.example.yaml` (15 entries covering all 4 transitionKinds). **Operator runbook**: `docs/admin/runbooks/mffd-process-chain-mapping.md`. Design: `aidocs/integrations/118`. 11 service JUnit + 4 REST JUnit + 11 page-contract Vitest + 17 validator pytest tests. | **✓ ↑** | `aidocs/34` MFFD-AF-TRACK-MAPPING + `aidocs/integrations/118` + `aidocs/16` MFFD-AF-TRACK-MAPPING-1/2-CYPHER + MFFD-MAPPING-REST-1 |
| **MFFD-SPATIAL-LINESCAN-IMPORTER-1 — TPS raw-data line-scan extension (W7b)** | n/a — upstream has no spatial-importer at all | **shipped 2026-06-02** — extends MFFD-SPATIAL (W7) with `--linescan-pass` on `plugins/spatial-importer/cli/main.py` + `cli/linescan.py`. Decodes the `Track_NN__Run_NN_/files/TPS raw data.N` 1292×964 8-bit grayscale PNG chunks into `SpatialDataContainer`s annotated `kind=brush-trace`. Each PNG row becomes one `SpatialDataPoint` whose `measurements.intensities` carries the full 1292-element intensity vector (JSONB on the postgres side); row-as-time when no source-side timestamp is available (container annotated `urn:shepard:spatial:t-axis = row-index`). Same SHA256-keyed idempotency + FileReference demotion shape as the AAC1 pointcloud pass. **Frontend co-ship**: `expandBrushTraceRows()` helper extracted to `frontend/utils/brushTrace.ts` (pure TS, vitest-friendly without a Vue SFC loader) + `brush-trace` render mode added to `SpatialPointsCanvas.vue` re-using the existing voxel-grid decimation cap (964 × 1292 ≈ 1.25M points per chunk capped at 500k). **Tests**: 28 pytest (open + decode + classify + sha256 + degenerate-image + per-chunk + 16-bit-mode + non-PNG-reject + format-drift guard); 6 backend JUnit (`BrushTraceIOTest` pinning the wire shape and 1292-wide intensity vector round-trip); 6 Vitest (row-expansion + decimation at 1.25M-point synthetic load + viridis cross-plugin colormap parity). Real-data regression fixture: `tps_raw_data_chunk18_64x128.png` (sha256 `874039f6…`), the upper-left 64×128 crop of `Track_66__Run_23133_/files/TPS raw data.18`. **Honest scope**: the brush-trace shape rides through the legacy v5 `spatial_data_points` table for now; promotion to a first-class `profile_kind` on the SPATIAL-V6 hypertable is deferred (`MFFD-SPATIAL-BRUSHTRACE-SCHEMA-1`). Per-pixel mm calibration + wall-clock timestamp loader deferred (`MFFD-SPATIAL-LINESCAN-CALIB-1`). Resolves the AAC1-anticipated `MFFD-SPATIAL-BRUSH-TRACE-FROM-RAW` follow-up. | **✓ ↑** | `aidocs/34` MFFD-SPATIAL-LINESCAN-IMPORTER-1 + `aidocs/integrations/113 §W7b` + `aidocs/16` MFFD-SPATIAL-LINESCAN-IMPORTER-1 / -BRUSHTRACE-SCHEMA-1 / -CALIB-1 / -PYRAMID-1 / -AI-1 / -FORMAT-DRIFT-1 |
| **MFFD-SPATIAL — pointcloud + trajectory promotion (W7) + frameAppId handshake** | n/a — upstream has no spatial-importer and no SpatialDataContainer-to-frame linkage | **shipped 2026-06-02** — closes GAP-5 from `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md` and the W7 wave of `aidocs/integrations/113-mffd-real-data-import-plan.md`. **`plugins/spatial-importer/`** (Python-only CLI; no Java code) consumes the existing `/shepard/api/spatialDataContainers/...` REST surface to promote opaque `:FileReference` rows holding ASCII pointclouds (`TPS 3D pointclouds.*`) and trajectories (`FSD course 3D pointclouds`) into typed `:SpatialDataContainer` rows on the existing `shepard_spatial.profile` PostGIS hypertable. SHA256 of source bytes is the idempotency key (stored as `urn:shepard:spatial:source-sha256` annotation on the container); re-runs are zero-write. RGB-uniform export-artefact (`0 130 255` Keyence default) is detected and dropped at parse time. 4-worker ThreadPoolExecutor + exponential backoff with jitter (1→60s ceiling). Demotes the original FileReference to `status=ARCHIVED` + `urn:shepard:spatial:promoted-to=<containerAppId>` annotation as a fire-and-forget secondary write. **Backend co-ship**: `SpatialDataContainer` gains a nullable `frameAppId` String property pointing at CST1 `:CoordinateFrame.appId` (mirrors the PostGIS `coord_frame_app_id` column so a Cypher traversal can reach the frame without a substrate hop); `V106` NOOP Cypher migration documents the additive property; `SpatialDataContainerIO.frameAppId` (omitted from the wire when null — legacy `/shepard/api/spatialDataContainers` callers see byte-identical responses); service create-flow propagates the field from IO to entity. **Frontend co-ship**: `SpatialPointsCanvas.vue` Three.js renderer with `pointcloud` mode (Points scatter, voxel-grid downsample capped at 500k) and `trajectory` mode (Line strip, time-as-colour, start/end markers); `DataObjectSpatialContainersPane.vue` lists per-DO spatial refs grouped by kind (profile / trajectory / brush-trace) with "Open in 3D viewer" buttons; spatial container detail page wires the viewer in place of the "viewer in development" placeholder banner. **Docs**: 4-page plugin bundle at `plugins/spatial-importer/docs/` (reference + quickstart + install). **Tests**: 4 IOTest + 2 service-test JUnit cases (frameAppId pass-through + null handling + wire serialization); 19 pytest cases against real Track 66 / Track 67 fixture bytes (CRLF preserved via .gitattributes); 13 Vitest cases (voxel-grid decimation + spatial kind classifier). **Update 2026-06-02 (later in the same day)**: the original "TPS raw data.0…37 stays as FileReferences" classification was overturned by the operator — see `MFFD-SPATIAL-LINESCAN-IMPORTER-1` row above. Each PNG row is one sensor-measurement instant along the AFP track and now promotes to a `kind=brush-trace` SpatialDataContainer. | **✓ ↑** | `aidocs/34` MFFD-SPATIAL + `aidocs/integrations/113 §W7` + `aidocs/16` MFFD-SPATIAL-IMPORTER-1 / MFFD-SPATIAL-FRAME-HANDSHAKE / MFFD-SPATIAL-FILEREF-DEMOTE / MFFD-SPATIAL-VIEWER-UI |
| **SPATIAL-UNIFY — spatial is a unified Reference kind (`kind=spatial`)** | n/a — upstream has no `/v2/references` surface; spatial only reachable via numeric-id v1 `spatialDataReferences` + a bolt-on container-promotion panel | **shipped 2026-06-10** (branch `124-spatial-unify`) — spatial data now appears as a **"Spatial (N)" tab** in the unified `/v2/references?kind=` Data-References table on the DataObject detail page, addressed by reference `appId` like File/TimeSeries/Video. **Backend**: `SpatialDataReferenceKindHandler` (`kind()="spatial"`) in `shepard-plugin-spatiotemporal` delegating to the existing `SpatialDataReferenceService`/`DAO` (the `SpatialDataReference extends BasicReference` + `[:IS_IN_CONTAINER]` shape already matched FileReference→FileContainer — a surface-convergence job, not a data-model rewrite); the container stays a storage primitive behind the reference. **In-context promote** (operator decision: per-file, one-click — NOT eager-on-upload): `POST /v2/spatial/promote?fileReferenceAppId=…` mints a `SpatialDataReference` + backing `SpatialDataContainer` (`promotionState=pending`, idempotent, Write-on-DataObject gated, `:Activity` auto-captured) from an eligible pointcloud/trajectory FileReference. **Frontend**: `useFetchSpatialReferencesV2` + `mapSpatialReferenceToDataTableElement` + "Promote to spatial"/"View as pointcloud" row actions; the bolt-on `DataObjectSpatialContainersPane.vue` + its "run the spatial-importer pass" panel are **DELETED**. **Migration**: `V113__SPATIAL_UNIFY_backfill_spatial_reference_appids.cypher` (+ `R__` twin) defensively mints appIds for any legacy `appId IS NULL` spatial nodes (no PostGIS DDL). **Tests**: 24 backend JUnit (handler + classifier + promote service) + 20 Vitest (mapper + eligibility). **Deferred**: SPATIAL-UNIFY-005 viewer re-root to `/v2/shapes/render`; SPATIAL-UNIFY-007 frozen-surface IT + `/v2/spatial-containers/{appId}` shelf; SPATIAL-UNIFY-004-SIDECAR (Python sidecar drains the `promotionState=pending` queue). | **✓ ↑** | `aidocs/integrations/124` + `aidocs/16` SPATIAL-UNIFY-002/003/004/006 |
| Drop `/v1/` long-id paths; flip cache key shape; drop TimescaleDB legacy column | n/a | TBD | 📐 (queued) | L2e / `aidocs/25` |
| **URI1 — HTTPS persistent identifiers** (`GET /id/{type}/{appId}` → `303 See Other`; `uri` field added to v2 IOs; `shepard.instance.base-url` config key; content-neg: JSON → API, HTML → UI; Phase 2: JSON-LD `@id` for RDF/SPARQL/DataCite) | bare UUID v7 only; not dereferenceable | designed | 📐 (designed) | `aidocs/platform/91-appid-uri-scheme.md` |
| **TS-ID substrate (PR-1) — `shepard_id` column on the Postgres `timeseries` table** | every timeseries channel is identified by a 5-tuple `(measurement, device, location, symbolicName, field)` | shipped (substrate only) — Flyway `V1.11.0` adds `shepard_id UUID NOT NULL UNIQUE` to `timeseries`; backfilled with `gen_random_uuid()` on existing rows. New `TsChannelResolver` JDBC service translates `shepardId ↔ 5-tuple`. **Wire unchanged in this PR.** Subsequent PRs (2–10) expose `shepardId` on `/v2/` wire as the canonical channel identifier (5-tuple stays valid for one release cycle). v1 `/shepard/api/...` is **frozen byte-for-byte** (proven by `V1WireFidelityTest`). Supersedes `aidocs/platform/87 §3 TS-IDa` — the canonical channel identifier lives in Postgres (`shepard_id`); channels are Postgres rows. Note: `:Timeseries` Neo4j nodes DO exist (596 confirmed post-MFFD ingest) and had `appId IS NULL`; NEO-AUDIT-003 (V82) backfills those with `randomUUID()` — see `aidocs/34` NEO-AUDIT-003 row. | **✓ BE substrate ↑ / 🚧 wire-rename PR-2..10 in-flight** | `aidocs/platform/87 CHANGELOG 2026-05-22` + `aidocs/34` TS-ID substrate row + NEO-AUDIT-003 |
| **TS-ID PR-2 — channel-listing endpoint exposing `shepardId` on /v2/** | n/a (v1 lists channels via `GET /shepard/api/timeseriesContainers/{id}/timeseries` returning `TimeseriesIO` with no single-field identity) | shipped — `GET /v2/timeseries-containers/{id}/channels` returns `TimeseriesChannelV2IO[]` carrying `shepardId` + the legacy 5-tuple. First /v2/ surface where the substrate is wire-visible. v1 endpoint untouched. 4 new unit tests including structural guard that the v2 IO is a Record (does NOT extend v1 `TimeseriesIO`). **APISIMP-TSCHANNEL-INT-ID-DEPRECATE (fire-218, PR #2089)**: `int id` and `long containerId` both marked `@Schema(deprecated=true)` so generated clients flag them as deprecated in favour of `shepardId`; 2 new reflection-based unit tests. Wire shape unchanged (additive annotation only). | **✓ ↑** (TS-ID PR-2) | `aidocs/34` TS-ID PR-2 row |
| **TS-ID PR-3 (TS-IDc) — single-key data-fetch endpoint** | n/a (v1 data fetch requires 5-tuple: `measurement + device + location + symbolicName + field`) | shipped 2026-05-26 — `GET /v2/timeseries-containers/{containerId}/channels/{shepardId}/data` resolves `shepardId → 5-tuple` via `TsChannelResolver.resolveTuple()` and returns `TimeseriesWithDataPoints` (same wire shape as v1). Optional `?downsample=lttb&max_points=N`. 404 on unknown shepardId. 5 unit tests. Frontend demo: `/timeseries/channels` playground. Beta acceptance criteria: ① 200+data for known shepardId; ② 404 for unknown; ③ LTTB ≤200pts when max_points=200; ④ V1WireFidelityTest still green; ⑤ live demo on shepard.nuclide.systems. | **✓ ↑** (TS-ID PR-3) | `aidocs/34` TS-IDc row |
| **TS-ID PR-3b (TS-IDc / task #58) — live-window endpoint accepts `?shepardId=<uuid>`** | n/a (v1 has no live-window endpoint; the v2 endpoint previously took 5-tuple only) | shipped 2026-05-29 (`9b88c1d66`) — `GET /v2/timeseries-containers/{containerAppId}/channels/live-window` now accepts `?shepardId=<uuid>` alongside the legacy 5-tuple. When both are supplied, shepardId wins. Backed by new `TsChannelResolver.findByContainerAndShepardId` (container-scoped wrapper around `findByShepardId`; cross-container leak guard returns empty → 404). 6 new unit tests + 3 resolver tests. Frontend: `useFetchChannelPreview` accepts `channelShepardId`, prefers the v2 path-param endpoint when set; `ChannelPreviewChart.vue` + `TimeseriesMeasurementsTable.vue` pass it through. 3 new Vitest cases. Plan-time evidence (MFFD container 1772, 113 channels): shepardId = 4.36 ms plan + 0.17 ms exec (index-only via `idx_timeseries_shepard_id`); 5-tuple = 2.18 ms plan + 0.07 ms exec (seq scan). shepardId wins on cost-growth shape (constant vs. O(channels-per-container)). v1 untouched. Unblocks TPL2c, MFFD-TS-MULTIREF-01, MFFD-SHARED-CONTAINER-01. | **✓ ↑** (TS-ID PR-3b) | `aidocs/34` TS-ID PR-3b row + `aidocs/platform/87 §3 TS-IDc` |
| **TS-OPT1 — SQL pre-aggregation guard for LTTB** | n/a (upstream has no LTTB downsampling layer) | shipped 2026-05-26 — `TimeseriesService.getDataPointsLttbOptimised()` routes large-window requests through a TimescaleDB `time_bucket()` pre-aggregation (returning only `target×5` rows) before LTTB; small windows and non-numeric types fall back to raw fetch. Activated automatically when `?downsample=lttb&max_points=N` is requested. No schema change, no config keys, no new endpoint. | **✓ ↑** (TS-OPT1) | `aidocs/34` TS-OPT1 row |
| **TS-OPT2 — Multi-channel bulk raw data endpoint + pool-fix** | n/a (upstream has no multi-channel bulk fetch endpoint) | shipped 2026-05-26 — `POST /v2/timeseries-containers/{containerId}/channels/data/bulk` accepts up to 200 `shepardIds` + time window; resolves all channels in one bulk JPQL query, fetches data sequentially (avoids PgBouncer pool exhaustion). Also fixes `parallelStream()` → sequential stream in `getManyTimeseriesWithDataPoints()` (TS-OPT2-POOL-FIX, commit a76bfa4e). | **✓ ↑** (TS-OPT2 + TS-OPT2-POOL-FIX) | `aidocs/34` TS-OPT2 row |
| **TS-OPT3-COPY — COPY-protocol bulk ingest endpoint** | n/a (upstream has no COPY-protocol ingest) | shipped 2026-05-26 (commit `69f26cc2`) — `POST /v2/timeseries-containers/{containerId}/channels/{shepardId}/data/ingest` uses PostgreSQL COPY protocol directly to TimescaleDB (3–5× faster than VALUES INSERT). Channel must pre-exist. Designed for historical bulk loads by import scripts. No ON CONFLICT handling; timestamps must be unique. | **✓ ↑** (TS-OPT3-COPY) | `aidocs/34` TS-OPT3-COPY row |
| **TS-CONFLICT-POLICY-1 — point-write defaults from silent last-wins to reject-on-duplicate** | upstream silently last-wins on `(channel, timestamp)` collision (`ON CONFLICT … DO UPDATE`) — masks accidental double-ingest | shipped 2026-06-27 (fire-250, PR pending) — new `ConflictException` (HTTP 409); `TimeseriesDataPointRepository.insertManyDataPoints(…, boolean overwrite)` uses `ON CONFLICT DO NOTHING` by default; throws when `affected < submitted`. Old 3-arg `saveDataPoints` delegates to `overwrite=true` (v1 wire byte-identical to upstream). New 4-arg `saveDataPoints(…, overwrite)` is the seam where v2 callers opt into reject semantics. 3 new `@QuarkusTest` cases. No schema migration. | **✓ ↑** (TS-CONFLICT-POLICY-1) | `aidocs/34` TS-CONFLICT-POLICY-1 row |
| **TS-CROSS-DO-VIEW — cross-DataObject timeseries view (channel-predicate keyed)** | n/a (upstream has no cross-DO TS aggregator) | shipped 2026-06-02; path renamed 2026-06-15 (APISIMP-CROSS-TS-BULK-PATH) — `POST /v2/data-objects/cross-timeseries-bulk` (was `/v2/timeseries/cross-data-object-bulk-data`) takes up to 100 DataObject appIds + one canonical channel-predicate IRI (e.g. `urn:shepard:afp:tcp-temperature-c`) and returns one LTTB-downsampled series per DO (default 500 pts / series, hard cap 5000). Reuses TS-OPT3 CAgg routing. Per-DO permission gate: forbidden DOs silently dropped (never 403 whole request); DOs without a matching channel return empty `points`. Deterministic pick on multi-match (first by `symbolicName` ASC). New `CrossDoChannelResolver` walks `DataObject → TSRef → Timeseries` and joins shared appId to `AnnotatableTimeseries → SemanticAnnotation`. V102 seeds the canonical "Cross-ply TCP temperature" VIEW_RECIPE `:ShepardTemplate` (`urn:shepard:afp:tcp-temperature-c` predicate, `urn:shepard:mffd:ply-number` axisGrouping). Frontend pane `CollectionCrossTrackViewPane.vue` mounted on Collection detail as the "Cross-track view" expansion panel — 4-column ECharts small-multiples grid with shared x/y range, hover-sync, and click-through to DO detail. Surfaced by `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md` GAP-2. 11 JUnit + 13 Vitest tests. | **✓ ↑** (TS-CROSS-DO-VIEW-1 + TS-CROSS-DO-VIEW-2-FE + TPL-VIEW-CROSS-PLY-TCP-1) | `aidocs/16` TS-CROSS-DO-VIEW-* + `aidocs/34` TS-CROSS-DO-VIEW-1 row + `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md §GAP-2` |
| **COLL-TIMELINE — Collection-landing process-chain timeline (swimlane chronograph)** | n/a (upstream has no Collection-level timeline view) | shipped 2026-06-02 — `GET /v2/collections/{appId}/timeline?binSizeDays=1\|7\|30\|90\|365` returns a swimlane envelope (`binSizeDays` echoed actual, `rangeStart`/`rangeEnd` ISO-UTC, `totalDataObjects`, `lanes[]` with `key`/`label`/`bins[]`). Lanes derived from distinct values of `urn:shepard:mffd:process-type` SemanticAnnotation (V100 seed); DataObjects without that annotation collect into an `unclassified` lane so non-MFFD Collections still render. Adaptive auto-coarsening: when span / requested-bin > 730 bins-per-lane, the ladder snaps upward (1 → 7 → 30 → 90 → 365). Bin counts and NCR / REJECTED status sums via single Cypher round-trip (`datetime().truncate('day')`); perf target ≤ 2 s for an MFFD-scale Collection (≈ 8.2k DOs × 5 lanes). Frontend: lazy-mounted Timeline `ExpansionPanelItem` on `/collections/{appId}` renders one ECharts stacked-bar swimlane per lane (green=OK, amber=NCR, red=REJECTED), Day/Week/Month bin-size toggle, hover tooltip "Lane, day — N DOs (n NCR, m REJ)", click drill-down to `?tab=data-objects&process-type=…&date=…` (filter pass-through tracked as `COLL-TIMELINE-DRILLDOWN-FILTER-1`). Bar click opens an action menu — "View DataObjects" (drill-down) or "Annotate this period" (opens `AddAnnotationDialog` scoped to the Collection with `prefill=urn:shepard:quality`; COLL-TIMELINE-ANNOTATE-1, shipped 2026-06-20). Cardinality-friendly answer where the Lineage graph collapses past ~500 nodes. Cache header `max-age=300, must-revalidate` (matches existing v2 convention; brief's `max-age=60` deviated for codebase consistency). 24 JUnit + 22 Vitest tests. Surfaced by `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md §GAP-8`. | **✓ ↑** (COLL-TIMELINE-1 + COLL-TIMELINE-1-FE + COLL-TIMELINE-ANNOTATE-1) | `aidocs/16` COLL-TIMELINE-* + `aidocs/34` COLL-TIMELINE-1 row + `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md §GAP-8` |
| **TS-CORE-SCHEMA-01 — 5-tuple moved to `channel_metadata` side table** | n/a | shipped 2026-05-26 — Flyway `V1.14.0` moves measurement/field/device/location/symbolicName from the hot `timeseries` table to a `channel_metadata` side table joined 1:1 via `timeseries_id`. JPA `@SecondaryTable` mapping preserves all existing callers transparently. Stepping stone for TS-SEMANTIC-01 (Neo4j dual-write). | **✓ ↑** (TS-CORE-SCHEMA-01) | `aidocs/34` TS-CORE-SCHEMA-01 row |
| **TS-SEMANTIC-01 — dual-write channel metadata to Neo4j `SemanticAnnotation` nodes** | n/a | shipped 2026-05-26 — `TimeseriesSemanticDualWriteService` called from `TimeseriesRepository.upsert()` on successful channel creation. Creates `AnnotatableTimeseries` + `SemanticAnnotation` nodes for each non-blank 5-tuple field. `AnnotatableTimeseries.appId` = `shepardId` (Postgres UUID) to bridge substrates for Phase 3. Best-effort: Neo4j failure is WARN-logged, Postgres write unaffected. 10 unit tests. Phase 3 (TS-SEMANTIC-02): flip reads to Neo4j and drop `channel_metadata`. | **✓ ↑** (TS-SEMANTIC-01) | `aidocs/16` TS-SEMANTIC-01 row + `aidocs/34` TS-SEMANTIC-01 row |
| **TS-SEMANTIC-REST — v2 REST + MCP surface for channel semantic annotations** | n/a | shipped 2026-05-27 — `GET/POST/DELETE /v2/timeseries-containers/{containerId}/channels/{channelShepardId}/annotations`. Two new MCP tools: `list_channel_annotations` + `create_channel_annotation`. **UI shipped 2026-06-02** — `ChannelAnnotationsPane.vue` (list / add / delete per channel) mounted on timeseries container detail page, confirmed by UI-GAP-SURVEY-2026-06-09. 5 unit tests. | **🅱️** (TS-SEMANTIC-REST — BE ✓, UI ✓) | `aidocs/16` TS-SEMANTIC-REST row + `aidocs/16` PLACEHOLDER-REPLACE-TS-SEMANTIC-REST (done 2026-06-02) + `aidocs/34` TS-SEMANTIC-REST row + `aidocs/agent-findings/ui-gap-survey-2026-06-09.md §UI-GAP-7` |
| **DB-OPT5 — DataObject list payload diet (`?fields=` + default-trim)** | n/a (v1 list endpoint emits the full `DataObjectIO` shape unconditionally) | shipped 2026-05-28 — `GET /v2/collections/{appId}/data-objects?fields=foo,bar` flat-CSV allow-list (GitHub REST convention) + default-trim that drops `description`, `attributes`, and the three deprecated `int` sibling counts. `id`/`appId`/`name` always included as identity. Unknown field → 400 before any DB hit. `?include=full` opt-back-in. New `X-Shepard-Payload-Diet` response header. Per-request `ObjectMapper.copy()` + `SimpleBeanPropertyFilter` keyed off `@JsonFilter("doListV2")`; allow-list reflected from the IO hierarchy so it stays in lock-step with the wire shape. Frontend `usePagedDataObjects` composable sends the panel-shaped 13-field allow-list. Measured: single-DO synthetic payload (600-char description + 12-attr) → default-trim 75.8% smaller than `?include=full`, panel `?fields=` 86.6% smaller, minimal-fields 93.8% smaller. 5-DO page end-to-end → default-trim 77.8% smaller. 10 (`DataObjectListFieldFilterTest`) + 8 (`DataObjectV2RestTest`) new unit tests. | **✓ ↑** (DB-OPT5) | `aidocs/16` DB-OPT5 row + `aidocs/34` DB-OPT5 row + `docs/reference/data-objects.md` |

## 5. API surface — additive endpoints

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| NDJSON streaming ingest for timeseries (`application/x-ndjson` on `POST /timeseriesContainers/{id}/payload`) | JSON-only | shipped | **✓ ↑** | P14 (commit `24d4585`) |
| Body-form selective RO-Crate export (`POST /collections/{id}/export` with `ExportSelection`) | GET-only | shipped — additive sibling, GET preserved | **✓ ↑** | R2 (commit `be0eb26`) |
| Per-payload selection (file OIDs / channel columns / time windows) | none | shipped | **✓ ↑** | R2b (commit `60a3ea1`) |
| Per-payload metadata-field redaction (closed enum of 6 fields) | none | shipped | **✓ ↑** | R2c (commit `f993e8b`) |
| Export emits permissions / versions / annotations / subscriptions documents | none | shipped (3 of 4 kinds via R2d, +subscriptions via R2d2) | **✓ ↑** | R2d / R2d2 |
| `application/merge-patch+json` PATCH semantics (P21x) | mixed shapes | shipped consistent across new endpoints | **✓ ↑** | P21x |
| `POST /sql/timeseries` curated SQL-over-HTTP for bulk reads | none | P10a + P10b + P10c shipped: `text/csv` default, `application/json`, `application/x-ndjson`; row + duration caps; `x-shepard-truncated` trailer; feature flag default-on; runtime-mutable caps via `GET/PATCH /v2/admin/sql-timeseries/config`. **UI is placeholder** — `/admin#sql-timeseries` mounts `PlaceholderFragmentPane` (slug `sql-timeseries`). Real admin pane tracked under `aidocs/16 PLACEHOLDER-REPLACE-P10c`. | **P10a+P10b ✓** / **🅰️ P10c (BE ✓, UI placeholder)** | `aidocs/29` + `aidocs/16` PLACEHOLDER-REPLACE-P10c + `aidocs/agent-findings/ui-scrutinizer-2026-05-30.md` |
| `DELETE /v2/containers/{appId}?force=…` — server-enforced safe delete collapsed onto unified surface | upstream always deletes (silently orphans references) | **Fully shipped.** DI1 + APISIMP-CONT-NS-COLLAPSE-6 (PR #1950) + APISIMP-CONT-NS-COLLAPSE-6b (fire-54). Unified `DELETE /v2/containers/{appId}?force=…` via `ContainerKindHandler.findLinkedDataObjectAppIds`; per-kind safe-delete REST resources deleted; `safeDeleteContainer.ts` on unified path. 409 + `SafeDeleteConflict` body (`{referenceCount, sampleDataObjectAppIds[≤10]}`) without `force`. Legacy `/shepard/api/{kind}Containers/{id}` DELETE unchanged (upstream wire frozen). Old `/v2/{kind}-containers/{id}` DELETE paths now 404. | **✓ ↑** (DI1 + APISIMP-CONT-NS-COLLAPSE-6 + 6b) | `aidocs/34` DI1 + APISIMP-CONT-NS-COLLAPSE-6/6b rows; `docs/reference/container-safe-delete.md`; `docs/help/delete-container-with-references.md` |
| `GET\|POST /v2/{kind}-containers/{id}/annotations` + `DELETE …/{annotationId}` — container-level semantic annotations | none | shipped — three new annotation REST resources extend `SemanticAnnotationRest` and reuse the type-generic `SemanticAnnotationService`. Three new frontend `Annotated*Container` classes; container detail pages now show a Semantic Annotations expansion panel. `:has_annotation` relationship and `BasicEntity.annotations` already existed — only the REST surface was missing. | **✓ ↑** (SA-CONT) | `aidocs/34` SA-CONT row; `docs/reference/container-annotations.md`; `docs/help/annotate-container.md` |
| `GET /v2/projects/{appId}` + `/sub-collections` + `/by-annotation/{p}/{v}` — Project-as-registry pattern (Collection carrying `urn:shepard:project = "true"` bundles non-exclusive child Collections via `urn:shepard:partOf`) | no concept | shipped 2026-06-02 — dedicated `/v2/projects/` namespace, runtime SHACL gate (`ProjectAnnotationConstraints`) on the existing `POST /v2/annotations` write path, four MCP tools, frontend top-nav route `/projects`, `CollectionSubCollectionsPanel.vue` auto-mounted on every Collection-detail page, `CollectionList.vue` row chip, idempotent `seed-mffd-collections.py` bootstrap. 32 backend + 7 frontend tests. | **✓ ↑** (PROJ-1) | `aidocs/integrations/121` · `aidocs/34` PROJ-1 row · `docs/reference/projects.md` · `docs/admin/runbooks/projects.md` |
| **SINGLETON-FILE-MIGRATION** — five-row tooling wave closing the single-file `:FileBundleReference` technical-debt loop (CLAUDE.md "Always: singleton FileReference for one-file uploads"). Sub-rows -01 (audit script) / -02 (URDF showcase seed migration) / -03 (PROV-O backfill wrapping V23) / -04 (UI singleton-default toggle) / -05 (SHACL `sh:Warning` guard). The data transformation itself stays in the already-shipped `V23__Split_singleton_bundles_to_FileReferences.java` (FR1b row above) — this wave adds the operator tooling around it. | no concept (upstream has only one File primitive — bundle-shaped) | shipped 2026-06-03 — `scripts/audit-single-file-bundles.py` + `scripts/backfill-single-file-bundles-to-singletons.py` + `backend/src/main/resources/shapes/single-file-bundle-warn.shape.ttl` + `DataObjectFileUploadDialog.vue` singleton-default + `docs/admin/runbooks/single-file-singletons.md`. 15 new Vitest cases on the dialog's mode-toggle behaviour. | **✓ ↑** (SINGLETON-FILE-MIGRATION) | `aidocs/16` §SINGLETON-FILE-MIGRATION · `aidocs/34` SINGLETON-FILE-MIGRATION-WAVE row · `docs/admin/runbooks/single-file-singletons.md` · `docs/reference/file-reference.md` |

## 6. Search

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| Unified `POST /search/v2` replacing legacy 5 routes | 5 legacy routes | TBD; gated on C5 | 📐 (queued, P7) | `aidocs/13` |
| Cursor pagination across paginated endpoints | mixed/missing | TBD | 📐 (queued, L6) | `aidocs/13` / `aidocs/18` |
| Search-as-you-type with tree/graph view | basic search page | TBD | 📐 (queued, L4) | `aidocs/13` / `aidocs/14` |
| Saved searches / search history | none | TBD | 📐 (queued) | `aidocs/13` |
| **UI-002 — Global header-search dropdown** (debounced multi-kind type-ahead) | header field exists but no dropdown — typing produces no results, no loading, no empty-state | `HeaderBar.vue` wired to new `useGlobalSearch` composer (composes existing `useCollectionSearch` + `useDataObjectSearch` (now optional `collectionId` ⇒ global mode) + `useContainerSearch`). 300ms debounce; three sectioned result lists (Collections / Data objects / Containers) with kind chips, each row clickable to detail route; explicit loading / empty / error states; footer link to Advanced Search; keyboard nav (Down to results, Enter to advanced page, Esc to close). 10 Vitest tests + 4 Playwright e2e tests (live). | **✓ ↑** (UI-002) | `aidocs/agent-findings/ui-002-header-search-fix-2026-05-24.md` |
| **SEARCH-V2-4-PRE — `GET /v2/users?q=` + `GET /v2/user-groups?q=` text-search endpoints** | `/shepard/api/search/users` + `/shepard/api/search/userGroups` (POST body; numeric id in responses) | New `UserSearchV2Rest` at `GET /v2/users?q=<text>` — OR-contains match across username / firstName / lastName / email; returns flat `List<UserIO>`; 400 on blank/null `?q=`. New `?q=` param on `GET /v2/user-groups` (existing `listUserGroups`): name-contains match via new `UserGroupSearchService.searchByText()`; returns paged `PagedResponseIO<UserGroupV2IO>`. Both use Jackson for injection-safe JSON query building. 6 unit tests. No numeric id on the wire. | **✅ shipped (PR #2273, fire-394)** | `aidocs/16` SEARCH-V2-4-PRE / SEARCH-V2-4 |
| **SEARCH-V2-4 — `useMemberSearch` + `usePermissionUserSearch` migrated from v1 SearchApi to v2 user/group endpoints** | `useShepardApi(SearchApi).searchUsers/searchUserGroups` (v1 POST body; complex JSON query params) | `useMemberSearch.ts` and `usePermissionUserSearch.ts` now use `useV2ShepardApi(UserApi).searchUsersV2({ q })` and `useV2ShepardApi(UserGroupsApi).listUserGroups({ q })`. `backend-client/UserApi` extended with `searchUsersV2` method; `ListUserGroupsRequest` gains `q?`. Groups mapped from `UserGroupV2` to `UserGroup` shape; equality in `EditPermissionsDialog` + `MemberPermissionList` migrated to `appId`. 12 new Vitest tests. Removes last `useShepardApi(SearchApi)` calls in the permissions subsystem. | **✅ shipped (PR #2275, fire-394)** | `aidocs/16` SEARCH-V2-4 |
| **APISIMP-MAP-PERMISSIONS-V2 — `mapPermissions.ts` `fetchUser` + `fetchUserGroupByAppId` migrated from v1 to `useV2ShepardApi`** | `useShepardApi(UserApi)` (v1 base URL) + `useUserGroupsV2().getUserGroup()` (v1 composable) | `fetchUser()` → `useV2ShepardApi(UserApi).value.getUser()`; `fetchUserGroupByAppId()` → `useV2ShepardApi(UserGroupsApi).value.getUserGroupV2({ appId })`; `v2ToUserGroup()` simplified (no string→Date conversion; backend-client types `createdAt: Date`). Removes `useShepardApi` + `useUserGroupsV2` imports from `mapPermissions.ts`. | **🔄 in-flight (fire-394, PR pending)** | `aidocs/16` APISIMP-MAP-PERMISSIONS-V2 |

## 7. Semantic annotations / knowledge graph

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| Annotate file / structured / spatial payloads (today: only timeseries + DataObjects) | timeseries + DataObjects only | TBD | 📐 (queued, L7) | `aidocs/14` |
| Nested annotation search | basic | TBD | 📐 (queued, #658) | `aidocs/14` |
| Term-search facet (search ontology terms) | none | TBD | 📐 | `aidocs/14 §6` |
| Better feedback on missing language labels | basic | TBD | 📐 (queued, #682) | `aidocs/14` |
| Refactor Neo4j representation of semantic annotations | older shape | TBD | 📐 (queued, #659) | `aidocs/14` |
| **QA-1 — quantified semantic annotations** (`numericValue: Double` + `unitIRI: String` on `:SemanticAnnotation`) | text-only annotations | `SemanticAnnotation` entity and `SemanticAnnotationIO` gain two optional nullable fields: `numericValue` (Double, numeric rendering) and `unitIRI` (String, e.g. QUDT unit IRI). Enables range queries and unit-aware comparison without parsing `valueName`. Both null on existing annotations. `V56` NOOP migration. | **✓ ↑** (QA-1) | `aidocs/34` QA-1 |
| **FAIR-1 — FAIR metadata fields on DataObjects** (`license: String` + `accessRights: String` on `:AbstractDataObject`) | no license / access-rights field | `AbstractDataObject` entity and `AbstractDataObjectIO` gain two optional nullable fields: `license` (SPDX expression, e.g. `CC-BY-4.0`) and `accessRights` (COAR Access Rights vocabulary term). Both `@JsonInclude(NON_NULL)` — absent from JSON when null. Settable via existing `PATCH /v2/...data-objects/{did}`. `V57` NOOP migration. | **✓ ↑** (FAIR-1) | `aidocs/34` FAIR-1 |
| **LIC1 — FAIR-1 completion: tightened semantics + UI + tests** (extends FAIR-1: enum-pinned `accessRights`, full create/edit/display surface, both Collection AND DataObject, comprehensive tests) | no license / access-rights field; no UI surface | Tightens FAIR-1: `accessRights` JavaDoc + `@Schema.enumeration` pinned to `OPEN \| RESTRICTED \| CLOSED \| EMBARGOED` (replacing the prior under-specified "COAR vocab term" comment). Fields land in the UI: `LicenseChip` / `AccessRightsChip` / `LicenseInput` / `AccessRightsInput` components; Collection list table gains an "Access" column with the color-coded badge; both detail pages (Collection + DataObject) show a FAIR metadata strip below the title; all four create/edit dialogs wire the fields. 14 backend Jackson tests on the wire contract + 13 Vitest cases on the curated SPDX_LICENSES + accessRights enum. Closes the deferred FAIR-1 test obligation. Takes Shepard from FAIR 6/12 → ~10/12. | **✓ ↑** (LIC1) | `aidocs/34` LIC1, `aidocs/semantics/98 §4.1` |
| **FAIR2 — `createdByOrcid` server-stamp on DataObject creation** (FAIR R1.2 — rich provenance for re-use) | no durable creator ORCID; provenance lost if User deleted | `AbstractDataObject.createdByOrcid` (nullable String) stamped server-side at create time from `User.orcid`; survives account deletion. Read-only in all IO shapes. DataObject detail page shows a linked ORCID badge. 4 Mockito unit tests. | **✓ ↑** (FAIR2, 2026-05-26) | `aidocs/34` FAIR2+FAIR3 |
| **FAIR3 — `embargoEndDate` field + `PublishService` enforcement** (FAIR A1.2 — access conditions machine-readable) | no embargo end-date; RESTRICTED/EMBARGOED entities could be published unchecked | `AbstractDataObject.embargoEndDate` (nullable ISO-8601 date String). `PublishService.publish(force=false)` rejects `RESTRICTED`/`EMBARGOED` entities. DataObject detail page: editable embargo chip + "Set embargo date" affordance. `V84` index migration. 4 Mockito unit tests. | **✓ ↑** (FAIR3, 2026-05-26) | `aidocs/34` FAIR2+FAIR3 |
| **FAIR7 — DMP snippet generator** (`GET /v2/collections/{appId}/dmp-snippet` + "Download DMP" button) | no DMP-export endpoint; researchers hand-assembled DMP text from scattered fields | Pure projection endpoint: `DmpSnippetV2Rest` + `DmpSnippetService` + `DmpSnippetIO`. Returns copy-paste-ready Markdown DMP block pre-filled with `license`, `accessRights`, `embargoEndDate` (latest across DataObjects), `createdByOrcid` (unique ORCIDs across DataObjects), payload kinds, DataObject count. Content negotiation: `text/markdown` (default) or `application/json` with `missingFields` array listing unpopulated FAIR fields. Auth: Read on Collection. No new entities, no migrations. 8 Mockito unit tests. **UI** (DMP-DOWNLOAD-NAV-01, 2026-05-30): "Download DMP" button on the Collection landing page next to `CiteThisCard.vue` calls the endpoint with `Accept: text/markdown`, downloads via `URL.createObjectURL` + `<a download>`, suggests a `<sanitised-name>-dmp-snippet.md` filename. 9 Vitest cases on the URL builder + filename sanitiser. The 2026-05-26 row was prematurely marked `✓` on backend-ship; this PR ships the actual UI surface that makes the row honest under the "backend + UI" definition of feature-complete. | **✓ ↑** (FAIR7, 2026-05-26 backend; 2026-05-30 UI) | `aidocs/34` FAIR7, `aidocs/16` FAIR7 + DMP-DOWNLOAD-NAV-01 |
| **RDM-001 — "Cite this dataset" card on Collection landing** (FAIR R1 — rich provenance for re-use) | no citation affordance on Collection landing — researcher hand-assembles citation from scattered fields | New frontend-only card directly below the Description on the Collection landing page. Renders APA-flavoured plain text / BibTeX / RIS / CSL JSON from existing wire fields (`name`, `createdBy`, `createdAt`, `license` post-LIC1, canonical URL). License-absent path omits the license line entirely. Pure formatter `frontend/utils/citation.ts` + Vuetify `v-tabs` card `frontend/components/context/collection/CiteThisCard.vue` + 46 Vitest cases + Playwright e2e on `/collections/42` and `/collections/661923`. No backend change. Highest per-day-of-effort win from the RDM Scrutinizer pass. Puts Shepard on par with Zenodo / Coscine / Dataverse for the funder-reviewing-the-dataset persona. | **✓ ↑** (RDM-001) | `aidocs/34` RDM-001, `aidocs/agent-findings/rdm-scrutinizer-2026-05-24.md §Top-5 #1`, `aidocs/agent-findings/rdm-001-cite-this-dataset-2026-05-24.md` |
| **RDM-005 — Metadata Completeness Score widget on Collection landing** (FAIR R1.1 / R1.3 — DMP-grade green/amber/red gauge) | no per-Collection FAIR-completeness signal — operators had no UI pressure to fill license / accessRights / annotation / ORCID gaps | New frontend-only card between the Cite-this card and the Semantic Annotations section. Renders a red/amber/green score chip (0–100, red < 50, amber 50–79, green ≥ 80) over a 9-check ladder: name + description-≥-50-chars + license (LIC1) + accessRights (LIC1) + creator-ORCID (RDM-002) + ≥1 semantic annotation + ≥1 lab-journal entry + heroImage + ≥1 DataObject. Each check carries a "Why this matters" tooltip mapping to DataCite §N + FAIR sub-principle; missing checks expose a tonal action button that `scrollIntoView`s the relevant on-page edit affordance (description / license-chip-strip / annotations heading / lab-journal panel / hero-image area / DataObjects panel) with a 1.6s primary-glow pulse — the ORCID check `router.push("/me")` instead. Pure scorer `frontend/utils/metadataCompleteness.ts` (no I/O, 36 Vitest cases including invariants + per-check pass/fail + band classification + boundary tests). Widget `frontend/components/context/collection/MetadataCompletenessCard.vue` fetches `SemanticAnnotation.getAllCollectionAnnotations` + `useFetchCollectionLabJournalEntries` + `UserApi.getUser` for the three off-shape inputs; auth-settle race fixed via watch on `useAuth().data.accessToken`. 5 Playwright e2e on live (`/collections/42` + `/collections/661923`). No backend change. Live LUMEN + MFFD-Dropbox both score 40/100 (red) at deploy — name + description + dataObjects pass, the six R1 fields fail. Closes the highest-recurring "more of the same checklist" complaint by being the one card that aggregates all FAIR gaps in one place. | **✓ ↑** (RDM-005) | `aidocs/34` RDM-005, `aidocs/agent-findings/rdm-scrutinizer-2026-05-24.md §Top-5 #5`, `aidocs/agent-findings/rdm-005-metadata-completeness-2026-05-24.md` |

## 7a. Internal semantic repository (neosemantics)

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| Pre-seeded common ontologies (PROV-O / Dublin Core / schema.org / FOAF / QUDT / OM-2 / W3C Time / GeoSPARQL / OBO Relation Ontology / NFDI4Ing metadata4ing / SIMAT / Lumen-inspired / NASA Thesaurus / shepard-experiment / CHAMEO / W3C SSN-SOSA / IEC-61360 / metadata4ing-HPMC / W3C iot-lite) inside the existing Neo4j | none — operator must wire an external triple store | `OntologySeedService` ships nineteen bundles via `ontologies-manifest.json`. Seventeen are SHA-256-pinned Turtle stubs; **NASA Thesaurus** (N1e) is fetched at startup via `n10s.rdf.import.fetch("https://sti.nasa.gov/docs/thesaurus/thesaurus-SKOS.xml", "RDF/XML")` — full ~18,400 concepts, 1.1 M triples; 120-concept propulsion stub used as offline fallback; **metadata4ing** (M4I-a) also uses `fetchUrl`/`fetchFormat` to pull the full 1.4.0 canonical TTL. `OntologyEntry` carries `fetchUrl`/`fetchFormat` fields; `seedOne()` prefers URL-fetch over inline. `invokeFetch()` visible-for-tests seam mirrors `invokeImport()`. Toggles: `shepard.semantic.internal.preseed-ontologies.{enabled,skip-bundles}` (default on), plus N1c2's runtime `:SemanticConfig` singleton (admin REST/CLI). `prov-o` + `obo-relations` required (admin disable refused 409). RO bundle added by ONT1a; metadata4ing by ONT1b; shepard-experiment by AI1r; NASA Thesaurus + extended index (V51) by N1e; CHAMEO + SSN/SOSA + IEC-61360 + HPMC by N1k (2026-05-27); iot-lite by IOT1 (2026-05-30). | **✓ ↑** (N1b + ONT1a + ONT1b + N1c2 + AI1r + N1e + N1k + IOT1) | `aidocs/48 §4` + `aidocs/58 §6` + `aidocs/65` + `aidocs/16` AI1r + N1e + N1k + IOT1 |
| Domain experiment ontology — 7 SKOS ConceptSchemes (ExperimentPhase / MeasurementRole / QualityFlag / DefectType / InspectionMethod / ManufacturingProcess / SensorRole) for annotation of engineering experimental data | none | OWL-Lite + SKOS stub at `backend/src/main/resources/ontologies/shepard-experiment.ttl`; 43 concepts across 7 schemes; ExperimentPhase subclasses prov:Activity for PROV1 provenance stamping; QUDT QuantityKind hook on calibration concepts. Picked up automatically by the manifest-driven `OntologySeedService` loop. | **✓** (AI1r) | `aidocs/16` AI1r |
| `SemanticRepositoryType.INTERNAL` enum value + `InternalSemanticConnector` (Cypher / SPARQL via `n10s`) | none | shipped — compose installs `n10s`; `N10sBootstrapHook` runs post-A1e; fail-soft when plugin absent. **Testcontainer-level integration test deferred** (unit coverage via mocked OGM session is in place). | **✓ ↑** (N1a, this commit) | `aidocs/48 §3` |
| `shepard-admin semantic refresh-ontologies` CLI | none | New `POST /v2/admin/semantic/refresh-ontologies` endpoint (instance-admin gated) + matching `shepard-admin semantic refresh-ontologies [--bundles=…] [--force]` CLI subcommand. Walks the bundled-ontology manifest, fetches each bundle's pinned canonical URL, recomputes SHA-256, and re-imports via `n10s.rdf.import.inline` when the hash differs. Best-effort per bundle (partial failures land in `errors[]` with 200 OK overall). Reuses the `shepard.semantic.internal.*` config namespace — no new keys. Coverage: backend 87%/74% line/branch on `OntologyRefreshService`, 100%/92% on `SemanticAdminRest`; CLI command 92% line / 92% branch. | **✓ ↑** (N1c, this commit) | `aidocs/48 §4` + `aidocs/22 §7.1` |
| Admin-configurable ontology preseed — runtime enable/disable + custom-bundle upload | none | Five new endpoints under `/v2/admin/semantic/ontologies` (instance-admin gated): `GET` merged-list, `POST {id}/enable`, `POST {id}/disable` (409 for required), `POST` multipart upload (10 MB cap, SHA-256, parse-shape heuristic), `DELETE {id}` (409 for built-ins). Backed by `:SemanticConfig` singleton (V27) + `:UserOntologyBundle` catalogue (V28) + `OntologyConfigService`. First-start seeds the singleton from deploy-time defaults; runtime row wins forever after (per CLAUDE.md "Always: surface operator knobs"). Five new CLI subcommands `shepard-admin semantic ontologies {list,enable,disable,upload,remove}`. New deploy-time-only key `shepard.semantic.internal.user-bundles-dir` (default `/var/lib/shepard/ontologies/`). Coverage: `OntologyConfigService` 90.8% line / 78.4% branch; `SemanticAdminRest` 97.9% line / 85.7% branch; `RuntimeConfig` 100/100. | **✓ ↑** (N1c2, this commit) | `aidocs/65` + `aidocs/22 §7.1` |
| **SEMA-V6-001/002/003 — consistent annotation surface foundation** — (001) 8 new nullable `@Property` fields on `:SemanticAnnotation` (`subjectKind`, `subjectAppId`, `vocabularyId`, `sourceMode`, `sourceActivityAppId`, `validFromMillis`, `validUntilMillis`, `confidence`); `hashCode`/`equals` extended; V71 adds two Neo4j indexes (`SemanticAnnotation_subjectAppId_idx`, `SemanticAnnotation_vocabularyId_idx`); 6 tests. (002) Two new Neo4j entity types `:Vocabulary` + `:Predicate` with `VocabularyDAO`/`PredicateDAO`; V72 creates 4 uniqueness constraints + seeds 10 bootstrap vocabularies (Dublin Core, PROV-O, schema.org, DataCite, CHAMEO, Material OWL, Shepard internal, m4i, SKOS, GeoSPARQL) keyed on `uri` (idempotent MERGE); 15 tests. (003) `:SemanticConfig` gains 4 new fields (`defaultVocabularyAppId`, `annotationMode`, `suggestionEnabled`, `suggestionModelId`); V73 idempotent coalesce backfill; new `SemanticConfigRest` at `GET`+`PATCH /v2/admin/semantic/config` (RFC 7396 merge-patch, instance-admin); `patchConfig()` in `OntologyConfigService`; 9 tests. | none — no typed annotation substrate, no vocabulary registry | **✓ shipped** — 30 new tests across 4 classes; V71/V72/V73 all idempotent and schemaless-safe | **✓ ↑** (SEMA-V6-001/002/003, `dc43bfdc`, 2026-05-26) | `aidocs/34` SEMA-V6 row + `aidocs/16` SEMA-V6-001/002/003 → done |
| **SEMA-V6-009 — `SemanticVocabularyProvider` SPI** — CDI `@ApplicationScoped` interface + `VocabularyPredicateDefinition` record + `SemanticVocabularyRegistry` CDI dispatcher (follows `AnalyticsRegistry` pattern: `StartupEvent` observer, duplicate-IRI handling, immutable snapshot, package-private test constructor) + `package-info.java` under `de.dlr.shepard.spi.semantic`. Reference implementation `GeoTimeVocabularyProvider` in `shepard-plugin-spatiotemporal` (renamed from `shepard-plugin-spatial` by SPATIAL-V6-001) contributes 5 predicates: `geosparql:hasGeometry`, `geosparql:asWKT`, `geosparql:sfWithin`, `time:hasBeginning`, `time:hasEnd`. Discovery is CDI-only (no `META-INF/services`). 10 backend unit tests + 9 plugin unit tests. `plugins/spatiotemporal/docs/reference.md` updated with vocabulary section. | none — no vocabulary SPI; plugins cannot contribute annotation predicates | **✓ shipped** — `SemanticVocabularyProvider`, `VocabularyPredicateDefinition`, `SemanticVocabularyRegistry`, `package-info.java` in core + `GeoTimeVocabularyProvider` in `shepard-plugin-spatiotemporal`; no REST endpoint yet (deferred) | **✓ ↑** (SEMA-V6-009, 2026-05-26; plugin renamed SPATIAL-V6-001, 2026-05-27) | `aidocs/16` SEMA-V6-009 → done |
| **SEMA-V6-004 — polymorphic `/v2/annotations/*` REST surface** — 7 endpoints: list (filters: `subjectAppId`, `subjectKind`, `predicateIri`, `vocabId`; pagination), find (text search), get-by-appId, create (POST with XOR literal/IRI object invariant), update (PUT RFC 7396 merge-patch), delete (hard), Turtle export (`GET /v2/annotations/{appId}/export/turtle` OA-framed). Permission gate: Collection subjects via `EntityIdResolver`+`isAccessTypeAllowedForUser`; all others via `isAccessAllowedForDataObjectAppId`. Legacy annotations (null `subjectAppId`) granted read, denied write. 32 unit tests. | none — no polymorphic annotation REST surface; v1 per-entity annotation endpoints are subject-kind-specific and not v2-shaped | **✓ shipped** — `SemanticAnnotationV2Rest`, `SemanticAnnotationV2DAO`, 3 IO classes, 32 tests; no new migrations (reuses SEMA-V6-001 columns) | **✓ ↑** (SEMA-V6-004, this commit, 2026-05-26) | `aidocs/34` SEMA-V6-004 row + `aidocs/16` SEMA-V6-004 → done |
| **F9 — `POST /v2/annotations` Reference-subject permission fix** — `checkAccessForSubject()` now detects Neo4j labels ending in "Reference" and walks Reference → DataObject → Collection via `ReferencesV2Service.resolveByAppId()` before gating. Previously any annotation on a Reference entity returned 403 (MFFD bridge import: ~4963 silently denied writes). Also fixes `resolveWithLabels` test NPE from `f9ecbd8f`; supersedes PR #2017. 44 unit tests. No migration. | n/a — upstream has no `/v2/annotations` REST surface | **✓ shipped** 2026-06-20 — branch `F9-ANNOTATION-REFERENCE-PERMISSION`; 44 tests pass | **✓ ↑** (F9, 2026-06-20) | `aidocs/34` F9 row + `aidocs/16` F9 → done |
| **SEMA-V6-013 — annotation delete-policy operator knob** — `annotationDeletePolicy` field on `:SemanticConfig` singleton (values: `'author-or-manager'` default / `'author-only'` / `'manager-only'`). `PATCH /v2/admin/semantic/config` wired to validate + apply. `SemanticAnnotation` gains `agentUsername` property captured at create time. DELETE endpoint policy gate: `isAuthor = caller.equals(agentUsername)` + `isManager = Manage-access check` evaluated against the active policy. 3 new unit tests in `SemanticAnnotationV2RestTest`. No migration (nullable fields, null = safe defaults). | no annotation delete policy — any writer could delete any annotation | **✓ shipped** — policy gate in `SemanticAnnotationV2Rest.delete()`, `agentUsername` on `SemanticAnnotation`, `annotationDeletePolicy` on `SemanticConfig` + IO classes + `SemanticConfigRest` validation; 3 new tests | **✓ ↑** (SEMA-V6-013, 2026-05-26) | `aidocs/34` SEMA-V6-013 row + `aidocs/16` SEMA-V6-013 → done |
| **SEMA-V6-007 — `:Activity` provenance capture for annotation writes** — `ProvenanceService.record()` called inline in `SemanticAnnotationV2Rest.create()`/`update()`/`delete()`; Activity `appId` back-stamped onto `:SemanticAnnotation.sourceActivityAppId` (Cypher SET via `SemanticAnnotationV2DAO.setSourceActivityAppId()`); returned in 201/200 response body; `X-AI-Agent` header detection: presence + no explicit `sourceMode` → `sourceMode="ai"` (EU AI Act Art. 50 transparency hook); `ProvenanceCaptureFilter.PROP_SKIP_CAPTURE` prevents duplicate Activity rows; 3 new tests (createAnnotation_writesSourceActivityAppId, updateAnnotation_writesNewSourceActivityAppId, createAnnotation_withAiAgentHeader_setsSourceModeAi). | none — no annotation-level provenance capture | **✓ shipped** — no new migrations (field already existed on `:SemanticAnnotation` from SEMA-V6-001) | **✓ ↑** (SEMA-V6-007, 2026-05-26) | `aidocs/16` SEMA-V6-007 → done |
| **SEMANTIC-ANNOTATE-BULK-REST-1 — `POST /v2/annotations/bulk` (bulk-create up to 100 annotations per round-trip)** — `SemanticAnnotationV2Rest.bulkCreate()` accepts `List<CreateAnnotationIO>` (max 100); best-effort per row (failed rows do not abort batch); returns `BulkAnnotationResultIO { requested, succeeded, failed, results[] }` with per-row `BulkAnnotationItemResultIO { ok, appId, subjectAppId, error }`. `X-AI-Agent` header sets `sourceMode=ai` on all rows. Single fire-and-forget summary `:Activity` with `PROP_SKIP_CAPTURE`. Frontend stub `BulkAnnotatePane.vue`. 8 new JUnit tests (65 total in `SemanticAnnotationV2RestTest`). | none — no bulk annotation endpoint upstream | **✓ shipped** 2026-07-01 — REST surface `POST /v2/annotations/bulk`; frontend stub; no migrations | **✓ ↑** (SEMANTIC-ANNOTATE-BULK-REST-1, fire-350) | `aidocs/16` SEMANTIC-ANNOTATE-BULK-REST-1 → done; `aidocs/34` SEMANTIC-ANNOTATE-BULK-REST-1 row |
| **SEMANTIC-ANNOTATE-BULK-UI-1 — multi-select bulk annotation in DataObject list** — checkbox column + selection action bar + `BulkAnnotationDialog.vue` wired to `POST /v2/annotations/bulk`. Select N DataObjects via checkboxes → "Bulk annotate" → predicate+value combobox (same `useTermSearch` autocomplete as `AddAnnotationDialog`) → single bulk call. Inline partial-failure alert. `BulkAnnotatePane.vue` updated to informational reference pane. 13 Vitest unit tests. | none — no bulk annotation upstream | **🚧 in-progress** fire-353 (branch `SEMANTIC-ANNOTATE-BULK-UI-1-1`); no migrations | 🚧 (SEMANTIC-ANNOTATE-BULK-UI-1, fire-353) | `aidocs/16` SEMANTIC-ANNOTATE-BULK-UI-1 row; PR pending CI |
| LUMEN seed integration — placeholder IRIs replaced with real PROV-O / QUDT / SKOS terms; SPARQL demo cell in notebook | seed uses placeholder IRIs only | TBD | 📐 (queued, N1d) | `aidocs/48 §5` |
| Annotation picker auto-completes from pre-seeded ontologies | none | `GET /v2/semantic/terms/search?q=…` searches n10s `:Resource` nodes. `AddAnnotationDialog.vue` IRI fields replaced with `v-combobox` (300ms debounce, min 2 chars, free-text fallback). **V50** fixed fulltext index for IGNORE-mode property names (`label`, `prefLabel`, `altLabel`, `name`, `title`); **V51** (N1e) extended index to add `hiddenLabel`, `notation`, OBO synonyms (`hasExactSynonym`, `hasRelatedSynonym`, `hasBroadSynonym`, `hasNarrowSynonym`), `alternateName`. `CONTAINS_CYPHER` fallback extended to match all the same properties plus `scopeNote`. `stripLangSuffix()` strips `@en`/`@de-CH` BCP-47 suffixes embedded by n10s IGNORE mode. `SemanticRepositoryPane.vue` probes `?q=aa&limit=1` when an INTERNAL repo is present and shows a warning banner when the result is empty (no labels loaded). 32 unit tests. | **✓ ↑** (N1e) | `aidocs/48` + `aidocs/14` |
| **L4 — Ontology search-as-you-type with tree + graph view** — standalone `/semantic/search` page: debounced (300 ms, min 2 chars, race-guarded) live search over every loaded n10s `:Resource` term via the existing `GET /v2/semantic/terms/search` (N1e) endpoint — **no backend change**. Matched terms render in a derived two-level namespace→term hierarchy (IRI split at last `#`/`/`; standard ontology-browser fallback when no broader/narrower edges exist — BioPortal usage study arXiv:1610.09160, FAIR URI-design arXiv:2003.13084) shown as a `v-list` tree **or** an ECharts+dagre graph (reuses `baseGraphSeriesConfig` + `@dagrejs/dagre`, no new lib). Each leaf links to the predicate detail + SPARQL playground. Entry points: `SemanticPane` quick-link + "Search ontology terms" button on `/semantic/vocabularies`. | none — no term discovery surface; N1e only fed the in-dialog picker | `frontend/utils/ontologyTermTree.ts` (pure helpers: `namespaceOf`/`shortPrefixOf`/`buildTermTree`/`countTerms`/`buildTermGraph`) + `frontend/composables/semantic/useOntologySearch.ts` (wraps existing `useTermSearch`) + `frontend/pages/semantic/search.vue`; 28 Vitest unit tests (`ontologyTermTree.test.ts` + `useOntologySearch.test.ts`). Frontend-only, v2-only, ungated, non-breaking. | **✓ ↑** (L4, 2026-06-29) | `aidocs/16` L4 + `aidocs/semantics/13` |
| **UI19 — `skos:related` concept discovery sidebar in `AddAnnotationDialog`** — when a value IRI is selected, issues `POST /v2/semantic/internal/sparql` with a VALUES-bound `skos:related`/`rdfs:label` SELECT query; results surface as a compact `v-chip-group` below the value preview via `v-expand-transition`; clicking a chip applies it as the new value. `useSkosRelated` composable mirrors `useSparqlPlayground` pattern (IRI validation guard, deduplication, swallow-on-error). | none | `frontend/composables/semantic/useSkosRelated.ts` + `AddAnnotationDialog.vue` updated; 13 Vitest unit tests in `frontend/tests/unit/useSkosRelated.test.ts`. No backend change — uses existing SPARQL proxy (`N1f`). | **✓ ↑** (UI19, 2026-05-27) | `aidocs/16` UI19 |
| **SPARQL proxy** — `GET/POST /v2/semantic/{repoAppId}/sparql` with auth gate, read-only guard (SELECT/ASK only; CONSTRUCT/INSERT/DELETE/UPDATE/DROP/… → 400), type dispatch (`INTERNAL` → n10s HTTP `POST /rdf/neo4j/sparql`; `SPARQL` → external endpoint proxy; `JSKOS`/`SKOSMOS` → 501), W3C SPARQL Results JSON response | none — no SPARQL access without direct Neo4j/Fuseki credentials | `SemanticSparqlRest` + `SparqlQueryValidator` + `SemanticRepositoryDAO.findByAppId`. New config key `shepard.semantic.internal.http-url` (default `http://neo4j:7474`). 37+21 unit tests. Integration test (n10s HTTP round-trip) deferred — tracked in `aidocs/34` N1f row. **UI shipped**: `frontend/pages/semantic/sparql/index.vue` playground reachable via `SemanticPane` hub-tile (4-click depth — lifts to 1-click when TOOLS-NAV-01 lands). Reconciled 2026-05-30 (MATRIX-N1F-RECONCILE-01). Runtime defect "404 on default `internal` repo" tracked separately as task #244. | **✓** (N1f, 2026-05-30) | `aidocs/16` N1f + MATRIX-N1F-RECONCILE-01 |
| **SHACL-1 — Jena SHACL validator + `POST /v2/shapes/validate`** — in-process Apache Jena `jena-shacl:5.4.0` validator (Apache-2.0); `JenaShaclValidator` (`@ApplicationScoped`) takes `{dataTurtle, shapeTurtle}` as Jena `Model`s and returns a `Report` with conformance + per-finding details (focusNode / resultPath / value / severity / message). REST surface: `POST /v2/shapes/validate` (validate-only contract — catalog is Template `kind=view` per aidocs/101 v2). Bad Turtle returns 200 with `parseError` set (NOT 400) so the MCP tool can branch on it. | none — no SHACL validator surface exists upstream | `de.dlr.shepard.v2.shapes.*` (validator + REST + IO). Foundation PR-1 + PR-4 of the SHACL changeover (brief 2026-05-22). Substrate (PR-2 — n10s → Jena Model extraction) DEFERRED — current contract is "client supplies the candidate payload + shape". 14 unit tests (`JenaShaclValidatorTest` 7 + `ShapeValidationReportIOTest` 3 + `ShapesValidateRestTest` 4). | **✓ ↑** (SHACL-1, this commit) — backend foundation ; **🚧 in-flight** for PR-2 / PR-5 / PR-6–8 / PR-9 | `aidocs/34` SHACL-1 + `aidocs/semantics/95 §11` + `aidocs/semantics/98` |

## 8. Provenance / lineage

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| OpenLineage events across the pipeline | none | TBD | 📐 (queued) | `aidocs/30` |
| `direction=upstream/downstream/both` lineage walk endpoint | none | TBD | 📐 | `aidocs/30 §4` |
| `sh.lineage.upstream(app_id, depth=N)` Python helper | none | TBD | 📐 | `aidocs/30 §5` |
| **SHACL-1b — HMAC audit chain on `:Activity`** — per-instance `instance_secret` carried on a `:InstanceConfig` Neo4j singleton (A3b/N1c2/UH1a pattern), seeded on first start from `SHEPARD_INSTANCE_SECRET` env or `SecureRandom`; every `Activity` write is stamped with `HMAC-SHA256(secret_v_n, prevHmac ‖ activityCanonical)` carrying `secretVersion` so rotation never invalidates the chain. Verifier (`HmacChainService.verify`) walks `prev → prev.prev → …` and picks the right key per chain segment by `secretVersion`. Best-effort: a chain stamp failure never blocks the user write (provenance is observability, not contract). | none — no tamper-evident audit on provenance rows | `de.dlr.shepard.provenance.entities.InstanceConfig` + `daos.InstanceConfigDAO` + `services.InstanceConfigService` + `services.HmacChainService`. `Activity` gains `auditHmac` / `auditPrevHmac` / `secretVersion` (all nullable). V59 (`:InstanceConfig` appId uniqueness, idempotent) + V60 (NOOP placeholder for the PR-5 predicate lift). 15 unit tests (`HmacChainServiceTest` 11 + `InstanceConfigServiceTest` 4). Rotation REST/CLI + cross-rotation verification test DEFERRED. | **✓ ↑** (SHACL-1b, this commit) — chain stamping live ; **🚧 in-flight** for the operator rotation surface | `aidocs/34` SHACL-1 + `aidocs/semantics/98 §Changelog v2-3` |
| **IMPORT-W1+W2+W3 — Smart warmup phase (v15.2)** — Script-side fail-fast warmup that probes auth + reads + source/dest writes + Garage against both sides BEFORE the main forging loop starts, diffing every response against the in-tree `backend/src/test/resources/fixtures/v5/openapi-5.4.0.json` (the canonical v5.4.0 wire-contract source). Structured diagnostic on failure (verb / url / expected / got / where-to-look / what-to-try / exit-code) plus a distinct integer per failure class (auth=2, source-unreachable=3, garage-down=4, operator-interrupt=5, wire-shape-drift=6, write-permission-denied=7). Throwaway probe DataObjects are deleted on every exit path. `--smart-warmup` default on; `--legacy-warmup` opts back to v15.1's read-only probe. 3/3 probed endpoints have schema coverage in the spec. | none — upstream has no warmup phase beyond a single startup ping | `examples/mffd-showcase/scripts/_smart_warmup.py` (~860 LOC including docstrings, stdlib + requests only). Co-located comparator (`compare_against_openapi`) walks one level of `properties` + `required`, resolves `$ref` + `allOf`, gracefully skips endpoints with no spec schema. 22 new tests; total suite 140 (was 118). No backend Java, no migration. | **✓ ↑** (IMPORT-W1, IMPORT-W2, IMPORT-W3, this commit) | `aidocs/16` IMPORT-W1/W2/W3 + `aidocs/agent-findings/v15.2-implementation.md` |
| **Metadata4Ing (m4i) deepening** — six-slice design (M4I-a through M4I-f) covering: (a) promote bundle stub → fetch canonical 1.4.0 TTL on first seed (NASA-Thesaurus N1e pattern); (b) **CRITICAL fix** — `ProvJsonLdRenderer` today emits non-canonical predicates `m4i:hasMethod` / `m4i:hasInput` / `m4i:hasOutput` that do **not exist** in canonical m4i — rewrite to `m4i:realizesMethod` + `obo:RO_0002233/4` (OBO Relations Ontology, already preseeded); (c) new `m4i:DataObjectShape` SHACL contract + `M4iDataObjectRenderer` + `Accept: application/ld+json; profile=metadata4ing` on `GET /v2/data-objects/{appId}` (mirrors PROV1h); (d) promote `:Activity.actionKind` + instruments + numeric annotations to first-class `m4i:Method` / `m4i:Tool` / `m4i:NumericalVariable` individuals; (e) operator runbook for NFDI4Ing federation (TS + Archetype Doris + Unhide); (f) HPMC sub-ontology bundle. | shipped m4i footprint already present (ONT1b stub bundle + PROV1h content-neg + UH1b feed inlining of `m4i:hasProcessingStep` / `m4i:hasIdentifier`); upstream has zero m4i awareness | **M4I-a + M4I-b + M4I-f shipped** (2026-05-26 / -27); M4I-c/d/e queued under `aidocs/16` §M4I | **✓ ↑** M4I-a/b/f shipped ; 🚧 in-flight for M4I-c/d/e | `aidocs/semantics/94-metadata4ing-integration-design.md` + `aidocs/16` M4I-a..f |
| **PROV-V15.1 — MFFD import v15.1 (snapshot brackets + per-DO F(AI)²R mode + FAIR R1 license + independent verifier + worker-pool wiring)** — Script-side closing of four persona-review gaps surfaced on v15: (G1+G4+G5) pre-import + post-import snapshot brackets with `as-imported` label discipline and Collection-anchored `prov:wasInformedBy`/`prov:generated` snapshot lineage triples (full G5 entity typing awaits backend SnapshotService extension); (G2+G8) per-DO F(AI)²R triples (`fair2r:modeOfProduction "ai"`, `fair2r:wasAcceptedAs "auto-applied"`, `fair2r:wasGeneratedByAi agent:claude-opus-4-7`) closing EU AI Act Art. 50 per-artefact-visibility gap (deadline 2026-08-02); (G7) typed predecessor edges via `prov:wasInformedBy` SHACL triples mirroring Neo4j `:PREDECESSOR_OF` so ODIX/RO-Crate see the lineage; FAIR R1 license + accessRights hydration with fail-fast gate on SOURCE/LOCAL paths; operator name preservation via `--name-mapping <csv>` + `dcterms:alternative` annotation; minimum-viable independent verifier (`--verify-imported` walks dest, counts DOs by pattern, file refs by `fileSize>0`, TS refs with non-empty `timeseries[]`, DAG spot-check); worker-pool wiring (`--workers N`, default 1 = sequential pass-through, N>1 instantiates `ThreadPoolExecutor` + bounded `Queue(256)`). | none — upstream has no provenance import discipline | `examples/mffd-showcase/scripts/mffd-import-v15.py` (+800 LOC). Script-only — no backend Java, no migration. 47 new unit tests across 7 files; total 118 stdlib unittest cases. | **✓ ↑** (PROV-V15.1, this commit) — script ready for the live MFFD ingest | `aidocs/integrations/93 §15` + `aidocs/agent-findings/v15.1-implementation.md` + `aidocs/agent-findings/v15-review-{data-ontologist,rdm,ime-aqe,reluctant-senior}.md` |
| **Cross-instance provenance UI** — design landed: typed `GET /v2/data-objects/{appId}/provenance` aggregator endpoint over `source_*` attrs + dest createdBy/At + `:Activity` log; new frontend components (`<SourceProvDisplay>`, `<SourceInstanceBadge>`, `<SourceAuthorChip>`, `<SourcePermissionsAudit>`, `<ProvenanceEmptyState>`) rendering a "who authored this on which instance" header strip on DataObject + Collection pages plus an EN 9100 audit pane (≤ 3-click test) and a permissions snapshot diff. Closes the today-broken FAIR-R hole where dest shows `createdBy = importer-api-key` for every migrated artefact. M4I-b migration seam — the aggregator's substrate switches transparently from opaque attrs to typed PROV-O edges. Backfill UX (`original` vs `backfilled` stamping) surfaces `:Activity`-traceable reconstruction. Visual language: hexagon-icon instance badges + person-icon author chips; colour-blind safe. 8 backlog rows filed under `FE-PROV-*` + `MFFD-IMPORT-{INSTANCE-URL,ATTRS-PROVENANCE}`. | none — upstream has no cross-instance prov rendering | designed (`aidocs/frontend/100`) | **📐 ↑** (designed 2026-05-23) | `aidocs/frontend/100-cross-instance-prov-ui.md` + `aidocs/agent-findings/v5-metadata-enrichment-survey-2026-05-23.md` + `aidocs/16` FE-PROV-* |
| **PROV-USER-MIRROR-ENDPOINT — `POST /v2/admin/users/mirror` (`:MirroredUser` shadow node, #229)** — new `MirroredUser extends AbstractEntity` Neo4j entity; idempotent admin endpoint MERGE on `(sourceInstance, sourceUsername)` pair; 201 on first create, 200 on re-call (same `appId`); V74 migration adds `appId` uniqueness + composite idx; PROV1a auto-captures the POST as an `:Activity`. Fields: `sourceInstance`, `sourceUsername`, `sourceDisplayName?`, `sourceEmail?`, `derivedFrom?` (recursive self-pointer for future multi-hop chains). Closes the attribution gap left by MFFD-IMPORT-USER-CAPTURE (v15.9 forwarded `X-Source-User-*` headers but the dest had no user-mint endpoint). Next step: PROV-USER-ENRICH wires those headers to resolved `:MirroredUser` nodes. | none — upstream has no cross-instance user mirroring | `de.dlr.shepard.auth.users.entities.MirroredUser` + `…daos.MirroredUserDAO` + `…v2.admin.users.MirroredUserRest` + `V74` migration + 9 unit tests (`MirroredUserRestTest`) | **✓ ↑** (PROV-USER-MIRROR-ENDPOINT, 2026-05-26) | `aidocs/34` PROV-USER-MIRROR-ENDPOINT row + `aidocs/16` PROV-USER-MIRROR-ENDPOINT |

## 9. Identifiers via `/v2/` payload kinds (designed, not yet shipped)

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| HDF5 / HSDS as a payload kind (`HdfContainer` / `HdfReference`); `h5pyd` parity | none | HSDS sidecar + shared-Keycloak token relay (`aidocs/35`). A5a ships: `HdfContainer` create/read/delete + `hdf` compose profile + V25. A5b ships: permission bridge + `POST /v2/admin/hdf/rebuild-acls`. A5c ships: `HdfReference extends BasicReference` per-DataObject anchor at dataset path; `GET/POST /v2/data-objects/{appId}/hdf-references` + `DELETE .../hdf-references/{refAppId}`; `V84` migration; `HdfReferencesPane.vue` on DataObject detail. **A5c-annotation ships**: `hdf` vocabulary (V87), `hdf-shapes.ttl` SHACL shape, `mdi-tag-outline` Annotate button per row wired to polymorphic `AnnotationDialog`. A5d ships: offline HDF5 download via HSDS bulk-export; Range pass-through. A5e still queued. **UI pending**: no HDF container list, create, or dataset-browse UI (PLUGIN-HDF5-AUDIT-2026-05-24-002). | **⚙ BE ✓ (A5a + A5b + A5c + A5c-annotation + A5d)** / UI ✓ (HdfReferencesPane + annotation button) / 📐 (A5e queued) | `aidocs/35` + `aidocs/63` |
| Git integration (`GitReference`); mode-a loose link + mode-b tracked artifact with inline preview (`GET …/preview`, PT5M cache); mode-c pinned snapshot (SHA resolved at create/PATCH time, frozen, server-managed); RO-Crate export emits `schema:SoftwareSourceCode` contextual entity for every `GitReference`; host adapters for GitLab + GitHub + Gitea; CRUD via `/v2/data-objects/{appId}/git-references`; UI renders loose-link as clickable link; "Download as RO-Crate" button on collection detail page (UI8) | none | Backend ✓ (G1a / G1b / G1c / G1d); UI ✓ (G1-cred UI, Git refs pane, RO-Crate download button) | **✓ ↑ (backend, G1a + G1b + G1c + G1d)** / **✓ UI (RO-Crate download button, UI8)** | `aidocs/38` + `aidocs/63` ADR-0021 |
| Per-user git credentials (host + username + AES-GCM encrypted PAT); `/v2/me/git-credentials` CRUD | none | ✓ backend (G1-cred) / ✓ UI (PR #1071) | 🚧 (G1-cred #1069) | — |
| **`VideoStreamReference` upload + ffprobe metadata extraction** (VID1a) — `POST /v2/data-objects/{appId}/video-stream-references` multipart upload; `GET/DELETE /{appId}`; `GET /{appId}/download`; `GET /` list. ffprobe extracts `durationSeconds`, `width`, `height`, `frameRate`, `videoCodec`, `audioCodec`, `wallClockTimestamp` (UTC ns from `creation_time` tag). `wallClockTimestamp` is the TM1 temporal anchor. `VideoProbeService` degrades gracefully when ffprobe absent. `V38` appId constraint. In-tree for VID1a; plugin extraction is post-VID1-series. **UI3a shipped:** `VideoStreamReferencesPane.vue` + `VideoPlayer.vue` + `useFetchVideoStreamReferences.ts`; native `<video>` player (progressive MP4/WebM); ffprobe metadata chips (duration, resolution, video codec, audio codec, fps, file size, bitrate); per-reference download button. **Auth fix (post-UI3a):** `VideoPlayer.vue` now fetches the video as a blob with `Authorization: Bearer` on mount and passes a `blob:` URL to `<video src>` — the download endpoint requires auth and `<video src>` cannot send custom headers. Blob URL revoked on unmount. HLS deferred to VID1b. `docs/reference/video-stream-references.md` added. | none | **VID1a shipped** — 3 test classes, 27 unit tests. `_shepard_videos` shared storage container. ffprobe metadata opt-in at OS level. **UI3a shipped** — Video References expansion panel on DataObject detail page. | **✓ ↑** (VID1a + UI3a) | `aidocs/16 VID1` + `aidocs/34 VID1a` + `aidocs/ops/86 UI3a` |
| **`VideoAnnotation` interval annotations** (VID1b-annotation) — `POST/GET/PATCH/DELETE /v2/references/{refAppId}/annotations[/{annotationAppId}]` (migrated from per-kind path via APISIMP-VIDEO-ANNOT-PATH, fire-57); `VideoAnnotation` Neo4j entity (`startSeconds`, `endSeconds`, `label`, `description`, `aiGenerated`, `confidence`); linked via `has_video_annotation` relationship; `V52` appId constraint; 15 unit tests. Use case: label experiment phases on a recording (ignition / burn / cooldown). **UI shipped:** `VideoStreamReferencesPane.vue` renders a 16px colour-coded timeline bar below each player when annotations are present; hover tooltip shows `label — description`; 8-colour palette cycles per label; point annotations render as 0.5%-wide marker. | none | **VID1b-annotation shipped** — backend (entity + DAO + IO + REST + V52) + frontend timeline bar + composable `useFetchVideoAnnotations`. **APISIMP-VIDEO-ANNOT-PATH shipped (fire-57, PR #1953)** — annotation CRUD relocated from per-kind per-DO URL to generic `/v2/references/{refAppId}/annotations`. | **✓ ↑** (VID1b-annotation + APISIMP-VIDEO-ANNOT-PATH) | `aidocs/16 VID1` + `aidocs/34 VID1b` + `aidocs/34 APISIMP-VIDEO-ANNOT-PATH` + `docs/reference/video-stream-references.md §Interval annotations` |
| **VID1b — `shepard-plugin-video` extraction** (plugin-first rule §1, CLAUDE.md) — all video classes extracted from backend into `plugins/video/shepard-plugin-video.jar`. `VideoPayload` marker interface retained in backend core for `DataObjectIO.videoStreamReferenceCount`. `VideoPayloadKind` SPI registers entity packages with `NeoConnector` (removing the hardcoded line). All 72 tests moved to the plugin module; wire shapes and REST paths unchanged. | none | **VID1b shipped** — packaging refactor, no user-visible change. | **✓** (VID1b plugin extraction) | `aidocs/34 VID1b` |
| **MFFD-VIDEOREF-SCALE-1 + MFFD-IMAGEBUNDLE-PAGINATE-1 + MFFD-IMAGEBUNDLE-SCRUBBER-1 — scale fixes for MFFD ingest** — closes GAP-9 in `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md`. **Backend Range support** on `GET /v2/data-objects/{appId}/video-stream-references/{appId}/download` (206 Partial Content + `Content-Range` + `Accept-Ranges`; 416 unsatisfiable; full-body fallback on legacy GridFS rows). Shared `HttpRangeUtil` extracted from the FR1b path so future content endpoints inherit the same shape. **`JWTFilter` query-param fallback**: `?access_token=<jwt>` (RFC 6750 §2.3) for HTML5 `<video src>`, `<img src>`, `<a download>`. Header takes precedence. **Paginated bundle files endpoint**: new `GET /v2/bundles/{bundleAppId}/groups/{groupAppId}/files?page=&size=` returning a `PagedFilesIO` envelope (default 200, server cap 1000). **Frontend rewrite**: `VideoPlayer.vue` drops the whole-blob download path (fatal on 6+ GB MP4s) and hands the URL with `?access_token=…` directly to `<video src>` for native browser scrubbing. **New `ImageBundleViewer.vue`**: large preview + range slider + virtualised thumbnail strip (Vuetify `v-virtual-scroll`), lazy-loaded thumbnails via TH1a, page-planning helpers in `utils/imageBundleScrubber.ts`. 45 new unit tests. | progressive-only `<video>`; ImageBundle not paginated | **shipped 2026-06-02**. HLS deferred (`MFFD-VIDEOREF-HLS-1`); pane-mount + compare-mode follow-ups filed. | **✓ ↑** | `aidocs/16 MFFD-VIDEOREF-SCALE-1` + `aidocs/34` + `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md` GAP-9 |
| **RDK-PARSE-1 — `shepard-plugin-fileformat-robotics` tier-1** (RoboDK `.rdk` text-scrape parser, plugin-first rule §1) — standalone Maven module (NOT yet wired into the aggregator pending the backend Quarkus/Jandex hang fix, same constraint as `fileformat-thermography`). Decompresses the `.rdk` zlib payload, walks length-prefixed UTF-16LE string records, classifies each via predicate-specific regex, and emits eight `urn:shepard:rdk:*` annotations on the parent FileReference: `appVersion` / `platform` / `programSource` / `cadRef[]` / `stepRef[]` / `apiEndpoint` / `robotController` + cross-file `companionSpatialAnalyzer → <base>.xit FileReference` (closing the metrology → digital-twin registration loop per aidocs/110 §4.3). Local `FileParserPlugin` SPI shim; canonical SPI rebind in `RDK-WIRE-AGGREGATOR-1`. Tier-1 creates no DataObjects, no containers; full kinematic tree deferred to RDK-PARSE-2 (RoboDK Python API sidecar). 17 JUnit tests green; live-fixture acceptance check reads the real 12.1 MB `examples/mffd-showcase/raw-data/mffd-data/cell/MFZ.rdk` and reproduces the aidocs/110 §4.3 expected output verbatim (`appVersion=5.5.3`, `platform=WIN64`, `programSource=D:/MFFD/RoboDK/Ply 1-15`, `apiEndpoint=127.0.0.1`, `robotController=R20_MFZDriver`, 3 `cadRef`s, 2 `stepRef`s, 1 `companionSpatialAnalyzer`). Three-audience docs trio: `plugins/fileformat-robotics/docs/{reference,quickstart,install}.md` + README. Frontend tier-1 stub deferred — annotations surface through the existing semantic-annotations panel (no new view component required; tier-2 will add the URDF/scenegraph viewer). | none | **RDK-PARSE-1 tier-1 shipped 2026-05-28** — `plugins/fileformat-robotics/` standalone module. Tier-2 (kinematic tree, `:DigitalTwinScene`) tracked as RDK-PARSE-2; aggregator wire-up as RDK-WIRE-AGGREGATOR-1. | **⚙ tier-1 ✓ shipped** / 📐 tier-2 (RDK-PARSE-2) / 📐 aggregator wire-up | `aidocs/integrations/110` §4.3 + `aidocs/16` RDK-PARSE-{1,2} / RDK-WIRE-AGGREGATOR-1 |
| **URDF-WEBVIEW-1 phase 1 — in-tree URDF viewer + animator** (URDF is Shepard's lingua franca for robot kinematics, consumed by ROS RViz, Isaac, MuJoCo, Foxglove, Gazebo). `urdf-loader@^0.12.0` (gkjohnson, MIT, Three.js based) added to `frontend/package.json`. Five Vue components: `UrdfCanvas` (scene + OrbitControls + ResizeObserver), `UrdfView` (VIEW_RECIPE-dispatched renderer when `ShapesRenderResponseIO.renderer === "urdf"`), `UrdfAnimator` (Vuetify play/pause/scrub + speed presets 0.1× / 1× / 10× + reverse), `UrdfJointPanel` (manual per-joint sliders for static-view mode), `UrdfChannelPicker` (channel→joint binding with annotation-driven preselection via `urn:shepard:urdf:joint`). `shapes/render.vue` URDF branch + `ViewRecipeBuilderDialog` renderer toggle (Trace3D / URDF / Thermography). Sample `two-link-arm.urdf` at `frontend/public/urdf-samples/`. End-to-end showcase: `examples/mffd-rdk-urdf-showcase/` seeds a Collection with the real MFZ.rdk → RDK-PARSE-1 tier-1 scrape on upload + **real** KUKA KR210 R2700/2 URDF (kroshu/kuka_robot_descriptions Apache-2.0 — the actual MFFD AFP cell robot; URDF swap landed 2026-05-30 replacing the 2026-05-29 L150 substitute per `urdf/SOURCES.md`) + synthetic 30s × 100 Hz × 6-joint AFP layup trajectory with `urn:shepard:urdf:joint` channel annotations. Phase 2 deferred: `UrdfRecordButton`, Foxglove iframe, `RdkToUrdfExporter` sidecar (RoboDK SDK licence), Trace3D+URDF composition. | none | **URDF-WEBVIEW-1 phase 1 shipped 2026-05-28** — 5 Vue components + 47 Vitest tests. **MFFD RDK→URDF showcase shipped 2026-05-29; URDF upgraded to real KR210 R2700/2 2026-05-30** — `examples/mffd-rdk-urdf-showcase/{seed.py,SHOWCASE.md,README.md,urdf/,trajectory/}`. Phase 2 queued. **SCENEGRAPH-NAV-02 in-flight fire-350** — `OpenIn3dViewButton` wired into the FileReference detail page (`filereferences/[fileReferenceId]/index.vue`); in-context "Open in 3D view / Create 3D view" affordance for URDF/RDK files (annotation-based detection + MAPPING_RECIPE mint). | **⚙ phase 1 ✓ shipped** / 📐 phase 2 (RdkToUrdfExporter, Foxglove, record-button, Trace3D-compose) / 🔄 SCENEGRAPH-NAV-02 in-flight fire-350 | `aidocs/integrations/113` + `aidocs/16` URDF-WEBVIEW-1 + SCENEGRAPH-NAV-02 |
| **KRL-INTERPRETER-01 — `shepard-plugin-krl-interpreter` design pass** (KUKA Robot Language offline interpreter sidecar; consumes a `.src` (+ optional `.dat`) FileReference + a URDF FileReference and emits a 6-channel joint trajectory TimeseriesReference with `urn:shepard:urdf:joint:joint_<n>` channel annotations for URDF-WEBVIEW-1 auto-binding). Design doc lands at `aidocs/integrations/117-krl-interpreter.md` with the full 14-section structure: §1 operator end-goal + 90-second click-walkthrough, §2 reuse survey (parser: `pykrlparser` MIT ANTLR4 + tier-1 grammar extensions for FOR/WHILE/LOOP/IF/`$BASE`/`$TOOL`; IK: `ikpy` Apache-2.0 pure-Python), §3 wire diagram, §4 KRL subset (`PTP`/`LIN`/`CIRC`/`WAIT`/`IF`/`FOR`/`WHILE`/`LOOP` + frame literals + base/tool switching, SPS/`INTERRUPT`/sensor-IO HARD-STOP), §5 IK benchmark methodology, §6 sidecar protocol (`POST /interpret` + 202 long-running), §7 backend wiring (`POST /v2/krl/interpret` + `:KrlInterpretActivity` + `X-AI-Agent` propagation), §8 frontend "Run / preview" button on the `.src` FileReference page + modal picker, §9 MCP tools (`krl_interpret`, `krl_list_unsupported`), §10 plugin manifest + `:KrlInterpreterConfig` admin singleton + CLI parity, §11 cross-refs, §12 open questions, §13 persona-board review (IME/AQE + Reluctant Senior + Digital Native + opposing KUKA-engineer lens — spawned **KRL-INTERPRETER-AUDIT-LABEL** sub-row to label every trajectory as `interpreter-resolved offline replay`). Sub-rows -02 parser, -03 IK, -04 sidecar, -05 REST, -06 UI, -07 MFFD showcase, -08 docs all unblock on this design landing. | none | **KRL-INTERPRETER-01 design landed 2026-05-29** — `aidocs/integrations/117-krl-interpreter.md` (`stage: feature-defined`). **KRL-INTERPRETER-02 parser, -03 IK, -04 sidecar landed on `main` 2026-05-30** (PR #1615 merged). **KRL-INTERPRETER-05 REST in-flight 2026-05-30** (branch `krl-interpreter-05-rest`): `POST /v2/krl/interpret` + `KrlInterpretService` + `KrlSidecarClient` + `KrlInterpreterConfig` + typed `:KrlInterpretActivity` overlay (USED → src/urdf/dat/scene, GENERATED → trajectory `TimeseriesReference`, WAS_ASSOCIATED_WITH → `:User`; `X-AI-Agent` → `sourceMode=ai`). Sidecar bytes passed inline base64 alongside appIds. 22 JUnit tests (8 REST + 14 service); JaCoCo 74.9% line over `de.dlr.shepard.v2.krl`. Three deploy-time `shepard.krl.sidecar.*` keys; runtime-mutable `:KrlInterpreterConfig` deferred to KRL-CONFIG-1; tier-2 auto-mint default container deferred to KRL-INTERPRETER-05-FOLLOWUP-AUTO-CONTAINER. Frontend stub omitted in favour of the natural KRL-INTERPRETER-06 UI consumer. Sub-rows -06 / -07 / -08 queued. **DISSOLVED into MAPPING_RECIPE (V2CONV-B5, shipped 2026-06-04):** the `/v2/krl/interpret` REST surface (-05) + bespoke UI (-06) are removed; KRL interpret is now a `KrlTrajectoryShape` MAPPING_RECIPE materialized via `POST /v2/mappings/{templateAppId}/materialize`. The sidecar (-02/-03/-04) is unchanged and reused by the in-tree `KrlTrajectoryTransformExecutor`. | ✓ dissolved into V2CONV-B5 (sidecar -02/-03/-04 retained; -05 REST + -06 UI replaced by the generic MAPPING_RECIPE path) | `aidocs/integrations/117` + `aidocs/16` KRL-INTERPRETER-{01..08} + V2CONV-B5 + `plugins/krl-interpreter/` |
| **MFFD-STRINGER-FILENAME-PARSER-1 — `StringerWeldingDirParser` in `shepard-plugin-fileformat-thermography`** (MFFD stringer-welding directory name parser, plugin-first rule §1) — pure utility class inside the existing `plugins/fileformat-thermography/` standalone module, mirroring `OTvisFilenameParser` exactly. Parses the `Stringer_schweissungen` run-directory grammar `P<pos>[Strich][_S]_{1|2}teBahn[_Fehler]` (e.g. `P02Strich_S_2teBahn_Fehler`) into a `StringerPosition` record (positionId / hasPrime / hasSide / weldPass / isDefect). Annotates five `urn:shepard:mffd:*` + `urn:shepard:status:*` keys on the parent DataObject: `stringer-position`, `stringer-variant`, `stringer-side`, `weld-pass`, `defect-run`. Strips both Unix + Windows path separators; regex anchored to the full basename. 17 JUnit tests (AssertJ) covering all grammar variants + path stripping + negative cases (missing pass, invalid pass number, missing P prefix, wrong separator, trailing junk). Plugin still standalone (Jandex aggregator wire-up deferred to `OTVIS-WIRE-AGGREGATOR-1`). | none | **MFFD-STRINGER-FILENAME-PARSER-1 ✓ shipped 2026-06-09** (PR #1797 merged `1457ab180`) — `plugins/fileformat-thermography/src/main/java/…/StringerWeldingDirParser.java` + `StringerWeldingDirParserTest.java`. Activates when `MFFD-STRINGER-COLLECTION-1` wires it into the `FileParserPlugin` annotation chain. | ✓ **shipped** | `plugins/fileformat-thermography/` + `aidocs/16` MFFD-STRINGER-FILENAME-PARSER-1 |
| **MFFD-TPL-MATERIAL-BATCH — `MffdMaterialBatchKind` seeds `mffd-material-batch-data-shape` DATAOBJECT_RECIPE template** (MFFD demonstrator keystone — batch provenance chain, B7 pattern) — `MffdMaterialBatchKind` in `de.dlr.shepard.v2.shapes.mffd` registered via ServiceLoader as a `PayloadKind`. At startup, `KindShapeSeeder` calls `shapeDescriptor()` and seeds an idempotent system-tagged `DATAOBJECT_RECIPE` template named `mffd-material-batch-data-shape` with seven property shapes: `urn:shepard:mffd:batch-id` (xsd:string), `urn:shepard:mffd:material-class` (xsd:string, sh:in {LMPAEK, SAERTEX-NCF, EPOXY-PREPREG, GLASS-FIBER-NCF, CARBON-FIBER-TAPE, OTHER}), `urn:shepard:mffd:supplier` (xsd:string), `urn:shepard:mffd:manufactured-date` (xsd:date), `urn:shepard:mffd:expiry-date` (xsd:date), `urn:shepard:mffd:storage-condition` (xsd:string), `urn:shepard:mffd:certificate-ref` (IRI, no datatype). All properties optional (sh:minCount 0, sh:maxCount 1). Open shape. 14 unit tests in `MffdMaterialBatchKindTest`. Downstream consuming templates (MFFD-TPL-AFP-COURSE, MFFD-TPL-WELD-STEP, MFFD-TPL-NDT-OTVIS-MEASUREMENT) link back to a material-batch DataObject via `urn:shepard:mffd:material-batch` IRI predicate (seeded in those templates). | none | **MFFD-TPL-MATERIAL-BATCH ✓ shipped 2026-06-09** — `backend/src/main/java/de/dlr/shepard/v2/shapes/mffd/MffdMaterialBatchKind.java`; registered in `backend/src/main/resources/META-INF/services/de.dlr.shepard.spi.payload.PayloadKind`; 14 unit tests in `MffdMaterialBatchKindTest`. | ✓ **shipped** | `de.dlr.shepard.v2.shapes.mffd` + `aidocs/16` MFFD-TPL-MATERIAL-BATCH |
| **MFFD-TPL-AFP-COURSE — `MffdAfpCourseKind` seeds `mffd-afp-course-data-shape` DATAOBJECT_RECIPE template** (MFFD demonstrator AFP process step — B7 pattern) — `MffdAfpCourseKind` in `de.dlr.shepard.v2.shapes.mffd` registered via ServiceLoader as a `PayloadKind`. At startup, `KindShapeSeeder` seeds an idempotent system-tagged `DATAOBJECT_RECIPE` template named `mffd-afp-course-data-shape` with six property shapes: `urn:shepard:mffd:tcp-path-ref` (IRI — appId of the TimeseriesReference carrying tcp_{x,y,z}_mm / tcp_{rx,ry,rz}_deg channels), `urn:shepard:mffd:tape-speed-setpoint` (xsd:decimal, m/min), `urn:shepard:mffd:laser-temp-setpoint` (xsd:decimal, °C), `urn:shepard:mffd:ply-id` (xsd:string), `urn:shepard:mffd:course-id` (xsd:string), `urn:shepard:mffd:material-batch` (IRI → mffd:material-batch DataObject). All properties optional. Open shape. 13 unit tests. | none | **MFFD-TPL-AFP-COURSE ✓ shipped 2026-06-09** — `MffdAfpCourseKind.java` + `MffdAfpCourseKindTest.java` in `de.dlr.shepard.v2.shapes.mffd`; registered in ServiceLoader. Seeds `mffd-afp-course-data-shape` DATAOBJECT_RECIPE at startup. | ✓ **shipped** | `de.dlr.shepard.v2.shapes.mffd` + `aidocs/16` MFFD-TPL-AFP-COURSE |
| **MFFD-TPL-NDT-OTVIS-MEASUREMENT — `MffdNdtOtvisMeasurementKind` seeds `mffd-ndt-otvis-measurement-data-shape` DATAOBJECT_RECIPE template** (MFFD demonstrator NDT measurement shape — B7 pattern, grid fields mirror `OTvisFilenameParser`) — `MffdNdtOtvisMeasurementKind` in `de.dlr.shepard.v2.shapes.mffd`. Nine property shapes: `urn:shepard:mffd:ndt-section` / `ndt-module` / `ndt-layer` / `ndt-frame` (xsd:string, carry literal prefix letters e.g. "S4"/"M13"/"L18"/"F4" for filename round-trip fidelity), `urn:shepard:mffd:ndt-frame-rate` (xsd:decimal, Hz), `urn:shepard:mffd:ndt-camera-setup` (xsd:string), `urn:shepard:mffd:ndt-ambient-temp` (xsd:decimal, °C), `urn:shepard:mffd:ndt-ambient-humidity` (xsd:decimal, %), `urn:shepard:mffd:ndt-source-fileref` (IRI → OTvis FileReference appId). All optional. Open shape. 12 unit tests. Unblocks MFFD-RENDER-NDT-GRID (the S×M×L×F tile mosaic renderer). | none | **MFFD-TPL-NDT-OTVIS-MEASUREMENT ✓ shipped 2026-06-09** — `MffdNdtOtvisMeasurementKind.java` + `MffdNdtOtvisMeasurementKindTest.java` in `de.dlr.shepard.v2.shapes.mffd`; registered in ServiceLoader. Seeds `mffd-ndt-otvis-measurement-data-shape` DATAOBJECT_RECIPE at startup. | ✓ **shipped** | `de.dlr.shepard.v2.shapes.mffd` + `aidocs/16` MFFD-TPL-NDT-OTVIS-MEASUREMENT |
| **MFFD-TPL-APPROVAL-GATE — `MffdApprovalGateKind` seeds `mffd-approval-gate-data-shape` DATAOBJECT_RECIPE template** (MFFD demonstrator EN 9100 quality-gate disposition record — B7 pattern, AS9100 §8.7 non-conforming output) — `MffdApprovalGateKind` in `de.dlr.shepard.v2.shapes.mffd`. Six property shapes: `urn:shepard:mffd:gate-type` (xsd:string — which production step this gate guards, e.g. "afp-course-exit"), `urn:shepard:mffd:disposition` (xsd:string, sh:in {accept-as-is, rework, repair, scrap}), `urn:shepard:mffd:inspector-id` (xsd:string), `urn:shepard:mffd:approval-date` (xsd:date), `urn:shepard:mffd:related-ncr` (IRI → NCR DataObject appId), `urn:shepard:mffd:evidence-fileref` (IRI → signed evidence FileReference appId). All optional. Open shape. 13 unit tests. Unblocks MFFD-STRINGER-FEHLER-NCR-1. | none | **MFFD-TPL-APPROVAL-GATE ✓ shipped 2026-06-09** — `MffdApprovalGateKind.java` + `MffdApprovalGateKindTest.java` in `de.dlr.shepard.v2.shapes.mffd`; registered in ServiceLoader. Seeds `mffd-approval-gate-data-shape` DATAOBJECT_RECIPE at startup. | ✓ **shipped** | `de.dlr.shepard.v2.shapes.mffd` + `aidocs/16` MFFD-TPL-APPROVAL-GATE |
| **MFFD-RENDER-NDT-GRID slices 1+2+3 — `shepard-plugin-vis-ndt-grid` module + `NdtGridTransformExecutor` + `NdtGridCanvas.vue` frontend** (MFFD demonstrator NDT thermography tile-grid mosaic renderer — vis-ndt-grid plugin manifest + NdtGridShape SHACL + executor + real CSS-grid mosaic frontend) — `VisNdtGridPluginManifest` in `plugins/vis-ndt-grid`; plugin ID `vis-ndt-grid`; PluginManifest SPI registered via ServiceLoader. `NdtGridShape` SHACL at classpath `/shapes/ndt-grid.shacl.ttl` (Shape IRI `http://semantics.dlr.de/shepard/transform#NdtGridShape`); 9 property shapes: `ndtgrid:collectionAppId` (req), `rowDimension`/`columnDimension`/`colourMode`/`colourMap`/`qualityAnnotationPredicate`/`layerFilter`/`sectionFilter`/`moduleFilter` (optional). Slice 1: `NdtGridCanvas.vue` placeholder stub + 23 manifest tests. Slice 2: `NdtGridTransformExecutor` ServiceLoader `TransformExecutor` SPI — loads top-level DataObjects, reads SMFL coordinate + cell-value annotations, builds sorted row/column axes + cells envelope; 21 unit tests. Slice 3: `NdtGridCanvas.vue` real CSS-grid mosaic replaces placeholder (colourMode mean-delta-t/pass-fail, colourMap via `resolveColourMap` helper); `frontend/utils/ndtGridCanvas.ts` pure helpers (28 Vitest unit tests); `frontend/pages/ndt-grid/[templateAppId].vue` materializes and renders the mosaic + selected-DataObject panel. | none | **Slice 1 ✓ shipped 2026-06-09** — plugin module + placeholder. **Slice 2 ✓ shipped 2026-06-11** — executor + 21 unit tests. **Slice 3 🚧 in-flight 2026-06-14** — `NdtGridCanvas.vue` real renderer + ndtGridCanvas.ts helpers + ndt-grid page + 28 Vitest tests. PR: `MFFD-RENDER-NDT-GRID-3-ndt-grid-view`. | 🚧 **in-flight** (slices 1+2 shipped; slice 3 PR open) | `plugins/vis-ndt-grid/` + `frontend/components/shapes/NdtGridCanvas.vue` + `frontend/pages/ndt-grid/[templateAppId].vue` + `aidocs/16` MFFD-RENDER-NDT-GRID |
| **MFFD-TPL-WELD-STEP — `MffdWeldStepKind` seeds `mffd-weld-step-data-shape` DATAOBJECT_RECIPE template** (MFFD demonstrator welding process step — B7 pattern, MFFD-STRINGER-WELDSUBTYPE-1 folded in) — `MffdWeldStepKind` in `de.dlr.shepard.v2.shapes.mffd` registered via ServiceLoader as a `PayloadKind`. A single shape covers all three MFFD welding technologies (ultrasonic/resistance/stud) via controlled `weldSubtype`; `weldMode` discriminator ({spot, continuous}) distinguishes spot welds from scan-path continuous seams; `weld-pass` (xsd:integer, sh:in {1, 2}) encodes 1teBahn / 2teBahn stringer passes. Eight property shapes: `urn:shepard:mffd:weld-subtype` (xsd:string, sh:in {ultrasonic, resistance, stud}), `urn:shepard:mffd:weld-mode` (xsd:string, sh:in {spot, continuous}), `urn:shepard:mffd:weld-energy` (xsd:decimal, kJ), `urn:shepard:mffd:weld-force` (xsd:decimal, N), `urn:shepard:mffd:weld-dwell` (xsd:decimal, ms), `urn:shepard:mffd:joint-id` (xsd:string), `urn:shepard:mffd:weld-pass` (xsd:integer, sh:in {1, 2}), `urn:shepard:mffd:material-batch` (IRI → mffd:material-batch DataObject). All properties optional. Open shape. 14 unit tests. | none | **MFFD-TPL-WELD-STEP ✓ shipped 2026-06-09** — `MffdWeldStepKind.java` + `MffdWeldStepKindTest.java` in `de.dlr.shepard.v2.shapes.mffd`; registered in ServiceLoader. Seeds `mffd-weld-step-data-shape` DATAOBJECT_RECIPE at startup. | ✓ **shipped** | `de.dlr.shepard.v2.shapes.mffd` + `aidocs/16` MFFD-TPL-WELD-STEP |
| **MFFD-RENDER-MATERIAL-BATCH-TRACE (slices 1+2+3) — `MffdMaterialBatchKind.viewShapeDescriptor()` seeds VIEW_RECIPE; `MaterialBatchTraceViewRenderer` walks inbound material-batch edges; `MaterialBatchTracePane.vue` renders in-context consumer list** (MFFD demonstrator batch-lineage render — all 3 slices shipped) — Slice 1: `MffdMaterialBatchKind` returns `ViewRecipeSpec` (shape IRI `urn:shepard:shape:mffd-material-batch-trace#MaterialBatchTraceShape`, renderer hint `lineage`, one required `materialBatch` binding). `KindShapeSeeder` seeds the system-tagged `mffd-material-batch-view-shape` VIEW_RECIPE at startup. Slice 2: `MaterialBatchTraceViewRenderer` registered via `META-INF/services/de.dlr.shepard.spi.view.ViewRecipeRenderer`. Queries `SemanticAnnotationDAO.findByPredicateAndValue(urn:shepard:mffd:material-batch, batchAppId, "DataObject", 0, 500)` — each non-null `subjectAppId` becomes an `OK` binding; empty → `MISSING`. 6+10 unit tests (21 in `MffdMaterialBatchKindTest`, 10 in `MaterialBatchTraceViewRendererTest`). Additive, no migration. Slice 3: `MaterialBatchTracePane.vue` mounts as an ExpansionPanelItem on the DataObject detail page; gated by `isMaterialBatchDo` (annotation `urn:shepard:mffd:batch-id` present). Calls `POST /v2/shapes/render` with file-rooted dispatch (shapeIri + focusShepardId); resolves consumer names via `GET /v2/collections/{coll}/data-objects/{appId}`; renders NuxtLinks for in-collection consumers, appId+copy-button for cross-collection. 12 Vitest tests. | none | **slice 1 ✓ shipped 2026-06-10; slice 2 ✓ shipped 2026-06-10; slice 3 ✓ shipped 2026-06-10** — `MaterialBatchTraceViewRenderer.java` + `META-INF/services/de.dlr.shepard.spi.view.ViewRecipeRenderer` + 10 tests (slices 1+2). `frontend/components/context/mffd/MaterialBatchTracePane.vue` + `frontend/tests/unit/MaterialBatchTracePane.test.ts` (12 Vitest tests) + DataObject detail page `isMaterialBatchDo` guard + ExpansionPanelItem mount (slice 3). | ✓ **shipped** | `de.dlr.shepard.v2.shapes.mffd` + `frontend/components/context/mffd/` + `aidocs/16` MFFD-RENDER-MATERIAL-BATCH-TRACE |
| **OTVIS-PARSE-1 — `shepard-plugin-fileformat-thermography` tier-1** (Edevis OTvis metadata parser, plugin-first rule §1) — standalone Maven module (NOT yet wired into the aggregator pending the backend Quarkus/Jandex `CompositeIndex.getClassByName` hang fix). Reads `.OTvis` POSIX tar → `content.xml` UTF-16 LE XML → 14 `urn:shepard:thermography:*` annotations on the FileReference (frame rate, integration time, excitation device/freq/amp/signal-type, recording type, resolution, periods, campaign, module, creating version, ISO-8601 created-at) + filename pattern `S<n>_M<n>_L<n>_F<n>.OTvis` → 4 `urn:shepard:mffd:*` grid annotations on the parent DataObject. Local `FileParserPlugin` SPI shim; canonical SPI rebind in `OTVIS-WIRE-AGGREGATOR-1`. Tier-1 creates no DataObjects, no containers, no `.diproj` parsing (DO-sprawl containment per aidocs/114 §0). 27 JUnit tests green against the real 8.4 MB `sample_S4_M13_L18_F4.OTvis` fixture. Frontend tier-1 stub shipped: `ThermographyView.vue` + `ThermographyCanvas.vue` + `ThermographyChannelPicker.vue` (metadata-summary panel) + 20 Vitest tests; wired into `shapes/render.vue` + `ViewRecipeBuilderDialog.vue` (third toggle alongside Trace 3D + URDF). | none | **OTVIS-PARSE-1 + OTVIS-VIEW-1 tier-1 shipped 2026-05-28; OTVIS-VIEWER decoded-frame REST + viewer shipped 2026-06-03** — `plugins/fileformat-thermography/` standalone module; `frontend/components/{shapes,container/timeseries}/Thermography*.vue` + `frontend/utils/thermographyChannelPicker.ts`. OTVIS-VIEWER adds `GET /v2/thermography/otvis/{fileReferenceAppId}/frames` (index) + `/frames/{n}?channel=amplitude\|phase\|temperature` (server-rendered heatmap PNG) decoding the real amplitude/phase frames via the pure-Java `OTvisFrameExtractor` (backend depends on the plugin as a plain-Java jar — svdx-style Jandex workaround); `DataObjectOtvisViewer.vue` mounts per-`.OTvis`-singleton on the DataObject detail page (amplitude/phase toggle + scrubber; phase default = NDT defect channel). Backend `OtvisFrameRenderService` +7 JUnit; frontend `utils/otvisViewer.ts` +7 Vitest. Full OME-Zarr chunked serving deferred → OTVIS-VIEWER-OMEZARR-SERVE. | **⚙ tier-1 + decoded-frame viewer ✓ shipped (alpha)** — viewer auto-mounts via `.OTvis` filename detection (DataObject expansion-panel grid; no top-nav entry). / 📐 OME-Zarr chunked serving (OTVIS-VIEWER-OMEZARR-SERVE) / 📐 tier-2 channels (THERMO-CHANNELS-1) / 📐 aggregator wire-up | `aidocs/integrations/114` + `115` + `aidocs/16` OTVIS-PARSE-{1,2} / OTVIS-VIEW-1 / OTVIS-VIEWER / THERMO-CHANNELS-1 / OTVIS-WIRE-AGGREGATOR-1 |
| **MFFD-PLUGIN-SVDX-1 — `shepard-plugin-fileformat-svdx` tier-1** (Beckhoff TwinCAT Scope `.svdx` manifest extractor, plugin-first rule §1) — standalone Maven module mirroring `fileformat-thermography`/`fileformat-robotics` (NOT yet wired into the aggregator pending the backend Quarkus/Jandex `CompositeIndex.getClassByName` hang fix). Decodes the 16-byte SVDX envelope (uint64 LE pointer to trailing XML manifest BOM + uint64 LE format-version stamp with stable magic markers `0x96 0x0c 0x00` at offsets 9..11), StAX-streams the trailing `<ScopeProject>` XML manifest, and emits 15+ `urn:shepard:svdx:*` annotations on the parent FileReference: `formatVersion`, `projectGuid`/`projectName`/`dataPoolGuid`/`assemblyName`, `mainServer`/`recordTimeNs`/`autoSaveMode`, `channelCount`/`acquisitionCount`, deduped `amsNetId`/`port`/`dataType`, every `channelName` (rendered chart channel) and `symbolName` (`GVL_IO_US_Endeffektor.*` ADS data source), `companionCsv` (sibling `<basename>.csv` or `_parsed.csv` FileReference appId). **The proprietary binary sample blocks are NOT decoded** — Beckhoff publishes no spec; the community `pytcs` library punts on binary. Real-fixture test reads the 7.2 MB smallest MFFD campaign file (when present at `/mnt/pve/unas/dump/dataset/Punktschweißungen/`) and asserts 46 channels + 149 acquisitions extracted. Local `FileParserPlugin` SPI shim; canonical SPI rebind in `MFFD-PLUGIN-SVDX-WIRE-1`. Tier-1 creates no DataObjects, no containers, no sample-level ingest — annotations surface through the existing semantic-annotations panel (no new view component required). | none | **MFFD-PLUGIN-SVDX-1 tier-1 shipped 2026-06-02** — `plugins/fileformat-svdx/` standalone module + four-audience docs (`docs/{reference,quickstart,install,byte-layout-notes}.md`, mirrored in `frontend/public/docs/plugins/fileformat-svdx/`). 22 JUnit tests green. Five concrete follow-up rows filed: binary-parser research (`MFFD-PLUGIN-SVDX-BINARY-PARSER-1`), CSV-export ingest (`MFFD-PLUGIN-SVDX-CSV-INGEST-1`), Beckhoff-naming semantic auto-detection (`MFFD-PLUGIN-SVDX-SEMANTIC-1`), folder-spanning bulk import (`MFFD-PLUGIN-SVDX-BULK-1`), aggregator wire-up (`MFFD-PLUGIN-SVDX-WIRE-1`). Partially closes GAP-1 in `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md`. | **⚙ tier-1 ✓ shipped** / 📐 tier-2 (binary parser, CSV ingest, semantic auto-detect, bulk import) / 📐 aggregator wire-up | `plugins/fileformat-svdx/` + `aidocs/16` MFFD-PLUGIN-SVDX-* |
| **MFFD-NDT-QUALITY-1 + MFFD-NDT-HEATMAP-2-UI — Thermography NDT quality-score backend + plate-heatmap pane** — `POST /v2/thermography/analyze` + `GET /v2/thermography/{appId}/plate-heatmap` for FileBundleReferences of TIFF frames. Stream-decodes each TIFF (TwelveMonkeys `imageio-tiff` 3.10.1), computes per-frame `peakDeltaC = max(pixel) − median(pixel)` + `meanDeltaC` + hot-spot pixel; accumulates a row-major `float[gridH][gridW]` running-max plate-heatmap (default 64×64); writes seven derived `urn:shepard:ndt:*` SemanticAnnotations on the bundle + a DataObject-level `quality-score = 1 − clip(maxPeakDeltaC/threshold, 0, 1)`. `sourceMode="ai"` + `confidence=1.0` per the deterministic-system-generated convention. Idempotent re-runs. Frontend: `DataObjectThermographyPane.vue` mounts conditionally on the DataObject detail page when a thermography-shaped FileBundleReference is attached (name match: `thermo` / `ndt` / `.tif`); Canvas-backed inferno-colormap heatmap + Vuetify quality chip (green ≥0.8 / amber 0.5–0.8 / red <0.5) + hover tooltip + Re-analyze CTA (Write-gated). Pure helpers in `~/utils/thermographyHeatmap.ts` (11 Vitest cases). Backend 26 JUnit tests (pure math + TIFF decoder + codec round-trip). Honest scope: lives in-tree at `backend/src/main/java/de/dlr/shepard/v2/thermography/` because the existing thermography plugin is currently standalone (Jandex blocker); relocation tracked as `MFFD-THERMO-MOVE-TO-PLUGIN-1`. AI vision tier-2 defect classification deferred to `MFFD-NDT-AI-CLASSIFY-1`. Closes GAP-7 in `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md`. | none | **MFFD-NDT-QUALITY-1 + MFFD-NDT-HEATMAP-2-UI shipped 2026-06-02** — `backend/src/main/java/de/dlr/shepard/v2/thermography/` + `frontend/components/context/thermography/DataObjectThermographyPane.vue` + `frontend/utils/thermographyHeatmap.ts` + 26 JUnit + 11 Vitest tests. Plugin docs trio (`plugins/fileformat-thermography/docs/{reference,quickstart,install}.md`) updated with the new surface. | **⚙ shipped (alpha)** — auto-mounts via dataReferences detection (no top-nav entry; pane appears in the DataObject expansion-panel grid). Promotion to beta gated on (a) cross-region heatmap comparison (`MFFD-NDT-CROSS-REGION-1`), (b) admin-configurable threshold (`MFFD-NDT-ADMIN-CONFIG-1`), (c) component-mount integration test (`MFFD-NDT-HEATMAP-MOUNT-TEST-1`). | `aidocs/34` + `aidocs/16` MFFD-NDT-* + `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md` GAP-7 |
| **MFFD-NDT-ADMIN-CONFIG-1 — Admin-configurable `:ThermographyConfig` singleton** — Promotes three deploy-time-only `application.properties` knobs (`shepard.v2.thermography.threshold-c`, `.grid-width`, `.grid-height`) to runtime-mutable via a Neo4j `:ThermographyConfig` singleton (HasAppId, single-instance). Pattern follows A3b/N1c2/UH1a/J1e. Entity: `ThermographyConfig` (nullable `thresholdC`, `gridWidth`, `gridHeight`). DAO: live-session pattern (CHOKE-03). IO: `ThermographyConfigIO` record + `ThermographyConfigPatchIO` (RFC 7396 tri-state via `@JsonSetter(nulls = Nulls.SET)` + `*Touched` flags). Service: `@Observes StartupEvent` seed, fail-soft, synchronized. REST: `GET/PATCH /v2/admin/thermography/config` (`@RolesAllowed("instance-admin")`). Wire: `ThermographyAnalysisService` injects service and prefers singleton over `@ConfigProperty` defaults (fail-soft `resolveConfig()` wrapper). Migration: `V111__Add_appId_constraint_ThermographyConfig.cypher` + rollback twin. CLI: `shepard-admin thermography {status, set-threshold --value/--reset, set-grid --width/--height/--reset}`. 12 JUnit unit tests (annotation gates + GET round-trip + PATCH tri-state cases + IO factory). Frontend: admin-configurable surface; UI stub deferred (operator CLI covers the immediate need; `MFFD-NDT-ADMIN-CONFIG-1-UI` tracked in `aidocs/16`). | none | **MFFD-NDT-ADMIN-CONFIG-1 shipped 2026-06-03** — `backend/src/main/java/de/dlr/shepard/v2/admin/thermography/` (entity + DAO + IO + service + REST) + `backend/src/main/resources/neo4j/migrations/V111__*.cypher` + rollback + `cli/src/main/java/de/dlr/shepard/cli/commands/Thermography*.java` + `cli/src/main/java/de/dlr/shepard/cli/io/ThermographyConfig.java`; 12 unit tests. `aidocs/34` MFFD-NDT-ADMIN-CONFIG-1 row (AWARE). | **⚙ shipped (alpha)** — operator CLI covers the immediate need; frontend admin tile deferred (`MFFD-NDT-ADMIN-CONFIG-1-UI`). Closes beta gate (b) on `MFFD-NDT-QUALITY-1`; remaining gates are `MFFD-NDT-CROSS-REGION-1` and `MFFD-NDT-HEATMAP-MOUNT-TEST-1`. | `aidocs/34` MFFD-NDT-ADMIN-CONFIG-1 + `aidocs/16` MFFD-NDT-ADMIN-CONFIG-1-UI |
| **MFFD-MULTIPLAYER-1 — Synchronised multi-payload player (GAP-11)** — Single shared time cursor across every payload kind a DataObject carries. `useSyncedTimeCursor` composable (Vue 3 `provide` / `inject` with `Symbol`-typed `InjectionKey<SyncedTimeCursor>`) graduates the AAA3 cross-track `crossTime` ref into a player API (`currentTime` / `isPlaying` / `rate` / `range` / `seek` / `play` / `pause` / `togglePlay` / `setRate` / `registerRange`). Each subscribed tile registers a `TileRange` (start, end in DO-relative ms); the playable range is the intersection. `MultiPlayerPane.vue` mounts in the DataObject detail page's expansion-panel grid between Spatial-data and Provenance when ≥ 2 distinct payload kinds are present (`selectMultiPlayerTiles` helper enforces the gate); renders a toolbar (play/pause + rewind + rate ∈ {0.5×, 1×, 2×, 4×} + scrubber + time readout) and a tile grid in canonical order TS → video → thermo → spatial. **Two tiles genuinely sync today**: `MultiPlayerTsTile` (mirrors AAA3 — tooltip writes cursor / echarts `markLine` reads it) and `MultiPlayerVideoTile` (extends `VideoPlayer.vue` with optional two-way `currentTime` prop + `update:currentTime` + `durationLoaded` emits, inert when callers don't bind). Thermography (composite max-projection, no time axis) and spatial (no animated brush head) tiles render informational placeholders with deferred-sync backlog references (`MFFD-MULTIPLAYER-THERMO-1`, `MFFD-MULTIPLAYER-SPATIAL-1`) — kept on the player as visual context, honest about what's not yet wired. 29 Vitest tests (22 on composable: intersect/clamp/play/pause/rate/range-replace/auto-pause/rewind/no-provider error/reactive propagation + 7 on `selectMultiPlayerTiles` gate). Closes GAP-11 in `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md`. | none | **MFFD-MULTIPLAYER-1 shipped 2026-06-02** — `frontend/composables/context/useSyncedTimeCursor.ts` + `frontend/components/context/multiplayer/{MultiPlayerPane,MultiPlayerTsTile,MultiPlayerVideoTile,MultiPlayerThermographyTile,MultiPlayerSpatialTile}.vue` + `frontend/utils/multiPlayerTiles.ts` + 29 Vitest tests; `VideoPlayer.vue` gains optional `currentTime` two-way binding (non-breaking). | **⚙ shipped (alpha)** — auto-mounts via payload-kind detection (no top-nav entry; pane appears in the DataObject expansion-panel grid). Promotion to beta gated on (a) frame-strip thermography sync (`MFFD-MULTIPLAYER-THERMO-1`), (b) spatial brush-head cursor (`MFFD-MULTIPLAYER-SPATIAL-1`), (c) Playwright 4K cross-tile playback test (`MFFD-MULTIPLAYER-PLAYWRIGHT-1`). | `aidocs/16` MFFD-MULTIPLAYER-* + `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md` GAP-11 |
| **MFFD-RENDER-AFP-THERMO-OVERLAY — `shepard-plugin-vis-afp-thermo-overlay` dual-pane AFP+NDT comparison view** (MFFD demonstrator, MFFD-RENDER-AFP-THERMO-OVERLAY tracker row) — Three slices: slice 1 (shape+manifest) ships `AfpThermoOverlayShape` SHACL + `VisAfpThermoOverlayPluginManifest` + frontend placeholder + plugin docs trio; slice 2 (`AfpThermoOverlayTransformExecutor` + 28 tests); slice 3 (`AfpThermoOverlayCanvas.vue` real dual-pane card + `afpThermoOverlay.ts` 7 helpers + 23 Vitest cases). Compares an AFP layup course trace with OTvis NDT inspection for the same tile — left: AFP robot-head course metadata (ply/course IDs, laser temp + tape speed setpoints, material batch IRI, time window, TCP timeseries ref); right: NDT OTvis FileRef + tile coords; header: tile-match verdict chip (MATCHED / MISMATCHED / UNVERIFIED). Phase 2 (deferred, MFFD-RENDER-AFP-THERMO-OVERLAY-4): Trace3DCanvas + ThermographyCanvas live embeds gated on TCP timeseries live-fetch composable + OTVIS-PARSE-2 / OTVIS-VIEW-1 tier-2. | none | **Slice 1 ✓ shipped 2026-06-10** — `plugins/vis-afp-thermo-overlay/` manifest + SHACL + 16 tests + `AfpThermoOverlayPlaceholder.vue` + docs trio. **Slice 2 ✓ shipped 2026-06-14** (PR #1914 merged) — `AfpThermoOverlayTransformExecutor` + 28 unit tests. **Slice 3 🚧 in-flight 2026-06-14** (PR #1920, branch `MFFD-RENDER-AFP-THERMO-OVERLAY-3-canvas`) — `AfpThermoOverlayCanvas.vue` + `frontend/utils/afpThermoOverlay.ts` + 23 Vitest tests. Phase 2 (live embeds) queued as MFFD-RENDER-AFP-THERMO-OVERLAY-4. | **🚧 in-flight (slices 1+2 ✓; slice 3 in PR #1920)** | `plugins/vis-afp-thermo-overlay/docs/` + `aidocs/16` MFFD-RENDER-AFP-THERMO-OVERLAY |
| Templates feature (`ShepardTemplate` entity + full CRUD REST + `TemplateBodyValidator` JSON DSL + `:USES_TEMPLATE`/`:ALLOWS_TEMPLATE` edges + server-side DataObject instantiation + YAML import/export) | none | T1a–T1f shipped; T1g (update-to-latest) parked | **✓ T1a–T1f shipped** | `aidocs/54` + `aidocs/16 T1` |
| Process design + runtime in shepard core (`ProcessDefinition` + browser-hosted stepper) | SPW desktop only | TBD | 📐 (queued, PR1) | `aidocs/40 §2` |
| **IMP1 — dry-run import validation (plan-seal pattern)** — `POST /v2/import/validate` dry-runs a manifest (DataObjects + Containers + References) and issues a commitId (plan seal) if structurally valid; `GET /v2/import/plans/{commitId}` retrieves a persisted plan; `GET /v2/import/context` returns a collection state snapshot (DataObject count + fingerprint + optional semantic annotation vocab) so agents can generate consistent manifests. Plans expire 24 h; the `POST /v2/import/jobs` commit endpoint is deferred. In-tree: the commitId / plan-seal contract is core infrastructure on which `shepard-plugin-importer` will build. Backend only (no UI yet). 38 unit tests (35 original + 3 for IMP1 TODO closure). **IMP1 TODO closure:** `GET /v2/import/context?includeSemanticGraph=true` now returns live collection-scoped annotations via `SemanticAnnotationDAO.findByCollectionAppId(String)` — previously returned empty list. | none | **IMP1 shipped + IMP1 TODO closed** — `ImportPlan` entity + `ImportPlanDAO` + `ImportValidationService` + `ImportV2Rest` + `ImportContextIO`/`ImportManifestIO`/`ImportPlanIO` + `V55` NOOP migration; 38 tests. `SemanticAnnotationDAO.findByCollectionAppId` added. | **⚙** (IMP1 backend, UI deferred) | `aidocs/34` IMP1 |
| **IMP2 — import execute leg (`POST /v2/import/jobs`)** — consumes a `commitId` plan seal and runs the manifest. Pre-flight: plan not found → 404; USED → 409; EXPIRED / null-manifestJson → 410; INVALIDATED → 422; fingerprint mismatch → 409; lock conflict → 409. Execution: two-pass DataObject creation (Pass 1 creates without links; Pass 2 wires parent + predecessorIds; successorIds left null to avoid strict-equality guard), then container creation (FILE/TIMESERIES/STRUCTURED_DATA). Plan marked USED even on PARTIAL_FAILURE (commitId is one-shot). Returns 201 `ImportJobResultIO {jobAppId, planCommitId, status, dataObjects, containers, errors}` or 207 on PARTIAL_FAILURE. **IMP2 limitation:** reference wiring (Container→DataObject attachment) is NOT performed — `ManifestReferenceIO` carries only localRefs, no payload OIDs. Warning added to `errors`. Deferred to IMP3. New classes: `ImportJobsV2Rest`, `ImportExecutionService`, `ImportJobRequestIO`, `ImportJobResultIO`. `ImportPlan` gains `manifestJson` field (stored by `ImportValidationService` from IMP2 onwards). `V83__ImportPlan_manifestJson.cypher` adds `commitId` index. 12 unit tests. Backend only (UI deferred). | none | **IMP2 shipped** — `ImportJobsV2Rest` + `ImportExecutionService` + `ImportJobRequestIO` + `ImportJobResultIO` + `ImportPlan.manifestJson` field + `V83` commitId index migration; 12 tests. Reference wiring deferred to IMP3. | **⚙** (IMP2 backend, UI deferred) | `aidocs/34` IMP2 |
| **IMP-LOCK — persistent import-in-progress lock with heartbeat** — `:ImportLock` Neo4j entity that survives backend restarts; client importers acquire/heartbeat/release via REST. Six endpoints: `GET /v2/import/lock`, `POST /v2/import/lock` (acquire), `POST /v2/import/lock/{lockId}/heartbeat`, `POST /v2/import/lock/{lockId}/release` (→ COMPLETED), `POST /v2/import/lock/{lockId}/abandon` (→ FAILED), `DELETE /v2/import/lock/{lockId}` (admin cancel). Stale-heartbeat eviction after 5 min (auto-ABANDONED, new lock created). All terminal states preserved in Neo4j for audit. 17 unit tests. Frontend visibility deferred. | none | **IMP-LOCK shipped** — `ImportLock` entity + `ImportLockDAO` + `ImportLockService` + `ImportLockV2Rest` + `ImportLockIO` + `V65` constraints migration; 17 tests (now 20 with IMP-DIAG emission verification). | **⚙** (IMP-LOCK backend, UI deferred) | `aidocs/34` IMP-LOCK |
| **IMP-DIAG — structured import diagnostic ring buffer + REST query (#193)** — in-memory ring buffer (last 10,000 events per run, 24h TTL) backed by `ConcurrentHashMap<String, Deque<ImportDiagnosticEvent>>`. `runId` = `ImportLock.lockId`. Four new `/v2/` endpoints: `GET /v2/import/diagnostics/{runId}` (event list with optional `?level=` and `?phase=` filters), `GET /v2/import/runs` (run summaries sorted by start time desc), `POST /v2/import/diagnostics/{runId}/events` (ingest single event from Python importer), `POST /v2/import/diagnostics/{runId}/events/batch` (batch ingest). Java-side lifecycle wiring in `ImportLockService`: acquire → WARMUP/INFO, release → COMPLETE/INFO, abandon → COMPLETE/ERROR. External importers push DO_CREATE/REF_ATTACH/FILE_UPLOAD events via the POST ingress. 35 unit tests: 18 (`ImportDiagnosticsLogTest`) + 17 updated (`ImportLockServiceTest`). Frontend deferred. | none | **IMP-DIAG shipped** — `ImportDiagnosticEvent` record + `ImportDiagnosticsLog` service + `ImportDiagnosticsIO` shapes + `ImportDiagnosticsV2Rest`; 35 tests. | **⚙** (IMP-DIAG backend, UI deferred) | `aidocs/34` IMP-DIAG |
| Snapshots (point-in-time, immutable, reproducible reads) | `Version` is a marker only | V2a shipped: `revision: long` counter on every `VersionableEntity` (backfilled by V36; surfaced read-only on all entity IOs). V2b shipped: `Snapshot` + `SnapshotEntry` model; `POST/GET /v2/collections/{appId}/snapshots`; `GET/manifest/DELETE /v2/snapshots/{appId}`; `V40` uniqueness constraint; 29 unit tests (22 + 7); `docs/reference/snapshots.md`. V2c shipped: `GET /v2/collections/{collectionAppId}/snapshots/{snapshotAppId}/data-objects` — snapshot-pinned DataObject list; `SnapshotDataObjectsIO` record; 2 new DAO methods + 1 service method; 9 unit tests; ownership 409 gate. **V2d shipped:** `ExportSelection.snapshotAppId` field; `ExportService.resolveSnapshotFilter()` helper; `SnapshotService` injected into `ExportService`; 3 new unit tests (`ExportSnapshotFilterTest`); backward-compat 2-arg constructor. V2e shipped: `GET /v2/snapshots/{a}/diff/{b}` diff endpoint (`SnapshotDiffRest`; `SnapshotDiffIO` + nested `DiffEntry`; added/removed/changed/unchangedCount; 9 unit tests). **UI1a shipped:** `SnapshotsPane.vue` + `useSnapshots.ts` composable + `CollectionSnapshotApi.ts` + `SnapshotApi.ts` clients; create/list/delete/diff UI on Collection detail page (owner/manager only). V2f queued. | **✓ V2a + V2b + V2c + V2d + V2e + UI1a** / 📐 V2f | `aidocs/41` |

## 10. User profile + settings

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| ORCID iD on user profile (#29) | none | `PATCH /v2/users/me` (orcid); ISO 7064 mod 11-2 checked; UI edit dialog in ProfilePane | **✓ ↑** | U1a (#1062) + U1-profile-ui |
| `displayName` override + `effectiveDisplayName` derivation + audit-trail render switch (#694) | username only | `displayName` field + `DisplayNameResolver` fallback chain (U1b #1064); render switch across all IO classes + RO-Crate export (U1b2) | **✓ ↑** | `aidocs/36` |
| `/me` route (split from Configuration) | mixed Configuration page | New `pages/me/index.vue` absorbs Profile/API Keys/Subscriptions/Git Credentials; `/user` → `/me` redirect; Semantic Repos + User Groups merged into `/admin`; Configuration nav removed; `/configuration` → `/admin` redirect; `useSettingDescriptor` skeleton | **✓ shipped (U1c)** | `aidocs/36 §5` |
| Role-in-context chip in collection sidebar (Owner / Editor / Reader + Instance Admin) | none | `GET /v2/me/role-in/{collectionAppId}` → `{ read, write, manage, isInstanceAdmin }`; `CollectionRoleChip.vue` + `useMyRoleIn` composable; watches reactive appId, refetches on navigation | **✓ shipped (U1c2)** | `aidocs/51 §9.4` |
| Preferences (`theme`, `language`, `timeZone`, `dateFormat`, `defaultPageSize`, `defaultLandingPage`) via open-world `Map<String,String>` on `User.preferencesJson` + `GET/PATCH /v2/users/me/preferences` | none | `GET/PATCH /v2/users/me/preferences` (RFC 7396 merge-patch; null removes key; open-world string keys; 401 when unauth) | **✓ shipped (U1d)** | `aidocs/36 §3.2 / §7` |
| Avatar (shepard-uploaded → IdP `picture` → Gravatar tier) | none | TBD | 📐 (queued, U1e) | `aidocs/36 §3.1` |
| Secret-class settings (encrypted-at-rest with `~/.shepard/keys/secrets.key`) | none | TBD | 📐 (queued, U2-coupled) | `aidocs/36 §3.3` |

## 11. Lab journal

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| Markdown body interpretation (CommonMark + GFM) | plain text | `GET /v2/lab-journal/{appId}/render` — CommonMark 0.24.0 + GFM tables/strikethrough/task-lists; `sanitizeUrls=true`; `contentFormat: "MARKDOWN"` on IO | ✓ shipped (J1a) | `aidocs/37` |
| Inline `.ipynb` static render — backend: `GET /v2/lab-journal/{dataObjectAppId}/notebooks` listing `.ipynb` file refs (singleton + bundle, case-insensitive, `referenceKind` discriminator) | none | `NotebookRest` + `NotebookReferenceIO`; `EntityIdResolver` + permission gate; 14 unit tests | ✓ shipped (J1b backend) | `aidocs/37` |
| "Open in Jupyter" deep link via `editor.preferredJupyter` | none | (DEPRECATED) `DataObjectNotebooksPane.vue` — superseded by J1e + REF-UNIFIED-TABLE-FR1B. Per-user preference is now inert. | ⊘ retired (J1c → J1e) | `aidocs/37` |
| Admin-configurable JupyterHub link-out gate + per-notebook launch button in the unified data-references table | none | **Plugin (`shepard-plugin-jupyter`)**: `:JupyterConfig` singleton + `GET/PATCH /v2/admin/plugins/jupyter/config` (instance-admin; legacy shim at old path with WARN log per J1e-PR-07) + `GET /v2/jupyter/config` (public read) + `shepard-admin jupyter {status,enable,disable,set-hub-url}` CLI + JupyterHub compose sidecar (path-mounted at `/jupyterhub`). Backend + CLI classes live in `plugins/jupyter/` (moved by J1e-PR-02/03); Neo4j migration relocated to the plugin module. Admin tile under **JupyterHub link-out** + per-row launch button on `.ipynb` rows in the unified table. 28 backend tests + 4 CLI tests + 27 Vitest tests. | ✓ shipped (J1e — `plugins/jupyter` plugin, **not** in-core) | `aidocs/37` + `plugins/jupyter/docs/` + `docs/admin/runbooks/jupyterhub-config.md` |
| Unified Data References table includes FR1b singletons (Files + Notebooks) alongside FR1a bundles, timeseries, and structured data | none | `GET /v2/files/by-data-object/{appId}` additive list endpoint + `useFetchSingletonFileReferences` composable + `mapSingletonFileReferenceToDataTableElement` + `isIpynbFilename` classifier. Row download/delete/annotate + conditional Jupyter launch all live in the unified table; the J1c dedicated panel is retired. | ✓ shipped (REF-UNIFIED-TABLE-FR1B) | `aidocs/16` REF-UNIFIED-TABLE-FR1B |
| Edit history (append-only revisions) | write-once | `LabJournalEntryRevision` node + `has_lab_journal_revision` edge; `LabJournalEntryRevisionDAO.findByEntry()`; snapshot created in `updateLabJournalEntry` before content is overwritten; `GET /v2/lab-journal/{entryAppId}/history` returns `[{appId, content, revisedAt, revisedBy, revisionNumber}]` ordered newest first; 13 unit tests. **UI pending**: no history panel or diff viewer in `LabJournalExistingEntry.vue`. | ⚙ BE ✓ / UI pending (J1d) | `aidocs/37` |
| Display perf for large lab-journal lists (#507) | flat scroll | TBD; gated on L6 pagination | 📐 (queued) | `aidocs/37` / #507 |
| Bulk lab-journal fetch for Collection panel (no N+1 fan-out) | N+1: one `GET /shepard/api/labJournalEntries?dataObjectId=N` per DataObject | `GET /v2/collections/{collectionAppId}/lab-journal-entries` — single Cypher walk `Collection → DataObject → LabJournalEntry`, sorted `createdAt DESC`, each entry carries `dataObjectId`; replaces the `Promise.all` fan-out in `CollectionLabJournalEntryList.vue` (8514 → 1 reqs on MFFD-Dropbox, 8311 → 2 console errors); 6/6 JUnit + 9/9 Vitest + 1/1 Playwright live; closes UI-020. | ✓ shipped (UI-020) | `aidocs/agent-findings/ui-020-labjournal-bulk-fix-2026-05-24.md` |
| **J2 — JupyterHub deep integration** (`shepard-plugin-jupyter`, task #60). Sub-plan: **J2a** = admin link-out + JupyterHub compose sidecar (path-mounted at `/jupyterhub`) — **✓ shipped as J1e** (`plugins/jupyter/`). **J2b** = `shepard-py` SDK (Python pkg, kernel-image pre-installed; auto-reads `SHEPARD_ACCESS_TOKEN` + `SHEPARD_BASE_URL`) — 📐 queued. **J2c** = JupyterLab data-picker sidebar extension (`shepard-kernel-ext`) — 📐 queued. **J2d** = "Send to Shepard" magic cell / template notebook — 📐 queued. | none | J2a shipped via J1e; J2b–J2d designed in `aidocs/81` | **✓ J2a / 📐 J2b–J2d** | `aidocs/integrations/81` + `plugins/jupyter/docs/` |

## 12. AI features

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| **OpenAI-compatible LLM provider plugin** (`shepard-plugin-ai`, AI1a) — `LlmProvider` SPI implementation for OPENAI_COMPAT transport; per-capability runtime config via `GET/PATCH /v2/admin/config/ai` (APISIMP-AI-ADMIN-REST: migrated from `/v2/admin/ai/capabilities/{capability}`); 8 capability slots (TEXT, FAST_TEXT, IMAGE_GEN, VISION, EMBEDDING, STRUCTURED, TRANSCRIPTION, MODERATION); `:AiCapabilityConfig` Neo4j singleton per slot; `:AiActivity` provenance node (SHA-256 promptHash, token counts, modelId, provider) on every call; prompt injection defence (trusted system role + delimiter-wrapped untrusted user role); `V58` uniqueness constraint; 28 unit tests (16 LlmRequest + 12 LlmProviderImpl). v0: TEXT capability only. **Admin UI shipped 2026-06-26** — `AdminAiConfigPane.vue` (8-slot table + per-slot edit dialog); `useAiConfig.ts` composable (GET/PATCH); 8 Vitest tests. | none | **AI1a + APISIMP-AI-ADMIN-REST shipped** — `shepard-plugin-ai` v0 provides the infrastructure all AI-augmented plugins compile against. Admin pane at `/admin#ai-config` — real functional table with per-slot editing. | **✓ ↑** (AI1a + admin UI 2026-06-26) | `plugins/ai/docs/`, `aidocs/43 §4` |
| **Rolling-median MAD anomaly detection** (AI1b) — `POST /v2/references/{appId}/actions?action=detect-anomalies` (formerly `/v2/references/{appId}/detect-anomalies` — moved APISIMP-ANOMALY-ACTION-PATH fire-513; old path tombstoned → 410); pure-Java translation of `examples/lumen-showcase/notebooks/anomaly-analysis.ipynb`; edge-padding, per-point median + MAD, 1.4826 consistency factor, MAD floor 1e-3; configurable `window` (default 51, forced odd, clamped to series length) and `k` threshold (default 6.0); optional `createAnnotations=true` persists a `TimeseriesAnnotation` per contiguous interval (label `"anomaly"`, `aiGenerated=true`, confidence = `min(1.0, maxZScore / (2k))`); 28 unit tests; no migration. **Auth fix (AI1b-fix)**: was calling `isAccessTypeAllowedForUser(ogmId, ..., 0L)` which always 403'd on DataObjects (no own `:Permissions` node); fixed to `isAccessAllowedForDataObjectAppId(appId, ...)` (walks up to parent Collection). `examples/lumen-showcase/seed.py` enriched with `best_effort_anomaly_detection()` — calls the endpoint post-seed to annotate anomalous intervals in the `vib_fuel_pump` channel; idempotent (checks for existing `aiGenerated` annotations before calling). **APISIMP-ANOMALY-5TUPLE-ADD-UUID (fire-511)**: request body now accepts `channelAppId` (UUID v7) as an alternative to the 5-tuple channel selector; mutually exclusive (422 if both; 400 if channelAppId not found in ref). **APISIMP-ANOMALY-ACTION-PATH (fire-513)**: moved from `/detect-anomalies` sub-path to generic `?action=` dispatcher (`ReferenceActionsRest`); non-timeseries kind → 422. | none | **AI1b shipped + auth fix applied + channelAppId selector added + action path unified** — endpoint works for any user with Collection Read permission; `createAnnotations=true` additionally requires Write; callers can now address channels by UUID instead of 5-tuple; path now `/v2/references/{appId}/actions?action=detect-anomalies`. | **✓ ↑** (AI1b + AI1b-fix + APISIMP-ANOMALY-5TUPLE-ADD-UUID + APISIMP-ANOMALY-ACTION-PATH) | `aidocs/43 §3.1`, `aidocs/16` AI1b, `aidocs/16` APISIMP-ANOMALY-5TUPLE-ADD-UUID, `aidocs/16` APISIMP-ANOMALY-ACTION-PATH, `aidocs/34` AI1b-fix |
| **Timeseries interval/point annotations** (TA1a) — `TimeseriesAnnotation` Neo4j entity with `startNs`, `endNs`, `label`, `description`, `aiGenerated`, `confidence`; linked from `TimeseriesReference` via `has_timeseries_annotation`; `/v2/timeseries-references/{refAppId}/annotations` CRUD; V35 migration; 15 unit tests | none | **TA1a shipped** — full CRUD REST surface, auth piggybacks DataObject permissions, `aiGenerated` flag ready for TA1c anomaly output | **✓ ↑** | `aidocs/16 TA1a` |
| **AT1 — `TimeseriesAnalytics` SPI extraction (`shepard-plugin-analytics-ts`)** — new `de.dlr.shepard.spi.analytics` SPI with `ExecutionMode` tier (`IN_PROCESS` / `VIA_ORCHESTRATOR`), `RemoteTimeseriesAnalytics` sub-interface for the future orchestrator-tier (`shepard-plugin-mlops` — Airflow/REBAR/SAIA adapters), `JobStatus` + `JobHandle` value classes, `AnalyticsRegistry` CDI bean (mirrors `MinterRegistry`). `plugins/analytics-ts/` Maven module ships `MADDetector implements TimeseriesAnalytics` registered as detector id `"mad-v1"` — the AI1b math extracted **verbatim** (JDK `Arrays.sort` + index-arithmetic median is the trusted primitive). `MADDetectorBehaviouralEquivalenceTest` asserts `Double.doubleToRawLongBits` equality between extracted and in-tree on 11 cases (100 randomized median trials + multiple synthetic series shapes + window/k sweeps). `AnomalyDetectRequestIO` gains optional `detectorId` field (default `mad-v1`); `AnomalyDetectRequestIOWireStabilityTest` proves omitting it is byte-identical to pre-AT1. SHACL TTL `mffd-anomaly-annotation.shacl.ttl` ships in `plugins/analytics-ts/src/main/resources/shapes/` proposing `fair2r:StatisticalPass` sibling-Activity to `fair2r:AuthoringPass` (relaxed required fields: `detectorId`, `parametersUsed`, `librarySource`). 33 plugin + 5 wire-stability tests, all green. | none | **AT1 partially shipped** — SPI seam + extracted plugin landed; existing in-tree `AnomalyDetectionService` still serves AI1b endpoint (dispatcher rewire pending); anomaly persistence continues to `:TimeseriesAnnotation` exactly as today. Plugin defaults off; opt-in via admin REST. SHACL substrate target documented; rewire pending parallel SHACL non-TS agent's substrate landing in main. | **🚧** (PR-1..PR-3 + PR-4 partial shipped; PR-4 dispatcher rewire / PR-5 SHACL substrate / PR-6 persistence rewire deferred) | `aidocs/34` AT1 row; `plugins/analytics-ts/docs/{reference,quickstart,install}.md` |
| Channel-quality scoring (background job: `TimeseriesReference.qualityScore` ∈ `[0.0, 1.0]` — completeness + coverage + stability heuristic; opt-in via `shepard.timeseries.quality-scoring.enabled`) | none | pure-heuristic scorer, no LLM | ⚙ BE ✓ / UI pending (AI1c) | `aidocs/43 §3.2`, `aidocs/16` AI1c |
| **AI1v-Phase-1 — channel unit auto-inference (deterministic floor)** — `ChannelUnitInferenceService` (in-tree CDI bean) maps `channel_metadata.field` names to QUDT canonical IRIs via three tiers (SUFFIX / PREFIX_HEURISTIC / WELDING_CAP). Wired into `TimeseriesSemanticDualWriteService` so every new channel gets a `:SemanticAnnotation{propertyIRI:'urn:shepard:unit'}` automatically. Tier-derived confidence (1.0 / 0.9 / 0.85); ambiguous fields skipped + WARN-logged for Phase 2. 59 unit tests + 4 dual-write integration tests. Python recovery script (`examples/mffd-showcase/scripts/recovery/annotate-channel-axes-and-units.py`) doubles as backfill for pre-AI1v instances. | none | shipped — auto-fires on every channel creation, no operator action required; ambiguous tail (≈16% of LUMEN+MFFD channels) hands off to AI1v-Phase-2 (gated on AI plugin / AI1a) | **✓ ↑** (AI1v-Phase-1, 2026-05-28) | `aidocs/16` AI1v-Phase-1 row; `aidocs/34` AI1v-Phase-1 row |
| **Time-reference model** (TM1a) — `timeReference` (`WALL_CLOCK` / `EXPERIMENT_RELATIVE`), `wallClockOffset` (UTC ns of experiment t=0), `wallClockOffsetSource` (provenance tag) on `TimeseriesReference`; `PATCH /v2/timeseries-references/{appId}` merge-patch endpoint; V37 backfill migration; 10 unit tests | none | **TM1a shipped (backend)** — enables correct wall-clock alignment for DAQs using experiment-relative timestamps (e.g. IRIG-B, custom epoch); offset mutable so corrections can be applied post-ingest. **UI pending**: no edit dialog or display for these fields. | **⚙ BE ✓ / UI pending ↑** | `aidocs/16 TM1a` |
| Embedding-based similarity (`/data-objects/{appId}/similar`) | none | TBD; needs `/v1/embeddings` endpoint | 📐 (queued, AI1d) | `aidocs/43 §3.5` |
| **Snap dashboards** — Claude-chat-style chat sidebar with closed tool-use catalogue + Vega-Lite v5 inline rendering | none | TBD; **headline killer feature** | 📐 (queued, AI1e) | `aidocs/43 §5.8` |
| Natural-language search | none | TBD | 📐 (queued, AI1f) | `aidocs/43 §5.1` |
| **Lab journal authoring assist** (`shepard-plugin-wiki-writer`, WW1) — `POST /v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}/wiki-write`; assembles layered prompt from DataObject metadata (attributes, status, description) + Collection siblings + predecessor/successor chain; calls LlmProvider TEXT capability; writes result as `LabJournalEntry` on the DataObject; returns `{labJournalEntryId, generatedSummary, activityAppId, inputTokens, outputTokens}`; soft-depends on `shepard-plugin-ai` (returns 503 when absent); 10 unit tests. v0: local lab journal entry only (no external wiki push). | none | **WW1 shipped** — wiki-writer plugin v0. Requires `shepard-plugin-ai` TEXT capability configured. Frontend "Generate journal entry" action: UI planned for v0b. | **⚙ BE ✓ / UI pending ↑** (WW1) | `plugins/wiki-writer/docs/`, `aidocs/43 §5.2` |
| Semantic-annotation suggestion | none | TBD | 📐 (queued, AI1h) | `aidocs/43 §5.4` |
| **Lumen experiment ontology** (`shepard-experiment.ttl`) — OWL-Lite + SKOS vocabulary: 7 concept schemes covering experiment phases, measurement roles, quality flags, defect types, inspection methods, manufacturing processes, sensor roles; extends PROV-O + metadata4ing + QUDT; registered via N1c2 admin bundle; vocabulary for annotation picker (N1e) + Lumen SPARQL tool (AI1q) + SB1 data-binding labels; supersedes N1d | n/a | design done | 📐 (queued, AI1r; ungated — just a .ttl upload) | `aidocs/43 §7.2` |
| **Lumen `query_knowledge_graph` SPARQL tool** — extends snap-dashboard tool catalogue with read-only SPARQL SELECT against n10s graph; enforced at proxy (N1f); every call logged to `:Activity`; unlocks structural cross-source reasoning over `shpe:` + `m4i:` + `prov:` terms | n/a | design done | 📐 (queued, AI1q; gated on AI1e + N1f) | `aidocs/43 §7.1` |
| Auto-summarisation of run outcomes | none | TBD | 📐 (queued, AI1i) | `aidocs/43 §5.3` |
| RO-Crate description generation | none | TBD | 📐 (queued, AI1j) | `aidocs/43 §5.5` |
| Conversational lineage (chat over the lineage graph) | none | TBD | 📐 (queued, AI1k) | `aidocs/43 §5.6` |
| Notebook scaffolding | none | TBD | 📐 (queued, AI1l) | `aidocs/43 §5.7` |
| **TPL9 — f(ai)²r AI provenance capture** — `AiActivityType` enum (6 values) + `AiProvenanceCapture` `@ApplicationScoped` CDI bean under `de.dlr.shepard.v2.ai`. `McpToolSupport.run()` wired to stamp `actionKind="AI_ACTION"` `:Activity` node with f(ai)²r metadata when inbound MCP call carries `X-AI-Agent` header. `Fair2rPredicates` gains `AI_ACTION_TYPE` constant. Capture is best-effort (never throws). 7 unit tests. EU AI Act Art. 50 transparency hook for inbound AI-agent MCP calls. PROMPT1 extends this with full prompt substrate. Design: `aidocs/platform/89-tpl9-fair2r-ai-provenance.md`. | none | shipped — no new endpoints or config; `AI_ACTION` rows appear in `GET /v2/provenance/activities` when `X-AI-Agent` header is set | **✓ ↑** (TPL9, 2026-05-26) | `aidocs/platform/89` + `aidocs/34` TPL9 row |
| **PromptLog substrate** (`shepard-plugin-promptlog`, PROMPT1) — prompts as first-class Shepard payload kind. Substrate realisation of TPL9's `fair2r:Prompt` + `fair2r:Transcript`. **ADOPT** OpenTelemetry GenAI v1.38+ wire format + Langfuse-pattern substrate split (Postgres metadata + Garage S3 bodies + pgvector embeddings) + PROV-O + F(AI)²R vocabulary; **BUILD** thin Shepard semantic adapter (`:PromptRun` Neo4j identity node bridging OTel spans ↔ `fair2r:Prompt`/`Transcript` ↔ `:DataObject` prov edges). Ten REST endpoints under `/v2/promptlog/*`; four SHACL shapes (`PromptTemplate`, `PromptRun`, `PromptChain`, `PromptEvaluation`); UI surface (list / detail / lineage DAG / similar / agent-runs / template-runs / eval); MCP integration (every MCP tool call auto-recorded); EU AI Act Article 50 evidence-pack endpoint emits JSON-LD per F(AI)²R reference shape. Sub-rows PROMPT-a..j (~29 days). Critical path a→b→i delivers Article 50 evidence by 2026-08-02 deadline. Subsumes TPL9e (replaces `:StructuredData` blob storage). | none | design done | 📐 (queued, PROMPT1 — design landed 2026-05-23) | `aidocs/semantics/99-promptlog-design.md`; `aidocs/16` PROMPT1 |
| **PROMPT-h2 — per-Collection `promptLogMode` field** — `Collection.promptLogMode` String + `PromptLogMode` enum (`HASH_ONLY` / `BODY_REDACTED` / `BODY_RAW`). `CollectionIO` wire field (NON_NULL, v1-safe). `CollectionService` create+update paths. `V91__Set_default_promptLogMode_on_collections.cypher` backfill migration. `PromptLogModeSelector.vue` (v-select, 3 options) in `EditCollectionDialog` under advanced mode. 7 tests. PROMPT-a storage logic reads this field at PromptRun write-time. Resolves ESCALATION-PROMPT-2 (PII × hash coherence). | none | shipped | **✓ ↑** (PROMPT-h2, 2026-05-27) | `aidocs/semantics/99 §10-11`; `aidocs/34` PROMPT-h2 row |

## 13. Admin tooling

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| `/v2/admin/instance-admins` (list / grant / revoke) — REST surface for admin-role mutation | none | shipped — `@RolesAllowed("instance-admin")`-gated; returns audit trail (`grantedBy`, `grantedAt`); CLI counterparts deferred to L1 Phase 1. **APISIMP-INSTANCE-ADMINS-BARE-LIST (fire-458, PR open)** — `GET /v2/admin/instance-admins` list response re-wrapped in `PagedResponseIO<InstanceAdminGrantIO>` after regression in PR #2360. | **✓ ↑** | A0 / `aidocs/51 §10` |
| `POST /v2/admin/bootstrap` — first instance-admin via the one-shot bootstrap token | none | shipped — unauthenticated (token-gated); replay-protected | **✓ ↑** | A0 / `aidocs/51 §5.2` |
| `GET /v2/admin/permission-audit` — list orphan-permissions entities | none | shipped (post-C3 fail-closed). **APISIMP-INSTANCE-ADMIN-LIST-ENVELOPES (fire-301)** — response wrapped in standard `PagedResponseIO<PermissionAuditEntryIO>` (consistent with sibling `/permission-audit/log` which was already paginated). | **✓ ↑** | A0 / `aidocs/51 §10` |
| Admin CLI (`shepard-admin`) — read-only commands (Phase 1: `features list`, `health`, `migrations status`) | none | **shipped** — top-level `cli/` Maven module, Java 21 + Picocli 4.7, shaded uber-jar, 54 tests / 89 % instruction / 81 % branch coverage. Auth via `X-API-KEY` + the instance-admin role per A0. Phase 2+ still designed. | **✓ ↑** (Phase 1) | `aidocs/22 §7.1` |
| Admin CLI cleanup of soft-deleted entities (TTL) | none | designed | 📐 (queued, L1 phase 2) | `aidocs/22 §4.1` |
| Admin CLI RO-Crate import / export | none | designed | 📐 (queued, L1 phase 3) | `aidocs/22 §4.7` |
| Admin CLI feature-toggle inspection / flipping (incl. profile-bound) | none | designed (read-only ✓ shipped under L1 Phase 1) | 📐 (write path queued) | `aidocs/22 §4.6 / §4.6a` |
| `shepard-admin init` TUI wizard for first-run `.env` (Lanterna) | none | designed; deferred from L1 Phase 1 ship | 📐 (queued) | `aidocs/22 §4.11` |
| Universal TUI mode for every command (auto-fill from server state) | none | designed | 📐 (queued) | `aidocs/22 §4.x` |
| Env-driven auth discovery (`SHEPARD_ADMIN_URL` / `SHEPARD_ADMIN_API_KEY`) for the CLI | none | **shipped** — flags > env > `~/.shepard/admin.toml` > defaults precedence ladder; `AdminConfigLoaderTest` covers each layer | **✓ ↑** | `aidocs/22 §3.4` |
| Init wizard's OIDC sub-flow (Keycloak / Pocket ID / external w/ auto-discovery) | none | designed; depends on F8 (configurable claim path) for non-Keycloak | 📐 (queued) | `aidocs/22 §4.11a` |
| **NTF1 — Notification transports admin pane** — backend: `:NotificationTransport` Neo4j entity + admin CRUD (`GET/POST/PATCH/DELETE /v2/admin/notifications/transports`) + SMTP sender (Eclipse Angus jakarta.mail) + Matrix sender (JDK HttpClient, client-server API v3) + per-transport test dispatch (`transportId` extension on `POST /v2/admin/notifications/test`) — all six `NTF1-BACKEND-*` rows ✓ shipped 2026-05-31 with 71 JUnit tests. Read-side IO omits `smtpPassword` + `matrixAccessToken` by type (compile-time guarantee + regression test). Frontend ✓ shipped 2026-05-31 via `NTF1-UI-TRANSPORT-CRUD-FOLLOWUP` — `AdminNotificationsPane.vue` carries per-kind `NotificationTransportSection.vue` sub-components (list + create + edit + delete + smoke-test) for both SMTP and Matrix; `useNotificationTransports.ts` composable enforces the write-only credential contract by omitting `smtpPassword` / `matrixAccessToken` from PATCH bodies when blank (preserves stored value; non-blank rotates). **APISIMP-NOTIF-TRANSPORT-LIST-ENVELOPE (fire-296, PR open)** — `GET /v2/admin/notifications/transports` list envelope migrated from bespoke `NotificationTransportListIO` (`{transports:[…]}`) to standard `PagedResponseIO<NotificationTransportReadIO>` (`{items:[…], total, page, pageSize}`); bespoke wrapper deleted. | none | **✓ shipped** | **✓ shipped** (NTF1 — BE + UI shipped 2026-05-31; APISIMP envelope cleanup fire-296) | `aidocs/integrations/40-notification-system.md` + `aidocs/16` NTF1-BACKEND-* + NTF1-UI-TRANSPORT-CRUD-FOLLOWUP + PLACEHOLDER-REPLACE-NTF1 + APISIMP-NOTIF-TRANSPORT-LIST-ENVELOPE + `aidocs/agent-findings/ui-scrutinizer-2026-05-30.md` |
| **ADM-MANAGE — instance-admin role grant/revoke UI** — backend REST shipped (lines 301 / 381–383: `GET/POST/DELETE /v2/admin/instance-admins`). Frontend admin pane `AdminInstanceAdminsPane.vue` (shipped 2026-05-31) lists current grants with source chips + grantedBy + grantedAt; per-row Revoke button for Neo4j-sourced grants (IdP grants flagged as IdP-managed). Shared `UserSearchAutocomplete.vue` is used by all three ADM-* panes. | none | **BE ✓ / UI ✓** | **✓ shipped** (ADM-MANAGE — frontend + composable + tests, 2026-05-31) | A0 / `aidocs/51 §10` + `aidocs/16` PLACEHOLDER-REPLACE-ADM-MANAGE + `aidocs/agent-findings/ui-scrutinizer-2026-05-30.md` |
| **ADM-USR-ORCID — admin pane to set/clear other users' ORCIDs** — backend admin override path shipped (instance-admin can override `/v2/users/me/orcid` on behalf of deactivated accounts / audit hand-offs). Frontend pane `AdminUserOrcidPane.vue` (shipped 2026-05-31): user-search + ORCID text input with inline `isValidOrcid` (ISO 7064 mod 11-2) validation + Save (sends `null` to clear). Per-user self-edit (line 249, U1a) is separate and ✓ shipped. | none | **BE ✓ / UI ✓** | **✓ shipped** (ADM-USR-ORCID — frontend + composable + tests, 2026-05-31) | `aidocs/16` ADM-USR-ORCID + PLACEHOLDER-REPLACE-ADM-USR-ORCID + `aidocs/agent-findings/ui-scrutinizer-2026-05-30.md` |
| **ADM-USR-GIT — admin pane to issue/rotate other users' git credentials** — backend exposes `POST /v2/admin/users/{username}/git-credentials` (idempotent create-or-replace by host), `GET /v2/admin/users/{username}/git-credentials` (list with PAT-omitted, `lastRotatedAt` per-row; shipped 2026-05-31 ADM-USR-GIT-BACKEND-1; list response migrated to `PagedResponseIO` envelope by APISIMP-GIT-CRED-LIST-ENVELOPE fire-301), and `POST /v2/admin/users/{username}/git-credentials/{appId}/rotate` (dedicated rotate; shipped 2026-05-31). `lastRotatedAt` field on `:GitCredential` (additive, nullable; shipped 2026-05-31). Frontend pane `AdminUserGitPane.vue` (shipped 2026-05-31): user-search + host/username/PAT/displayName form + per-user credentials table (host, displayName, lastRotatedAt) + explicit Rotate dialog wired to `ADM-USR-GIT-BACKEND-1-FE` (shipped 2026-05-31). Per-user self-edit (line 229, G1-cred) is separate and ✓ shipped. | none | **✓ shipped** | **✓ shipped** (ADM-USR-GIT — backend + UI shipped 2026-05-31) | `aidocs/integrations/47-dev-experience-and-plugin-system.md` + `aidocs/16` ADM-USR-GIT + PLACEHOLDER-REPLACE-ADM-USR-GIT + ADM-USR-GIT-BACKEND-1 + `aidocs/agent-findings/ui-scrutinizer-2026-05-30.md` |
| **FE-PROV-INSTANCE-REGISTRY — peer Shepard instance registry admin UI** — `:InstanceRegistry` Neo4j singleton + `GET/PATCH /v2/admin/instances` shipped 2026-05-27 (aidocs/16 line 1308). Drives the friendly tooltip text on cross-instance provenance badges ("DLR BT, Augsburg" instead of `dlr-augsburg`). Frontend: `AdminInstanceRegistryPane.vue` (add-form with instanceId/displayName/baseUrl/dlrInstitute validation, `v-data-table` list, delete action) + `useInstanceRegistryAdmin.ts` composable (148 lines) + 36 Vitest cases; mounted at `/admin/instance-registry` + wired into `/admin#instance-registry` fragment. Confirmed shipped fire-69. | none | **BE + UI ✓** | **✓ ↑** (FE-PROV-INSTANCE-REGISTRY — BE + UI shipped, fire-69) | `aidocs/16` FE-PROV-INSTANCE-REGISTRY + PLACEHOLDER-REPLACE-FE-PROV-INSTANCE-REGISTRY + `aidocs/frontend/100 §5 + §4.7` + `frontend/components/context/admin/AdminInstanceRegistryPane.vue` |
| **SEMA-V6-PRED-UI — per-predicate usage stats page** — `/semantic/predicates/{predicateIri}` route renders only the IRI as a heading + a "Suggested query" code block + a `PlaceholderImplStatus` chip + an "Open in SPARQL playground" button. No usage count, no top values, no annotated-entity list. Backend stats endpoint (`GET /v2/semantic/predicates/{iri}/stats`) is not yet shipped. **UI is placeholder** — see `aidocs/16 PLACEHOLDER-REPLACE-SEMA-V6-PRED-UI`. | none | **BE partial / UI placeholder** | **🅰️ ↑** (SEMA-V6-PRED-UI — BE partial, UI placeholder) | `aidocs/semantics/100` §5 + `aidocs/16` PLACEHOLDER-REPLACE-SEMA-V6-PRED-UI + `aidocs/agent-findings/ui-scrutinizer-2026-05-30.md` |
| **#75 — `scripts/upgrade-from-v5.sh` operator upgrade wizard** — pure-bash interactive upgrade wizard for operators upgrading from upstream v5.2.0 to this fork. Dry-run default (safe); `--apply` flag executes steps; `--no-prompt` for unattended CI runs; tee'd `upgrade-YYYYMMDD-HHMMSS.log`. Pre-flight: Docker probe, version endpoint, required + fork-introduced env-var audit, backup confirmation. Compose-diff summary (Garage / HSDS / Prometheus / Grafana sidecars). Config-key table (G1-cred, A3c, KIP1h, plugin defaults, HDF, S3, AI). Runtime-enumerated Neo4j `.cypher` and SQL `.sql` migration lists with rollback hints. Interactive breaking-change prompts (G1-cred / C3 / A0 / A3c / KIP1h / Unhide default / V6 plugin compat). Apply phase: `docker compose pull && up -d`. Post-upgrade checklist. | none | **✓ ↑** (#75, shipped 2026-05-26) | `scripts/upgrade-from-v5.sh` + `aidocs/34` #75 row |
## 13a. File storage backend
| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| Files stored in MongoDB GridFS (1 MiB chunks, one bucket per FileContainer) | **=** | **=** (today) | **=** | `FileService.java` |
| **`FileStorage` SPI seam in core** — `de.dlr.shepard.storage.{FileStorage, FileStorageRegistry, StorageLocator, StoragePutRequest, StorageGetResponse, StorageException}` family. In-core `GridFsFileStorage` default adapter (wraps `FileService`); `shepard.storage.provider=gridfs` deploy-time key (cluster-identity exception). Optional-posture registry (no fail-fast on missing/disabled adapter — 503 `storage.provider.not-installed` envelope). `:ShepardFile.providerId` Neo4j property + V34 backfill. `shepard-admin storage status` CLI verb. | none | shipped (FS1a) | **✓ ↑** | `aidocs/45 §3.2`, `de.dlr.shepard.storage.*` |
| **`shepard-plugin-file-s3`** — S3-compatible file storage adapter (FS1b). `S3FileStorage implements FileStorage` (AWS SDK v2); any S3-compatible endpoint works (Garage, AWS S3, Cloudflare R2, Backblaze B2, Wasabi, MinIO, SeaweedFS, Ceph RGW). Locator: `<containerOid>/<uuid>` (bucket not stored — read from config at runtime). Config: `shepard.files.s3.{endpoint,region,access-key-id,secret-access-key,bucket,path-style-access}` (deploy-time only per cluster-identity exception). `FileS3PluginManifest` in `GET /v2/admin/plugins`. `shepard.storage.provider=s3` activates; `shepard.plugins.file-s3.enabled=false` default (opt-in). | none | **FS1b + FS1d shipped** (2026-05-16) | **✓ ↑** (FS1b + FS1d, this commit) | `aidocs/45 §3.2 + §3a + §9` + `aidocs/34 FS1b` |
| **Garage sidecar in `infrastructure/docker-compose.yml`** (FS1d) — `files-s3` profile; `dxflrs/garage:v1.0.1` image; port 3900; volume `./garage-data`; quick-start operator runbook in compose comment. `docker compose --profile files-s3 up -d` enables it. | none | **FS1b + FS1d shipped** (2026-05-16) | **✓ ↑** (FS1b + FS1d, this commit) | `aidocs/45 §9 FS1d` |
| **Presigned-URL endpoints for S3 direct upload + download** (FS1c) — `POST /v2/file-containers/{containerAppId}/upload-url` → presigned PUT URL + assigned oid (15 min TTL). `POST .../upload-url/commit` → registers `ShepardFile` after upload. `GET /v2/file-containers/{containerAppId}/files/{oid}/download-url` → presigned GET URL (5 min TTL). Backend no longer a bytes proxy for S3. `FileStorage` SPI grows `presignedUploadUrl()` / `presignedDownloadUrl()` default methods (return `Optional.empty()` for non-S3 adapters). `FileContainerDAO.findByAppId()` added. `FileContainerService.commitUpload()` creates `ShepardFile` in Neo4j from presigned-upload metadata. New IO types: `PresignedUploadRequestIO`, `PresignedUploadUrlIO`, `UploadCommitIO`, `PresignedDownloadUrlIO` under `de.dlr.shepard.v2.filecontainer.io`. | none | **FS1c shipped** (2026-05-16) | **✓ ↑** (FS1c, this commit) | `aidocs/45 §4 + §9 FS1c` + `aidocs/34 FS1c` |
| **Big-bang file-storage migration** (FS1e1) — `POST /v2/admin/files/migrate` triggers an async sweep: for each `:ShepardFile` with `providerId = source`, streams bytes from the source adapter to the target adapter (OID preserved via `StoragePutRequest.assignedObjectKey` — only `providerId` changes in Neo4j), then deletes from source. `GET /v2/admin/files/migrate/status` polls in-memory state (IDLE / RUNNING / DONE / FAILED). `shepard-admin files migrate <source> <target>` + `shepard-admin files migrate-status` CLI verbs. `GET /v2/admin/storage` returns all discovered adapters with enabled/active state. Frontend: `AdminFileMigrationPane.vue` (506 lines — storage-adapter pickers, confirm dialog, live progress bar, per-file rollback dialog) + `useFileMigration.ts` composable (268 lines); mounted at `/admin#file-migration`. Confirmed fully shipped fire-69. Test obligation: `useFileMigration.ts` Vitest tests queued (`PLACEHOLDER-FS1e1-TESTS`). | none | **BE + UI ✓** | **✓ ↑** (FS1e1 — BE + UI fully shipped, fire-69) | `aidocs/45 §6` + `aidocs/34 FS1e1` + `aidocs/16` PLACEHOLDER-REPLACE-FS1e1 + `frontend/components/context/admin/AdminFileMigrationPane.vue` |
| **Per-file rollback fields + endpoint** (FS1e3) — four nullable wire-hidden fields on `:ShepardFile` (`previousProviderId`, `previousLocator`, `migratedAt`, `migrationHmac`) capture pre-migration state in the same Cypher SET as the `providerId` swap (left-to-right SET preserves the "stamp before swap" invariant inside one transaction). New `POST /v2/admin/files/migrate/rollback/{appId}` (instance-admin) reads bytes from the current adapter, writes back to the previous adapter using the preserved oid as `assignedObjectKey`, then clears the four bookkeeping fields and restores `providerId`. Refusal: 404 on unknown appId, 409 on "nothing to roll back" (previousProviderId null). Today's fused-Phase-4 `source.delete()` stays per task-spec ("no behaviour split yet — just record-keeping"). 13 new unit tests (entity wire-shape regression + Cypher contract + service round-trip + REST exception mapping). V59 NOOP additive cypher migration. Verify/commit/sweep-orphans verbs deferred to FS1e3c/d/e. | none | **FS1e3 BE shipped** (2026-05-22) | **⚙ BE ✓ / UI pending ↑** (FS1e3) | `aidocs/data/46-gridfs-to-s3-migration-runbook.md §17.B` + `aidocs/34 FS1e3` |
| **Background continuous-sweep storage migration** (FS1e2) — `@Scheduled autoSweep()` on `FileMigrationService`; fires every `shepard.migration.auto-sweep.interval` (default PT5M). Gated by `shepard.migration.auto-sweep.enabled=false` (default off — zero overhead for deployments not migrating). Configures source + target adapter ids via `application.properties`. Skips ticks when a migration is already RUNNING. Defers to `triggerMigration()` for all validation and state transitions. P3 SSE progress deferred; poll `GET /v2/admin/files/migrate/status`. 5 unit tests in `FileMigrationAutoSweepTest`. **UI pending**: no progress display in admin UI. | none | **FS1e2 BE shipped** (2026-05-17) | **⚙ BE ✓ / UI pending ↑** (FS1e2) | `aidocs/34 FS1e2` + `aidocs/16 FS1e2` |
| **Frontend presigned upload** (FS1f) — `FileContainerAccessor.uploadFile()` tries presigned PUT path when container has `appId` set; falls back to legacy `POST /shepard/api/fileContainers/{id}/payload` on 503 (GridFS active). Three-step flow: obtain presigned URL → PUT bytes direct to S3 → commit. `FileContainer` TypeScript model (`backend-client`) grows `appId?: string \| null` (was already returned by the backend; now declared in generated type). `backend-client` rebuilt. | proxied through backend | **FS1f shipped** (2026-05-16) | **✓ ↑** (FS1f) | `aidocs/45 §9 FS1f` + `aidocs/34 FS1f` |
| **Upload progress + cancel UI** (task #135, frontend-only) — `XMLHttpRequest.upload.onprogress` wired through both the legacy multipart and the presigned-PUT leg (fetch() can't observe upload progress). New `frontend/composables/container/xhrUpload.ts` (multipart + presigned PUT helpers with `{onProgress, signal, xhrFactory}` options + `UploadAbortError` / `UploadHttpError`), new `frontend/composables/container/useFileUploadProgress.ts` (per-file + aggregate state, EWMA rate, ETA, indeterminate fallback after 2 s stall, cancel via shared `AbortController`), new `frontend/components/common/dialog/FileUploadProgressPanel.vue` (Vuetify `v-progress-linear` + bytes/percent/ETA + per-file rows + Cancel button). `FileContainerAccessor.uploadFile()` grows optional `XhrUploadOptions` second arg; callers without progress unaffected. `DataObjectFileUploadDialog.vue`, `FileUploadDialog.vue`, `UploadFilesButton.vue`, `pages/containers/files/[containerId]/index.vue`, `VideoStreamReferencesPane.vue` wired. 22 new Vitest tests (xhr seam + composable). User flagged this while uploading a 507 MB Confluence zip — uploads now show bytes uploaded / total, percentage, ETA, filename, and a Cancel button instead of an opaque spinner. | proxied / no progress | **task #135 shipped** (2026-05-22) | **✓ ↑** (task #135) | `aidocs/45 §9` + `feedback_large_file_download` memory |
| **RO-Crate export delivery via presigned URL (FS1g — sync variant of aidocs/31 §O3)** — `POST /v2/collections/{appId}/export-url`; builds RO-Crate ZIP in-memory and returns a presigned S3 GET URL (30 min TTL) instead of proxying bytes through the JVM. Sync variant ships first; async (O2-gated full offload) deferred. `FileStorage` SPI grows `presignedExportUrl(String key, byte[] zipBytes, String fileName, Duration ttl) → Optional<URI>` default method; `S3FileStorage` implements it (uploads to `exports/<uuid>.zip` in-bucket, returns presigned GET URL). Non-S3 adapters (`Optional.empty()`) get 503. New IO type: `ExportUrlIO {downloadUrl, fileName, expiresAt}`. New REST resource: `CollectionExportUrlRest` under `de.dlr.shepard.v2.collection.resources`. Operator action: configure lifecycle rule on `exports/` prefix (recommended 24 h TTL so stale ZIPs auto-clean). | proxied through backend | **FS1g shipped** (2026-05-16) | **✓ ↑** (FS1g) | `aidocs/45` / `aidocs/31 §O3` / `aidocs/34 FS1g` |
| **RO-Crate streaming export on v2 (EXPORT-V2-STREAM)** — `GET /v2/collections/{appId}/export`; streams the RO-Crate ZIP directly to the caller via `StreamingOutput`. Works on all storage backends including GridFS (unlike FS1g which returns 503 on GridFS). New `CollectionStreamExportRest`; frontend `downloadRoCrate()` migrated from v1 `CollectionApi.exportCollection({collectionId})` to this v2 endpoint. Removes numeric-id dependency from the download path. 5 unit tests. | proxied through backend (v1) | **EXPORT-V2-STREAM shipped** (2026-06-18, fire-110) | **✓ ↑** (EXPORT-V2-STREAM) | `aidocs/16 EXPORT-V2-STREAM` + `aidocs/34 EXPORT-V2-STREAM` |
| **Regulatory Evidence Pack (REP) export (TPL14)** — `POST /v2/collections/{appId}/export/regulatory-evidence` builds a BagIt 1.0 (RFC 8493) ZIP with `data/ro-crate-metadata.json` (RO-Crate 1.1) + `data/PROV-O.jsonld` + `bagit.txt` + `bag-info.txt` + `manifest-sha256.txt` + `tagmanifest-sha256.txt`. Bags ≤ 1 MB returned inline (Base64). `GET /latest` stub reserved (returns 404 until TPL14b persistence ships). New Java package `de.dlr.shepard.v2.export.rep` (`RepExportIO`, `RoCrateBuilder`, `ProvOBuilder`, `BagItPacker`, `RepExportService`, `RepExportV2Rest`). Reuses `ProvJsonLdRenderer.renderProvO()` for PROV-O. 8 unit tests. UI button "Regulatory Evidence Pack" on Collection detail page. | none | **TPL14 shipped** (2026-05-26) | **✓ ↑** (TPL14) | `aidocs/16 TPL14` + `aidocs/34 TPL14` + `docs/reference/regulatory-evidence-pack.md` |
| **Distributed ledger anchoring for tamper evidence (TPL17)** — Design doc + REST skeleton returning 501. Admin endpoints under `/v2/admin/ledger/`: `POST /anchor`, `GET /anchor/{jobId}`, `GET /data-objects/{appId}/ledger-anchors`. SHA-256 of `prov:Activity` JSON-LD anchored on Bloxberg (Phase 2) or OpenTimestamps (Phase 3). `Activity.ledgerAnchor` nullable `String` property added (JSON blob: `{provider, digest, txHash, anchoredAt, receipt}`). Bean gated by `@LookupIfProperty("shepard.ledger.enabled=true")` — off by default. EN 9100 / EASA Part 21 tamper-evidence requirement; IME/AQE flagged CRITICAL. 9 unit tests. Design: `aidocs/integrations/111-tpl17-distributed-ledger-anchoring.md`. | none | **TPL17 designed** (2026-05-26) — design doc + skeleton; Bloxberg client (TPL17a) queued | **📐** (designed) | `aidocs/16 LDGR1/TPL17a/TPL17b` + `aidocs/34 TPL17` + `aidocs/integrations/111` |
| **Presign-vs-cache TTL invariant (P23)** — `PresignTtlValidator` (`@ApplicationScoped`) caps effective presigned-URL TTLs at the permissions cache TTL so a presigned URL cannot outlive the cached permission grant it was issued under. Config: `shepard.storage.presign.upload-ttl` (default PT15M), `.download-ttl` (PT5M), `.export-ttl` (PT30M); `shepard.permissions.cache.ttl` (default PT5M). Warns at startup when capping occurs. `effectiveUploadTtl()` / `effectiveDownloadTtl()` / `effectiveExportTtl()` injected into `FileContainerPresignedUrlRest` and `CollectionExportUrlRest` (hardcoded constants removed). 8 unit tests in `PresignTtlValidatorTest`. | n/a | **P23 shipped** (2026-05-17) | **✓ ↑** (P23) | `aidocs/34 P23` / `aidocs/16 P23` |
| **CC1b — "Referenced by" expansion panel wired with real DataObject data** — Three new `GET /v2/{file,timeseries,structured-data}-containers/{id}/linked-data-objects` endpoints return `List<DataObjectIO>` for DataObjects linked via references. DAOs execute `MATCH (do:DataObject)-[:has_reference]->()-[:has_payload]->(c:Container) WHERE c.appId=$appId AND … RETURN DISTINCT do` against each container label. Services gate on numeric-id lookup + Read-permission check. Frontend "Referenced by" panel on File, Timeseries, and StructuredData container detail pages replaced with a real `v-list` (spinner → empty state → DataObject links to `/collections/{collectionId}/dataObjects/{id}`). 6 new DAO unit tests. Raw fetch composables under `composables/containers/`. No `/shepard/api/` surface touched. | none (upstream has no "Referenced by" panel) | **CC1b shipped** (2026-05-17) — 3 new REST resources, 3 DAO methods, 3 service methods, 3 composables, 3 page updates, 6 unit tests | **✓ ↑** (CC1b) | `aidocs/16 CC1b` + `aidocs/34 CC1b` + `aidocs/ops/87-collection-container-duality.md §4.5` |
| **ROR1 — instance-level Research Organization Registry config** — `:InstanceRorConfig` Neo4j singleton (seeded blank on first start, idempotent). `GET/PATCH /v2/admin/instance/ror` (RFC 7396, `instance-admin` gated). `rorUrl` computed as `https://ror.org/<rorId>`. Frontend: `InstanceRorPane.vue` admin pane (`mdi-office-building`, `AdminFragments.INSTANCE_ROR`). `V42__Add_appId_constraint_InstanceRorConfig.cypher`. 6 unit tests. CLI parity: `shepard-admin instance ror status` + `shepard-admin instance ror set [--ror-id] [--org-name]` (`InstanceCommand` → `InstanceRorCommand` → `InstanceRorStatusCommand` / `InstanceRorSetCommand`; `InstanceRorConfig` IO; 11 new CLI tests; RFC 7396 empty-string-to-null clear semantics via `LinkedHashMap` patch body). | none | **ROR1 fully shipped** (admin-REST + UI 2026-05-17; CLI 2026-05-17) | **✓ ↑** (ROR1 complete) | `aidocs/16 ROR1` + `aidocs/34 ROR1` |
## 13b. CI / quality gates
| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| JaCoCo coverage report on `mvn verify` | configured but reads the wrong (Quarkus-side) exec file → reports ~0.5% | reads the real `target/jacoco.exec` → reports ~68% line / 66% branch | **✓ ↑** | `backend/pom.xml` jacoco-maven-plugin (post-fix) |
| JaCoCo `check` gate at 60% line / 60% branch (`haltOnFailure` in CI) | none | shipped; `-Djacoco.haltOnFailure=true` in `backend-ci.yml` | **✓ ↑** | `backend/pom.xml` + `.github/workflows/backend-ci.yml` |
| PR coverage comment + per-changed-file 70% gate | none | shipped via `madrapps/jacoco-report` | **✓ ↑** | `.github/workflows/backend-ci.yml` |
| SpotBugs + findsecbugs running on `mvn verify` (was `<reporting>` only — `aidocs/07` M12) | `<reporting>` only — never invoked | shipped at `Effort=Max`, `Threshold=High`, `failOnError` in CI | **✓ ↑** | `backend/pom.xml` + `.github/workflows/backend-ci.yml` |
| OWASP Dependency-Check (weekly + on pom changes; `failBuildOnCVSS=7`) | none | shipped via `.github/workflows/security.yml`; suppressions in `backend/dependency-check-suppressions.xml` with CVE+reason format | **✓ ↑** | `.github/workflows/security.yml` |
| Secret scanning (gitleaks weekly + on push) | none | shipped via `.github/workflows/security.yml` | **✓ ↑** | `.github/workflows/security.yml` |
| GitHub Pages site CI | none | shipped (separate workflow) | **✓ ↑** | `.github/workflows/pages.yml` |
| Container images published to GHCR (`ghcr.io/noheton/shepard-{backend,frontend}:{latest,sha-<7>,vX.Y.Z}`) | upstream publishes to gitlab.com | shipped via `.github/workflows/ci.yml` `build-images` job (gated on unit-tests + spotbugs) | **✓ ↑** | `.github/workflows/ci.yml` |
| **Demo auto-seeder** (`seeder` one-shot Docker Compose service in `docker-compose.override.yml`; Keycloak ROPC → bootstrap → API-key → `seed.py --regenerate`; `SHEPARD_BOOTSTRAP_TOKEN_PATH` shared via named volume; idempotent; `restart: "no"`) | none | shipped — CI2 commit | **✓ ↑** | `infrastructure/docker-compose.override.yml` + `examples/lumen-showcase/entrypoint.sh` |
| **Playwright e2e test suite** (`@playwright/test` 1.49; targeting production `https://shepard.nuclide.systems`; auth helper, collections CRUD, nav coverage, smoke API health) | none | shipped — CI2 commit | **✓ ↑** | `e2e/` |
| **API integration test suite** (pytest + httpx; 22 test cases across 8 modules; LUMEN seed oracle; auth via `SHEPARD_API_KEY` or Keycloak ROPC; daily drift-detection workflow `integration-tests.yml`; `make integration-test` local target) | none | shipped — `e2e/api/` | **✓ ↑** | `aidocs/ops/75` + `e2e/api/` |
| **k6 per-endpoint SLO suite** (named-endpoint `Trend` metrics for TimescaleDB range-scan, Neo4j provenance walk, `/v2/` data-objects, timeseries ingest batch; three executor shapes: steady 10 VUs × 2 min, ramp 0 → 50 VUs, spike 100 VU/s burst; `apikey` auth; Prometheus remote-write + optional Grafana Cloud output; nightly CI job `perf-endpoints.yml`; two new `recommend.py` rules for slow TS range scan and slow provenance walk) | none | PERF4a–c shipped (`k6-endpoints.js`, Grafana dashboard, perf README); PERF4d designed | **✓ ↑** | `aidocs/ops/77` |
| **DOC-STAGE2 — pre-commit hook + CI gate for doc-stage index drift** (`scripts/regenerate-doc-stage-index.py --check` extended to detect missing `stage:` fields; `.git-hooks/pre-commit` tracked chmod-755 hook + `scripts/install-hooks.sh` installer; `.github/workflows/doc-stage-check.yml` triggers on `aidocs/**` path changes; one untagged file fixed: `aidocs/data/46-gridfs-to-s3-migration-runbook.md`) | none | shipped | **✓ ↑** | `aidocs/16` DOC-STAGE2 + `aidocs/34` DOC-STAGE2 row |
| **CodeQL** SAST (Java + JS/TS, `security-extended` query set) | none | shipped via `.github/workflows/codeql.yml`; weekly + per-PR; SARIF → Code Scanning | **✓ ↑** | `.github/workflows/codeql.yml` |
| **Trivy** container scan on every published GHCR image (CRITICAL+HIGH, ignore-unfixed) | none | shipped in `build-images.yml`; SARIF → Code Scanning per-image | **✓ ↑** | `.github/workflows/build-images.yml` |
| **SBOM** (CycloneDX) per published image via `anchore/sbom-action`; uploaded as artifact + attached to GitHub releases | none | shipped in `build-images.yml` | **✓ ↑** | `.github/workflows/build-images.yml` |
| **Dependency-review** (PR-time licence + new-CVE check) banning GPL/AGPL/SSPL families with `.github/dependency-review-config.yml` allowlist | none | shipped in `security.yml` | **✓ ↑** | `.github/workflows/security.yml` |
| **`PluginManifest` SPI + drop-in JAR discovery** (`de.dlr.shepard.plugin.{PluginManifest,PluginRegistry,PluginContext,PluginEntry,PluginState}`; `backend/plugins/*.jar` walked at startup via `ServiceLoader` post-`MigrationsRunner.apply()`; per-plugin `shepard.plugins.<id>.enabled` runtime toggle; fail-soft per plugin (a throwing `onRegister` marks the plugin `FAILED` and continues); path-traversal hardening on the plugins dir; UH1a flipped to a real `shepard-plugin-unhide-${revision}.jar` as the first plugin under the new shape) | none | **PM1a shipped** — 15 new unit tests; backend's `build-helper-maven-plugin` source-stitching of `plugins/unhide/src/{main,test}` removed; the plugin module produces a real JAR with `provided` scope on backend (so the JAR doesn't bundle backend.jar); backend declares the plugin via a default-active `with-plugins` Maven profile (`-DnoPlugins` breaks the bootstrap circular reference); Quarkus's build-time CDI scanner indexes the plugin via `quarkus.index-dependency.shepard-plugin-unhide.*`; PluginRegistry's classpath-then-JAR duplicate check silently shadows the redundant `/deployments/plugins/` copy. PM1b2 (child-classloader CDI integration + semver enforcement + JAR signature verifier) + PM1c (persist runtime overrides) queued. | **✓ ↑** (PM1a, this commit) | `aidocs/47 §2.5` + `aidocs/63 ADR-0023` + `aidocs/16 PM1a` |
| **Plugin registry admin REST + CLI parity** (`GET /v2/admin/plugins` lists every discovered plugin including DISABLED + FAILED rows with id / version / shepardCompatibility / state / enabled / sourcePath / registeredAt / failureMessage; `PATCH /v2/admin/plugins/{id}` RFC 7396 merge-patch on the `enabled` toggle with RFC 7807 `plugin.not-found` 404 + `plugin.config.read-only-field` 400 envelopes; `shepard-admin plugins {list,enable,disable}` CLI verbs with shared `--output={human,json}` / `--url` / `--api-key`; `PluginEntry.registeredAt` timestamp added so the registry surfaces discovery order chronologically; PROV1a auto-captures each PATCH as an `:Activity` row with `targetKind=PluginEntry`) | none | **PM1b shipped** — 12 backend admin-REST unit tests + 10 CLI end-to-end tests; new resource under `de.dlr.shepard.v2.admin.plugins.PluginsAdminRest` with IO records under `…plugins.io.{PluginEntryIO,PluginPatchIO}` (`PluginListIO` deleted fire-298: APISIMP-PLUGINS-LIST-ENVELOPE — list response migrated to `PagedResponseIO<PluginEntryIO>`); CLI commands under `de.dlr.shepard.cli.commands.{PluginsCommand,PluginsListCommand,PluginsEnableCommand,PluginsDisableCommand}` plus IO mirrors `…cli.io.{PluginInfo,PluginList}`; runtime override stays in-memory only — PM1d persists. Pre-existing CLI compile-break (duplicate `delete(String)` in `ShepardHttpClient` left over from PM1a's PR merge) fixed in the same patch. **UI shipped:** `PluginsAdminPane.vue` — card-per-plugin, colour-coded state chip (ENABLED/DISABLED/FAILED/DEGRADED/DISCOVERED), enable/disable switch, failure message alert, licence + homepage + repo links, dependency chips, restart warning banner. | **✓ ↑** (PM1b, PM1b-UI) | `aidocs/63 ADR-0023` forward-work checklist + `aidocs/16 PM1b` |
| **`PluginManifest` SPI enrichment + plugin-dependency resolution** (six new default methods on `PluginManifest`: `title()` / `description()` / `homepageUrl()` / `repositoryUrl()` / `licence()` / `dependencies()`; new `PluginDependency` record + in-tree `VersionRange` parser supporting exact / `[lo,hi)` / `[lo,)` / `(,hi]` / wildcard syntax with semver-aware numeric compare; `PluginRegistry.validateAndOrderDependencies()` does Kahn's topological sort between discovery and lifecycle; failures fail-soft per plugin (`plugin.dependency.missing` / `plugin.dependency.version-mismatch` / `plugin.dependency.cycle`); registry now guarantees a plugin's `onRegister` runs after every declared dependency's `onRegister`; `GET /v2/admin/plugins` wire shape grows six fields all backwards-compatible additive; `shepard-admin plugins list` table grows three columns (TITLE 30c / LICENCE 12c / REPOSITORY 40c, truncate with `…`); `UnhidePluginManifest` filled in with title / description / homepage / repository / `Apache-2.0` licence; dependencies left empty for UH1a) | none | **PM1c shipped** — 8 `PluginManifestTest` (default-method semantics) + 12 `VersionRangeTest` (parsing + range arithmetic + semver-aware compare) + 12 `PluginRegistryDependencyTest` (linear chain registers in topo order C → B → A; two-node cycle A→B→A fails both; three-node cycle fails all three; missing dep fails dependent only; unsatisfied / satisfied / unparseable version constraint; pre-existing-failed entry isn't a dep target) + 2 new `PluginsAdminRestTest` (rich + bare manifest IO assertions) + 3 new + 3 helper-unit CLI `PluginsCommandsTest` (new columns rendered, truncation marker, bare manifest dashes, truncate helper unit tests). New core types: `de.dlr.shepard.plugin.{PluginDependency,VersionRange}`. New IO record: `de.dlr.shepard.v2.admin.plugins.io.PluginDependencyIO`. **No new dependencies pulled** — the version-range parser is hand-rolled in-tree to avoid `org.apache.maven:maven-artifact` (same posture as N1c2's ontology-bundle slug parser). | **✓ ↑** (PM1c, this commit) | `aidocs/63 ADR-0023` forward-work checklist + `aidocs/16 PM1c` |
| **CLI extensibility SPI + Unhide CLI migrates to `plugins/unhide/`** (`de.dlr.shepard.cli.plugin.AdminCliCommandProvider` SPI: plugin JARs ship their own `shepard-admin` Picocli subcommands via `META-INF/services/`; `CliPluginBootstrap` ServiceLoader-scans the same plugin directory the backend uses — `SHEPARD_PLUGINS_DIR` env or `shepard.plugins.dir` sys-prop, defaults to `/deployments/plugins` then `cli/plugins`; `ShepardAdmin.commandLine()` is the new public entry that runs the bootstrap; the seven `Unhide*Command` files + `UnhideAdminPaths` + `UnhideCommand` move from `cli/src/main/java/de/dlr/shepard/cli/commands/` to `plugins/unhide/src/main/java/de/dlr/shepard/plugins/unhide/cli/`; `UnhideConfig` + `HarvestKeyMinted` IO records move alongside; `UnhideCommandsTest` follows; CLI's `with-plugins` Maven profile copies `de.dlr.shepard.plugins:*` deps into `target/plugins/` at packaging time mirroring backend's profile shape; CLI's pom grows a `maven-jar-plugin` `test-jar` execution so plugin modules can pull the shared `StubBackend`/`CliRunner` test fixtures via `<type>test-jar</type>`; end-user UX byte-identical — `shepard-admin unhide …` works exactly as before; same verbs, same flags, same JSON shapes, same exit codes) | none | **PM1d shipped** — 5 new `AdminCliCommandProviderTest` cases on the JAR-walk path (synthetic plugin JAR built inline via the Java compiler API + fail-fast smoke through the bootstrap, bare-JAR-no-service-file skip, non-existent-dir warning, no-default-dir silent no-op, duplicate-subcommand-name warn-and-skip), 4 new `ShepardAdminBootstrapTest` cases on the `ShepardAdmin.commandLine()` wiring (core subcommands intact, idempotent across calls, --help, --version). 12 relocated `UnhideCommandsTest` cases run unmodified in their new location. Backend untouched — `cli/`-side + `plugins/`-side refactor only. | **✓ ↑** (PM1d, this commit) | `aidocs/63 ADR-0023` forward-work checklist + `aidocs/16 PM1d` |
| **Plugin runtime hardening: JAR signature verifier + semver enforcement + DEGRADED state** (new `de.dlr.shepard.plugin.JarSignatureVerifier` `@ApplicationScoped` bean — `KeyStore.getInstance("JKS"/"PKCS12")` for trust-anchor loading + `JarFile(verify=true)` for signature parsing; four `shepard.plugins.signing.*` deploy-time keys (`required=false` default, `truststore.path`, `truststore.password`, `trust-anchors` for future PEM support); when `required=true` an UNSIGNED or UNTRUSTED JAR's entry lands FAILED with `failureMessage=plugin.signature.unsigned` / `plugin.signature.untrusted`. `PluginRegistry.enforceCompatibility()` runs between discovery + dependency-resolution so compat failures pre-empt the dependent's `plugin.dependency.missing`; `shepard.plugins.compatibility.strict=true` default + operator-override valve — incompatible plugins land FAILED with `plugin.compatibility.failed: requires shepard <range>, running <version>` or `plugin.compatibility.unparseable: <details>`. New `PluginState.DEGRADED` lifecycle state + `PluginEntry.markDegraded(String)` hook (forward placeholder — PM1b3 detection of runtime-only JARs without build-time CDI indexing per `aidocs/69`). `VersionRange.parse` extended with operator-comma syntax (`>=5.2.0,<6`) every existing plugin manifest already uses — pre-PM1b2 the parser only accepted Maven-bracket syntax (`[5.2.0,6)`) which no plugin actually wrote, so the compat field's actual enforcement is unblocked by this slice. PM1b3 (true runtime CDI integration via Vert.x router) queued as the remaining slice with the full design rationale in `aidocs/69`.) | none | **PM1b2 shipped** — 9 `JarSignatureVerifierTest` (unsigned → UNSIGNED / self-signed-no-truststore → UNTRUSTED / signed-by-anchor → TRUSTED / tampered → JarSignatureException via JDK `JarFile` digest mismatch / empty-truststore → UNTRUSTED / default-not-required / invalid-truststore-path fail-soft WARN / `isSigningRequired()` / multi-anchor-any-match — all keys minted via `keytool` ProcessBuilder + signed via JDK's `jdk.security.jarsigner.JarSigner` API; no internal-class access so no `--add-exports` flags needed) + 11 `PluginRegistryCompatibilityTest` (compatible / too-old / too-new / empty constraint / null constraint / unparseable → FAILED / strict=false incompatible-registers / strict=false unparseable-registers / compat-precedes-dependency / multi-plugin only-incompatible-fails / already-failed-not-reprocessed) + 8 new `VersionRangeTest` operator-comma cases (>=5.2.0,<6 / single-clause ge / single-clause lt / =-operator / empty-clause IAE / malformed-clause IAE / semver-numeric compare 1.10>1.9 / bare-clause-as-exact) + 4 `PluginRegistryDegradedStateTest` (markDegraded state transition / list visibility / enum name() stability for wire shape / markEnabled clears DEGRADED failureMessage). New backend types: `de.dlr.shepard.plugin.{JarSignatureVerifier, JarSignatureException}`. New design doc: `aidocs/platform/69-runtime-plugin-cdi.md` parking the deferred PM1b3 child-classloader CDI integration with the Option A (Quarkus extension) vs Option B (Vert.x router) trade-off + the verified-against-Quarkus-3.27.x finding that Arc doesn't expose a runtime bean-addition API. `PluginEntryIO.state` OpenAPI descriptor extended to mention `DEGRADED`. SpotBugs clean; backend coverage gate met. | **✓ ↑** (PM1b2, this commit) | `aidocs/63 ADR-0023` forward-work checklist (JarSignatureVerifier + semver-range enforcement ticked; child-classloader CDI integration deferred to PM1b3) + `aidocs/16 PM1b2` + new `aidocs/69` design doc |
| **Plugin runtime override persistence** (`:PluginRuntimeOverride` Neo4j label — one row per plugin id, V32 uniqueness constraint mirroring V11/V22/V25/V27/V30; `PluginRuntimeOverride` `@NodeEntity` with `HasAppId` + `pluginId` + `enabled` + `updatedAt` + `updatedBy`; `PluginRuntimeOverrideDAO` for findByPluginId / findAllOverrides / save / deleteByPluginId; `PluginRegistry.seedOverridesFromDao()` runs at startup before lifecycle invocation; `PluginRegistry.setEnabled(id, enabled, actorSub)` writes through synchronously within the PATCH request; sparse-table semantics — reset-to-default DELETEs the row rather than upserts; `PluginsAdminRest.patch` takes `@Context SecurityContext` and propagates the caller's `sub` to the registry → `updatedBy` field; CLI success messages updated from "runtime override survives until restart" to "override persisted; survives restart"; the deploy-time `shepard.plugins.<id>.enabled` keys remain valid as the install default — runtime override row wins forever after, matching the A3b / N1c2 / UH1a "admin-configurable" idiom with the persistent-override variant) | none | **PM1e shipped** — 13 new `PluginRuntimeOverrideDAOTest` (Mockito-on-Session: entityType / findByPluginId hit+miss+blank / findAllOverrides empty+null+populated / save mints appId / save rejects null+blank / deleteByPluginId hit+miss+blank) + 11 new `PluginRegistryPersistenceTest` (startup-seed populates / empty-table no-op / idempotent / fail-soft on DAO exception / disable persists / reset-to-default deletes / unknown plugin throws / null actor → anonymous / write-failure preserves in-memory cache / simulated restart preserves disable / 2-arg setEnabled delegates) + 4 new `PluginsAdminRestTest` (caller-sub from SecurityContext / null principal / blank-name principal / 2-arg overload) + 3 new `PluginsCommandsTest` (CLI success message contract for enable + disable + two-PATCH simulated restart). New backend types: `de.dlr.shepard.plugin.{PluginRuntimeOverride, PluginRuntimeOverrideDAO}`. New migration: `V32__Add_pluginId_constraint_PluginRuntimeOverride.cypher` (V11-shape uniqueness constraint, idempotent + fail-fast). Fail-soft on DAO outage at startup (registry falls back to deploy-time defaults — same as pre-PM1e) and on PATCH write (logs WARN; in-memory cache is current; the override doesn't survive restart but operator can re-PATCH after DB recovers). PM1 series complete; PM1b2 (child-classloader CDI + JAR signature verifier + semver-compat enforcement) is the remaining queued slice. | **✓ ↑** (PM1e, this commit) | `aidocs/63 ADR-0023` forward-work checklist + `aidocs/16 PM1e` |
| **`shepard-plugin-v1-compat` (V1COMPAT.0 Phase 1: marker plugin)** — control plane for the upstream-frozen `/shepard/api/...` surface. Phase 1 moves **zero code**: the 29 v1 REST classes and IOs stay in core. What ships in the plugin module: `V1CompatPluginManifest`, `:LegacyV1Config` Neo4j singleton (`{enabled: boolean}` only, A3b pattern mirror of `:UnhideConfig` / `:SemanticConfig`), `LegacyV1ConfigDAO`, `LegacyV1ConfigService` (5-second in-process read-through cache + fail-open on DAO error), admin REST `/v2/admin/legacy/v1/{config,stats}` (GET + RFC 7396 PATCH for config; GET for in-memory stats counters), `LegacyV1GateFilter` (HTTP 410 + RFC 7807 `v1-disabled` body when `enabled=false`; runs before `JWTFilter` so 410 is returned to anonymous callers, not 401), `LegacyV1DeprecationFilter` (counter increment + first-hit WARN per `(path-pattern × principal)` per session + audit event on writes; emits `Deprecation: true` + `Link` per RFC 8594 + `X-Shepard-Legacy: true` fork marker). New core touch: NeoConnector explicit-package entry for `:LegacyV1Config` scan. Frontend: `useV1Deprecation` composable + `V1DeprecationBanner` (session-scoped, watches the `X-Shepard-Legacy` header via the `useV1DeprecationMiddleware` chained into `useShepardApi`) + `AdminLegacyV1Pane` (toggle + live stats + confirm-on-disable guard when recent traffic seen). Plugin docs: `plugins/v1-compat/docs/{install,quickstart,reference}.md`. End-user-facing doc: `docs/reference/v1-deprecation.md`. Default state: `shepard.plugins.v1-compat.enabled=true` + `:LegacyV1Config.enabled=true` — every byte of every `/shepard/api/...` response stays identical to upstream shepard 5.2.0 (plus three additive RFC-standard headers). Phase 2 (`aidocs/platform/103`) moves the v1 REST classes themselves into the JAR; Phase 1 is the operator-visible control plane shipped first. **Aidocs/68 superseded** in favour of the 103/103a Phase 1 + Phase 2 lineage. 63 backend unit tests (entity / DAO behaviour / service cache + seed + setEnabled / stats counters + dedup / both filters / both admin REST resources) + 12 Vitest tests (banner composable). | none (upstream has no v1 compat marker, no deprecation telemetry, no admin lever to flip the v1 surface off) | **V1COMPAT.0 Phase 1 shipped on worktree** — three commits on `worktree-agent-af66b19ed0129b625`: scaffold + singleton + migration; admin REST + filters + IOs + docs; UI banner + admin pane wiring. Integration tests against a real-Neo4j testcontainer + v5 byte-fidelity replay deferred (v5 fixture corpus lands with #128; this worktree base predates #128). | **✓ ↑** (V1COMPAT.0 Phase 1, 3-PR series on worktree) | `aidocs/platform/103a-v1-compat-marker-plugin.md` + `aidocs/platform/103-v1-compat-plugin-extraction.md` + `aidocs/34` V1COMPAT.0 row |
| **Version 6.0.0 + opt-in plugin default** — `backend/pom.xml` `<revision>` bumped from `1.0.0-SNAPSHOT` → `6.0.0-SNAPSHOT`; all four plugin `pom.xml` revisions + CLI `pom.xml` revision bumped to `6.0.0-SNAPSHOT`; all four `SHEPARD_COMPATIBILITY` declarations updated from `">=5.2.0,<6"` → `">=6.0.0-SNAPSHOT,<7"` (SNAPSHOT-inclusive lower bound so dev builds satisfy PM1b2's semver-range gate); `frontend/package.json` version bumped to `6.0.0`; `PluginRegistry.isEnabled()` + `readDeployTimeDefault()` default flipped from `Boolean.TRUE` → `Boolean.FALSE` (opt-in posture — "all classpath plugins are optional"); `application.properties` adds explicit `shepard.plugins.{unhide,kip,minter-local}.enabled=true` for the three bundled plugins that should be on by default (minter-datacite stays `false` until credentials are configured); 11 `PluginRegistryTest` + `PluginRegistryPersistenceTest` cases updated to reflect the new default semantics; new `snapshotInclusiveLowerBound_acceptsSnapshotAndRelease` `VersionRangeTest` case validates `>=6.0.0-SNAPSHOT,<7` acceptance. | none | **V6 shipped** — breaking for third-party plugin manifests declaring `">=5.2.0,<6"` (PM1b2 semver-range gate rejects them under `6.0.0-SNAPSHOT`; fix: change to `">=6.0.0-SNAPSHOT,<7"`; escape valve: `shepard.plugins.compatibility.strict=false`). The `/shepard/api/...` wire surface remains byte-compatible with upstream 5.2.0 per the API-version policy. | **✓ ↑** (V6, this commit) | `aidocs/34` V6 row |
| **`PayloadKind` SPI** (`de.dlr.shepard.spi.payload.PayloadKind` — `name()` + `entityPackages()`; ServiceLoader-discovered plain POJO; `NeoConnector.connect()` iterates impls to register OGM entity packages; `META-INF/services/de.dlr.shepard.spi.payload.PayloadKind` in each plugin JAR) | none | **SPI1a / PL1a shipped** (2026-05-16) | **✓ ↑** (SPI1a, this commit) | `aidocs/47 §2` + `aidocs/34 SPI1a` |
| **`shepard-plugin-spatiotemporal`** (SPATIAL-V6-001) — `shepard-plugin-spatial` renamed to `shepard-plugin-spatiotemporal`; plugin id `"spatial"` → `"spatiotemporal"`; `PayloadKind.name()` `"spatial"` → `"spatiotemporal"`; v6 green-field `shepard_spatial` schema added as `V2.0.0__green_field_schema.sql` (profile hypertable on TimescaleDB + PostGIS co-located, BRIN+GIST+GIN indexes, 7-day compression policy); separate `postgis` compose container retired (Decision D2, aidocs/data/90); custom `infrastructure/timescaledb-postgis/Dockerfile` (`apk add postgis` on Alpine). Java package structure and Quarkus datasource name `"spatial"` unchanged. 3 new test classes (SpatiotemporalPluginManifestTest, GeoTimeVocabularyProviderTest migrated, new SpatiotemporalPayloadKind class). | n/a — upstream has no plugin system | **SPATIAL-V6-001 shipped** (2026-05-27) | **✓ ↑** (SPATIAL-V6-001, this commit) | `aidocs/47 §3` + `aidocs/34 SPATIAL-V6-001` + `aidocs/data/90` |
| **`shepard-plugin-hdf5`** — extraction of HDF5/HSDS payload kind to drop-in plugin JAR. `HdfPayloadKind implements PayloadKind` registers `de.dlr.shepard.data.hdf.entities` OGM package (fixing latent OGM-gap bug); `HdfPluginManifest implements PluginManifest`. 9 production Java files + 10 unit tests moved to `plugins/hdf5/`; `HdfFeatureToggle` stays in backend. `shepard.plugins.hdf5.enabled=false` default (matches pre-extraction posture). | n/a | **PL1c shipped** (2026-05-16) | **✓ ↑** (PL1c, this commit) | `aidocs/47 §3` + `aidocs/34 PL1c` |
| **`shepard-plugin-git`** — extraction of G1 (Git reference) payload kind to drop-in plugin JAR. `GitPayloadKind implements PayloadKind` registers `de.dlr.shepard.context.references.git.entities` OGM package (fixing latent OGM-gap bug); `GitPluginManifest implements PluginManifest`. 20 production Java files + 9 unit tests moved to `plugins/git/`; `GitCredential` entity/DAO/service + migrations V19/V20/V26 stay in backend (auth perimeter). `shepard.plugins.git.enabled=true` default (mirrors pre-extraction posture). | n/a | **PL1d shipped** (2026-05-16) | **✓ ↑** (PL1d, this commit) | `aidocs/47 §3` + `aidocs/34 PL1d` |
| Codegen archetype `mvn shepard:scaffold-payload-kind` | none | TBD | 📐 (queued, DX3) | `aidocs/47 §2.5` |
| `make dev` single-command bootstrap (init wizard + compose up + smoke) | none | TBD | 📐 (queued, DX4) | `aidocs/47 §4.4` |
| Unified `ShepardTestStack` testcontainer resource | none | Phase 1 shipped — `backend/src/test/java/de/dlr/shepard/testing/ShepardTestStack.java`; pom.xml gains testcontainers deps (all BOM-managed). Neo4j, MongoDB, Postgres started in parallel. `@TestSecurity` from `quarkus-test-security`. 7/7 self-tests pass. Phase 2 (full `@QuarkusTest` + Flyway) is DX1b. | **🚧 ↑** (DX1 phase 1) | `aidocs/16` DX1 |
| `ShepardTestFixtures` shared typed builders (Collection / DataObject / User / Permissions / BasicReference) | none | shipped — `backend/src/test/java/de/dlr/shepard/testing/fixtures/ShepardTestFixtures.java`; pilot adopters `CollectionServiceTest` / `DataObjectServiceTest` / `BasicReferenceServiceTest` | **✓ ↑** (DX2) | `aidocs/16` DX2 |
| BI integrations — Grafana data-source plugin + Superset SQLAlchemy recipe | none | TBD; "SQL win" via P10 (C5 cleared) | 📐 (queued, DX8) | `aidocs/47 §4.8` + `aidocs/29` |
| External-service minimal-instance test infrastructure — real service containers (Fuseki/Databus, BaSyx AAS, Synapse/Matrix, Open3D sidecar) + WireMock fallbacks for non-self-hostable services (DataCite, ePIC, Unhide harvester); `ExternalServiceStack` Quarkus test-resource; `docker-compose.external-services.yml` for local dev | none | TBD | 📐 (queued, IX1a; IX1 prerequisite for all external-integration ITs) | `aidocs/16` IX1 |
| Selective export ✓ (see §5 above) | GET-only | **✓** (R2 series shipped) | **✓ ↑** | §5 |
| Streaming RO-Crate export for large Collections | possible OOM | TBD | 📐 (queued) | `aidocs/31` |
| Long-running export pattern (job-id polling) | synchronous only | TBD | 📐 (queued) | `aidocs/32` |
| Reproducible-by-snapshot exports | n/a (no snapshots) | `snapshotAppId` field on `ExportSelection`; `ExportService` filters DataObjects by snapshot manifest; 3 unit tests | **✓ shipped (V2d)** | `aidocs/41 §5` |
| Endpoint group | Internal change | User-visible? |
| `/shepard/api/.../search` | Cypher now parameter-bound (C5); identifiers whitelisted | No — request body unchanged |
| `/shepard/api/.../healthz` | Per-DB up/down state in body (A1b) | Additive fields only |
| `/shepard/api/timeseriesContainers/{id}/payload` | Now also accepts `application/x-ndjson` (P14) | Additive content-type |
| `/shepard/api/collections/{id}/export` (GET) | Unchanged | No |
| Endpoint | Status | Origin |
| `POST /v2/collections/{appId}/export` (body-form ExportSelection) | ✓ shipped | R2 / `aidocs/16` |
| `POST /v2/admin/bootstrap` (unauthenticated; consumes `/opt/shepard/.bootstrap-token`) | ✓ shipped | A0 / `aidocs/51 §5.2` |
| `GET /v2/admin/instance-admins` (list, source column: Neo4j) | ✓ shipped | A0 / `aidocs/51 §10` |
| `POST /v2/admin/instance-admins` (grant role) | ✓ shipped | A0 / `aidocs/51 §10` |
| `DELETE /v2/admin/instance-admins/{username}` (revoke role) | ✓ shipped | A0 / `aidocs/51 §10` |
| `GET /v2/admin/permission-audit` (list orphan-permissions entities) | ✓ shipped | A0 / `aidocs/51 §8` |
| `GET /v2/admin/runtime-toggles` (list runtime feature toggles; renamed from `/v2/admin/features` in fire-356) | ✓ shipped | A3b |
| `PATCH /v2/admin/runtime-toggles/{name}` (set toggle enabled/disabled; renamed from `/v2/admin/features/{name}` in fire-356) | ✓ shipped | A3b |
| (None of the L2c/L2d/L2e `/v2/...` URL forms have shipped yet — those land at L2d.) | | |

### 14a.3 New `/v2/...` endpoints — designed (queued)

| Endpoint(s) | Slice | Refs |
|---|---|---|
| `GET /v2/lab-journal/{appId}/render`, `/notebooks` | J1a/J1b | `aidocs/37` |
| `POST /v2/data-objects/{id}/git-references`, `GET /v2/git-references/{appId}{,/content}`, `PATCH/DELETE` | G1a/b/c/d | `aidocs/38` |
| `GET /v2/templates`, `GET /v2/templates/{appId}`, `POST/PATCH/DELETE`, `GET/PUT /v2/collections/{appId}/allowed-templates`, `POST /v2/collections/{appId}/data-objects/from-template/{templateAppId}`, `GET /v2/templates/export`, `POST /v2/templates/import` | T1a-T1f | `aidocs/39` |
| `POST /v2/processes/{appId}/runs`, `POST /v2/process-runs/{appId}/steps/{stepId}/complete`, `GET /v2/process-runs/{appId}` | PR1a/b | `aidocs/40 §2` |
| `POST /v2/collections/{appId}/snapshots`, `GET /v2/snapshots/{appId}{,/manifest}`, `GET /v2/collections/{appId}?snapshot=`, `POST /v2/collections/{appId}/export?snapshot=`, `GET /v2/snapshots/{a}/diff/{b}` | V2a-e | `aidocs/41` |
| `GET /v2/lab-journal/{appId}/render`/`/notebooks` (duplicate listing — same as J1) | | |
| `GET /users/me`, `PATCH /users/me`, `PUT /users/me/avatar`, `GET /users/{appId}`, `GET /users/{appId}/avatar` | U1a-U1e | `aidocs/36` |
| `GET /v2/templates/{...}/processes/...` (process runtime; subset of PR1) | | `aidocs/40 §2` |
| `POST /v2/timeseries/{appId}/detect-anomalies`, `GET /v2/data-objects/{appId}/similar`, `POST /v2/search/natural`, `POST /v2/lab-journal/assist`, `POST /v2/semantic-annotations/suggest`, `POST /v2/collections/{appId}/export?aiAssist=true`, the snap-dashboards tool-use catalogue | AI1a-AI1l | `aidocs/43` |
| ~~`GET /v2/file-containers/{containerAppId}/files/{originalName}/versions`~~ — **REMOVED 2026-06-10 (APISIMP-PV-UNIFY)** | PV1a — ✓ shipped (2026-05-20); ~~superseded 2026-06-10~~ | `aidocs/46` |
| ~~`GET /v2/structured-data-containers/{containerAppId}/files/{originalName}/versions`~~ — **REMOVED 2026-06-10 (APISIMP-PV-UNIFY)** | PV1b — ✓ shipped (2026-05-20); ~~superseded 2026-06-10~~ | `aidocs/46` |
| `GET /v2/containers/{appId}/files/{fileName}/versions` — unified payload-version endpoint; 415 for kinds without file-payload versioning (timeseries/hdf) | APISIMP-PV-UNIFY — ✓ shipped (2026-06-10) | `aidocs/16` APISIMP-PV-UNIFY |
| `GET /v2/admin/permission-audit/log` — response entries now carry `appId: string` (UUID) instead of `id: number` (BIGSERIAL); `V1.19.0` adds `app_id` UUID column with unique index | APISIMP-PERM-AUDIT-LOG-APPID — ✓ shipped (2026-06-12) | `aidocs/16` APISIMP-PERM-AUDIT-LOG-APPID |
| `GET /v2/containers/{appId}`, `GET /v2/containers?kind=…` — `id` (Neo4j numeric node-id) removed from response body; `appId` (UUID v7) is the sole wire identifier for containers. `BasicEntityV2IO` foundation class + `BasicContainerV2IO` introduced. | APISIMP-BASICENTITY-DROP-ID slice 1 — ✓ shipped (2026-06-12) | `aidocs/16` APISIMP-BASICENTITY-DROP-ID |
| `GET|POST|PATCH /v2/collections[/{appId}]`, `GET /v2/collections/{appId}/data-objects[/{appId}]` — `id` (Neo4j numeric node-id) removed from response body; `CollectionV2IO` sidecar class introduced; `DataObjectDetailV2IO`+`DataObjectListItemV2IO` annotated `@JsonIgnoreProperties({"id"})`. | APISIMP-BASICENTITY-DROP-ID-2 — ✓ shipped (2026-06-12) | `aidocs/16` APISIMP-BASICENTITY-DROP-ID-2 |
| `PATCH /v2/collections/{appId}/publication-state`, `PATCH /v2/containers/{appId}/publication-state`, `POST|PATCH /v2/templates`, `GET /v2/containers/{appId}/files/{name}/versions` — 4xx error body shape unified to RFC-7807 `application/problem+json`; `errors` list in extension field for validation errors. | APISIMP-ERROR-ENVELOPE-RESIDUALS — ✓ shipped (2026-06-12) | `aidocs/16` APISIMP-ERROR-ENVELOPE-RESIDUALS |
| `POST /v2/shapes/render`, `GET\|POST /v2/templates/export\|import`, `POST /v2/admin/aas/...` — 15 residual `Map.of` error bodies converted to `ProblemJson`; `renderer`, `code`, `line` extension members. | APISIMP-SHAPES-RENDER+TPL+AAS — ✓ shipped (2026-06-12) | `aidocs/16` APISIMP-SHAPES-RENDER-ERROR-ENVELOPE + APISIMP-TPL-PORTABILITY-ERROR-ENVELOPE + APISIMP-AAS-ADMIN-ERROR-ENVELOPE |
| `GET /v2/git-references/{appId}/preview` — hand-rolled `LinkedHashMap` RFC-7807 body replaced with shared `ProblemJson` (wire-identical). | APISIMP-GIT-REF-PROBLEM-TYPE — ✓ shipped (2026-06-12) | `aidocs/16` APISIMP-GIT-REF-PROBLEM-TYPE |
| `GET /v2/.well-known/kip/{suffix}` error responses — `KipResolverRest.problem()` helper migrated from raw `LinkedHashMap` to `ProblemJson`; wire-identical (same JSON fields, same `Content-Type: application/problem+json`); removes unused `Map`/`LinkedHashMap` imports. | APISIMP-KIP-PROBLEM-HELPER — ✓ shipped (2026-06-12) | `aidocs/16` APISIMP-KIP-PROBLEM-HELPER |
| `GET /v2/collections/{appId}/data-objects?fields=<bad>` — 400 response now RFC 7807 `ProblemJson` with `type` field and `application/problem+json` content-type (was near-miss `Map.of(title,detail,status)` missing `type`). | APISIMP-DO-V2-FIELDS-ERROR-ENVELOPE — ✓ shipped (2026-06-12) | `aidocs/16` APISIMP-DO-V2-FIELDS-ERROR-ENVELOPE |
| `GET\|POST /v2/aas/shells[/{aasId}[/submodels]]` AAS-disabled 501 response body — migrated from `Map.of("message","...")` to RFC-7807 `ProblemJson` with `type=/problems/aas.integration-disabled`, `title`, `status=501`, `detail`; adds `Content-Type: application/problem+json`. Wire break on disabled-501 only; success paths unchanged. | APISIMP-AAS-SHELLS-DISABLED-ENVELOPE — ✓ shipped (2026-06-12) | `aidocs/16` APISIMP-AAS-SHELLS-DISABLED-ENVELOPE |
| `POST /v2/admin/bootstrap` 201 response body — replaced untyped `Map.of("username","role")` with `BootstrapResponseIO` record; wire-identical JSON fields; OpenAPI schema `BootstrapResponse` now declared on the 201 `@APIResponse`. | APISIMP-BOOTSTRAP-SUCCESS-IO — ✓ shipped (2026-06-12) | `aidocs/16` APISIMP-BOOTSTRAP-SUCCESS-IO |
| `GET /v2/collections/{appId}` — response now includes `defaultFileContainerAppId` (UUID v7 string) alongside the existing `defaultFileContainerId` (numeric; deprecated, kept for v1 compat). `@JsonInclude(NON_NULL)` omits it on the v1 wire when null. `LabJournalEntry.vue` null-check migrated to `defaultFileContainerAppId`. | APISIMP-COLLECTIONIO-NUMERIC-FILECONTAINER — ✓ shipped (2026-06-14 fire-31) | `aidocs/16` APISIMP-COLLECTIONIO-NUMERIC-FILECONTAINER |
| `POST /v2/…/wiki-write` — response now includes `labJournalEntryAppId` (UUID v7) alongside deprecated numeric `labJournalEntryId`. `WikiWriterService` populates from `entry.getAppId()`. TS backend-client type updated. | APISIMP-WIKIWRITE-LJERID — 🔄 in-flight (fire-32) | `aidocs/16` APISIMP-WIKIWRITE-LJERID |
| `POST /v2/sql/timeseries` body `where.container_app_id_in: List<String>` (UUID v7) added; `container_id_in: List<Long>` deprecated (kept for backward compat; appId path takes precedence). `EntityIdResolver.resolveLong()` resolves appIds to Neo4j longs internally; unknown appIds silently excluded. 4 new permission unit tests in `SqlTimeseriesPermissionTest` + `StubEntityIdResolver`. | APISIMP-SQL-CONTAINER-ID-NUMERIC — 🚧 in-flight (fire-214, PR #2088) | `aidocs/16` APISIMP-SQL-CONTAINER-ID-NUMERIC; `aidocs/34` APISIMP-SQL-CONTAINER-ID-NUMERIC row |
| `PATCH /v2/collections/{cId}/data-objects/{dId}` body now accepts `predecessorAppIds: string[]` as a UUID v7 companion to `predecessorIds: long[]`. When non-null+non-empty, overrides `predecessorIds`. Response also populates `predecessorAppIds` alongside `predecessorIds`. Fixes PATCH on post-L2b instances where predecessors have no numeric `shepardId`. Unblocks `APISIMP-TYPED-PREDECESSOR-NUMERIC-ID`. | BUG-PREDECESSOR-IDS-NUMERIC-IN-V2-PATCH — ✓ shipped (2026-06-14 fire-37) | `aidocs/16` BUG-PREDECESSOR-IDS-NUMERIC-IN-V2-PATCH |
| `POST /v2/timeseries/{appId}/reingest`, `GET /v2/file-references/{appId}/versions{,/N}`, `POST /v2/file-references/{appId}/payload`, `DELETE /v2/file-references/{appId}/versions/N`, `GET /v2/collections/{appId}?snapshot=` (extension) | PV1c-f — queued | `aidocs/46` |
| `POST /v2/file-containers/{containerAppId}/upload-url`, `POST /v2/file-containers/{containerAppId}/upload-url/commit`, `GET /v2/file-containers/{containerAppId}/files/{oid}/download-url` | FS1c — ✓ shipped | `aidocs/45 §7` |
| `GET /v2/artifacts/{type}/{id}/url` | FS1g | `aidocs/45` |
| `GET /v2/admin/runtime-toggles`, `PATCH /v2/admin/runtime-toggles/{name}` | DX7 / A3b / `aidocs/22 §4.6` — ✓ shipped (renamed fire-356) | — |
| `GET /v2/processes`, `POST /v2/processes/import` | PR1a, PR1c | `aidocs/40 §2` |
| `POST /v2/hdf-containers`, `GET /v2/hdf-containers/{appId}{,/file,/datasets/{path}/value}`, `POST /v2/data-objects/{id}/hdf-references`, `POST /api-keys/{id}/hsds-token` | A5a-e | `aidocs/35` |
| `POST /v2/sql/timeseries` | P10a+P10b+P10c — ✓ shipped | `aidocs/29` |
| `GET /v2/admin/instance/ror`, `PATCH /v2/admin/instance/ror` | ROR1 — ✓ shipped | `aidocs/16 ROR1` |
| `GET /v2/notifications`, `GET /v2/notifications/count`, `PATCH /v2/notifications/{appId}/read`, `DELETE /v2/notifications/{appId}`, `POST /v2/admin/notifications/test` | NTF1a — ✓ shipped (2026-05-20) | — |
| `GET /v2/collections/{appId}/watches`, `GET /v2/collections/{appId}/watches/me`, `POST /v2/collections/{appId}/watches`, `DELETE /v2/collections/{appId}/watches/me` | CW1 — ✓ shipped (2026-05-20) — user watches Collection; in-app notification on new DataObject; bell icon + star-watch UI | — |
| `POST /v2/quality/independence-proof` | TPL11 — ✓ shipped (2026-05-26) — checks two DataObject sets for provenance-ancestor overlap (10-hop `has_successor` walk) and shared annotation key-value pairs; returns `independent` bool + `sharedAncestors[]` + `sharedAnnotations[]` + `checkedAt`; `@Authenticated`; no Neo4j migration; 6 unit tests; `IndependenceBadge.vue` frontend badge; `IndependenceProofApi.ts` client | `aidocs/34` TPL11 row |
| **DataObject `status` closed-enum enforcement (MFG5)** | none | **MFG5 ✓ shipped (2026-05-26)** — `StatusTransitionGuard` in `DataObjectService`: invalid status → 400; forbidden downgrade (`PUBLISHED → {DRAFT, IN_REVIEW}`, `ARCHIVED → *`) → 409. Service-layer only (all write paths go through the service). `AbstractDataObjectIO` already documents the enum via `@Schema(enumeration=...)`; the guard adds runtime enforcement. 11 unit tests. Closes the single largest gap identified in `aidocs/agent-findings/manufacturing-quality.md §11.2` for aerospace quality compliance. | `aidocs/34` MFG5 row + `aidocs/16` MFG5 row |
| **ARCHIVED publication state — Collection + Container** (#27-ARCHIVED) | none | **#27-ARCHIVED ✓ shipped (2026-05-31)** — extends the existing ARCHIVED status (MFG5, DataObject-only) to Collection and Container. Collection inherits `status` from `AbstractDataObject` (no code change needed). `BasicContainer` gains a new `status` String field that propagates to every subclass (`FileContainer`, `TimeseriesContainer`, `StructuredDataContainer`, `HdfContainer`). Two new owner-or-instance-admin REST paths: `PATCH /v2/collections/{appId}/publication-state` + `PATCH /v2/containers/{appId}/publication-state` (polymorphic across container kinds). `ArchiveStateGuard` service raises HTTP 409 on child writes when the parent is ARCHIVED — wired into `DataObjectService.createDataObject`/`updateDataObject` and into `AbstractContainerService.assertIsAllowedToEditContainer` (single seam — every container kind inherits the gate). NOOP migration `V97` + rollback twin (Neo4j is schema-less; field is additive nullable). Frontend: `CollectionArchiveControl.vue` + `ContainerArchiveControl.vue` — "Archived" chip + owner-gated Archive/Unarchive buttons with confirmation dialog; wired into `pages/collections/[collectionId]/index.vue`. Cascading rules deferred (`ARCHIVE-CASCADE-1` follow-up in `aidocs/16`). JUnit + Vitest cover guard logic, REST RBAC, and decision logic. | `aidocs/34` #27-ARCHIVED row + `aidocs/16` #27-ARCHIVED-01/02/03 + `#27-CONTAINER-STATUS-01` |
| **MFG1 + MFG2 — EN 9100 quality statuses + quality-engineer role guard + predecessor gate** | none | **MFG1+MFG2 ✓ shipped (2026-05-27)** — (MFG1) `StatusTransitionGuard` extended: 4 new quality statuses (`NCR_OPEN`, `ON_HOLD`, `REJECTED`, `CERTIFIED`) added to `VALID_STATUSES`; `QUALITY_RESTRICTED_STATUSES` set + `validateQualityRole()` method enforce `quality-engineer` Keycloak role on write (403 without role); `Constants.QUALITY_ENGINEER_ROLE`; wired into `createDataObject()` + `updateDataObject()`. Frontend: `EditDataObjectDialog.vue` dropdown + `StatusChip.vue` color mapping (NCR_OPEN/REJECTED = error, ON_HOLD = orange, CERTIFIED = success). `ImportValidationService.VALID_STATUSES` updated. (MFG2) `ManufacturingQualityGatesFeatureToggle` (default off, `shepard.features.manufacturing-quality-gates.enabled`); registered in `FeatureToggleRegistry` as `"manufacturing-quality-gates"`; gate in `createDataObject()` blocks successor creation when predecessor is `NCR_OPEN` or `ON_HOLD` (HTTP 409). 13 unit tests in `ManufacturingQualityTest`. | `aidocs/34` MFG1+MFG2 row + `aidocs/16` MFG1+MFG2 rows |
| **QM1a/b/c — NCR-as-first-class (GAP-3 from MFFD audit)** | none | **QM1a+b+c ✓ shipped (2026-06-02)** — (QM1a) `StatusTransitionGuard` adds `CONCESSION_PENDING` (EN 9100 §8.7 disposition window between `NCR_OPEN` and final closure); quality-engineer role-gated; `AbstractDataObjectIO` `@Schema(enumeration)` carries the full 10-value set. (QM1b) PROV1k `TypedPredecessorIO.ALLOWED_TYPES` adds `fair2r:concession`; new `PATCH /v2/collections/{cid}/data-objects/{did}/predecessors/{predAppId}` body `{relationshipType}` annotates an existing edge via `DataObjectService.setPredecessorRelationshipType()`. The four-value PROV1k vocab (`prov:wasInformedBy/wasRevisionOf` + `fair2r:repairs/concession`) covers the QM1b `normal/re-test/rework/concession` design with no parallel edge property. (QM1c) `V103__Disposition_record_template.cypher` seeds one `STRUCTURED_RECIPE` "Disposition record" with the EN 9100 §8.7 fields (`ncr_id / defect_type / disposition[use-as-is\|rework\|scrap\|concession] / approver_orcid / approver_username / decided_at / notes`); `TemplateBodyValidator` gains the `STRUCTURED_RECIPE` kind. Frontend: amber-outlined `CONCESSION_PENDING` chip + `mdi-shield-alert-outline`; amber `concession` predecessor-relationship chip; `EditDataObjectDialog` dropdown + `CollectionDataObjectsPanel` filter updated. Tests: `StatusChip.test.ts` (4) + `PredecessorRelationshipTypeChip.test.ts` (+1) + `StatusTransitionGuardTest` (+7) + `TypedPredecessorTest` (+1) + new `SetPredecessorRelationshipTypeTest` (7) + new `V103DispositionRecordTemplateTest` (6). Out of scope (queued): approval workflow `QM2`; cross-Collection NCR query `QM3`; per-status permissions `QM4`. | `aidocs/34` QM1a/b/c rows + `aidocs/16` QM1a/b/c rows |
| `GET /v2/collections/{appId}/events` | P13 — ✓ shipped (2026-05-26) — SSE change-feed replacing polling; emits `DATA_OBJECT_CREATED`, `DATA_OBJECT_UPDATED`, `DATA_OBJECT_DELETED`, `COLLECTION_UPDATED`, `HEARTBEAT` (30 s); `CollectionEventBus` (`@ApplicationScoped`, `ConcurrentHashMap`); hooks in `DataObjectService` + `CollectionService`; frontend `useCollectionEvents.ts` composable (`fetch()` + streaming reader; no native `EventSource` — cannot carry `Authorization` header); `CollectionSidebar.vue` wired to `refreshItems()` on structural changes; 5 unit tests; `ANNOTATION_CREATED` deferred | `aidocs/34` P13 row + `aidocs/16` P13 row |

| `PATCH /v2/user-groups/{appId}/permissions` — numeric `readerGroupIds`/`writerGroupIds` now blocked with HTTP 400; new UUID v7 `readerGroupAppIds`/`writerGroupAppIds` accepted. Mirrors the existing `ContainersV2Rest` block pattern (fire-403). | APISIMP-USERGROUP-NUMERIC-PERMS-BLOCK — ✓ shipped (fire-510) | `aidocs/16` APISIMP-USERGROUP-NUMERIC-PERMS-BLOCK; `aidocs/34` APISIMP-USERGROUP-NUMERIC-PERMS-BLOCK row |

This list is **maintained alongside the design docs that propose
each endpoint**; if you add a new design doc that introduces
`/v2/` paths, add a row here in the same PR.

### 14a.4 Convention reminder

- `/v2/<kind>-references/{appId}/...` per-payload-kind read/write
- `/v2/<kind>-containers/{appId}/...` per-container CRUD
- `/v2/admin/...` admin-role gated
- `/users/me` and `/users/{appId}` for profile (no `/v2` prefix —
  matches `aidocs/36 §6`'s decision to put profile at the top
  level for stability)
- `/v2/artifacts/{type}/{id}/url` for any non-payload-kind blob

Plugins (per `aidocs/47 §2.1`) get their own `/v2/<kind-name>-...`
namespace; core enforces the shape.

## 13c. Frontend UI features

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| **Inline timeseries chart** on container pages — Apache ECharts `LineChart`; channel select checkboxes; persisted curated channel view (`GET/PATCH /v2/containers/{id}/chart-view`; `TS_CHART_VIEW1` composable; unified path — formerly `/v2/timeseries-containers/{id}/chart-view`, collapsed by APISIMP-CONT-NS-COLLAPSE-3 2026-06-15); per-session "show all channels" override. `TimeseriesChart.vue` + `TimeseriesAllChannelsChart.vue` | none | **shipped** — `frontend/components/common/chart/TimeseriesChart.vue`, `frontend/components/container/timeseries/TimeseriesAllChannelsChart.vue`; `useTimeseriesContainerChartView` composable; step-line toggle for boolean channels; smooth toggle; `animationDuration` prop for live-mode transitions | **✓ ↑** (TS_CHART1, APISIMP-CONT-NS-COLLAPSE-3) | `frontend/components/common/chart/` + `frontend/composables/containers/useTimeseriesContainerChartView.ts` |
| **Live-mode auto-refresh** on inline timeseries chart — interval-based re-fetch of latest data points; smooth animation on data transition | none | **shipped** — configurable interval; `animationDuration` prop on `TimeseriesChart`; `smooth` prop on for live feeds | **✓ ↑** | `frontend/components/common/chart/TimeseriesChart.vue` |
| **Channel preview mini-chart** — per-row sparkline (last-N points, 1-second averages) rendered when a channel row is expanded on the timeseries container page | none | **shipped** — `frontend/components/container/timeseries/ChannelPreviewChart.vue`; `useFetchChannelPreview` composable calling `GET /shepard/api/timeseriesContainers/{id}/payload` with short window | **✓ ↑** (TS_PREVIEW1) | `frontend/components/container/timeseries/ChannelPreviewChart.vue` + `frontend/composables/container/useFetchChannelPreview.ts` |
| **Timeseries container storage stats chip** — point count shown as a coloured chip in the container header via `GET /v2/timeseries-containers/{containerId}/stats` | none | **shipped** — `useFetchTimeseriesContainerStats` composable; `frontend/pages/containers/timeseries/[containerId]/index.vue` renders chip | **✓ ↑** (TS_STATS1) | `frontend/composables/containers/useFetchTimeseriesContainerStats.ts` |
| **UI21-SIZEBAR-DATA — per-kind cardinality sizebar on /containers list** — container list "Scale" bar shows domain-meaningful counts (channel count for TS, file count for FC, entry count for SDC) instead of the CC1e referenced-by proxy. Backend: `GET /v2/file-containers/{id}/stats` → `{ fileCount }` (new `FileContainerStatsRest`; IO pre-existed); `GET /v2/structured-data-containers/{id}/stats` → `{ entryCount }` (new `StructuredDataContainerStatsRest` + `StructuredDataContainerStatsIO`); TS was already covered by TS_STATS1. Frontend: `useContainerCardinality` composable, `ContainerCardinalityCell.vue` lazy-per-row cell, `ContainerList.vue` sizebar wired to real cardinality. | none | **shipped** | **✓ ↑** (UI21-SIZEBAR-DATA) | `aidocs/16` UI21-SIZEBAR-DATA row |
| **uPlot chart replacement** (TS56) — replace Apache ECharts (`~500 KB`) with uPlot (`~45 KB`) across `TimeseriesChart.vue`, `TimeseriesAllChannelsChart.vue`, `ChannelPreviewChart.vue`; `Float64Array`-columnar `AlignedData` model with union-merge adapter for independent-timestamp series; `setData(data, false)` streaming append for live mode; rAF-gated updates; custom tooltip + legend; dark-mode CSS wiring; PNG export via `canvas.toBlob()`; TS56a benchmark harness; TS56d removes ECharts + vue-echarts from bundle | n/a (upstream has no charting layer) | design done | 📐 (TS56a–TS56d, backlog #56) | `aidocs/ux/76-uplot-timeseries-chart.md` |
| **Collection Lineage Graph** — interactive ECharts dagre-layered graph on the collection page showing parent/child and predecessor/successor edges between DataObjects; colour-coded by lifecycle status; drag, zoom, hover for details, click to open the DataObject detail page. **2026-06-02 — MFFD-scale rewrite (LINEAGE-GRAPH-MFFD-SCALE):** silent `NODE_CAP = 150` truncation removed (renders all DOs); three-tier Level-Of-Detail (macro ply / process-type bubbles at zoom < 0.3; meso nodes-only at 0.3 ≤ zoom < 0.8; detail labels at zoom ≥ 0.8); filter pill row (Status, Process-type via `urn:shepard:mffd:process-type` annotation, "Around DO N depth ≤ K" neighborhood BFS); minimap (second `vue-echarts` instance, roam off, toggleable). Engine kept on ECharts Canvas — the substrate that hits the 20k-node performance budget. Layout / clustering / filter / LOD machinery extracted to a pure-helper module so a 20k-node perf smoke can run without mounting Vuetify. | none | **shipped** — `frontend/components/context/collection/CollectionLineageGraph.vue`; **2026-06-02 rewrite** adds `frontend/utils/lineageLayout.ts` (pure helpers) + 32 Vitest cases (layout determinism, LOD threshold flip, filter composition, ply/process-type clustering with edge-weight roll-up, neighborhood BFS, 20k-node perf smoke). No new backend endpoints (still uses existing DataObject `parent`/`predecessors` fields). | **✓ ↑** (UI_LINEAGE1, LINEAGE-GRAPH-MFFD-SCALE) | `frontend/components/context/collection/CollectionLineageGraph.vue` + `frontend/utils/lineageLayout.ts` + `docs/help/collection-lineage.md` + `aidocs/16` LINEAGE-GRAPH-MFFD-SCALE row + `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md §GAP-12` |
| **DataObject Provenance Graph** — force-directed graph on the dataset detail page; dataset node at centre; predecessor/child dataset nodes (orange circles); agent nodes for users who acted on it (purple diamonds); edge labels for action kinds and counts; reads from `GET /v2/provenance/entity/{appId}` | none | **shipped** — `frontend/components/context/data-object/DataObjectProvGraph.vue`; reads `ProvenanceApi.getEntityActivities`; `useFetchAllDataObjects` for peer-node labels | **✓ ↑** (UI_PROV1) | `frontend/components/context/data-object/DataObjectProvGraph.vue` + `docs/help/provenance-tracing.md` |
| **Personal landing page** — authenticated users land on `PersonalDigest` instead of the static marketing page; shows a greeting card (avatar + name + collection count), a 6-card grid of the most-recently-updated accessible collections (sorted by `updatedAt` desc via `SearchApi`), and quick-action buttons (browse all / create new) | none | **shipped** — `frontend/components/context/home/PersonalDigest.vue`; `useFetchRecentCollections` composable; `frontend/pages/index.vue` routes to digest when `status === "authenticated"` | **✓ ↑** (#43) | `frontend/components/context/home/PersonalDigest.vue` + `frontend/composables/context/useFetchRecentCollections.ts` |
| **UX-PIN1 — Pinned channel tiles on PersonalDigest (shop-floor IME entry point)** — any timeseries channel can be pinned to the landing page; tile shows last value + trend arrow (↑/↓/→ based on 2% threshold) + 60-point LTTB sparkline (last 60s); pins persist in `localStorage` under `shepard:pinnedChannels` (UX-PIN1b defers server-side sync); pin/unpin from `TimeseriesMeasurementsTable` with a single click; pin button disabled (not broken) while `useFetchV2Channels` resolves the 5-tuple→`shepardId` map via `GET /v2/timeseries-containers/{id}/channels?size=2000`; `containerPath` stored per pin for navigation back to source; 10 Vitest unit tests covering deduplication, persistence, localStorage round-trip | none | **shipped** (4dd71495a, 2026-05-27) — `usePinnedChannels.ts` (module-scope singleton ref, same pattern as `useWatchedCollections`) + `useFetchV2Channels.ts` (5-tuple→shepardId resolver) + `PinnedChannelTile.vue` + `PersonalDigest.vue` pinned section + `TimeseriesMeasurementsTable.vue` pin-action column + `tests/unit/usePinnedChannels.test.ts` (10 tests) | **✓ ↑** (UX-PIN1) | `aidocs/16` UX-PIN1 · `frontend/composables/container/usePinnedChannels.ts` · `frontend/components/container/timeseries/PinnedChannelTile.vue` |
| **401 auto-refresh + session-expiry warning + chunk-error guard** — API client middleware intercepts 401, calls `refresh()`, retries with new token, redirects to sign-in only on hard failure; `SessionExpiryWarning` snackbar fires 5 min before JWT exp; `chunk-error-recovery` plugin catches post-deploy `ChunkLoadError` outside navigation path and reloads once per session | none | **shipped** — `useAuthRefreshMiddleware.ts` wired into `useShepardApi` + `useV2ShepardApi`; `SessionExpiryWarning.vue` mounted in `DefaultLayout`; `chunk-error-recovery.client.ts` plugin | **✓ ↑** (#45 #49) | `frontend/composables/common/api/useAuthRefreshMiddleware.ts` + `frontend/components/layout/SessionExpiryWarning.vue` + `frontend/plugins/chunk-error-recovery.client.ts` |

## 13d. Deployment shapes

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| **K8s / Helm deployment** — Helm umbrella chart at `deploy/helm/shepard/` siblings `infrastructure/docker-compose.yml`. Umbrella + six sub-chart skeletons (Neo4j / Postgres+TimescaleDB / Mongo / Garage / Keycloak / PgBouncer); `values.yaml` covers image overrides, OIDC split-horizon front/back-channel issuers, external-S3 override, Ingress with path-mount support for plugin sidecars per CLAUDE.md, `migrations.mode = disabled\|init-container\|job`, per-plugin toggles, observability ServiceMonitor knob, per-substrate persistence sizing. Phase 1 ships skeleton only — production-grade templates land in Phase 2; operator runbook in Phase 3; PM1f-driven plugin sidecar value emission in Phase 4; CI gate in Phase 5; ghcr.io OCI + GitHub Pages Helm repo in Phase 6. | docker-compose only | **📐 designed** — HELM-K8S-DEPLOY-01 Phase 1 chart skeleton shipped; HELM-K8S-DEPLOY-02..07 queued | 📐 (HELM-K8S-DEPLOY-01 Phase 1 shipped 2026-05-29; Phase 2+ queued) | `aidocs/ops/88-helm-deployment.md` · `aidocs/16` §HELM-K8S-DEPLOY · `deploy/helm/shepard/` |
| **docker-compose deployment** (single-node default; dev / showcase / institutional pilot) | shipped | shipped — extended with plugin compose-profile overlays and operator upgrade wizard (`scripts/upgrade-from-v5.sh`) | **=** + **✓ ↑** | `infrastructure/docker-compose.yml` · `aidocs/34` #75 row |

## 15. API versioning policy

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| `/shepard/api/...` byte-frozen with upstream | n/a (it IS upstream) | enforced via `CLAUDE.md` standing rule | **✓** | `CLAUDE.md` / `aidocs/34` |
| `/v2/...` reserved for this fork's additive surface | n/a | enforced; all new endpoints in design docs go here | **✓** (rule) / 📐 (endpoints follow as designed) | `CLAUDE.md` |
| Generated clients split (5.x compat tag vs 6.x next tag) | single track | TBD | 📐 (planned, `aidocs/40 §4`) | `aidocs/40 §4` |

## 16. Documentation

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| GitHub Pages docs site (Jekyll under `docs/`) | none | shipped at https://noheton.github.io/shepard/ | **✓ ↑** | `docs/` |
| Live researcher-facing vision doc | none | shipped (`aidocs/42-vision.md`, Live status) | **✓ ↑** | `aidocs/42` |
| Live ecosystem doc (SPW + sTC + others) | none | shipped (`aidocs/workflows/40-spw-stc-integration-design.md`) | **✓ ↑** | `aidocs/40` |
| Upstream upgrade-path tracker (admin-facing) | n/a | shipped (`aidocs/34-upstream-upgrade-path.md`, Live) | **✓ ↑** | `aidocs/34` |
| **This** fork-vs-upstream feature matrix (contributor-facing) | n/a | this doc | **✓ ↑** | this doc |
| LUMEN-inspired showcase seed + analysis notebook (expanded to 25 channels × 15 runs; 2 hold days; 4 new signal profiles: valve, tank, gimbal, combustion) | none | shipped (PR #1001; expanded in CI2 commit — `N_RUNS=15`, 25 channels, `examples/lumen-showcase/data/generate.py`) | **✓ ↑** | PR #1001 + CI2 |
| Upstream-current parallel import script (`import_upstream.py`) for the same showcase data | n/a (the upstream itself) | shipped | **✓ ↑** | PR #1001 |
| **In-app user docs** — Nuxt `/help` route serving `docs/*.md` from the same source as the Pages site | none | Shipped: `frontend/pages/help.vue` + `HelpFrame.vue` + `HelpSidebar.vue` + `helpMarkdown.ts`; `marked` + `highlight.js` renderer; Jekyll frontmatter/Liquid pre-pass; two-column sidebar layout with mobile drawer. "Help" link added to top nav. | **✓ ↑** (D1a) | `aidocs/49` |
| **Playwright screenshot pipeline** capturing against a CI-booted compose stack, committing PNGs to `docs/assets/screenshots/` | none | TBD; closes 9-month-old screenshot-placeholder backlog | 📐 (queued, D1b) | `aidocs/49 §3` |
| Task-shaped help pages for casual users | none | Partial: `upload-data`, `create-from-template`, `publish-data-object`, `publish-to-helmholtz-unhide`, `monitor-collection-activity`, `timeseries-plotting`, `collection-lineage`, `provenance-tracing`, `annotate-container`, `delete-container-with-references`, `minter-epic-quickstart` shipped under `docs/help/` and registered in `helpMarkdown.ts` | **🚧 ↑** (D1c partial) | `aidocs/49 §2.2` + `docs/help/` |
| Version-stamped in-app docs ("Help for shepard X.Y") | n/a | Shipped: `NUXT_PUBLIC_SHEPARD_VERSION` runtimeConfig key + help footer "Help for shepard v{{ version }}" in `HelpFrame.vue`. Default matches `backend/pom.xml` `<revision>` (`6.0.0-SNAPSHOT`). | **✓ ↑** (D1d) | `aidocs/49 §2.3` |
| **Operator runbook library** — 10 numbered single-page copy-paste runbooks at `docs/admin/runbooks/01-10-*.md` + README index | none | Shipped 2026-05-26: cube hotpatch (01), orphan payload wipe (02), API key rotation (03), Neo4j restore (04), TimescaleDB restore (05), Garage S3 restore (06), add instance-admin (07), enable plugin (08), permission repair (09), cut a release (10). Plus 3 pre-existing (`migration-chain-integrity`, `restore-tsdb-container-neo4j-shadow`, `docker-bind-mount-inode-drift`). | **✓ ↑** | ADMIN-RUNBOOKS-LIBRARY (`aidocs/16`) |

## 16a. Experiment orchestration

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| `shepard-experiment-coordinator` service driving manufacturing experiments end-to-end (PLC / SPS / KUKA / OPC/UA / KUKA RSI) | none | TBD | 📐 (queued, EXP1a) | `aidocs/50` |
| Three timing strategies — pre-seed (eager) / JIT (lazy default) / post-process (staged) | n/a | TBD | 📐 (queued, EXP1d/EXP1e + JIT in EXP1a) | `aidocs/50 §2 / §5` |
| Checkpoint + restart-whole + restart-at-step on top of V2 snapshots | n/a | TBD | 📐 (queued, EXP1f + EXP1g) | `aidocs/50 §6` |
| KUKA RSI telemetry routing into shepard TimeseriesReferences | n/a | TBD | 📐 (queued, EXP1i) | `aidocs/50 §4.2` |
| Operator UI for live experiment monitoring + restart controls | none | TBD | 📐 (queued, EXP1j) | `aidocs/50 §3.3` |
| Recipe storage as `templateKind = "EXPERIMENT_RECIPE"` in `__templates` (T1) | n/a | TBD | 📐 (queued, EXP1k) | `aidocs/50 §3.2` |
| **TemplateKind extension: `PROCESS_RECIPE` + `VIEW_RECIPE`** — TPL2 surface; `PROCESS_RECIPE` for manufacturing/process-chain blueprints; `VIEW_RECIPE` for shape-driven projections consumed by `POST /v2/shapes/render`. First concrete `VIEW_RECIPE` consumer: Trace3D (X/Y/Z + scalar → color-mapped 3D path). **TPL2a shipped**: allow-list extension in `TemplateBodyValidator` + meta-shape at `backend/src/main/resources/shapes/view-recipe-meta.shacl.ttl` (defines `shepard-ui:ViewRecipe`, `shepard-ui:ChannelBinding` with role + selector + qudt:unit + required). **TPL2b shipped**: `POST /v2/shapes/render` — stateless VIEW_RECIPE projection endpoint; takes `{templateAppId, focusShepardId}`; returns `{renderer, channelBindings[{role, channelSelector, unit, required, status="DECLARED", resolved=null}]}`; 422 for non-VIEW_RECIPE kinds; `shapes-render` placeholder registered in frontend placeholderRegistry. Live channel resolution (TPL2c) deferred until TS-ID migration (`aidocs/platform/87`) stabilises single-key channel identity. **M1 dispatch shipped**: `frontend/pages/shapes/render.vue` now branches on `renderer` hint: `"tresjs"|"trace-3d"` → `<Trace3DView>` (flat-array adapter + colour-bar legend); `"table"` → inline `<v-table>`; unknown → `<v-alert>` with unsupported-renderer note. MFFD demonstrator: `examples/mffd-showcase/shapes.py` seeds LBR iiwa force-torque VIEW_RECIPE (force_x/y/z_N + torque_z_Nm → Trace3D). M1 tracker: `aidocs/platform/83-tpl1-tpl2-shapes-templates-views.md`. | n/a | **✓ shipped (TPL2a + TPL2b + M1 dispatch)** — TPL2c queued | `aidocs/semantics/98 §1.1–§1.2` + `aidocs/platform/83` |
| **VIS-S1 — `ViewRecipeRenderer` SPI dispatcher** — cross-cutting in-tree platform piece for the VIS-* plugin family (vis-trace3d, vis-cad, vis-fem, vis-volume). New SPI under `de.dlr.shepard.spi.view`: `ViewRecipeRenderer` interface + POJO IO records (`RenderRequest` / `RenderResponse` / `RenderException`) + `ViewRecipeRendererRegistry` (`@ApplicationScoped`, ServiceLoader-loaded at startup). The existing `POST /v2/shapes/render` endpoint now reads the requested VIEW_RECIPE template body's `viewRecipeShape` IRI and dispatches to the renderer that claims that shape IRI; absent a claim, falls back to the in-tree TPL2b `DECLARED`-status projection — preserving the wire shape for clients whose plugin isn't installed. Pattern mirrors the `PayloadKind` P-series SPI (ServiceLoader, plain-POJO impls). Fail-fast at startup on duplicate shape IRI across two renderers (build-time packaging defect, refuses to start with a clear `IllegalStateException` naming both registrants). Renderer-thrown `RenderException(code, msg)` → HTTP 422 with RFC 7807 body; unexpected `RuntimeException` → HTTP 500. Three-audience docs: in-tree `docs/reference/view-recipe-spi.md` + `docs/help/plugin-renderer-not-loaded.md`. **Unblocks VIS-T1 sprint-2** (the TraceFrameResolver implementing `ViewRecipeRenderer` for `trace3d:Trace3DViewShape`) + the VIS-C1 / VIS-F1 / VIS-V1 plugin shipment lane. 32 unit tests (14 registry + 18 dispatcher). | n/a | **✓ shipped (VIS-S1)** — VIS-T1 sprint-2 / VIS-C1 / VIS-F1 / VIS-V1 queued (plugin impls in follow-ups) | `aidocs/34` VIS-S1 row · `aidocs/16` VIS-S1 row · `aidocs/semantics/98 §1.2` · `aidocs/agent-findings/trace3d-spike.md §3` |
| **V2CONV-B3 — `MAPPING_RECIPE` kind + `TransformExecutor` SPI + materialization path** — the transform-direction sibling of VIS-S1, and the generic enabler that later dissolves the scene-graph + KRL namespaces (V2CONV-B4/B5). New `templateKind` value `MAPPING_RECIPE` (a recipe binds existing input reference appIds → a derived output). New SPI under `de.dlr.shepard.spi.transform`: `TransformExecutor` interface + POJO records (`TransformRequest` / `TransformResult` (REFERENCE\|VIEW) / `TransformException`) + `TransformExecutorRegistry` (`@ApplicationScoped`, ServiceLoader at startup, fail-soft `resolve(iri)` → `Optional`, fail-fast on duplicate IRI) — mirrors the `ViewRecipeRenderer` SPI exactly. New endpoint `POST /v2/mappings/{templateAppId}/materialize` (dedicated `/v2/mappings` resource — distinct "materialize" verb from "render", same shape-IRI → executor dispatch): 404 template/executor absent, 422 non-MAPPING_RECIPE / no `mappingRecipeShape`, executor `TransformException` codes → 404/422, unexpected → 500. Built-in `NoOpTransformExecutor` identity default (claims `…/transform#IdentityTransformShape`, echoes first input reference appId) so the path works with zero plugins. Provenance: typed `EXECUTE` `:Activity` fire-and-forget + skip-capture handoff. Frontend stub `pages/tools/materialize-mapping.vue` + `composables/useMaterializeMapping.ts`. **Scene-graph (B4) + KRL (B5) executors NOT built here** — this is the machinery only. 32 JUnit + 6 Vitest. | n/a | **✓ shipped (V2CONV-B3)** — B4/B5 consumers queued | `aidocs/34` V2CONV-B3 row · `aidocs/16` V2CONV-B3 row · `aidocs/platform/191 §4` |
| **TPL3a-lite — upper-ontology alignment registry** — 12 `(:OntologyAlignment)` nodes seeded by V70 migration mapping Shepard concepts to upper-ontology classes (BFO 2020, IAO, PROV-O, IOF Core). New entity `OntologyAlignment`, DAO, `OntologyAlignmentIO`, and `GET /v2/semantic/ontology/alignment` read-only admin endpoint. Frontend: admin panel tile `#ontology-alignment` with `PlaceholderRestDump`. Alignment rows: Resource/Collection/DataObject/SemanticAnnotation/FileBundle/Container/Activity(×2)/Agent/User/Shape/Template. Full TTL-loaded axiomatic layer (TPL3b) queued. 5 unit tests. Authority: `aidocs/semantics/96`. **UI is placeholder** — `/admin#ontology-alignment` is `PlaceholderFragmentPane` + `PlaceholderRestDump` (raw JSON only, no human-readable rendering, no diff). Real pane tracked under `aidocs/16 PLACEHOLDER-REPLACE-TPL3a-lite`. **APISIMP-ONTOLOGY-ALIGNMENT-FAKE-PAGED (fire-363):** `GET /v2/semantic/ontology/alignment` response format changed from `PagedResponseIO` envelope (`{items,total,page,pageSize}`) to plain JSON array — callers must treat the body as a direct array. | n/a | **🅰️ ↑** (TPL3a-lite — BE ✓, UI is RestDump placeholder) | `aidocs/34` TPL3a-lite row · `aidocs/semantics/97` · `aidocs/16` PLACEHOLDER-REPLACE-TPL3a-lite · `aidocs/agent-findings/ui-scrutinizer-2026-05-30.md` |
| **TPL4 — attributes → annotations dual-write** — backward-compat migration bridge between the legacy `attributes` freetext map on DataObject and the SHACL/ontology annotation layer. Ships two things: (a) `AttributeAnnotationDualWriteService` wired into `DataObjectService.create/update` — synthetic `:SemanticAnnotation` nodes with `propertyIRI="urn:shepard:attribute:<key>"`, `valueName=<value>`, `source="attributes-backfill"` — gated on deploy-time toggle `shepard.tpl4.dual-write.enabled=false`; (b) `V69` Cypher one-shot backfill for pre-existing DataObjects + rollback `V69_R__...cypher`. New `source` field on `:SemanticAnnotation` (additive, nullable, schema-free). 6 unit tests (`AttributeAnnotationDualWriteServiceTest`). Constants: `ANNOTATION_SOURCE_ATTRIBUTES_BACKFILL` + `TPL4_ATTRIBUTE_PREDICATE_PREFIX` (value `"urn:shepard:attribute:"`) in `Constants.java`. | n/a | **✓ shipped (TPL4)** | `aidocs/34` TPL4 row |
| **TPL5 — git-based ontology ingestion** — admin-configurable `:OntologyGitSource` entities that allow Shepard to pull `.ttl`/`.owl`/`.rdf` files from any git repository (HTTPS or SSH) and ingest them as `UserOntologyBundle` entries into the internal n10s store. Ships: `OntologyGitSource` Neo4j entity + `OntologyGitSourceDAO` + `OntologyGitIngestService` (`ProcessBuilder`-based shallow clone, URL + branch security validation, glob path-pattern file matching, bundle-id namespacing `git-<slug>-<stem>`, delete-and-re-upload for idempotent re-ingest, temp dir cleanup). Nightly scheduler at 02:00 (`cron="0 0 2 * * ?"`), feature-gated via `shepard.tpl5.git-ingest.enabled=false`. Four admin REST endpoints under `/v2/admin/semantic/git-sources`: `GET` list, `POST` create, `DELETE /{appId}`, `POST /{appId}/ingest` (on-demand trigger). V67 migration (`:OntologyGitSource` appId uniqueness constraint). 33 unit tests covering URL/branch security validation, git command construction, path-pattern matching at root + subdirectory depth, bundle-id derivation, error handling (clone failure, oversize file, invalid URL/branch/pattern, temp dir cleanup). | n/a | **✓ shipped (TPL5)** | `aidocs/65 §5` "External-repo connectors" now addressed |
| **SHACL-PR5 — `predicate_vocabulary` substrate-routing table + `GET /v2/shapes/predicates`** (PR-5 enabler, task #156) — `predicate_vocabulary` Postgres table mapping 51 `shepard:` predicate URIs to substrate (`neo4j` 41 / `postgres` 8 / `garage` 2); `CHECK` constraints on substrate + cardinality; `PredicateVocabularyRepository` (JDBC, `@ApplicationScoped`); `GET /v2/shapes/predicates?substrate=` (filtered or full vocabulary); 13 unit tests. This is the routing table that lets the SHACL shape write path (PR-5) send each predicate to the correct store — the prerequisite for a correct write path. SHACL write-path wiring (PR-5 proper) deferred until substrate split is complete. | none | **✓ shipped** — Postgres migration V1.16.0 + REST endpoint + JDBC repository + tests | `aidocs/semantics/98 §1.3` + `aidocs/34` SHACL-PR5 row |

## 17. Companion ecosystem

| Tool | Upstream version | This fork status | Notes |
|---|---|---|---|
| `shepard-process-wizard` (desktop JavaFX) | upstream-only | unchanged compat (frozen API); future absorption into shepard core via PR1 designed | `aidocs/40 §2` |
| `shepard-timeseries-collector` (Java OPC/UA + MQTT + KUKA RSI) | upstream-only | 10 prioritised improvements documented; some need shepard-side dependencies (P14 ✓ shipped, A1b ✓ shipped, L2c queued) | `aidocs/40 §3` |
| Generated clients (`python` / `typescript` / `java`) | upstream OpenAPI | unchanged for `/shepard/api/`; `/v2/` will need a parallel client crank when L2d lands | `aidocs/40 §4` |
| **`shepard` Python convenience client** (P16 Phase 1) — `clients/python/shepard/`; collapses the 14-line prelude to 3 lines; `Client` + 13 domain proxy attributes (`collections`, `timeseries`, `files`, …) forwarding all generated `*Api` methods; `ShepardError` hierarchy (400/401/403/404/409/422/5xx); `iter_pages` + `.iter()`/`.list()` pagination wrappers; `to_pandas` / `to_excel` / `ro_crate` workflow helpers (pandas/openpyxl optional extras); 65 unit tests; no live server required | none | **✓ ↑ (Phase 1 shipped)** — Phase 3 (TypeScript) + Phase 4 (P10 retarget) queued | `aidocs/ops/27` / `aidocs/16` P16 |
| `shepard-frontend` | upstream-only | `aidocs/33` analysis covers UX improvements; W11–W2 design ranked | `aidocs/33` |
| `shepard-dataship` (publication pipeline) | upstream-only | parked under `aidocs/16` X1 | `aidocs/16` X1 |
| ~~**`shepard-plugin-mcp`** — Python FastMCP sidecar~~ *(retired 2026-05-21)* | none | **✕ retired** — replaced by the native Quarkus MCP server (next row). Source tree, compose service, Caddy route, and SPI manifest stub all removed. | n/a | `aidocs/34` MCP-1 (sunset note) |
| **Native JVM MCP server** — `quarkus-mcp-server-http:1.9.1` running in the backend JVM at `/v2/mcp/sse`; Phase 1 8-tool surface (`list_collections` / `list_data_objects` / `get_data_object` / `list_channels` / `get_channel_data` with LTTB / `list_files` / `list_structured_data` / `list_annotations`); no HTTP hop / no token relay (tool methods call the service layer directly); auth via `JwtTokenAuthService` (shared with `JWTFilter`) + `McpAuthFilter` (Vert.x `@Observes Filters`, priority 100) + `McpContextBridge` (per-invocation CDI scope principal copy); `DataObjectDAO.findByShepardIdAtDepth` for the get_data_object container detail; user-facing endpoint discovery at `/me#mcp`; coexists with Python sidecar on a different path (`/v2/mcp/sse` vs `/mcp/sse`). **API1 fix (2026-05-26):** `get_data_object` response now includes `timeseriesReferenceAppIds` / `fileReferenceAppIds` / `structuredDataReferenceAppIds` typed arrays (UUID v7 per reference node kind) alongside the existing `containers.timeseries[].containerAppId` path; `get_data_object` tool description updated with explicit warning that containerAppId ≠ referenceAppId. Eliminates the MCP-agent 404 class caused by passing reference-node IDs to `list_channels`. **MCP-COV-03/04/05/11 expansion (2026-05-30):** seven new tools — `ts_describe` + `ts_query_multi` (TS-OPT2 bulk wrapper, no 5-tuple in agent contract), `file_upload` + `file_content` (singleton FR1b round-trip, 10 MiB base64 cap), `semantic_annotate_bulk` (N-rows-per-roundtrip sequential), `get_predecessor_chain` + `get_successor_chain` (transitive lineage walk wrapping existing DAO + REST endpoints). 39 new unit tests. **MCP-COV-06/10/12 expansion (2026-05-31):** fourteen further tools across four new catalogues — `LabJournalMcpTools.labJournal{List,Create,Update,Delete}` (per-DataObject lab journal CRUD over `LabJournalEntryService`, entity-faithful single `content` field), `VersionMcpTools.version{List,Get}` (Collection-scoped versions over the OGM `uid`), `WatchMcpTools.watch{List,Add,Remove}` (CW1 user-level Collection watching via `CollectionWatcherService`), `ShapesMcpTools.shape{Render,Validate}` + `repExport` (in-tree DECLARED projection + Jena SHACL validate + synchronous TPL14 REP build), `AiMcpTools.ai{Capabilities,Invoke}` (LlmProvider/AiRegistry SPI with the local-noop fail-soft default per the CLAUDE.md "ship a working local default" rule — no API key required). 57 new unit tests. **SEARCH-MCP-PERMS-1 + MCP-PERMS-AUDIT-2 (2026-05-31):** `SearchMcpTools.search` now gates every row through its parent-Collection Read walk + per-Collection cache (filter sits between dedupe and pagination, `total` is post-filter); `VersionMcpTools.versionList/versionGet` gate Read on parent Collection via the `:has_version` walk; `ShapesMcpTools.repExport` MCP surface tightens to Write on the Collection because REP records a Collection-scoped PROV-O Activity (REST flip queued under MCP-PERMS-AUDIT-2). 16 new JUnit tests across the three tool catalogues. | none | **✓ shipped Phase 1 (MCP-2) + API1 fix (2026-05-26) + MCP-COV-03/04/05/06/07/08/10/11/12/13 expansion (2026-05-30 → 2026-06-29)** — ~43-tool surface; ~212 backend + 4 frontend unit tests. MCP-COV-07 (SPARQL `sparql_query` tool) shipped fire-306. **MCP-COV-02 ✅ complete (fire-307 + fire-308):** Slice 1 shipped fire-307 — 6 Collection/DataObject CRUD tools in `CollectionMcpTools` (16 unit tests). Slice 2 shipped fire-308 — 5 Reference kind-dispatch CRUD tools (`list_references`, `get_reference`, `reference_create`, `reference_update`, `reference_delete`) in `ReferencesMcpTools`; kind normalised to lower-case; V2CONV-A2 dispatch; 27 unit tests. Remaining: -06 (snapshots). **MCP-COV-09-KRL ✅ shipped fire-312:** plugin-local `KrlMcpTools` in `plugins/krl-interpreter/`; `krl_interpret` + `krl_capabilities`; 11 unit tests. | **✓ ↑** (MCP-2 + API1 + MCP-COV expansion) | `aidocs/platform/88-quarkus-mcp-server-migration.md` + `aidocs/34` MCP-2 + `aidocs/16` API1 + MCP-COV-03/04/05/06/08/10/11/12/13 |

---

## Headline state of progress

**Shipped on this fork (vs upstream 5.2.0):** 6 DB-resilience improvements, 5 config/cache improvements, 1 API-key auth feature (L5), 4 security fixes (M2/M4/M5 + the L2a additive identifier substrate), 5 endpoint-additive features (P3, P14, R2/b/c/d/d2), the GitHub Pages docs site with three deploy guides, the **25-channel LUMEN showcase seed + notebook** (15 runs, 2 hold days, 4 signal profiles), **demo auto-seeder** service with bootstrap flow, **Playwright e2e test suite**, API Docs nav link, and **two Live tracking docs** (`aidocs/34` admin-facing + this matrix contributor-facing).

**In flight (agents dispatched):** none currently — C5/C5b/L2b/L2c have all landed; the remaining L2 chain (L2d/L2e) is gated on P4 + H4.

**Designed and queued (substantial):** the entire L2 chain after L2a (b/c/d/e), unified search + pagination (`aidocs/13`), semantic-annotation expansion (`aidocs/14`), HDF5/HSDS (A5), Templates (T1), Process design+runtime (PR1), Git integration (G1), User profile (U1), Lab journal v2 + Jupyter (J1), Snapshots (V2), AI features w/ snap-dashboards killer feature (AI1), Admin CLI L1 Phase 2+ (Phase 1 already shipped — see §13 row above), permission-system evolutions (F1-F8), provenance (`aidocs/30`).

**Headline next-horizon line items** (per `aidocs/42` vision):
1. Snap dashboards (AI1e) — the killer feature
2. HDF5 / HSDS (A5)
3. Templates + processes (T1 + PR1)
4. User profile + ORCID (U1)

---

## 18. Newly designed (2026-05-12 batch)

| Capability | Upstream | This fork | Status | Refs |
|---|---|---|---|---|
| **Performance recommender** (`scripts/perf/recommend.py` — stdlib-only Python 3.9+; reads k6 `last-run.json` + optional Prometheus instant queries; 6 rules covering JVM heap, GC pauses, permissions-cache hit rate, Mongo latency, search p95, collections-list p95; ranked suggestions with `safe_to_apply` flag; `--json` + `--output` + `--dry-run`; exit 1 when suggestions found for CI use) | n/a | **PERF2b shipped** | **✓ ↑** | `aidocs/59 §4` / `aidocs/16 PERF2b` |
| **Instance-Admin role + bootstrap** (`instance-admin` tier, `:HAS_ROLE` Neo4j edge, configurable OIDC roles-claim path, file-on-disk bootstrap token, `/v2/admin/...` REST surface) | n/a | backend slice shipped (PR #1037; A0 + C3 + F8) | **✓ ↑** | `aidocs/51` / `aidocs/16 A0` |
| **AAS backend integration** (Adapter shim at `/v2/aas/...` mapping Shell / Submodel / SubmodelElement → Collection / DataObject / Reference; conformance targets IDTA Nameplate + TechnicalData + TimeSeriesData) | n/a | design done; **AAS1-well-known shipped**; **AAS1a shipped**; **AAS1-reg Commit 1 shipped** — `:AasRegistration` outbox entity + `AasRegistrationDAO`; V46 migration; `shepard.aas.registry.url` wired into well-known; 7 new tests; **AAS1-reg Commit 2 shipped** — `AasRegistryClient` (JDK HttpClient, `POST {registryUrl}/shell-descriptors`) + `AasRegistryOutboxService.syncAll()` (seeds PENDING rows, pushes PENDING/FAILED to registry, updates status) + startup hook (`@Observes StartupEvent`, virtual thread, best-effort); `AasShellDescriptorIO` wire shape; 2 new optional config keys (`shepard.aas.registry.api-key`, `shepard.aas.base-url`); 12 new unit tests (`AasRegistryOutboxServiceTest`); **AAS1-reg Commit 3 shipped** — `GET /v2/admin/aas/registrations` (list outbox rows) + `POST /v2/admin/aas/registrations/sync` (on-demand retry); `AasRegistrationIO` + `AasSyncResultIO`; 5 new unit tests (`AasRegistrationAdminRestTest`); **AAS1b Commit 1 shipped** — `GET /v2/aas/shells/{aasId}` (single Shell lookup; resolves base64url-encoded IRI per IDTA-01002-3-2 §4.3 or bare appId; 404-on-no-read); `CollectionDAO.findByAppId(appId, username)`; 9 new unit tests in `AasShellsRestTest`; **AAS1b Commit 2 shipped** — `GET /v2/aas/shells/{aasId}/submodels` (top-level DataObjects as `ExternalReference` Submodel refs); `AasReferenceIO` + `AasKeyIO` records; `DataObjectDAO.findTopLevelByCollectionAppId`; `AasShellMappingService.toSubmodelRefs + toShell(Collection, List<DataObject>)`; `AasShellIO.submodels` type changed from `List<String>` → `List<AasReferenceIO>` (breaking on `/v2/`, was always empty in AAS1a); `DATAOBJECT_URN_PREFIX` added; 8 new unit tests (`AasShellMappingServiceTest` 3 + `AasShellsRestTest` 5); **AAS1d shipped** — three bundled IDTA Submodel Templates (Digital Nameplate v3.0, Technical Data v2.0, Time Series Data v1.1) as `ShepardTemplate` with `templateKind=AAS_SUBMODEL_TEMPLATE`; `AasIdtaTemplateImportService` (idempotent — skip-if-identical, copy-on-write-if-changed); `POST /v2/admin/aas/import-idta-templates`; `AasAdminRest`; `AasIdtaImportResultIO`; `AAS_SUBMODEL_TEMPLATE_KIND` widened to `public static final`; CLI: `AasCommand` + `AasImportIdtaTemplatesCommand` + `AasIdtaImportResult`; 9 new unit tests (`AasIdtaTemplateImportServiceTest` 5 + `AasAdminRestTest` 5); templates auto-surface in `/.well-known/aas-server.supportedSubmodelTemplates`; **AAS1-plugin shipped** — extracted to `shepard-plugin-aas`; `AasPayloadKind` SPI registers `:AasRegistration` OGM package; V46 migration stays in backend resources; `plugins/aas/docs/{reference,quickstart,install}.md`; AAS1c (SubmodelElement drill-down) queued in plugin; **AAS1l shipped** — `:AasConfig` singleton + `GET/PATCH /v2/admin/aas/config` (instance-admin gated); RFC 7396 merge-patch; `registryApiKey` masked as `apiKeyPresent` boolean; `AasRegistryOutboxService` + `AasServerSelfDescriptionService` prefer runtime config over deploy-time `@ConfigProperty`; V88 migration adds `AasConfig_appId_unique` constraint; 17 unit tests; **APISIMP-AAS-SHELLS-LIST-ENVELOPE + APISIMP-AAS-SUBMODELS-UNBOUNDED shipped (fire-281)** — `GET /v2/aas/shells` response wrapped in `PagedResponseIO<AasShellIO>` (was plain array); `collectionDAO.countAllCollectionsByShepardId(username)` count added; `GET /v2/aas/shells/{aasId}/submodels` gains `?page`/`?pageSize` (default 0/50, cap 200) + `dataObjectDAO.countTopLevelByCollectionAppId()` count + `findTopLevelByCollectionAppId(appId, page, pageSize)` paginated overload; response wrapped in `PagedResponseIO<AasReferenceIO>`; 7 tests updated/added; **MISSING-aas-ui fully shipped (fire-295, 13 slices)** — AAS Shell list page (`/aas/shells`, paginated, Tools-tile nav entry); AAS Shell detail page (`/aas/shells/{collectionId}` — Asset Information + Submodels table with "Open DataObject" per-row); AAS Submodel detail page (`/aas/submodels/{shellId}/{submodelId}` — SubmodelElements panel showing semantic annotations as AAS Properties + back-to-shell + open-DataObject buttons); `CollectionAasPane.vue` (in-context Shell IRI + "Open AAS Shell" on Collection detail); `DataObjectAasPane.vue` (in-context Shell IRI + Submodel IRI + nav buttons on DataObject detail); `useAasShell` / `useAasSubmodel` / `useAasShells` composables (all v2 surface, appId-routed); Tools-landing tile + `/aas/shells` route registered; admin AAS config pane + AAS registrations pane. Auto-mapping approach (Collection=Shell, DataObject=Submodel) means no AAS-specific CRUD — the feature reuses the existing DataObject annotation surface for SubmodelElements. | **✓ ↑** (AAS1-plugin + MISSING-aas-ui) | `aidocs/52` / `aidocs/16 AAS1-reg` / `aidocs/16 MISSING-aas-ui` |
| **`FileReference` → `FileBundleReference` rename + `FileGroup` sub-node** (camera cyclic-capture grouping; legacy `:FileReference` label + `/shepard/api/.../fileReferences/...` wire shape stay frozen; new `/v2/bundles/{appId}/groups/...` shelf surfaces the new shape; V21 + V22 migrations idempotent + fail-fast) | n/a | **FR1a shipped** (rename, V21/V22 migrations, default-group auto-create on new bundles, full `/v2/bundles/...` REST surface, 81 new tests, JaCoCo / SpotBugs green) | **✓ ↑** | `aidocs/53` / FR1a PR |
| **Singleton `FileReference` (single-file primitive, FR1b)** + `/v2/files/...` shelf + V23 opt-in carve-out migration | n/a | **FR1b shipped** (new `:SingletonFileReference` entity + DAO + service, shared `_shepard_files` Mongo namespace, `/v2/files/{appId}/{,content}` shelf with HTTP-range support, V23 Java-based migration gated by `shepard.migration.split-singletons.enabled` + V23_R rollback + V24 constraint, 76 new tests, JaCoCo / SpotBugs green) | **✓ ↑** | `aidocs/53 §1.8` / FR1b PR |
| **Video as a first-class payload kind** (dedicated PayloadStorage plugin; segments + HLS manifest; navigation by video-time + wall-clock; live ingest via sibling `shepard-video-collector` or MediaMTX sidecar) | n/a | design done | 📐 (queued, VID1a) | `aidocs/53` |
| **TEMPLATE-ICONS-1 — `:ShepardTemplate` carries its visual identity (`iconKey: String?`)** — admin-configurable MDI icon name (e.g. `mdi-layers`) on every template. The UI renders that icon wherever a template is shown: the MyTemplatesPane catalogue, the AdminTemplatesPane table, the AdminTemplateDialog (admin-editable field + live preview), the TemplateAutocomplete dropdown items, and the TemplatePickerDialog cards. Falls back to a per-kind default (`mdi-folder-multiple` for Collection, `mdi-circle-medium` for DataObject, …) when null. COW path carries the field through unchanged so admins editing a description don't lose the icon. PATCH treats `""` as a clear request (revert to default). V107 migration backfills 16 shipped templates (V93 EquipmentItem + V100's 8 MFFD process steps + V101's 6 demo templates + V102's cross-ply view recipe + V103's disposition record) and pre-claims 10 future MFFD CamelCase template names per `aidocs/integrations/122 §4` (no-op until those seeds land). Subsumes the long-pending task #22 (admin-configurable type icons). 31 backend JUnit + 14 Vitest tests. | n/a | **✓ shipped (TEMPLATE-ICONS-1 + TEMPLATE-ICONS-2-FE)** | `aidocs/integrations/122` + `aidocs/34` TEMPLATE-ICONS-1 row + `aidocs/16` TEMPLATE-ICONS-1/2-FE rows |
| **TPL-INHERIT — `:ShepardTemplate` single-parent inheritance (`parentTemplateAppId: String?`)** — an admin template can extend a parent template; the child inherits the parent's flattened definition (JSON-DSL body, reference-creation hints, `iconKey`, semantic-annotation defaults) with the child's own fields overriding on collision. Single-parent for v1 (matches Java/XSD/most schema systems; multi-parent's diamond problem has no demonstrated authoring need). `TemplateInheritanceResolver` walks the chain root-first and deep-merges (objects recurse; arrays merge positionally so `dataobjects[0].attributes` inherits + per-key overrides; `shapeGraph` Turtle concatenated parent-ahead-of-child = `sh:and`). `GET /v2/templates/{appId}?flatten=true` returns the flattened view. `POST`/`PATCH` guard parent existence, kind-match, and cycles (`wouldCreateCycle`); COW carries the parent edge through. DO-from-template instantiation flattens before extracting attributes, so a child template pre-fills inherited + own fields. `AdminTemplateDialog` gains an appId-keyed parent picker (same-kind, non-retired, self+descendants excluded) + read-only inherited-fields preview (basic+advanced). The opposing API-Minimalist lens (a new edge vs SHACL `sh:node` composition) is engaged in the design doc §1.4 — the edge wins because templates store opaque JSON, not live SHACL, so `sh:node` is unreachable from the instantiation path without a reasoner. V110 additive-nullable migration + index. 18 backend JUnit + 8 Vitest tests. | n/a | **✓ shipped (TPL-INHERIT)** | `aidocs/integrations/123` + `aidocs/34` TPL-INHERIT row + `aidocs/16` TPL-INHERIT |
| **Templates as a first-class admin entity** (`:ShepardTemplate` Neo4j entity in admin-only subgraph, JSON DSL bodies, copy-on-write versioning, admin-gated CRUD at `/v2/templates`; `:USES_TEMPLATE` + `:ALLOWS_TEMPLATE` edges + Collection template wiring REST; `TemplateBodyValidator` JSON DSL validation; **T1e** server-side DataObject instantiation at `POST /v2/collections/{collectionAppId}/data-objects/from-template/{templateAppId}` with `[:CREATED_FROM_TEMPLATE]` relationship; **T1f** YAML round-trip import/export — `GET /v2/templates/export` + `POST /v2/templates/import`, admin-only, copy-on-write on name collision, `jackson-dataformat-yaml` dep; **UI2a** admin templates browser — `AdminTemplatesPane.vue` with `v-data-table` + create/edit/retire dialogs + `ShepardTemplateApi.ts` backend client; **T1e2** frontend template-driven creation — `TemplatePickerDialog.vue`, `CollectionTemplateApi.ts`, template picker as default step in `CreateDataObjectDialog` + `CreateCollectionDialog`, "Start from blank" secondary option; **T1i** built-in `EquipmentItem` DATAOBJECT_RECIPE template (`V93`); **TPL-SEED-MFFD-1** built-in MFFD process-step templates — 8 × DATAOBJECT_RECIPE seeded by `V100__Mffd_process_templates_seed.cypher`, one per step of the MFFD AFP→assembly chain (`MFFD AFP Layup` with TCP temperature + consolidation force + thermal-trail TS ref; `MFFD Ultrasonic / Resistance / Stud Welding` with weld-process-specific kinematics; `MFFD NDT Inspection` with method + result enums for the EN 9100 rework loop; `MFFD Frame Welding` with Q1/Q2/merged track designator; `MFFD Stringer Connection` with joint-type enum; `MFFD LBR Cleats Assembly` with cleat-count + force-torque-profile ref). Each carries `urn:shepard:domain="aerospace-manufacturing"`, `urn:shepard:mffd:process-type=<canonical>`, `urn:shepard:mffd:step-number=<1..8>` annotations embedded in the body JSON; CF/LMPAEK steps also carry `urn:shepard:material="CF/LMPAEK"`. Annotation propagation through `TemplateInstantiationRest` tracked TPL-INSTANTIATE-ANNOTATIONS-1.) | n/a | **T1a/b/c/d/e/f + UI2a + T1e2 + T1i + TPL-SEED-MFFD-1 shipped** (entity + DAO + CRUD REST + collection wiring + validator + server-side instantiation + YAML portability + admin browser UI + frontend template picker as default creation path + EquipmentItem seed + 8 MFFD process-step seeds) | **✓ ↑** (T1a–T1f + UI2a + T1e2 + T1i + TPL-SEED-MFFD-1) | `aidocs/54` / `aidocs/16` T1e, T1f, T1e2, UI5, T1i, TPL-SEED-MFFD-1 / `aidocs/34` TPL-SEED-MFFD-1 |
| **PROV-O provenance + activity capture + dashboard + metadata4ing (m4i) content-neg** (`:Activity` Neo4j entity, JAX-RS request filter, `GET /v2/provenance/{activities,count,stats,entity/{appId}}` query endpoints, per-Collection activity sparkline UI, PROV-N JSON + PROV-O JSON-LD + metadata4ing-profile JSON-LD content-negotiation) | n/a | **PROV1a/b/c/d/f/g/h shipped** — capture filter, query endpoints, stats aggregation with cumulative integral, retention TTL, PROV-N JSON content negotiation, the per-Collection Vue dashboard (sparkline + cumulative overlay + action-kind histogram + time-range picker), **and m4i content-negotiation** (`Accept: application/ld+json` for PROV-O; `; profile=metadata4ing` for the engineering-research flavour with `m4i:ProcessingStep` / `m4i:InvestigatedObject` / `m4i:Person` subtypes; 406 RFC 7807 on unknown profile). Instance-admin dashboard (PROV1e) queued. **PROV1j shipped (2026-05-26)** — `provenanceMode` field (`null`/`"human"`/`"ai"`/`"collaborative"`) on `:DataObject` entity + `DataObjectDetailV2IO` + `DataObjectListItemV2IO`; `CreateDataObjectV2IO` v2 create body; `X-AI-Agent` header auto-detect in `DataObjectV2Rest.create()` mirrors SEMA-V6-007 pattern; `DataObjectService` instanceof guard; 35 unit tests (3 new PROV1j tests pass). Closes EU AI Act Art. 50 per-artefact visibility gap (deadline 2026-08-02). **PROV1j (activity-layer) also shipped (2026-05-26)** — `sourceMode` + `agentId` fields on `:Activity` entity (schema-free, no migration); `ProvenanceCaptureFilter` reads `X-AI-Agent` header and stamps `sourceMode="human"|"ai"` + `agentId=<value>` on every captured `:Activity` row; `X-Provenance-Mode` + `X-AI-Agent-Captured` response headers on `/v2/` responses; `ActivityIO` gains two nullable fields; `ProvenanceService.record()` 13-arg overload; 11 + 5 new unit tests. **PROV1k shipped (2026-05-26)** — typed predecessor relationships: `TypedPredecessorIO` (input record; `predecessorAppId` + `relationshipType`; allowed: `prov:wasInformedBy` / `prov:wasRevisionOf` / `fair2r:repairs`; defaults to wasInformedBy); `typedPredecessorsJson` JSON string on `:DataObject`; `TypedPredecessorSummaryIO` on `DataObjectDetailV2IO`; `DataObjectDAO.findByAppId()`; V84 migration (index on `typedPredecessorsJson`) + V84_R rollback; `PredecessorRelationshipTypeChip.vue` (Vuetify chip + tooltip, 3 color variants); 10 backend unit tests + 8 Vitest tests; additive-only on `/v2/` surface, v1 wire shape unchanged. | **✓ ↑** | `aidocs/55`, `aidocs/64 §3.2` / PROV1a-c commits + PROV1d + PROV1g + PROV1h + PROV1j (2026-05-26) + PROV1k (2026-05-26) |
| **Provenance target-resolver right-to-left path walk + v1 numeric-id lookup** (PROV-RESOLVER-PATHWALK + PROV-V1-NUMERIC-LOOKUP) — pure `PathTargetParser` walks request path right-to-left for the deepest known `(kind-plural, id)` pair (UUID or numeric); CDI-bean `TargetEntityResolver` + `EntityAppIdLookup` helper exchanges v1 numeric `shepardId` for the entity's `appId` via one parameterised Cypher over the static label allow-list. Closes RDM-2026-05-24-004 buckets B+C: subresource creates (`POST /v2/collections/<C>/data-objects/<D>`) attribute to the DataObject, not the Collection; v1 numeric writes (`PATCH /shepard/api/collections/42/dataObjects/45/...`) resolve to the correct appId. Removes title-case fallback so verb segments (`payload`/`diff`/`detect-anomalies`) cannot pollute `targetKind`. | n/a | **✓ ↑** (PROV-RESOLVER-PATHWALK + PROV-V1-NUMERIC-LOOKUP, this PR) — 30+ new unit tests + live e2e against `BASE_URL=https://shepard.nuclide.systems`. Optional Cypher backfill of ~280K orphaned historic rows queued as `aidocs/16 PROV-BACKFILL-MIGRATION`. | `aidocs/agent-findings/prov-resolver-fix-2026-05-24.md` + `aidocs/16` PROV-RESOLVER-PATHWALK / PROV-V1-NUMERIC-LOOKUP / PROV-BACKFILL-MIGRATION |
| **NEO-AUDIT-001 — PROV-O graph edges wired on `:Activity` + V77 backfill** — three PROV-O relationship types (`WAS_ASSOCIATED_WITH` → `:User`, `GENERATED` / `USED` → `:BasicEntity`) now wired at write time via `ActivityDAO.wireEdges()` called in `ProvenanceService.record()` post-save. `V77__backfill_activity_prov_edges.cypher` adds two indexes (`Activity_targetAppId_idx`, `Activity_agentUsername_idx`) and backfills 284,530 orphaned Activity nodes in batched CALL blocks. Javadoc direction corrected (OUTGOING from Activity per PROV-O spec). Rollback `V77_R` file ships. 5 new tests. | n/a (upstream has no PROV-O edge wiring) | **✓ ↑** (NEO-AUDIT-001, 2026-05-26) — forward-wiring live; V77 backfill runs on next startup | `aidocs/16` NEO-AUDIT-2026-05-24-001 → **done** · `aidocs/34` NEO-AUDIT-001 row |
| Network-drive mount (read-only WebDAV at `/v2/webdav/...`, FileBundle directory tree + synthetic per-entity files + CSV-default timeseries) | none | design done; default-off | 📐 (queued, MNT1a) | `aidocs/61` |
| **BTKVS Phase B — SHACL form templates (forms as the write-direction of the shapes UX)** — no new TemplateKind: a form is the write projection of a DATA-kind template's `shapeGraph` (the same graph V2CONV-B2 validates at instantiation). DASH (`dash:editor` + editor catalogue) adopted for widget hints + 3 minted `urn:shepard:form:*` predicates (`visibleWhen`/`placeholder`/`hidden`) + `shepard-ui:*Editor` individuals; `GET /v2/templates/{appId}/form` form-descriptor compiler; instantiation 422 grows structured `violations[]` (SHACL `resultPath` ↔ field mapping); unified discovery `GET /v2/shapes/applicable` with `mode: VIEW\|FORM` + one `ActionMenuButton` ("View as…"/"Record a…") pairing with the 2026-06-12 UX-audit render contract; Excel ↔ JSON round-trip on the shape spine via `urn:btkvs:cell-mapping` (POI; retires the BT-KVS group's xlwings + `docket_version_upgrader` maintenance) | n/a (upstream has no template/shape system) | **🚧 in-flight** — the BTKVS-B2 validating slice shipped 2026-06-12: docket `:general` shape authored via the `ShaclShapeBuilder` DSL (`sh:pattern` + `sh:targetNode` + DASH hints + `sh:PropertyGroup`s + 2 cell-mappings), `GET /v2/templates/{appId}/form` descriptor compiler (`FormDescriptorCompiler` + `TemplateFormRest`; ETag; 404/409/422), structured `violations[]` on the instantiation 422 (FORM-422-FIELDS) + caller-supplied `attributes` on the submit leg, Streamlit demo (`form_demo.py`) + `/tools/form-preview` placeholder stub. **Second 2026-06-12 slice (SHAPES-APPLICABLE-FORMS + FORM-UX-ACTIONBUTTON):** unified discovery `GET /v2/shapes/applicable?focusAppId=…` (`listApplicableShapes`; `mode: VIEW\|FORM`; VIEW = attached non-retired VIEW_RECIPE, FORM = data-kind templates with `shapeGraph`, Collection allow-list scoped; `FocusEntityRepository` generic focus resolution; problem-JSON 400/404; fail-soft, empty-items-valid) + `ActionMenuButton.vue` mounted on DataObject detail ("View as …" → `/shapes/render` prefill, absorbing the Tools menu's "Render view" entry; "Record a …" → `/tools/form-preview?template=…&focusAppId=…`); JUnit `ShapesApplicableRestTest` + Vitest `actionMenu.test.ts`. **Excel EXPORT leg shipped 2026-06-12 (BTKVS-C1-EXCEL-EXPORT):** `GET /v2/templates/{appId}/export?dataObjectAppId=…` — `CellMappingExcelExporter` (Apache POI) walks the shape's `urn:btkvs:cell-mapping`/`urn:btkvs:sheet` annotations and writes the DataObject's attribute values into the mapped cells; sibling of `/form` on the generic template surface (no `/v2/btkvs/…` mint per `aidocs/platform/191`; the stale BTKVS-C1 `POST /v2/btkvs/dockets` design reconciled in `aidocs/16`); "Download Excel" on `/tools/form-preview` + `export_demo.py`. Remaining: Excel IMPORT (BTKVS-C2, recommended seam `POST /v2/templates/{appId}/import`), FORM-DASH-VOCAB semantic-repo seeding, `?focusAppId=` edit-form prefill, Collection/reference/container button mounts, Playwright 4K pass | `aidocs/integrations/125-btkvs-shacl-form-templates.md` · `aidocs/16` §BTKVS-DOCKET-SHOWCASE · `aidocs/platform/191 §3` |
| **v2 API simplification + output profiles + MCP-friendly OpenAPI** (flat appId-indexed paths for single-entity endpoints, `?profile=metadata\|relations\|all` projections, `x-mcp-tool-name` + `x-mcp-side-effects` extensions per operation, ArchUnit fence for admin ↔ `@RolesAllowed("instance-admin")`) | n/a | design done | 📐 (queued, V2S1a) | `aidocs/56` |
| **OpenAPI client generators — dual baseline** (Kiota new baseline for `/v2/` shelf, OpenAPI Generator still-maintained legacy for byte-frozen `/shepard/api/...` shelf, Hey API as TS-only tactical secondary; ADR-0022) | n/a | **CG1a shipped** (Kiota baseline wired for Python + TypeScript + Java under `clients-v2/`; CI workflow `.github/workflows/clients-kiota.yml` regenerates on every release tag; **integration test suite added** — 27 httpx tests in `clients-v2/tests/integration/` covering /v2/users/me, /v2/collections CRUD, /v2/data-objects CRUD, /v2/instance/capabilities; gated on `SHEPARD_URL`+`SHEPARD_API_KEY`, skip cleanly when absent; Kiota adapter fixture ready for `make generate-python`) + **CG1b shipped** (OpenAPI-Generator pipeline retargeted from combined `openapi.json` → `openapi_v1.json` via new build-time slicer; per-language `v1-scope-smoke-test.sh` regression fence; dual-posture READMEs); CG1c (PyPI / npm / Maven Central publishing) queued; CG1d (Go / Rust / C#) deferred | **✓ ↑** (CG1a + CG1b) | `aidocs/57` / `aidocs/63` ADR-0022 |
| **Tree drag-and-drop** (lefthand tree; move default, copy on modifier; cycle-prevention server-side) | n/a | design done | 📐 (queued, UI1a) | `aidocs/58 §2` |
| **Navigable Collection graph view** (cytoscape.js; per-user layout persistence; entity-kind filtering) | n/a | design done | 📐 (queued, UI2a) | `aidocs/58 §3` |
| **`@`-mention autocomplete for internal entity citations** (TipTap mention extension + `GET /v2/search?q=…&kinds=…`; opaque `[entity:<appId>]` syntax; orphan-handling) | n/a | design done | 📐 (queued, UI3a) | `aidocs/58 §4` |
| **`:CollectionProperties` properties-node** (folds template-info + default-FC-strategy + cross-cutting Collection config into one place; replaces the `default_filecontainer` hack) | n/a | design done | 📐 (queued, CP1a) | `aidocs/58 §5` |
| **RO (Relation Ontology) added to the pre-seed bundle** (`obo-relations.ttl`, SHA-256 pinned, CC0 licence, LUMEN seed uses RO terms post-ONT1c) | n/a | ONT1a shipped (bundle + manifest + tests); ONT1c (LUMEN seed cites RO terms) + ONT1d (frontend ontology-picker) queued | **✓ ↑** (ONT1a) | `aidocs/58 §6` + `aidocs/48` |
| **metadata4ing (NFDI4Ing v1.4.0) added to the pre-seed bundle** (`metadata4ing.ttl`, SHA-256 pinned, CC BY 4.0; engineering-research extension of PROV-O — `m4i:ProcessingStep` subtype of `prov:Activity`, `m4i:Method`, `m4i:Tool`, `m4i:InvestigatedObject`, `m4i:NumericalVariable` + QUDT units, `m4i:Person`/`Organization` subtypes of prov equivalents) | n/a | ONT1b shipped (bundle + manifest + tests; bundle count now 10); unblocks future PROV1h slice that lifts `/v2/provenance/*` rendering to `m4i:`-flavoured shapes via `Accept: application/ld+json; profile=metadata4ing` | **✓ ↑** (ONT1b, this commit) | `aidocs/16` ONT1b |
| **Helmholtz Unhide publish plugin** (`/v2/unhide/feed.jsonld` schema.org + metadata4ing JSON-LD feed for HKG / Unhide harvester; `:UnhideConfig` admin-configurable singleton + admin REST + `shepard-admin unhide ...` CLI parity for master toggle / feedPublic / contactEmail / harvest-key rotate / revoke; RFC 7807 envelopes for the disabled-feed / missing-key / read-only-field paths; **UH1b** — each feed entry inlines a `m4i:hasProcessingStep` array of the most-recent N `:Activity` rows targeting the Collection, rendered via PROV1h's `ProvJsonLdRenderer.renderActivityAsM4iNode(...)` so the m4i shape lives in one place; **UH1c** — each feed entry cites the current KIP1a Publication via `schema:identifier` (PropertyValue PID) + `schema:url` (the `.well-known/kip` resolver) + `m4i:hasIdentifier`; **UH1d** — per-Collection `publishToHelmholtzKG` opt-out toggle; **UH1e** — `?validate=true` structural validation report for operators) | n/a | **UH1a + UH1b + UH1c + UH1d + UH1e shipped** — first `shepard-plugin-*` module ever. **PM1a phase 3 flipped UH1a to a real drop-in JAR**: `shepard-plugin-unhide-${revision}.jar` declared as a backend `<dependency>` via the default-active `with-plugins` Maven profile; the source-stitch via build-helper is gone. Harvest-key plaintext returned exactly once at mint, never logged (only the SHA-256 fingerprint), never enters `:Activity`. V30 migration ships the appId-unique constraint on the singleton label. UH1b provenance window tuned via `shepard.unhide.feed.provenance-window`. UH1c reuses `PublicationDAO.findByEntityAppId` from KIP1a. UH1d: `CollectionProperties.publishToHelmholtzKG` opt-out toggle (absent node = include). UH1e: `?validate=true` returns `UnhideValidationReportIO` (checks `@id`/`name`/`description` per entry); auth gates still apply; no SHACL dep added. | **✓ ↑** (UH1a–UH1e + PM1a) | `aidocs/67` + `aidocs/16` UH1a–UH1e |
| **3D Geometry & FEM Annotator** (`shepard-plugin-cad`) — `CadReference` PayloadKind; dual Three.js/vtk.js renderer; cascadio WASM + pythonocc sidecar conversion (STEP/IGES/BDF/INP/CGNS); URDF bundles; `GeometryAnnotation` model (point/region/surface/FEM node-set); scene graph export (glTF/USD/SDF/VTU); Paradigms + Pandora DLR-internal adapter hooks | n/a | design done | 📐 (queued, CAD1a; gated on FS1a) | `aidocs/78` |
| **CPACS Annotator** (`shepard-plugin-cpacs`) — `CpacReference` PayloadKind; TiGL sidecar (CPACS XML → GLB); `:CpacComponent` hierarchy; `CpacAnnotation` XPath-anchored; `needsReview` on mesh-hash change; non-geometry CPACS data (aero tables, mass) | n/a | design done | 📐 (queued, CPACS1a; gated on FS1a + CAD1b) | `aidocs/79` |
| **RCE Integration** (`shepard-plugin-rce`) — `Shepard Sink` + `Shepard Source` RCE tool integrations; OpenLineage → PROV-O `:Activity` per component execution; CPACS-typed MDO provenance chain; MDO data bus (parameter-sweep Collections + batch ingest); `:RceConfig` singleton | n/a | design done | 📐 (queued, RCE1a; RCE1b gated on PROV1a) | `aidocs/80` |
| **Matrix notification + MCP bot** (`shepard-plugin-matrix-notify`) — Matrix room as N10 notification sink; maubot + MCP server generated from `/v2/` OpenAPI spec for researcher chat; `x-mcp-tool-name` extensions already in `aidocs/56` | n/a | concept (design pending) | 📐 (concept, MTX1) | `aidocs/16` MTX1 |
| **Log-store sidecar with SHACL EventShape** (`shepard-plugin-logs`) — cluster-wide log substrate replacing the ad-hoc SD-runlog container; VictoriaLogs (Apache-2.0, per-field index, no JVM) primary + Vector (MPL-2.0, VRL pre-validation) as router; SHACL `shp:EventShape` contract; per-plugin emission via `plugin.yaml` with `emits[]` + `retention_class` + `cardinality_budget`; four retention tiers (audit / ops-30d / debug-7d / ephemeral-1d); REST `/v2/logs/*` + SSE stream + MCP tools (`search_logs`, `tail_logs`, `list_log_shapes`, `violations_today`); Vuetify `/admin/logs` view + per-DataObject activity tab + per-Collection ops sparklines | n/a | design done | 📐 (queued, LOGSTORE1) | `aidocs/integrations/94` |
| **Spatial Data Binding** (SB1) — `:DataBinding` Neo4j node linking `GeometryAnnotation` to any Reference; five display modes (BADGE / HEATMAP / GLYPH / JOINT_ANGLE / DEFORMATION); time scrubber; SSE live mode; semantic edge types `[:CHARACTERISES]` / `[:VALIDATES]` / `[:INSPECTS]` / `[:LOCATES_SENSOR]`; ZLP-specific data chains | n/a | design done | 📐 (queued, SB1a; gated on CAD1b + PROV1a) | `aidocs/81` |
| **Point Cloud + Live Overlay** (PC1 + SB2) — `PointCloudReference` (Potree streaming); ICP alignment; surface-annotation matching (PC1e); eight overlay modes incl. UT C-scan (SB2g) + POSE_6D (SB2h); SA1/RDK1/IL1 ingest pipeline SPIs | n/a | design done | 📐 (queued, PC1a; gated on CAD1a) | `aidocs/83` |
| **Live Digital Twin** (DT1) — moving-frame annotations (`urdfLinkId`); `DigitalTwinScene` composite; WebSocket CBOR state stream; interactive WebRTC server-side render; historical replay; `DigitalTwinSnapshot` (alarm-triggered / manual). **DT1-PHASE-0 (entity scaffold) shipped 2026-05-29** — `:DigitalTwinScene` + `:CoordinateFrame` + `:Joint` `@NodeEntity`s with `HasAppId`, three `@ApplicationScoped` DAOs, and `V95__DT1_Phase0_DigitalTwinScene_scaffold.cypher` with rollback twin. **SCENEGRAPH-REST-1 (REST + MCP + URDF export) shipped 2026-05-29** — nine `/v2/scene-graphs/...` endpoints, seven MCP tools (`scene_graph_*`), URDF XML exporter, frame-edge / joint-edge writes via `SceneGraphService` raw Cypher (`:HAS_FRAME` / `:HAS_PARENT_FRAME` / `:HAS_JOINT` / `:JOINT_PARENT` / `:JOINT_CHILD`), `:Activity` + `:WAS_DERIVED_FROM` per mutation, frontend placeholder at `/scene-graphs/[appId]`. 18 new tests (`UrdfExporterTest` 6 + `SceneGraphServiceTest` 12). **SCENEGRAPH-REST-1-UI (real browser UI) in-flight 2026-05-30** — page at `/scene-graphs/{appId}` with recursive frame tree, sticky inspector, sibling joints table, add/edit/delete dialogs, URDF download, 404/empty/loading states; 53 Vitest cases on pure helpers + Playwright 4K spec. **First real-data scene shipped 2026-05-30 (MFFD-SCENE-BOOTSTRAP-1)** — `examples/mffd-rdk-urdf-showcase/scenegraph/build_mffd_scene.py` parses the kr210_r2700_2 URDF (kroshu, Apache-2.0) with stdlib `xml.etree.ElementTree`, mints "MFFD AFP cell — Q3 demonstrator" with two robot subtrees (`r10_` + `r20_`) under a shared `cell_base` root (21 frames + 18 joints), back-annotates the URDF FileReference with `urn:shepard:scenegraph:scene-appId`. Idempotent re-runs. Live verification blocked: `de.dlr.shepard.v2.scenegraph.entities` not registered in `NeoConnector.packages` — POST 500s with "DigitalTwinScene is not a valid entity class" on the deployed image. Fix is one line; out of this PR's hard-guard scope; tracked as a follow-on issue. **SCENEGRAPH-CREATE-FROM-URDF-1 (click-to-mint backend) shipped 2026-05-30** — `POST /v2/scene-graphs/from-urdf/{fileReferenceAppId}` ports the python `parse_urdf` to Java (`javax.xml.parsers.DocumentBuilder`, OWASP secure defaults), resolves the FR1b singleton, walks Write permission on the parent Collection, idempotency-checks for an existing `urn:shepard:scenegraph:scene-appId` back-annotation (409 with `existingSceneAppId`), parses URDF, mints scene + frames + joints via `SceneGraphService`, writes the back-annotation, records one umbrella `:Activity`. 18 JUnit tests; ~91% line / ~75% branch coverage on new code. Frontend wiring (modal confirm-button) queued as SCENEGRAPH-CREATE-FROM-URDF-2-FE. **SCENEGRAPH-PERMS-1 (per-scene permission gate) shipped 2026-05-31** — `SceneGraphRest` swaps `@Authenticated`-only for a real walk via new `SceneGraphPermissionService` (`scene.sourceFileAppId → FileReference → DataObject → Collection`, reusing `PermissionsService.isAccessAllowedForDataObjectAppId`). Read endpoints gate on Read; write endpoints gate on Write; list endpoint post-filters page to readable subset; hand-built scenes fail closed unless caller is `instance-admin`. 25 JUnit tests. MCP-layer bypass tracked under `SCENEGRAPH-PERMS-1-MCP`. **COLL-SCENE-1 + COLL-SCENE-2-UI (Collection-level hero scene-graph) shipped 2026-06-02** — new scalar `:Collection.sceneGraphAppId` (additive nullable; V102 NOOP migration + rollback) + `/v2/collections/{appId}/scene-graph` (GET/PUT/DELETE) with two-sided permission gate (Write on Collection + Read on the target scene for PUT) so the MFFD `MFZ.rdk`-derived URDF surfaces on the Collection landing page. Frontend `CollectionSceneGraphHeader.vue` renders the URDF via existing `UrdfCanvas`; writers see a "Link scene-graph" CTA when no link is set; dangling pointer falls back to `EntityNotFound`. 18 JUnit + 11 Vitest cases. Closes GAP-6 in `aidocs/agent-findings/mffd-feature-gaps-2026-06-02.md`. **SCENEGRAPH-CANVAS-1 (3D viewport on the scene detail page) shipped 2026-06-03** — `SceneGraphCanvasPanel.vue` mounts a `UrdfCanvas` atop `/scene-graphs/{appId}` so a URDF-minted scene finally *renders* the robot instead of only its frame tree. Source-file bytes (real STL meshes) via new `useUrdfReferenceBlob` (`GET /v2/files/{appId}/content` → blob URL, no-URL-input compliant) when `sourceFileAppId` is set; reconstructed `export.urdf` (frame links) for hand-built scenes via the existing `fetchSceneUrdfBlobUrl` helper. Manual joint posing via `UrdfJointPanel`. 11 Vitest. Closes the MFFD scene-building gap flo flagged; channel-driven animation queued as SCENEGRAPH-CANVAS-ANIM-1, `package://` mesh resolution as SCENEGRAPH-CANVAS-MESH-1. **V2CONV-B4 — bespoke scene-graph DISSOLVED 2026-06-04** — the `/v2/scene-graphs/*` REST namespace, the `:DigitalTwinScene`/`:CoordinateFrame`/`:Joint` stored graph, the 10 `scene_graph_*` MCP tools, and the bespoke `/scene-graphs/*` frontend editor are all DELETED. A scene-graph is now a `MAPPING_RECIPE` `ShepardTemplate` binding a URDF FileReference (parsed on demand) → `SceneGraphPlayShape` → `SceneGraphPlayTransformExecutor` (in `plugins/vis-trace3d`) → `TransformResult.view` play envelope for the Trace3D renderer. Migration `V111__B4_dissolve_scenegraph.cypher` (+ rollback). New FE: `OpenIn3dViewButton.vue` + `useSceneGraphPlay.ts` + `/scene-graphs/play/[templateAppId].vue`. Collection hero link repointed to a MAPPING_RECIPE template appId (path + `:Collection.sceneGraphAppId` property kept). 16 plugin JUnit + V111 migration test + 16 Vitest. | n/a | DT1-PHASE-0 scaffold ✓ shipped 2026-05-29; SCENEGRAPH-REST-1 ✓ shipped 2026-05-29; SCENEGRAPH-REST-1-UI 🚧 in-flight 2026-05-30; MFFD-SCENE-BOOTSTRAP-1 ✓ shipped 2026-05-30 (script); SCENEGRAPH-CREATE-FROM-URDF-1 ✓ shipped 2026-05-30 (one-call endpoint); SCENEGRAPH-PERMS-1 ✓ shipped 2026-05-31 (per-scene permission gate); COLL-SCENE-1 + COLL-SCENE-2-UI ✓ shipped 2026-06-02 (Collection-level hero scene link + UI band); SCENEGRAPH-CANVAS-1 ✓ shipped 2026-06-03 (3D viewport on scene detail page) | 🚧 (DT1-PHASE-0 ✓; SCENEGRAPH-REST-1 ✓; SCENEGRAPH-REST-1-UI 🚧; MFFD-SCENE-BOOTSTRAP-1 ✓; SCENEGRAPH-CREATE-FROM-URDF-1 ✓ replaces the shell bootstrap with a one-call endpoint; SCENEGRAPH-PERMS-1 ✓; COLL-SCENE ✓; **V2CONV-B4 dissolved the bespoke scene-graph into a MAPPING_RECIPE view-shape 2026-06-04 — `/v2/scene-graphs/*` + the stored graph removed; scene-graph now ships via `plugins/vis-trace3d` `SceneGraphPlayShape`**; DT1a (WebSocket/WebRTC live state) still gated on CAD1b + SB1d) | `aidocs/84` + `aidocs/platform/191` + `aidocs/16` DT1-PHASE-0 + SCENEGRAPH-* + COLL-SCENE-1/-2-UI + V2CONV-B4 |
| **Time Reference Model** (TM1) — `timeReference: enum WALL_CLOCK \| EXPERIMENT_RELATIVE` + `wallClockOffset: long` (nanoseconds epoch of DAQ t=0) on `TimeseriesReference`; both fields mutable via `PATCH /v2/timeseries/{appId}` so an offset can be corrected when a better anchor is discovered (ffprobe, SA sync signal, manual); V-series Cypher backfill of `timeReference="WALL_CLOCK"` on existing rows; `wallClockOffsetSource` provenance tag; enables cross-source alignment with video (SB2a), SA 6D data (SB2h), and multi-channel timeline (SB2d) | n/a | design done | 📐 (queued, TM1a; ungated) | `aidocs/16` TM1 |
| **Camera-to-scene calibration** (VID2) — click-based 2D→3D correspondence picker (user marks N point pairs in a video frame + the matching 3D CAD point; sparse ok, skip-able features; EPNP+RANSAC PnP solver via Open3D sidecar); stores `[:CAMERA_ALIGNED_TO {matrix:[16], rmse, K:[9]}]` edge on `VideoStream→CadReference`; unlocks SB2a CAMERA_FRUSTUM viewer mode, 3D annotation → video pixel projection overlay, and DT1 live camera viewport | n/a | design sketch | 📐 (queued, VID2a; gated on VID1a + PC1b sidecar) | `aidocs/16` VID2 |
| **DBpedia Databus rich-reference plugin** (preview / description / title fetched + 24h-cached) | n/a | design done | 📐 (queued, REF1a; off-by-default until v1) | `aidocs/58 §7` |
| **GraphRAG on shepard** (embeddings per DataObject / Collection / lab-journal entry / Reference; similarity endpoint `GET /v2/search/similar?to=<appId>`; native Neo4j 5.13+ vector index — no extra service) | n/a | design done | 📐 (queued, GR1a) | `aidocs/58 §8` + `aidocs/43` |
| **HMC Kernel Information Profile baseline** (`Minter` SPI in core, all minter impls ship as plugins post-KIP1h; `MinterRegistry` discovery + optional posture — 503 RFC 7807 when no minter is wired, resolver still works against existing rows; `:Publication` entity + `HAS_PUBLICATION` edge + `versionNumber` property; `POST /v2/{kind}/{appId}/publish` with Writer/Manager auth + idempotent re-POST + `?force=true` re-mint as version-bump; unauthenticated `GET /v2/.well-known/kip/{pid-suffix}` returning the public HMC KIP JSON-LD record with `digitalObjectVersion: "v<n>"`; `PublishableKindRegistry` for URL-segment dispatch — adding bundles/files/lab-journal-entries doesn't change the URL shape; V29 + V31 migrations; **KIP1f (retire-publication) + KIP1g (resolver as plugin) + KIP1h (minter as plugin + versioned PIDs Phase 1) shipped**) | n/a | **KIP1a + KIP1f + KIP1g + KIP1h shipped** — KIP1a baseline (KIP1b folded in). KIP1g extracts the HMC-flavoured resolver + record JSON-LD shape into `plugins/kip/` (`shepard-plugin-kip`). KIP1h extracts the renamed `MockMinter` → `LocalMinter` into `plugins/minter-local/` (`shepard-plugin-minter-local`), relaxes the `MinterRegistry` fail-fast to optional posture, and ships versioned PIDs Phase 1 (local format `shepard:<instance.id>:<kind>:<appId>:v<n>`; `Publication.versionNumber`; V31 backfill stamps `versionNumber=1` on legacy rows). KIP1f (this PR) ships `DELETE /v2/{kind}/{appId}/publish` — sets `digitalObjectMutability='retired'` on the most-recent `:Publication` row (append-only; no delete); `PublicationIO` grows additive nullable `digitalObjectMutability` field. 20 new tests (DAO +5, service +5, REST +8, IO +2). No Neo4j migration. The `Minter` SPI seam stays in core; every impl ships as a plugin per CLAUDE.md heuristic #3. Phase 2 (`:EntityVersion` graph) queued as ENT1 umbrella. | **✓ ↑** (KIP1a + KIP1f + KIP1g + KIP1h) | `aidocs/66` / `aidocs/16 KIP1a` + `KIP1f` + `KIP1g` + `KIP1h` |
| **Local PID minter plugin (versioned PIDs)** (`shepard-plugin-minter-local` — default minter for fresh shepard installs; mints stable `shepard:<instance.id>:<kind>:<appId>:v<n>` PIDs; renamed from the pre-KIP1h in-core `MockMinter`; ships as a drop-in JAR per CLAUDE.md heuristic #3 "SPIs in core, adapters in plugins"; the `LocalMinter` `@ApplicationScoped` bean is picked up by Quarkus's build-time CDI scanner via the backend's `with-plugins` Maven profile) | n/a | **KIP1h shipped** — `LocalMinter` reads `MintRequest.versionNumber` (computed by `PublishService` as `findLatestVersionNumber + 1`), encodes as `v<n>` segment in the PID. Same inputs → same PID (no epoch-millis timestamp; PIDs are stable). `shepard.instance.id` reused as the namespace segment; fallback to `local` with a startup WARN if unset. 12 `LocalMinterTest` cases. | **✓ ↑** (KIP1h, this commit) | `aidocs/66` / `aidocs/16 KIP1h` |
| **DataCite DOI minter plugin** (`shepard-plugin-minter-datacite` — real DOIs against DataCite Fabrica test or production; sibling JAR to `shepard-plugin-minter-local` under `plugins/minter-datacite/`; `:DataciteMinterConfig` singleton + `/v2/admin/minters/datacite/` REST surface + `shepard-admin minters datacite ...` CLI parity; AES-GCM credential cipher keyed off `shepard.instance.id`; versioned-PID respect via `MintRequest.versionNumber()` + DataCite `IsNewVersionOf` / `HasVersion` chain) | n/a | **KIP1d shipped** — admin-runtime config (idiom: A3b + UH1a + N1c2 — single-instance `:DataciteMinterConfig` node, seed-on-first-start from `shepard.minters.datacite.*`, runtime PATCH wins). Five admin REST endpoints: `GET/PATCH /config` (RFC 7396), `POST/DELETE /credential`, `POST /test-connection`. Twelve CLI verbs (status, enable, disable, set-{api-url, prefix, repository-id, publisher, landing-page-base, state, password}, clear-password, test-connection) — `set-password` reads stdin / tty only, never on the command line. Network hardening: 10s connect + 30s request timeout, one retry on 5xx / network error with 1s backoff, `MinterException` on second failure → 500 `publish.minter.failed`. Default `apiBaseUrl=https://api.test.datacite.org` (Fabrica test, safe for fresh install). `V33` migration adds the uniqueness constraint on `:DataciteMinterConfig.appId`. 126 plugin tests across 9 classes (`DataciteMinterTest` 30 + `DataciteMinterConfigServiceTest` 24 + `DataciteAdminRestTest` 17 + `DataciteCommandsTest` 22 + `CredentialCipherTest` 12 + `DataciteIoTest` 7 + `MintersAdminCliCommandProviderTest` 5 + `DataciteMinterConfigTest` 5 + `DataciteMinterPluginManifestTest` 4). **APISIMP-MINTER-CRED-CONFIG-UNIFY shipped (fire-454)** — `POST/DELETE /credential` tombstoned → 410 Gone (redirect message instructs clients to use `PATCH /v2/admin/config/minter-datacite` with `password` field); `password` write-only field added to `DataciteConfigDescriptor.applyMergePatch()` (non-blank → `setCredential()`; explicit null → `clearCredential()`; absent → no change; blank → 400 `ConfigPatchException`); `DataciteAdminRestTest` 3 credential tests replaced with 410 assertions; `DataciteConfigDescriptorTest` 3 new `password_*` tests added. | **✓ ↑** (KIP1d + APISIMP-MINTER-CRED-CONFIG-UNIFY) | `aidocs/66 §6` / `aidocs/16 KIP1d` + `APISIMP-MINTER-CRED-CONFIG-UNIFY` |
| **ePIC Handle Service minter plugin** (`shepard-plugin-minter-epic` — B2HANDLE-compatible ePIC Handle Service minter; sibling JAR to `shepard-plugin-minter-datacite` under `plugins/minter-epic/`; `:EpicMinterConfig` singleton + `/v2/admin/minters/epic/` REST surface + `shepard-admin minters epic ...` CLI parity; AES-GCM credential cipher keyed off `shepard.instance.id`; PID format `https://hdl.handle.net/<prefix>/<uuid>` via `PUT <apiBaseUrl>/handles/<prefix>/<uuid>` B2HANDLE JSON body) | n/a | **KIP1c shipped** — admin-runtime config mirroring KIP1d idiom (`:EpicMinterConfig` singleton, seed-on-first-start from `shepard.minters.epic.*`, runtime PATCH wins). Five admin REST endpoints: `GET/PATCH /config` (RFC 7396 — patchable: enabled, apiBaseUrl, handlePrefix), `POST/DELETE /credential`, `POST /test-connection`. Eight CLI verbs (status, enable, disable, set-api-url, set-prefix, set-credential, clear-credential, test-connection) — `set-credential` reads stdin / tty only. Mint: `PUT <apiBaseUrl>/handles/<prefix>/<uuid>` + Basic auth + B2HANDLE JSON; one retry on 5xx/network; 4xx final. `V45` migration adds the uniqueness constraint on `:EpicMinterConfig.appId`. Plugin tests across 6 classes. Docs: `docs/reference/minter-epic.md` + `docs/help/minter-epic-quickstart.md` + `docs/install/minter-epic.md`. **APISIMP-MINTER-CRED-CONFIG-UNIFY shipped (fire-454)** — `POST/DELETE /credential` tombstoned → 410 Gone; `credential` write-only field added to `EpicConfigDescriptor.applyMergePatch()` (`PATCH /v2/admin/config/minter-epic` body); `EpicAdminRestTest` 3 credential tests replaced with 410 assertions; `EpicConfigDescriptorTest` 3 new `credential_*` tests added. | **✓ ↑** (KIP1c + APISIMP-MINTER-CRED-CONFIG-UNIFY) | `aidocs/16 KIP1c` + `APISIMP-MINTER-CRED-CONFIG-UNIFY` |
| **HMC KIP — Vue "Publish" button** (frontend surface for KIP1a; `PublishButton.vue` + `PublishModal.vue` under `frontend/components/context/publish/` mounted on the Collection + DataObject detail panes; SPDX licence picker `CC-BY-4.0 / CC-BY-SA-4.0 / CC0-1.0 / MIT / Apache-2.0 / LGPL-3.0 / GPL-3.0`; post-publish snackbar with copy-to-clipboard for both the PID and the resolver URL) | n/a | **KIP1e shipped** — pure frontend slice; calls existing KIP1a endpoints via a new `usePublishEntity` composable (raw `fetch` until the OpenAPI Generator regeneration for KIP1a lands the `PublishApi` client). Scope-downs: existing-publication-state display deferred (no list-publications endpoint on KIP1a + `BasicEntityIO` frozen), licence wire-shape deferred (KIP1a's POST takes no body — the dropdown is informational and forwards the SPDX id on submit for when the backend grows a body), i18n not wired in this Nuxt app, no frontend test suite exists. | **✓ ↑** (KIP1e, this PR) | `aidocs/66 §7` / `aidocs/16 KIP1e` |
| **Feature B — hero image URL on Collections** | n/a | **✓ shipped** — optional `heroImageUrl` (nullable String) on `Collection` entity. `CollectionIO` read DTO exposes the field; `CollectionService` persists it on create and update. `PATCH /v2/collections/{appId}` (RFC 7396 merge-patch: `null` clears, absent leaves unchanged). No `/shepard/api/` surface touched — zero upstream impact. Frontend: `<v-img>` hero banner (220px, cover) above the Collection title on the detail page; hidden when `heroImageUrl` is null. Hero URL field in `EditCollectionDialog.vue` (advanced mode only, per basic/advanced policy). TypeScript `Collection` interface updated (`heroImageUrl?: string | null`). 3 unit tests in `CollectionV2RestTest` (set on POST, PATCH null clears, PATCH absent leaves unchanged). `V54__NOOP_heroImageUrl_additive.cypher` documents that no schema migration is needed. | `aidocs/34` Feature B |

| **ST1a — lifecycle `status` field on `DataObject` and `Collection`** | n/a | **shipped** — nullable free-form `String status` on `AbstractDataObject` entity (`Collection` + `DataObject` both extend it). `AbstractDataObjectIO` serialises with `@JsonInclude(NON_NULL)`: field is omitted from JSON when null so upstream API clients see no wire change. Suggested values DRAFT / IN_REVIEW / READY / PUBLISHED / ARCHIVED; any free-form string accepted (no server-side state machine). `CollectionService` + `DataObjectService` propagate the field on create + update. Backend-client `Collection.ts`, `DataObject.ts`, `AbstractDataObject.ts` all declare `status?: string | null`. Frontend: `StatusChip.vue` coloured chip shown next to entity name in `TitleAndMetadataDisplay.vue`; clearable `v-select` in both `EditCollectionDescriptionDialog.vue` and `EditDataObjectDescriptionDialog.vue`. No migration needed — Neo4j adds the property lazily on next write. | **✓ ↑** (ST1a) | `aidocs/34` ST1a |
| **IMP1 — `shepard-plugin-importer` (agentic data-management pipeline)** | n/a | **🚧 IMP1a + IMP1b landed (PRs 1-2 of 7)** — plugin module `plugins/importer/` produces `shepard-plugin-importer-${revision}.jar`. **IMP1a (PR-1)**: `ImporterPluginManifest` + `PluginManifest` SPI hook + 4 manifest tests + docs trio. Wired into `backend/pom.xml`'s default-active `with-plugins` profile + self-excluded from every sibling plugin pom (`aas`, `file-s3`, `git`, `hdf5`, `kip`, `minter-{datacite,epic,local}`, `spatial`, `unhide`, `video`) to keep the build-time circular reference acyclic. **IMP1b (PR-2)**: `importer_run` Postgres table via plugin-supplied Flyway migration `V1.11.0` — the first concrete instance of `aidocs/platform/32`'s broader JobService design (column set deliberately mirrors that §3 kernel so a future generic `Job` entity can adopt this table by renaming the backing class without touching SQL or wire shape). Three new classes in `de.dlr.shepard.plugins.importer.runs`: `ImporterRun` (JPA `@Entity`), `ImporterRunRepository` (Panache repo), `ImporterRunService` (mirrors `JobService` interface method-for-method per aidocs/32 §4). UUID v7 ids minted at submit time. Status enum `PENDING/RUNNING/SUCCEEDED/FAILED/CANCELLED` with `isTerminal()` invariant. Four compound indexes (principal+status, partial-running-progress, partial-terminal-finished, partial-target-collection). Permission boundary at service level (cross-principal row existence not leaked). 28 new unit tests (3 status + 2 entity + 23 service); total 32 tests for the plugin module now. **PR-3..PR-7 queued**: PR-3 extracts `DLRv5Source` from `examples/mffd-showcase/scripts/mffd-dropbox-import.py` v12's `ShepardClient` lines 239-1170; PR-4 the REST surface (`/v2/imports/...`) + scheduler + reaper; PR-5 the `shepard-admin importer …` CLI; PR-6 the frontend with 2s polling; PR-7 the end-to-end testcontainer integration test. Partial-plugin sequence (PR-1..PR-5 backend-only) explicitly authorised by user; row flips 🚧 → ✓ on PR-6 (frontend). | **🚧 ↑** (IMP1a + IMP1b) | `aidocs/34` IMP1a/IMP1b + `aidocs/platform/32 §13` + memory `project_importer_plugin` |

---

## Cross-references

- **Companion docs:** `aidocs/34` (admin-facing upgrade path), `aidocs/16` (live backlog), `aidocs/42` (researcher-facing vision), `aidocs/00-index` (full design corpus index).
- **Standing rules** in `CLAUDE.md`: API-version policy, vision-currency, upstream-upgrade-path tracking, this matrix.
| **`GET /v2/projects` — optional `page`/`pageSize` pagination + `X-Total-Count` header** (`APISIMP-PROJECTS-LIST-NO-PAGINATION`) | ✓ shipped (fire-219, 2026-06-25) | `aidocs/34` APISIMP-PROJECTS-LIST-NO-PAGINATION row; `aidocs/16` APISIMP-PROJECTS-LIST-NO-PAGINATION |
| **`GET /v2/collections/{collectionAppId}/watched-containers` — optional `page`/`pageSize` pagination + `X-Total-Count` header** (`APISIMP-WATCHES-LIST-NO-PAGINATION`) | ✓ shipped (fire-220, 2026-06-25) | `aidocs/34` APISIMP-WATCHES-LIST-NO-PAGINATION row; `aidocs/16` APISIMP-WATCHES-LIST-NO-PAGINATION |
| **`GET /v2/collections/{collectionAppId}/watches` — optional `page`/`pageSize` pagination + `X-Total-Count` header** (`APISIMP-PAGINATION-LIST-WATCHERS`) | ✓ shipped (fire-222, 2026-06-25) | `aidocs/34` APISIMP-PAGINATION-LIST-WATCHERS row; `aidocs/16` APISIMP-PAGINATION-LIST-WATCHERS |
| **`GET /v2/collections/{collectionAppId}/dqr` — optional `page`/`pageSize` pagination + `X-Total-Count` header** (`APISIMP-PAGINATION-LIST-DQR`) | ✓ shipped (fire-223, 2026-06-25, 87e2aeb) | `aidocs/34` APISIMP-PAGINATION-LIST-DQR row; `aidocs/16` APISIMP-PAGINATION-LIST-DQR |
| **`GET /v2/admin/semantic/git-sources` — optional `page`/`pageSize` pagination + `X-Total-Count` header** (`APISIMP-PAGINATION-LIST-GIT-SOURCES`) | ✓ shipped (fire-224, 2026-06-25, b298c8de) | `aidocs/34` APISIMP-PAGINATION-LIST-GIT-SOURCES row; `aidocs/16` APISIMP-PAGINATION-LIST-GIT-SOURCES |
| **New `GET /v2/publications?entityAppId={appId}` flat alias; `GET /v2/{kind}/{appId}/publications` deprecated in OpenAPI** (`APISIMP-PUBLICATIONS-KIND-PATH-SEGMENT`) | ✓ shipped (fire-225, 2026-06-25, 3749ac657) | `aidocs/34` APISIMP-PUBLICATIONS-KIND-PATH-SEGMENT row; `aidocs/16` APISIMP-PUBLICATIONS-KIND-PATH-SEGMENT |
| **`GET /v2/templates` response wrapped in `PagedResponseIO` envelope; `?page`/`?pageSize` params added; `ShepardTemplateDAO` gains paginated overload** (`APISIMP-TEMPLATES-LIST-UNBOUNDED`) | none (fork-only `/v2/` endpoint) | ✓ shipped (fire-277, PR #2143, fa3e3ed1) — `ShepardTemplateRest.list()` returns `{items, total, page, pageSize}` instead of plain array; `count()` + 4-arg `list()` DAO overloads; `backend-client` `PagedResponseShepardTemplate` model; 5 FE callers migrated to `.items`; 6 new JUnit tests | `aidocs/34` APISIMP-TEMPLATES-LIST-UNBOUNDED row; `aidocs/16` APISIMP-TEMPLATES-LIST-UNBOUNDED |
| **Collection-templates endpoints paginated: `GET /v2/collections/{appId}/templates/allowed` and `GET /v2/collections/{appId}/templates/used` now return `PagedResponseIO` envelope with `?page`/`?pageSize`; `PUT /v2/collections/{appId}/templates/allowed` returns 204 No Content; `backend-client` updated** (`APISIMP-COLLECTION-TEMPLATES`) | none (fork-only `/v2/` endpoint) | ✓ shipped (fire-278, PR #2144, 93eb6ac0) — `ShepardTemplateDAO` gains 4 new overloads (`countAllowedForCollection`, `listAllowedForCollection(page,pageSize)`, `countUsedByCollection`, `listUsedByCollection(page,pageSize)`); PUT allowed returns 204; `backend-client` `AllowedTemplates` model + `listAllowedTemplates`/`listUsedTemplates` return `PagedResponseShepardTemplate`; `CreateDataObjectDialog.vue` reads `.items` | `aidocs/34` APISIMP-COLLECTION-TEMPLATES row; `aidocs/16` APISIMP-COLLECTION-TEMPLATES |
| **`GET /v2/bundles/{appId}/groups` response wrapped in `PagedResponseIO` envelope; `?page`/`?pageSize` params added; `FileGroupDAO` gains paginated overload + `countByBundleAppId()`; `FileGroupService` delegates; `backend-client` `PagedResponseFileGroup` model** (`APISIMP-UNBOUNDED-FILEGROUPS`) | none (fork-only `/v2/` endpoint) | ✓ shipped (fire-279, PR #2145, 7364ece55) — `FileBundleReferenceRest.listGroups()` returns `{items, total, page, pageSize}` instead of plain array; `FileGroupDAO.countByBundleAppId()` + paginated `findByBundleAppId(page,pageSize)` overload; `FileGroupService.countGroups()` + `listGroups()` delegates; `backend-client` `PagedResponseFileGroup` model + `FileBundlesApi` updated; 50 unit tests green | `aidocs/34` APISIMP-UNBOUNDED-FILEGROUPS row; `aidocs/16` APISIMP-UNBOUNDED-FILEGROUPS |
| **`GET /v2/search` — new appId-keyed full-text search endpoint + frontend migration** (`MISSING-V2-APPID-IN-SEARCH`) | none (v1 `POST /shepard/api/search` returns numeric Neo4j ids only) | ✓ shipped backend fire-226 (PR #2096) + frontend fire-227: `SearchV2ItemIO.parentCollectionAppId`, `useGlobalSearch` → `GET /v2/search`, `HeaderBar.vue` drops collection-resolver from search path | `aidocs/34` MISSING-V2-APPID-IN-SEARCH row; `aidocs/16` MISSING-V2-APPID-IN-SEARCH |
| `POST /v2/references?kind=structured-data`, `GET\|PATCH\|DELETE /v2/references/{appId}`, `GET /v2/references?kind=structured-data&dataObjectAppId=<uuid>` — `StructuredDataReferenceKindHandler` now registered in the CDI `ReferenceKindHandler` registry; `StructuredDataReferenceDAO.findByAppId(String)` added; v1 paths **unchanged** | APISIMP-STRUCTURED-DATA-KIND slice 1 — 🚧 PR open (fire-94) | `aidocs/16` APISIMP-STRUCTURED-DATA-KIND; `aidocs/34` APISIMP-STRUCTURED-DATA-KIND row |
| `POST /v2/references?kind=structured-data`, `GET\|PATCH\|DELETE /v2/references/{appId}`, `GET /v2/references?kind=structured-data&dataObjectAppId=<uuid>` — `StructuredDataReferenceKindHandler` registered; `StructuredDataReferenceDAO.findByAppId(String)` added; v1 paths **unchanged**. **Slice 2:** `PUT /v2/references/{appId}/content` now accepts a JSON body, stores it in MongoDB via the container, and links the OID to the reference. | APISIMP-STRUCTURED-DATA-KIND slice 1 — 🚧 PR open (fire-94); slice 2 — 🚧 PR open (fire-95) | `aidocs/16` APISIMP-STRUCTURED-DATA-KIND; `aidocs/34` APISIMP-STRUCTURED-DATA-KIND row |
| **v2 surface convergence** — minimalist core: `?kind=` generic references/containers, `fileKind` subtypes on FileReference (krl/svdx/otvis/urdf/xit/video — video files play inline via `VideoPlayer`, MP4-PROMOTE-VIDEO 2026-07-10, `V119` backfill), templates-as-SHACL-shapes (JSON DSL→compiler), `MAPPING_RECIPE` kind (scene-graph + KRL dissolve), content-negotiated `/v2/shapes/render`, admin `ConfigRegistry`, plugin-namespace allowlist (Jupyter/AAS) with OpenAPI disable | n/a (fork-only architecture) | design doc landed; **B2 shipped** (`ccc90021c` — SHACL validation at template instantiation, 422 on violation); **B3 shipped 2026-06-03** (`MAPPING_RECIPE` kind + `TransformExecutor` SPI + `POST /v2/mappings/{appId}/materialize` + identity default); **A4 shipped 2026-06-03** (generic `GET\|PATCH /v2/admin/config/{feature}` + `ConfigDescriptor` SPI + `ConfigRegistry`; bespoke `*ConfigRest` for semantic/jupyter/sql-timeseries/ror deleted); **B4 shipped 2026-06-04** (scene-graph dissolved into a MAPPING_RECIPE view-shape — `SceneGraphPlayShape` IRI + `SceneGraphPlayTransformExecutor` in `plugins/vis-trace3d`; `/v2/scene-graphs/*` namespace + `:DigitalTwinScene`/`:CoordinateFrame`/`:Joint` graph + `scene_graph_*` MCP tools DELETED; migration V111; Collection hero link repointed to a MAPPING_RECIPE template appId); **A5 shipped 2026-06-04** (`RestNamespaceContributor` marker on `PluginManifest` + `RestNamespaceRegistry` + `DisabledNamespaceRequestFilter` 404s a disabled plugin/feature's `/v2/<prefix>/*` + `DisabledNamespaceOasFilter` strips it from `/shepard/doc/openapi/v2.json`; allowlist = AAS `/v2/aas` (plugin-gated) + Jupyter `/v2/jupyter` (feature-gated); data-driven, fail-soft); **B5 shipped 2026-06-04** (KRL interpret dissolved into a MAPPING_RECIPE transform-shape — `KrlTrajectoryShape` IRI + **in-tree** `KrlTrajectoryTransformExecutor` returning a `TransformResult.reference` derived TimeseriesReference; `POST /v2/krl/interpret` + `:KrlInterpretActivity` entity + bespoke FE DELETED; sidecar client reused/relocated; migration V112 relabels `:KrlInterpretActivity`→`:KrlTrajectoryActivity`; in-context "Interpret as joint trajectory" button on `.src`/`.krl` FileRef detail); **A2 shipped 2026-06-04** (unified `/v2/references?kind=…` create/get/patch/delete/list via a `ReferenceKindHandler` SPI — chosen over static delegation because video/git/hdf reference REST live in plugin modules core can't import; core file/timeseries/uri handlers in-tree; `ReferenceV2IO` = `kind`+`referenceShape`+`fileKind`+per-kind `payload`; nullable `fileKind` on FileReference; PATCH-only `timeseries-references`+`uri-references` DELETED; plugin reference handlers + their per-kind CRUD deletion deferred to `PLUGIN-REF-HANDLER-*`); **A3 shipped 2026-06-04** (unified `/v2/containers?kind=…` create/get/patch/delete/list via a `ContainerKindHandler` SPI — sibling of A2; core file/timeseries/structured-data handlers in-tree delegating to the existing container services; `ContainerV2IO` = `kind`+per-kind read-only `payload`; **purely additive — NO path deleted** because container CRUD only ever lived on the frozen v1 surface, so A3 ships the FIRST appId-keyed `/v2/` container CRUD alongside it; every `/v2/…-containers/…` special-op kept; hdf handler deferred to `PLUGIN-CONTAINER-HANDLER-HDF`); **CONTAINER-PERMS-V2 shipped 2026-06-04** (`GET\|PATCH /v2/containers/{appId}/permissions` on `ContainersV2Rest`, Manage-gated, RFC 7396 merge-patch; 10 new JUnit tests; frontend create composables repointed from v1 `*ContainerApi` to the new v2 path — closes the last v1 fallback in the container create flow); **B6 shipped 2026-06-04** (visual template editor — thin `POST /v2/shapes/build` wrapping the B1 `ShaclShapeBuilder` (`ShapesBuildRest` + `ShapeBuild{Request,Response}IO`); `TemplateShapeEditor.vue` composes a `ShepardTemplate` SHACL shape by picking predicates from the `/v2/shapes/predicates` + `/v2/semantic/terms/search` palette → property-shape rows → live Turtle preview → `/v2/shapes/validate` round-trip; wired into `AdminTemplateDialog` as a Visual/Raw toggle, stores `editorState`+compiled `shapeGraph` on the template body; `useShapePalette`/`useShapeBuilder` composables + pure `utils/templateShapeDsl.ts`; 9 backend tests + Vitest helper tests; polish → `V2CONV-B6-POLISH`); **B7 shipped 2026-06-04** (`PayloadKind.shapeDescriptor()` default method returning `ShapeSpec` (null = opt-out); `KindShapeSeeder` `@ApplicationScoped` StartupEvent observer iterates `ServiceLoader<PayloadKind>`, seeds idempotent `DATAOBJECT_RECIPE` templates tagged `system:kind-shape-seeder` with copy-on-write versioning and admin-edit protection; `UrdfKindShapeExample` in-tree POC; 7 JUnit in `KindShapeSeederTest`); **B8 shipped 2026-06-09** (`PayloadKind.viewShapeDescriptor()` default method returning `ViewRecipeSpec` (null = opt-out); `KindShapeSeeder.seedViewKind()` mirrors `seedKind()` for `VIEW_RECIPE` templates named `<kind>-view-shape`; `ViewRecipeSpec` + `ChannelBindingSpec` records in `de.dlr.shepard.v2.shapes.builder`; 6 new JUnit in `KindShapeSeederTest` (13 total)); **PLUGIN-PERKIND-CRUD-CLEANUP shipped 2026-06-09** (`HdfReferenceRest` deleted; `GitReferenceRest` stripped to preview+check-update; `VideoStreamReferenceV2Rest` stripped to upload+download — per-kind CRUD now fully handled by `ReferenceKindHandler` dispatch via `/v2/references?kind=…`; **B6-SHACLPREFILL ✓ shipped 2026-06-09** (`2b93988a` — `TemplateShapeEditor.vue` `focusAppId` prop + `prefillDataGraph()` on mount + prefill chip/loading/error; `AdminTemplatesPane` reads `?newTemplate=1&targetEntityAppId=` query params on mount; `AdminTemplateDialog` passes `focusAppId` to editor; 9 new Vitest in `templateShapeEditorPrefill.test.ts`)); **A6 shipped 2026-06-04** (`216b61401` — `KrlTrajectoryTransformExecutor` extracted from in-tree into `plugins/krl-interpreter/`; svdx/thermography plugins already standalone; scene-graph dissolved B4; all bespoke core REST verticals gone); **V2-SWEEP-003 slice 1 shipped 2026-06-10** (`ContainerListPage.vue` bulk-delete FILE/TIMESERIES/STRUCTUREDDATA → `safeDeleteContainer`; removes v1 `FileContainerApi`/`TimeseriesContainerApi`/`StructuredDataContainerApi` from the bulk-delete path); **V2-SWEEP-003-2 in-flight 2026-06-10** (fetchPermissions/updatePermissions × 3 container accessors → `GET/PATCH /v2/containers/{appId}/permissions`; dead `useFetchTimeseriesContainer.ts` retired; `appId` forward-filled in `TimeseriesContainer`/`StructuredDataContainer` models; V2-SWEEP-003-3 fetchData deferred to route-appId migration); **A1b shipped 2026-06-10** (additive render-SPI extension: `RenderRequest.params` (E1) + file-rooted dispatch via `shapeIri`+`focusFileRefAppId` with `templateAppId` optional (E2) + `FocusPayloadResolver` SPI seam so ServiceLoader-POJO renderers reach focus bytes (E3) + describe via `params.mode=index` (E4); legacy 4-arg `RenderRequest`/3-arg `ShapesRenderRequestIO` constructors preserved → Trace3D byte-compatible); **A7-THERMO shipped 2026-06-10** (on A1b — `/v2/thermography/*` REST dissolved onto `POST /v2/shapes/render` via `OtvisFrameRenderer` (`OtvisFrameShape` → frame catalogue JSON + heatmap PNG) + `ThermographyHeatmapRenderer` (`ThermographyHeatmapShape` → `application/json` plate-heatmap grid); `ThermographyV2Rest` + `AnalyzeRequestIO` deleted; all 5 FE callers repointed; Re-analyze button removed — analysis is the upload-time file-parse side-effect); **V2-SWEEP-002-USERGROUP-V2 slice-1 in-flight 2026-06-10** (`UserGroupV2Rest` at `/v2/user-groups`, `UserGroupDAO.findByAppId`, `UserGroupV2IO`, 12 unit tests); **V2-SWEEP-002-2 shipped 2026-06-10** (frontend admin user-groups pane migrated to appId-keyed `/v2/user-groups` CRUD + membership via new `useUserGroupsV2`; numeric-id surfaces dropped from list/detail; permissions/roles remain V1-EXCEPTION → V2-SWEEP-002-PERMISSIONS; `tests/unit/useUserGroupsV2.test.ts` 9 tests); **A7-PLUGIN-ADMIN-CONFIG slice 1 shipped 2026-06-10** (`UnhideConfigDescriptor` migrates `GET|PATCH /v2/admin/unhide/config` onto generic `/v2/admin/config/unhide`; harvest-key credential sisters kept at `/v2/admin/unhide/harvest-key/*`; 9 `UnhideConfigDescriptorTest` unit tests; `useUnhideAdminConfig.ts` + CLI + feed error messages repointed; old path 404); **A7-PLUGIN-ADMIN-CONFIG slice 2 shipped 2026-06-10** (`VideoConfigDescriptor` migrates `GET|PATCH /v2/admin/video/config` onto generic `/v2/admin/config/video`; `VideoAdminRest` deleted (config-only class); `VideoAdminPaths.CONFIG` repointed; OGM bug fixed — `VideoPayloadKind.entityPackages()` now registers `de.dlr.shepard.plugins.video.entities` so `VideoConfigDAO` no longer 500s on startup; 8 `VideoConfigDescriptorTest` unit tests; old path 404); **A7-UNHIDE-NS-REVIEW shipped 2026-06-10** (verdict: `/v2/unhide/feed.jsonld` stays outside Tier-3 allowlist — 503-on-disable > 404-on-disable for harvesters; documented in `aidocs/platform/191 §7c`); **A7-PLUGIN-ADMIN-CONFIG slice 4 shipped 2026-06-11** (`AasConfigDescriptor` featureName=`"aas"` + `LegacyV1ConfigDescriptor` featureName=`"legacy-v1"` — `AasConfigAdminRest` + `LegacyV1ConfigAdminRest` deleted; `AdminLegacyV1Pane.vue` repointed from `/v2/admin/legacy/v1/config` to `/v2/admin/config/legacy-v1`; 13+9 unit tests; merged `dbe11f7`); **V2-SWEEP-004-1 in-flight 2026-06-11** (`CollectionReferenceKindHandler` kind=`collection` + `DataObjectReferenceKindHandler` kind=`dataobject` wired into `/v2/references?kind=…` dispatch; frontend migration V2-SWEEP-004-2 queued); **UI-GAP-3 shipped 2026-06-11** (AdminConfigOverviewPane.vue — config registry overview pane; CONFIG_OVERVIEW fragment wired in /admin; bespoke-pane links for semantic/jupyter/unhide/legacy-v1/sql-timeseries); **A7-SVDX shipped 2026-06-12** (bespoke `/v2/svdx/ingest` (`SvdxIngestRest` + `SvdxIngest{Request,Response}IO`) DELETED; the real CSV→TimescaleDB ingest rehomed as `SvdxCsvTransformExecutor` in `plugins/fileformat-svdx` claiming `SvdxCsvIngestShape` IRI, reusing `SvdxCsvIngestionService` verbatim via `CDI.current()`, materialized through `POST /v2/mappings/{appId}/materialize` — mirrors KRL/B5 + URScript; deterministic `svdx-ingest:<svdxId>+<csvId>` idempotency preserved; shape TTL `shapes/svdx-csv-ingest.shacl.ttl`; `SvdxManifestParser` annotate-on-upload untouched; `SvdxCsvTransformExecutorTest` 10 cases; no migration — svdx ingest had no typed `:*Activity`); **APISIMP-STRUCTURED-DATA-KIND slices 1+2 READY (PRs #1974, #1975); slice 3 🚧 in-flight 2026-06-17** (`StructuredDataReferenceKindHandler` in-tree backend + `uploadContent` JSON→MongoDB + FE `useCreateStructuredDataReference.ts` composable + 13 Vitest tests; two-step `POST /v2/references?kind=structured-data&dataObjectAppId=…` + optional `PUT /v2/references/{appId}/content?filename=…`) | ✓ **shipped** | `aidocs/platform/191` / `aidocs/16` V2CONV-* / V2-SWEEP-003 |
| **v2 surface convergence** — minimalist core: `?kind=` generic references/containers, `fileKind` subtypes on FileReference (krl/svdx/otvis/urdf/xit/video — video files play inline via `VideoPlayer`, MP4-PROMOTE-VIDEO 2026-07-10, `V119` backfill), templates-as-SHACL-shapes (JSON DSL→compiler), `MAPPING_RECIPE` kind (scene-graph + KRL dissolve), content-negotiated `/v2/shapes/render`, admin `ConfigRegistry`, plugin-namespace allowlist (Jupyter/AAS) with OpenAPI disable | n/a (fork-only architecture) | design doc landed; **B2 shipped** (`ccc90021c` — SHACL validation at template instantiation, 422 on violation); **B3 shipped 2026-06-03** (`MAPPING_RECIPE` kind + `TransformExecutor` SPI + `POST /v2/mappings/{appId}/materialize` + identity default); **A4 shipped 2026-06-03** (generic `GET\|PATCH /v2/admin/config/{feature}` + `ConfigDescriptor` SPI + `ConfigRegistry`; bespoke `*ConfigRest` for semantic/jupyter/sql-timeseries/ror deleted); **B4 shipped 2026-06-04** (scene-graph dissolved into a MAPPING_RECIPE view-shape — `SceneGraphPlayShape` IRI + `SceneGraphPlayTransformExecutor` in `plugins/vis-trace3d`; `/v2/scene-graphs/*` namespace + `:DigitalTwinScene`/`:CoordinateFrame`/`:Joint` graph + `scene_graph_*` MCP tools DELETED; migration V111; Collection hero link repointed to a MAPPING_RECIPE template appId); **A5 shipped 2026-06-04** (`RestNamespaceContributor` marker on `PluginManifest` + `RestNamespaceRegistry` + `DisabledNamespaceRequestFilter` 404s a disabled plugin/feature's `/v2/<prefix>/*` + `DisabledNamespaceOasFilter` strips it from `/shepard/doc/openapi/v2.json`; allowlist = AAS `/v2/aas` (plugin-gated) + Jupyter `/v2/jupyter` (feature-gated); data-driven, fail-soft); **B5 shipped 2026-06-04** (KRL interpret dissolved into a MAPPING_RECIPE transform-shape — `KrlTrajectoryShape` IRI + **in-tree** `KrlTrajectoryTransformExecutor` returning a `TransformResult.reference` derived TimeseriesReference; `POST /v2/krl/interpret` + `:KrlInterpretActivity` entity + bespoke FE DELETED; sidecar client reused/relocated; migration V112 relabels `:KrlInterpretActivity`→`:KrlTrajectoryActivity`; in-context "Interpret as joint trajectory" button on `.src`/`.krl` FileRef detail); **A2 shipped 2026-06-04** (unified `/v2/references?kind=…` create/get/patch/delete/list via a `ReferenceKindHandler` SPI — chosen over static delegation because video/git/hdf reference REST live in plugin modules core can't import; core file/timeseries/uri handlers in-tree; `ReferenceV2IO` = `kind`+`referenceShape`+`fileKind`+per-kind `payload`; nullable `fileKind` on FileReference; PATCH-only `timeseries-references`+`uri-references` DELETED; plugin reference handlers + their per-kind CRUD deletion deferred to `PLUGIN-REF-HANDLER-*`); **A3 shipped 2026-06-04** (unified `/v2/containers?kind=…` create/get/patch/delete/list via a `ContainerKindHandler` SPI — sibling of A2; core file/timeseries/structured-data handlers in-tree delegating to the existing container services; `ContainerV2IO` = `kind`+per-kind read-only `payload`; **purely additive — NO path deleted** because container CRUD only ever lived on the frozen v1 surface, so A3 ships the FIRST appId-keyed `/v2/` container CRUD alongside it; every `/v2/…-containers/…` special-op kept; hdf handler deferred to `PLUGIN-CONTAINER-HANDLER-HDF`); **CONTAINER-PERMS-V2 shipped 2026-06-04** (`GET\|PATCH /v2/containers/{appId}/permissions` on `ContainersV2Rest`, Manage-gated, RFC 7396 merge-patch; 10 new JUnit tests; frontend create composables repointed from v1 `*ContainerApi` to the new v2 path — closes the last v1 fallback in the container create flow); **B6 shipped 2026-06-04** (visual template editor — thin `POST /v2/shapes/build` wrapping the B1 `ShaclShapeBuilder` (`ShapesBuildRest` + `ShapeBuild{Request,Response}IO`); `TemplateShapeEditor.vue` composes a `ShepardTemplate` SHACL shape by picking predicates from the `/v2/shapes/predicates` + `/v2/semantic/terms/search` palette → property-shape rows → live Turtle preview → `/v2/shapes/validate` round-trip; wired into `AdminTemplateDialog` as a Visual/Raw toggle, stores `editorState`+compiled `shapeGraph` on the template body; `useShapePalette`/`useShapeBuilder` composables + pure `utils/templateShapeDsl.ts`; 9 backend tests + Vitest helper tests; polish → `V2CONV-B6-POLISH`); **B7 shipped 2026-06-04** (`PayloadKind.shapeDescriptor()` default method returning `ShapeSpec` (null = opt-out); `KindShapeSeeder` `@ApplicationScoped` StartupEvent observer iterates `ServiceLoader<PayloadKind>`, seeds idempotent `DATAOBJECT_RECIPE` templates tagged `system:kind-shape-seeder` with copy-on-write versioning and admin-edit protection; `UrdfKindShapeExample` in-tree POC; 7 JUnit in `KindShapeSeederTest`); **B8 shipped 2026-06-09** (`PayloadKind.viewShapeDescriptor()` default method returning `ViewRecipeSpec` (null = opt-out); `KindShapeSeeder.seedViewKind()` mirrors `seedKind()` for `VIEW_RECIPE` templates named `<kind>-view-shape`; `ViewRecipeSpec` + `ChannelBindingSpec` records in `de.dlr.shepard.v2.shapes.builder`; 6 new JUnit in `KindShapeSeederTest` (13 total)); **PLUGIN-PERKIND-CRUD-CLEANUP shipped 2026-06-09** (`HdfReferenceRest` deleted; `GitReferenceRest` stripped to preview+check-update; `VideoStreamReferenceV2Rest` stripped to upload+download — per-kind CRUD now fully handled by `ReferenceKindHandler` dispatch via `/v2/references?kind=…`; **B6-SHACLPREFILL ✓ shipped 2026-06-09** (`2b93988a` — `TemplateShapeEditor.vue` `focusAppId` prop + `prefillDataGraph()` on mount + prefill chip/loading/error; `AdminTemplatesPane` reads `?newTemplate=1&targetEntityAppId=` query params on mount; `AdminTemplateDialog` passes `focusAppId` to editor; 9 new Vitest in `templateShapeEditorPrefill.test.ts`)); **A6 shipped 2026-06-04** (`216b61401` — `KrlTrajectoryTransformExecutor` extracted from in-tree into `plugins/krl-interpreter/`; svdx/thermography plugins already standalone; scene-graph dissolved B4; all bespoke core REST verticals gone); **V2-SWEEP-003 slice 1 shipped 2026-06-10** (`ContainerListPage.vue` bulk-delete FILE/TIMESERIES/STRUCTUREDDATA → `safeDeleteContainer`; removes v1 `FileContainerApi`/`TimeseriesContainerApi`/`StructuredDataContainerApi` from the bulk-delete path); **V2-SWEEP-003-2 in-flight 2026-06-10** (fetchPermissions/updatePermissions × 3 container accessors → `GET/PATCH /v2/containers/{appId}/permissions`; dead `useFetchTimeseriesContainer.ts` retired; `appId` forward-filled in `TimeseriesContainer`/`StructuredDataContainer` models; V2-SWEEP-003-3 fetchData deferred to route-appId migration); **A1b shipped 2026-06-10** (additive render-SPI extension: `RenderRequest.params` (E1) + file-rooted dispatch via `shapeIri`+`focusFileRefAppId` with `templateAppId` optional (E2) + `FocusPayloadResolver` SPI seam so ServiceLoader-POJO renderers reach focus bytes (E3) + describe via `params.mode=index` (E4); legacy 4-arg `RenderRequest`/3-arg `ShapesRenderRequestIO` constructors preserved → Trace3D byte-compatible); **A7-THERMO shipped 2026-06-10** (on A1b — `/v2/thermography/*` REST dissolved onto `POST /v2/shapes/render` via `OtvisFrameRenderer` (`OtvisFrameShape` → frame catalogue JSON + heatmap PNG) + `ThermographyHeatmapRenderer` (`ThermographyHeatmapShape` → `application/json` plate-heatmap grid); `ThermographyV2Rest` + `AnalyzeRequestIO` deleted; all 5 FE callers repointed; Re-analyze button removed — analysis is the upload-time file-parse side-effect); **V2-SWEEP-002-USERGROUP-V2 slice-1 in-flight 2026-06-10** (`UserGroupV2Rest` at `/v2/user-groups`, `UserGroupDAO.findByAppId`, `UserGroupV2IO`, 12 unit tests); **V2-SWEEP-002-2 shipped 2026-06-10** (frontend admin user-groups pane migrated to appId-keyed `/v2/user-groups` CRUD + membership via new `useUserGroupsV2`; numeric-id surfaces dropped from list/detail; permissions/roles remain V1-EXCEPTION → V2-SWEEP-002-PERMISSIONS; `tests/unit/useUserGroupsV2.test.ts` 9 tests); **A7-PLUGIN-ADMIN-CONFIG slice 1 shipped 2026-06-10** (`UnhideConfigDescriptor` migrates `GET|PATCH /v2/admin/unhide/config` onto generic `/v2/admin/config/unhide`; harvest-key credential sisters kept at `/v2/admin/unhide/harvest-key/*`; 9 `UnhideConfigDescriptorTest` unit tests; `useUnhideAdminConfig.ts` + CLI + feed error messages repointed; old path 404); **A7-PLUGIN-ADMIN-CONFIG slice 2 shipped 2026-06-10** (`VideoConfigDescriptor` migrates `GET|PATCH /v2/admin/video/config` onto generic `/v2/admin/config/video`; `VideoAdminRest` deleted (config-only class); `VideoAdminPaths.CONFIG` repointed; OGM bug fixed — `VideoPayloadKind.entityPackages()` now registers `de.dlr.shepard.plugins.video.entities` so `VideoConfigDAO` no longer 500s on startup; 8 `VideoConfigDescriptorTest` unit tests; old path 404); **A7-UNHIDE-NS-REVIEW shipped 2026-06-10** (verdict: `/v2/unhide/feed.jsonld` stays outside Tier-3 allowlist — 503-on-disable > 404-on-disable for harvesters; documented in `aidocs/platform/191 §7c`); **A7-PLUGIN-ADMIN-CONFIG slice 4 shipped 2026-06-11** (`AasConfigDescriptor` featureName=`"aas"` + `LegacyV1ConfigDescriptor` featureName=`"legacy-v1"` — `AasConfigAdminRest` + `LegacyV1ConfigAdminRest` deleted; `AdminLegacyV1Pane.vue` repointed from `/v2/admin/legacy/v1/config` to `/v2/admin/config/legacy-v1`; 13+9 unit tests; merged `dbe11f7`); **V2-SWEEP-004-1 in-flight 2026-06-11** (`CollectionReferenceKindHandler` kind=`collection` + `DataObjectReferenceKindHandler` kind=`dataobject` wired into `/v2/references?kind=…` dispatch; frontend migration V2-SWEEP-004-2 queued); **UI-GAP-3 shipped 2026-06-11** (AdminConfigOverviewPane.vue — config registry overview pane; CONFIG_OVERVIEW fragment wired in /admin; bespoke-pane links for semantic/jupyter/unhide/legacy-v1/sql-timeseries); **A7-SVDX shipped 2026-06-12** (bespoke `/v2/svdx/ingest` (`SvdxIngestRest` + `SvdxIngest{Request,Response}IO`) DELETED; the real CSV→TimescaleDB ingest rehomed as `SvdxCsvTransformExecutor` in `plugins/fileformat-svdx` claiming `SvdxCsvIngestShape` IRI, reusing `SvdxCsvIngestionService` verbatim via `CDI.current()`, materialized through `POST /v2/mappings/{appId}/materialize` — mirrors KRL/B5 + URScript; deterministic `svdx-ingest:<svdxId>+<csvId>` idempotency preserved; shape TTL `shapes/svdx-csv-ingest.shacl.ttl`; `SvdxManifestParser` annotate-on-upload untouched; `SvdxCsvTransformExecutorTest` 10 cases; no migration — svdx ingest had no typed `:*Activity`) | ✓ **shipped** | `aidocs/platform/191` / `aidocs/16` V2CONV-* / V2-SWEEP-003 |
| Git integration (`GitReference`); mode-a loose link + mode-b tracked artifact with inline preview; mode-c pinned snapshot; RO-Crate export; host adapters for GitLab + GitHub + Gitea; CRUD via `/v2/references?kind=git`; UI renders loose-link as clickable link; "Download as RO-Crate" button on collection detail page (UI8); **APISIMP-GIT-REF-PATH (2026-06-16, fire-84)**: preview (`GET /v2/references/{appId}/preview`) and check-update (`POST /v2/references/{appId}/check-update`) relocated from per-DataObject namespace to unified reference surface; old paths return 410 Gone | none | Backend ✓ (G1a / G1b / G1c / G1d); UI ✓ (G1-cred UI, Git refs pane, RO-Crate download button); APISIMP-GIT-REF-PATH ✓ | **✓ ↑ (backend, G1a + G1b + G1c + G1d)** / **✓ UI (RO-Crate download button, UI8)** | `aidocs/38` + `aidocs/63` ADR-0021 |
| `GET /v2/data-objects/{doAppId}/git-references/{appId}/preview` and `POST …/{appId}/check-update` — action sub-paths relocated to unified reference surface: `GET /v2/references/{appId}/preview` and `POST /v2/references/{appId}/check-update`; old paths return 410 Gone; `GitReferenceActionsRest` is the new carrier; 11 new JUnit tests. | APISIMP-GIT-REF-PATH — 🔄 in-flight (fire-84) | `aidocs/16` APISIMP-GIT-REF-PATH |
| **`GitReferenceIO.resolvedAtMillis` (epoch-ms `Long`) → `resolvedAt` (ISO 8601 `String`); `CheckUpdateResultIO.checkedAtMillis` (epoch-ms `long`) → `checkedAt` (ISO 8601 `String`); `GitReferenceKindHandler.toIO()` + `GitReferenceService.checkForUpdate()` use `DateTimeFormatter.ISO_INSTANT`; TypeScript client `GitReference.resolvedAt: string` + `CheckUpdateResultIO.checkedAt: string`** (`APISIMP-GIT-REFERENCE-EPOCH-MS-TO-ISO`) | none (fork-only `/v2/` plugin surface) | 🔄 in-flight (fire-584, PR open) | `aidocs/16` APISIMP-GIT-REFERENCE-EPOCH-MS-TO-ISO |
| **`DataObjectV2Rest` three early-return paths (unknown parent/predecessor/successor appId) gain `Cache-Control: max-age=300, must-revalidate` + `X-Shepard-Payload-Diet: default-trim` headers via `emptyListResponse()` private static helper; consistent with normal list path headers** (`APISIMP-DO-EMPTY-LIST-ENVELOPE`) | none (fork-only `/v2/` surface; `/shepard/api/` v1 untouched) | 🔄 in-flight (fire-585, PR open) — `emptyListResponse()` helper consolidates 3 inline `Response.ok("[]")` sites; 80 JUnit tests pass (`DataObjectV2RestTest`) | `aidocs/16` APISIMP-DO-EMPTY-LIST-ENVELOPE |
| **`GET /v2/semantic/vocabularies` + `…/used-by/{appId}` drop fake `PagedResponseIO` envelope; `GET …/{vocabId}/predicates` gains real `?page=`/`?pageSize=` pagination with JSR-380 caps** (`APISIMP-VOCAB-BROWSE-FAKE-PAGED`) | none (fork-only `/v2/` endpoint) | ✓ shipped (fire-364, branch `APISIMP-VOCAB-BROWSE-FAKE-PAGED-drop-fake-paged`) — `listVocabularies()` and `listVocabulariesUsedBy()` return plain arrays; `listPredicatesForVocabulary()` accepts `?page=0&pageSize=50` (max 200) with `@PositiveOrZero`/`@Min`/`@Max`; `VocabularyBrowseRestTest` updated + 2 new tests; `useSemanticVocabularyPredicates.ts` + Vitest tests updated | `aidocs/34` APISIMP-VOCAB-BROWSE-FAKE-PAGED row; `aidocs/16` APISIMP-VOCAB-BROWSE-FAKE-PAGED |
| **`POST /v2/collections/{collectionAppId}/dqr/evaluate` gains `?limit=` cap (default 5 000, max 5 000) and returns `DQRResultsIO` envelope `{results, truncated, total}`** (`APISIMP-DQR-EVALUATE-BARE-LIST`) | none (fork-only `/v2/` endpoint; TPL10) | ✓ shipped (fire-371, branch `APISIMP-DQR-EVALUATE-BARE-LIST`) — new `DQRResultsIO` record in `de.dlr.shepard.v2.quality.io`; `CollectionDQRRest.evaluate()` adds `@QueryParam("limit") @DefaultValue("5000") @Min(1) @Max(5000) int limit`; slices and sets `truncated`; 5 new JUnit tests in `CollectionDQRRestTest` | `aidocs/34` APISIMP-DQR-EVALUATE-BARE-LIST row; `aidocs/16` APISIMP-DQR-EVALUATE-BARE-LIST |
| **`GET /v2/collections/{collectionAppId}/dqr` — in-memory `subList` removed; `DataQualityRequirementDAO` gains `findByCollectionAppId(String, int, int)` + `countByCollectionAppId(String)` with Cypher SKIP/LIMIT; `DataQualityRequirementService.list()` signature changed to `(collectionAppId, caller, page, pageSize)` returning `PagedResponseIO<DQRIO>`** (`APISIMP-DQR-LIST-IN-MEMORY-PAGING`) | none (fork-only `/v2/` endpoint; TPL10) | ✓ shipped (fire-439, branch `APISIMP-DQR-LIST-IN-MEMORY-PAGING-1`) — wire shape unchanged; `page`/`pageSize`/`X-Total-Count`/`PagedResponseIO` envelope pre-existed; only pagination is now DB-side instead of in-memory | `aidocs/34` APISIMP-DQR-LIST-IN-MEMORY-PAGING row; `aidocs/16` APISIMP-DQR-LIST-IN-MEMORY-PAGING |
| **`GET /v2/timeseries-containers/{appId}/channels/{channelShepardId}/annotations` and `GET /v2/timeseries-containers/{appId}/temporal-annotations` — in-memory `subList` removed; `AnnotatableTimeseriesDAO` gains `countAnnotationsByAppId` + `findAnnotationsByAppId(skip,limit)` Cypher SKIP/LIMIT; `TimeseriesAnnotationDAO` gains `countByContainerId` + `findByContainerId(skip,limit)` Cypher SKIP/LIMIT; `AnnotatableTimeseriesService` adds paged channel-annotation methods; unbounded variants `@Deprecated` (kept for MCP tools)** (`APISIMP-TS-CONT-ANNOT-IN-MEMORY-PAGING`) | none (fork-only `/v2/` endpoint) | 🚧 in-progress (fire-441, PR #2360, branch `APISIMP-TS-CONT-ANNOT-IN-MEMORY-PAGING-1`) — wire shape unchanged; `page`/`pageSize`/`X-Total-Count`/`PagedResponseIO` envelope pre-existed; paging is now DB-side | `aidocs/34` APISIMP-TS-CONT-ANNOT-IN-MEMORY-PAGING row; `aidocs/16` APISIMP-TS-CONT-ANNOT-IN-MEMORY-PAGING |
| **`GET /v2/collections/{collectionAppId}/snapshots/{snapshotAppId}/data-objects` gains `?page=`/`?pageSize=` pagination (default 500, max 2000); `SnapshotDataObjectsIO` gains `totalDataObjects`, `page`, `pageSize` fields; `dataObjectAppIds` is now a sliced page window** (`APISIMP-SNAPSHOT-PINNED-DO-UNCAPPED`) | none (fork-only `/v2/` endpoint) | ✓ shipped (fire-372, PR #2247) — `SnapshotDataObjectsIO` record gains 3 new fields + new 5-arg constructor; `SnapshotPinnedReadRest.getDataObjects()` gains `@QueryParam("page")` + `@QueryParam("pageSize")` with JSR-380 caps (`@PositiveOrZero`, `@Min(1)`, `@Max(2000)`); in-memory `subList()` slicing; 14 unit tests in `SnapshotPinnedReadRestTest` | `aidocs/34` APISIMP-SNAPSHOT-PINNED-DO-UNCAPPED row; `aidocs/16` APISIMP-SNAPSHOT-PINNED-DO-UNCAPPED |
| **`GET /v2/collections/{collectionAppId}/snapshots/{appId}/data-objects` `pageSize` cap lowered `@Max(2000)` → `@Max(200)`, default lowered 500 → 50** (`APISIMP-SNAPPINNED-PAGECAP`) | none (fork-only `/v2/` endpoint; XS APISIMP pagecap) | 🔄 in-flight (fire-543, branch `APISIMP-SNAPPINNED-PAGECAP-1`) — `SnapshotPinnedReadRest.java:133` `@Max(2000)` → `@Max(200)`, `@DefaultValue("500")` → `@DefaultValue("50")`; `@Schema` + `@Operation` description updated; 1 new reflection-based regression test `maxPageSize_isAtMost200`; existing `paginates_beyondLastPage_returnsEmptyList` updated from `pageSize=500` to `pageSize=50` | `aidocs/34` APISIMP-SNAPPINNED-PAGECAP row; `aidocs/16` APISIMP-SNAPPINNED-PAGECAP |
| **`?name=` backward-compat alias removed from `GET /v2/containers`, `GET /v2/collections`, `GET /v2/data-objects/{collectionAppId}/data-objects`; `Deprecation` header no longer emitted; `list_nameParamIsGone` reflection tests added to all three test classes** (`APISIMP-NAME-ALIAS-RETIRE`) | none (fork-only `/v2/` surface; `/shepard/api/` v1 untouched) | ✓ shipped (fire-547, branch `APISIMP-NAME-ALIAS-RETIRE`) — `ContainersV2Rest.list()`, `CollectionV2Rest.list()`, `DataObjectV2Rest.list()` `@Deprecated @QueryParam("name") String nameLegacy` params + `Deprecation: true` header emission removed; method signatures shrunk by 1 arg each; `String filter = q != null ? q : nameLegacy` → `if (q != null) params = params.withName(q)` simplification; 3 `list_nameParamIsGone` reflection tests guard against re-introduction | `aidocs/34` APISIMP-NAME-ALIAS-RETIRE row; `aidocs/16` APISIMP-NAME-ALIAS-RETIRE |
| **Helmholtz Unhide feed filter+sort+pagination pushed to Cypher; `CollectionDAO` gains `findForUnhideFeed(skip,limit)` + `countForUnhideFeed()` with bounded OPTIONAL MATCH query; `UnhideFeedService.buildFeed()` drops in-memory `findAll()`+`subList()` anti-pattern; `CollectionPropertiesDAO` removed from feed path** (`APISIMP-UNHIDE-FEED-IN-MEMORY-PAGING`) | none (internal performance fix; wire shape unchanged) | ✓ shipped (fire-429, PR APISIMP-UNHIDE-FEED-IN-MEMORY-PAGING-1) — `CollectionDAO.findForUnhideFeed(int,int)` + `countForUnhideFeed()` use `OPTIONAL MATCH (c)-[:HAS_PROPERTIES]->(p:CollectionProperties) WITH c,p WHERE p IS NULL OR p.publishToHelmholtzKG <> false ORDER BY c.createdAt ASC, c.appId ASC SKIP $skip LIMIT $limit`; `UnhideFeedService` drops `CollectionPropertiesDAO` injection; 36 `UnhideFeedServiceTest` tests; 133 plugin tests green | `aidocs/16` APISIMP-UNHIDE-FEED-IN-MEMORY-PAGING |
| **v2 surface convergence** — minimalist core: `?kind=` generic references/containers, `fileKind` subtypes on FileReference (krl/svdx/otvis/urdf/xit/video — video files play inline via `VideoPlayer`, MP4-PROMOTE-VIDEO 2026-07-10, `V119` backfill), templates-as-SHACL-shapes (JSON DSL→compiler), `MAPPING_RECIPE` kind (scene-graph + KRL dissolve), content-negotiated `/v2/shapes/render`, admin `ConfigRegistry`, plugin-namespace allowlist (Jupyter/AAS) with OpenAPI disable | n/a (fork-only architecture) | design doc landed; **B2 shipped** (`ccc90021c` — SHACL validation at template instantiation, 422 on violation); **B3 shipped 2026-06-03** (`MAPPING_RECIPE` kind + `TransformExecutor` SPI + `POST /v2/mappings/{appId}/materialize` + identity default); **A4 shipped 2026-06-03** (generic `GET\|PATCH /v2/admin/config/{feature}` + `ConfigDescriptor` SPI + `ConfigRegistry`; bespoke `*ConfigRest` for semantic/jupyter/sql-timeseries/ror deleted); **B4 shipped 2026-06-04** (scene-graph dissolved into a MAPPING_RECIPE view-shape — `SceneGraphPlayShape` IRI + `SceneGraphPlayTransformExecutor` in `plugins/vis-trace3d`; `/v2/scene-graphs/*` namespace + `:DigitalTwinScene`/`:CoordinateFrame`/`:Joint` graph + `scene_graph_*` MCP tools DELETED; migration V111; Collection hero link repointed to a MAPPING_RECIPE template appId); **A5 shipped 2026-06-04** (`RestNamespaceContributor` marker on `PluginManifest` + `RestNamespaceRegistry` + `DisabledNamespaceRequestFilter` 404s a disabled plugin/feature's `/v2/<prefix>/*` + `DisabledNamespaceOasFilter` strips it from `/shepard/doc/openapi/v2.json`; allowlist = AAS `/v2/aas` (plugin-gated) + Jupyter `/v2/jupyter` (feature-gated); data-driven, fail-soft); **B5 shipped 2026-06-04** (KRL interpret dissolved into a MAPPING_RECIPE transform-shape — `KrlTrajectoryShape` IRI + **in-tree** `KrlTrajectoryTransformExecutor` returning a `TransformResult.reference` derived TimeseriesReference; `POST /v2/krl/interpret` + `:KrlInterpretActivity` entity + bespoke FE DELETED; sidecar client reused/relocated; migration V112 relabels `:KrlInterpretActivity`→`:KrlTrajectoryActivity`; in-context "Interpret as joint trajectory" button on `.src`/`.krl` FileRef detail); **A2 shipped 2026-06-04** (unified `/v2/references?kind=…` create/get/patch/delete/list via a `ReferenceKindHandler` SPI — chosen over static delegation because video/git/hdf reference REST live in plugin modules core can't import; core file/timeseries/uri handlers in-tree; `ReferenceV2IO` = `kind`+`referenceShape`+`fileKind`+per-kind `payload`; nullable `fileKind` on FileReference; PATCH-only `timeseries-references`+`uri-references` DELETED; plugin reference handlers + their per-kind CRUD deletion deferred to `PLUGIN-REF-HANDLER-*`); **A3 shipped 2026-06-04** (unified `/v2/containers?kind=…` create/get/patch/delete/list via a `ContainerKindHandler` SPI — sibling of A2; core file/timeseries/structured-data handlers in-tree delegating to the existing container services; `ContainerV2IO` = `kind`+per-kind read-only `payload`; **purely additive — NO path deleted** because container CRUD only ever lived on the frozen v1 surface, so A3 ships the FIRST appId-keyed `/v2/` container CRUD alongside it; every `/v2/…-containers/…` special-op kept; hdf handler deferred to `PLUGIN-CONTAINER-HANDLER-HDF`); **CONTAINER-PERMS-V2 shipped 2026-06-04** (`GET\|PATCH /v2/containers/{appId}/permissions` on `ContainersV2Rest`, Manage-gated, RFC 7396 merge-patch; 10 new JUnit tests; frontend create composables repointed from v1 `*ContainerApi` to the new v2 path — closes the last v1 fallback in the container create flow); **B6 shipped 2026-06-04** (visual template editor — thin `POST /v2/shapes/build` wrapping the B1 `ShaclShapeBuilder` (`ShapesBuildRest` + `ShapeBuild{Request,Response}IO`); `TemplateShapeEditor.vue` composes a `ShepardTemplate` SHACL shape by picking predicates from the `/v2/shapes/predicates` + `/v2/semantic/terms/search` palette → property-shape rows → live Turtle preview → `/v2/shapes/validate` round-trip; wired into `AdminTemplateDialog` as a Visual/Raw toggle, stores `editorState`+compiled `shapeGraph` on the template body; `useShapePalette`/`useShapeBuilder` composables + pure `utils/templateShapeDsl.ts`; 9 backend tests + Vitest helper tests; polish → `V2CONV-B6-POLISH`); **B7 shipped 2026-06-04** (`PayloadKind.shapeDescriptor()` default method returning `ShapeSpec` (null = opt-out); `KindShapeSeeder` `@ApplicationScoped` StartupEvent observer iterates `ServiceLoader<PayloadKind>`, seeds idempotent `DATAOBJECT_RECIPE` templates tagged `system:kind-shape-seeder` with copy-on-write versioning and admin-edit protection; `UrdfKindShapeExample` in-tree POC; 7 JUnit in `KindShapeSeederTest`); **B8 shipped 2026-06-09** (`PayloadKind.viewShapeDescriptor()` default method returning `ViewRecipeSpec` (null = opt-out); `KindShapeSeeder.seedViewKind()` mirrors `seedKind()` for `VIEW_RECIPE` templates named `<kind>-view-shape`; `ViewRecipeSpec` + `ChannelBindingSpec` records in `de.dlr.shepard.v2.shapes.builder`; 6 new JUnit in `KindShapeSeederTest` (13 total)); **PLUGIN-PERKIND-CRUD-CLEANUP shipped 2026-06-09** (`HdfReferenceRest` deleted; `GitReferenceRest` stripped to preview+check-update; `VideoStreamReferenceV2Rest` stripped to upload+download — per-kind CRUD now fully handled by `ReferenceKindHandler` dispatch via `/v2/references?kind=…`; **B6-SHACLPREFILL ✓ shipped 2026-06-09** (`2b93988a` — `TemplateShapeEditor.vue` `focusAppId` prop + `prefillDataGraph()` on mount + prefill chip/loading/error; `AdminTemplatesPane` reads `?newTemplate=1&targetEntityAppId=` query params on mount; `AdminTemplateDialog` passes `focusAppId` to editor; 9 new Vitest in `templateShapeEditorPrefill.test.ts`)); **A6 shipped 2026-06-04** (`216b61401` — `KrlTrajectoryTransformExecutor` extracted from in-tree into `plugins/krl-interpreter/`; svdx/thermography plugins already standalone; scene-graph dissolved B4; all bespoke core REST verticals gone); **V2-SWEEP-003 slice 1 shipped 2026-06-10** (`ContainerListPage.vue` bulk-delete FILE/TIMESERIES/STRUCTUREDDATA → `safeDeleteContainer`; removes v1 `FileContainerApi`/`TimeseriesContainerApi`/`StructuredDataContainerApi` from the bulk-delete path); **V2-SWEEP-003-2 in-flight 2026-06-10** (fetchPermissions/updatePermissions × 3 container accessors → `GET/PATCH /v2/containers/{appId}/permissions`; dead `useFetchTimeseriesContainer.ts` retired; `appId` forward-filled in `TimeseriesContainer`/`StructuredDataContainer` models; V2-SWEEP-003-3 fetchData deferred to route-appId migration); **A1b shipped 2026-06-10** (additive render-SPI extension: `RenderRequest.params` (E1) + file-rooted dispatch via `shapeIri`+`focusFileRefAppId` with `templateAppId` optional (E2) + `FocusPayloadResolver` SPI seam so ServiceLoader-POJO renderers reach focus bytes (E3) + describe via `params.mode=index` (E4); legacy 4-arg `RenderRequest`/3-arg `ShapesRenderRequestIO` constructors preserved → Trace3D byte-compatible); **A7-THERMO shipped 2026-06-10** (on A1b — `/v2/thermography/*` REST dissolved onto `POST /v2/shapes/render` via `OtvisFrameRenderer` (`OtvisFrameShape` → frame catalogue JSON + heatmap PNG) + `ThermographyHeatmapRenderer` (`ThermographyHeatmapShape` → `application/json` plate-heatmap grid); `ThermographyV2Rest` + `AnalyzeRequestIO` deleted; all 5 FE callers repointed; Re-analyze button removed — analysis is the upload-time file-parse side-effect); **V2-SWEEP-002-USERGROUP-V2 slice-1 in-flight 2026-06-10** (`UserGroupV2Rest` at `/v2/user-groups`, `UserGroupDAO.findByAppId`, `UserGroupV2IO`, 12 unit tests); **V2-SWEEP-002-2 shipped 2026-06-10** (frontend admin user-groups pane migrated to appId-keyed `/v2/user-groups` CRUD + membership via new `useUserGroupsV2`; numeric-id surfaces dropped from list/detail; permissions/roles remain V1-EXCEPTION → V2-SWEEP-002-PERMISSIONS; `tests/unit/useUserGroupsV2.test.ts` 9 tests); **A7-PLUGIN-ADMIN-CONFIG slice 1 shipped 2026-06-10** (`UnhideConfigDescriptor` migrates `GET|PATCH /v2/admin/unhide/config` onto generic `/v2/admin/config/unhide`; harvest-key credential sisters kept at `/v2/admin/unhide/harvest-key/*`; 9 `UnhideConfigDescriptorTest` unit tests; `useUnhideAdminConfig.ts` + CLI + feed error messages repointed; old path 404); **A7-PLUGIN-ADMIN-CONFIG slice 2 shipped 2026-06-10** (`VideoConfigDescriptor` migrates `GET|PATCH /v2/admin/video/config` onto generic `/v2/admin/config/video`; `VideoAdminRest` deleted (config-only class); `VideoAdminPaths.CONFIG` repointed; OGM bug fixed — `VideoPayloadKind.entityPackages()` now registers `de.dlr.shepard.plugins.video.entities` so `VideoConfigDAO` no longer 500s on startup; 8 `VideoConfigDescriptorTest` unit tests; old path 404); **A7-UNHIDE-NS-REVIEW shipped 2026-06-10** (verdict: `/v2/unhide/feed.jsonld` stays outside Tier-3 allowlist — 503-on-disable > 404-on-disable for harvesters; documented in `aidocs/platform/191 §7c`); **A7-PLUGIN-ADMIN-CONFIG slice 4 shipped 2026-06-11** (`AasConfigDescriptor` featureName=`"aas"` + `LegacyV1ConfigDescriptor` featureName=`"legacy-v1"` — `AasConfigAdminRest` + `LegacyV1ConfigAdminRest` deleted; `AdminLegacyV1Pane.vue` repointed from `/v2/admin/legacy/v1/config` to `/v2/admin/config/legacy-v1`; 13+9 unit tests; merged `dbe11f7`); **V2-SWEEP-004-1 in-flight 2026-06-11** (`CollectionReferenceKindHandler` kind=`collection` + `DataObjectReferenceKindHandler` kind=`dataobject` wired into `/v2/references?kind=…` dispatch; frontend migration V2-SWEEP-004-2 queued); **UI-GAP-3 shipped 2026-06-11** (AdminConfigOverviewPane.vue — config registry overview pane; CONFIG_OVERVIEW fragment wired in /admin; bespoke-pane links for semantic/jupyter/unhide/legacy-v1/sql-timeseries); **A7-SVDX shipped 2026-06-12** (bespoke `/v2/svdx/ingest` (`SvdxIngestRest` + `SvdxIngest{Request,Response}IO`) DELETED; the real CSV→TimescaleDB ingest rehomed as `SvdxCsvTransformExecutor` in `plugins/fileformat-svdx` claiming `SvdxCsvIngestShape` IRI, reusing `SvdxCsvIngestionService` verbatim via `CDI.current()`, materialized through `POST /v2/mappings/{appId}/materialize` — mirrors KRL/B5 + URScript; deterministic `svdx-ingest:<svdxId>+<csvId>` idempotency preserved; shape TTL `shapes/svdx-csv-ingest.shacl.ttl`; `SvdxManifestParser` annotate-on-upload untouched; `SvdxCsvTransformExecutorTest` 10 cases; no migration — svdx ingest had no typed `:*Activity`) | ✓ **shipped** | `aidocs/platform/191` / `aidocs/16` V2CONV-* / V2-SWEEP-003 |
| **v2 surface convergence** — minimalist core: `?kind=` generic references/containers, `fileKind` subtypes on FileReference (krl/svdx/otvis/urdf/xit/video — video files play inline via `VideoPlayer`, MP4-PROMOTE-VIDEO 2026-07-10, `V119` backfill), templates-as-SHACL-shapes (JSON DSL→compiler), `MAPPING_RECIPE` kind (scene-graph + KRL dissolve), content-negotiated `/v2/shapes/render`, admin `ConfigRegistry`, plugin-namespace allowlist (Jupyter/AAS) with OpenAPI disable | n/a (fork-only architecture) | design doc landed; **B2 shipped** (`ccc90021c` — SHACL validation at template instantiation, 422 on violation); **B3 shipped 2026-06-03** (`MAPPING_RECIPE` kind + `TransformExecutor` SPI + `POST /v2/mappings/{appId}/materialize` + identity default); **A4 shipped 2026-06-03** (generic `GET\|PATCH /v2/admin/config/{feature}` + `ConfigDescriptor` SPI + `ConfigRegistry`; bespoke `*ConfigRest` for semantic/jupyter/sql-timeseries/ror deleted); **B4 shipped 2026-06-04** (scene-graph dissolved into a MAPPING_RECIPE view-shape — `SceneGraphPlayShape` IRI + `SceneGraphPlayTransformExecutor` in `plugins/vis-trace3d`; `/v2/scene-graphs/*` namespace + `:DigitalTwinScene`/`:CoordinateFrame`/`:Joint` graph + `scene_graph_*` MCP tools DELETED; migration V111; Collection hero link repointed to a MAPPING_RECIPE template appId); **A5 shipped 2026-06-04** (`RestNamespaceContributor` marker on `PluginManifest` + `RestNamespaceRegistry` + `DisabledNamespaceRequestFilter` 404s a disabled plugin/feature's `/v2/<prefix>/*` + `DisabledNamespaceOasFilter` strips it from `/shepard/doc/openapi/v2.json`; allowlist = AAS `/v2/aas` (plugin-gated) + Jupyter `/v2/jupyter` (feature-gated); data-driven, fail-soft); **B5 shipped 2026-06-04** (KRL interpret dissolved into a MAPPING_RECIPE transform-shape — `KrlTrajectoryShape` IRI + **in-tree** `KrlTrajectoryTransformExecutor` returning a `TransformResult.reference` derived TimeseriesReference; `POST /v2/krl/interpret` + `:KrlInterpretActivity` entity + bespoke FE DELETED; sidecar client reused/relocated; migration V112 relabels `:KrlInterpretActivity`→`:KrlTrajectoryActivity`; in-context "Interpret as joint trajectory" button on `.src`/`.krl` FileRef detail); **A2 shipped 2026-06-04** (unified `/v2/references?kind=…` create/get/patch/delete/list via a `ReferenceKindHandler` SPI — chosen over static delegation because video/git/hdf reference REST live in plugin modules core can't import; core file/timeseries/uri handlers in-tree; `ReferenceV2IO` = `kind`+`referenceShape`+`fileKind`+per-kind `payload`; nullable `fileKind` on FileReference; PATCH-only `timeseries-references`+`uri-references` DELETED; plugin reference handlers + their per-kind CRUD deletion deferred to `PLUGIN-REF-HANDLER-*`); **A3 shipped 2026-06-04** (unified `/v2/containers?kind=…` create/get/patch/delete/list via a `ContainerKindHandler` SPI — sibling of A2; core file/timeseries/structured-data handlers in-tree delegating to the existing container services; `ContainerV2IO` = `kind`+per-kind read-only `payload`; **purely additive — NO path deleted** because container CRUD only ever lived on the frozen v1 surface, so A3 ships the FIRST appId-keyed `/v2/` container CRUD alongside it; every `/v2/…-containers/…` special-op kept; hdf handler deferred to `PLUGIN-CONTAINER-HANDLER-HDF`); **CONTAINER-PERMS-V2 shipped 2026-06-04** (`GET\|PATCH /v2/containers/{appId}/permissions` on `ContainersV2Rest`, Manage-gated, RFC 7396 merge-patch; 10 new JUnit tests; frontend create composables repointed from v1 `*ContainerApi` to the new v2 path — closes the last v1 fallback in the container create flow); **B6 shipped 2026-06-04** (visual template editor — thin `POST /v2/shapes/build` wrapping the B1 `ShaclShapeBuilder` (`ShapesBuildRest` + `ShapeBuild{Request,Response}IO`); `TemplateShapeEditor.vue` composes a `ShepardTemplate` SHACL shape by picking predicates from the `/v2/shapes/predicates` + `/v2/semantic/terms/search` palette → property-shape rows → live Turtle preview → `/v2/shapes/validate` round-trip; wired into `AdminTemplateDialog` as a Visual/Raw toggle, stores `editorState`+compiled `shapeGraph` on the template body; `useShapePalette`/`useShapeBuilder` composables + pure `utils/templateShapeDsl.ts`; 9 backend tests + Vitest helper tests; polish → `V2CONV-B6-POLISH`); **B7 shipped 2026-06-04** (`PayloadKind.shapeDescriptor()` default method returning `ShapeSpec` (null = opt-out); `KindShapeSeeder` `@ApplicationScoped` StartupEvent observer iterates `ServiceLoader<PayloadKind>`, seeds idempotent `DATAOBJECT_RECIPE` templates tagged `system:kind-shape-seeder` with copy-on-write versioning and admin-edit protection; `UrdfKindShapeExample` in-tree POC; 7 JUnit in `KindShapeSeederTest`); **B8 shipped 2026-06-09** (`PayloadKind.viewShapeDescriptor()` default method returning `ViewRecipeSpec` (null = opt-out); `KindShapeSeeder.seedViewKind()` mirrors `seedKind()` for `VIEW_RECIPE` templates named `<kind>-view-shape`; `ViewRecipeSpec` + `ChannelBindingSpec` records in `de.dlr.shepard.v2.shapes.builder`; 6 new JUnit in `KindShapeSeederTest` (13 total)); **PLUGIN-PERKIND-CRUD-CLEANUP shipped 2026-06-09** (`HdfReferenceRest` deleted; `GitReferenceRest` stripped to preview+check-update; `VideoStreamReferenceV2Rest` stripped to upload+download — per-kind CRUD now fully handled by `ReferenceKindHandler` dispatch via `/v2/references?kind=…`; **APISIMP-VIDEO-STREAMREF-PATH in-flight 2026-06-17 (fire-86)** — final two video-specific paths (`POST /v2/data-objects/{id}/video-stream-references` multipart upload and `GET /v2/data-objects/{id}/video-stream-references/{id}/download`) tombstoned to 410 Gone; video upload migrated to two-step `POST /v2/references?kind=video` + `PUT /v2/references/{appId}/content`; video download migrated to `GET /v2/references/{appId}/content`; `ReferenceKindHandler.downloadContent()` SPI default method added; `VideoStreamReferenceKindHandler` implements `create()` (metadata node) + `uploadContent()` (bytes+ffprobe) + `downloadContent()` (range-aware 206); frontend `VideoStreamReferencesPane.vue` + `useFetchVideoStreamReferences.ts` + `xhrUpload.ts` repointed); **B6-SHACLPREFILL ✓ shipped 2026-06-09** (`2b93988a` — `TemplateShapeEditor.vue` `focusAppId` prop + `prefillDataGraph()` on mount + prefill chip/loading/error; `AdminTemplatesPane` reads `?newTemplate=1&targetEntityAppId=` query params on mount; `AdminTemplateDialog` passes `focusAppId` to editor; 9 new Vitest in `templateShapeEditorPrefill.test.ts`)); **A6 shipped 2026-06-04** (`216b61401` — `KrlTrajectoryTransformExecutor` extracted from in-tree into `plugins/krl-interpreter/`; svdx/thermography plugins already standalone; scene-graph dissolved B4; all bespoke core REST verticals gone); **V2-SWEEP-003 slice 1 shipped 2026-06-10** (`ContainerListPage.vue` bulk-delete FILE/TIMESERIES/STRUCTUREDDATA → `safeDeleteContainer`; removes v1 `FileContainerApi`/`TimeseriesContainerApi`/`StructuredDataContainerApi` from the bulk-delete path); **V2-SWEEP-003-2 in-flight 2026-06-10** (fetchPermissions/updatePermissions × 3 container accessors → `GET/PATCH /v2/containers/{appId}/permissions`; dead `useFetchTimeseriesContainer.ts` retired; `appId` forward-filled in `TimeseriesContainer`/`StructuredDataContainer` models; V2-SWEEP-003-3 fetchData deferred to route-appId migration); **A1b shipped 2026-06-10** (additive render-SPI extension: `RenderRequest.params` (E1) + file-rooted dispatch via `shapeIri`+`focusFileRefAppId` with `templateAppId` optional (E2) + `FocusPayloadResolver` SPI seam so ServiceLoader-POJO renderers reach focus bytes (E3) + describe via `params.mode=index` (E4); legacy 4-arg `RenderRequest`/3-arg `ShapesRenderRequestIO` constructors preserved → Trace3D byte-compatible); **A7-THERMO shipped 2026-06-10** (on A1b — `/v2/thermography/*` REST dissolved onto `POST /v2/shapes/render` via `OtvisFrameRenderer` (`OtvisFrameShape` → frame catalogue JSON + heatmap PNG) + `ThermographyHeatmapRenderer` (`ThermographyHeatmapShape` → `application/json` plate-heatmap grid); `ThermographyV2Rest` + `AnalyzeRequestIO` deleted; all 5 FE callers repointed; Re-analyze button removed — analysis is the upload-time file-parse side-effect); **V2-SWEEP-002-USERGROUP-V2 slice-1 in-flight 2026-06-10** (`UserGroupV2Rest` at `/v2/user-groups`, `UserGroupDAO.findByAppId`, `UserGroupV2IO`, 12 unit tests); **V2-SWEEP-002-2 shipped 2026-06-10** (frontend admin user-groups pane migrated to appId-keyed `/v2/user-groups` CRUD + membership via new `useUserGroupsV2`; numeric-id surfaces dropped from list/detail; permissions/roles remain V1-EXCEPTION → V2-SWEEP-002-PERMISSIONS; `tests/unit/useUserGroupsV2.test.ts` 9 tests); **A7-PLUGIN-ADMIN-CONFIG slice 1 shipped 2026-06-10** (`UnhideConfigDescriptor` migrates `GET|PATCH /v2/admin/unhide/config` onto generic `/v2/admin/config/unhide`; harvest-key credential sisters kept at `/v2/admin/unhide/harvest-key/*`; 9 `UnhideConfigDescriptorTest` unit tests; `useUnhideAdminConfig.ts` + CLI + feed error messages repointed; old path 404); **A7-PLUGIN-ADMIN-CONFIG slice 2 shipped 2026-06-10** (`VideoConfigDescriptor` migrates `GET|PATCH /v2/admin/video/config` onto generic `/v2/admin/config/video`; `VideoAdminRest` deleted (config-only class); `VideoAdminPaths.CONFIG` repointed; OGM bug fixed — `VideoPayloadKind.entityPackages()` now registers `de.dlr.shepard.plugins.video.entities` so `VideoConfigDAO` no longer 500s on startup; 8 `VideoConfigDescriptorTest` unit tests; old path 404); **A7-UNHIDE-NS-REVIEW shipped 2026-06-10** (verdict: `/v2/unhide/feed.jsonld` stays outside Tier-3 allowlist — 503-on-disable > 404-on-disable for harvesters; documented in `aidocs/platform/191 §7c`); **A7-PLUGIN-ADMIN-CONFIG slice 4 shipped 2026-06-11** (`AasConfigDescriptor` featureName=`"aas"` + `LegacyV1ConfigDescriptor` featureName=`"legacy-v1"` — `AasConfigAdminRest` + `LegacyV1ConfigAdminRest` deleted; `AdminLegacyV1Pane.vue` repointed from `/v2/admin/legacy/v1/config` to `/v2/admin/config/legacy-v1`; 13+9 unit tests; merged `dbe11f7`); **V2-SWEEP-004-1 in-flight 2026-06-11** (`CollectionReferenceKindHandler` kind=`collection` + `DataObjectReferenceKindHandler` kind=`dataobject` wired into `/v2/references?kind=…` dispatch; frontend migration V2-SWEEP-004-2 queued); **UI-GAP-3 shipped 2026-06-11** (AdminConfigOverviewPane.vue — config registry overview pane; CONFIG_OVERVIEW fragment wired in /admin; bespoke-pane links for semantic/jupyter/unhide/legacy-v1/sql-timeseries); **A7-SVDX shipped 2026-06-12** (bespoke `/v2/svdx/ingest` (`SvdxIngestRest` + `SvdxIngest{Request,Response}IO`) DELETED; the real CSV→TimescaleDB ingest rehomed as `SvdxCsvTransformExecutor` in `plugins/fileformat-svdx` claiming `SvdxCsvIngestShape` IRI, reusing `SvdxCsvIngestionService` verbatim via `CDI.current()`, materialized through `POST /v2/mappings/{appId}/materialize` — mirrors KRL/B5 + URScript; deterministic `svdx-ingest:<svdxId>+<csvId>` idempotency preserved; shape TTL `shapes/svdx-csv-ingest.shacl.ttl`; `SvdxManifestParser` annotate-on-upload untouched; `SvdxCsvTransformExecutorTest` 10 cases; no migration — svdx ingest had no typed `:*Activity`) | ✓ **shipped** | `aidocs/platform/191` / `aidocs/16` V2CONV-* / V2-SWEEP-003 |
| **v2 surface convergence** — minimalist core: `?kind=` generic references/containers, `fileKind` subtypes on FileReference (krl/svdx/otvis/urdf/xit/video — video files play inline via `VideoPlayer`, MP4-PROMOTE-VIDEO 2026-07-10, `V119` backfill), templates-as-SHACL-shapes (JSON DSL→compiler), `MAPPING_RECIPE` kind (scene-graph + KRL dissolve), content-negotiated `/v2/shapes/render`, admin `ConfigRegistry`, plugin-namespace allowlist (Jupyter/AAS) with OpenAPI disable | n/a (fork-only architecture) | design doc landed; **B2 shipped** (`ccc90021c` — SHACL validation at template instantiation, 422 on violation); **B3 shipped 2026-06-03** (`MAPPING_RECIPE` kind + `TransformExecutor` SPI + `POST /v2/mappings/{appId}/materialize` + identity default); **A4 shipped 2026-06-03** (generic `GET\|PATCH /v2/admin/config/{feature}` + `ConfigDescriptor` SPI + `ConfigRegistry`; bespoke `*ConfigRest` for semantic/jupyter/sql-timeseries/ror deleted); **B4 shipped 2026-06-04** (scene-graph dissolved into a MAPPING_RECIPE view-shape — `SceneGraphPlayShape` IRI + `SceneGraphPlayTransformExecutor` in `plugins/vis-trace3d`; `/v2/scene-graphs/*` namespace + `:DigitalTwinScene`/`:CoordinateFrame`/`:Joint` graph + `scene_graph_*` MCP tools DELETED; migration V111; Collection hero link repointed to a MAPPING_RECIPE template appId); **A5 shipped 2026-06-04** (`RestNamespaceContributor` marker on `PluginManifest` + `RestNamespaceRegistry` + `DisabledNamespaceRequestFilter` 404s a disabled plugin/feature's `/v2/<prefix>/*` + `DisabledNamespaceOasFilter` strips it from `/shepard/doc/openapi/v2.json`; allowlist = AAS `/v2/aas` (plugin-gated) + Jupyter `/v2/jupyter` (feature-gated); data-driven, fail-soft); **B5 shipped 2026-06-04** (KRL interpret dissolved into a MAPPING_RECIPE transform-shape — `KrlTrajectoryShape` IRI + **in-tree** `KrlTrajectoryTransformExecutor` returning a `TransformResult.reference` derived TimeseriesReference; `POST /v2/krl/interpret` + `:KrlInterpretActivity` entity + bespoke FE DELETED; sidecar client reused/relocated; migration V112 relabels `:KrlInterpretActivity`→`:KrlTrajectoryActivity`; in-context "Interpret as joint trajectory" button on `.src`/`.krl` FileRef detail); **A2 shipped 2026-06-04** (unified `/v2/references?kind=…` create/get/patch/delete/list via a `ReferenceKindHandler` SPI — chosen over static delegation because video/git/hdf reference REST live in plugin modules core can't import; core file/timeseries/uri handlers in-tree; `ReferenceV2IO` = `kind`+`referenceShape`+`fileKind`+per-kind `payload`; nullable `fileKind` on FileReference; PATCH-only `timeseries-references`+`uri-references` DELETED; plugin reference handlers + their per-kind CRUD deletion deferred to `PLUGIN-REF-HANDLER-*`); **A3 shipped 2026-06-04** (unified `/v2/containers?kind=…` create/get/patch/delete/list via a `ContainerKindHandler` SPI — sibling of A2; core file/timeseries/structured-data handlers in-tree delegating to the existing container services; `ContainerV2IO` = `kind`+per-kind read-only `payload`; **purely additive — NO path deleted** because container CRUD only ever lived on the frozen v1 surface, so A3 ships the FIRST appId-keyed `/v2/` container CRUD alongside it; every `/v2/…-containers/…` special-op kept; hdf handler deferred to `PLUGIN-CONTAINER-HANDLER-HDF`); **CONTAINER-PERMS-V2 shipped 2026-06-04** (`GET\|PATCH /v2/containers/{appId}/permissions` on `ContainersV2Rest`, Manage-gated, RFC 7396 merge-patch; 10 new JUnit tests; frontend create composables repointed from v1 `*ContainerApi` to the new v2 path — closes the last v1 fallback in the container create flow); **B6 shipped 2026-06-04** (visual template editor — thin `POST /v2/shapes/build` wrapping the B1 `ShaclShapeBuilder` (`ShapesBuildRest` + `ShapeBuild{Request,Response}IO`); `TemplateShapeEditor.vue` composes a `ShepardTemplate` SHACL shape by picking predicates from the `/v2/shapes/predicates` + `/v2/semantic/terms/search` palette → property-shape rows → live Turtle preview → `/v2/shapes/validate` round-trip; wired into `AdminTemplateDialog` as a Visual/Raw toggle, stores `editorState`+compiled `shapeGraph` on the template body; `useShapePalette`/`useShapeBuilder` composables + pure `utils/templateShapeDsl.ts`; 9 backend tests + Vitest helper tests; polish → `V2CONV-B6-POLISH`); **B7 shipped 2026-06-04** (`PayloadKind.shapeDescriptor()` default method returning `ShapeSpec` (null = opt-out); `KindShapeSeeder` `@ApplicationScoped` StartupEvent observer iterates `ServiceLoader<PayloadKind>`, seeds idempotent `DATAOBJECT_RECIPE` templates tagged `system:kind-shape-seeder` with copy-on-write versioning and admin-edit protection; `UrdfKindShapeExample` in-tree POC; 7 JUnit in `KindShapeSeederTest`); **B8 shipped 2026-06-09** (`PayloadKind.viewShapeDescriptor()` default method returning `ViewRecipeSpec` (null = opt-out); `KindShapeSeeder.seedViewKind()` mirrors `seedKind()` for `VIEW_RECIPE` templates named `<kind>-view-shape`; `ViewRecipeSpec` + `ChannelBindingSpec` records in `de.dlr.shepard.v2.shapes.builder`; 6 new JUnit in `KindShapeSeederTest` (13 total)); **PLUGIN-PERKIND-CRUD-CLEANUP shipped 2026-06-09** (`HdfReferenceRest` deleted; `GitReferenceRest` stripped to preview+check-update; `VideoStreamReferenceV2Rest` stripped to upload+download — per-kind CRUD now fully handled by `ReferenceKindHandler` dispatch via `/v2/references?kind=…`; **B6-SHACLPREFILL ✓ shipped 2026-06-09** (`2b93988a` — `TemplateShapeEditor.vue` `focusAppId` prop + `prefillDataGraph()` on mount + prefill chip/loading/error; `AdminTemplatesPane` reads `?newTemplate=1&targetEntityAppId=` query params on mount; `AdminTemplateDialog` passes `focusAppId` to editor; 9 new Vitest in `templateShapeEditorPrefill.test.ts`)); **A6 shipped 2026-06-04** (`216b61401` — `KrlTrajectoryTransformExecutor` extracted from in-tree into `plugins/krl-interpreter/`; svdx/thermography plugins already standalone; scene-graph dissolved B4; all bespoke core REST verticals gone); **V2-SWEEP-003 slice 1 shipped 2026-06-10** (`ContainerListPage.vue` bulk-delete FILE/TIMESERIES/STRUCTUREDDATA → `safeDeleteContainer`; removes v1 `FileContainerApi`/`TimeseriesContainerApi`/`StructuredDataContainerApi` from the bulk-delete path); **V2-SWEEP-003-2 in-flight 2026-06-10** (fetchPermissions/updatePermissions × 3 container accessors → `GET/PATCH /v2/containers/{appId}/permissions`; dead `useFetchTimeseriesContainer.ts` retired; `appId` forward-filled in `TimeseriesContainer`/`StructuredDataContainer` models; V2-SWEEP-003-3 fetchData deferred to route-appId migration); **A1b shipped 2026-06-10** (additive render-SPI extension: `RenderRequest.params` (E1) + file-rooted dispatch via `shapeIri`+`focusFileRefAppId` with `templateAppId` optional (E2) + `FocusPayloadResolver` SPI seam so ServiceLoader-POJO renderers reach focus bytes (E3) + describe via `params.mode=index` (E4); legacy 4-arg `RenderRequest`/3-arg `ShapesRenderRequestIO` constructors preserved → Trace3D byte-compatible); **A7-THERMO shipped 2026-06-10** (on A1b — `/v2/thermography/*` REST dissolved onto `POST /v2/shapes/render` via `OtvisFrameRenderer` (`OtvisFrameShape` → frame catalogue JSON + heatmap PNG) + `ThermographyHeatmapRenderer` (`ThermographyHeatmapShape` → `application/json` plate-heatmap grid); `ThermographyV2Rest` + `AnalyzeRequestIO` deleted; all 5 FE callers repointed; Re-analyze button removed — analysis is the upload-time file-parse side-effect); **V2-SWEEP-002-USERGROUP-V2 slice-1 in-flight 2026-06-10** (`UserGroupV2Rest` at `/v2/user-groups`, `UserGroupDAO.findByAppId`, `UserGroupV2IO`, 12 unit tests); **V2-SWEEP-002-2 shipped 2026-06-10** (frontend admin user-groups pane migrated to appId-keyed `/v2/user-groups` CRUD + membership via new `useUserGroupsV2`; numeric-id surfaces dropped from list/detail; permissions/roles remain V1-EXCEPTION → V2-SWEEP-002-PERMISSIONS; `tests/unit/useUserGroupsV2.test.ts` 9 tests); **A7-PLUGIN-ADMIN-CONFIG slice 1 shipped 2026-06-10** (`UnhideConfigDescriptor` migrates `GET|PATCH /v2/admin/unhide/config` onto generic `/v2/admin/config/unhide`; harvest-key credential sisters kept at `/v2/admin/unhide/harvest-key/*`; 9 `UnhideConfigDescriptorTest` unit tests; `useUnhideAdminConfig.ts` + CLI + feed error messages repointed; old path 404); **A7-PLUGIN-ADMIN-CONFIG slice 2 shipped 2026-06-10** (`VideoConfigDescriptor` migrates `GET|PATCH /v2/admin/video/config` onto generic `/v2/admin/config/video`; `VideoAdminRest` deleted (config-only class); `VideoAdminPaths.CONFIG` repointed; OGM bug fixed — `VideoPayloadKind.entityPackages()` now registers `de.dlr.shepard.plugins.video.entities` so `VideoConfigDAO` no longer 500s on startup; 8 `VideoConfigDescriptorTest` unit tests; old path 404); **A7-UNHIDE-NS-REVIEW shipped 2026-06-10** (verdict: `/v2/unhide/feed.jsonld` stays outside Tier-3 allowlist — 503-on-disable > 404-on-disable for harvesters; documented in `aidocs/platform/191 §7c`); **A7-PLUGIN-ADMIN-CONFIG slice 4 shipped 2026-06-11** (`AasConfigDescriptor` featureName=`"aas"` + `LegacyV1ConfigDescriptor` featureName=`"legacy-v1"` — `AasConfigAdminRest` + `LegacyV1ConfigAdminRest` deleted; `AdminLegacyV1Pane.vue` repointed from `/v2/admin/legacy/v1/config` to `/v2/admin/config/legacy-v1`; 13+9 unit tests; merged `dbe11f7`); **V2-SWEEP-004-1 in-flight 2026-06-11** (`CollectionReferenceKindHandler` kind=`collection` + `DataObjectReferenceKindHandler` kind=`dataobject` wired into `/v2/references?kind=…` dispatch; frontend migration V2-SWEEP-004-2 queued); **UI-GAP-3 shipped 2026-06-11** (AdminConfigOverviewPane.vue — config registry overview pane; CONFIG_OVERVIEW fragment wired in /admin; bespoke-pane links for semantic/jupyter/unhide/legacy-v1/sql-timeseries); **A7-SVDX shipped 2026-06-12** (bespoke `/v2/svdx/ingest` (`SvdxIngestRest` + `SvdxIngest{Request,Response}IO`) DELETED; the real CSV→TimescaleDB ingest rehomed as `SvdxCsvTransformExecutor` in `plugins/fileformat-svdx` claiming `SvdxCsvIngestShape` IRI, reusing `SvdxCsvIngestionService` verbatim via `CDI.current()`, materialized through `POST /v2/mappings/{appId}/materialize` — mirrors KRL/B5 + URScript; deterministic `svdx-ingest:<svdxId>+<csvId>` idempotency preserved; shape TTL `shapes/svdx-csv-ingest.shacl.ttl`; `SvdxManifestParser` annotate-on-upload untouched; `SvdxCsvTransformExecutorTest` 10 cases; no migration — svdx ingest had no typed `:*Activity`) | ✓ **shipped** | `aidocs/platform/191` / `aidocs/16` V2CONV-* / V2-SWEEP-003 |
| **`?entityId` tombstone param + 400 rejection removed from `ProvenanceRest.stats()` after 10-fire stabilization window** (`APISIMP-PROVENANCE-ENTITYID-TOMBSTONE-DROP`) | none (fork-only `/v2/` surface; `/shepard/api/` v1 untouched) | 🔄 PR open (fire-578, branch `APISIMP-PROVENANCE-ENTITYID-TOMBSTONE-DROP`) — `@Parameter(hidden=true) @QueryParam("entityId") String legacyEntityId` + rejection `if` block removed from `ProvenanceRest.stats()`; `@APIResponse(400)` description simplified; no behavior change for callers already using `?subject=` | `aidocs/34` APISIMP-PROVENANCE-ENTITYID-TOMBSTONE-DROP row; `aidocs/16` APISIMP-PROVENANCE-ENTITYID-TOMBSTONE-DROP |
| **`GET/PUT/DELETE /v2/lab-journal/{appId}` — appId-keyed single-entry CRUD; `LabJournalExistingEntry.vue` migrated to v2; `LabJournalEntryIO.id` suppressed** (`APISIMP-LJE-ENTRY-V2-CRUD`) | none (fork-only `/v2/` surface) | 🔄 PR open (fire-582, branch `APISIMP-LJE-ENTRY-V2-CRUD`) — `LabJournalEntryRest` at `/v2/lab-journal` with `GET/{appId}` (Read gate) + `PUT/{appId}` (Write + creator gate, body `UpdateLabJournalEntryIO`) + `DELETE/{appId}` (Write + creator gate, 204 no-content); `LabJournalEntryIO.id` annotated `@JsonIgnore` (BREAKING on v1 wire — see `aidocs/34`); `LabJournalEntryV2Api.ts` + `UpdateLabJournalEntryV2.ts` new TS client; `LabJournalEntry.ts` `id` optional; `LabJournalExistingEntry.vue` migrated to `useV2ShepardApi(LabJournalEntryV2Api)` + `appId`-keyed API calls + `dataObjectAppId` link | **✓ ↑** (APISIMP-LJE-ENTRY-V2-CRUD, fire-582) | `aidocs/16` APISIMP-LJE-ENTRY-V2-CRUD; `aidocs/34` APISIMP-LJE-ENTRY-V2-CRUD (BREAKING on v1 id field) |
| **`isAccessAllowedForDataObjectAppId` replaces numeric-id `isAccessTypeAllowedForUser` calls in lab-journal REST** (`APISIMP-LJE-NUMERIC-PARENT-ID`) | none (fork-only `/v2/` internal permission routing; no wire change) | ✓ shipped (fire-596, PR #2556) — `LabJournalEntryRest` + `LabJournalDataObjectRest` permission checks migrated from `permissionsService.isAccessTypeAllowedForUser(numericId, …)` to `permissionsService.isAccessAllowedForDataObjectAppId(appId, …)`; numeric Neo4j id no longer appears on the hot path | **✓ ↑** | `aidocs/16` APISIMP-LJE-NUMERIC-PARENT-ID; `aidocs/34` APISIMP-LJE-NUMERIC-PARENT-ID |
| **`@ToolArg Long` nanosecond params in `TimeseriesMcpTools` + `ReferencesMcpTools` converted to ISO 8601 UTC `String`** (`APISIMP-MCP-TS-NANOS-ISO`) | none (fork-only MCP tool surface; no v1 REST change) | ✓ shipped (fire-597, PR #2557) — 8 `@ToolArg` slots changed from `Long` (nanoseconds) to `String` (ISO 8601 UTC); `isoToNanos()` helper converts at MCP boundary; response `window.startTime`/`window.endTime` echo back ISO strings; point `timestamp` values stay nanosecond longs; 8 test call sites updated; `ReferencesMcpTools` `start`/`end` params likewise ISO strings | **✓ ↑** | `aidocs/16` APISIMP-MCP-TS-NANOS-ISO; `aidocs/34` APISIMP-MCP-TS-NANOS-ISO |
| **`TimeseriesAnnotationIO.startNs`/`endNs` + `AnomalyIntervalIO.startNs`/`endNs` renamed `start`/`end` (ISO 8601 `String`)** (`APISIMP-TS-ANNOTATION-IO-NS-TO-ISO` + `APISIMP-ANOMALY-INTERVAL-NS-TO-ISO`) | none (fork-only `/v2/` temporal-annotation + anomaly-detect surfaces; no v1 REST change) | ✓ shipped (fire-606, PR #2567) — `TimeseriesAnnotationIO`: `Long startNs`/`Long endNs` → `String start`/`String end`; constructor uses `DataObjectListItemV2IO.toIsoNs(a.getStartNs())`; handler `createTemporalAnnotation`/`updateTemporalAnnotation` parse with `parseNs(String iso)` helper. `AnomalyIntervalIO`: `long startNs`/`long endNs` → `String start`/`String end`. `AnomalyDetectionService.collectIntervals()` return type changed to `List<AnomalyInterval>` (raw-ns SPI); `toIO(AnomalyInterval)` converts to wire form. Tests updated: `collectIntervals_*` tests use `AnomalyInterval`; `detect_spikeIsDetected` uses `isoToNs()` helper. | **✓ ↑** | `aidocs/16` APISIMP-TS-ANNOTATION-IO-NS-TO-ISO + APISIMP-ANOMALY-INTERVAL-NS-TO-ISO; `aidocs/34` combined wire-break row |
| **`GET /v2/me/git-credentials` response wrapped in `PagedResponseIO` envelope** (`APISIMP-ME-GIT-CRED-LIST-ENVELOPE`) | none (fork-only `/v2/me/` surface; no upstream git-credentials endpoint) | ✓ shipped (fire-606, PR #2568) — `MeCredentialsRest.list()` (plugin-git) now accepts `?page` (default 0) and `?pageSize` (default 50, max 200); returns `PagedResponseIO<GitCredentialIO>`. Mirrors the admin counterpart shape (PR #2563). `backend-client` `listUserGitCredentials()` return type changed from `Array<GitCredential>` to `PagedResponse`. `useFetchGitCredentials.ts` reads `.items ?? []`. 4 new pagination unit tests added (empty page, paged list, beyond-total, pageSize=1 slice). | **✓ ↑** | `aidocs/16` APISIMP-ME-GIT-CRED-LIST-ENVELOPE; `aidocs/34` APISIMP-ME-GIT-CRED-LIST-ENVELOPE row |
| **`GET /v2/containers/{appId}/channels/{channelId}/data` `start`/`end` query params and `POST /v2/data-objects/cross-bulk?kind=timeseries` body `start`/`end` fields changed from nanosecond longs to ISO 8601 strings** (`APISIMP-REST-CHANNEL-DATA-NANOS-TO-ISO` + `APISIMP-CROSS-DO-BULK-START-END-NANOS`) | none (fork-only `/v2/` channel-data + cross-DO surfaces; no upstream equivalents) | ✓ shipped (fire-607, PR TBD — branch `APISIMP-REST-CHANNEL-NANOS-TO-ISO-crossdo`) — (1) `ContainersV2Rest`: `@QueryParam("start"/"end") @NotNull @PositiveOrZero Long` → `@NotNull String`; `parseChannelNs(String iso)` static helper converts to ns for `ContainerKindHandler.getChannelData()`; `DateTimeParseException` → 400. Frontend: `useFetchChannelPreview.ts` + `PinnedChannelTile.vue` + `useJointTracksLoader.ts` send ISO via shared `nsToIso(epochNs)` helper. (2) `CrossDoBulkDataRequestIO`: `Long start`/`Long end` with `@PositiveOrZero` → `String start`/`String end`; `CrossDoBulkDataRest.parseNs(String iso)` converts to ns; `DateTimeParseException` → 400. Frontend: `CollectionCrossTrackViewPane.vue` + `MultiPlayerTsTile.vue` use `nsToIso()`. `nsToIso()` exported from `useCrossDoBulkData.ts`. Tests updated to ISO string constants; bad-ISO-400 test added. | **✓ ↑** | `aidocs/16` APISIMP-REST-CHANNEL-DATA-NANOS-TO-ISO + APISIMP-CROSS-DO-BULK-START-END-NANOS; `aidocs/34` combined wire-break row |
| **`ProvenanceStatsIO.bucketMillis` (nanosecond bucket-width long) renamed `bucketDuration` (ISO 8601 duration `String`, e.g. `"PT86400S"`)** (`APISIMP-PROVSTATS-BUCKET-MILLIS`) | none (fork-only `/v2/provenance/stats` surface; no upstream stats endpoint) | ✓ shipped (fire-613, PR #2575) — `ProvenanceStatsIO`: `long bucketMillis` → `String bucketDuration`; `toIsoDuration(long ms)` helper via `Duration.ofMillis(ms).toString()`; `ProvenanceStatsService.compute()` fills `bucketDuration`; `ProvenanceStatsIOTest` 4 tests cover helper + serialisation. Wire break on `bucketMillis` field name + type for `GET /v2/provenance/stats` response (numeric ms → ISO 8601 duration string). | **✓ ↑** | `aidocs/16` APISIMP-PROVSTATS-BUCKET-MILLIS; `aidocs/34` APISIMP-PROVSTATS-BUCKET-MILLIS row |
| **`ReferenceV2IO` `payload.start`/`payload.end` on v2 `TimeseriesReference` wire changed from nanosecond `long` to ISO 8601 UTC `String`; create/patch accept both ISO strings and legacy nanosecond longs** (`APISIMP-TSREF-TIMEWINDOW-NANOS`) | none (fork-only `/v2/references?kind=timeseries` payload; frozen v1 `TimeseriesReferenceIO` field types unchanged) | ✓ shipped (fire-614, PR #2577) — `TimeseriesReferenceKindHandler.toIO()`: `io.put("start"/"end", nanosToIso(kindIO.getStart()/getEnd()))` via `Instant.ofEpochSecond(ns/1_000_000_000L, ns%1_000_000_000L).toString()`; `create()` normalises ISO strings to ns longs before `objectMapper.convertValue()`; `patch()` uses `isoOrLongToNanos()` helper (ISO parse first, then `Long.parseLong`, then `BadRequestException`); `TimeseriesReferenceIO.java` field types remain `long` (v1 freeze). 4 new JUnit tests. Wire break on `start`/`end` value format in `GET /v2/references?kind=timeseries` response (numeric ns → ISO UTC string). | **✓ ↑** | `aidocs/16` APISIMP-TSREF-TIMEWINDOW-NANOS; `aidocs/34` APISIMP-TSREF-TIMEWINDOW-NANOS row |
| **`GET /v2/references/{appId}/annotations` and `GET .../annotations/{annotationAppId}` emit `start`/`end` as ISO 8601 UTC strings (was `startNs`/`endNs` nanosecond longs); `POST`/`PATCH` accept ISO strings or legacy `startNs`/`endNs` longs** (`APISIMP-TSREF-ANNOT-MAP-NS-TO-ISO`) | none (fork-only `/v2/references/{appId}/annotations` surface; no upstream annotation-on-reference endpoint) | ✓ shipped (fire-615, PR #2579) — `TimeseriesReferenceKindHandler.annotationToMap()` at lines 315–316 fixed: `m.put("startNs"/"endNs", ns-long)` → `m.put("start"/"end", nanosToIso(...))`. `createAnnotation()` + `patchAnnotation()` accept both `"start"`/`"end"` (ISO) and backward-compat `"startNs"`/`"endNs"` (long) via `isoOrLongToNanos()`. `ReferenceAnnotationRest` OpenAPI descriptions updated. 4 new JUnit tests. Channel annotation path (`/v2/timeseries-containers/…/channels/…/annotations`) unchanged — fixed in fire-606. | **✓ ↑** | `aidocs/16` APISIMP-TSREF-ANNOT-MAP-NS-TO-ISO; `aidocs/34` APISIMP-TSREF-ANNOT-MAP-NS-TO-ISO row |
| **`AdminMetricsSummaryIO.httpMeanRequestMillis` (Double, raw ms) renamed to `httpMeanRequestDuration` (ISO 8601 duration `String`)** (`APISIMP-METRICS-HTTP-MEAN-MILLIS`) | none (fork-only `/v2/admin/metrics-summary` surface; no upstream admin metrics endpoint) | ✓ shipped (fire-616, PR #2580) — `AdminMetricsSummaryIO`: `Double httpMeanRequestMillis` → `String httpMeanRequestDuration`; service converts via `Duration.ofNanos(Math.round(millis * 1_000_000)).toString()` (e.g. `"PT0.2S"`); null-guarded for zero-traffic. Frontend `AdminMetricsCard.vue`: `formatRequestDuration()` parses ISO 8601 duration for ms display. Backend-client `AdminMetricsSummary.ts` updated from `number | null` to `string | null`. Wire break on `httpMeanRequestMillis` field name + type for `GET /v2/admin/metrics-summary`. | **✓ ↑** | `aidocs/16` APISIMP-METRICS-HTTP-MEAN-MILLIS; `aidocs/34` APISIMP-METRICS-HTTP-MEAN-MILLIS row |
| **`update_annotation` MCP tool args `validFromMillis`/`validUntilMillis` (epoch-ms `Long`) renamed to `validFrom`/`validUntil` (ISO 8601 UTC `String`)** (`APISIMP-MCP-ANNOTATION-VALID-EPOCH`) | none (fork-only MCP tool surface; no v1 REST change; REST wire already used ISO 8601 since fire-601) | ✓ shipped (fire-617, PR #2581) — `@ToolArg Long validFromMillis`/`validUntilMillis` → `@ToolArg String validFrom`/`validUntil`; `parseIso()` helper converts to epoch-ms at MCP boundary; `DateTimeParseException` → `McpToolSupport.invalidParams()`; `get_annotation` and `update_annotation` tool descriptions updated; no openapi.json change (MCP args not in generated client) | **✓ ↑** | `aidocs/16` APISIMP-MCP-ANNOTATION-VALID-EPOCH; `aidocs/34` APISIMP-MCP-ANNOTATION-VALID-EPOCH row |
| **`openapi.json` aligned to already-shipped APISIMP renames: `wallClockTimestamp` integer→string, `bucketMillis`→`bucketDuration` string, `uptimeMillis`→`uptime` string; `VideoStreamReference.ts` property type integer→string** (`APISIMP-OPENAPI-STALE-FIELDS`) | none (spec doc fix; wire was already correct since fire-612/613/611; no upstream openapi surface) | 🔄 in PR (fire-618, branch `APISIMP-OPENAPI-STALE-FIELDS`) — 3 stale `"format":"int64","type":"integer"` entries in `openapi.json` corrected to `"type":"string"` with ISO 8601 descriptions; `required` arrays updated for renamed fields; `VideoStreamReference.ts:135` `wallClockTimestamp?: number` → `string`; no Java or Neo4j changes | n/a | `aidocs/16` APISIMP-OPENAPI-STALE-FIELDS |
| **`POST /v2/snapshots/{appId}/diff` response: `snapshotACapturedAtMs`/`snapshotBCapturedAtMs` (epoch ms `long`) → `snapshotACapturedAt`/`snapshotBCapturedAt` (ISO 8601 UTC string)** (`APISIMP-SNAPSHOT-CAPTURED-AT-MS`) | none (fork-only `/v2/` endpoint) | 🔄 in PR (fire-618, PR #2582, branch `APISIMP-SNAPSHOT-CAPTURED-AT-MS`) — `SnapshotDiffIO` record fields renamed + type changed; `SnapshotDiffRest` converts via `Instant.ofEpochMilli(...).toString()`; `SnapshotDiffIOTest` updated; `openapi.json` + TS client model updated | `aidocs/34` APISIMP-SNAPSHOT-CAPTURED-AT-MS row; `aidocs/16` APISIMP-SNAPSHOT-CAPTURED-AT-MS |
| **`POST /v2/admin/minters/datacite/test-connection` + `POST /v2/admin/minters/epic/test-connection` response: `latencyMs` (epoch ms `long`) → `latency` (ISO 8601 duration string, e.g. `PT0.123S`)** (`APISIMP-DATACITE-LATENCY-MS` + `APISIMP-EPIC-LATENCY-MS`) | none (fork-only `/v2/` endpoints) | 🔄 in PR (fire-618, PR #2583, branch `APISIMP-DATACITE-LATENCY-MS`) — `DataciteTestConnectionIO` + `EpicTestConnectionIO` records: field renamed + type changed to `String`; `*AdminRest` converts via `Duration.ofMillis(...).toString()`; CLI DTOs + commands updated; `*IoTest` + `*AdminRestTest` updated; `openapi.json` + TS client models updated | `aidocs/34` APISIMP-DATACITE-LATENCY-MS row; `aidocs/16` APISIMP-DATACITE-LATENCY-MS / APISIMP-EPIC-LATENCY-MS |
