image: python:3

before_script:
  - pip install poetry
  - poetry config virtualenvs.create false
  - poetry install

lint:
  script:
    - python -m ruff check shepard_scripts

format:
  script:
    - python -m ruff format shepard_scripts
