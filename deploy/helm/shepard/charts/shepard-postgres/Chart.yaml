apiVersion: v2
name: shepard-postgres
description: |
  PostgreSQL + TimescaleDB substrate for shepard — Phase 1 STUB sub-chart.
  Hosts the timeseries hot path (TimescaleDB hypertables, per
  aidocs/platform/87) and PG-backed plugin data (TableContainer / pgvector when
  enabled). Production chart in HELM-K8S-DEPLOY-03.
type: application
version: 0.1.0
appVersion: "16.4"
kubeVersion: ">= 1.28.0-0"
