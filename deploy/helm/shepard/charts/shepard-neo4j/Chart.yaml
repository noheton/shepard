apiVersion: v2
name: shepard-neo4j
description: |
  Neo4j graph substrate for shepard — Phase 1 STUB sub-chart.
  Production replacement lands in HELM-K8S-DEPLOY-03 (likely wrapping neo4j-helm
  upstream chart with n10s + APOC preconfigured).
type: application
version: 0.1.0
appVersion: "5.20.0"
kubeVersion: ">= 1.28.0-0"
