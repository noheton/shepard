/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AllowedTemplates,
  DataObject,
  ShepardTemplate,
  TemplateInstantiateRequest,
  TemplateInstantiation,
} from '../models/index';
import {
    AllowedTemplatesFromJSON,
    AllowedTemplatesToJSON,
    DataObjectFromJSON,
    DataObjectToJSON,
    ShepardTemplateFromJSON,
    ShepardTemplateToJSON,
    TemplateInstantiateRequestFromJSON,
    TemplateInstantiateRequestToJSON,
    TemplateInstantiationFromJSON,
    TemplateInstantiationToJSON,
} from '../models/index';

export interface InstantiateRequest {
    appId: string;
    templateAppId: string;
}

export interface InstantiateDataObjectRequest {
    collectionAppId: string;
    templateAppId: string;
    templateInstantiateRequest?: TemplateInstantiateRequest;
}

export interface ListAllowedRequest {
    appId: string;
}

export interface ListUsedRequest {
    appId: string;
}

export interface SetAllowedRequest {
    appId: string;
    allowedTemplates: AllowedTemplates;
}

/**
 * 
 */
export class CollectionTemplatesApi extends runtime.BaseAPI {

    /**
     * Stamps the :USES_TEMPLATE edge (idempotent) so the Collection\'s provenance trail records which template it was created from. Returns the template body so the client (frontend / CLI) can interpret the JSON DSL and mint entities. Server-side body interpretation lands in T1c-apply (queued). Requires Write on the Collection.
     * [v2] Instantiate this Collection from a template — records :USES_TEMPLATE and returns the recipe body.
     */
    async instantiateRaw(requestParameters: InstantiateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TemplateInstantiation>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling instantiate().'
            );
        }

        if (requestParameters['templateAppId'] == null) {
            throw new runtime.RequiredError(
                'templateAppId',
                'Required parameter "templateAppId" was null or undefined when calling instantiate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{appId}/templates/from/{templateAppId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))).replace(`{${"templateAppId"}}`, encodeURIComponent(String(requestParameters['templateAppId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TemplateInstantiationFromJSON(jsonValue));
    }

    /**
     * Stamps the :USES_TEMPLATE edge (idempotent) so the Collection\'s provenance trail records which template it was created from. Returns the template body so the client (frontend / CLI) can interpret the JSON DSL and mint entities. Server-side body interpretation lands in T1c-apply (queued). Requires Write on the Collection.
     * [v2] Instantiate this Collection from a template — records :USES_TEMPLATE and returns the recipe body.
     */
    async instantiate(requestParameters: InstantiateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TemplateInstantiation> {
        const response = await this.instantiateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Parses the template body\'s `dataobjects[0].attributes` and applies them to the new DataObject. Records a `:CREATED_FROM_TEMPLATE` relationship back to the template. Requires Write permission on the Collection. The template must be non-retired; if the Collection has a non-empty allow-list the template must appear in it. When the flattened template body includes a `shapeGraph` (Turtle) field, the candidate DataObject\'s attributes are validated against it before creation — 422 is returned on violation (V2CONV-B2 / aidocs/platform/191 §3).
     * [v2] Create a DataObject from a ShepardTemplate (server-side instantiation).
     */
    async instantiateDataObjectRaw(requestParameters: InstantiateDataObjectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DataObject>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling instantiateDataObject().'
            );
        }

        if (requestParameters['templateAppId'] == null) {
            throw new runtime.RequiredError(
                'templateAppId',
                'Required parameter "templateAppId" was null or undefined when calling instantiateDataObject().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/data-objects/from-template/{templateAppId}`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"templateAppId"}}`, encodeURIComponent(String(requestParameters['templateAppId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: TemplateInstantiateRequestToJSON(requestParameters['templateInstantiateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DataObjectFromJSON(jsonValue));
    }

    /**
     * Parses the template body\'s `dataobjects[0].attributes` and applies them to the new DataObject. Records a `:CREATED_FROM_TEMPLATE` relationship back to the template. Requires Write permission on the Collection. The template must be non-retired; if the Collection has a non-empty allow-list the template must appear in it. When the flattened template body includes a `shapeGraph` (Turtle) field, the candidate DataObject\'s attributes are validated against it before creation — 422 is returned on violation (V2CONV-B2 / aidocs/platform/191 §3).
     * [v2] Create a DataObject from a ShepardTemplate (server-side instantiation).
     */
    async instantiateDataObject(requestParameters: InstantiateDataObjectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DataObject> {
        const response = await this.instantiateDataObjectRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * [v2] List templates the Collection owner has curated as allowed inside this Collection.
     */
    async listAllowedRaw(requestParameters: ListAllowedRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ShepardTemplate>>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling listAllowed().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{appId}/templates/allowed`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ShepardTemplateFromJSON));
    }

    /**
     * [v2] List templates the Collection owner has curated as allowed inside this Collection.
     */
    async listAllowed(requestParameters: ListAllowedRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ShepardTemplate>> {
        const response = await this.listAllowedRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * [v2] List templates the Collection has cited via :USES_TEMPLATE.
     */
    async listUsedRaw(requestParameters: ListUsedRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ShepardTemplate>>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling listUsed().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{appId}/templates/used`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ShepardTemplateFromJSON));
    }

    /**
     * [v2] List templates the Collection has cited via :USES_TEMPLATE.
     */
    async listUsed(requestParameters: ListUsedRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ShepardTemplate>> {
        const response = await this.listUsedRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Full replace (not merge). Empty list = no curation; the picker falls back to all live templates.
     * [v2] Replace the allowed-template set for this Collection.
     */
    async setAllowedRaw(requestParameters: SetAllowedRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ShepardTemplate>>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling setAllowed().'
            );
        }

        if (requestParameters['allowedTemplates'] == null) {
            throw new runtime.RequiredError(
                'allowedTemplates',
                'Required parameter "allowedTemplates" was null or undefined when calling setAllowed().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{appId}/templates/allowed`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: AllowedTemplatesToJSON(requestParameters['allowedTemplates']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ShepardTemplateFromJSON));
    }

    /**
     * Full replace (not merge). Empty list = no curation; the picker falls back to all live templates.
     * [v2] Replace the allowed-template set for this Collection.
     */
    async setAllowed(requestParameters: SetAllowedRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ShepardTemplate>> {
        const response = await this.setAllowedRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
