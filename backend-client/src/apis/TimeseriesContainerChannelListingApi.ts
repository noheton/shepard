/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkChannelDataRequest,
  CopyIngestRequest,
  SpatialRolesIO,
  TimeseriesChannelV2,
  TimeseriesWithDataPoints,
} from '../models/index';
import {
    BulkChannelDataRequestFromJSON,
    BulkChannelDataRequestToJSON,
    CopyIngestRequestFromJSON,
    CopyIngestRequestToJSON,
    SpatialRolesIOFromJSON,
    SpatialRolesIOToJSON,
    TimeseriesChannelV2FromJSON,
    TimeseriesChannelV2ToJSON,
    TimeseriesWithDataPointsFromJSON,
    TimeseriesWithDataPointsToJSON,
} from '../models/index';

export interface GetBulkChannelDataRequest {
    containerAppId: string;
    bulkChannelDataRequest: BulkChannelDataRequest;
}

export interface GetChannelDataRequest {
    containerAppId: string;
    shepardId: string;
    end: number;
    start: number;
    downsample?: string;
    maxPoints?: number;
}

export interface GetChannelSpatialRolesRequest {
    containerAppId: string;
}

export interface IngestChannelDataRequest {
    containerAppId: string;
    shepardId: string;
    copyIngestRequest: CopyIngestRequest;
}

export interface ListChannelsRequest {
    containerAppId: string;
    page?: number;
    size?: number;
}

/**
 * 
 */
export class TimeseriesContainerChannelListingApi extends runtime.BaseAPI {

    /**
     * Accepts a list of shepardIds (max 200) plus a shared time window and returns raw data points — one TimeseriesWithDataPoints entry per resolved channel. Unknown IDs are silently skipped. No downsampling is applied; use the single-channel endpoint with ?downsample=lttb when a reduced point count is needed.
     * [v2] Fetch raw data for multiple channels in one call (TS-OPT2).
     */
    async getBulkChannelDataRaw(requestParameters: GetBulkChannelDataRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<TimeseriesWithDataPoints>>> {
        if (requestParameters['containerAppId'] == null) {
            throw new runtime.RequiredError(
                'containerAppId',
                'Required parameter "containerAppId" was null or undefined when calling getBulkChannelData().'
            );
        }

        if (requestParameters['bulkChannelDataRequest'] == null) {
            throw new runtime.RequiredError(
                'bulkChannelDataRequest',
                'Required parameter "bulkChannelDataRequest" was null or undefined when calling getBulkChannelData().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/timeseries-containers/{containerAppId}/channels/data/bulk`.replace(`{${"containerAppId"}}`, encodeURIComponent(String(requestParameters['containerAppId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkChannelDataRequestToJSON(requestParameters['bulkChannelDataRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(TimeseriesWithDataPointsFromJSON));
    }

    /**
     * Accepts a list of shepardIds (max 200) plus a shared time window and returns raw data points — one TimeseriesWithDataPoints entry per resolved channel. Unknown IDs are silently skipped. No downsampling is applied; use the single-channel endpoint with ?downsample=lttb when a reduced point count is needed.
     * [v2] Fetch raw data for multiple channels in one call (TS-OPT2).
     */
    async getBulkChannelData(requestParameters: GetBulkChannelDataRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<TimeseriesWithDataPoints>> {
        const response = await this.getBulkChannelDataRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Resolves the single-field shepardId to the legacy 5-tuple internally and returns data points for the requested time window. Accepts optional LTTB downsampling via ?downsample=lttb&max_points=N. This endpoint supersedes the 5-tuple query params on the v1 surface for new integrations.
     * [v2] Fetch data points for a channel by shepardId (TS-IDc).
     */
    async getChannelDataRaw(requestParameters: GetChannelDataRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TimeseriesWithDataPoints>> {
        if (requestParameters['containerAppId'] == null) {
            throw new runtime.RequiredError(
                'containerAppId',
                'Required parameter "containerAppId" was null or undefined when calling getChannelData().'
            );
        }

        if (requestParameters['shepardId'] == null) {
            throw new runtime.RequiredError(
                'shepardId',
                'Required parameter "shepardId" was null or undefined when calling getChannelData().'
            );
        }

        if (requestParameters['end'] == null) {
            throw new runtime.RequiredError(
                'end',
                'Required parameter "end" was null or undefined when calling getChannelData().'
            );
        }

        if (requestParameters['start'] == null) {
            throw new runtime.RequiredError(
                'start',
                'Required parameter "start" was null or undefined when calling getChannelData().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['downsample'] != null) {
            queryParameters['downsample'] = requestParameters['downsample'];
        }

        if (requestParameters['end'] != null) {
            queryParameters['end'] = requestParameters['end'];
        }

        if (requestParameters['maxPoints'] != null) {
            queryParameters['max_points'] = requestParameters['maxPoints'];
        }

        if (requestParameters['start'] != null) {
            queryParameters['start'] = requestParameters['start'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/timeseries-containers/{containerAppId}/channels/{shepardId}/data`.replace(`{${"containerAppId"}}`, encodeURIComponent(String(requestParameters['containerAppId']))).replace(`{${"shepardId"}}`, encodeURIComponent(String(requestParameters['shepardId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TimeseriesWithDataPointsFromJSON(jsonValue));
    }

    /**
     * Resolves the single-field shepardId to the legacy 5-tuple internally and returns data points for the requested time window. Accepts optional LTTB downsampling via ?downsample=lttb&max_points=N. This endpoint supersedes the 5-tuple query params on the v1 surface for new integrations.
     * [v2] Fetch data points for a channel by shepardId (TS-IDc).
     */
    async getChannelData(requestParameters: GetChannelDataRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TimeseriesWithDataPoints> {
        const response = await this.getChannelDataRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Scans the container\'s channels for axis-role annotations (propertyIRI = urn:shepard:spatial:axis) and returns one shepardId per role. Null fields mean \'no annotation found for that role.\' The Trace3D recipe builder uses this to pre-populate its axis dropdowns on open.
     * [v2] Return per-axis channel assignments for the Trace3D view recipe (TS-AXIS-AUTO).
     */
    async getChannelSpatialRolesRaw(requestParameters: GetChannelSpatialRolesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SpatialRolesIO>> {
        if (requestParameters['containerAppId'] == null) {
            throw new runtime.RequiredError(
                'containerAppId',
                'Required parameter "containerAppId" was null or undefined when calling getChannelSpatialRoles().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/timeseries-containers/{containerAppId}/channels/spatial-roles`.replace(`{${"containerAppId"}}`, encodeURIComponent(String(requestParameters['containerAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SpatialRolesIOFromJSON(jsonValue));
    }

    /**
     * Scans the container\'s channels for axis-role annotations (propertyIRI = urn:shepard:spatial:axis) and returns one shepardId per role. Null fields mean \'no annotation found for that role.\' The Trace3D recipe builder uses this to pre-populate its axis dropdowns on open.
     * [v2] Return per-axis channel assignments for the Trace3D view recipe (TS-AXIS-AUTO).
     */
    async getChannelSpatialRoles(requestParameters: GetChannelSpatialRolesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SpatialRolesIO> {
        const response = await this.getChannelSpatialRolesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Uses the PostgreSQL COPY protocol, which is 3–5× faster than the VALUES INSERT path for bulk historical loads. The channel (identified by shepardId) must already exist; create it first via the regular endpoint if needed. No ON CONFLICT handling is applied: timestamps must be unique within the batch and must not duplicate rows already stored for this channel. Designed for high-throughput import scripts that batch thousands of points per call.
     * [v2] High-throughput COPY ingest for a single channel (TS-OPT3-COPY).
     */
    async ingestChannelDataRaw(requestParameters: IngestChannelDataRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['containerAppId'] == null) {
            throw new runtime.RequiredError(
                'containerAppId',
                'Required parameter "containerAppId" was null or undefined when calling ingestChannelData().'
            );
        }

        if (requestParameters['shepardId'] == null) {
            throw new runtime.RequiredError(
                'shepardId',
                'Required parameter "shepardId" was null or undefined when calling ingestChannelData().'
            );
        }

        if (requestParameters['copyIngestRequest'] == null) {
            throw new runtime.RequiredError(
                'copyIngestRequest',
                'Required parameter "copyIngestRequest" was null or undefined when calling ingestChannelData().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/timeseries-containers/{containerAppId}/channels/{shepardId}/data/ingest`.replace(`{${"containerAppId"}}`, encodeURIComponent(String(requestParameters['containerAppId']))).replace(`{${"shepardId"}}`, encodeURIComponent(String(requestParameters['shepardId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CopyIngestRequestToJSON(requestParameters['copyIngestRequest']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Uses the PostgreSQL COPY protocol, which is 3–5× faster than the VALUES INSERT path for bulk historical loads. The channel (identified by shepardId) must already exist; create it first via the regular endpoint if needed. No ON CONFLICT handling is applied: timestamps must be unique within the batch and must not duplicate rows already stored for this channel. Designed for high-throughput import scripts that batch thousands of points per call.
     * [v2] High-throughput COPY ingest for a single channel (TS-OPT3-COPY).
     */
    async ingestChannelData(requestParameters: IngestChannelDataRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.ingestChannelDataRaw(requestParameters, initOverrides);
    }

    /**
     * Returns one entry per channel in the container, each carrying its stable single-field identity (shepardId) plus the legacy 5-tuple (measurement, device, location, symbolicName, field). The 5-tuple stays valid as a lookup key for one release cycle; new integrations should prefer shepardId.
     * [v2] List all channels of a TimeseriesContainer with shepardId.
     */
    async listChannelsRaw(requestParameters: ListChannelsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<TimeseriesChannelV2>>> {
        if (requestParameters['containerAppId'] == null) {
            throw new runtime.RequiredError(
                'containerAppId',
                'Required parameter "containerAppId" was null or undefined when calling listChannels().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/timeseries-containers/{containerAppId}/channels`.replace(`{${"containerAppId"}}`, encodeURIComponent(String(requestParameters['containerAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(TimeseriesChannelV2FromJSON));
    }

    /**
     * Returns one entry per channel in the container, each carrying its stable single-field identity (shepardId) plus the legacy 5-tuple (measurement, device, location, symbolicName, field). The 5-tuple stays valid as a lookup key for one release cycle; new integrations should prefer shepardId.
     * [v2] List all channels of a TimeseriesContainer with shepardId.
     */
    async listChannels(requestParameters: ListChannelsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<TimeseriesChannelV2>> {
        const response = await this.listChannelsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
