/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PredicateVocabularyEntryIO,
  ShapeBuildRequestIO,
  ShapeBuildResponseIO,
  ShapeValidationReportIO,
  ShapeValidationRequestIO,
  ShapesApplicableResponseIO,
  ShapesRenderRequestIO,
  ShapesRenderResponseIO,
} from '../models/index';
import {
    PredicateVocabularyEntryIOFromJSON,
    PredicateVocabularyEntryIOToJSON,
    ShapeBuildRequestIOFromJSON,
    ShapeBuildRequestIOToJSON,
    ShapeBuildResponseIOFromJSON,
    ShapeBuildResponseIOToJSON,
    ShapeValidationReportIOFromJSON,
    ShapeValidationReportIOToJSON,
    ShapeValidationRequestIOFromJSON,
    ShapeValidationRequestIOToJSON,
    ShapesApplicableResponseIOFromJSON,
    ShapesApplicableResponseIOToJSON,
    ShapesRenderRequestIOFromJSON,
    ShapesRenderRequestIOToJSON,
    ShapesRenderResponseIOFromJSON,
    ShapesRenderResponseIOToJSON,
} from '../models/index';

export interface BuildRequest {
    shapeBuildRequestIO: ShapeBuildRequestIO;
}

export interface ListApplicableShapesRequest {
    focusAppId: string;
}

export interface PredicatesRequest {
    substrate?: string;
}

export interface RenderShapeRequest {
    shapesRenderRequestIO: ShapesRenderRequestIO;
}

export interface ValidateShapeRequest {
    shapeValidationRequestIO: ShapeValidationRequestIO;
}

/**
 * 
 */
export class ShapesApi extends runtime.BaseAPI {

    /**
     * Stateless, read-only. The visual template editor emits the JSON DSL; this returns the deterministic shape graph the same author then stores on a ShepardTemplate. A structurally invalid DSL (blank predicate path, etc.) is a 400 with the reason in the error field.
     * [v2] Compile a JSON DSL into canonical SHACL Turtle.
     */
    async buildRaw(requestParameters: BuildRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShapeBuildResponseIO>> {
        if (requestParameters['shapeBuildRequestIO'] == null) {
            throw new runtime.RequiredError(
                'shapeBuildRequestIO',
                'Required parameter "shapeBuildRequestIO" was null or undefined when calling build().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/shapes/build`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ShapeBuildRequestIOToJSON(requestParameters['shapeBuildRequestIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShapeBuildResponseIOFromJSON(jsonValue));
    }

    /**
     * Stateless, read-only. The visual template editor emits the JSON DSL; this returns the deterministic shape graph the same author then stores on a ShepardTemplate. A structurally invalid DSL (blank predicate path, etc.) is a 400 with the reason in the error field.
     * [v2] Compile a JSON DSL into canonical SHACL Turtle.
     */
    async build(requestParameters: BuildRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShapeBuildResponseIO> {
        const response = await this.buildRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * One discovery for both directions of the shapes UX (doc 125 §5.3). VIEW entries are non-retired VIEW_RECIPE templates attached to the focus entity, consumed via POST /v2/shapes/render. FORM entries are non-retired data-kind templates carrying a shapeGraph (scoped to the owning Collection\'s allow-list when non-empty), consumed via GET /v2/templates/{appId}/form. An empty items list is a valid response — never an error.
     * [v2] List the renderable views (mode=VIEW) and fillable forms (mode=FORM) applicable to a focus entity.
     */
    async listApplicableShapesRaw(requestParameters: ListApplicableShapesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShapesApplicableResponseIO>> {
        if (requestParameters['focusAppId'] == null) {
            throw new runtime.RequiredError(
                'focusAppId',
                'Required parameter "focusAppId" was null or undefined when calling listApplicableShapes().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['focusAppId'] != null) {
            queryParameters['focusAppId'] = requestParameters['focusAppId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/shapes/applicable`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShapesApplicableResponseIOFromJSON(jsonValue));
    }

    /**
     * One discovery for both directions of the shapes UX (doc 125 §5.3). VIEW entries are non-retired VIEW_RECIPE templates attached to the focus entity, consumed via POST /v2/shapes/render. FORM entries are non-retired data-kind templates carrying a shapeGraph (scoped to the owning Collection\'s allow-list when non-empty), consumed via GET /v2/templates/{appId}/form. An empty items list is a valid response — never an error.
     * [v2] List the renderable views (mode=VIEW) and fillable forms (mode=FORM) applicable to a focus entity.
     */
    async listApplicableShapes(requestParameters: ListApplicableShapesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShapesApplicableResponseIO> {
        const response = await this.listApplicableShapesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the predicate_vocabulary table entries mapping each known shepard: predicate URI to its authoritative storage substrate (neo4j | timescaledb | postgres | garage). Optionally filtered by substrate. Read-only — the table is populated by Flyway migrations.
     * [v2] List the shepard: predicate vocabulary with substrate routing.
     */
    async predicatesRaw(requestParameters: PredicatesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<PredicateVocabularyEntryIO>>> {
        const queryParameters: any = {};

        if (requestParameters['substrate'] != null) {
            queryParameters['substrate'] = requestParameters['substrate'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/shapes/predicates`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(PredicateVocabularyEntryIOFromJSON));
    }

    /**
     * Returns the predicate_vocabulary table entries mapping each known shepard: predicate URI to its authoritative storage substrate (neo4j | timescaledb | postgres | garage). Optionally filtered by substrate. Read-only — the table is populated by Flyway migrations.
     * [v2] List the shepard: predicate vocabulary with substrate routing.
     */
    async predicates(requestParameters: PredicatesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<PredicateVocabularyEntryIO>> {
        const response = await this.predicatesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Stateless, read-only. Returns the template\'s channel binding declarations. Beta (TPL2b): all bindings have status=DECLARED — live channel resolution (OK / MISSING / UNIT_MISMATCH) ships in TPL2c. 422 when templateKind != VIEW_RECIPE.
     * [v2] Project a VIEW_RECIPE template\'s channel bindings onto a focus DataObject.
     */
    async renderShapeRaw(requestParameters: RenderShapeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShapesRenderResponseIO>> {
        if (requestParameters['shapesRenderRequestIO'] == null) {
            throw new runtime.RequiredError(
                'shapesRenderRequestIO',
                'Required parameter "shapesRenderRequestIO" was null or undefined when calling renderShape().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/shapes/render`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ShapesRenderRequestIOToJSON(requestParameters['shapesRenderRequestIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShapesRenderResponseIOFromJSON(jsonValue));
    }

    /**
     * Stateless, read-only. Returns the template\'s channel binding declarations. Beta (TPL2b): all bindings have status=DECLARED — live channel resolution (OK / MISSING / UNIT_MISMATCH) ships in TPL2c. 422 when templateKind != VIEW_RECIPE.
     * [v2] Project a VIEW_RECIPE template\'s channel bindings onto a focus DataObject.
     */
    async renderShape(requestParameters: RenderShapeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShapesRenderResponseIO> {
        const response = await this.renderShapeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Stateless, read-only. Returns the SHACL validation report. Bad Turtle in either input is reported via parseError in the 200 body, NOT as a 400 — so a caller (e.g. an MCP tool) can distinguish \'malformed payload\' from \'malformed request\'.
     * [v2] Validate a candidate RDF payload against a SHACL shape graph.
     */
    async validateShapeRaw(requestParameters: ValidateShapeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShapeValidationReportIO>> {
        if (requestParameters['shapeValidationRequestIO'] == null) {
            throw new runtime.RequiredError(
                'shapeValidationRequestIO',
                'Required parameter "shapeValidationRequestIO" was null or undefined when calling validateShape().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/shapes/validate`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ShapeValidationRequestIOToJSON(requestParameters['shapeValidationRequestIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShapeValidationReportIOFromJSON(jsonValue));
    }

    /**
     * Stateless, read-only. Returns the SHACL validation report. Bad Turtle in either input is reported via parseError in the 200 body, NOT as a 400 — so a caller (e.g. an MCP tool) can distinguish \'malformed payload\' from \'malformed request\'.
     * [v2] Validate a candidate RDF payload against a SHACL shape graph.
     */
    async validateShape(requestParameters: ValidateShapeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShapeValidationReportIO> {
        const response = await this.validateShapeRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
