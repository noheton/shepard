/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  MeIO,
  MeRoleIn,
  PatchMe,
  User,
} from '../models/index';
import {
    MeIOFromJSON,
    MeIOToJSON,
    MeRoleInFromJSON,
    MeRoleInToJSON,
    PatchMeFromJSON,
    PatchMeToJSON,
    UserFromJSON,
    UserToJSON,
} from '../models/index';

export interface GetAvatarRequest {
    appId: string;
}

export interface PatchMeRequest {
    patchMe: PatchMe;
}

export interface PatchPreferencesRequest {
    body: any | null;
}

export interface PutAvatarRequest {
    file?: Blob;
}

export interface RoleInRequest {
    collectionAppId: string;
}

/**
 * 
 */
export class MeApi extends runtime.BaseAPI {

    /**
     * [v2] Remove the caller\'s avatar.
     */
    async deleteAvatarRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/users/me/avatar`,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * [v2] Remove the caller\'s avatar.
     */
    async deleteAvatar(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteAvatarRaw(initOverrides);
    }

    /**
     * [v2] Get a user\'s avatar. No authentication required; returns 404 if none uploaded.
     */
    async getAvatarRaw(requestParameters: GetAvatarRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling getAvatar().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/users/{appId}/avatar`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * [v2] Get a user\'s avatar. No authentication required; returns 404 if none uploaded.
     */
    async getAvatar(requestParameters: GetAvatarRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getAvatarRaw(requestParameters, initOverrides);
    }

    /**
     * Includes all upstream User fields plus v2 additions: `watchedCollectionCount` (CW1) — number of collections the caller is watching.
     * [v2] Return the caller\'s enriched v2 profile.
     */
    async getMeRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MeIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/users/me`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MeIOFromJSON(jsonValue));
    }

    /**
     * Includes all upstream User fields plus v2 additions: `watchedCollectionCount` (CW1) — number of collections the caller is watching.
     * [v2] Return the caller\'s enriched v2 profile.
     */
    async getMe(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MeIO> {
        const response = await this.getMeRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the per-user preference map. Returns an empty object `{}` when no preferences have been set. Keys are open-world strings; values are always strings.
     * [v2] Get the caller\'s UI preferences.
     */
    async getPreferencesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<object>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/users/me/preferences`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Returns the per-user preference map. Returns an empty object `{}` when no preferences have been set. Keys are open-world strings; values are always strings.
     * [v2] Get the caller\'s UI preferences.
     */
    async getPreferences(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<object> {
        const response = await this.getPreferencesRaw(initOverrides);
        return await response.value();
    }

    /**
     * RFC 7396 JSON Merge Patch. Accepts `orcid` (U1a), `displayName` (U1b), and `anonymizeInProvenance` (PROV1l, GDPR opt-out — when true, identity is omitted from :Activity records). ORCID format: `NNNN-NNNN-NNNN-NNN[N|X]` (ISO 7064 mod 11-2 checked). Empty-string on orcid/displayName clears the field. Null on any field = leave unchanged. Unknown body fields are ignored.
     * [v2] Partial-update the caller\'s User record.
     */
    async patchMeRaw(requestParameters: PatchMeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<User>> {
        if (requestParameters['patchMe'] == null) {
            throw new runtime.RequiredError(
                'patchMe',
                'Required parameter "patchMe" was null or undefined when calling patchMe().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/merge-patch+json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/users/me`,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PatchMeToJSON(requestParameters['patchMe']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UserFromJSON(jsonValue));
    }

    /**
     * RFC 7396 JSON Merge Patch. Accepts `orcid` (U1a), `displayName` (U1b), and `anonymizeInProvenance` (PROV1l, GDPR opt-out — when true, identity is omitted from :Activity records). ORCID format: `NNNN-NNNN-NNNN-NNN[N|X]` (ISO 7064 mod 11-2 checked). Empty-string on orcid/displayName clears the field. Null on any field = leave unchanged. Unknown body fields are ignored.
     * [v2] Partial-update the caller\'s User record.
     */
    async patchMe(requestParameters: PatchMeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<User> {
        const response = await this.patchMeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Applies an RFC 7396 JSON Merge Patch to the caller\'s preference map. Keys with non-null string values are set or updated. Keys with explicit JSON `null` values are REMOVED. Keys absent from the body are PRESERVED. The endpoint returns the resulting map after the patch is applied.  Example: set theme and remove a stale key — `{\"theme\": \"dark\", \"defaultPageSize\": null}`.  Known UI keys (open-world — unknown string keys are also accepted): `theme` (light / dark / auto), `language` (BCP-47), `timeZone` (IANA), `dateFormat`, `defaultPageSize` (integer-as-string), `defaultLandingPage` (URL path), `ui.advancedMode` (\"true\"/\"false\"), `ui.showOrcidBadge` (\"true\"/\"false\"). Plugins may stake out their own keys (recommended namespacing: `<plugin>.<setting>`).  Content-Type: prefer `application/merge-patch+json`; the endpoint also accepts `application/json` for clients that can\'t set the dedicated type.  Auth: any authenticated user — preferences are scoped to the caller\'s `:User` node and never affect other users.  Constraint: non-string non-null values return 400.
     * [v2] Partial-update the caller\'s UI preferences.
     */
    async patchPreferencesRaw(requestParameters: PatchPreferencesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<object>> {
        if (requestParameters['body'] == null) {
            throw new runtime.RequiredError(
                'body',
                'Required parameter "body" was null or undefined when calling patchPreferences().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/merge-patch+json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/users/me/preferences`,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Applies an RFC 7396 JSON Merge Patch to the caller\'s preference map. Keys with non-null string values are set or updated. Keys with explicit JSON `null` values are REMOVED. Keys absent from the body are PRESERVED. The endpoint returns the resulting map after the patch is applied.  Example: set theme and remove a stale key — `{\"theme\": \"dark\", \"defaultPageSize\": null}`.  Known UI keys (open-world — unknown string keys are also accepted): `theme` (light / dark / auto), `language` (BCP-47), `timeZone` (IANA), `dateFormat`, `defaultPageSize` (integer-as-string), `defaultLandingPage` (URL path), `ui.advancedMode` (\"true\"/\"false\"), `ui.showOrcidBadge` (\"true\"/\"false\"). Plugins may stake out their own keys (recommended namespacing: `<plugin>.<setting>`).  Content-Type: prefer `application/merge-patch+json`; the endpoint also accepts `application/json` for clients that can\'t set the dedicated type.  Auth: any authenticated user — preferences are scoped to the caller\'s `:User` node and never affect other users.  Constraint: non-string non-null values return 400.
     * [v2] Partial-update the caller\'s UI preferences.
     */
    async patchPreferences(requestParameters: PatchPreferencesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<object> {
        const response = await this.patchPreferencesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * [v2] Upload or replace the caller\'s avatar (max 2 MB; JPEG/PNG/GIF/WebP).
     */
    async putAvatarRaw(requestParameters: PutAvatarRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        // use FormData to transmit files using content-type "multipart/form-data"
        useForm = canConsumeForm;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['file'] != null) {
            formParams.append('file', requestParameters['file'] as any);
        }

        const response = await this.request({
            path: `/v2/users/me/avatar`,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * [v2] Upload or replace the caller\'s avatar (max 2 MB; JPEG/PNG/GIF/WebP).
     */
    async putAvatar(requestParameters: PutAvatarRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.putAvatarRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the caller\'s effective capabilities on the Collection identified by `collectionAppId` as four booleans: `read`, `write`, `manage`, and `isInstanceAdmin`.  The booleans are additive: `manage` implies `write`, and `write` implies `read`. A caller with the Owner or Manager role gets `manage=true`; a Writer gets `write=true`; a Reader gets only `read=true`. Instance admins get `isInstanceAdmin=true` regardless of their Collection-level role. The UI renders the highest effective role as a chip (Owner / Editor / Reader) and shows a separate Instance-Admin chip when applicable.  Existence-protection pattern: a 403 is returned when the Collection exists but the caller has no roles and is not an instance admin, so a caller without access cannot distinguish \'does not exist\' from \'exists but forbidden\' — 404 is reserved for Collections that genuinely have no matching `appId`.  Auth: any authenticated user. The endpoint is intentionally unauthenticated-user hostile — 401 is returned before any Collection lookup.
     * [v2] Caller\'s effective role in a Collection.
     */
    async roleInRaw(requestParameters: RoleInRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MeRoleIn>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling roleIn().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/me/role-in/{collectionAppId}`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MeRoleInFromJSON(jsonValue));
    }

    /**
     * Returns the caller\'s effective capabilities on the Collection identified by `collectionAppId` as four booleans: `read`, `write`, `manage`, and `isInstanceAdmin`.  The booleans are additive: `manage` implies `write`, and `write` implies `read`. A caller with the Owner or Manager role gets `manage=true`; a Writer gets `write=true`; a Reader gets only `read=true`. Instance admins get `isInstanceAdmin=true` regardless of their Collection-level role. The UI renders the highest effective role as a chip (Owner / Editor / Reader) and shows a separate Instance-Admin chip when applicable.  Existence-protection pattern: a 403 is returned when the Collection exists but the caller has no roles and is not an instance admin, so a caller without access cannot distinguish \'does not exist\' from \'exists but forbidden\' — 404 is reserved for Collections that genuinely have no matching `appId`.  Auth: any authenticated user. The endpoint is intentionally unauthenticated-user hostile — 401 is returned before any Collection lookup.
     * [v2] Caller\'s effective role in a Collection.
     */
    async roleIn(requestParameters: RoleInRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MeRoleIn> {
        const response = await this.roleInRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
