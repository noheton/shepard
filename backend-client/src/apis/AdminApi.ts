/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AasRegistrationIO,
  AasSyncResultIO,
  AiCapabilityConfigIO,
  BootstrapRequest,
  ConfigFeature,
  DataciteCredentialIO,
  DataciteCredentialSetIO,
  DataciteMinterConfigIO,
  DataciteTestConnectionIO,
  EpicCredentialIO,
  EpicCredentialSetIO,
  EpicMinterConfigIO,
  EpicTestConnectionIO,
  FeatureToggle,
  PagedResponseFeatureToggle,
  FileMigrationStateIO,
  FileMigrationTriggerIO,
  GrantInstanceAdmin,
  HarvestKeyMintedIO,
  InstanceAdminGrant,
  InstanceRegistryIO,
  InstanceRegistryPatchIO,
  JsonNode,
  LedgerAnchorJob,
  LedgerAnchorRequest,
  LegacyV1StatsIO,
  MirroredUserCreateIO,
  MirroredUserIO,
  NotificationIO,
  NotificationTransportListIO,
  NotificationTransportReadIO,
  NotificationTransportWriteIO,
  NukeRequest,
  NukeResult,
  OntologyBundleIO,
  OntologyBundleListIO,
  OntologyGitIngestResultIO,
  OntologyGitSourceIO,
  PatchFeatureToggle,
  PermissionAuditEntry,
  PermissionAuditLogEntry,
  PluginEntry,
  PluginList,
  PluginPatch,
  ProblemJson,
  ProcessChainMappingResultIO,
  RefreshOntologiesRequest,
  RefreshOntologiesResult,
  RefreshSnapshotsResult,
  StorageStatusIO,
  TestNotificationIO,
  UnhideConfigIO,
} from '../models/index';
import {
    AasRegistrationIOFromJSON,
    AasRegistrationIOToJSON,
    AasSyncResultIOFromJSON,
    AasSyncResultIOToJSON,
    AiCapabilityConfigIOFromJSON,
    AiCapabilityConfigIOToJSON,
    BootstrapRequestFromJSON,
    BootstrapRequestToJSON,
    ConfigFeatureFromJSON,
    ConfigFeatureToJSON,
    DataciteCredentialIOFromJSON,
    DataciteCredentialIOToJSON,
    DataciteCredentialSetIOFromJSON,
    DataciteCredentialSetIOToJSON,
    DataciteMinterConfigIOFromJSON,
    DataciteMinterConfigIOToJSON,
    DataciteTestConnectionIOFromJSON,
    DataciteTestConnectionIOToJSON,
    EpicCredentialIOFromJSON,
    EpicCredentialIOToJSON,
    EpicCredentialSetIOFromJSON,
    EpicCredentialSetIOToJSON,
    EpicMinterConfigIOFromJSON,
    EpicMinterConfigIOToJSON,
    EpicTestConnectionIOFromJSON,
    EpicTestConnectionIOToJSON,
    FeatureToggleFromJSON,
    FeatureToggleToJSON,
    PagedResponseFeatureToggleFromJSON,
    FileMigrationStateIOFromJSON,
    FileMigrationStateIOToJSON,
    FileMigrationTriggerIOFromJSON,
    FileMigrationTriggerIOToJSON,
    GrantInstanceAdminFromJSON,
    GrantInstanceAdminToJSON,
    HarvestKeyMintedIOFromJSON,
    HarvestKeyMintedIOToJSON,
    InstanceAdminGrantFromJSON,
    InstanceAdminGrantToJSON,
    InstanceRegistryIOFromJSON,
    InstanceRegistryIOToJSON,
    InstanceRegistryPatchIOFromJSON,
    InstanceRegistryPatchIOToJSON,
    JsonNodeFromJSON,
    JsonNodeToJSON,
    LedgerAnchorJobFromJSON,
    LedgerAnchorJobToJSON,
    LedgerAnchorRequestFromJSON,
    LedgerAnchorRequestToJSON,
    LegacyV1StatsIOFromJSON,
    LegacyV1StatsIOToJSON,
    MirroredUserCreateIOFromJSON,
    MirroredUserCreateIOToJSON,
    MirroredUserIOFromJSON,
    MirroredUserIOToJSON,
    NotificationIOFromJSON,
    NotificationIOToJSON,
    NotificationTransportListIOFromJSON,
    NotificationTransportListIOToJSON,
    NotificationTransportReadIOFromJSON,
    NotificationTransportReadIOToJSON,
    NotificationTransportWriteIOFromJSON,
    NotificationTransportWriteIOToJSON,
    NukeRequestFromJSON,
    NukeRequestToJSON,
    NukeResultFromJSON,
    NukeResultToJSON,
    OntologyBundleIOFromJSON,
    OntologyBundleIOToJSON,
    OntologyBundleListIOFromJSON,
    OntologyBundleListIOToJSON,
    OntologyGitIngestResultIOFromJSON,
    OntologyGitIngestResultIOToJSON,
    OntologyGitSourceIOFromJSON,
    OntologyGitSourceIOToJSON,
    PatchFeatureToggleFromJSON,
    PatchFeatureToggleToJSON,
    PermissionAuditEntryFromJSON,
    PermissionAuditEntryToJSON,
    PermissionAuditLogEntryFromJSON,
    PermissionAuditLogEntryToJSON,
    PluginEntryFromJSON,
    PluginEntryToJSON,
    PluginListFromJSON,
    PluginListToJSON,
    PluginPatchFromJSON,
    PluginPatchToJSON,
    ProblemJsonFromJSON,
    ProblemJsonToJSON,
    ProcessChainMappingResultIOFromJSON,
    ProcessChainMappingResultIOToJSON,
    RefreshOntologiesRequestFromJSON,
    RefreshOntologiesRequestToJSON,
    RefreshOntologiesResultFromJSON,
    RefreshOntologiesResultToJSON,
    RefreshSnapshotsResultFromJSON,
    RefreshSnapshotsResultToJSON,
    StorageStatusIOFromJSON,
    StorageStatusIOToJSON,
    TestNotificationIOFromJSON,
    TestNotificationIOToJSON,
    UnhideConfigIOFromJSON,
    UnhideConfigIOToJSON,
} from '../models/index';

export interface AnchorRequest {
    ledgerAnchorRequest: LedgerAnchorRequest;
}

export interface ApplyRequest {
    body: string;
}

export interface BootstrapOperationRequest {
    bootstrapRequest: BootstrapRequest;
}

export interface CreateNotificationTransportRequest {
    notificationTransportWriteIO: NotificationTransportWriteIO;
}

export interface CreateSemanticGitSourceRequest {
    ontologyGitSourceIO: Omit<OntologyGitSourceIO, 'appId'|'lastIngestedAt'|'lastStatus'|'lastError'|'createdAt'|'createdBy'>;
}

export interface DeleteNotificationTransportRequest {
    appId: string;
}

export interface DeleteOntologyRequest {
    bundleId: string;
}

export interface DeleteSemanticGitSourceRequest {
    appId: string;
}

export interface DisableOntologyRequest {
    bundleId: string;
}

export interface EnableOntologyRequest {
    bundleId: string;
}

export interface GetAnchorsForDataObjectRequest {
    appId: string;
}

export interface GetCapabilityRequest {
    capability: string;
}

export interface GetFeatureConfigRequest {
    feature: string;
}

export interface GetJobRequest {
    jobId: string;
}

export interface GetLegacyV1StatsRequest {
    topN?: number;
}

export interface GrantInstanceAdminRequest {
    grantInstanceAdmin: GrantInstanceAdmin;
}

export interface MirrorUsersRequest {
    mirroredUserCreateIO: MirroredUserCreateIO;
}

export interface NukeOperationRequest {
    nukeRequest: NukeRequest;
}

export interface PatchCapabilityRequest {
    capability: string;
    aiCapabilityConfigIO: AiCapabilityConfigIO;
}

export interface PatchFeatureConfigRequest {
    feature: string;
    jsonNode: JsonNode;
}

export interface PatchFeatureToggleRequest {
    name: string;
    patchFeatureToggle: PatchFeatureToggle;
}

export interface PatchNotificationTransportRequest {
    appId: string;
    notificationTransportWriteIO: NotificationTransportWriteIO;
}

export interface PatchPluginRequest {
    id: string;
    pluginPatch: PluginPatch;
}

export interface PatchRegistryRequest {
    instanceRegistryPatchIO: InstanceRegistryPatchIO;
}

export interface PermissionAuditLogRequest {
    actor?: string;
    entityAppId?: string;
    from?: string;
    page?: number;
    pageSize?: number;
    to?: string;
}

export interface RefreshOntologiesOperationRequest {
    refreshOntologiesRequest: RefreshOntologiesRequest;
}

export interface RevokeInstanceAdminRequest {
    username: string;
}

export interface RollbackRequest {
    appId: string;
}

export interface SendTestRequest {
    testNotificationIO: TestNotificationIO;
}

export interface SetDataciteMinterCredentialRequest {
    dataciteCredentialIO: DataciteCredentialIO;
}

export interface SetEpicMinterCredentialRequest {
    epicCredentialIO: EpicCredentialIO;
}

export interface TriggerRequest {
    fileMigrationTriggerIO: FileMigrationTriggerIO;
}

export interface TriggerIngestRequest {
    appId: string;
}

export interface UploadOntologyRequest {
    file?: Blob;
    metadata?: string;
}

/**
 * 
 */
export class AdminApi extends runtime.BaseAPI {

    /**
     * Computes a SHA-256 digest of each requested Activity\'s JSON-LD serialisation and submits it to the configured ledger provider (Bloxberg or OpenTimestamps). Returns a jobId immediately; poll GET /anchor/{jobId} for status. Requires instance-admin. Feature must be enabled via shepard.ledger.enabled=true.
     * [v2] Anchor Activity records on a distributed ledger.
     */
    async anchorRaw(requestParameters: AnchorRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LedgerAnchorJob>> {
        if (requestParameters['ledgerAnchorRequest'] == null) {
            throw new runtime.RequiredError(
                'ledgerAnchorRequest',
                'Required parameter "ledgerAnchorRequest" was null or undefined when calling anchor().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/ledger/anchor`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LedgerAnchorRequestToJSON(requestParameters['ledgerAnchorRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LedgerAnchorJobFromJSON(jsonValue));
    }

    /**
     * Computes a SHA-256 digest of each requested Activity\'s JSON-LD serialisation and submits it to the configured ledger provider (Bloxberg or OpenTimestamps). Returns a jobId immediately; poll GET /anchor/{jobId} for status. Requires instance-admin. Feature must be enabled via shepard.ledger.enabled=true.
     * [v2] Anchor Activity records on a distributed ledger.
     */
    async anchor(requestParameters: AnchorRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LedgerAnchorJob> {
        const response = await this.anchorRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Parses the YAML body, matches source and target DataObjects via their urn:shepard:mffd:* SemanticAnnotation predicates, and MERGEs (s)-[r:has_successor]->(t) edges between each (source × target) pair. The MERGE is idempotent: re-running converges; mutating an entry\'s transitionKind and re-running updates the existing edge. The loader never deletes edges. Records a single :Activity (EXECUTE / MffdProcessChainMapping) with the matched / unmatched / edgesCreated counters in the summary.
     * [v2] Apply a MFFD process-chain mapping YAML payload (admin-only).
     */
    async applyRaw(requestParameters: ApplyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProcessChainMappingResultIO>> {
        if (requestParameters['body'] == null) {
            throw new runtime.RequiredError(
                'body',
                'Required parameter "body" was null or undefined when calling apply().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'text/yaml';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/mffd/process-chain-mapping`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProcessChainMappingResultIOFromJSON(jsonValue));
    }

    /**
     * Parses the YAML body, matches source and target DataObjects via their urn:shepard:mffd:* SemanticAnnotation predicates, and MERGEs (s)-[r:has_successor]->(t) edges between each (source × target) pair. The MERGE is idempotent: re-running converges; mutating an entry\'s transitionKind and re-running updates the existing edge. The loader never deletes edges. Records a single :Activity (EXECUTE / MffdProcessChainMapping) with the matched / unmatched / edgesCreated counters in the summary.
     * [v2] Apply a MFFD process-chain mapping YAML payload (admin-only).
     */
    async apply(requestParameters: ApplyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProcessChainMappingResultIO> {
        const response = await this.applyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Consume the one-shot bootstrap token + grant instance-admin to a user. Unauthenticated — the token is the auth proof. Token-replay returns 403.
     * [v2] Bootstrap
     */
    async bootstrapRaw(requestParameters: BootstrapOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['bootstrapRequest'] == null) {
            throw new runtime.RequiredError(
                'bootstrapRequest',
                'Required parameter "bootstrapRequest" was null or undefined when calling bootstrap().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/bootstrap`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BootstrapRequestToJSON(requestParameters['bootstrapRequest']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Consume the one-shot bootstrap token + grant instance-admin to a user. Unauthenticated — the token is the auth proof. Token-replay returns 403.
     * [v2] Bootstrap
     */
    async bootstrap(requestParameters: BootstrapOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.bootstrapRaw(requestParameters, initOverrides);
    }

    /**
     * Wipes :DataciteMinterConfig.passwordCipher + .passwordHash. Subsequent mint calls throw publish.minter.failed until a fresh credential is set. The action is captured as an :Activity row via PROV1a.
     * [v2] Clear the stored DataCite credential.
     */
    async clearDataciteMinterCredentialRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DataciteMinterConfigIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/minters/datacite/credential`,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DataciteMinterConfigIOFromJSON(jsonValue));
    }

    /**
     * Wipes :DataciteMinterConfig.passwordCipher + .passwordHash. Subsequent mint calls throw publish.minter.failed until a fresh credential is set. The action is captured as an :Activity row via PROV1a.
     * [v2] Clear the stored DataCite credential.
     */
    async clearDataciteMinterCredential(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DataciteMinterConfigIO> {
        const response = await this.clearDataciteMinterCredentialRaw(initOverrides);
        return await response.value();
    }

    /**
     * Wipes :EpicMinterConfig.credentialKey + .credentialHash. Subsequent mint calls throw publish.minter.failed until a fresh credential is set. The action is captured as an :Activity row via PROV1a.
     * [v2] Clear the stored ePIC credential.
     */
    async clearEpicMinterCredentialRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EpicMinterConfigIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/minters/epic/credential`,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EpicMinterConfigIOFromJSON(jsonValue));
    }

    /**
     * Wipes :EpicMinterConfig.credentialKey + .credentialHash. Subsequent mint calls throw publish.minter.failed until a fresh credential is set. The action is captured as an :Activity row via PROV1a.
     * [v2] Clear the stored ePIC credential.
     */
    async clearEpicMinterCredential(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EpicMinterConfigIO> {
        const response = await this.clearEpicMinterCredentialRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates a :NotificationTransport row. Required fields: `kind` (must be a valid TransportKind), `name`. All other fields are optional and kind-specific. Credentials (smtpPassword, matrixAccessToken) are accepted on write and stored — they will NOT appear in the GET response (the read-side IO omits them by type). Returns 201 + the newly-minted appId in the body.
     * [v2] Create a new notification transport.
     */
    async createNotificationTransportRaw(requestParameters: CreateNotificationTransportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NotificationTransportReadIO>> {
        if (requestParameters['notificationTransportWriteIO'] == null) {
            throw new runtime.RequiredError(
                'notificationTransportWriteIO',
                'Required parameter "notificationTransportWriteIO" was null or undefined when calling createNotificationTransport().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/notifications/transports`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: NotificationTransportWriteIOToJSON(requestParameters['notificationTransportWriteIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NotificationTransportReadIOFromJSON(jsonValue));
    }

    /**
     * Creates a :NotificationTransport row. Required fields: `kind` (must be a valid TransportKind), `name`. All other fields are optional and kind-specific. Credentials (smtpPassword, matrixAccessToken) are accepted on write and stored — they will NOT appear in the GET response (the read-side IO omits them by type). Returns 201 + the newly-minted appId in the body.
     * [v2] Create a new notification transport.
     */
    async createNotificationTransport(requestParameters: CreateNotificationTransportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NotificationTransportReadIO> {
        const response = await this.createNotificationTransportRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates an OntologyGitSource record. The source is not immediately ingested — call .../ingest to trigger an on-demand run, or wait for the nightly scheduler (02:00 server-local, when shepard.tpl5.git-ingest.enabled=true). Required fields: name, repoUrl, pathPattern. Defaults: branch=main, enabled=true.
     * [v2] Register a new ontology git source.
     */
    async createSemanticGitSourceRaw(requestParameters: CreateSemanticGitSourceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OntologyGitSourceIO>> {
        if (requestParameters['ontologyGitSourceIO'] == null) {
            throw new runtime.RequiredError(
                'ontologyGitSourceIO',
                'Required parameter "ontologyGitSourceIO" was null or undefined when calling createSemanticGitSource().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/semantic/git-sources`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: OntologyGitSourceIOToJSON(requestParameters['ontologyGitSourceIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OntologyGitSourceIOFromJSON(jsonValue));
    }

    /**
     * Creates an OntologyGitSource record. The source is not immediately ingested — call .../ingest to trigger an on-demand run, or wait for the nightly scheduler (02:00 server-local, when shepard.tpl5.git-ingest.enabled=true). Required fields: name, repoUrl, pathPattern. Defaults: branch=main, enabled=true.
     * [v2] Register a new ontology git source.
     */
    async createSemanticGitSource(requestParameters: CreateSemanticGitSourceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OntologyGitSourceIO> {
        const response = await this.createSemanticGitSourceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * [v2] Revoke the current harvest API key (DELETE-verb variant of revoke).
     */
    async deleteHarvestKeyRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UnhideConfigIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/unhide/harvest-key`,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UnhideConfigIOFromJSON(jsonValue));
    }

    /**
     * [v2] Revoke the current harvest API key (DELETE-verb variant of revoke).
     */
    async deleteHarvestKey(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UnhideConfigIO> {
        const response = await this.deleteHarvestKeyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Removes the :NotificationTransport row identified by appId. No cascade — historical :Activity rows referencing the deleted transport\'s appId are preserved per the CLAUDE.md \"audit trail is a graph\" rule. Returns 204 on success, 404 when the appId is unknown.
     * [v2] Delete a notification transport.
     */
    async deleteNotificationTransportRaw(requestParameters: DeleteNotificationTransportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling deleteNotificationTransport().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/notifications/transports/{appId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes the :NotificationTransport row identified by appId. No cascade — historical :Activity rows referencing the deleted transport\'s appId are preserved per the CLAUDE.md \"audit trail is a graph\" rule. Returns 204 on success, 404 when the appId is unknown.
     * [v2] Delete a notification transport.
     */
    async deleteNotificationTransport(requestParameters: DeleteNotificationTransportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteNotificationTransportRaw(requestParameters, initOverrides);
    }

    /**
     * Drops the on-disk TTL + the :UserOntologyBundle catalogue row. Refused (409 RFC 7807 semantic.bundle.builtin-not-removable) for built-in bundle ids — those ship in the JAR and update via release upgrades.
     * [v2] Remove an operator-uploaded ontology bundle.
     */
    async deleteOntologyRaw(requestParameters: DeleteOntologyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['bundleId'] == null) {
            throw new runtime.RequiredError(
                'bundleId',
                'Required parameter "bundleId" was null or undefined when calling deleteOntology().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/semantic/ontologies/{bundleId}`.replace(`{${"bundleId"}}`, encodeURIComponent(String(requestParameters['bundleId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Drops the on-disk TTL + the :UserOntologyBundle catalogue row. Refused (409 RFC 7807 semantic.bundle.builtin-not-removable) for built-in bundle ids — those ship in the JAR and update via release upgrades.
     * [v2] Remove an operator-uploaded ontology bundle.
     */
    async deleteOntology(requestParameters: DeleteOntologyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteOntologyRaw(requestParameters, initOverrides);
    }

    /**
     * Removes the OntologyGitSource record. Does NOT remove the UserOntologyBundle entries that were previously ingested from this source — those remain in the catalogue and must be deleted separately via DELETE /v2/admin/semantic/ontologies/{bundleId} if desired.
     * [v2] Delete an ontology git source.
     */
    async deleteSemanticGitSourceRaw(requestParameters: DeleteSemanticGitSourceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling deleteSemanticGitSource().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/semantic/git-sources/{appId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes the OntologyGitSource record. Does NOT remove the UserOntologyBundle entries that were previously ingested from this source — those remain in the catalogue and must be deleted separately via DELETE /v2/admin/semantic/ontologies/{bundleId} if desired.
     * [v2] Delete an ontology git source.
     */
    async deleteSemanticGitSource(requestParameters: DeleteSemanticGitSourceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSemanticGitSourceRaw(requestParameters, initOverrides);
    }

    /**
     * Adds the id to :SemanticConfig.disabledBundles. The bundle stops seeding on the next startup. Refused (409 RFC 7807 semantic.bundle.required) for bundles whose manifest entry carries required=true (prov-o, obo-relations today).
     * [v2] Runtime-disable an ontology bundle.
     */
    async disableOntologyRaw(requestParameters: DisableOntologyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OntologyBundleIO>> {
        if (requestParameters['bundleId'] == null) {
            throw new runtime.RequiredError(
                'bundleId',
                'Required parameter "bundleId" was null or undefined when calling disableOntology().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/semantic/ontologies/{bundleId}/disable`.replace(`{${"bundleId"}}`, encodeURIComponent(String(requestParameters['bundleId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OntologyBundleIOFromJSON(jsonValue));
    }

    /**
     * Adds the id to :SemanticConfig.disabledBundles. The bundle stops seeding on the next startup. Refused (409 RFC 7807 semantic.bundle.required) for bundles whose manifest entry carries required=true (prov-o, obo-relations today).
     * [v2] Runtime-disable an ontology bundle.
     */
    async disableOntology(requestParameters: DisableOntologyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OntologyBundleIO> {
        const response = await this.disableOntologyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes the id from :SemanticConfig.disabledBundles. No-op if the id is not currently disabled. The bundle seeds on the next startup.
     * [v2] Runtime-enable a previously-disabled ontology bundle.
     */
    async enableOntologyRaw(requestParameters: EnableOntologyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OntologyBundleIO>> {
        if (requestParameters['bundleId'] == null) {
            throw new runtime.RequiredError(
                'bundleId',
                'Required parameter "bundleId" was null or undefined when calling enableOntology().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/semantic/ontologies/{bundleId}/enable`.replace(`{${"bundleId"}}`, encodeURIComponent(String(requestParameters['bundleId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OntologyBundleIOFromJSON(jsonValue));
    }

    /**
     * Removes the id from :SemanticConfig.disabledBundles. No-op if the id is not currently disabled. The bundle seeds on the next startup.
     * [v2] Runtime-enable a previously-disabled ontology bundle.
     */
    async enableOntology(requestParameters: EnableOntologyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OntologyBundleIO> {
        const response = await this.enableOntologyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns all discovered FileStorage adapters with their id, enabled state, and which one is active for new uploads. The active provider is set via shepard.storage.provider (deploy-time only).
     * [v2] List all file-storage adapters.
     */
    async getAdminStorageRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StorageStatusIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/storage`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StorageStatusIOFromJSON(jsonValue));
    }

    /**
     * Returns all discovered FileStorage adapters with their id, enabled state, and which one is active for new uploads. The active provider is set via shepard.storage.provider (deploy-time only).
     * [v2] List all file-storage adapters.
     */
    async getAdminStorage(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StorageStatusIO> {
        const response = await this.getAdminStorageRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the runtime config for every known AiCapability slot. The raw API key is never returned — the response carries apiKeySet=true/false and the masked sentinel \'***\' in the apiKey field when a key is configured.
     * [v2] List all AI capability slot configurations.
     */
    async getAllCapabilitiesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AiCapabilityConfigIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/ai/capabilities`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AiCapabilityConfigIOFromJSON(jsonValue));
    }

    /**
     * Returns the runtime config for every known AiCapability slot. The raw API key is never returned — the response carries apiKeySet=true/false and the masked sentinel \'***\' in the apiKey field when a key is configured.
     * [v2] List all AI capability slot configurations.
     */
    async getAllCapabilities(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AiCapabilityConfigIO> {
        const response = await this.getAllCapabilitiesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns all Activity nodes linked to the given DataObject that carry a non-null ledgerAnchor field.  Useful for auditors who need to verify tamper evidence for a specific DataObject without knowing Activity appIds in advance.
     * [v2] List ledger anchors for a DataObject.
     */
    async getAnchorsForDataObjectRaw(requestParameters: GetAnchorsForDataObjectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling getAnchorsForDataObject().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/ledger/data-objects/{appId}/ledger-anchors`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Returns all Activity nodes linked to the given DataObject that carry a non-null ledgerAnchor field.  Useful for auditors who need to verify tamper evidence for a specific DataObject without knowing Activity appIds in advance.
     * [v2] List ledger anchors for a DataObject.
     */
    async getAnchorsForDataObject(requestParameters: GetAnchorsForDataObjectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getAnchorsForDataObjectRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the runtime config for the named capability. If no config has been set for this slot yet, a disabled skeleton is seeded and returned. Unknown capability names return 404.
     * [v2] Get the config for one AI capability slot.
     */
    async getCapabilityRaw(requestParameters: GetCapabilityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AiCapabilityConfigIO>> {
        if (requestParameters['capability'] == null) {
            throw new runtime.RequiredError(
                'capability',
                'Required parameter "capability" was null or undefined when calling getCapability().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/ai/capabilities/{capability}`.replace(`{${"capability"}}`, encodeURIComponent(String(requestParameters['capability']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AiCapabilityConfigIOFromJSON(jsonValue));
    }

    /**
     * Returns the runtime config for the named capability. If no config has been set for this slot yet, a disabled skeleton is seeded and returned. Unknown capability names return 404.
     * [v2] Get the config for one AI capability slot.
     */
    async getCapability(requestParameters: GetCapabilityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AiCapabilityConfigIO> {
        const response = await this.getCapabilityRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Resolves the ConfigDescriptor for {feature} and returns its current, fully-resolved config shape. 404 (problem+json) when no feature is registered under that key.
     * [v2] Read the current config for a feature.
     */
    async getFeatureConfigRaw(requestParameters: GetFeatureConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['feature'] == null) {
            throw new runtime.RequiredError(
                'feature',
                'Required parameter "feature" was null or undefined when calling getFeatureConfig().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/config/{feature}`.replace(`{${"feature"}}`, encodeURIComponent(String(requestParameters['feature']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Resolves the ConfigDescriptor for {feature} and returns its current, fully-resolved config shape. 404 (problem+json) when no feature is registered under that key.
     * [v2] Read the current config for a feature.
     */
    async getFeatureConfig(requestParameters: GetFeatureConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getFeatureConfigRaw(requestParameters, initOverrides);
    }

    /**
     * Returns current status (queued | running | complete | failed) and a human-readable summary.  Poll until status is \'complete\' or \'failed\'.
     * [v2] Poll an anchor job by jobId.
     */
    async getJobRaw(requestParameters: GetJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LedgerAnchorJob>> {
        if (requestParameters['jobId'] == null) {
            throw new runtime.RequiredError(
                'jobId',
                'Required parameter "jobId" was null or undefined when calling getJob().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/ledger/anchor/{jobId}`.replace(`{${"jobId"}}`, encodeURIComponent(String(requestParameters['jobId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LedgerAnchorJobFromJSON(jsonValue));
    }

    /**
     * Returns current status (queued | running | complete | failed) and a human-readable summary.  Poll until status is \'complete\' or \'failed\'.
     * [v2] Poll an anchor job by jobId.
     */
    async getJob(requestParameters: GetJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LedgerAnchorJob> {
        const response = await this.getJobRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns total hits, per-endpoint breakdown, per-principal breakdown, and first/most-recent hit timestamps. Process restart resets the counters. The topN query parameter caps both breakdown lists (default 50, max 1000).
     * [v2] Read the in-memory v1 hit-stats snapshot.
     */
    async getLegacyV1StatsRaw(requestParameters: GetLegacyV1StatsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LegacyV1StatsIO>> {
        const queryParameters: any = {};

        if (requestParameters['topN'] != null) {
            queryParameters['topN'] = requestParameters['topN'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/legacy/v1/stats`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LegacyV1StatsIOFromJSON(jsonValue));
    }

    /**
     * Returns total hits, per-endpoint breakdown, per-principal breakdown, and first/most-recent hit timestamps. Process restart resets the counters. The topN query parameter caps both breakdown lists (default 50, max 1000).
     * [v2] Read the in-memory v1 hit-stats snapshot.
     */
    async getLegacyV1Stats(requestParameters: GetLegacyV1StatsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LegacyV1StatsIO> {
        const response = await this.getLegacyV1StatsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the list of registered peer Shepard instances. Public endpoint — no JWT required (same posture as /v2/instance/capabilities). Used by the frontend badge hover to resolve an instance ID to a friendly name before the user authenticates. An empty \'instances\' list means no peer instances have been registered yet.
     * [v2] Read the current :InstanceRegistry singleton.
     */
    async getRegistryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InstanceRegistryIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/instances`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InstanceRegistryIOFromJSON(jsonValue));
    }

    /**
     * Returns the list of registered peer Shepard instances. Public endpoint — no JWT required (same posture as /v2/instance/capabilities). Used by the frontend badge hover to resolve an instance ID to a friendly name before the user authenticates. An empty \'instances\' list means no peer instances have been registered yet.
     * [v2] Read the current :InstanceRegistry singleton.
     */
    async getRegistry(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InstanceRegistryIO> {
        const response = await this.getRegistryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates a `:InstanceAdminGrant` node in Neo4j recording who granted the role and when. The OIDC role is unaffected by this call.
     * [v2] Grant the instance-admin role to a user.
     */
    async grantInstanceAdminRaw(requestParameters: GrantInstanceAdminRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InstanceAdminGrant>> {
        if (requestParameters['grantInstanceAdmin'] == null) {
            throw new runtime.RequiredError(
                'grantInstanceAdmin',
                'Required parameter "grantInstanceAdmin" was null or undefined when calling grantInstanceAdmin().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/instance-admins`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: GrantInstanceAdminToJSON(requestParameters['grantInstanceAdmin']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InstanceAdminGrantFromJSON(jsonValue));
    }

    /**
     * Creates a `:InstanceAdminGrant` node in Neo4j recording who granted the role and when. The OIDC role is unaffected by this call.
     * [v2] Grant the instance-admin role to a user.
     */
    async grantInstanceAdmin(requestParameters: GrantInstanceAdminRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InstanceAdminGrant> {
        const response = await this.grantInstanceAdminRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns all registered feature toggles with their current enabled state. Changes made via PATCH take effect immediately in the running JVM but are not persisted across restarts — the config-property value is restored on next startup.
     * [v2] List runtime feature toggles.
     */
    async listFeatureTogglesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponseFeatureToggle>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/features`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseFeatureToggleFromJSON(jsonValue));
    }

    /**
     * Returns all registered feature toggles with their current enabled state. Changes made via PATCH take effect immediately in the running JVM but are not persisted across restarts — the config-property value is restored on next startup.
     * [v2] List runtime feature toggles.
     */
    async listFeatureToggles(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponseFeatureToggle> {
        const response = await this.listFeatureTogglesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one row per registered config feature — the {feature} path segment plus a description. Use the returned keys with GET|PATCH /v2/admin/config/{feature}.
     * [v2] List runtime-configurable features.
     */
    async listFeaturesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ConfigFeature>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/config`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ConfigFeatureFromJSON));
    }

    /**
     * Returns one row per registered config feature — the {feature} path segment plus a description. Use the returned keys with GET|PATCH /v2/admin/config/{feature}.
     * [v2] List runtime-configurable features.
     */
    async listFeatures(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ConfigFeature>> {
        const response = await this.listFeaturesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns all active `:InstanceAdminGrant` nodes with their `grantedBy` and `grantedAt` audit fields.
     * [v2] List Neo4j-side instance-admin grants.
     */
    async listInstanceAdminsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<InstanceAdminGrant>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/instance-admins`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(InstanceAdminGrantFromJSON));
    }

    /**
     * Returns all active `:InstanceAdminGrant` nodes with their `grantedBy` and `grantedAt` audit fields.
     * [v2] List Neo4j-side instance-admin grants.
     */
    async listInstanceAdmins(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<InstanceAdminGrant>> {
        const response = await this.listInstanceAdminsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns every :NotificationTransport row in the instance, ordered by name ascending. CREDENTIAL FIELDS ARE OMITTED — smtpPassword + matrixAccessToken never appear on this response (compile-time guarantee via NotificationTransportReadIO). Gated on the instance-admin role.
     * [v2] List all configured notification transports.
     */
    async listNotificationTransportsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NotificationTransportListIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/notifications/transports`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NotificationTransportListIOFromJSON(jsonValue));
    }

    /**
     * Returns every :NotificationTransport row in the instance, ordered by name ascending. CREDENTIAL FIELDS ARE OMITTED — smtpPassword + matrixAccessToken never appear on this response (compile-time guarantee via NotificationTransportReadIO). Gated on the instance-admin role.
     * [v2] List all configured notification transports.
     */
    async listNotificationTransports(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NotificationTransportListIO> {
        const response = await this.listNotificationTransportsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Built-ins first (manifest declaration order), then user-uploaded bundles (id ASC). Each row\'s `enabled` is the effective state under the precedence rules (required wins; otherwise \'not in runtime disabledBundles ∪ deploy-time skip-bundles\').
     * [v2] List every pre-seeded + operator-uploaded ontology bundle.
     */
    async listOntologiesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OntologyBundleListIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/semantic/ontologies`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OntologyBundleListIOFromJSON(jsonValue));
    }

    /**
     * Built-ins first (manifest declaration order), then user-uploaded bundles (id ASC). Each row\'s `enabled` is the effective state under the precedence rules (required wins; otherwise \'not in runtime disabledBundles ∪ deploy-time skip-bundles\').
     * [v2] List every pre-seeded + operator-uploaded ontology bundle.
     */
    async listOntologies(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OntologyBundleListIO> {
        const response = await this.listOntologiesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one row per plugin observed by the PM1a PluginRegistry — including DISABLED + FAILED rows so the operator sees the full state space. Order matches the registry\'s insertion order (classpath scan first, then drop-in JAR walk). The `enabled` column reflects the effective runtime toggle (PATCH override wins; falls through to shepard.plugins.<id>.enabled from application.properties).
     * [v2] List every discovered plugin.
     */
    async listPluginsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PluginList>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/plugins`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PluginListFromJSON(jsonValue));
    }

    /**
     * Returns one row per plugin observed by the PM1a PluginRegistry — including DISABLED + FAILED rows so the operator sees the full state space. Order matches the registry\'s insertion order (classpath scan first, then drop-in JAR walk). The `enabled` column reflects the effective runtime toggle (PATCH override wins; falls through to shepard.plugins.<id>.enabled from application.properties).
     * [v2] List every discovered plugin.
     */
    async listPlugins(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PluginList> {
        const response = await this.listPluginsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns one row per (shell, registry-url) pair tracked in the :AasRegistration outbox. Status is PENDING, SYNCED, or FAILED. Gated on the instance-admin role.
     * [v2] List all AAS registry outbox rows.
     */
    async listRegistrationsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AasRegistrationIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/aas/registrations`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AasRegistrationIOFromJSON(jsonValue));
    }

    /**
     * Returns one row per (shell, registry-url) pair tracked in the :AasRegistration outbox. Status is PENDING, SYNCED, or FAILED. Gated on the instance-admin role.
     * [v2] List all AAS registry outbox rows.
     */
    async listRegistrations(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AasRegistrationIO> {
        const response = await this.listRegistrationsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns every registered OntologyGitSource (enabled and disabled), ordered by name. Includes last-ingest status and error for quick health assessment.
     * [v2] List all ontology git sources.
     */
    async listSemanticGitSourcesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<OntologyGitSourceIO>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/semantic/git-sources`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(OntologyGitSourceIOFromJSON));
    }

    /**
     * Returns every registered OntologyGitSource (enabled and disabled), ordered by name. Includes last-ingest status and error for quick health assessment.
     * [v2] List all ontology git sources.
     */
    async listSemanticGitSources(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<OntologyGitSourceIO>> {
        const response = await this.listSemanticGitSourcesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Mints a :MirroredUser shadow node for a user from a remote Shepard instance. Idempotent: if a node with the same (sourceInstance, sourceUsername) pair already exists, its mutable fields (sourceDisplayName, sourceEmail) are updated and the same appId is returned. Returns 201 on first creation, 200 on subsequent calls (same appId, updated fields). Required fields: sourceInstance, sourceUsername. Auth: instance-admin role only.
     * [v2] Create or update a :MirroredUser node (idempotent).
     */
    async mirrorUsersRaw(requestParameters: MirrorUsersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MirroredUserIO>> {
        if (requestParameters['mirroredUserCreateIO'] == null) {
            throw new runtime.RequiredError(
                'mirroredUserCreateIO',
                'Required parameter "mirroredUserCreateIO" was null or undefined when calling mirrorUsers().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/users/mirror`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MirroredUserCreateIOToJSON(requestParameters['mirroredUserCreateIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MirroredUserIOFromJSON(jsonValue));
    }

    /**
     * Mints a :MirroredUser shadow node for a user from a remote Shepard instance. Idempotent: if a node with the same (sourceInstance, sourceUsername) pair already exists, its mutable fields (sourceDisplayName, sourceEmail) are updated and the same appId is returned. Returns 201 on first creation, 200 on subsequent calls (same appId, updated fields). Required fields: sourceInstance, sourceUsername. Auth: instance-admin role only.
     * [v2] Create or update a :MirroredUser node (idempotent).
     */
    async mirrorUsers(requestParameters: MirrorUsersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MirroredUserIO> {
        const response = await this.mirrorUsersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes every Collection, DataObject, Reference, Container, LabJournalEntry, SemanticAnnotation, Activity, and their secondary-store payloads (MongoDB, Postgres). Users, API keys, and instance configuration are preserved.  The caller must be instance-admin AND send `confirmPhrase: \"yes drop everything\"` in the request body to guard against accidental use.
     * [v2] Nuclear instance reset — wipes all research data (confirmation phrase required).
     */
    async nukeRaw(requestParameters: NukeOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NukeResult>> {
        if (requestParameters['nukeRequest'] == null) {
            throw new runtime.RequiredError(
                'nukeRequest',
                'Required parameter "nukeRequest" was null or undefined when calling nuke().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/instance/nuke`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: NukeRequestToJSON(requestParameters['nukeRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NukeResultFromJSON(jsonValue));
    }

    /**
     * Deletes every Collection, DataObject, Reference, Container, LabJournalEntry, SemanticAnnotation, Activity, and their secondary-store payloads (MongoDB, Postgres). Users, API keys, and instance configuration are preserved.  The caller must be instance-admin AND send `confirmPhrase: \"yes drop everything\"` in the request body to guard against accidental use.
     * [v2] Nuclear instance reset — wipes all research data (confirmation phrase required).
     */
    async nuke(requestParameters: NukeOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NukeResult> {
        const response = await this.nukeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Patchable fields: endpointUrl, model, apiKey, transport, guardrailsPrefix, guardrailsSuffix, maxTokens, temperature, enabled. RFC 7396 semantics — absent = leave alone. Sending apiKey=\'***\' (the masked sentinel from GET) leaves the stored key unchanged. Mutations are captured as :Activity rows by PROV1a\'s ProvenanceCaptureFilter.
     * [v2] RFC 7396 merge-patch one AI capability slot.
     */
    async patchCapabilityRaw(requestParameters: PatchCapabilityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AiCapabilityConfigIO>> {
        if (requestParameters['capability'] == null) {
            throw new runtime.RequiredError(
                'capability',
                'Required parameter "capability" was null or undefined when calling patchCapability().'
            );
        }

        if (requestParameters['aiCapabilityConfigIO'] == null) {
            throw new runtime.RequiredError(
                'aiCapabilityConfigIO',
                'Required parameter "aiCapabilityConfigIO" was null or undefined when calling patchCapability().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/ai/capabilities/{capability}`.replace(`{${"capability"}}`, encodeURIComponent(String(requestParameters['capability']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: AiCapabilityConfigIOToJSON(requestParameters['aiCapabilityConfigIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AiCapabilityConfigIOFromJSON(jsonValue));
    }

    /**
     * Patchable fields: endpointUrl, model, apiKey, transport, guardrailsPrefix, guardrailsSuffix, maxTokens, temperature, enabled. RFC 7396 semantics — absent = leave alone. Sending apiKey=\'***\' (the masked sentinel from GET) leaves the stored key unchanged. Mutations are captured as :Activity rows by PROV1a\'s ProvenanceCaptureFilter.
     * [v2] RFC 7396 merge-patch one AI capability slot.
     */
    async patchCapability(requestParameters: PatchCapabilityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AiCapabilityConfigIO> {
        const response = await this.patchCapabilityRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Resolves the ConfigDescriptor for {feature} and applies an RFC-7396 merge-patch: absent = leave alone, null = clear, value = replace. Returns the updated shape (same as GET). PROV1a\'s ProvenanceCaptureFilter captures this PATCH as an :Activity row. 404 (problem+json) for an unknown feature; 4xx (problem+json) for a validation failure declared by the descriptor.
     * [v2] RFC 7396 merge-patch the config for a feature.
     */
    async patchFeatureConfigRaw(requestParameters: PatchFeatureConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['feature'] == null) {
            throw new runtime.RequiredError(
                'feature',
                'Required parameter "feature" was null or undefined when calling patchFeatureConfig().'
            );
        }

        if (requestParameters['jsonNode'] == null) {
            throw new runtime.RequiredError(
                'jsonNode',
                'Required parameter "jsonNode" was null or undefined when calling patchFeatureConfig().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/merge-patch+json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/config/{feature}`.replace(`{${"feature"}}`, encodeURIComponent(String(requestParameters['feature']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: JsonNodeToJSON(requestParameters['jsonNode']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Resolves the ConfigDescriptor for {feature} and applies an RFC-7396 merge-patch: absent = leave alone, null = clear, value = replace. Returns the updated shape (same as GET). PROV1a\'s ProvenanceCaptureFilter captures this PATCH as an :Activity row. 404 (problem+json) for an unknown feature; 4xx (problem+json) for a validation failure declared by the descriptor.
     * [v2] RFC 7396 merge-patch the config for a feature.
     */
    async patchFeatureConfig(requestParameters: PatchFeatureConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.patchFeatureConfigRaw(requestParameters, initOverrides);
    }

    /**
     * Sets the enabled state of the named feature toggle for the lifetime of the current JVM process. The change does not survive a restart.
     * [v2] Toggle a runtime feature flag.
     */
    async patchFeatureToggleRaw(requestParameters: PatchFeatureToggleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FeatureToggle>> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling patchFeatureToggle().'
            );
        }

        if (requestParameters['patchFeatureToggle'] == null) {
            throw new runtime.RequiredError(
                'patchFeatureToggle',
                'Required parameter "patchFeatureToggle" was null or undefined when calling patchFeatureToggle().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/features/{name}`.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters['name']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PatchFeatureToggleToJSON(requestParameters['patchFeatureToggle']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FeatureToggleFromJSON(jsonValue));
    }

    /**
     * Sets the enabled state of the named feature toggle for the lifetime of the current JVM process. The change does not survive a restart.
     * [v2] Toggle a runtime feature flag.
     */
    async patchFeatureToggle(requestParameters: PatchFeatureToggleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FeatureToggle> {
        const response = await this.patchFeatureToggleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Patches the :NotificationTransport identified by appId. RFC 7396 semantics: absent = leave alone, explicit null = clear, value = replace. Touched flags on the request body distinguish absent from null. The PATCH is captured as an :Activity row by PROV1a\'s ProvenanceCaptureFilter.
     * [v2] RFC 7396 merge-patch an existing notification transport.
     */
    async patchNotificationTransportRaw(requestParameters: PatchNotificationTransportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NotificationTransportReadIO>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling patchNotificationTransport().'
            );
        }

        if (requestParameters['notificationTransportWriteIO'] == null) {
            throw new runtime.RequiredError(
                'notificationTransportWriteIO',
                'Required parameter "notificationTransportWriteIO" was null or undefined when calling patchNotificationTransport().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/merge-patch+json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/notifications/transports/{appId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: NotificationTransportWriteIOToJSON(requestParameters['notificationTransportWriteIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NotificationTransportReadIOFromJSON(jsonValue));
    }

    /**
     * Patches the :NotificationTransport identified by appId. RFC 7396 semantics: absent = leave alone, explicit null = clear, value = replace. Touched flags on the request body distinguish absent from null. The PATCH is captured as an :Activity row by PROV1a\'s ProvenanceCaptureFilter.
     * [v2] RFC 7396 merge-patch an existing notification transport.
     */
    async patchNotificationTransport(requestParameters: PatchNotificationTransportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NotificationTransportReadIO> {
        const response = await this.patchNotificationTransportRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Patchable fields: `enabled` (Boolean). PM1e — the flip is persisted to the :PluginRuntimeOverride table synchronously within this request so it survives restart. Flipping back to the deploy-time default (shepard.plugins.<id>.enabled, default true) DELETEs the override row so the table stays sparse. Returns the updated entry. PROV1a\'s ProvenanceCaptureFilter captures this PATCH as an :Activity row (targetKind=PluginEntry) so the audit trail records who flipped which plugin when; the persisted row also carries updatedBy for fast lookups without scanning :Activity.
     * [v2] RFC 7396 merge-patch a plugin\'s enabled toggle.
     */
    async patchPluginRaw(requestParameters: PatchPluginRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PluginEntry>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling patchPlugin().'
            );
        }

        if (requestParameters['pluginPatch'] == null) {
            throw new runtime.RequiredError(
                'pluginPatch',
                'Required parameter "pluginPatch" was null or undefined when calling patchPlugin().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/plugins/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PluginPatchToJSON(requestParameters['pluginPatch']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PluginEntryFromJSON(jsonValue));
    }

    /**
     * Patchable fields: `enabled` (Boolean). PM1e — the flip is persisted to the :PluginRuntimeOverride table synchronously within this request so it survives restart. Flipping back to the deploy-time default (shepard.plugins.<id>.enabled, default true) DELETEs the override row so the table stays sparse. Returns the updated entry. PROV1a\'s ProvenanceCaptureFilter captures this PATCH as an :Activity row (targetKind=PluginEntry) so the audit trail records who flipped which plugin when; the persisted row also carries updatedBy for fast lookups without scanning :Activity.
     * [v2] RFC 7396 merge-patch a plugin\'s enabled toggle.
     */
    async patchPlugin(requestParameters: PatchPluginRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PluginEntry> {
        const response = await this.patchPluginRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces the \'instances\' list atomically (RFC 7396 array semantics). Absent = leave current list alone; null = clear the list (set to []); present array (even []) = full replace. No element-level merge is performed. PROV1a\'s ProvenanceCaptureFilter captures this PATCH as an :Activity row. Requires the instance-admin role.
     * [v2] RFC 7396 merge-patch the :InstanceRegistry singleton.
     */
    async patchRegistryRaw(requestParameters: PatchRegistryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InstanceRegistryIO>> {
        if (requestParameters['instanceRegistryPatchIO'] == null) {
            throw new runtime.RequiredError(
                'instanceRegistryPatchIO',
                'Required parameter "instanceRegistryPatchIO" was null or undefined when calling patchRegistry().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/instances`,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: InstanceRegistryPatchIOToJSON(requestParameters['instanceRegistryPatchIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InstanceRegistryIOFromJSON(jsonValue));
    }

    /**
     * Replaces the \'instances\' list atomically (RFC 7396 array semantics). Absent = leave current list alone; null = clear the list (set to []); present array (even []) = full replace. No element-level merge is performed. PROV1a\'s ProvenanceCaptureFilter captures this PATCH as an :Activity row. Requires the instance-admin role.
     * [v2] RFC 7396 merge-patch the :InstanceRegistry singleton.
     */
    async patchRegistry(requestParameters: PatchRegistryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InstanceRegistryIO> {
        const response = await this.patchRegistryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns all `:BasicEntity` nodes that have no `:has_permissions` edge (post-C3 integrity check). Run the repair endpoint to recreate the missing edges.
     * [v2] List entities with orphaned permissions.
     */
    async permissionAuditRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<PermissionAuditEntry>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/permission-audit`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(PermissionAuditEntryFromJSON));
    }

    /**
     * Returns all `:BasicEntity` nodes that have no `:has_permissions` edge (post-C3 integrity check). Run the repair endpoint to recreate the missing edges.
     * [v2] List entities with orphaned permissions.
     */
    async permissionAudit(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<PermissionAuditEntry>> {
        const response = await this.permissionAuditRaw(initOverrides);
        return await response.value();
    }

    /**
     * F3: Query the Postgres permission audit log. Returns GRANT/REVOKE/UPDATE events sorted by occurred_at DESC. Supports optional filters: entityAppId, actor, from/to (ISO-8601), and pagination via page/size.
     * [v2] Permission Audit Log
     */
    async permissionAuditLogRaw(requestParameters: PermissionAuditLogRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<PermissionAuditLogEntry>>> {
        const queryParameters: any = {};

        if (requestParameters['actor'] != null) {
            queryParameters['actor'] = requestParameters['actor'];
        }

        if (requestParameters['entityAppId'] != null) {
            queryParameters['entityAppId'] = requestParameters['entityAppId'];
        }

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/permission-audit/log`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(PermissionAuditLogEntryFromJSON));
    }

    /**
     * F3: Query the Postgres permission audit log. Returns GRANT/REVOKE/UPDATE events sorted by occurred_at DESC. Supports optional filters: entityAppId, actor, from/to (ISO-8601), and pagination via page/size.
     * [v2] Permission Audit Log
     */
    async permissionAuditLog(requestParameters: PermissionAuditLogRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<PermissionAuditLogEntry>> {
        const response = await this.permissionAuditLogRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Walks every bundle in ontologies-manifest.json (or the subset named in request.bundles), fetches each bundle\'s canonicalUrl, recomputes its SHA-256, and re-imports into n10s when the hash differs. Use after a major ontology release, or to land the full canonical Turtle in bulk over the shipped minimum-viable stubs (per N1b / ADR-0019). Best-effort per bundle — partial failures return 200 with errors[].
     * [v2] Refresh bundled ontologies against their pinned canonical URLs.
     */
    async refreshOntologiesRaw(requestParameters: RefreshOntologiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RefreshOntologiesResult>> {
        if (requestParameters['refreshOntologiesRequest'] == null) {
            throw new runtime.RequiredError(
                'refreshOntologiesRequest',
                'Required parameter "refreshOntologiesRequest" was null or undefined when calling refreshOntologies().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/semantic/refresh-ontologies`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RefreshOntologiesRequestToJSON(requestParameters['refreshOntologiesRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RefreshOntologiesResultFromJSON(jsonValue));
    }

    /**
     * Walks every bundle in ontologies-manifest.json (or the subset named in request.bundles), fetches each bundle\'s canonicalUrl, recomputes its SHA-256, and re-imports into n10s when the hash differs. Use after a major ontology release, or to land the full canonical Turtle in bulk over the shipped minimum-viable stubs (per N1b / ADR-0019). Best-effort per bundle — partial failures return 200 with errors[].
     * [v2] Refresh bundled ontologies against their pinned canonical URLs.
     */
    async refreshOntologies(requestParameters: RefreshOntologiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RefreshOntologiesResult> {
        const response = await this.refreshOntologiesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Pages through every :SemanticAnnotation node in batches of 500, re-resolves propertyIRI and valueIRI against the live n10s repository, and writes back the propertyName / valueName snapshot only when the label has changed. Use after importing or updating an ontology bundle to propagate new labels to existing annotations. Runs synchronously; returns the count of updated nodes.
     * [v2] Refresh stale SemanticAnnotation label snapshots.
     */
    async refreshSnapshotsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RefreshSnapshotsResult>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/semantic/refresh-snapshots`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RefreshSnapshotsResultFromJSON(jsonValue));
    }

    /**
     * Pages through every :SemanticAnnotation node in batches of 500, re-resolves propertyIRI and valueIRI against the live n10s repository, and writes back the propertyName / valueName snapshot only when the label has changed. Use after importing or updating an ontology bundle to propagate new labels to existing annotations. Runs synchronously; returns the count of updated nodes.
     * [v2] Refresh stale SemanticAnnotation label snapshots.
     */
    async refreshSnapshots(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RefreshSnapshotsResult> {
        const response = await this.refreshSnapshotsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Clears :UnhideConfig.harvestApiKeyHash. When feedPublic=false (the default), the feed becomes reachable only by instance-admin callers until a fresh key is minted. The revoke is captured as an :Activity row via PROV1a.
     * [v2] Revoke the current harvest API key.
     */
    async revokeHarvestKeyRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UnhideConfigIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/unhide/harvest-key/revoke`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UnhideConfigIOFromJSON(jsonValue));
    }

    /**
     * Clears :UnhideConfig.harvestApiKeyHash. When feedPublic=false (the default), the feed becomes reachable only by instance-admin callers until a fresh key is minted. The revoke is captured as an :Activity row via PROV1a.
     * [v2] Revoke the current harvest API key.
     */
    async revokeHarvestKey(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UnhideConfigIO> {
        const response = await this.revokeHarvestKeyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Removes the `:InstanceAdminGrant` node for the named user. The OIDC role (if any) is not affected by this call.
     * [v2] Revoke the Neo4j-side instance-admin grant from a user.
     */
    async revokeInstanceAdminRaw(requestParameters: RevokeInstanceAdminRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['username'] == null) {
            throw new runtime.RequiredError(
                'username',
                'Required parameter "username" was null or undefined when calling revokeInstanceAdmin().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/instance-admins/{username}`.replace(`{${"username"}}`, encodeURIComponent(String(requestParameters['username']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes the `:InstanceAdminGrant` node for the named user. The OIDC role (if any) is not affected by this call.
     * [v2] Revoke the Neo4j-side instance-admin grant from a user.
     */
    async revokeInstanceAdmin(requestParameters: RevokeInstanceAdminRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.revokeInstanceAdminRaw(requestParameters, initOverrides);
    }

    /**
     * FS1e3 — per-file rollback. Re-writes the file\'s bytes from the current adapter (providerId) back to the previous adapter (previousProviderId), using the preserved previousLocator, then restores providerId and clears the FS1e3 bookkeeping fields (previousProviderId, previousLocator, migratedAt, migrationHmac). Synchronous — the response carries the outcome.  Refuses with 409 when the :ShepardFile row\'s previousProviderId is null (never migrated, or already rolled back). Refuses with 404 when the appId is unknown. Caller must hold the instance-admin role.  Note: this endpoint re-writes bytes; it does NOT delete from the current adapter. The orphaned current-adapter bytes are catalogued by a future \'sweep-orphans\' verb (runbook §17.E).
     * [v2] Roll back a single file\'s storage-adapter swap.
     */
    async rollbackRaw(requestParameters: RollbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling rollback().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/files/migrate/rollback/{appId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * FS1e3 — per-file rollback. Re-writes the file\'s bytes from the current adapter (providerId) back to the previous adapter (previousProviderId), using the preserved previousLocator, then restores providerId and clears the FS1e3 bookkeeping fields (previousProviderId, previousLocator, migratedAt, migrationHmac). Synchronous — the response carries the outcome.  Refuses with 409 when the :ShepardFile row\'s previousProviderId is null (never migrated, or already rolled back). Refuses with 404 when the appId is unknown. Caller must hold the instance-admin role.  Note: this endpoint re-writes bytes; it does NOT delete from the current adapter. The orphaned current-adapter bytes are catalogued by a future \'sweep-orphans\' verb (runbook §17.E).
     * [v2] Roll back a single file\'s storage-adapter swap.
     */
    async rollback(requestParameters: RollbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.rollbackRaw(requestParameters, initOverrides);
    }

    /**
     * Generates a UUID v4 (SecureRandom-backed), stores its SHA-256 hex on :UnhideConfig.harvestApiKeyHash, and returns the plaintext exactly once. Rotates if a key already exists (the prior hash is replaced, the prior plaintext becomes unusable). The response body is NOT recorded in :Activity — PROV1a\'s ProvenanceCaptureFilter captures only the request method + path + status, never response bodies, so the plaintext never enters the audit trail. The plaintext is also never logged server-side; only the masked fingerprint (first 8 hex chars of the SHA-256) appears in INFO logs.
     * [v2] Mint a fresh harvest API key.
     */
    async rotateHarvestKeyRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<HarvestKeyMintedIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/unhide/harvest-key/rotate`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => HarvestKeyMintedIOFromJSON(jsonValue));
    }

    /**
     * Generates a UUID v4 (SecureRandom-backed), stores its SHA-256 hex on :UnhideConfig.harvestApiKeyHash, and returns the plaintext exactly once. Rotates if a key already exists (the prior hash is replaced, the prior plaintext becomes unusable). The response body is NOT recorded in :Activity — PROV1a\'s ProvenanceCaptureFilter captures only the request method + path + status, never response bodies, so the plaintext never enters the audit trail. The plaintext is also never logged server-side; only the masked fingerprint (first 8 hex chars of the SHA-256) appears in INFO logs.
     * [v2] Mint a fresh harvest API key.
     */
    async rotateHarvestKey(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<HarvestKeyMintedIO> {
        const response = await this.rotateHarvestKeyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Publishes a test notification to validate that the notification system is working. The notification appears in the target audience\'s bell panel within one poll cycle (~30 seconds). Use audience=INSTANCE_ADMIN to send only to admins, or audience=USER with targetUsername to target a specific user. This endpoint is the acceptance test for NTF1a.
     * [v2] Send a test in-app notification.
     */
    async sendTestRaw(requestParameters: SendTestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['testNotificationIO'] == null) {
            throw new runtime.RequiredError(
                'testNotificationIO',
                'Required parameter "testNotificationIO" was null or undefined when calling sendTest().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/notifications/test`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: TestNotificationIOToJSON(requestParameters['testNotificationIO']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Publishes a test notification to validate that the notification system is working. The notification appears in the target audience\'s bell panel within one poll cycle (~30 seconds). Use audience=INSTANCE_ADMIN to send only to admins, or audience=USER with targetUsername to target a specific user. This endpoint is the acceptance test for NTF1a.
     * [v2] Send a test in-app notification.
     */
    async sendTest(requestParameters: SendTestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.sendTestRaw(requestParameters, initOverrides);
    }

    /**
     * Body: {\"password\": \"<plaintext>\"}. The plaintext is encrypted with AES-GCM keyed off the shepard instance id and stored on :DataciteMinterConfig. The response carries only the fingerprint (first 8 hex of the SHA-256) — the plaintext is never echoed. ProvenanceCaptureFilter captures the request method + path + status only, so the plaintext does not enter the audit trail.
     * [v2] Set or rotate the DataCite Member password.
     */
    async setDataciteMinterCredentialRaw(requestParameters: SetDataciteMinterCredentialRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DataciteCredentialSetIO>> {
        if (requestParameters['dataciteCredentialIO'] == null) {
            throw new runtime.RequiredError(
                'dataciteCredentialIO',
                'Required parameter "dataciteCredentialIO" was null or undefined when calling setDataciteMinterCredential().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/minters/datacite/credential`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DataciteCredentialIOToJSON(requestParameters['dataciteCredentialIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DataciteCredentialSetIOFromJSON(jsonValue));
    }

    /**
     * Body: {\"password\": \"<plaintext>\"}. The plaintext is encrypted with AES-GCM keyed off the shepard instance id and stored on :DataciteMinterConfig. The response carries only the fingerprint (first 8 hex of the SHA-256) — the plaintext is never echoed. ProvenanceCaptureFilter captures the request method + path + status only, so the plaintext does not enter the audit trail.
     * [v2] Set or rotate the DataCite Member password.
     */
    async setDataciteMinterCredential(requestParameters: SetDataciteMinterCredentialRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DataciteCredentialSetIO> {
        const response = await this.setDataciteMinterCredentialRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Body: {\"credential\": \"<plaintext>\"}. The plaintext is encrypted with AES-GCM keyed off the shepard instance id and stored on :EpicMinterConfig. The response carries only the fingerprint (first 8 hex of the SHA-256) — the plaintext is never echoed. ProvenanceCaptureFilter captures the request method + path + status only.
     * [v2] Set or rotate the ePIC credential.
     */
    async setEpicMinterCredentialRaw(requestParameters: SetEpicMinterCredentialRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EpicCredentialSetIO>> {
        if (requestParameters['epicCredentialIO'] == null) {
            throw new runtime.RequiredError(
                'epicCredentialIO',
                'Required parameter "epicCredentialIO" was null or undefined when calling setEpicMinterCredential().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/minters/epic/credential`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: EpicCredentialIOToJSON(requestParameters['epicCredentialIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EpicCredentialSetIOFromJSON(jsonValue));
    }

    /**
     * Body: {\"credential\": \"<plaintext>\"}. The plaintext is encrypted with AES-GCM keyed off the shepard instance id and stored on :EpicMinterConfig. The response carries only the fingerprint (first 8 hex of the SHA-256) — the plaintext is never echoed. ProvenanceCaptureFilter captures the request method + path + status only.
     * [v2] Set or rotate the ePIC credential.
     */
    async setEpicMinterCredential(requestParameters: SetEpicMinterCredentialRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EpicCredentialSetIO> {
        const response = await this.setEpicMinterCredentialRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the in-memory state of the most recent migration job (IDLE when none has been triggered since the last restart).
     * [v2] Get the current file-storage migration status.
     */
    async statusRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FileMigrationStateIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/files/migrate/status`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FileMigrationStateIOFromJSON(jsonValue));
    }

    /**
     * Returns the in-memory state of the most recent migration job (IDLE when none has been triggered since the last restart).
     * [v2] Get the current file-storage migration status.
     */
    async status(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FileMigrationStateIO> {
        const response = await this.statusRaw(initOverrides);
        return await response.value();
    }

    /**
     * Issues a GET against <apiBaseUrl>/heartbeat (DataCite\'s documented uptime endpoint) and reports reachable/statusCode/latency. Useful for operators to verify config before enabling minting.
     * [v2] Diagnose connectivity to the configured DataCite API.
     */
    async testDataciteMinterConnectionRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DataciteTestConnectionIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/minters/datacite/test-connection`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DataciteTestConnectionIOFromJSON(jsonValue));
    }

    /**
     * Issues a GET against <apiBaseUrl>/heartbeat (DataCite\'s documented uptime endpoint) and reports reachable/statusCode/latency. Useful for operators to verify config before enabling minting.
     * [v2] Diagnose connectivity to the configured DataCite API.
     */
    async testDataciteMinterConnection(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DataciteTestConnectionIO> {
        const response = await this.testDataciteMinterConnectionRaw(initOverrides);
        return await response.value();
    }

    /**
     * Issues a GET against <apiBaseUrl> (or <apiBaseUrl>/handles if the root redirects) and reports reachable/statusCode/latency. Useful for operators to verify config before enabling minting.
     * [v2] Diagnose connectivity to the configured ePIC API.
     */
    async testEpicMinterConnectionRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EpicTestConnectionIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/minters/epic/test-connection`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EpicTestConnectionIOFromJSON(jsonValue));
    }

    /**
     * Issues a GET against <apiBaseUrl> (or <apiBaseUrl>/handles if the root redirects) and reports reachable/statusCode/latency. Useful for operators to verify config before enabling minting.
     * [v2] Diagnose connectivity to the configured ePIC API.
     */
    async testEpicMinterConnection(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EpicTestConnectionIO> {
        const response = await this.testEpicMinterConnectionRaw(initOverrides);
        return await response.value();
    }

    /**
     * Copies every ShepardFile whose providerId equals sourceProviderId to the targetProviderId adapter, then flips the providerId in Neo4j. Migration runs in the background — the endpoint returns 202 immediately. Poll GET .../status to track progress. Only one migration may run at a time. Re-running after a partial failure is safe (already-migrated files are skipped). OIDs are preserved — existing API clients keep working.
     * [v2] Trigger a file-storage migration.
     */
    async triggerRaw(requestParameters: TriggerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FileMigrationStateIO>> {
        if (requestParameters['fileMigrationTriggerIO'] == null) {
            throw new runtime.RequiredError(
                'fileMigrationTriggerIO',
                'Required parameter "fileMigrationTriggerIO" was null or undefined when calling trigger().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/files/migrate`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FileMigrationTriggerIOToJSON(requestParameters['fileMigrationTriggerIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FileMigrationStateIOFromJSON(jsonValue));
    }

    /**
     * Copies every ShepardFile whose providerId equals sourceProviderId to the targetProviderId adapter, then flips the providerId in Neo4j. Migration runs in the background — the endpoint returns 202 immediately. Poll GET .../status to track progress. Only one migration may run at a time. Re-running after a partial failure is safe (already-migrated files are skipped). OIDs are preserved — existing API clients keep working.
     * [v2] Trigger a file-storage migration.
     */
    async trigger(requestParameters: TriggerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FileMigrationStateIO> {
        const response = await this.triggerRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Clones the repository (shallow, --depth=1), finds files matching pathPattern, and ingests each as a UserOntologyBundle in the internal n10s store. Runs synchronously — returns when the ingest is complete or has failed. The source\'s lastStatus and lastIngestedAt are updated on completion. Works regardless of the source\'s enabled flag.
     * [v2] Trigger an immediate ingest from a git source.
     */
    async triggerIngestRaw(requestParameters: TriggerIngestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OntologyGitIngestResultIO>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling triggerIngest().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/semantic/git-sources/{appId}/ingest`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OntologyGitIngestResultIOFromJSON(jsonValue));
    }

    /**
     * Clones the repository (shallow, --depth=1), finds files matching pathPattern, and ingests each as a UserOntologyBundle in the internal n10s store. Runs synchronously — returns when the ingest is complete or has failed. The source\'s lastStatus and lastIngestedAt are updated on completion. Works regardless of the source\'s enabled flag.
     * [v2] Trigger an immediate ingest from a git source.
     */
    async triggerIngest(requestParameters: TriggerIngestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OntologyGitIngestResultIO> {
        const response = await this.triggerIngestRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Calls AasRegistryOutboxService.syncAll(): seeds PENDING rows for any unregistered collections, then pushes all PENDING/FAILED rows to the configured IDTA AAS Registry. Best-effort — failures flip the row to FAILED and are logged at WARN. Returns the count of shells successfully registered in this invocation. Gated on the instance-admin role.
     * [v2] Trigger an on-demand AAS registry sync.
     */
    async triggerSyncRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AasSyncResultIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/aas/registrations/sync`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AasSyncResultIOFromJSON(jsonValue));
    }

    /**
     * Calls AasRegistryOutboxService.syncAll(): seeds PENDING rows for any unregistered collections, then pushes all PENDING/FAILED rows to the configured IDTA AAS Registry. Best-effort — failures flip the row to FAILED and are logged at WARN. Returns the count of shells successfully registered in this invocation. Gated on the instance-admin role.
     * [v2] Trigger an on-demand AAS registry sync.
     */
    async triggerSync(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AasSyncResultIO> {
        const response = await this.triggerSyncRaw(initOverrides);
        return await response.value();
    }

    /**
     * Multipart: file=<ttl bytes> plus metadata={\"id\":...,\"name\":...,\"iriPrefix\":...,\"canonicalUrl\":...,\"license\":...}. Server SHA-256s the bytes, writes them to <shepard.semantic.internal.user-bundles-dir>/<id>.ttl, persists a :UserOntologyBundle catalogue row. The bundle joins the seed loop on next startup. Validation: id must match ^[a-z0-9][a-z0-9_-]{0,63}$ and not collide with a built-in or another user bundle; payload ≤ 10 MB; payload must look like Turtle.
     * [v2] Upload an operator-supplied ontology bundle.
     */
    async uploadOntologyRaw(requestParameters: UploadOntologyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OntologyBundleIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        // use FormData to transmit files using content-type "multipart/form-data"
        useForm = canConsumeForm;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['file'] != null) {
            formParams.append('file', requestParameters['file'] as any);
        }

        if (requestParameters['metadata'] != null) {
            formParams.append('metadata', requestParameters['metadata'] as any);
        }

        const response = await this.request({
            path: `/v2/admin/semantic/ontologies`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OntologyBundleIOFromJSON(jsonValue));
    }

    /**
     * Multipart: file=<ttl bytes> plus metadata={\"id\":...,\"name\":...,\"iriPrefix\":...,\"canonicalUrl\":...,\"license\":...}. Server SHA-256s the bytes, writes them to <shepard.semantic.internal.user-bundles-dir>/<id>.ttl, persists a :UserOntologyBundle catalogue row. The bundle joins the seed loop on next startup. Validation: id must match ^[a-z0-9][a-z0-9_-]{0,63}$ and not collide with a built-in or another user bundle; payload ≤ 10 MB; payload must look like Turtle.
     * [v2] Upload an operator-supplied ontology bundle.
     */
    async uploadOntology(requestParameters: UploadOntologyRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OntologyBundleIO> {
        const response = await this.uploadOntologyRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
