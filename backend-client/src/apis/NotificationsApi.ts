/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  NotificationCountIO,
  NotificationIO,
} from '../models/index';
import {
    NotificationCountIOFromJSON,
    NotificationCountIOToJSON,
    NotificationIOFromJSON,
    NotificationIOToJSON,
} from '../models/index';

export interface DismissRequest {
    appId: string;
}

export interface MarkReadRequest {
    appId: string;
}

/**
 * 
 */
export class NotificationsApi extends runtime.BaseAPI {

    /**
     * Returns a single object with the unread notification count. Poll this endpoint (e.g. every 30 seconds) to drive the bell-icon badge without fetching full payloads.
     * [v2] Count unread in-app notifications for the authenticated user.
     */
    async countRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NotificationCountIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/notifications/count`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NotificationCountIOFromJSON(jsonValue));
    }

    /**
     * Returns a single object with the unread notification count. Poll this endpoint (e.g. every 30 seconds) to drive the bell-icon badge without fetching full payloads.
     * [v2] Count unread in-app notifications for the authenticated user.
     */
    async count(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NotificationCountIO> {
        const response = await this.countRaw(initOverrides);
        return await response.value();
    }

    /**
     * Permanently removes the notification from the caller\'s notification list. The notification must be visible to the authenticated user.
     * [v2] Dismiss (delete) a notification.
     */
    async dismissRaw(requestParameters: DismissRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling dismiss().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/notifications/{appId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Permanently removes the notification from the caller\'s notification list. The notification must be visible to the authenticated user.
     * [v2] Dismiss (delete) a notification.
     */
    async dismiss(requestParameters: DismissRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.dismissRaw(requestParameters, initOverrides);
    }

    /**
     * Returns all non-expired notifications visible to the caller, ordered most-recent-first. Includes notifications addressed to the caller\'s username, ALL-audience broadcasts, and (when the caller is an instance-admin) INSTANCE_ADMIN-audience broadcasts. Capped at 200 rows.
     * [v2] List in-app notifications for the authenticated user.
     */
    async listNotificationsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<NotificationIO>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/notifications`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(NotificationIOFromJSON));
    }

    /**
     * Returns all non-expired notifications visible to the caller, ordered most-recent-first. Includes notifications addressed to the caller\'s username, ALL-audience broadcasts, and (when the caller is an instance-admin) INSTANCE_ADMIN-audience broadcasts. Capped at 200 rows.
     * [v2] List in-app notifications for the authenticated user.
     */
    async listNotifications(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<NotificationIO>> {
        const response = await this.listNotificationsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Marks the notification identified by its appId as read. The notification must be visible to the authenticated user.
     * [v2] Mark a notification as read.
     */
    async markReadRaw(requestParameters: MarkReadRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NotificationIO>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling markRead().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/notifications/{appId}/read`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NotificationIOFromJSON(jsonValue));
    }

    /**
     * Marks the notification identified by its appId as read. The notification must be visible to the authenticated user.
     * [v2] Mark a notification as read.
     */
    async markRead(requestParameters: MarkReadRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NotificationIO> {
        const response = await this.markReadRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
