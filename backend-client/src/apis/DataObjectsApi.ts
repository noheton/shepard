/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CreateDataObjectV2,
  DataObject,
  DataObjectBatchCreateItem,
  DataObjectBatchResponse,
  DataObjectDetail,
  DataObjectListItemV2,
  DataObjectSummary,
  PredecessorEdgePatch,
} from '../models/index';
import {
    CreateDataObjectV2FromJSON,
    CreateDataObjectV2ToJSON,
    DataObjectFromJSON,
    DataObjectToJSON,
    DataObjectBatchCreateItemFromJSON,
    DataObjectBatchCreateItemToJSON,
    DataObjectBatchResponseFromJSON,
    DataObjectBatchResponseToJSON,
    DataObjectDetailFromJSON,
    DataObjectDetailToJSON,
    DataObjectListItemV2FromJSON,
    DataObjectListItemV2ToJSON,
    DataObjectSummaryFromJSON,
    DataObjectSummaryToJSON,
    PredecessorEdgePatchFromJSON,
    PredecessorEdgePatchToJSON,
} from '../models/index';

export interface BatchRequest {
    dataObjectBatchCreateItem: Array<DataObjectBatchCreateItem>;
}

export interface ChildrenRequest {
    collectionAppId: string;
    dataObjectAppId: string;
}

export interface CreateDataObjectV2Request {
    collectionAppId: string;
    createDataObjectV2: Omit<CreateDataObjectV2, 'id'|'createdAt'|'createdBy'|'updatedAt'|'updatedBy'|'appId'|'revision'|'createdByOrcid'|'collectionId'|'referenceIds'|'successorIds'|'childrenIds'|'incomingIds'|'timeseriesReferenceCount'|'fileBundleCount'|'structuredDataReferenceCount'|'attachedTemplateAppId'|'videoStreamReferenceCount'>;
    xAIAgent?: string;
}

export interface DeleteDataObjectV2Request {
    collectionAppId: string;
    dataObjectAppId: string;
}

export interface GetDataObjectV2Request {
    collectionAppId: string;
    dataObjectAppId: string;
}

export interface GetRdfRequest {
    appId: string;
}

export interface ListDataObjectsRequest {
    collectionAppId: string;
    fields?: string;
    include?: string;
    name?: string;
    page?: number;
    pageSize?: number;
    status?: string;
}

export interface PatchDataObjectV2Request {
    collectionAppId: string;
    dataObjectAppId: string;
    dataObject: Omit<DataObject, 'id'|'createdAt'|'createdBy'|'updatedAt'|'updatedBy'|'appId'|'revision'|'createdByOrcid'|'collectionId'|'referenceIds'|'successorIds'|'childrenIds'|'incomingIds'|'timeseriesReferenceCount'|'fileBundleCount'|'structuredDataReferenceCount'|'attachedTemplateAppId'|'videoStreamReferenceCount'>;
}

export interface PatchPredecessorEdgeRequest {
    collectionAppId: string;
    dataObjectAppId: string;
    predecessorAppId: string;
    predecessorEdgePatch: PredecessorEdgePatch;
}

export interface PredecessorChainRequest {
    collectionAppId: string;
    dataObjectAppId: string;
    depth?: number;
}

export interface PredecessorsRequest {
    collectionAppId: string;
    dataObjectAppId: string;
}

export interface SuccessorChainRequest {
    collectionAppId: string;
    dataObjectAppId: string;
    depth?: number;
}

export interface SuccessorsRequest {
    collectionAppId: string;
    dataObjectAppId: string;
}

/**
 * 
 */
export class DataObjectsApi extends runtime.BaseAPI {

    /**
     * Accepts a JSON array of DataObject creation requests (1–500 items) and returns HTTP 207 Multi-Status with a per-item outcome. Items are processed sequentially; a failure on item N does not prevent items N+1..M from being attempted.  **Required fields per item:** `collectionAppId` (target Collection, UUID v7), `name` (non-blank string).  **Optional fields per item:** `description`, `parentAppId` (UUID v7 of an existing DataObject to use as the hierarchical parent), `attributes` (string→string map), `status` (free-form string; suggested: DRAFT / IN_REVIEW / READY / PUBLISHED / ARCHIVED / NCR_OPEN / ON_HOLD / REJECTED / CERTIFIED;     NCR_OPEN / ON_HOLD / REJECTED / CERTIFIED require the \'quality-engineer\' role — MFG1).  **HTTP 400** is returned before any processing if the array is empty or exceeds 500 items.  **HTTP 207** is always returned for valid-sized batches, even when all items fail (the `failed` counter reflects item-level failures; a genuine server error returns 500).  **Per-item error codes:** `COLLECTION_NOT_FOUND`, `FORBIDDEN`, `PARENT_NOT_FOUND`, `INVALID_INPUT`, `INTERNAL_ERROR`.  **Auth:** Caller must be authenticated. Each item\'s `collectionAppId` requires Write permission. The permission result per Collection is memoised within the batch — repeated items targeting the same Collection do not re-query the permissions service.  **Provenance:** One `CREATE` `[:Activity]` is recorded for the whole batch request (HTTP 207 is in the 2xx range, so `ProvenanceCaptureFilter` fires once). Per-item provenance granularity is a PROV2 deferred item.  Example body: `[{\"collectionAppId\":\"018f…\",\"name\":\"TR-001\"}, {\"collectionAppId\":\"018f…\",\"name\":\"TR-002\",\"attributes\":{\"campaign\":\"Q3\"}}]`
     * [v2] Bulk-create DataObjects across one or more Collections (HTTP 207).
     */
    async batchRaw(requestParameters: BatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DataObjectBatchResponse>> {
        if (requestParameters['dataObjectBatchCreateItem'] == null) {
            throw new runtime.RequiredError(
                'dataObjectBatchCreateItem',
                'Required parameter "dataObjectBatchCreateItem" was null or undefined when calling batch().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/data-objects/batch`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['dataObjectBatchCreateItem']!.map(DataObjectBatchCreateItemToJSON),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DataObjectBatchResponseFromJSON(jsonValue));
    }

    /**
     * Accepts a JSON array of DataObject creation requests (1–500 items) and returns HTTP 207 Multi-Status with a per-item outcome. Items are processed sequentially; a failure on item N does not prevent items N+1..M from being attempted.  **Required fields per item:** `collectionAppId` (target Collection, UUID v7), `name` (non-blank string).  **Optional fields per item:** `description`, `parentAppId` (UUID v7 of an existing DataObject to use as the hierarchical parent), `attributes` (string→string map), `status` (free-form string; suggested: DRAFT / IN_REVIEW / READY / PUBLISHED / ARCHIVED / NCR_OPEN / ON_HOLD / REJECTED / CERTIFIED;     NCR_OPEN / ON_HOLD / REJECTED / CERTIFIED require the \'quality-engineer\' role — MFG1).  **HTTP 400** is returned before any processing if the array is empty or exceeds 500 items.  **HTTP 207** is always returned for valid-sized batches, even when all items fail (the `failed` counter reflects item-level failures; a genuine server error returns 500).  **Per-item error codes:** `COLLECTION_NOT_FOUND`, `FORBIDDEN`, `PARENT_NOT_FOUND`, `INVALID_INPUT`, `INTERNAL_ERROR`.  **Auth:** Caller must be authenticated. Each item\'s `collectionAppId` requires Write permission. The permission result per Collection is memoised within the batch — repeated items targeting the same Collection do not re-query the permissions service.  **Provenance:** One `CREATE` `[:Activity]` is recorded for the whole batch request (HTTP 207 is in the 2xx range, so `ProvenanceCaptureFilter` fires once). Per-item provenance granularity is a PROV2 deferred item.  Example body: `[{\"collectionAppId\":\"018f…\",\"name\":\"TR-001\"}, {\"collectionAppId\":\"018f…\",\"name\":\"TR-002\",\"attributes\":{\"campaign\":\"Q3\"}}]`
     * [v2] Bulk-create DataObjects across one or more Collections (HTTP 207).
     */
    async batch(requestParameters: BatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DataObjectBatchResponse> {
        const response = await this.batchRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the compact summary (appId, id, name, status) of each non-deleted DataObject that is a direct child of the DataObject identified by `dataObjectAppId`.  Auth: Read permission on the parent Collection (inherited).
     * [v2] List direct children of a DataObject.
     */
    async childrenRaw(requestParameters: ChildrenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DataObjectSummary>>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling children().'
            );
        }

        if (requestParameters['dataObjectAppId'] == null) {
            throw new runtime.RequiredError(
                'dataObjectAppId',
                'Required parameter "dataObjectAppId" was null or undefined when calling children().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}/children`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"dataObjectAppId"}}`, encodeURIComponent(String(requestParameters['dataObjectAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DataObjectSummaryFromJSON));
    }

    /**
     * Returns the compact summary (appId, id, name, status) of each non-deleted DataObject that is a direct child of the DataObject identified by `dataObjectAppId`.  Auth: Read permission on the parent Collection (inherited).
     * [v2] List direct children of a DataObject.
     */
    async children(requestParameters: ChildrenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DataObjectSummary>> {
        const response = await this.childrenRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a `:DataObject` in the Collection identified by `collectionAppId` and returns the full entity (as `DataObjectDetail`) in the 201 body. The server mints `appId` (UUID v7) and `id` (legacy long).  Body fields:   - `name` (string, required, non-blank).   - `description` (string, optional, CommonMark + GFM).   - `attributes` (string-to-string map, optional).   - `status` (optional, DRAFT/IN_REVIEW/READY/PUBLISHED/ARCHIVED/NCR_OPEN/ON_HOLD/REJECTED/CERTIFIED;     NCR_OPEN/ON_HOLD/REJECTED/CERTIFIED require the \'quality-engineer\' role — MFG1).   - `provenanceMode` (optional, PROV1j / EU AI Act Art. 50): \'human\', \'ai\',     or \'collaborative\'. When omitted, auto-detected from the `X-AI-Agent`     request header (present and non-blank → \'ai\'; otherwise null).  Example body: `{\"name\": \"TR-001\", \"description\": \"hot-fire run\", \"attributes\": {\"campaign\": \"Q3\"}}`.  Auth: Write on the parent Collection.  Side effects: ProvenanceCaptureFilter records a `CREATE` Activity addressable at `GET /v2/provenance/entity/{appId}`. Versioning HEAD is advanced on the parent Collection.  Next step: `POST /shepard/api/collections/{cid}/data-objects/{doid}/timeseriesReferences` (or fileReferences / structuredDataReferences) to attach payload, or `GET /v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}` to confirm the entity.
     * [v2] Create a DataObject inside a Collection.
     */
    async createDataObjectV2Raw(requestParameters: CreateDataObjectV2Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DataObjectDetail>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling createDataObjectV2().'
            );
        }

        if (requestParameters['createDataObjectV2'] == null) {
            throw new runtime.RequiredError(
                'createDataObjectV2',
                'Required parameter "createDataObjectV2" was null or undefined when calling createDataObjectV2().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAIAgent'] != null) {
            headerParameters['X-AI-Agent'] = String(requestParameters['xAIAgent']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/data-objects`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateDataObjectV2ToJSON(requestParameters['createDataObjectV2']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DataObjectDetailFromJSON(jsonValue));
    }

    /**
     * Creates a `:DataObject` in the Collection identified by `collectionAppId` and returns the full entity (as `DataObjectDetail`) in the 201 body. The server mints `appId` (UUID v7) and `id` (legacy long).  Body fields:   - `name` (string, required, non-blank).   - `description` (string, optional, CommonMark + GFM).   - `attributes` (string-to-string map, optional).   - `status` (optional, DRAFT/IN_REVIEW/READY/PUBLISHED/ARCHIVED/NCR_OPEN/ON_HOLD/REJECTED/CERTIFIED;     NCR_OPEN/ON_HOLD/REJECTED/CERTIFIED require the \'quality-engineer\' role — MFG1).   - `provenanceMode` (optional, PROV1j / EU AI Act Art. 50): \'human\', \'ai\',     or \'collaborative\'. When omitted, auto-detected from the `X-AI-Agent`     request header (present and non-blank → \'ai\'; otherwise null).  Example body: `{\"name\": \"TR-001\", \"description\": \"hot-fire run\", \"attributes\": {\"campaign\": \"Q3\"}}`.  Auth: Write on the parent Collection.  Side effects: ProvenanceCaptureFilter records a `CREATE` Activity addressable at `GET /v2/provenance/entity/{appId}`. Versioning HEAD is advanced on the parent Collection.  Next step: `POST /shepard/api/collections/{cid}/data-objects/{doid}/timeseriesReferences` (or fileReferences / structuredDataReferences) to attach payload, or `GET /v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}` to confirm the entity.
     * [v2] Create a DataObject inside a Collection.
     */
    async createDataObjectV2(requestParameters: CreateDataObjectV2Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DataObjectDetail> {
        const response = await this.createDataObjectV2Raw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Soft-deletes the `:DataObject` identified by `dataObjectAppId` within the Collection identified by `collectionAppId`. Cascades to the DataObject\'s attached references (TimeseriesReferences, FileReferences, StructuredDataReferences, CollectionReferences, LabJournalEntries). The underlying containers (FileContainer, TimeseriesContainer, StructuredDataContainer) are not deleted — they are top-level entities independent of the DataObject hierarchy; use the container-kind DELETE endpoints to remove them.  Idempotency: returns 204 whether or not the DataObject existed before the call.  Auth: Write permission on the parent Collection.  Side effects: `ProvenanceCaptureFilter` records a `DELETE` Activity addressable at `GET /v2/provenance/entity/{appId}`. Subscriptions attached to the DataObject fire.
     * [v2] Delete a DataObject and its references.
     */
    async deleteDataObjectV2Raw(requestParameters: DeleteDataObjectV2Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling deleteDataObjectV2().'
            );
        }

        if (requestParameters['dataObjectAppId'] == null) {
            throw new runtime.RequiredError(
                'dataObjectAppId',
                'Required parameter "dataObjectAppId" was null or undefined when calling deleteDataObjectV2().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"dataObjectAppId"}}`, encodeURIComponent(String(requestParameters['dataObjectAppId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Soft-deletes the `:DataObject` identified by `dataObjectAppId` within the Collection identified by `collectionAppId`. Cascades to the DataObject\'s attached references (TimeseriesReferences, FileReferences, StructuredDataReferences, CollectionReferences, LabJournalEntries). The underlying containers (FileContainer, TimeseriesContainer, StructuredDataContainer) are not deleted — they are top-level entities independent of the DataObject hierarchy; use the container-kind DELETE endpoints to remove them.  Idempotency: returns 204 whether or not the DataObject existed before the call.  Auth: Write permission on the parent Collection.  Side effects: `ProvenanceCaptureFilter` records a `DELETE` Activity addressable at `GET /v2/provenance/entity/{appId}`. Subscriptions attached to the DataObject fire.
     * [v2] Delete a DataObject and its references.
     */
    async deleteDataObjectV2(requestParameters: DeleteDataObjectV2Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteDataObjectV2Raw(requestParameters, initOverrides);
    }

    /**
     * Returns the full `DataObjectIO` shape for the DataObject identified by `dataObjectAppId` (UUID v7) within the Collection identified by `collectionAppId`.  Content negotiation (M4I-c): Pass `Accept: application/ld+json; profile=\"https://w3id.org/nfdi4ing/metadata4ing/\"` (or short `profile=metadata4ing`) to receive a metadata4ing (NFDI4Ing m4i 1.4.0) JSON-LD projection of the DataObject — m4i:InvestigatedObject type, dcterms:identifier/title, schema:dateCreated, m4i:hasIdentifier (KIP1a PID), obo:RO_0002233/4 (predecessor/successor), prov:wasGeneratedBy + m4i:realizesMethod (most-recent Activity), m4i:hasEmployedTool, and m4i:hasNumericalVariable (numeric SemanticAnnotations with QUDT unit refs) + schema:keywords (free-text annotations). Plain JSON callers see no change. Unknown profile → 406 RFC 7807 `dataobject.unsupported-profile`. Shape contract: `backend/src/main/resources/shapes/m4i-dataobject-shape.ttl`. Design: aidocs/semantics/94 §4.3.  The response includes `id` (legacy long), `appId` (UUID v7, canonical), `name`, `description`, `status`, `attributes` (string-to-string map), `referenceIds[]` (legacy long ids of all references — timeseries, file, structured-data — attached to this DataObject), `childrenIds[]` (direct child DataObjects), and timestamps.  Auth: Read permission on the parent Collection (DataObjects inherit Collection permissions; there is no per-DataObject permission node). Returns 404 when either `collectionAppId` or `dataObjectAppId` is unknown, or when the DataObject does not belong to the stated Collection.  Next step: use the `referenceIds` values with the upstream `GET /shepard/api/...` reference endpoints to fetch payload, or `PATCH /v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}` to update.
     * [v2] Get a DataObject by appId.
     */
    async getDataObjectV2Raw(requestParameters: GetDataObjectV2Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DataObjectDetail>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling getDataObjectV2().'
            );
        }

        if (requestParameters['dataObjectAppId'] == null) {
            throw new runtime.RequiredError(
                'dataObjectAppId',
                'Required parameter "dataObjectAppId" was null or undefined when calling getDataObjectV2().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"dataObjectAppId"}}`, encodeURIComponent(String(requestParameters['dataObjectAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DataObjectDetailFromJSON(jsonValue));
    }

    /**
     * Returns the full `DataObjectIO` shape for the DataObject identified by `dataObjectAppId` (UUID v7) within the Collection identified by `collectionAppId`.  Content negotiation (M4I-c): Pass `Accept: application/ld+json; profile=\"https://w3id.org/nfdi4ing/metadata4ing/\"` (or short `profile=metadata4ing`) to receive a metadata4ing (NFDI4Ing m4i 1.4.0) JSON-LD projection of the DataObject — m4i:InvestigatedObject type, dcterms:identifier/title, schema:dateCreated, m4i:hasIdentifier (KIP1a PID), obo:RO_0002233/4 (predecessor/successor), prov:wasGeneratedBy + m4i:realizesMethod (most-recent Activity), m4i:hasEmployedTool, and m4i:hasNumericalVariable (numeric SemanticAnnotations with QUDT unit refs) + schema:keywords (free-text annotations). Plain JSON callers see no change. Unknown profile → 406 RFC 7807 `dataobject.unsupported-profile`. Shape contract: `backend/src/main/resources/shapes/m4i-dataobject-shape.ttl`. Design: aidocs/semantics/94 §4.3.  The response includes `id` (legacy long), `appId` (UUID v7, canonical), `name`, `description`, `status`, `attributes` (string-to-string map), `referenceIds[]` (legacy long ids of all references — timeseries, file, structured-data — attached to this DataObject), `childrenIds[]` (direct child DataObjects), and timestamps.  Auth: Read permission on the parent Collection (DataObjects inherit Collection permissions; there is no per-DataObject permission node). Returns 404 when either `collectionAppId` or `dataObjectAppId` is unknown, or when the DataObject does not belong to the stated Collection.  Next step: use the `referenceIds` values with the upstream `GET /shepard/api/...` reference endpoints to fetch payload, or `PATCH /v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}` to update.
     * [v2] Get a DataObject by appId.
     */
    async getDataObjectV2(requestParameters: GetDataObjectV2Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DataObjectDetail> {
        const response = await this.getDataObjectV2Raw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Serialises the DataObject + first-level neighbors + direct semantic annotations + attached-template edge as Turtle. Designed to seed the data-graph textarea of the SHACL validation playground (`/shapes/validate?focusAppId=<doAppId>`).  The body is tight on purpose — focus node + edges to first-level neighbors only; no nested neighbor bodies. Callers wanting the full m4i flavour should request `Accept: application/ld+json; profile=\"metadata4ing\"` on the collection-scoped GET.  Auth: Read on the parent Collection. Returns 404 when no such DataObject exists, 403 when the caller lacks Read.
     * [v2] Return a Turtle subgraph for the DataObject (SHACL focus node).
     */
    async getRdfRaw(requestParameters: GetRdfRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling getRdf().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/data-objects/{appId}/rdf`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Serialises the DataObject + first-level neighbors + direct semantic annotations + attached-template edge as Turtle. Designed to seed the data-graph textarea of the SHACL validation playground (`/shapes/validate?focusAppId=<doAppId>`).  The body is tight on purpose — focus node + edges to first-level neighbors only; no nested neighbor bodies. Callers wanting the full m4i flavour should request `Accept: application/ld+json; profile=\"metadata4ing\"` on the collection-scoped GET.  Auth: Read on the parent Collection. Returns 404 when no such DataObject exists, 403 when the caller lacks Read.
     * [v2] Return a Turtle subgraph for the DataObject (SHACL focus node).
     */
    async getRdf(requestParameters: GetRdfRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getRdfRaw(requestParameters, initOverrides);
    }

    /**
     * Returns a page of `:DataObject` entities belonging to the Collection identified by `collectionAppId`. Each row in the response carries the full `DataObjectIO` fields plus three per-kind reference counts: `timeseriesCount`, `fileCount`, `structuredDataCount`. Counts reflect non-deleted references only and are computed in a single Cypher round-trip (no N+1 queries).  Pagination: omit `page` / `size` to get the first 50; supply both to paginate. `size` capped at 200 server-side.  Filtering: `name` does a case-insensitive substring match. Each row also carries `referenceIds[]` (legacy long ids of all refs) and `childrenIds[]` (direct child DOs).  Optional enrichment via `?include=time-bounds`: adds `timeBoundsStart` and `timeBoundsEnd` (epoch nanoseconds) to each item, reflecting the earliest and latest data-point timestamps across all timeseries channels. Null on items with no timeseries data. Omitted from the response entirely when `?include=time-bounds` is not requested.  **Payload diet (DB-OPT5).** By default the list response drops fields the collection-detail UI never reads — `description`, `attributes`, and the three deprecated `int` count siblings (`timeseriesReferenceCount`, `fileBundleCount`, `structuredDataReferenceCount`). Use `?include=full` to opt back into the full wire shape (transitional safety valve until a breaking-version bump can drop the deprecated ints unconditionally). For finer-grained control, pass `?fields=appId,name,createdAt,...` (flat CSV of field names; GitHub REST convention). `id`, `appId`, and `name` are always included as resource identity. Unknown field names return 400 with the offending name in the body. Dotted-path nested selection (e.g. `attributes.bench`) is not supported in this iteration — see DB-OPT5-NESTED in the backlog.  Auth: Read on the parent Collection. DataObjects inherit Collection permissions; there is no per-DO permission gate.  Next step: `GET /v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}` to fetch a specific DataObject with full reference detail.
     * [v2] List DataObjects under a Collection.
     */
    async listDataObjectsRaw(requestParameters: ListDataObjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DataObjectListItemV2>>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling listDataObjects().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['fields'] != null) {
            queryParameters['fields'] = requestParameters['fields'];
        }

        if (requestParameters['include'] != null) {
            queryParameters['include'] = requestParameters['include'];
        }

        if (requestParameters['name'] != null) {
            queryParameters['name'] = requestParameters['name'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/data-objects`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DataObjectListItemV2FromJSON));
    }

    /**
     * Returns a page of `:DataObject` entities belonging to the Collection identified by `collectionAppId`. Each row in the response carries the full `DataObjectIO` fields plus three per-kind reference counts: `timeseriesCount`, `fileCount`, `structuredDataCount`. Counts reflect non-deleted references only and are computed in a single Cypher round-trip (no N+1 queries).  Pagination: omit `page` / `size` to get the first 50; supply both to paginate. `size` capped at 200 server-side.  Filtering: `name` does a case-insensitive substring match. Each row also carries `referenceIds[]` (legacy long ids of all refs) and `childrenIds[]` (direct child DOs).  Optional enrichment via `?include=time-bounds`: adds `timeBoundsStart` and `timeBoundsEnd` (epoch nanoseconds) to each item, reflecting the earliest and latest data-point timestamps across all timeseries channels. Null on items with no timeseries data. Omitted from the response entirely when `?include=time-bounds` is not requested.  **Payload diet (DB-OPT5).** By default the list response drops fields the collection-detail UI never reads — `description`, `attributes`, and the three deprecated `int` count siblings (`timeseriesReferenceCount`, `fileBundleCount`, `structuredDataReferenceCount`). Use `?include=full` to opt back into the full wire shape (transitional safety valve until a breaking-version bump can drop the deprecated ints unconditionally). For finer-grained control, pass `?fields=appId,name,createdAt,...` (flat CSV of field names; GitHub REST convention). `id`, `appId`, and `name` are always included as resource identity. Unknown field names return 400 with the offending name in the body. Dotted-path nested selection (e.g. `attributes.bench`) is not supported in this iteration — see DB-OPT5-NESTED in the backlog.  Auth: Read on the parent Collection. DataObjects inherit Collection permissions; there is no per-DO permission gate.  Next step: `GET /v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}` to fetch a specific DataObject with full reference detail.
     * [v2] List DataObjects under a Collection.
     */
    async listDataObjects(requestParameters: ListDataObjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DataObjectListItemV2>> {
        const response = await this.listDataObjectsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Applies an RFC 7396 JSON Merge Patch to the `:DataObject` identified by `dataObjectAppId` within `collectionAppId`:   - Fields PRESENT in the body REPLACE the corresponding fields.   - Fields ABSENT from the body are LEFT UNCHANGED.   - Fields set to explicit JSON `null` are CLEARED (except `name`, which is     `@NotBlank` — clearing it returns 400).  The merged result is Bean-Validated; violations on the final state return 400.  Example: rename without touching other fields — `{\"name\": \"TR-001-renamed\"}`. Example: advance status — `{\"status\": \"IN_REVIEW\"}`. Example: add an attribute — `{\"attributes\": {\"campaign\": \"Q3\"}}`.  Content-Type: prefer `application/merge-patch+json` (RFC 7396); `application/json` is also accepted.  Auth: Write permission on the parent Collection. DataObjects do not have their own permission node — the check walks up to the Collection.  Side effects: `ProvenanceCaptureFilter` records an `UPDATE` Activity addressable at `GET /v2/provenance/entity/{appId}`.
     * [v2] Partially update a DataObject (RFC 7396 JSON Merge Patch).
     */
    async patchDataObjectV2Raw(requestParameters: PatchDataObjectV2Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DataObject>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling patchDataObjectV2().'
            );
        }

        if (requestParameters['dataObjectAppId'] == null) {
            throw new runtime.RequiredError(
                'dataObjectAppId',
                'Required parameter "dataObjectAppId" was null or undefined when calling patchDataObjectV2().'
            );
        }

        if (requestParameters['dataObject'] == null) {
            throw new runtime.RequiredError(
                'dataObject',
                'Required parameter "dataObject" was null or undefined when calling patchDataObjectV2().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/merge-patch+json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"dataObjectAppId"}}`, encodeURIComponent(String(requestParameters['dataObjectAppId']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: DataObjectToJSON(requestParameters['dataObject']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DataObjectFromJSON(jsonValue));
    }

    /**
     * Applies an RFC 7396 JSON Merge Patch to the `:DataObject` identified by `dataObjectAppId` within `collectionAppId`:   - Fields PRESENT in the body REPLACE the corresponding fields.   - Fields ABSENT from the body are LEFT UNCHANGED.   - Fields set to explicit JSON `null` are CLEARED (except `name`, which is     `@NotBlank` — clearing it returns 400).  The merged result is Bean-Validated; violations on the final state return 400.  Example: rename without touching other fields — `{\"name\": \"TR-001-renamed\"}`. Example: advance status — `{\"status\": \"IN_REVIEW\"}`. Example: add an attribute — `{\"attributes\": {\"campaign\": \"Q3\"}}`.  Content-Type: prefer `application/merge-patch+json` (RFC 7396); `application/json` is also accepted.  Auth: Write permission on the parent Collection. DataObjects do not have their own permission node — the check walks up to the Collection.  Side effects: `ProvenanceCaptureFilter` records an `UPDATE` Activity addressable at `GET /v2/provenance/entity/{appId}`.
     * [v2] Partially update a DataObject (RFC 7396 JSON Merge Patch).
     */
    async patchDataObjectV2(requestParameters: PatchDataObjectV2Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DataObject> {
        const response = await this.patchDataObjectV2Raw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets / replaces the relationship type for the predecessor edge from the DataObject identified by `dataObjectAppId` to the predecessor identified by `predecessorAppId`. The edge must already exist (use create / merge-patch to add new predecessor links).  Allowed `relationshipType` values:   - `prov:wasInformedBy` — default / generic informational dependency (the QM1b \'normal\' kind)   - `prov:wasRevisionOf` — direct revision / correction (the QM1b \'re-test\' kind)   - `fair2r:repairs` — rework / NCR-repair (the QM1b \'rework\' kind)   - `fair2r:concession` — concession / use-as-is disposition (QM1b)  Idempotent: re-running with the same `relationshipType` is a no-op.  Auth: Write permission on the parent Collection.  Side effects: `ProvenanceCaptureFilter` records an `UPDATE` Activity addressable at `GET /v2/provenance/entity/{appId}`.
     * [v2] QM1b — set the PROV-O / FAIR²R relationship type on an existing predecessor edge.
     */
    async patchPredecessorEdgeRaw(requestParameters: PatchPredecessorEdgeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DataObjectDetail>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling patchPredecessorEdge().'
            );
        }

        if (requestParameters['dataObjectAppId'] == null) {
            throw new runtime.RequiredError(
                'dataObjectAppId',
                'Required parameter "dataObjectAppId" was null or undefined when calling patchPredecessorEdge().'
            );
        }

        if (requestParameters['predecessorAppId'] == null) {
            throw new runtime.RequiredError(
                'predecessorAppId',
                'Required parameter "predecessorAppId" was null or undefined when calling patchPredecessorEdge().'
            );
        }

        if (requestParameters['predecessorEdgePatch'] == null) {
            throw new runtime.RequiredError(
                'predecessorEdgePatch',
                'Required parameter "predecessorEdgePatch" was null or undefined when calling patchPredecessorEdge().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}/predecessors/{predecessorAppId}`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"dataObjectAppId"}}`, encodeURIComponent(String(requestParameters['dataObjectAppId']))).replace(`{${"predecessorAppId"}}`, encodeURIComponent(String(requestParameters['predecessorAppId']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PredecessorEdgePatchToJSON(requestParameters['predecessorEdgePatch']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DataObjectDetailFromJSON(jsonValue));
    }

    /**
     * Sets / replaces the relationship type for the predecessor edge from the DataObject identified by `dataObjectAppId` to the predecessor identified by `predecessorAppId`. The edge must already exist (use create / merge-patch to add new predecessor links).  Allowed `relationshipType` values:   - `prov:wasInformedBy` — default / generic informational dependency (the QM1b \'normal\' kind)   - `prov:wasRevisionOf` — direct revision / correction (the QM1b \'re-test\' kind)   - `fair2r:repairs` — rework / NCR-repair (the QM1b \'rework\' kind)   - `fair2r:concession` — concession / use-as-is disposition (QM1b)  Idempotent: re-running with the same `relationshipType` is a no-op.  Auth: Write permission on the parent Collection.  Side effects: `ProvenanceCaptureFilter` records an `UPDATE` Activity addressable at `GET /v2/provenance/entity/{appId}`.
     * [v2] QM1b — set the PROV-O / FAIR²R relationship type on an existing predecessor edge.
     */
    async patchPredecessorEdge(requestParameters: PatchPredecessorEdgeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DataObjectDetail> {
        const response = await this.patchPredecessorEdgeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Traverses the `has_successor` edges backwards from the DataObject identified by `dataObjectAppId` up to `depth` hops (default 10, clamped at 50 server-side). Returns compact summaries of all reachable non-deleted predecessors, excluding the start node itself. Ordering is by shepardId (insert order approximation).  Auth: Read permission on the parent Collection (inherited).
     * [v2] Traverse the predecessor chain up to a given depth.
     */
    async predecessorChainRaw(requestParameters: PredecessorChainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DataObjectSummary>>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling predecessorChain().'
            );
        }

        if (requestParameters['dataObjectAppId'] == null) {
            throw new runtime.RequiredError(
                'dataObjectAppId',
                'Required parameter "dataObjectAppId" was null or undefined when calling predecessorChain().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['depth'] != null) {
            queryParameters['depth'] = requestParameters['depth'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}/predecessor-chain`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"dataObjectAppId"}}`, encodeURIComponent(String(requestParameters['dataObjectAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DataObjectSummaryFromJSON));
    }

    /**
     * Traverses the `has_successor` edges backwards from the DataObject identified by `dataObjectAppId` up to `depth` hops (default 10, clamped at 50 server-side). Returns compact summaries of all reachable non-deleted predecessors, excluding the start node itself. Ordering is by shepardId (insert order approximation).  Auth: Read permission on the parent Collection (inherited).
     * [v2] Traverse the predecessor chain up to a given depth.
     */
    async predecessorChain(requestParameters: PredecessorChainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DataObjectSummary>> {
        const response = await this.predecessorChainRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the compact summary (appId, id, name, status) of each non-deleted DataObject that is a direct predecessor of the DataObject identified by `dataObjectAppId`.  Auth: Read permission on the parent Collection (inherited).
     * [v2] List direct predecessors of a DataObject.
     */
    async predecessorsRaw(requestParameters: PredecessorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DataObjectSummary>>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling predecessors().'
            );
        }

        if (requestParameters['dataObjectAppId'] == null) {
            throw new runtime.RequiredError(
                'dataObjectAppId',
                'Required parameter "dataObjectAppId" was null or undefined when calling predecessors().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}/predecessors`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"dataObjectAppId"}}`, encodeURIComponent(String(requestParameters['dataObjectAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DataObjectSummaryFromJSON));
    }

    /**
     * Returns the compact summary (appId, id, name, status) of each non-deleted DataObject that is a direct predecessor of the DataObject identified by `dataObjectAppId`.  Auth: Read permission on the parent Collection (inherited).
     * [v2] List direct predecessors of a DataObject.
     */
    async predecessors(requestParameters: PredecessorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DataObjectSummary>> {
        const response = await this.predecessorsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Traverses the `has_successor` edges forwards from the DataObject identified by `dataObjectAppId` up to `depth` hops (default 10, clamped at 50 server-side). Returns compact summaries of all reachable non-deleted successors, excluding the start node itself. Ordering is by shepardId (insert order approximation).  Auth: Read permission on the parent Collection (inherited).
     * [v2] Traverse the successor chain up to a given depth.
     */
    async successorChainRaw(requestParameters: SuccessorChainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DataObjectSummary>>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling successorChain().'
            );
        }

        if (requestParameters['dataObjectAppId'] == null) {
            throw new runtime.RequiredError(
                'dataObjectAppId',
                'Required parameter "dataObjectAppId" was null or undefined when calling successorChain().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['depth'] != null) {
            queryParameters['depth'] = requestParameters['depth'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}/successor-chain`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"dataObjectAppId"}}`, encodeURIComponent(String(requestParameters['dataObjectAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DataObjectSummaryFromJSON));
    }

    /**
     * Traverses the `has_successor` edges forwards from the DataObject identified by `dataObjectAppId` up to `depth` hops (default 10, clamped at 50 server-side). Returns compact summaries of all reachable non-deleted successors, excluding the start node itself. Ordering is by shepardId (insert order approximation).  Auth: Read permission on the parent Collection (inherited).
     * [v2] Traverse the successor chain up to a given depth.
     */
    async successorChain(requestParameters: SuccessorChainRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DataObjectSummary>> {
        const response = await this.successorChainRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the compact summary (appId, id, name, status) of each non-deleted DataObject that is a direct successor of the DataObject identified by `dataObjectAppId`.  Auth: Read permission on the parent Collection (inherited).
     * [v2] List direct successors of a DataObject.
     */
    async successorsRaw(requestParameters: SuccessorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DataObjectSummary>>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling successors().'
            );
        }

        if (requestParameters['dataObjectAppId'] == null) {
            throw new runtime.RequiredError(
                'dataObjectAppId',
                'Required parameter "dataObjectAppId" was null or undefined when calling successors().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/data-objects/{dataObjectAppId}/successors`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"dataObjectAppId"}}`, encodeURIComponent(String(requestParameters['dataObjectAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DataObjectSummaryFromJSON));
    }

    /**
     * Returns the compact summary (appId, id, name, status) of each non-deleted DataObject that is a direct successor of the DataObject identified by `dataObjectAppId`.  Auth: Read permission on the parent Collection (inherited).
     * [v2] List direct successors of a DataObject.
     */
    async successors(requestParameters: SuccessorsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DataObjectSummary>> {
        const response = await this.successorsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
