/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AnnotationV2,
  BulkAnnotationResult,
  CreateAnnotationV2,
  PagedResponse,
  UpdateAnnotationV2,
} from '../models/index';
import {
    AnnotationV2FromJSON,
    AnnotationV2ToJSON,
    BulkAnnotationResultFromJSON,
    CreateAnnotationV2FromJSON,
    CreateAnnotationV2ToJSON,
    PagedResponseFromJSON,
    PagedResponseToJSON,
    UpdateAnnotationV2FromJSON,
    UpdateAnnotationV2ToJSON,
} from '../models/index';

export interface CreateAnnotationRequest {
    createAnnotationV2: CreateAnnotationV2;
    xAIAgent?: string;
}

export interface DeleteAnnotationRequest {
    appId: string;
}

export interface ExportTurtleRequest {
    appId: string;
}

export interface FindRequest {
    q: string;
    page?: number;
    pageSize?: number;
    vocabId?: string;
}

export interface GetAnnotationRequest {
    appId: string;
}

export interface ListAnnotationsRequest {
    page?: number;
    pageSize?: number;
    predicateIri?: string;
    subjectAppId?: string;
    subjectKind?: string;
    vocabId?: string;
}

export interface UpdateAnnotationRequest {
    appId: string;
    updateAnnotationV2: UpdateAnnotationV2;
}

export interface BulkCreateAnnotationsRequest {
    createAnnotationV2: Array<CreateAnnotationV2>;
    xAIAgent?: string;
}

/**
 *
 */
export class SemanticAnnotationsApi extends runtime.BaseAPI {

    /**
     * Bulk-create up to 100 semantic annotations in one round-trip. Best-effort per row.
     * [v2] Bulk-create semantic annotations.
     */
    async bulkCreateAnnotationsRaw(requestParameters: BulkCreateAnnotationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkAnnotationResult>> {
        if (requestParameters['createAnnotationV2'] == null) {
            throw new runtime.RequiredError(
                'createAnnotationV2',
                'Required parameter "createAnnotationV2" was null or undefined when calling bulkCreateAnnotations().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAIAgent'] != null) {
            headerParameters['X-AI-Agent'] = String(requestParameters['xAIAgent']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/annotations/bulk`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['createAnnotationV2'].map(CreateAnnotationV2ToJSON),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkAnnotationResultFromJSON(jsonValue));
    }

    /**
     * Bulk-create up to 100 semantic annotations in one round-trip. Best-effort per row.
     * [v2] Bulk-create semantic annotations.
     */
    async bulkCreateAnnotations(requestParameters: BulkCreateAnnotationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkAnnotationResult> {
        const response = await this.bulkCreateAnnotationsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a `:SemanticAnnotation` node for the given subject entity. The server mints `appId` (UUID v7) for the new annotation.  Required fields: `subjectAppId`, `subjectKind`, `predicateIri`. Exactly one of `objectLiteral` / `objectIri` must be non-null.  Auth: caller must be able to Write the subject entity (inherited from its Collection). `sourceMode` defaults to `\'human\'` if not provided. `confidence` defaults to `1.0` for human writes.  Returns `201 Created` with the full AnnotationV2 body.
     * [v2] Create a new semantic annotation.
     */
    async createAnnotationRaw(requestParameters: CreateAnnotationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AnnotationV2>> {
        if (requestParameters['createAnnotationV2'] == null) {
            throw new runtime.RequiredError(
                'createAnnotationV2',
                'Required parameter "createAnnotationV2" was null or undefined when calling createAnnotation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAIAgent'] != null) {
            headerParameters['X-AI-Agent'] = String(requestParameters['xAIAgent']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/annotations`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateAnnotationV2ToJSON(requestParameters['createAnnotationV2']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AnnotationV2FromJSON(jsonValue));
    }

    /**
     * Creates a `:SemanticAnnotation` node for the given subject entity. The server mints `appId` (UUID v7) for the new annotation.  Required fields: `subjectAppId`, `subjectKind`, `predicateIri`. Exactly one of `objectLiteral` / `objectIri` must be non-null.  Auth: caller must be able to Write the subject entity (inherited from its Collection). `sourceMode` defaults to `\'human\'` if not provided. `confidence` defaults to `1.0` for human writes.  Returns `201 Created` with the full AnnotationV2 body.
     * [v2] Create a new semantic annotation.
     */
    async createAnnotation(requestParameters: CreateAnnotationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AnnotationV2> {
        const response = await this.createAnnotationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes the `:SemanticAnnotation` node identified by `appId`. Auth is governed by the operator-configured `annotationDeletePolicy` in `:SemanticConfig`:  - `\'author-or-manager\'` (default) — the annotation author OR any collection manager may delete. - `\'author-only\'` — only the annotation author may delete. - `\'manager-only\'` — only collection managers may delete.  Returns `204 No Content` on success.  Note: this is a hard delete. For soft-delete (set `validUntilMillis`), use `PUT /v2/annotations/{appId}` with `{\"validUntilMillis\": <now>}`.
     * [v2] Delete an annotation.
     */
    async deleteAnnotationRaw(requestParameters: DeleteAnnotationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling deleteAnnotation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/annotations/{appId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes the `:SemanticAnnotation` node identified by `appId`. Auth is governed by the operator-configured `annotationDeletePolicy` in `:SemanticConfig`:  - `\'author-or-manager\'` (default) — the annotation author OR any collection manager may delete. - `\'author-only\'` — only the annotation author may delete. - `\'manager-only\'` — only collection managers may delete.  Returns `204 No Content` on success.  Note: this is a hard delete. For soft-delete (set `validUntilMillis`), use `PUT /v2/annotations/{appId}` with `{\"validUntilMillis\": <now>}`.
     * [v2] Delete an annotation.
     */
    async deleteAnnotation(requestParameters: DeleteAnnotationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteAnnotationRaw(requestParameters, initOverrides);
    }

    /**
     * Produces a minimal Turtle document containing both the \'flat triple\' form (`<subjectEntityIri> <predicateIri> <objectValue> .`) and the W3C Open Annotations (OA) frame (`oa:Annotation` with `oa:hasTarget` / `oa:hasBody` and `prov:wasGeneratedBy`). Content-Type is `text/turtle`. Per §3.3 of aidocs/semantics/100.  Auth: caller must be able to Read the subject entity.
     * [v2] Export one annotation as OA-framed Turtle.
     */
    async exportTurtleRaw(requestParameters: ExportTurtleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling exportTurtle().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/annotations/{appId}/export/turtle`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Produces a minimal Turtle document containing both the \'flat triple\' form (`<subjectEntityIri> <predicateIri> <objectValue> .`) and the W3C Open Annotations (OA) frame (`oa:Annotation` with `oa:hasTarget` / `oa:hasBody` and `prov:wasGeneratedBy`). Content-Type is `text/turtle`. Per §3.3 of aidocs/semantics/100.  Auth: caller must be able to Read the subject entity.
     * [v2] Export one annotation as OA-framed Turtle.
     */
    async exportTurtle(requestParameters: ExportTurtleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.exportTurtleRaw(requestParameters, initOverrides);
    }

    /**
     * Case-insensitive substring search over annotation value names and predicate names. Supply `q` with at least 1 character. Optional `vocabId` narrows to one vocabulary. Pagination via `page` / `pageSize`.  Auth: result is the union of annotations the caller can Read; supply `subjectAppId` or `subjectKind` in the parent `list` endpoint for entity-scoped discovery.
     * [v2] Text search over annotation values and predicate names.
     */
    async findRaw(requestParameters: FindRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponse>> {
        if (requestParameters['q'] == null) {
            throw new runtime.RequiredError(
                'q',
                'Required parameter "q" was null or undefined when calling find().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['vocabId'] != null) {
            queryParameters['vocabId'] = requestParameters['vocabId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/annotations/find`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseFromJSON(jsonValue));
    }

    /**
     * Case-insensitive substring search over annotation value names and predicate names. Supply `q` with at least 1 character. Optional `vocabId` narrows to one vocabulary. Pagination via `page` / `pageSize`.  Auth: result is the union of annotations the caller can Read; supply `subjectAppId` or `subjectKind` in the parent `list` endpoint for entity-scoped discovery.
     * [v2] Text search over annotation values and predicate names.
     */
    async find(requestParameters: FindRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponse> {
        const response = await this.findRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the full AnnotationV2 for the annotation identified by `appId` (UUID v7). 404 when the annotation does not exist. Auth: caller must be able to Read the subject entity (inherited from its Collection).  Next step: `PUT /v2/annotations/{appId}` to update, `DELETE /v2/annotations/{appId}` to delete, or `GET /v2/annotations/{appId}/export/turtle` for the Turtle export.
     * [v2] Get one annotation by its appId.
     */
    async getAnnotationRaw(requestParameters: GetAnnotationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AnnotationV2>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling getAnnotation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/annotations/{appId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AnnotationV2FromJSON(jsonValue));
    }

    /**
     * Returns the full AnnotationV2 for the annotation identified by `appId` (UUID v7). 404 when the annotation does not exist. Auth: caller must be able to Read the subject entity (inherited from its Collection).  Next step: `PUT /v2/annotations/{appId}` to update, `DELETE /v2/annotations/{appId}` to delete, or `GET /v2/annotations/{appId}/export/turtle` for the Turtle export.
     * [v2] Get one annotation by its appId.
     */
    async getAnnotation(requestParameters: GetAnnotationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AnnotationV2> {
        const response = await this.getAnnotationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a page of `:SemanticAnnotation` nodes matching the provided filter parameters. All filters are optional and AND-combined. Pagination is controlled by `page` (zero-based, default 0) and `pageSize` (default 50, max 200).  Filters: `subjectAppId` — only annotations on this entity; `subjectKind` — narrow to a specific entity kind; `predicateIri` — only annotations using this predicate; `vocabId` — only annotations from this vocabulary.  Auth: the caller\'s Read permission on the subject entity is enforced per annotation row — annotations whose subject the caller cannot Read are excluded from the result. Supply `subjectAppId` to filter to one entity (and trigger a single permission check at the list boundary).  Next step: `GET /v2/annotations/{appId}` for a single annotation, or `POST /v2/annotations` to create.
     * [v2] List annotations with optional filters.
     */
    async listAnnotationsRaw(requestParameters: ListAnnotationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponse>> {
        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        if (requestParameters['predicateIri'] != null) {
            queryParameters['predicateIri'] = requestParameters['predicateIri'];
        }

        if (requestParameters['subjectAppId'] != null) {
            queryParameters['subjectAppId'] = requestParameters['subjectAppId'];
        }

        if (requestParameters['subjectKind'] != null) {
            queryParameters['subjectKind'] = requestParameters['subjectKind'];
        }

        if (requestParameters['vocabId'] != null) {
            queryParameters['vocabId'] = requestParameters['vocabId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/annotations`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseFromJSON(jsonValue));
    }

    /**
     * Returns a page of `:SemanticAnnotation` nodes matching the provided filter parameters. All filters are optional and AND-combined. Pagination is controlled by `page` (zero-based, default 0) and `pageSize` (default 50, max 200).  Filters: `subjectAppId` — only annotations on this entity; `subjectKind` — narrow to a specific entity kind; `predicateIri` — only annotations using this predicate; `vocabId` — only annotations from this vocabulary.  Auth: the caller\'s Read permission on the subject entity is enforced per annotation row — annotations whose subject the caller cannot Read are excluded from the result. Supply `subjectAppId` to filter to one entity (and trigger a single permission check at the list boundary).  Next step: `GET /v2/annotations/{appId}` for a single annotation, or `POST /v2/annotations` to create.
     * [v2] List annotations with optional filters.
     */
    async listAnnotations(requestParameters: ListAnnotationsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponse> {
        const response = await this.listAnnotationsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * RFC 7396 merge-patch: only non-null fields in the request body are applied. The subject and predicate of an annotation are immutable. Patchable fields: `objectLiteral`, `objectIri`, `numericValue`, `unitIri`, `validFromMillis`, `validUntilMillis`, `confidence`, `sourceMode`.  Auth: caller must be able to Write the subject entity (inherited from its Collection). Returns `200 OK` with the updated AnnotationV2.
     * [v2] Update (merge-patch) an annotation.
     */
    async updateAnnotationRaw(requestParameters: UpdateAnnotationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AnnotationV2>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling updateAnnotation().'
            );
        }

        if (requestParameters['updateAnnotationV2'] == null) {
            throw new runtime.RequiredError(
                'updateAnnotationV2',
                'Required parameter "updateAnnotationV2" was null or undefined when calling updateAnnotation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/annotations/{appId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: UpdateAnnotationV2ToJSON(requestParameters['updateAnnotationV2']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AnnotationV2FromJSON(jsonValue));
    }

    /**
     * RFC 7396 merge-patch: only non-null fields in the request body are applied. The subject and predicate of an annotation are immutable. Patchable fields: `objectLiteral`, `objectIri`, `numericValue`, `unitIri`, `validFromMillis`, `validUntilMillis`, `confidence`, `sourceMode`.  Auth: caller must be able to Write the subject entity (inherited from its Collection). Returns `200 OK` with the updated AnnotationV2.
     * [v2] Update (merge-patch) an annotation.
     */
    async updateAnnotation(requestParameters: UpdateAnnotationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AnnotationV2> {
        const response = await this.updateAnnotationRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
