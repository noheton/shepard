/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AdminGitCredentialIO,
  AdminGitCredentialResultIO,
  AdminGitCredentialRotateIO,
  PagedResponseAdminGitCredentialListItem,
} from '../models/index';
import {
    AdminGitCredentialIOFromJSON,
    AdminGitCredentialIOToJSON,
    AdminGitCredentialResultIOFromJSON,
    AdminGitCredentialResultIOToJSON,
    AdminGitCredentialRotateIOFromJSON,
    AdminGitCredentialRotateIOToJSON,
    PagedResponseAdminGitCredentialListItemFromJSON,
    PagedResponseAdminGitCredentialListItemToJSON,
} from '../models/index';

export interface ListGitCredentialsRequest {
    username: string;
}

export interface PreseedGitCredentialRequest {
    username: string;
    adminGitCredentialIO: AdminGitCredentialIO;
}

export interface RotateRequest {
    appId: string;
    username: string;
    adminGitCredentialRotateIO: AdminGitCredentialRotateIO;
}

/**
 * 
 */
export class AdminGitCredentialPreseedApi extends runtime.BaseAPI {

    /**
     * Returns the discovery metadata for every credential the named user owns: appId, host, username, displayName, and lastRotatedAt. The PAT itself is **never** returned on the wire (write-only credential rule). Use `POST /v2/admin/users/{username}/git-credentials/{appId}/rotate` to refresh a credential\'s PAT.  Pre-ADM-USR-GIT-BACKEND-1 credentials (rows persisted before the `lastRotatedAt` field existed) return `null` for that field until they are next rotated.
     * [v2] List a user\'s git credentials (admin-only).
     */
    async listGitCredentialsRaw(requestParameters: ListGitCredentialsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponseAdminGitCredentialListItem>> {
        if (requestParameters['username'] == null) {
            throw new runtime.RequiredError(
                'username',
                'Required parameter "username" was null or undefined when calling listGitCredentials().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/users/{username}/git-credentials`.replace(`{${"username"}}`, encodeURIComponent(String(requestParameters['username']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseAdminGitCredentialListItemFromJSON(jsonValue));
    }

    /**
     * Returns the discovery metadata for every credential the named user owns: appId, host, username, displayName, and lastRotatedAt. The PAT itself is **never** returned on the wire (write-only credential rule). Use `POST /v2/admin/users/{username}/git-credentials/{appId}/rotate` to refresh a credential\'s PAT.  Pre-ADM-USR-GIT-BACKEND-1 credentials (rows persisted before the `lastRotatedAt` field existed) return `null` for that field until they are next rotated.
     * [v2] List a user\'s git credentials (admin-only).
     */
    async listGitCredentials(requestParameters: ListGitCredentialsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponseAdminGitCredentialListItem> {
        const response = await this.listGitCredentialsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates or replaces a git credential for the named user. If a credential for the same host already exists, it is overwritten. The PAT is encrypted with AES-256-GCM using the instance encryption key (`shepard.secrets.encryption-key`) and never returned on the wire. The target user must already exist (i.e. they have logged in at least once). Returns 503 when the encryption key is not configured.
     * [v2] Preseed a git credential for another user (admin-only).
     */
    async preseedGitCredentialRaw(requestParameters: PreseedGitCredentialRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AdminGitCredentialResultIO>> {
        if (requestParameters['username'] == null) {
            throw new runtime.RequiredError(
                'username',
                'Required parameter "username" was null or undefined when calling preseedGitCredential().'
            );
        }

        if (requestParameters['adminGitCredentialIO'] == null) {
            throw new runtime.RequiredError(
                'adminGitCredentialIO',
                'Required parameter "adminGitCredentialIO" was null or undefined when calling preseedGitCredential().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/users/{username}/git-credentials`.replace(`{${"username"}}`, encodeURIComponent(String(requestParameters['username']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AdminGitCredentialIOToJSON(requestParameters['adminGitCredentialIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AdminGitCredentialResultIOFromJSON(jsonValue));
    }

    /**
     * Creates or replaces a git credential for the named user. If a credential for the same host already exists, it is overwritten. The PAT is encrypted with AES-256-GCM using the instance encryption key (`shepard.secrets.encryption-key`) and never returned on the wire. The target user must already exist (i.e. they have logged in at least once). Returns 503 when the encryption key is not configured.
     * [v2] Preseed a git credential for another user (admin-only).
     */
    async preseedGitCredential(requestParameters: PreseedGitCredentialRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AdminGitCredentialResultIO> {
        const response = await this.preseedGitCredentialRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces the encrypted PAT on the named credential and stamps `lastRotatedAt = now`. Other fields (host, username, displayName) are untouched. The new PAT is encrypted with AES-256-GCM using the instance encryption key and never returned on the wire.  This is the explicit rotation path; the existing `POST /v2/admin/users/{username}/git-credentials` keeps its idempotent create-or-replace semantics for the same host.
     * [v2] Rotate a git credential\'s PAT (admin-only).
     */
    async rotateRaw(requestParameters: RotateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling rotate().'
            );
        }

        if (requestParameters['username'] == null) {
            throw new runtime.RequiredError(
                'username',
                'Required parameter "username" was null or undefined when calling rotate().'
            );
        }

        if (requestParameters['adminGitCredentialRotateIO'] == null) {
            throw new runtime.RequiredError(
                'adminGitCredentialRotateIO',
                'Required parameter "adminGitCredentialRotateIO" was null or undefined when calling rotate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/admin/users/{username}/git-credentials/{appId}/rotate`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))).replace(`{${"username"}}`, encodeURIComponent(String(requestParameters['username']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AdminGitCredentialRotateIOToJSON(requestParameters['adminGitCredentialRotateIO']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Replaces the encrypted PAT on the named credential and stamps `lastRotatedAt = now`. Other fields (host, username, displayName) are untouched. The new PAT is encrypted with AES-256-GCM using the instance encryption key and never returned on the wire.  This is the explicit rotation path; the existing `POST /v2/admin/users/{username}/git-credentials` keeps its idempotent create-or-replace semantics for the same host.
     * [v2] Rotate a git credential\'s PAT (admin-only).
     */
    async rotate(requestParameters: RotateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.rotateRaw(requestParameters, initOverrides);
    }

}
