/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ExportSelection,
  ExportUrlIO,
} from '../models/index';
import {
    ExportSelectionFromJSON,
    ExportSelectionToJSON,
    ExportUrlIOFromJSON,
    ExportUrlIOToJSON,
} from '../models/index';

export interface GetExportUrlRequest {
    appId: string;
    exportSelection: ExportSelection;
}

/**
 * 
 */
export class CollectionExportURLApi extends runtime.BaseAPI {

    /**
     * Builds the collection\'s RO-Crate ZIP export, uploads it to the active S3-compatible storage backend under exports/<uuid>.zip, and returns a presigned GET URL valid for 30 minutes. The client downloads the ZIP directly from S3 — no bytes are streamed through the shepard backend during the download. Returns 503 when the active storage provider does not support presigned export (GridFS); callers should fall back to GET /collections/{collectionId}/export. Requires Read permission on the collection.
     * [v2] Build and upload an RO-Crate ZIP to S3, returning a presigned download URL.
     */
    async getExportUrlRaw(requestParameters: GetExportUrlRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ExportUrlIO>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling getExportUrl().'
            );
        }

        if (requestParameters['exportSelection'] == null) {
            throw new runtime.RequiredError(
                'exportSelection',
                'Required parameter "exportSelection" was null or undefined when calling getExportUrl().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{appId}/export-url`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ExportSelectionToJSON(requestParameters['exportSelection']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ExportUrlIOFromJSON(jsonValue));
    }

    /**
     * Builds the collection\'s RO-Crate ZIP export, uploads it to the active S3-compatible storage backend under exports/<uuid>.zip, and returns a presigned GET URL valid for 30 minutes. The client downloads the ZIP directly from S3 — no bytes are streamed through the shepard backend during the download. Returns 503 when the active storage provider does not support presigned export (GridFS); callers should fall back to GET /collections/{collectionId}/export. Requires Read permission on the collection.
     * [v2] Build and upload an RO-Crate ZIP to S3, returning a presigned download URL.
     */
    async getExportUrl(requestParameters: GetExportUrlRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ExportUrlIO> {
        const response = await this.getExportUrlRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
