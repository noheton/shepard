/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CrossDoBulkDataRequest,
  CrossDoSeries,
} from '../models/index';
import {
    CrossDoBulkDataRequestFromJSON,
    CrossDoBulkDataRequestToJSON,
    CrossDoSeriesFromJSON,
    CrossDoSeriesToJSON,
} from '../models/index';

export interface GetCrossDoBulkDataRequest {
    crossDoBulkDataRequest: CrossDoBulkDataRequest;
}

/**
 * 
 */
export class TimeseriesCrossDataObjectViewApi extends runtime.BaseAPI {

    /**
     * Resolves each DataObject (by appId) to the channel whose `SemanticAnnotation.propertyIRI` equals the request body\'s `channelPredicate`, then returns LTTB-downsampled points in the supplied time window — one entry per resolved DataObject. Up to 100 DataObjects per request; default downsample target is 500 rows per series (max 5000). DataObjects with no matching channel return an empty `points` list. DataObjects the caller can\'t read are silently dropped (no 403 for the whole request). Where a single DataObject has multiple channels matching the predicate, the first by `symbolicName` ascending is picked (deterministic). Timestamps are absolute UTC nanoseconds; clients render within-DO relative time on the frontend by subtracting each series\' first timestamp.
     * [v2] Fetch one channel-predicate series across many DataObjects (TS-CROSS-DO-VIEW-1).
     */
    async getCrossDoBulkDataRaw(requestParameters: GetCrossDoBulkDataRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<CrossDoSeries>>> {
        if (requestParameters['crossDoBulkDataRequest'] == null) {
            throw new runtime.RequiredError(
                'crossDoBulkDataRequest',
                'Required parameter "crossDoBulkDataRequest" was null or undefined when calling getCrossDoBulkData().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/data-objects/cross-timeseries-bulk`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CrossDoBulkDataRequestToJSON(requestParameters['crossDoBulkDataRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(CrossDoSeriesFromJSON));
    }

    /**
     * Resolves each DataObject (by appId) to the channel whose `SemanticAnnotation.propertyIRI` equals the request body\'s `channelPredicate`, then returns LTTB-downsampled points in the supplied time window — one entry per resolved DataObject. Up to 100 DataObjects per request; default downsample target is 500 rows per series (max 5000). DataObjects with no matching channel return an empty `points` list. DataObjects the caller can\'t read are silently dropped (no 403 for the whole request). Where a single DataObject has multiple channels matching the predicate, the first by `symbolicName` ascending is picked (deterministic). Timestamps are absolute UTC nanoseconds; clients render within-DO relative time on the frontend by subtracting each series\' first timestamp.
     * [v2] Fetch one channel-predicate series across many DataObjects (TS-CROSS-DO-VIEW-1).
     */
    async getCrossDoBulkData(requestParameters: GetCrossDoBulkDataRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<CrossDoSeries>> {
        const response = await this.getCrossDoBulkDataRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
