/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PublicationState,
} from '../models/index';
import {
    PublicationStateFromJSON,
    PublicationStateToJSON,
} from '../models/index';

export interface GetCollectionPublicationStateRequest {
    collectionAppId: string;
}

export interface PatchCollectionPublicationStateRequest {
    collectionAppId: string;
    publicationState: Omit<PublicationState, 'archived'>;
}

/**
 * 
 */
export class CollectionLifecycleApi extends runtime.BaseAPI {

    /**
     * Returns the current `status` value of the Collection (DRAFT, IN_REVIEW, READY, PUBLISHED, ARCHIVED) plus a convenience `archived` boolean. Auth: Read permission on the Collection.
     * [v2] Read the current publication state of a Collection.
     */
    async getCollectionPublicationStateRaw(requestParameters: GetCollectionPublicationStateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PublicationState>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling getCollectionPublicationState().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/publication-state`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PublicationStateFromJSON(jsonValue));
    }

    /**
     * Returns the current `status` value of the Collection (DRAFT, IN_REVIEW, READY, PUBLISHED, ARCHIVED) plus a convenience `archived` boolean. Auth: Read permission on the Collection.
     * [v2] Read the current publication state of a Collection.
     */
    async getCollectionPublicationState(requestParameters: GetCollectionPublicationStateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PublicationState> {
        const response = await this.getCollectionPublicationStateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets the Collection\'s `status` to one of DRAFT, IN_REVIEW, READY, PUBLISHED, ARCHIVED. When the new state is ARCHIVED, every subsequent write to the Collection\'s children (DataObjects, References) returns 409 until the state is flipped back. Reads are unaffected.  Auth: Manage permission on the Collection OR the `instance-admin` role. A user with only Write permission (the standard contributor role) cannot archive — the generic Collection PATCH endpoint would accept the field but this dedicated endpoint exists for the stricter Owner gate.  Body: `{\"state\": \"ARCHIVED\"}`. Unrecognised state values return 400.
     * [v2] Flip the publication state of a Collection (Owner or instance-admin only).
     */
    async patchCollectionPublicationStateRaw(requestParameters: PatchCollectionPublicationStateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PublicationState>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling patchCollectionPublicationState().'
            );
        }

        if (requestParameters['publicationState'] == null) {
            throw new runtime.RequiredError(
                'publicationState',
                'Required parameter "publicationState" was null or undefined when calling patchCollectionPublicationState().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/publication-state`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PublicationStateToJSON(requestParameters['publicationState']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PublicationStateFromJSON(jsonValue));
    }

    /**
     * Sets the Collection\'s `status` to one of DRAFT, IN_REVIEW, READY, PUBLISHED, ARCHIVED. When the new state is ARCHIVED, every subsequent write to the Collection\'s children (DataObjects, References) returns 409 until the state is flipped back. Reads are unaffected.  Auth: Manage permission on the Collection OR the `instance-admin` role. A user with only Write permission (the standard contributor role) cannot archive — the generic Collection PATCH endpoint would accept the field but this dedicated endpoint exists for the stricter Owner gate.  Body: `{\"state\": \"ARCHIVED\"}`. Unrecognised state values return 400.
     * [v2] Flip the publication state of a Collection (Owner or instance-admin only).
     */
    async patchCollectionPublicationState(requestParameters: PatchCollectionPublicationStateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PublicationState> {
        const response = await this.patchCollectionPublicationStateRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
