/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  Snapshot,
  SnapshotDataObjects,
  SnapshotDiff,
  SnapshotEntry,
  SnapshotListPageIO,
} from '../models/index';
import {
    SnapshotFromJSON,
    SnapshotToJSON,
    SnapshotDataObjectsFromJSON,
    SnapshotDataObjectsToJSON,
    SnapshotDiffFromJSON,
    SnapshotDiffToJSON,
    SnapshotEntryFromJSON,
    SnapshotEntryToJSON,
    SnapshotListPageIOFromJSON,
    SnapshotListPageIOToJSON,
} from '../models/index';

export interface CreateCollectionSnapshotRequest {
    collectionAppId: string;
    snapshot: Omit<Snapshot, 'appId'|'snapshotCapturedAt'|'snapshotCreatedByUsername'|'collectionAppId'|'entryCount'>;
}

export interface DeleteSnapshotRequest {
    snapshotAppId: string;
}

export interface DiffRequest {
    aAppId: string;
    bAppId: string;
}

export interface GetDataObjectsRequest {
    collectionAppId: string;
    snapshotAppId: string;
}

export interface GetSnapshotRequest {
    snapshotAppId: string;
}

export interface ListCollectionSnapshotsRequest {
    collectionAppId: string;
    page?: number;
    size?: number;
}

export interface ListSnapshotsRequest {
    collectionAppId?: string;
    page?: number;
    size?: number;
}

export interface ManifestRequest {
    snapshotAppId: string;
}

/**
 * 
 */
export class SnapshotsApi extends runtime.BaseAPI {

    /**
     * Creates a point-in-time `:Snapshot` of the Collection identified by `collectionAppId`. The server walks the Collection subtree up to 15 hops deep and records the current `revision` counter of every `:VersionableEntity` (DataObject, Reference, …) it reaches. The resulting Snapshot is immutable — future writes to the underlying entities don\'t affect what the Snapshot reports.  Body fields:   - `name` (required, non-blank) — operator-chosen label, e.g. `\"v1.0\"`, `\"pre-publication\"`, `\"after-fix\"`.   - `description` (optional) — free-form, CommonMark/GFM.  Example body: `{\"name\": \"v1.0\", \"description\": \"Initial publication-ready state.\"}`.  Auth: Write on the Collection (snapshotting is a state-recording mutation on the snapshot subgraph, even though it doesn\'t modify the snapshotted entities themselves).  Side effects: a `:Snapshot` node plus one `:SnapshotEntry` per reachable VersionableEntity. ProvenanceCaptureFilter records a `CREATE` Activity addressable at `GET /v2/provenance/entity/{appId}`.  Next step: `GET /v2/collections/{collectionAppId}/snapshots` to list, `GET /v2/snapshots/{snapshotAppId}` for metadata, or `GET /v2/snapshots/{snapshotAppId}/manifest` for the per-entity entries.
     * [v2] Create a Snapshot of a Collection\'s current state (V2b).
     */
    async createCollectionSnapshotRaw(requestParameters: CreateCollectionSnapshotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Snapshot>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling createCollectionSnapshot().'
            );
        }

        if (requestParameters['snapshot'] == null) {
            throw new runtime.RequiredError(
                'snapshot',
                'Required parameter "snapshot" was null or undefined when calling createCollectionSnapshot().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/snapshots`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SnapshotToJSON(requestParameters['snapshot']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SnapshotFromJSON(jsonValue));
    }

    /**
     * Creates a point-in-time `:Snapshot` of the Collection identified by `collectionAppId`. The server walks the Collection subtree up to 15 hops deep and records the current `revision` counter of every `:VersionableEntity` (DataObject, Reference, …) it reaches. The resulting Snapshot is immutable — future writes to the underlying entities don\'t affect what the Snapshot reports.  Body fields:   - `name` (required, non-blank) — operator-chosen label, e.g. `\"v1.0\"`, `\"pre-publication\"`, `\"after-fix\"`.   - `description` (optional) — free-form, CommonMark/GFM.  Example body: `{\"name\": \"v1.0\", \"description\": \"Initial publication-ready state.\"}`.  Auth: Write on the Collection (snapshotting is a state-recording mutation on the snapshot subgraph, even though it doesn\'t modify the snapshotted entities themselves).  Side effects: a `:Snapshot` node plus one `:SnapshotEntry` per reachable VersionableEntity. ProvenanceCaptureFilter records a `CREATE` Activity addressable at `GET /v2/provenance/entity/{appId}`.  Next step: `GET /v2/collections/{collectionAppId}/snapshots` to list, `GET /v2/snapshots/{snapshotAppId}` for metadata, or `GET /v2/snapshots/{snapshotAppId}/manifest` for the per-entity entries.
     * [v2] Create a Snapshot of a Collection\'s current state (V2b).
     */
    async createCollectionSnapshot(requestParameters: CreateCollectionSnapshotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Snapshot> {
        const response = await this.createCollectionSnapshotRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Soft-deletes the snapshot and all its SnapshotEntry rows. The underlying entity data is unaffected. Requires Write permission on the root Collection.
     * [v2] Delete a snapshot.
     */
    async deleteSnapshotRaw(requestParameters: DeleteSnapshotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['snapshotAppId'] == null) {
            throw new runtime.RequiredError(
                'snapshotAppId',
                'Required parameter "snapshotAppId" was null or undefined when calling deleteSnapshot().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/snapshots/{snapshotAppId}`.replace(`{${"snapshotAppId"}}`, encodeURIComponent(String(requestParameters['snapshotAppId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Soft-deletes the snapshot and all its SnapshotEntry rows. The underlying entity data is unaffected. Requires Write permission on the root Collection.
     * [v2] Delete a snapshot.
     */
    async deleteSnapshot(requestParameters: DeleteSnapshotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSnapshotRaw(requestParameters, initOverrides);
    }

    /**
     * Compares the entity sets of snapshot A (base) and snapshot B (head). Returns lists of entityAppIds that were added (in B, not in A), removed (in A, not in B), or changed revision (present in both with different revision), plus a count of unchanged entities (same revision in both). All lists are sorted by entityAppId ascending. Requires an authenticated caller; no per-collection permission gate in v1.
     * [v2] Diff two snapshots.
     */
    async diffRaw(requestParameters: DiffRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SnapshotDiff>> {
        if (requestParameters['aAppId'] == null) {
            throw new runtime.RequiredError(
                'aAppId',
                'Required parameter "aAppId" was null or undefined when calling diff().'
            );
        }

        if (requestParameters['bAppId'] == null) {
            throw new runtime.RequiredError(
                'bAppId',
                'Required parameter "bAppId" was null or undefined when calling diff().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/snapshots/{aAppId}/diff/{bAppId}`.replace(`{${"aAppId"}}`, encodeURIComponent(String(requestParameters['aAppId']))).replace(`{${"bAppId"}}`, encodeURIComponent(String(requestParameters['bAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SnapshotDiffFromJSON(jsonValue));
    }

    /**
     * Compares the entity sets of snapshot A (base) and snapshot B (head). Returns lists of entityAppIds that were added (in B, not in A), removed (in A, not in B), or changed revision (present in both with different revision), plus a count of unchanged entities (same revision in both). All lists are sorted by entityAppId ascending. Requires an authenticated caller; no per-collection permission gate in v1.
     * [v2] Diff two snapshots.
     */
    async diff(requestParameters: DiffRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SnapshotDiff> {
        const response = await this.diffRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the appIds of all DataObject nodes that were captured in the given snapshot for the specified Collection. Collections, References, and soft-deleted entities are excluded. Snapshot metadata (name, capturedAt, totalEntries) is included for caller convenience. Requires Read permission on the Collection.
     * [v2] List DataObjects captured in a snapshot.
     */
    async getDataObjectsRaw(requestParameters: GetDataObjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SnapshotDataObjects>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling getDataObjects().'
            );
        }

        if (requestParameters['snapshotAppId'] == null) {
            throw new runtime.RequiredError(
                'snapshotAppId',
                'Required parameter "snapshotAppId" was null or undefined when calling getDataObjects().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/snapshots/{snapshotAppId}/data-objects`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"snapshotAppId"}}`, encodeURIComponent(String(requestParameters['snapshotAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SnapshotDataObjectsFromJSON(jsonValue));
    }

    /**
     * Returns the appIds of all DataObject nodes that were captured in the given snapshot for the specified Collection. Collections, References, and soft-deleted entities are excluded. Snapshot metadata (name, capturedAt, totalEntries) is included for caller convenience. Requires Read permission on the Collection.
     * [v2] List DataObjects captured in a snapshot.
     */
    async getDataObjects(requestParameters: GetDataObjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SnapshotDataObjects> {
        const response = await this.getDataObjectsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns name, description, snapshotCapturedAt, snapshotCreatedByUsername, collectionAppId, and entryCount. Requires Read permission on the root Collection.
     * [v2] Read snapshot metadata.
     */
    async getSnapshotRaw(requestParameters: GetSnapshotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Snapshot>> {
        if (requestParameters['snapshotAppId'] == null) {
            throw new runtime.RequiredError(
                'snapshotAppId',
                'Required parameter "snapshotAppId" was null or undefined when calling getSnapshot().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/snapshots/{snapshotAppId}`.replace(`{${"snapshotAppId"}}`, encodeURIComponent(String(requestParameters['snapshotAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SnapshotFromJSON(jsonValue));
    }

    /**
     * Returns name, description, snapshotCapturedAt, snapshotCreatedByUsername, collectionAppId, and entryCount. Requires Read permission on the root Collection.
     * [v2] Read snapshot metadata.
     */
    async getSnapshot(requestParameters: GetSnapshotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Snapshot> {
        const response = await this.getSnapshotRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one page of non-deleted `:Snapshot` entities for the Collection identified by `collectionAppId`, ordered by `snapshotCapturedAtMs` DESC (newest first).  Each `SnapshotIO` carries: `appId`, `name`, `description`, `snapshotCapturedAtMs` (millis-epoch), `snapshotCreatedByUsername`, and `entryCount` (number of VersionableEntities recorded in the manifest).  Pagination: omit `page` / `size` to get the first 50; supply both to paginate. `size` capped at 200 server-side.  Auth: Read on the Collection.  Next step: `GET /v2/snapshots/{snapshotAppId}/manifest` for the per-entity entries of a specific Snapshot, or `GET /v2/snapshots/{a}/diff/{b}` to compare two Snapshots.
     * [v2] List Snapshots of a Collection, newest first (V2b).
     */
    async listCollectionSnapshotsRaw(requestParameters: ListCollectionSnapshotsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<Snapshot>>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling listCollectionSnapshots().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/snapshots`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SnapshotFromJSON));
    }

    /**
     * Returns one page of non-deleted `:Snapshot` entities for the Collection identified by `collectionAppId`, ordered by `snapshotCapturedAtMs` DESC (newest first).  Each `SnapshotIO` carries: `appId`, `name`, `description`, `snapshotCapturedAtMs` (millis-epoch), `snapshotCreatedByUsername`, and `entryCount` (number of VersionableEntities recorded in the manifest).  Pagination: omit `page` / `size` to get the first 50; supply both to paginate. `size` capped at 200 server-side.  Auth: Read on the Collection.  Next step: `GET /v2/snapshots/{snapshotAppId}/manifest` for the per-entity entries of a specific Snapshot, or `GET /v2/snapshots/{a}/diff/{b}` to compare two Snapshots.
     * [v2] List Snapshots of a Collection, newest first (V2b).
     */
    async listCollectionSnapshots(requestParameters: ListCollectionSnapshotsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<Snapshot>> {
        const response = await this.listCollectionSnapshotsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one page of `:Snapshot` rows the caller has Read on, ordered newest first (`snapshotCapturedAtMs DESC`). Optional `collectionAppId` scopes the result to a single Collection; when absent, the list spans every Collection the caller can read.  Response envelope: `{ items[], total, page, pageSize }`. **`total` reports the unfiltered count** (every snapshot in scope, not just the ones the caller can read) — so a caller looking at `items.length` vs. `total` can infer how many they can\'t see. The page is post-filtered to the readable subset, so `items.length` may be lower than `pageSize` even when more snapshots exist.  Surfaces in `SNAPSHOT-LIST-1-FE` (the `/snapshots/diff` picker follow-up).
     * [v2] List snapshots the caller can read (paginated, optionally scoped to one Collection).
     */
    async listSnapshotsRaw(requestParameters: ListSnapshotsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SnapshotListPageIO>> {
        const queryParameters: any = {};

        if (requestParameters['collectionAppId'] != null) {
            queryParameters['collectionAppId'] = requestParameters['collectionAppId'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/snapshots`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SnapshotListPageIOFromJSON(jsonValue));
    }

    /**
     * Returns one page of `:Snapshot` rows the caller has Read on, ordered newest first (`snapshotCapturedAtMs DESC`). Optional `collectionAppId` scopes the result to a single Collection; when absent, the list spans every Collection the caller can read.  Response envelope: `{ items[], total, page, pageSize }`. **`total` reports the unfiltered count** (every snapshot in scope, not just the ones the caller can read) — so a caller looking at `items.length` vs. `total` can infer how many they can\'t see. The page is post-filtered to the readable subset, so `items.length` may be lower than `pageSize` even when more snapshots exist.  Surfaces in `SNAPSHOT-LIST-1-FE` (the `/snapshots/diff` picker follow-up).
     * [v2] List snapshots the caller can read (paginated, optionally scoped to one Collection).
     */
    async listSnapshots(requestParameters: ListSnapshotsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SnapshotListPageIO> {
        const response = await this.listSnapshotsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every (entityAppId, revision) pair captured at snapshot time. Ordered by entityAppId ascending for deterministic diff tooling. Requires Read permission on the root Collection.
     * [v2] Read the full snapshot manifest.
     */
    async manifestRaw(requestParameters: ManifestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SnapshotEntry>>> {
        if (requestParameters['snapshotAppId'] == null) {
            throw new runtime.RequiredError(
                'snapshotAppId',
                'Required parameter "snapshotAppId" was null or undefined when calling manifest().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/snapshots/{snapshotAppId}/manifest`.replace(`{${"snapshotAppId"}}`, encodeURIComponent(String(requestParameters['snapshotAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SnapshotEntryFromJSON));
    }

    /**
     * Returns every (entityAppId, revision) pair captured at snapshot time. Ordered by entityAppId ascending for deterministic diff tooling. Requires Read permission on the root Collection.
     * [v2] Read the full snapshot manifest.
     */
    async manifest(requestParameters: ManifestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SnapshotEntry>> {
        const response = await this.manifestRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
