/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AbandonRequestIO,
  AcquireRequestIO,
  BatchIngestIO,
  ImportContextIO,
  ImportManifestIO,
  IngestEventIO,
  LockStatusIO,
  PagedResponseRunSummaryIO,
  RunSummaryIO,
} from '../models/index';
import {
    AbandonRequestIOFromJSON,
    AbandonRequestIOToJSON,
    AcquireRequestIOFromJSON,
    AcquireRequestIOToJSON,
    BatchIngestIOFromJSON,
    BatchIngestIOToJSON,
    ImportContextIOFromJSON,
    ImportContextIOToJSON,
    ImportManifestIOFromJSON,
    ImportManifestIOToJSON,
    IngestEventIOFromJSON,
    IngestEventIOToJSON,
    LockStatusIOFromJSON,
    LockStatusIOToJSON,
    PagedResponseRunSummaryIOFromJSON,
    RunSummaryIOFromJSON,
    RunSummaryIOToJSON,
} from '../models/index';

export interface AbandonRequest {
    lockId: string;
    abandonRequestIO: AbandonRequestIO;
}

export interface AcquireRequest {
    acquireRequestIO: AcquireRequestIO;
}

export interface CancelRequest {
    lockId: string;
}

export interface GetContextRequest {
    collectionAppId?: string;
    includeSemanticGraph?: boolean;
}

export interface GetEventsRequest {
    runId: string;
    level?: string;
    phase?: string;
}

export interface GetPlanRequest {
    commitId: string;
}

export interface HeartbeatRequest {
    lockId: string;
}

export interface IngestBatchRequest {
    runId: string;
    batchIngestIO: BatchIngestIO;
}

export interface IngestEventRequest {
    runId: string;
    ingestEventIO: IngestEventIO;
}

export interface ReleaseRequest {
    lockId: string;
}

export interface ValidateImportRequest {
    importManifestIO: ImportManifestIO;
}

/**
 * 
 */
export class ImportApi extends runtime.BaseAPI {

    /**
     * Transitions a RUNNING lock to FAILED status with an error description. Use this when the import terminates abnormally. Returns 404 if the lock does not exist; returns 409 if the lock is not in RUNNING status.
     * [v2] Abandon import lock on error (IMP-LOCK)
     */
    async abandonRaw(requestParameters: AbandonRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LockStatusIO>> {
        if (requestParameters['lockId'] == null) {
            throw new runtime.RequiredError(
                'lockId',
                'Required parameter "lockId" was null or undefined when calling abandon().'
            );
        }

        if (requestParameters['abandonRequestIO'] == null) {
            throw new runtime.RequiredError(
                'abandonRequestIO',
                'Required parameter "abandonRequestIO" was null or undefined when calling abandon().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/lock/{lockId}/abandon`.replace(`{${"lockId"}}`, encodeURIComponent(String(requestParameters['lockId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AbandonRequestIOToJSON(requestParameters['abandonRequestIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LockStatusIOFromJSON(jsonValue));
    }

    /**
     * Transitions a RUNNING lock to FAILED status with an error description. Use this when the import terminates abnormally. Returns 404 if the lock does not exist; returns 409 if the lock is not in RUNNING status.
     * [v2] Abandon import lock on error (IMP-LOCK)
     */
    async abandon(requestParameters: AbandonRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LockStatusIO> {
        const response = await this.abandonRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a new RUNNING import lock for the specified collection. Returns 409 if a fresh lock already exists (heartbeat within the last 5 minutes). If the existing lock is stale (> 5 min without heartbeat), it is automatically transitioned to ABANDONED and a new lock is created.
     * [v2] Acquire an import lock (IMP-LOCK)
     */
    async acquireRaw(requestParameters: AcquireRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LockStatusIO>> {
        if (requestParameters['acquireRequestIO'] == null) {
            throw new runtime.RequiredError(
                'acquireRequestIO',
                'Required parameter "acquireRequestIO" was null or undefined when calling acquire().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/lock`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AcquireRequestIOToJSON(requestParameters['acquireRequestIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LockStatusIOFromJSON(jsonValue));
    }

    /**
     * Creates a new RUNNING import lock for the specified collection. Returns 409 if a fresh lock already exists (heartbeat within the last 5 minutes). If the existing lock is stale (> 5 min without heartbeat), it is automatically transitioned to ABANDONED and a new lock is created.
     * [v2] Acquire an import lock (IMP-LOCK)
     */
    async acquire(requestParameters: AcquireRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LockStatusIO> {
        const response = await this.acquireRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Transitions a RUNNING lock to CANCELLED status. Terminal locks (COMPLETED, FAILED, CANCELLED, ABANDONED) are returned as-is — the operation is idempotent with respect to admin intent. Requires instance-admin role.
     * [v2] Admin cancel an import lock (IMP-LOCK)
     */
    async cancelRaw(requestParameters: CancelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LockStatusIO>> {
        if (requestParameters['lockId'] == null) {
            throw new runtime.RequiredError(
                'lockId',
                'Required parameter "lockId" was null or undefined when calling cancel().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/lock/{lockId}`.replace(`{${"lockId"}}`, encodeURIComponent(String(requestParameters['lockId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LockStatusIOFromJSON(jsonValue));
    }

    /**
     * Transitions a RUNNING lock to CANCELLED status. Terminal locks (COMPLETED, FAILED, CANCELLED, ABANDONED) are returned as-is — the operation is idempotent with respect to admin intent. Requires instance-admin role.
     * [v2] Admin cancel an import lock (IMP-LOCK)
     */
    async cancel(requestParameters: CancelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LockStatusIO> {
        const response = await this.cancelRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a snapshot of the target collection — DataObject count and a SHA-256 fingerprint of the collection state — so that an importer or agent can generate a manifest that is consistent with the collection as it currently exists.  When `includeSemanticGraph=true` is supplied, the response also includes the semantic annotations currently attached to DataObjects in the collection, giving agents the vocabulary they need to annotate new DataObjects consistently with existing terms.  The `includeSemanticGraph=false` (default) path is fast — it executes a single Cypher count query and skips any annotation lookup.  Auth: Read on the target Collection. 404 when the `collectionAppId` does not resolve.
     * [v2] Get the current collection state for importers/agents (IMP1)
     */
    async getContextRaw(requestParameters: GetContextRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ImportContextIO>> {
        const queryParameters: any = {};

        if (requestParameters['collectionAppId'] != null) {
            queryParameters['collectionAppId'] = requestParameters['collectionAppId'];
        }

        if (requestParameters['includeSemanticGraph'] != null) {
            queryParameters['includeSemanticGraph'] = requestParameters['includeSemanticGraph'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/context`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ImportContextIOFromJSON(jsonValue));
    }

    /**
     * Returns a snapshot of the target collection — DataObject count and a SHA-256 fingerprint of the collection state — so that an importer or agent can generate a manifest that is consistent with the collection as it currently exists.  When `includeSemanticGraph=true` is supplied, the response also includes the semantic annotations currently attached to DataObjects in the collection, giving agents the vocabulary they need to annotate new DataObjects consistently with existing terms.  The `includeSemanticGraph=false` (default) path is fast — it executes a single Cypher count query and skips any annotation lookup.  Auth: Read on the target Collection. 404 when the `collectionAppId` does not resolve.
     * [v2] Get the current collection state for importers/agents (IMP1)
     */
    async getContext(requestParameters: GetContextRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ImportContextIO> {
        const response = await this.getContextRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the most recent import lock regardless of status, so callers can tell whether an import is currently running or what the last import\'s outcome was. Returns 204 when no lock has ever been created.
     * [v2] Get current import lock status (IMP-LOCK)
     */
    async getCurrentRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LockStatusIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/lock`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LockStatusIOFromJSON(jsonValue));
    }

    /**
     * Returns the most recent import lock regardless of status, so callers can tell whether an import is currently running or what the last import\'s outcome was. Returns 204 when no lock has ever been created.
     * [v2] Get current import lock status (IMP-LOCK)
     */
    async getCurrent(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LockStatusIO | null | undefined > {
        const response = await this.getCurrentRaw(initOverrides);
        switch (response.raw.status) {
            case 200:
                return await response.value();
            case 204:
                return null;
            default:
                return await response.value();
        }
    }

    /**
     * Returns all in-memory diagnostic events for the given runId (= ImportLock.lockId), ordered oldest-first. Optional query parameters: `level` filters to a single severity (INFO / WARN / ERROR); `phase` filters to a single import phase (WARMUP / DO_CREATE / REF_ATTACH / FILE_UPLOAD / COMPLETE). Returns an empty array when the runId is unknown or has no events. Events are held in-memory — they are not persisted across server restarts and are evicted after 24 hours.
     * [v2] Get diagnostic events for an import run (IMP-DIAG)
     */
    async getEventsRaw(requestParameters: GetEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['runId'] == null) {
            throw new runtime.RequiredError(
                'runId',
                'Required parameter "runId" was null or undefined when calling getEvents().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['level'] != null) {
            queryParameters['level'] = requestParameters['level'];
        }

        if (requestParameters['phase'] != null) {
            queryParameters['phase'] = requestParameters['phase'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/diagnostics/{runId}`.replace(`{${"runId"}}`, encodeURIComponent(String(requestParameters['runId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Returns all in-memory diagnostic events for the given runId (= ImportLock.lockId), ordered oldest-first. Optional query parameters: `level` filters to a single severity (INFO / WARN / ERROR); `phase` filters to a single import phase (WARMUP / DO_CREATE / REF_ATTACH / FILE_UPLOAD / COMPLETE). Returns an empty array when the runId is unknown or has no events. Events are held in-memory — they are not persisted across server restarts and are evicted after 24 hours.
     * [v2] Get diagnostic events for an import run (IMP-DIAG)
     */
    async getEvents(requestParameters: GetEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getEventsRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the validation plan associated with a commitId. Expired plans are still returned with status=EXPIRED; the jobs endpoint rejects them.
     * [v2] Get an import plan by commitId (IMP1)
     */
    async getPlanRaw(requestParameters: GetPlanRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['commitId'] == null) {
            throw new runtime.RequiredError(
                'commitId',
                'Required parameter "commitId" was null or undefined when calling getPlan().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/plans/{commitId}`.replace(`{${"commitId"}}`, encodeURIComponent(String(requestParameters['commitId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Returns the validation plan associated with a commitId. Expired plans are still returned with status=EXPIRED; the jobs endpoint rejects them.
     * [v2] Get an import plan by commitId (IMP1)
     */
    async getPlan(requestParameters: GetPlanRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getPlanRaw(requestParameters, initOverrides);
    }

    /**
     * Updates lastHeartbeatAt to now for the specified RUNNING lock. Call approximately every 30 s while the import is running. Returns 404 if the lock does not exist; returns 409 if the lock is not in RUNNING status.
     * [v2] Extend import lock heartbeat (IMP-LOCK)
     */
    async heartbeatRaw(requestParameters: HeartbeatRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LockStatusIO>> {
        if (requestParameters['lockId'] == null) {
            throw new runtime.RequiredError(
                'lockId',
                'Required parameter "lockId" was null or undefined when calling heartbeat().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/lock/{lockId}/heartbeat`.replace(`{${"lockId"}}`, encodeURIComponent(String(requestParameters['lockId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LockStatusIOFromJSON(jsonValue));
    }

    /**
     * Updates lastHeartbeatAt to now for the specified RUNNING lock. Call approximately every 30 s while the import is running. Returns 404 if the lock does not exist; returns 409 if the lock is not in RUNNING status.
     * [v2] Extend import lock heartbeat (IMP-LOCK)
     */
    async heartbeat(requestParameters: HeartbeatRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LockStatusIO> {
        const response = await this.heartbeatRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Ingests multiple structured diagnostic events into the in-memory log for the specified run in a single HTTP call. All events are applied atomically in list order. If any event in the batch has invalid level/phase values, the entire batch is rejected with 400 and no events are recorded. Returns 204 on success.
     * [v2] Push a batch of diagnostic events from an external importer (IMP-DIAG)
     */
    async ingestBatchRaw(requestParameters: IngestBatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['runId'] == null) {
            throw new runtime.RequiredError(
                'runId',
                'Required parameter "runId" was null or undefined when calling ingestBatch().'
            );
        }

        if (requestParameters['batchIngestIO'] == null) {
            throw new runtime.RequiredError(
                'batchIngestIO',
                'Required parameter "batchIngestIO" was null or undefined when calling ingestBatch().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/diagnostics/{runId}/events/batch`.replace(`{${"runId"}}`, encodeURIComponent(String(requestParameters['runId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BatchIngestIOToJSON(requestParameters['batchIngestIO']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Ingests multiple structured diagnostic events into the in-memory log for the specified run in a single HTTP call. All events are applied atomically in list order. If any event in the batch has invalid level/phase values, the entire batch is rejected with 400 and no events are recorded. Returns 204 on success.
     * [v2] Push a batch of diagnostic events from an external importer (IMP-DIAG)
     */
    async ingestBatch(requestParameters: IngestBatchRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.ingestBatchRaw(requestParameters, initOverrides);
    }

    /**
     * Ingests a single structured diagnostic event into the in-memory log for the specified run. Intended for the external Python importer, which calls this endpoint to record DO_CREATE / REF_ATTACH / FILE_UPLOAD phase events that the Java service cannot capture directly. The runId must match an ImportLock.lockId obtained via POST /v2/import/lock. Returns 204 on success. Returns 400 when required fields are missing or level/phase values are invalid.
     * [v2] Push a single diagnostic event from an external importer (IMP-DIAG)
     */
    async ingestEventRaw(requestParameters: IngestEventRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['runId'] == null) {
            throw new runtime.RequiredError(
                'runId',
                'Required parameter "runId" was null or undefined when calling ingestEvent().'
            );
        }

        if (requestParameters['ingestEventIO'] == null) {
            throw new runtime.RequiredError(
                'ingestEventIO',
                'Required parameter "ingestEventIO" was null or undefined when calling ingestEvent().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/diagnostics/{runId}/events`.replace(`{${"runId"}}`, encodeURIComponent(String(requestParameters['runId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: IngestEventIOToJSON(requestParameters['ingestEventIO']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Ingests a single structured diagnostic event into the in-memory log for the specified run. Intended for the external Python importer, which calls this endpoint to record DO_CREATE / REF_ATTACH / FILE_UPLOAD phase events that the Java service cannot capture directly. The runId must match an ImportLock.lockId obtained via POST /v2/import/lock. Returns 204 on success. Returns 400 when required fields are missing or level/phase values are invalid.
     * [v2] Push a single diagnostic event from an external importer (IMP-DIAG)
     */
    async ingestEvent(requestParameters: IngestEventRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.ingestEventRaw(requestParameters, initOverrides);
    }

    /**
     * Returns a list of run IDs known to the in-memory diagnostic log, sorted by start time descending (most recent first). Each entry includes: runId, startedAt, lastEventAt, and lastLevel (most-severe level seen: INFO / WARN / ERROR). The list is populated by both Java-side lock lifecycle events and events ingested from external processes via POST /v2/import/diagnostics/{runId}/events. Runs are evicted after 24 hours; the list does not include runs from before the last server restart.
     * [v2] List recent import runs with diagnostic summary (IMP-DIAG)
     */
    async listRunsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponseRunSummaryIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/runs`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseRunSummaryIOFromJSON(jsonValue));
    }

    /**
     * Returns a list of run IDs known to the in-memory diagnostic log, sorted by start time descending (most recent first). Each entry includes: runId, startedAt, lastEventAt, and lastLevel (most-severe level seen: INFO / WARN / ERROR). The list is populated by both Java-side lock lifecycle events and events ingested from external processes via POST /v2/import/diagnostics/{runId}/events. Runs are evicted after 24 hours; the list does not include runs from before the last server restart.
     * [v2] List recent import runs with diagnostic summary (IMP-DIAG)
     */
    async listRuns(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponseRunSummaryIO> {
        const response = await this.listRunsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Transitions a RUNNING lock to COMPLETED status (normal import completion). Returns 404 if the lock does not exist; returns 409 if the lock is not in RUNNING status.
     * [v2] Release import lock on completion (IMP-LOCK)
     */
    async releaseRaw(requestParameters: ReleaseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LockStatusIO>> {
        if (requestParameters['lockId'] == null) {
            throw new runtime.RequiredError(
                'lockId',
                'Required parameter "lockId" was null or undefined when calling release().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/lock/{lockId}/release`.replace(`{${"lockId"}}`, encodeURIComponent(String(requestParameters['lockId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LockStatusIOFromJSON(jsonValue));
    }

    /**
     * Transitions a RUNNING lock to COMPLETED status (normal import completion). Returns 404 if the lock does not exist; returns 409 if the lock is not in RUNNING status.
     * [v2] Release import lock on completion (IMP-LOCK)
     */
    async release(requestParameters: ReleaseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LockStatusIO> {
        const response = await this.releaseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Validates the manifest without writing any DataObjects, Containers, or References. On success, returns a commitId (plan seal) that must be presented to POST /v2/import/jobs to execute the import. The commitId is bound to the collection state at validation time — if the collection changes before the import runs, the jobs endpoint will reject the plan. Plans expire 24 hours after issuance.
     * [v2] Dry-run validate an import manifest (IMP1)
     */
    async validateImportRaw(requestParameters: ValidateImportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['importManifestIO'] == null) {
            throw new runtime.RequiredError(
                'importManifestIO',
                'Required parameter "importManifestIO" was null or undefined when calling validateImport().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/import/validate`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ImportManifestIOToJSON(requestParameters['importManifestIO']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Validates the manifest without writing any DataObjects, Containers, or References. On success, returns a commitId (plan seal) that must be presented to POST /v2/import/jobs to execute the import. The commitId is bound to the collection state at validation time — if the collection changes before the import runs, the jobs endpoint will reject the plan. Plans expire 24 hours after issuance.
     * [v2] Dry-run validate an import manifest (IMP1)
     */
    async validateImport(requestParameters: ValidateImportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.validateImportRaw(requestParameters, initOverrides);
    }

}
