/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PublicationState,
} from '../models/index';
import {
    PublicationStateFromJSON,
    PublicationStateToJSON,
} from '../models/index';

export interface GetContainerPublicationStateRequest {
    containerAppId: string;
}

export interface PatchContainerPublicationStateRequest {
    containerAppId: string;
    publicationState: Omit<PublicationState, 'archived'>;
}

/**
 * 
 */
export class ContainerLifecycleApi extends runtime.BaseAPI {

    /**
     * Returns the container\'s `status` and a convenience `archived` boolean. Works for any container kind (File / Timeseries / StructuredData / Hdf). Auth: Read permission on the container.
     * [v2] Read the current publication state of a Container.
     */
    async getContainerPublicationStateRaw(requestParameters: GetContainerPublicationStateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PublicationState>> {
        if (requestParameters['containerAppId'] == null) {
            throw new runtime.RequiredError(
                'containerAppId',
                'Required parameter "containerAppId" was null or undefined when calling getContainerPublicationState().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/containers/{containerAppId}/publication-state`.replace(`{${"containerAppId"}}`, encodeURIComponent(String(requestParameters['containerAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PublicationStateFromJSON(jsonValue));
    }

    /**
     * Returns the container\'s `status` and a convenience `archived` boolean. Works for any container kind (File / Timeseries / StructuredData / Hdf). Auth: Read permission on the container.
     * [v2] Read the current publication state of a Container.
     */
    async getContainerPublicationState(requestParameters: GetContainerPublicationStateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PublicationState> {
        const response = await this.getContainerPublicationStateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets the container\'s `status` to one of DRAFT, IN_REVIEW, READY, PUBLISHED, ARCHIVED. When ARCHIVED, every subsequent payload write (POST/PATCH/DELETE on files, channels, structured-data rows) returns 409 until the state is flipped back. Reads are unaffected.  Auth: Manage permission on the container OR `instance-admin` role.  Body: `{\"state\": \"ARCHIVED\"}`.
     * [v2] Flip the publication state of a Container (Owner or instance-admin only).
     */
    async patchContainerPublicationStateRaw(requestParameters: PatchContainerPublicationStateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PublicationState>> {
        if (requestParameters['containerAppId'] == null) {
            throw new runtime.RequiredError(
                'containerAppId',
                'Required parameter "containerAppId" was null or undefined when calling patchContainerPublicationState().'
            );
        }

        if (requestParameters['publicationState'] == null) {
            throw new runtime.RequiredError(
                'publicationState',
                'Required parameter "publicationState" was null or undefined when calling patchContainerPublicationState().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/containers/{containerAppId}/publication-state`.replace(`{${"containerAppId"}}`, encodeURIComponent(String(requestParameters['containerAppId']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PublicationStateToJSON(requestParameters['publicationState']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PublicationStateFromJSON(jsonValue));
    }

    /**
     * Sets the container\'s `status` to one of DRAFT, IN_REVIEW, READY, PUBLISHED, ARCHIVED. When ARCHIVED, every subsequent payload write (POST/PATCH/DELETE on files, channels, structured-data rows) returns 409 until the state is flipped back. Reads are unaffected.  Auth: Manage permission on the container OR `instance-admin` role.  Body: `{\"state\": \"ARCHIVED\"}`.
     * [v2] Flip the publication state of a Container (Owner or instance-admin only).
     */
    async patchContainerPublicationState(requestParameters: PatchContainerPublicationStateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PublicationState> {
        const response = await this.patchContainerPublicationStateRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
