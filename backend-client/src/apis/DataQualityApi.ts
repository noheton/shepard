/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  IndependenceProofRequestIO,
  IndependenceProofResultIO,
} from '../models/index';
import {
    IndependenceProofRequestIOFromJSON,
    IndependenceProofRequestIOToJSON,
    IndependenceProofResultIOFromJSON,
    IndependenceProofResultIOToJSON,
} from '../models/index';

export interface CheckRequest {
    independenceProofRequestIO: IndependenceProofRequestIO;
}

/**
 * 
 */
export class DataQualityApi extends runtime.BaseAPI {

    /**
     * Runs two checks against the two supplied DataObject appId sets:  1. **Provenance ancestry** — are there any DataObjects that are    common ancestors (within 10 hops) of members in both sets? 2. **Shared annotations** — do any members of setA and setB share an    annotation key-value pair (strict equality on both key and value)?  `independent: true` is returned only when both checks find no overlap.  **Typical use case:** validate that a training set and a test set used for AI/ML training do not share data lineage.  **Hop cap:** the ancestor walk is bounded at 10 hops. Provenance chains longer than 10 levels are not covered by this check — the result is best-effort, not a formal mathematical proof.  **Auth:** any authenticated user. The endpoint returns only structural metadata (ancestor appIds, annotation keys/values) — no DataObject content.
     * [v2] Check whether two DataObject sets are mutually independent.
     */
    async checkRaw(requestParameters: CheckRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<IndependenceProofResultIO>> {
        if (requestParameters['independenceProofRequestIO'] == null) {
            throw new runtime.RequiredError(
                'independenceProofRequestIO',
                'Required parameter "independenceProofRequestIO" was null or undefined when calling check().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/quality/independence-proof`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: IndependenceProofRequestIOToJSON(requestParameters['independenceProofRequestIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => IndependenceProofResultIOFromJSON(jsonValue));
    }

    /**
     * Runs two checks against the two supplied DataObject appId sets:  1. **Provenance ancestry** — are there any DataObjects that are    common ancestors (within 10 hops) of members in both sets? 2. **Shared annotations** — do any members of setA and setB share an    annotation key-value pair (strict equality on both key and value)?  `independent: true` is returned only when both checks find no overlap.  **Typical use case:** validate that a training set and a test set used for AI/ML training do not share data lineage.  **Hop cap:** the ancestor walk is bounded at 10 hops. Provenance chains longer than 10 levels are not covered by this check — the result is best-effort, not a formal mathematical proof.  **Auth:** any authenticated user. The endpoint returns only structural metadata (ancestor appIds, annotation keys/values) — no DataObject content.
     * [v2] Check whether two DataObject sets are mutually independent.
     */
    async check(requestParameters: CheckRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<IndependenceProofResultIO> {
        const response = await this.checkRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
