/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CreateWatchIO,
  PagedResponse,
  WatchIO,
} from '../models/index';
import {
    CreateWatchIOFromJSON,
    CreateWatchIOToJSON,
    PagedResponseFromJSON,
    PagedResponseToJSON,
    WatchIOFromJSON,
    WatchIOToJSON,
} from '../models/index';

export interface CreateWatchedContainerRequest {
    collectionAppId: string;
    createWatchIO: CreateWatchIO;
}

export interface DeleteWatchedContainerRequest {
    collectionAppId: string;
    watchAppId: string;
}

export interface ListWatchedContainersRequest {
    collectionAppId: string;
    page?: number;
    pageSize?: number;
}

/**
 * 
 */
export class CollectionWatchedContainersApi extends runtime.BaseAPI {

    /**
     * Creates a `:Watch` edge from the Collection identified by `collectionAppId` to the Container identified by the body. The target container is NOT modified — only a new graph edge is added.  Body fields (all required):   - `containerKind` (one of `TIMESERIES`, `FILE`, `STRUCTURED_DATA`).   - `containerAppId` (UUID v7 of an existing container of that kind).  Example body: `{\"containerKind\": \"TIMESERIES\", \"containerAppId\": \"019e3c96-…\"}`.  Idempotency: the `(collection, container)` pair is the dedupe key. A second POST with the same pair returns 201 carrying the EXISTING WatchIO instead of erroring with 409 — safe for re-running seed scripts.  Auth: Write on the Collection AND Read on the target Container. The Read check on the container is enforced at create time so a caller can\'t slip a watch in to entities they can\'t see (otherwise the watch panel would always show \'forbidden\' for that row, which is just noise).  Side effects: ProvenanceCaptureFilter records a `CREATE` Activity on the new Watch\'s appId.  Next step: `GET /v2/collections/{collectionAppId}/watched-containers` to confirm the list, or `DELETE ../watched-containers/{watchAppId}` to remove.
     * [v2] Add a Watch link from a Collection to a Container (WATCH1).
     */
    async createWatchedContainerRaw(requestParameters: CreateWatchedContainerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WatchIO>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling createWatchedContainer().'
            );
        }

        if (requestParameters['createWatchIO'] == null) {
            throw new runtime.RequiredError(
                'createWatchIO',
                'Required parameter "createWatchIO" was null or undefined when calling createWatchedContainer().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/watched-containers`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateWatchIOToJSON(requestParameters['createWatchIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WatchIOFromJSON(jsonValue));
    }

    /**
     * Creates a `:Watch` edge from the Collection identified by `collectionAppId` to the Container identified by the body. The target container is NOT modified — only a new graph edge is added.  Body fields (all required):   - `containerKind` (one of `TIMESERIES`, `FILE`, `STRUCTURED_DATA`).   - `containerAppId` (UUID v7 of an existing container of that kind).  Example body: `{\"containerKind\": \"TIMESERIES\", \"containerAppId\": \"019e3c96-…\"}`.  Idempotency: the `(collection, container)` pair is the dedupe key. A second POST with the same pair returns 201 carrying the EXISTING WatchIO instead of erroring with 409 — safe for re-running seed scripts.  Auth: Write on the Collection AND Read on the target Container. The Read check on the container is enforced at create time so a caller can\'t slip a watch in to entities they can\'t see (otherwise the watch panel would always show \'forbidden\' for that row, which is just noise).  Side effects: ProvenanceCaptureFilter records a `CREATE` Activity on the new Watch\'s appId.  Next step: `GET /v2/collections/{collectionAppId}/watched-containers` to confirm the list, or `DELETE ../watched-containers/{watchAppId}` to remove.
     * [v2] Add a Watch link from a Collection to a Container (WATCH1).
     */
    async createWatchedContainer(requestParameters: CreateWatchedContainerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WatchIO> {
        const response = await this.createWatchedContainerRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes the `:Watch` edge identified by `watchAppId` from the Collection identified by `collectionAppId`. The target Container is untouched — only the edge is removed.  Idempotency: deleting a non-existent or already-deleted Watch returns 204 without error.  Sanity: the `watchAppId` must belong to a Watch attached to THIS Collection. A Watch that exists but belongs to a different Collection returns 404, not 403 (the caller has no way to tell whether the Watch exists at all from another Collection\'s vantage).  Auth: Write on the Collection.
     * [v2] Remove a Watch link by its appId (WATCH1).
     */
    async deleteWatchedContainerRaw(requestParameters: DeleteWatchedContainerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling deleteWatchedContainer().'
            );
        }

        if (requestParameters['watchAppId'] == null) {
            throw new runtime.RequiredError(
                'watchAppId',
                'Required parameter "watchAppId" was null or undefined when calling deleteWatchedContainer().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/watched-containers/{watchAppId}`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"watchAppId"}}`, encodeURIComponent(String(requestParameters['watchAppId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes the `:Watch` edge identified by `watchAppId` from the Collection identified by `collectionAppId`. The target Container is untouched — only the edge is removed.  Idempotency: deleting a non-existent or already-deleted Watch returns 204 without error.  Sanity: the `watchAppId` must belong to a Watch attached to THIS Collection. A Watch that exists but belongs to a different Collection returns 404, not 403 (the caller has no way to tell whether the Watch exists at all from another Collection\'s vantage).  Auth: Write on the Collection.
     * [v2] Remove a Watch link by its appId (WATCH1).
     */
    async deleteWatchedContainer(requestParameters: DeleteWatchedContainerRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteWatchedContainerRaw(requestParameters, initOverrides);
    }

    /**
     * Returns one `WatchIO` row per `:Watch` edge attached to the Collection identified by `collectionAppId`. A Watch is a link from a Collection to a Container the Collection does NOT own through any DataObject reference — useful for live-data Collections that surface containers shared from elsewhere (e.g. the home-showcase demo Collection watches three home-energy TimeseriesContainers it doesn\'t own).  Each `WatchIO` carries:   - `appId` (UUID v7 of the Watch edge itself).   - `containerKind` (`TIMESERIES` / `FILE` / `STRUCTURED_DATA`).   - `containerAppId`.   - `containerName` (inlined when the caller has Read on the target, else null).   - `containerAvailability` (`available` / `forbidden` / `deleted` / `error`) — tells the UI whether to render the row as clickable, permission-denied, tombstoned, or error.   - `since` (millis when the watch was added).   - `addedBy` (username).  Auth: Read on the Collection. The per-container availability check filters out container details the caller can\'t see, but the watch row itself is always returned so the operator knows the link exists.  Pagination (APISIMP-WATCHES-LIST-NO-PAGINATION): supply both `page` (0-based) and `pageSize` (1–200) to receive a slice. Omit both to return all watches. `X-Total-Count` header carries the total before paging.  Next step: `POST /v2/collections/{collectionAppId}/watched-containers` to add a watch, or click the target container\'s appId in the UI.
     * [v2] List containers this Collection is watching (WATCH1).
     */
    async listWatchedContainersRaw(requestParameters: ListWatchedContainersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponse>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling listWatchedContainers().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/watched-containers`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseFromJSON(jsonValue));
    }

    /**
     * Returns one `WatchIO` row per `:Watch` edge attached to the Collection identified by `collectionAppId`. A Watch is a link from a Collection to a Container the Collection does NOT own through any DataObject reference — useful for live-data Collections that surface containers shared from elsewhere (e.g. the home-showcase demo Collection watches three home-energy TimeseriesContainers it doesn\'t own).  Each `WatchIO` carries:   - `appId` (UUID v7 of the Watch edge itself).   - `containerKind` (`TIMESERIES` / `FILE` / `STRUCTURED_DATA`).   - `containerAppId`.   - `containerName` (inlined when the caller has Read on the target, else null).   - `containerAvailability` (`available` / `forbidden` / `deleted` / `error`) — tells the UI whether to render the row as clickable, permission-denied, tombstoned, or error.   - `since` (millis when the watch was added).   - `addedBy` (username).  Auth: Read on the Collection. The per-container availability check filters out container details the caller can\'t see, but the watch row itself is always returned so the operator knows the link exists.  Pagination (APISIMP-WATCHES-LIST-NO-PAGINATION): supply both `page` (0-based) and `pageSize` (1–200) to receive a slice. Omit both to return all watches. `X-Total-Count` header carries the total before paging.  Next step: `POST /v2/collections/{collectionAppId}/watched-containers` to add a watch, or click the target container\'s appId in the UI.
     * [v2] List containers this Collection is watching (WATCH1).
     */
    async listWatchedContainers(requestParameters: ListWatchedContainersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponse> {
        const response = await this.listWatchedContainersRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
