/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CreateDQRIO,
  DQRIO,
  DQRResultIO,
  PagedResponse,
} from '../models/index';
import {
    CreateDQRIOFromJSON,
    CreateDQRIOToJSON,
    DQRIOFromJSON,
    DQRIOToJSON,
    DQRResultIOFromJSON,
    DQRResultIOToJSON,
    PagedResponseFromJSON,
    PagedResponseToJSON,
} from '../models/index';

export interface AssignRequest {
    collectionAppId: string;
    createDQRIO: CreateDQRIO;
}

export interface EvaluateRequest {
    collectionAppId: string;
}

export interface ListDataQualityRequirementsRequest {
    collectionAppId: string;
    page?: number;
    pageSize?: number;
}

export interface RemoveRequest {
    collectionAppId: string;
    dqrAppId: string;
}

/**
 * 
 */
export class DataQualityRequirementsApi extends runtime.BaseAPI {

    /**
     * Creates a new Data Quality Requirement node and attaches it to the Collection via an APPLIES_TO relationship. The DQR applies to all DataObjects in the Collection when evaluation is triggered.  Supported ruleTypes: - `ANNOTATION_REQUIRED` — DataObject must have a non-null attribute for the key in `ruleParam`. **Implemented.** - `NO_TIMESERIES_GAP`   — no timeseries gap > `ruleParam` seconds. **Stub (always PASS).** - `FILE_COUNT_MIN`      — file container must have >= `ruleParam` files. **Stub (always PASS).** - `CUSTOM_CYPHER`       — arbitrary Cypher predicate. **Stub (always PASS).**  Auth: Write permission on the Collection.
     * [v2] Assign a new DQR to this Collection (TPL10).
     */
    async assignRaw(requestParameters: AssignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DQRIO>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling assign().'
            );
        }

        if (requestParameters['createDQRIO'] == null) {
            throw new runtime.RequiredError(
                'createDQRIO',
                'Required parameter "createDQRIO" was null or undefined when calling assign().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/dqr`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateDQRIOToJSON(requestParameters['createDQRIO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DQRIOFromJSON(jsonValue));
    }

    /**
     * Creates a new Data Quality Requirement node and attaches it to the Collection via an APPLIES_TO relationship. The DQR applies to all DataObjects in the Collection when evaluation is triggered.  Supported ruleTypes: - `ANNOTATION_REQUIRED` — DataObject must have a non-null attribute for the key in `ruleParam`. **Implemented.** - `NO_TIMESERIES_GAP`   — no timeseries gap > `ruleParam` seconds. **Stub (always PASS).** - `FILE_COUNT_MIN`      — file container must have >= `ruleParam` files. **Stub (always PASS).** - `CUSTOM_CYPHER`       — arbitrary Cypher predicate. **Stub (always PASS).**  Auth: Write permission on the Collection.
     * [v2] Assign a new DQR to this Collection (TPL10).
     */
    async assign(requestParameters: AssignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DQRIO> {
        const response = await this.assignRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Runs every enabled Data Quality Requirement assigned to this Collection and returns one result per (DQR, DataObject) pair. A result with `passed == false` carries a human-readable `message` describing the violation.  Only DQRs with `enabled == true` are evaluated. Disabled DQRs are silently skipped.  Evaluation is synchronous and may be slow on large Collections. A future version will add an async variant.  Auth: Read permission on the Collection.
     * [v2] Evaluate all enabled DQRs for this Collection (TPL10).
     */
    async evaluateRaw(requestParameters: EvaluateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DQRResultIO>>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling evaluate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/dqr/evaluate`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DQRResultIOFromJSON));
    }

    /**
     * Runs every enabled Data Quality Requirement assigned to this Collection and returns one result per (DQR, DataObject) pair. A result with `passed == false` carries a human-readable `message` describing the violation.  Only DQRs with `enabled == true` are evaluated. Disabled DQRs are silently skipped.  Evaluation is synchronous and may be slow on large Collections. A future version will add an async variant.  Auth: Read permission on the Collection.
     * [v2] Evaluate all enabled DQRs for this Collection (TPL10).
     */
    async evaluate(requestParameters: EvaluateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DQRResultIO>> {
        const response = await this.evaluateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns all Data Quality Requirements that have been assigned to this Collection via the APPLIES_TO relationship. Results are unordered and unfiltered — include both enabled and disabled DQRs.  Pagination (APISIMP-PAGINATION-LIST-DQR): supply both `page` (0-based) and `pageSize` (1–200) to receive a slice. Omit both to return all DQRs. `X-Total-Count` header carries the total DQR count before paging.  Auth: Read permission on the Collection.
     * [v2] List DQRs assigned to this Collection (TPL10).
     */
    async listDataQualityRequirementsRaw(requestParameters: ListDataQualityRequirementsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponse>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling listDataQualityRequirements().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/dqr`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseFromJSON(jsonValue));
    }

    /**
     * Returns all Data Quality Requirements that have been assigned to this Collection via the APPLIES_TO relationship. Results are unordered and unfiltered — include both enabled and disabled DQRs.  Pagination (APISIMP-PAGINATION-LIST-DQR): supply both `page` (0-based) and `pageSize` (1–200) to receive a slice. Omit both to return all DQRs. `X-Total-Count` header carries the total DQR count before paging.  Auth: Read permission on the Collection.
     * [v2] List DQRs assigned to this Collection (TPL10).
     */
    async listDataQualityRequirements(requestParameters: ListDataQualityRequirementsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponse> {
        const response = await this.listDataQualityRequirementsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes the Data Quality Requirement node and detaches all its relationships. Returns 404 when the DQR does not exist or is not assigned to this Collection.  Auth: Write permission on the Collection.
     * [v2] Remove a DQR from this Collection (TPL10).
     */
    async removeRaw(requestParameters: RemoveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['collectionAppId'] == null) {
            throw new runtime.RequiredError(
                'collectionAppId',
                'Required parameter "collectionAppId" was null or undefined when calling remove().'
            );
        }

        if (requestParameters['dqrAppId'] == null) {
            throw new runtime.RequiredError(
                'dqrAppId',
                'Required parameter "dqrAppId" was null or undefined when calling remove().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{collectionAppId}/dqr/{dqrAppId}`.replace(`{${"collectionAppId"}}`, encodeURIComponent(String(requestParameters['collectionAppId']))).replace(`{${"dqrAppId"}}`, encodeURIComponent(String(requestParameters['dqrAppId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes the Data Quality Requirement node and detaches all its relationships. Returns 404 when the DQR does not exist or is not assigned to this Collection.  Auth: Write permission on the Collection.
     * [v2] Remove a DQR from this Collection (TPL10).
     */
    async remove(requestParameters: RemoveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.removeRaw(requestParameters, initOverrides);
    }

}
