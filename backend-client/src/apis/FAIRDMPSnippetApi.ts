/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  DmpSnippet,
} from '../models/index';
import {
    DmpSnippetFromJSON,
    DmpSnippetToJSON,
} from '../models/index';

export interface GetDmpSnippetRequest {
    appId: string;
}

/**
 * 
 */
export class FAIRDMPSnippetApi extends runtime.BaseAPI {

    /**
     * Returns a copy-paste-ready Markdown Data Management Plan (DMP) block pre-filled with all FAIR-relevant fields from the Collection (`name`, `description`, `license`, `accessRights`) and its DataObjects (`createdByOrcid`, `embargoEndDate`, reference kinds). Designed for researchers who need to fill in DFG / EU Horizon Europe DMP forms.  **Content negotiation:** - `Accept: text/markdown` (default): returns the Markdown block as plain text. - `Accept: application/json`: returns a JSON wrapper with `collectionAppId`, `collectionName`, `snippet` (the same Markdown), and `missingFields` (list of FAIR fields that are null/blank and should be set).  **Missing-fields detection:** the `missingFields` array (JSON variant) lists field names that improve the DMP statement if populated: `license`, `accessRights`, `orcid` (no DataObject has a `createdByOrcid`), `description` (Collection is blank).  **Auth:** Read permission on the Collection. `404` for unknown appIds; `403` for callers without Read access; `401` when unauthenticated.  **PID note:** persistent identifier (PID) lookup is not implemented in this version. The snippet always emits \'no PID assigned\'; see `aidocs/16` FAIR7 row.  **No side effects:** pure read projection — no entity writes, no migrations required.
     * [v2] Generate a FAIR DMP snippet for a Collection.
     */
    async getDmpSnippetRaw(requestParameters: GetDmpSnippetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling getDmpSnippet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{appId}/dmp-snippet`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Returns a copy-paste-ready Markdown Data Management Plan (DMP) block pre-filled with all FAIR-relevant fields from the Collection (`name`, `description`, `license`, `accessRights`) and its DataObjects (`createdByOrcid`, `embargoEndDate`, reference kinds). Designed for researchers who need to fill in DFG / EU Horizon Europe DMP forms.  **Content negotiation:** - `Accept: text/markdown` (default): returns the Markdown block as plain text. - `Accept: application/json`: returns a JSON wrapper with `collectionAppId`, `collectionName`, `snippet` (the same Markdown), and `missingFields` (list of FAIR fields that are null/blank and should be set).  **Missing-fields detection:** the `missingFields` array (JSON variant) lists field names that improve the DMP statement if populated: `license`, `accessRights`, `orcid` (no DataObject has a `createdByOrcid`), `description` (Collection is blank).  **Auth:** Read permission on the Collection. `404` for unknown appIds; `403` for callers without Read access; `401` when unauthenticated.  **PID note:** persistent identifier (PID) lookup is not implemented in this version. The snippet always emits \'no PID assigned\'; see `aidocs/16` FAIR7 row.  **No side effects:** pure read projection — no entity writes, no migrations required.
     * [v2] Generate a FAIR DMP snippet for a Collection.
     */
    async getDmpSnippet(requestParameters: GetDmpSnippetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string> {
        const response = await this.getDmpSnippetRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
