/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  LiveWindowResponseIO,
} from '../models/index';
import {
    LiveWindowResponseIOFromJSON,
    LiveWindowResponseIOToJSON,
} from '../models/index';

export interface GetLiveWindowRequest {
    containerAppId: string;
    device?: string;
    field?: string;
    location?: string;
    measurement?: string;
    shepardId?: string;
    symbolicName?: string;
    windowSeconds?: number;
    withBoundaryPoints?: boolean;
}

/**
 * 
 */
export class TimeseriesContainerLiveWindowApi extends runtime.BaseAPI {

    /**
     * Returns the last `windowSeconds` of data for a single channel in the specified TimeseriesContainer.  **Channel lookup** — two equivalent shapes accepted: - `shepardId` (preferred, TS-IDc): the channel\'s stable single-field UUID identity. Index-only scan; planning time dominates execution. - 5-tuple (legacy bridge): `measurement`, `device`, `location`, `symbolicName`, `field`. Only `measurement` is required; the remaining fields default to the literal string `\"\"` when omitted so channels ingested without those dimensions can be addressed without noise.  If both forms are supplied, `shepardId` wins and the 5-tuple is ignored. If multiple channels match the non-null filter fields the endpoint returns 400 (ambiguous channel); add more filter fields to narrow to one. If no channel matches the filters the endpoint returns 404.  **Boundary points** (when `withBoundaryPoints=true`, the default): if the first raw data point in the window is at T+Δ (not at the window start T), a linearly interpolated point is inserted at exactly T using the last known point before T. Same logic at the trailing edge. Boundary points carry `interpolated: true`. Boundary points are only added for numeric channel types (Double, Integer); Boolean and String channels never get boundary points.  **No extrapolation**: if no point exists before the window start, no start boundary is added. If the window contains no data at all, `points` is empty.  **Timestamps**: all timestamps in the response are epoch milliseconds (UTC). The `windowStart` and `windowEnd` fields reflect the actual computed window boundaries regardless of whether any data fell in the window.  Auth: Read permission on the container (enforced by `containerAppId` lookup).
     * [v2] Fetch the most recent N seconds of a timeseries channel.
     */
    async getLiveWindowRaw(requestParameters: GetLiveWindowRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LiveWindowResponseIO>> {
        if (requestParameters['containerAppId'] == null) {
            throw new runtime.RequiredError(
                'containerAppId',
                'Required parameter "containerAppId" was null or undefined when calling getLiveWindow().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['device'] != null) {
            queryParameters['device'] = requestParameters['device'];
        }

        if (requestParameters['field'] != null) {
            queryParameters['field'] = requestParameters['field'];
        }

        if (requestParameters['location'] != null) {
            queryParameters['location'] = requestParameters['location'];
        }

        if (requestParameters['measurement'] != null) {
            queryParameters['measurement'] = requestParameters['measurement'];
        }

        if (requestParameters['shepardId'] != null) {
            queryParameters['shepardId'] = requestParameters['shepardId'];
        }

        if (requestParameters['symbolicName'] != null) {
            queryParameters['symbolicName'] = requestParameters['symbolicName'];
        }

        if (requestParameters['windowSeconds'] != null) {
            queryParameters['windowSeconds'] = requestParameters['windowSeconds'];
        }

        if (requestParameters['withBoundaryPoints'] != null) {
            queryParameters['withBoundaryPoints'] = requestParameters['withBoundaryPoints'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/timeseries-containers/{containerAppId}/channels/live-window`.replace(`{${"containerAppId"}}`, encodeURIComponent(String(requestParameters['containerAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LiveWindowResponseIOFromJSON(jsonValue));
    }

    /**
     * Returns the last `windowSeconds` of data for a single channel in the specified TimeseriesContainer.  **Channel lookup** — two equivalent shapes accepted: - `shepardId` (preferred, TS-IDc): the channel\'s stable single-field UUID identity. Index-only scan; planning time dominates execution. - 5-tuple (legacy bridge): `measurement`, `device`, `location`, `symbolicName`, `field`. Only `measurement` is required; the remaining fields default to the literal string `\"\"` when omitted so channels ingested without those dimensions can be addressed without noise.  If both forms are supplied, `shepardId` wins and the 5-tuple is ignored. If multiple channels match the non-null filter fields the endpoint returns 400 (ambiguous channel); add more filter fields to narrow to one. If no channel matches the filters the endpoint returns 404.  **Boundary points** (when `withBoundaryPoints=true`, the default): if the first raw data point in the window is at T+Δ (not at the window start T), a linearly interpolated point is inserted at exactly T using the last known point before T. Same logic at the trailing edge. Boundary points carry `interpolated: true`. Boundary points are only added for numeric channel types (Double, Integer); Boolean and String channels never get boundary points.  **No extrapolation**: if no point exists before the window start, no start boundary is added. If the window contains no data at all, `points` is empty.  **Timestamps**: all timestamps in the response are epoch milliseconds (UTC). The `windowStart` and `windowEnd` fields reflect the actual computed window boundaries regardless of whether any data fell in the window.  Auth: Read permission on the container (enforced by `containerAppId` lookup).
     * [v2] Fetch the most recent N seconds of a timeseries channel.
     */
    async getLiveWindow(requestParameters: GetLiveWindowRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LiveWindowResponseIO> {
        const response = await this.getLiveWindowRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
