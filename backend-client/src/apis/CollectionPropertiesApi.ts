/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CollectionProperties,
  PatchCollectionProperties,
} from '../models/index';
import {
    CollectionPropertiesFromJSON,
    CollectionPropertiesToJSON,
    PatchCollectionPropertiesFromJSON,
    PatchCollectionPropertiesToJSON,
} from '../models/index';

export interface GetCollectionPropertiesRequest {
    appId: string;
}

export interface PatchCollectionPropertiesRequest {
    appId: string;
    patchCollectionProperties: PatchCollectionProperties;
}

/**
 * 
 */
export class CollectionPropertiesApi extends runtime.BaseAPI {

    /**
     * Returns the `:CollectionProperties` node attached to the Collection identified by `appId` (UUID v7). The node is created lazily on first access if it does not yet exist — this covers legacy Collections created before CP1b shipped.  Fields in the response include `webdavVisible` (boolean, controls whether the Collection is mounted in the WebDAV tree), `defaultOntologyUri` (the ontology pre-selected in the annotation UI), `uiDefaultsJson` (arbitrary JSON string the frontend stores per-Collection UI preferences in), and `publishToHelmholtzKG` (boolean, opt-in to Helmholtz Knowledge Graph harvest).  Auth: Read permission on the parent Collection. Returns 403 when the caller lacks Read access; 404 when no Collection with that appId exists.  Next step: `PATCH /v2/collections/{appId}/properties` to flip any of the settings (requires Manage permission).
     * [v2] Read the per-Collection settings node.
     */
    async getCollectionPropertiesRaw(requestParameters: GetCollectionPropertiesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CollectionProperties>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling getCollectionProperties().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{appId}/properties`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CollectionPropertiesFromJSON(jsonValue));
    }

    /**
     * Returns the `:CollectionProperties` node attached to the Collection identified by `appId` (UUID v7). The node is created lazily on first access if it does not yet exist — this covers legacy Collections created before CP1b shipped.  Fields in the response include `webdavVisible` (boolean, controls whether the Collection is mounted in the WebDAV tree), `defaultOntologyUri` (the ontology pre-selected in the annotation UI), `uiDefaultsJson` (arbitrary JSON string the frontend stores per-Collection UI preferences in), and `publishToHelmholtzKG` (boolean, opt-in to Helmholtz Knowledge Graph harvest).  Auth: Read permission on the parent Collection. Returns 403 when the caller lacks Read access; 404 when no Collection with that appId exists.  Next step: `PATCH /v2/collections/{appId}/properties` to flip any of the settings (requires Manage permission).
     * [v2] Read the per-Collection settings node.
     */
    async getCollectionProperties(requestParameters: GetCollectionPropertiesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CollectionProperties> {
        const response = await this.getCollectionPropertiesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Applies a partial update to the `:CollectionProperties` node for the Collection identified by `appId` (UUID v7). Every body field is optional; only the fields present in the request are changed — absent fields are left unchanged.  Patchable fields: `webdavVisible` (boolean), `defaultOntologyUri` (string, URI of the ontology to pre-select in the annotation UI), `uiDefaultsJson` (arbitrary JSON string for frontend per-Collection preferences), `publishToHelmholtzKG` (boolean).  Example: enable WebDAV visibility — `{\"webdavVisible\": true}`. Example: set a default ontology — `{\"defaultOntologyUri\": \"https://example.org/onto/v1\"}`. Example: opt into Helmholtz KG publish — `{\"publishToHelmholtzKG\": true}`.  Auth: Manage permission on the parent Collection (stronger than Write). Returns 403 when the caller only has Read or Write access; 404 when no Collection with that appId exists.  Side effects: the `:CollectionProperties` node is created lazily if it does not yet exist before applying the patch. Changes to `publishToHelmholtzKG` take effect on the next Helmholtz KG harvest cycle.
     * [v2] Partial-update the per-Collection settings node.
     */
    async patchCollectionPropertiesRaw(requestParameters: PatchCollectionPropertiesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CollectionProperties>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling patchCollectionProperties().'
            );
        }

        if (requestParameters['patchCollectionProperties'] == null) {
            throw new runtime.RequiredError(
                'patchCollectionProperties',
                'Required parameter "patchCollectionProperties" was null or undefined when calling patchCollectionProperties().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/collections/{appId}/properties`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PatchCollectionPropertiesToJSON(requestParameters['patchCollectionProperties']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CollectionPropertiesFromJSON(jsonValue));
    }

    /**
     * Applies a partial update to the `:CollectionProperties` node for the Collection identified by `appId` (UUID v7). Every body field is optional; only the fields present in the request are changed — absent fields are left unchanged.  Patchable fields: `webdavVisible` (boolean), `defaultOntologyUri` (string, URI of the ontology to pre-select in the annotation UI), `uiDefaultsJson` (arbitrary JSON string for frontend per-Collection preferences), `publishToHelmholtzKG` (boolean).  Example: enable WebDAV visibility — `{\"webdavVisible\": true}`. Example: set a default ontology — `{\"defaultOntologyUri\": \"https://example.org/onto/v1\"}`. Example: opt into Helmholtz KG publish — `{\"publishToHelmholtzKG\": true}`.  Auth: Manage permission on the parent Collection (stronger than Write). Returns 403 when the caller only has Read or Write access; 404 when no Collection with that appId exists.  Side effects: the `:CollectionProperties` node is created lazily if it does not yet exist before applying the patch. Changes to `publishToHelmholtzKG` take effect on the next Helmholtz KG harvest cycle.
     * [v2] Partial-update the per-Collection settings node.
     */
    async patchCollectionProperties(requestParameters: PatchCollectionPropertiesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CollectionProperties> {
        const response = await this.patchCollectionPropertiesRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
