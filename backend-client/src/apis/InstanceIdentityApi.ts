/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  InstanceRorConfigIO,
} from '../models/index';
import {
    InstanceRorConfigIOFromJSON,
    InstanceRorConfigIOToJSON,
} from '../models/index';

/**
 * 
 */
export class InstanceIdentityApi extends runtime.BaseAPI {

    /**
     * Returns only ENABLED plugin IDs — a lightweight capability probe for the frontend so it can gate plugin-specific UI surfaces (e.g. the Unhide Publishing panel). Does not require the instance-admin role — any authenticated user may call it. The full admin surface (all plugin states, PATCH toggle) lives at GET/PATCH /v2/admin/plugins.
     * [v2] List the IDs of plugins that are currently ENABLED on this instance.
     */
    async getCapabilitiesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/instance/capabilities`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Returns only ENABLED plugin IDs — a lightweight capability probe for the frontend so it can gate plugin-specific UI surfaces (e.g. the Unhide Publishing panel). Does not require the instance-admin role — any authenticated user may call it. The full admin surface (all plugin states, PATCH toggle) lives at GET/PATCH /v2/admin/plugins.
     * [v2] List the IDs of plugins that are currently ENABLED on this instance.
     */
    async getCapabilities(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.getCapabilitiesRaw(initOverrides);
    }

    /**
     * Returns the configured ROR id, organisation name, and computed ror.org URL. All three fields are absent when no ROR id has been configured. Public read — any authenticated user; the admin write surface lives at PATCH /v2/admin/config/ror.
     * [v2] Read this instance\'s organisational identity (ROR-based).
     */
    async getIdentityRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InstanceRorConfigIO>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/instance/identity`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InstanceRorConfigIOFromJSON(jsonValue));
    }

    /**
     * Returns the configured ROR id, organisation name, and computed ror.org URL. All three fields are absent when no ROR id has been configured. Public read — any authenticated user; the admin write surface lives at PATCH /v2/admin/config/ror.
     * [v2] Read this instance\'s organisational identity (ROR-based).
     */
    async getIdentity(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InstanceRorConfigIO> {
        const response = await this.getIdentityRaw(initOverrides);
        return await response.value();
    }

}
