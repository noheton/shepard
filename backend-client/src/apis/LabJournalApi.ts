/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  LabJournalRender,
  LabJournalRevision,
  NotebookReference,
} from '../models/index';
import {
    LabJournalRenderFromJSON,
    LabJournalRenderToJSON,
    LabJournalRevisionFromJSON,
    LabJournalRevisionToJSON,
    NotebookReferenceFromJSON,
    NotebookReferenceToJSON,
} from '../models/index';

export interface HistoryRequest {
    entryAppId: string;
}

export interface ListNotebooksRequest {
    dataObjectAppId: string;
}

export interface RenderLabJournalRequest {
    appId: string;
}

/**
 * 
 */
export class LabJournalApi extends runtime.BaseAPI {

    /**
     * Returns an append-only revision list for the entry identified by entryAppId. Each element captures the entry\'s content as it existed immediately before the corresponding update was applied. Revisions are ordered newest first (highest revisionNumber first). An empty array is returned when the entry has never been edited. Permission: Read on the parent DataObject.
     * [v2] List the edit history of a lab journal entry.
     */
    async historyRaw(requestParameters: HistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<LabJournalRevision>>> {
        if (requestParameters['entryAppId'] == null) {
            throw new runtime.RequiredError(
                'entryAppId',
                'Required parameter "entryAppId" was null or undefined when calling history().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/lab-journal/{entryAppId}/history`.replace(`{${"entryAppId"}}`, encodeURIComponent(String(requestParameters['entryAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(LabJournalRevisionFromJSON));
    }

    /**
     * Returns an append-only revision list for the entry identified by entryAppId. Each element captures the entry\'s content as it existed immediately before the corresponding update was applied. Revisions are ordered newest first (highest revisionNumber first). An empty array is returned when the entry has never been edited. Permission: Read on the parent DataObject.
     * [v2] List the edit history of a lab journal entry.
     */
    async history(requestParameters: HistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<LabJournalRevision>> {
        const response = await this.historyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every FileReference (singleton FR1b) and every ShepardFile inside a FileBundleReference (FR1a) whose filename ends with .ipynb (case-insensitive). The result is ordered: singletons first (by their createdAt ascending), then bundle files (by bundle createdAt ascending, then by filename). Returns an empty array when no .ipynb files are attached. Permission: Read on the parent DataObject.
     * [v2] List .ipynb file references attached to a DataObject.
     */
    async listNotebooksRaw(requestParameters: ListNotebooksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<NotebookReference>>> {
        if (requestParameters['dataObjectAppId'] == null) {
            throw new runtime.RequiredError(
                'dataObjectAppId',
                'Required parameter "dataObjectAppId" was null or undefined when calling listNotebooks().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/lab-journal/{dataObjectAppId}/notebooks`.replace(`{${"dataObjectAppId"}}`, encodeURIComponent(String(requestParameters['dataObjectAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(NotebookReferenceFromJSON));
    }

    /**
     * Returns every FileReference (singleton FR1b) and every ShepardFile inside a FileBundleReference (FR1a) whose filename ends with .ipynb (case-insensitive). The result is ordered: singletons first (by their createdAt ascending), then bundle files (by bundle createdAt ascending, then by filename). Returns an empty array when no .ipynb files are attached. Permission: Read on the parent DataObject.
     * [v2] List .ipynb file references attached to a DataObject.
     */
    async listNotebooks(requestParameters: ListNotebooksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<NotebookReference>> {
        const response = await this.listNotebooksRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Parses the entry\'s journalContent field as CommonMark + GFM markdown (tables, strikethrough, task-list items) and returns sanitised HTML. javascript: hrefs are stripped (sanitizeUrls=true). Plain-text entries render as <p> elements — no migration required. Content negotiation: Accept: text/html returns the raw HTML string; Accept: application/json (or default) returns {html, sourceLength}. Permission: Read on the parent DataObject.
     * [v2] Render a lab journal entry as sanitised HTML.
     */
    async renderLabJournalRaw(requestParameters: RenderLabJournalRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling renderLabJournal().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/lab-journal/{appId}/render`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Parses the entry\'s journalContent field as CommonMark + GFM markdown (tables, strikethrough, task-list items) and returns sanitised HTML. javascript: hrefs are stripped (sanitizeUrls=true). Plain-text entries render as <p> elements — no migration required. Content negotiation: Accept: text/html returns the raw HTML string; Accept: application/json (or default) returns {html, sourceLength}. Permission: Read on the parent DataObject.
     * [v2] Render a lab journal entry as sanitised HTML.
     */
    async renderLabJournal(requestParameters: RenderLabJournalRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string> {
        const response = await this.renderLabJournalRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
