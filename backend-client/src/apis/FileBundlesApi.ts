/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CreateFileGroup,
  FileBundleReference,
  FileGroup,
  JsonNode,
  PagedFiles,
  ShepardFile,
} from '../models/index';
import {
    CreateFileGroupFromJSON,
    CreateFileGroupToJSON,
    FileBundleReferenceFromJSON,
    FileBundleReferenceToJSON,
    FileGroupFromJSON,
    FileGroupToJSON,
    JsonNodeFromJSON,
    JsonNodeToJSON,
    PagedFilesFromJSON,
    PagedFilesToJSON,
    ShepardFileFromJSON,
    ShepardFileToJSON,
} from '../models/index';

export interface CreateGroupRequest {
    bundleAppId: string;
    createFileGroup: CreateFileGroup;
}

export interface DeleteGroupRequest {
    bundleAppId: string;
    groupAppId: string;
    force?: boolean;
}

export interface GetBundleRequest {
    bundleAppId: string;
}

export interface GetGroupRequest {
    bundleAppId: string;
    groupAppId: string;
}

export interface ListGroupFilesRequest {
    bundleAppId: string;
    groupAppId: string;
    page?: number;
    size?: number;
}

export interface ListGroupsRequest {
    bundleAppId: string;
}

export interface PatchGroupRequest {
    bundleAppId: string;
    groupAppId: string;
    jsonNode: JsonNode;
}

export interface UploadFileIntoGroupRequest {
    bundleAppId: string;
    groupAppId: string;
    file?: Blob;
}

/**
 * 
 */
export class FileBundlesApi extends runtime.BaseAPI {

    /**
     * Creates a `:FileGroup` node and links it to the `:FileBundleReference` identified by `bundleAppId` (UUID v7). The server mints `appId` (UUID v7) for the new group.  Body fields (`CreateFileGroupIO`): `name` (string, required, non-blank — display name of the group), `description` (string, optional), `attributes` (string-to-string map, optional), `startedAt` (ISO-8601 timestamp, optional — start of the experiment period this group covers), `endedAt` (ISO-8601 timestamp, optional), `index` (integer, optional — display order; if omitted, the group is appended after the last existing group).  Example minimal body: `{\"name\": \"run-001\"}`. Example full body: `{\"name\": \"run-001\", \"description\": \"hot-fire sequence\", \"startedAt\": \"2026-05-01T10:00:00Z\", \"endedAt\": \"2026-05-01T10:30:00Z\", \"index\": 0}`.  Auth: Write permission on the parent DataObject (inherited from its Collection).  Next step: `POST /v2/bundles/{bundleAppId}/groups/{groupAppId}/files` to upload files into the new group.
     * [v2] Create a new FileGroup under a bundle.
     */
    async createGroupRaw(requestParameters: CreateGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FileGroup>> {
        if (requestParameters['bundleAppId'] == null) {
            throw new runtime.RequiredError(
                'bundleAppId',
                'Required parameter "bundleAppId" was null or undefined when calling createGroup().'
            );
        }

        if (requestParameters['createFileGroup'] == null) {
            throw new runtime.RequiredError(
                'createFileGroup',
                'Required parameter "createFileGroup" was null or undefined when calling createGroup().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/bundles/{bundleAppId}/groups`.replace(`{${"bundleAppId"}}`, encodeURIComponent(String(requestParameters['bundleAppId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateFileGroupToJSON(requestParameters['createFileGroup']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FileGroupFromJSON(jsonValue));
    }

    /**
     * Creates a `:FileGroup` node and links it to the `:FileBundleReference` identified by `bundleAppId` (UUID v7). The server mints `appId` (UUID v7) for the new group.  Body fields (`CreateFileGroupIO`): `name` (string, required, non-blank — display name of the group), `description` (string, optional), `attributes` (string-to-string map, optional), `startedAt` (ISO-8601 timestamp, optional — start of the experiment period this group covers), `endedAt` (ISO-8601 timestamp, optional), `index` (integer, optional — display order; if omitted, the group is appended after the last existing group).  Example minimal body: `{\"name\": \"run-001\"}`. Example full body: `{\"name\": \"run-001\", \"description\": \"hot-fire sequence\", \"startedAt\": \"2026-05-01T10:00:00Z\", \"endedAt\": \"2026-05-01T10:30:00Z\", \"index\": 0}`.  Auth: Write permission on the parent DataObject (inherited from its Collection).  Next step: `POST /v2/bundles/{bundleAppId}/groups/{groupAppId}/files` to upload files into the new group.
     * [v2] Create a new FileGroup under a bundle.
     */
    async createGroup(requestParameters: CreateGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FileGroup> {
        const response = await this.createGroupRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes the `:FileGroup` identified by `groupAppId` from the `:FileBundleReference` identified by `bundleAppId`. Two safety guards prevent accidental data loss:    1. If the group has files attached and `?force=true` is not passed, the call returns 400 with a message explaining the conflict. Pass `?force=true` to delete the group and permanently remove all its files from the FileContainer (bytes in storage are deleted).   2. If the group is the last remaining group in the bundle, the call returns 400 regardless of `force` — a bundle must always have at least one group, so removing the last one is never allowed.  Auth: Write permission on the parent DataObject (inherited from its Collection).  Side effects when `force=true`: files stored in the backing GridFS / S3 container are permanently deleted. This operation is not reversible.  Note: deleting a group that does not belong to the stated bundle returns 404 (not 400), to prevent cross-bundle leakage of group existence information.
     * [v2] Delete a FileGroup from a bundle.
     */
    async deleteGroupRaw(requestParameters: DeleteGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['bundleAppId'] == null) {
            throw new runtime.RequiredError(
                'bundleAppId',
                'Required parameter "bundleAppId" was null or undefined when calling deleteGroup().'
            );
        }

        if (requestParameters['groupAppId'] == null) {
            throw new runtime.RequiredError(
                'groupAppId',
                'Required parameter "groupAppId" was null or undefined when calling deleteGroup().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['force'] != null) {
            queryParameters['force'] = requestParameters['force'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/bundles/{bundleAppId}/groups/{groupAppId}`.replace(`{${"bundleAppId"}}`, encodeURIComponent(String(requestParameters['bundleAppId']))).replace(`{${"groupAppId"}}`, encodeURIComponent(String(requestParameters['groupAppId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes the `:FileGroup` identified by `groupAppId` from the `:FileBundleReference` identified by `bundleAppId`. Two safety guards prevent accidental data loss:    1. If the group has files attached and `?force=true` is not passed, the call returns 400 with a message explaining the conflict. Pass `?force=true` to delete the group and permanently remove all its files from the FileContainer (bytes in storage are deleted).   2. If the group is the last remaining group in the bundle, the call returns 400 regardless of `force` — a bundle must always have at least one group, so removing the last one is never allowed.  Auth: Write permission on the parent DataObject (inherited from its Collection).  Side effects when `force=true`: files stored in the backing GridFS / S3 container are permanently deleted. This operation is not reversible.  Note: deleting a group that does not belong to the stated bundle returns 404 (not 400), to prevent cross-bundle leakage of group existence information.
     * [v2] Delete a FileGroup from a bundle.
     */
    async deleteGroup(requestParameters: DeleteGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteGroupRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the full `FileBundleReferenceIO` for the `:FileBundleReference` identified by `bundleAppId` (UUID v7), including the embedded list of `:FileGroup` nodes ordered by `index` ascending.  The `FileBundleReferenceIO` shape includes `appId`, `name`, `description`, `attributes` (string-to-string map), `parentDataObjectAppId`, and `groups[]` — each group carries `appId`, `name`, `description`, `attributes`, `startedAt`, `endedAt`, `index`, and `files[]` (the list of `ShepardFile` records in that group).  Auth: Read permission on the parent DataObject (inherited from its Collection). The permission check walks from the bundle\'s graph relationship to the DataObject and then up to the Collection.  Next step: `GET /v2/bundles/{bundleAppId}/groups` for a groups-only view, or `POST /v2/bundles/{bundleAppId}/groups/{groupAppId}/files` to upload a file.
     * [v2] Get a FileBundleReference with its FileGroups by appId.
     */
    async getBundleRaw(requestParameters: GetBundleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FileBundleReference>> {
        if (requestParameters['bundleAppId'] == null) {
            throw new runtime.RequiredError(
                'bundleAppId',
                'Required parameter "bundleAppId" was null or undefined when calling getBundle().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/bundles/{bundleAppId}`.replace(`{${"bundleAppId"}}`, encodeURIComponent(String(requestParameters['bundleAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FileBundleReferenceFromJSON(jsonValue));
    }

    /**
     * Returns the full `FileBundleReferenceIO` for the `:FileBundleReference` identified by `bundleAppId` (UUID v7), including the embedded list of `:FileGroup` nodes ordered by `index` ascending.  The `FileBundleReferenceIO` shape includes `appId`, `name`, `description`, `attributes` (string-to-string map), `parentDataObjectAppId`, and `groups[]` — each group carries `appId`, `name`, `description`, `attributes`, `startedAt`, `endedAt`, `index`, and `files[]` (the list of `ShepardFile` records in that group).  Auth: Read permission on the parent DataObject (inherited from its Collection). The permission check walks from the bundle\'s graph relationship to the DataObject and then up to the Collection.  Next step: `GET /v2/bundles/{bundleAppId}/groups` for a groups-only view, or `POST /v2/bundles/{bundleAppId}/groups/{groupAppId}/files` to upload a file.
     * [v2] Get a FileBundleReference with its FileGroups by appId.
     */
    async getBundle(requestParameters: GetBundleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FileBundleReference> {
        const response = await this.getBundleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the `FileGroupIO` for the `:FileGroup` identified by `groupAppId` (UUID v7) within the `:FileBundleReference` identified by `bundleAppId`. The response includes the group\'s `appId`, `name`, `description`, `attributes`, `startedAt`, `endedAt`, `index`, and `files[]` — the list of `ShepardFile` records attached to this group via the FileContainer.  Both path parameters are required; 404 is returned if the bundle is unknown, if the group is unknown, or if the group does not belong to the stated bundle (prevents cross-bundle access).  Auth: Read permission on the parent DataObject (inherited from its Collection).  Next step: `PATCH /v2/bundles/{bundleAppId}/groups/{groupAppId}` to update the group metadata, or `POST .../files` to upload a file into the group.
     * [v2] Get a single FileGroup with its files.
     */
    async getGroupRaw(requestParameters: GetGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FileGroup>> {
        if (requestParameters['bundleAppId'] == null) {
            throw new runtime.RequiredError(
                'bundleAppId',
                'Required parameter "bundleAppId" was null or undefined when calling getGroup().'
            );
        }

        if (requestParameters['groupAppId'] == null) {
            throw new runtime.RequiredError(
                'groupAppId',
                'Required parameter "groupAppId" was null or undefined when calling getGroup().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/bundles/{bundleAppId}/groups/{groupAppId}`.replace(`{${"bundleAppId"}}`, encodeURIComponent(String(requestParameters['bundleAppId']))).replace(`{${"groupAppId"}}`, encodeURIComponent(String(requestParameters['groupAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FileGroupFromJSON(jsonValue));
    }

    /**
     * Returns the `FileGroupIO` for the `:FileGroup` identified by `groupAppId` (UUID v7) within the `:FileBundleReference` identified by `bundleAppId`. The response includes the group\'s `appId`, `name`, `description`, `attributes`, `startedAt`, `endedAt`, `index`, and `files[]` — the list of `ShepardFile` records attached to this group via the FileContainer.  Both path parameters are required; 404 is returned if the bundle is unknown, if the group is unknown, or if the group does not belong to the stated bundle (prevents cross-bundle access).  Auth: Read permission on the parent DataObject (inherited from its Collection).  Next step: `PATCH /v2/bundles/{bundleAppId}/groups/{groupAppId}` to update the group metadata, or `POST .../files` to upload a file into the group.
     * [v2] Get a single FileGroup with its files.
     */
    async getGroup(requestParameters: GetGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FileGroup> {
        const response = await this.getGroupRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the files attached to the `:FileGroup` identified by `groupAppId` within the `:FileBundleReference` identified by `bundleAppId`, paginated.  Designed for high-cardinality ImageBundles (e.g. MFFD TPS raw-data PNG frames). The `GET /v2/bundles/{bundleAppId}/groups/{groupAppId}` route still returns the group\'s full embedded `files[]` for backward compatibility, but UI surfaces that scrub through thousands of frames should consume this paginated route instead.  Query parameters: * `page` — 0-based page index. Default `0`. Values past the last page return an empty `items[]` with the correct `totalElements`/`totalPages`. * `size` — page size. Default `200`; min `1`; max `1000`. Values outside the range are clamped, never reject — the API gives a useful result even when a client supplies an out-of-policy hint.  Auth: Read permission on the parent DataObject (inherited from its Collection).
     * [v2] List files in a FileGroup, paginated (MFFD-IMAGEBUNDLE-PAGINATE-1).
     */
    async listGroupFilesRaw(requestParameters: ListGroupFilesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedFiles>> {
        if (requestParameters['bundleAppId'] == null) {
            throw new runtime.RequiredError(
                'bundleAppId',
                'Required parameter "bundleAppId" was null or undefined when calling listGroupFiles().'
            );
        }

        if (requestParameters['groupAppId'] == null) {
            throw new runtime.RequiredError(
                'groupAppId',
                'Required parameter "groupAppId" was null or undefined when calling listGroupFiles().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/bundles/{bundleAppId}/groups/{groupAppId}/files`.replace(`{${"bundleAppId"}}`, encodeURIComponent(String(requestParameters['bundleAppId']))).replace(`{${"groupAppId"}}`, encodeURIComponent(String(requestParameters['groupAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedFilesFromJSON(jsonValue));
    }

    /**
     * Returns the files attached to the `:FileGroup` identified by `groupAppId` within the `:FileBundleReference` identified by `bundleAppId`, paginated.  Designed for high-cardinality ImageBundles (e.g. MFFD TPS raw-data PNG frames). The `GET /v2/bundles/{bundleAppId}/groups/{groupAppId}` route still returns the group\'s full embedded `files[]` for backward compatibility, but UI surfaces that scrub through thousands of frames should consume this paginated route instead.  Query parameters: * `page` — 0-based page index. Default `0`. Values past the last page return an empty `items[]` with the correct `totalElements`/`totalPages`. * `size` — page size. Default `200`; min `1`; max `1000`. Values outside the range are clamped, never reject — the API gives a useful result even when a client supplies an out-of-policy hint.  Auth: Read permission on the parent DataObject (inherited from its Collection).
     * [v2] List files in a FileGroup, paginated (MFFD-IMAGEBUNDLE-PAGINATE-1).
     */
    async listGroupFiles(requestParameters: ListGroupFilesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedFiles> {
        const response = await this.listGroupFilesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the ordered list of `:FileGroup` nodes belonging to the `:FileBundleReference` identified by `bundleAppId` (UUID v7). Groups are sorted by their `index` field ascending (lowest index first), which matches the display order in the UI.  Each `FileGroupIO` includes `appId`, `name`, `description`, `attributes`, `startedAt`, `endedAt`, `index`, and `files[]` (the `ShepardFile` records attached to that group via the FileContainer).  Auth: Read permission on the parent DataObject (inherited from its Collection).  Next step: `GET /v2/bundles/{bundleAppId}/groups/{groupAppId}` for a single group, or `POST /v2/bundles/{bundleAppId}/groups` to add a new group.
     * [v2] List FileGroups under a bundle, ordered by ascending index.
     */
    async listGroupsRaw(requestParameters: ListGroupsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<FileGroup>>> {
        if (requestParameters['bundleAppId'] == null) {
            throw new runtime.RequiredError(
                'bundleAppId',
                'Required parameter "bundleAppId" was null or undefined when calling listGroups().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/bundles/{bundleAppId}/groups`.replace(`{${"bundleAppId"}}`, encodeURIComponent(String(requestParameters['bundleAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(FileGroupFromJSON));
    }

    /**
     * Returns the ordered list of `:FileGroup` nodes belonging to the `:FileBundleReference` identified by `bundleAppId` (UUID v7). Groups are sorted by their `index` field ascending (lowest index first), which matches the display order in the UI.  Each `FileGroupIO` includes `appId`, `name`, `description`, `attributes`, `startedAt`, `endedAt`, `index`, and `files[]` (the `ShepardFile` records attached to that group via the FileContainer).  Auth: Read permission on the parent DataObject (inherited from its Collection).  Next step: `GET /v2/bundles/{bundleAppId}/groups/{groupAppId}` for a single group, or `POST /v2/bundles/{bundleAppId}/groups` to add a new group.
     * [v2] List FileGroups under a bundle, ordered by ascending index.
     */
    async listGroups(requestParameters: ListGroupsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<FileGroup>> {
        const response = await this.listGroupsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Applies a partial update to the `:FileGroup` identified by `groupAppId` within the `:FileBundleReference` identified by `bundleAppId`.  Patchable fields: `name` (string, non-blank required if present), `description` (string, nullable — explicit JSON `null` clears it), `attributes` (string-to-string map, nullable), `startedAt` (ISO-8601 timestamp, nullable), `endedAt` (ISO-8601 timestamp, nullable), `index` (integer — repositions the group in the display order; other groups\' indices are not automatically adjusted).  Fields absent from the body are left unchanged; explicit JSON `null` clears a nullable field. Setting `name` to `null` returns 400.  Example: rename and set experiment window — `{\"name\": \"run-001-relabelled\", \"startedAt\": \"2026-05-01T10:00:00Z\", \"endedAt\": \"2026-05-01T10:30:00Z\"}`.  Content-Type: prefer `application/merge-patch+json`; `application/json` also accepted.  Auth: Write permission on the parent DataObject (inherited from its Collection).
     * [v2] RFC 7396 merge-patch on a FileGroup.
     */
    async patchGroupRaw(requestParameters: PatchGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FileGroup>> {
        if (requestParameters['bundleAppId'] == null) {
            throw new runtime.RequiredError(
                'bundleAppId',
                'Required parameter "bundleAppId" was null or undefined when calling patchGroup().'
            );
        }

        if (requestParameters['groupAppId'] == null) {
            throw new runtime.RequiredError(
                'groupAppId',
                'Required parameter "groupAppId" was null or undefined when calling patchGroup().'
            );
        }

        if (requestParameters['jsonNode'] == null) {
            throw new runtime.RequiredError(
                'jsonNode',
                'Required parameter "jsonNode" was null or undefined when calling patchGroup().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/merge-patch+json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/bundles/{bundleAppId}/groups/{groupAppId}`.replace(`{${"bundleAppId"}}`, encodeURIComponent(String(requestParameters['bundleAppId']))).replace(`{${"groupAppId"}}`, encodeURIComponent(String(requestParameters['groupAppId']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: JsonNodeToJSON(requestParameters['jsonNode']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FileGroupFromJSON(jsonValue));
    }

    /**
     * Applies a partial update to the `:FileGroup` identified by `groupAppId` within the `:FileBundleReference` identified by `bundleAppId`.  Patchable fields: `name` (string, non-blank required if present), `description` (string, nullable — explicit JSON `null` clears it), `attributes` (string-to-string map, nullable), `startedAt` (ISO-8601 timestamp, nullable), `endedAt` (ISO-8601 timestamp, nullable), `index` (integer — repositions the group in the display order; other groups\' indices are not automatically adjusted).  Fields absent from the body are left unchanged; explicit JSON `null` clears a nullable field. Setting `name` to `null` returns 400.  Example: rename and set experiment window — `{\"name\": \"run-001-relabelled\", \"startedAt\": \"2026-05-01T10:00:00Z\", \"endedAt\": \"2026-05-01T10:30:00Z\"}`.  Content-Type: prefer `application/merge-patch+json`; `application/json` also accepted.  Auth: Write permission on the parent DataObject (inherited from its Collection).
     * [v2] RFC 7396 merge-patch on a FileGroup.
     */
    async patchGroup(requestParameters: PatchGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FileGroup> {
        const response = await this.patchGroupRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Accepts a `multipart/form-data` body with a single `file` part and stores the bytes in the `FileContainer` backing the `:FileBundleReference` identified by `bundleAppId`. The stored `ShepardFile` is then linked to the `:FileGroup` identified by `groupAppId`.  Form field: `file` (required, binary) — the file bytes. The original filename from the `Content-Disposition` header of the form part is preserved as the `ShepardFile.name`.  The endpoint returns 400 when the bundle has no underlying `FileContainer` (i.e. the bundle was created by a legacy code path that didn\'t initialise a container; in that case contact an admin to run the backfill).  Auth: Write permission on the parent DataObject (inherited from its Collection). Both `bundleAppId` and `groupAppId` must resolve, and the group must belong to the bundle; otherwise 404 is returned.  Side effects: bytes are stored in the active storage backend (GridFS or S3). The `ShepardFile` node is linked to the `FileGroup` in Neo4j. A `PayloadVersion` record is created for the file (PV1a).  Next step: `GET /v2/bundles/{bundleAppId}/groups/{groupAppId}` to confirm the file appears in the group\'s `files[]` list.
     * [v2] Upload one file into a specific FileGroup.
     */
    async uploadFileIntoGroupRaw(requestParameters: UploadFileIntoGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShepardFile>> {
        if (requestParameters['bundleAppId'] == null) {
            throw new runtime.RequiredError(
                'bundleAppId',
                'Required parameter "bundleAppId" was null or undefined when calling uploadFileIntoGroup().'
            );
        }

        if (requestParameters['groupAppId'] == null) {
            throw new runtime.RequiredError(
                'groupAppId',
                'Required parameter "groupAppId" was null or undefined when calling uploadFileIntoGroup().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        // use FormData to transmit files using content-type "multipart/form-data"
        useForm = canConsumeForm;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['file'] != null) {
            formParams.append('file', requestParameters['file'] as any);
        }

        const response = await this.request({
            path: `/v2/bundles/{bundleAppId}/groups/{groupAppId}/files`.replace(`{${"bundleAppId"}}`, encodeURIComponent(String(requestParameters['bundleAppId']))).replace(`{${"groupAppId"}}`, encodeURIComponent(String(requestParameters['groupAppId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShepardFileFromJSON(jsonValue));
    }

    /**
     * Accepts a `multipart/form-data` body with a single `file` part and stores the bytes in the `FileContainer` backing the `:FileBundleReference` identified by `bundleAppId`. The stored `ShepardFile` is then linked to the `:FileGroup` identified by `groupAppId`.  Form field: `file` (required, binary) — the file bytes. The original filename from the `Content-Disposition` header of the form part is preserved as the `ShepardFile.name`.  The endpoint returns 400 when the bundle has no underlying `FileContainer` (i.e. the bundle was created by a legacy code path that didn\'t initialise a container; in that case contact an admin to run the backfill).  Auth: Write permission on the parent DataObject (inherited from its Collection). Both `bundleAppId` and `groupAppId` must resolve, and the group must belong to the bundle; otherwise 404 is returned.  Side effects: bytes are stored in the active storage backend (GridFS or S3). The `ShepardFile` node is linked to the `FileGroup` in Neo4j. A `PayloadVersion` record is created for the file (PV1a).  Next step: `GET /v2/bundles/{bundleAppId}/groups/{groupAppId}` to confirm the file appears in the group\'s `files[]` list.
     * [v2] Upload one file into a specific FileGroup.
     */
    async uploadFileIntoGroup(requestParameters: UploadFileIntoGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShepardFile> {
        const response = await this.uploadFileIntoGroupRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
