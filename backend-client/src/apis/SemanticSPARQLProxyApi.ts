/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';

export interface QueryGetRequest {
    repoAppId: string;
    query: string;
}

export interface QueryPostRequest {
    repoAppId: string;
    query?: string;
}

/**
 * 
 */
export class SemanticSPARQLProxyApi extends runtime.BaseAPI {

    /**
     * Accepts a SPARQL query via the `query` URL parameter and proxies it to the backend identified by `repoAppId`, following SPARQL 1.1 Protocol §2.1.2.  Only `SELECT` and `ASK` query forms are permitted. Any mutation form (`CONSTRUCT`, `INSERT`, `DELETE`, `UPDATE`, …) is rejected with 400 before reaching the backend.  Repository-type dispatch:   - `INTERNAL` — proxies to the n10s (neosemantics) HTTP SPARQL endpoint on     the Neo4j HTTP port (default `http://localhost:7474`). Configurable via     `shepard.semantic.internal.http-url`.   - `SPARQL` — proxies to the external endpoint URL stored on the entity.     The caller\'s `Authorization` header is stripped before forwarding to     prevent credential leakage.   - `JSKOS` / `SKOSMOS` — returns 501; these repository types do not     implement the SPARQL protocol.  Auth: any authenticated shepard user may query any SemanticRepository. There is no per-entity permission check beyond authentication, matching the posture of the v1 SemanticRepository catalogue.  Response: W3C SPARQL Results JSON (`application/sparql-results+json`) passed through verbatim from the backend. Non-200 backend responses are mapped to 502 (backend error) or 503 (unreachable).
     * [v2] Execute a read-only SPARQL SELECT or ASK query (GET form).
     */
    async queryGetRaw(requestParameters: QueryGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['repoAppId'] == null) {
            throw new runtime.RequiredError(
                'repoAppId',
                'Required parameter "repoAppId" was null or undefined when calling queryGet().'
            );
        }

        if (requestParameters['query'] == null) {
            throw new runtime.RequiredError(
                'query',
                'Required parameter "query" was null or undefined when calling queryGet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['query'] != null) {
            queryParameters['query'] = requestParameters['query'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/semantic/{repoAppId}/sparql`.replace(`{${"repoAppId"}}`, encodeURIComponent(String(requestParameters['repoAppId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Accepts a SPARQL query via the `query` URL parameter and proxies it to the backend identified by `repoAppId`, following SPARQL 1.1 Protocol §2.1.2.  Only `SELECT` and `ASK` query forms are permitted. Any mutation form (`CONSTRUCT`, `INSERT`, `DELETE`, `UPDATE`, …) is rejected with 400 before reaching the backend.  Repository-type dispatch:   - `INTERNAL` — proxies to the n10s (neosemantics) HTTP SPARQL endpoint on     the Neo4j HTTP port (default `http://localhost:7474`). Configurable via     `shepard.semantic.internal.http-url`.   - `SPARQL` — proxies to the external endpoint URL stored on the entity.     The caller\'s `Authorization` header is stripped before forwarding to     prevent credential leakage.   - `JSKOS` / `SKOSMOS` — returns 501; these repository types do not     implement the SPARQL protocol.  Auth: any authenticated shepard user may query any SemanticRepository. There is no per-entity permission check beyond authentication, matching the posture of the v1 SemanticRepository catalogue.  Response: W3C SPARQL Results JSON (`application/sparql-results+json`) passed through verbatim from the backend. Non-200 backend responses are mapped to 502 (backend error) or 503 (unreachable).
     * [v2] Execute a read-only SPARQL SELECT or ASK query (GET form).
     */
    async queryGet(requestParameters: QueryGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.queryGetRaw(requestParameters, initOverrides);
    }

    /**
     * Identical semantics to the GET variant, but accepts the query as a URL-encoded form body (`Content-Type: application/x-www-form-urlencoded`, field name `query`), following SPARQL 1.1 Protocol §2.1.3.  Use this variant when the SPARQL query is too long to fit in a URL — many HTTP proxies and load balancers reject query strings longer than 2 KB. POST form bodies carry no such limit.  All constraints and error responses are identical to the GET form: only `SELECT` and `ASK` are permitted; auth, repository-type dispatch, and upstream error mapping behave the same way.
     * [v2] Execute a read-only SPARQL SELECT or ASK query (POST form variant).
     */
    async queryPostRaw(requestParameters: QueryPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['repoAppId'] == null) {
            throw new runtime.RequiredError(
                'repoAppId',
                'Required parameter "repoAppId" was null or undefined when calling queryPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const consumes: runtime.Consume[] = [
            { contentType: 'application/x-www-form-urlencoded' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['query'] != null) {
            formParams.append('query', requestParameters['query'] as any);
        }

        const response = await this.request({
            path: `/v2/semantic/{repoAppId}/sparql`.replace(`{${"repoAppId"}}`, encodeURIComponent(String(requestParameters['repoAppId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Identical semantics to the GET variant, but accepts the query as a URL-encoded form body (`Content-Type: application/x-www-form-urlencoded`, field name `query`), following SPARQL 1.1 Protocol §2.1.3.  Use this variant when the SPARQL query is too long to fit in a URL — many HTTP proxies and load balancers reject query strings longer than 2 KB. POST form bodies carry no such limit.  All constraints and error responses are identical to the GET form: only `SELECT` and `ASK` are permitted; auth, repository-type dispatch, and upstream error mapping behave the same way.
     * [v2] Execute a read-only SPARQL SELECT or ASK query (POST form variant).
     */
    async queryPost(requestParameters: QueryPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.queryPostRaw(requestParameters, initOverrides);
    }

}
