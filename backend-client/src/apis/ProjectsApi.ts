/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PagedResponse,
  ProjectByAnnotationIO,
  ProjectIO,
  SubCollectionsIO,
} from '../models/index';
import {
    PagedResponseFromJSON,
    PagedResponseToJSON,
    ProjectByAnnotationIOFromJSON,
    ProjectByAnnotationIOToJSON,
    ProjectIOFromJSON,
    ProjectIOToJSON,
    SubCollectionsIOFromJSON,
    SubCollectionsIOToJSON,
} from '../models/index';

export interface ByAnnotationRequest {
    appId: string;
    predicate: string;
    value: string;
    include?: string;
    inherit?: boolean;
    page?: number;
    pageSize?: number;
}

export interface GetProjectRequest {
    appId: string;
}

export interface ListProjectsRequest {
    page?: number;
    pageSize?: number;
}

export interface SubCollectionsRequest {
    appId: string;
}

/**
 * 
 */
export class ProjectsApi extends runtime.BaseAPI {

    /**
     * Returns every DataObject across the Project\'s sub-Collections whose direct semantic annotation matches `{predicate} = {value}`. The `predicate` path parameter is the predicate IRI (URL-encoded — e.g. `urn%3Ashepard%3Amffd%3Alayer`); the `value` parameter is the object literal or IRI to match.  Pagination via `page` + `pageSize` (max 500). `include=annotations` populates the per-row `matchedAnnotations` array with the matched predicate+value+source rows (current implementation reports the direct match only; full parent-walk lands as a follow-up).  404 when the appId is not a Project. 422 when the predicate is blank.
     * [v2] Cross-Collection by-annotation roll-up over a Project.
     */
    async byAnnotationRaw(requestParameters: ByAnnotationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectByAnnotationIO>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling byAnnotation().'
            );
        }

        if (requestParameters['predicate'] == null) {
            throw new runtime.RequiredError(
                'predicate',
                'Required parameter "predicate" was null or undefined when calling byAnnotation().'
            );
        }

        if (requestParameters['value'] == null) {
            throw new runtime.RequiredError(
                'value',
                'Required parameter "value" was null or undefined when calling byAnnotation().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['include'] != null) {
            queryParameters['include'] = requestParameters['include'];
        }

        if (requestParameters['inherit'] != null) {
            queryParameters['inherit'] = requestParameters['inherit'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/projects/{appId}/by-annotation/{predicate}/{value}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))).replace(`{${"predicate"}}`, encodeURIComponent(String(requestParameters['predicate']))).replace(`{${"value"}}`, encodeURIComponent(String(requestParameters['value']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProjectByAnnotationIOFromJSON(jsonValue));
    }

    /**
     * Returns every DataObject across the Project\'s sub-Collections whose direct semantic annotation matches `{predicate} = {value}`. The `predicate` path parameter is the predicate IRI (URL-encoded — e.g. `urn%3Ashepard%3Amffd%3Alayer`); the `value` parameter is the object literal or IRI to match.  Pagination via `page` + `pageSize` (max 500). `include=annotations` populates the per-row `matchedAnnotations` array with the matched predicate+value+source rows (current implementation reports the direct match only; full parent-walk lands as a follow-up).  404 when the appId is not a Project. 422 when the predicate is blank.
     * [v2] Cross-Collection by-annotation roll-up over a Project.
     */
    async byAnnotation(requestParameters: ByAnnotationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectByAnnotationIO> {
        const response = await this.byAnnotationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the Project-flavoured envelope: the underlying Collection\'s name + description + ownerGroup, plus the Project-only fields (`programmes`, `subCollectionCount`, `aggregateDoCount`, `lastActivityMillis`).  404 when the appId does not resolve to a Project — i.e. either the appId is unknown OR the Collection at that appId does not carry `urn:shepard:project = \"true\"`.
     * [v2] Get a Project envelope by appId.
     */
    async getProjectRaw(requestParameters: GetProjectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectIO>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling getProject().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/projects/{appId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProjectIOFromJSON(jsonValue));
    }

    /**
     * Returns the Project-flavoured envelope: the underlying Collection\'s name + description + ownerGroup, plus the Project-only fields (`programmes`, `subCollectionCount`, `aggregateDoCount`, `lastActivityMillis`).  404 when the appId does not resolve to a Project — i.e. either the appId is unknown OR the Collection at that appId does not carry `urn:shepard:project = \"true\"`.
     * [v2] Get a Project envelope by appId.
     */
    async getProject(requestParameters: GetProjectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectIO> {
        const response = await this.getProjectRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the appIds of every Collection that carries `urn:shepard:project = \"true\"`. Returned in name-order. The list is always-visible — the frontend `/projects` route uses this to render its tile grid.  Pagination (APISIMP-PROJECTS-LIST-NO-PAGINATION): supply both `page` (0-based) and `pageSize` (1–200) to receive a slice. Omit both to return all projects. `X-Total-Count` header carries the total before paging.  For each appId in the response, follow up with `GET /v2/projects/{appId}` for the Project envelope (with programmes and aggregate counts).
     * [v2] List Project appIds.
     */
    async listProjectsRaw(requestParameters: ListProjectsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponse>> {
        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/projects`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseFromJSON(jsonValue));
    }

    /**
     * Returns the appIds of every Collection that carries `urn:shepard:project = \"true\"`. Returned in name-order. The list is always-visible — the frontend `/projects` route uses this to render its tile grid.  Pagination (APISIMP-PROJECTS-LIST-NO-PAGINATION): supply both `page` (0-based) and `pageSize` (1–200) to receive a slice. Omit both to return all projects. `X-Total-Count` header carries the total before paging.  For each appId in the response, follow up with `GET /v2/projects/{appId}` for the Project envelope (with programmes and aggregate counts).
     * [v2] List Project appIds.
     */
    async listProjects(requestParameters: ListProjectsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponse> {
        const response = await this.listProjectsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every Collection that declares this Project as a parent via `urn:shepard:partOf`. Each row carries the child\'s appId + name + DataObject count + lastActivity + ownerGroup; when the child belongs to other Projects too (multi-`partOf`), those appIds land in `alsoMemberOf`.  The envelope also carries the parent Project\'s `programmes` list so the UI can render the programme strip and the tile grid in one request.
     * [v2] List a Project\'s sub-Collections.
     */
    async subCollectionsRaw(requestParameters: SubCollectionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubCollectionsIO>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling subCollections().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/projects/{appId}/sub-collections`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubCollectionsIOFromJSON(jsonValue));
    }

    /**
     * Returns every Collection that declares this Project as a parent via `urn:shepard:partOf`. Each row carries the child\'s appId + name + DataObject count + lastActivity + ownerGroup; when the child belongs to other Projects too (multi-`partOf`), those appIds land in `alsoMemberOf`.  The envelope also carries the parent Project\'s `programmes` list so the UI can render the programme strip and the tile grid in one request.
     * [v2] List a Project\'s sub-Collections.
     */
    async subCollections(requestParameters: SubCollectionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubCollectionsIO> {
        const response = await this.subCollectionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
