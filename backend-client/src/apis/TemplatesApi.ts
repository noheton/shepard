/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CreateShepardTemplate,
  PatchShepardTemplate,
  ShepardTemplate,
} from '../models/index';
import {
    CreateShepardTemplateFromJSON,
    CreateShepardTemplateToJSON,
    PatchShepardTemplateFromJSON,
    PatchShepardTemplateToJSON,
    ShepardTemplateFromJSON,
    ShepardTemplateToJSON,
} from '../models/index';

export interface ExportRequest {
    includeRetired?: boolean;
}

export interface CreateTemplateRequest {
    createShepardTemplate: CreateShepardTemplate;
}

export interface DeleteTemplateRequest {
    appId: string;
}

export interface GetTemplateRequest {
    appId: string;
    flatten?: boolean;
}

export interface ImportTemplatesRequest {
    body: string;
}

export interface ListTemplatesRequest {
    includeRetired?: boolean;
    kind?: string;
}

export interface PatchTemplateRequest {
    appId: string;
    patchShepardTemplate: PatchShepardTemplate;
}

export interface TagsRequest {
    kind?: string;
}

/**
 * 
 */
export class TemplatesApi extends runtime.BaseAPI {

    /**
     * Returns every non-retired ShepardTemplate as a YAML list. The output is directly importable via POST /v2/templates/import. Set ?includeRetired=true to also export retired templates.
     * [v2] Export all templates as a YAML document (admin-only, T1f).
     */
    async _exportRaw(requestParameters: ExportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        const queryParameters: any = {};

        if (requestParameters['includeRetired'] != null) {
            queryParameters['includeRetired'] = requestParameters['includeRetired'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/templates/export`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Returns every non-retired ShepardTemplate as a YAML list. The output is directly importable via POST /v2/templates/import. Set ?includeRetired=true to also export retired templates.
     * [v2] Export all templates as a YAML document (admin-only, T1f).
     */
    async _export(requestParameters: ExportRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string> {
        const response = await this._exportRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Names need not be unique. Two templates can share a name when they\'re different kinds; same-kind same-name should use PATCH which triggers copy-on-write.
     * [v2] Mint a new template (version=1). Admin-only.
     */
    async createTemplateRaw(requestParameters: CreateTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShepardTemplate>> {
        if (requestParameters['createShepardTemplate'] == null) {
            throw new runtime.RequiredError(
                'createShepardTemplate',
                'Required parameter "createShepardTemplate" was null or undefined when calling createTemplate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/templates`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateShepardTemplateToJSON(requestParameters['createShepardTemplate']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShepardTemplateFromJSON(jsonValue));
    }

    /**
     * Names need not be unique. Two templates can share a name when they\'re different kinds; same-kind same-name should use PATCH which triggers copy-on-write.
     * [v2] Mint a new template (version=1). Admin-only.
     */
    async createTemplate(requestParameters: CreateTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShepardTemplate> {
        const response = await this.createTemplateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets retired = true; the row stays on disk so existing citations remain valid.
     * [v2] Retire a template (soft-delete). Admin-only.
     */
    async deleteTemplateRaw(requestParameters: DeleteTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling deleteTemplate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/templates/{appId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Sets retired = true; the row stays on disk so existing citations remain valid.
     * [v2] Retire a template (soft-delete). Admin-only.
     */
    async deleteTemplate(requestParameters: DeleteTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteTemplateRaw(requestParameters, initOverrides);
    }

    /**
     * [v2] Read one template by appId.
     */
    async getTemplateRaw(requestParameters: GetTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShepardTemplate>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling getTemplate().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['flatten'] != null) {
            queryParameters['flatten'] = requestParameters['flatten'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/templates/{appId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShepardTemplateFromJSON(jsonValue));
    }

    /**
     * [v2] Read one template by appId.
     */
    async getTemplate(requestParameters: GetTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShepardTemplate> {
        const response = await this.getTemplateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Accepts a YAML list of templates in the same shape as GET /v2/templates/export. Each entry is body-validated via TemplateBodyValidator. appIds are never reused — import always mints new entities. If a live (non-retired) template with the same name + templateKind already exists, import mints the next copy-on-write version and retires the prior row. Returns 200 + JSON array of created ShepardTemplateIO objects. Returns 400 if the YAML cannot be parsed or any template body fails validation.
     * [v2] Import templates from a YAML document (admin-only, T1f).
     */
    async importTemplatesRaw(requestParameters: ImportTemplatesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ShepardTemplate>>> {
        if (requestParameters['body'] == null) {
            throw new runtime.RequiredError(
                'body',
                'Required parameter "body" was null or undefined when calling importTemplates().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'text/yaml';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/templates/import`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ShepardTemplateFromJSON));
    }

    /**
     * Accepts a YAML list of templates in the same shape as GET /v2/templates/export. Each entry is body-validated via TemplateBodyValidator. appIds are never reused — import always mints new entities. If a live (non-retired) template with the same name + templateKind already exists, import mints the next copy-on-write version and retires the prior row. Returns 200 + JSON array of created ShepardTemplateIO objects. Returns 400 if the YAML cannot be parsed or any template body fails validation.
     * [v2] Import templates from a YAML document (admin-only, T1f).
     */
    async importTemplates(requestParameters: ImportTemplatesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ShepardTemplate>> {
        const response = await this.importTemplatesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Any authenticated user can browse. Retired rows are excluded by default; admins may set ?includeRetired=true to see them too.
     * [v2] List templates (latest non-retired version per name).
     */
    async listTemplatesRaw(requestParameters: ListTemplatesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ShepardTemplate>>> {
        const queryParameters: any = {};

        if (requestParameters['includeRetired'] != null) {
            queryParameters['includeRetired'] = requestParameters['includeRetired'];
        }

        if (requestParameters['kind'] != null) {
            queryParameters['kind'] = requestParameters['kind'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/templates`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ShepardTemplateFromJSON));
    }

    /**
     * Any authenticated user can browse. Retired rows are excluded by default; admins may set ?includeRetired=true to see them too.
     * [v2] List templates (latest non-retired version per name).
     */
    async listTemplates(requestParameters: ListTemplatesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ShepardTemplate>> {
        const response = await this.listTemplatesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The prior row is marked retired = true; a new row is minted with version + 1 carrying the patched fields (unchanged fields copied through).
     * [v2] Edit a template (triggers copy-on-write). Admin-only.
     */
    async patchTemplateRaw(requestParameters: PatchTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShepardTemplate>> {
        if (requestParameters['appId'] == null) {
            throw new runtime.RequiredError(
                'appId',
                'Required parameter "appId" was null or undefined when calling patchTemplate().'
            );
        }

        if (requestParameters['patchShepardTemplate'] == null) {
            throw new runtime.RequiredError(
                'patchShepardTemplate',
                'Required parameter "patchShepardTemplate" was null or undefined when calling patchTemplate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/templates/{appId}`.replace(`{${"appId"}}`, encodeURIComponent(String(requestParameters['appId']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PatchShepardTemplateToJSON(requestParameters['patchShepardTemplate']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShepardTemplateFromJSON(jsonValue));
    }

    /**
     * The prior row is marked retired = true; a new row is minted with version + 1 carrying the patched fields (unchanged fields copied through).
     * [v2] Edit a template (triggers copy-on-write). Admin-only.
     */
    async patchTemplate(requestParameters: PatchTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShepardTemplate> {
        const response = await this.patchTemplateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Used by the picker UI\'s tag-autocomplete. Optionally narrow to one templateKind.
     * [v2] Distinct list of tags across all non-retired templates.
     */
    async tagsRaw(requestParameters: TagsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<string>>> {
        const queryParameters: any = {};

        if (requestParameters['kind'] != null) {
            queryParameters['kind'] = requestParameters['kind'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apikey authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v2/templates/tags`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Used by the picker UI\'s tag-autocomplete. Optionally narrow to one templateKind.
     * [v2] Distinct list of tags across all non-retired templates.
     */
    async tags(requestParameters: TagsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<string>> {
        const response = await this.tagsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
