/* tslint:disable */
/* eslint-disable */
/**
 * shepard backend
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.0.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface ProvenanceStats
 */
export interface ProvenanceStats {
    /**
     * Scope of the query: 'instance', 'collection', 'user'.
     * @type {string}
     * @memberof ProvenanceStats
     */
    scope: string;
    /**
     * Entity / user id the scope narrows to. Null for scope=instance.
     * @type {string}
     * @memberof ProvenanceStats
     */
    id?: string | null;
    /**
     * Inclusive lower bound of the window (millis since epoch).
     * @type {number}
     * @memberof ProvenanceStats
     */
    sinceMillis: number;
    /**
     * Inclusive upper bound of the window (millis since epoch).
     * @type {number}
     * @memberof ProvenanceStats
     */
    untilMillis: number;
    /**
     * Width of one sparkline bucket in millis (daily = 86_400_000; weekly = 604_800_000).
     * @type {number}
     * @memberof ProvenanceStats
     */
    bucketMillis: number;
    /**
     * Total activity count over the window.
     * @type {number}
     * @memberof ProvenanceStats
     */
    totalCount: number;
    /**
     * Distinct contributor count over the window (instance-wide or scope-narrowed).
     * @type {number}
     * @memberof ProvenanceStats
     */
    distinctAgents: number;
    /**
     * Per-action-kind totals: { CREATE: N, READ: M, UPDATE: O, DELETE: P, EXECUTE: Q }.
     * @type {{ [key: string]: number; }}
     * @memberof ProvenanceStats
     */
    totalsByActionKind: { [key: string]: number; };
    /**
     * Sparkline buckets: each entry is [bucketStartMillis, count]. Empty buckets within the window are NOT filled.
     * @type {Array<Array<number>>}
     * @memberof ProvenanceStats
     */
    buckets: Array<Array<number>>;
    /**
     * Cumulative-integral buckets: each entry is [bucketStartMillis, runningTotal]. Running sum of activity counts up to and including the bucket — useful for 'total captured so far' tiles in the dashboard. Same bucket alignment as `buckets`.
     * @type {Array<Array<number>>}
     * @memberof ProvenanceStats
     */
    cumulative: Array<Array<number>>;
    /**
     * Entity-count census for the scope at query time (NOT window-filtered). Keys: dataObjects, fileReferences, timeseriesReferences, structuredDataReferences, spatialDataReferences, labJournalEntries. Drives the casual-user dashboard's 'what's in here' tiles. Null when scope=user (the census is not per-user).
     * @type {{ [key: string]: number; }}
     * @memberof ProvenanceStats
     */
    contentCensus?: { [key: string]: number; } | null;
    /**
     * Byte-size totals for the scope at query time (NOT window-filtered). Keys: `fileBytes` (sum of `ShepardFile.fileSize` reachable from the scope). Pre-FB1a rows uploaded before `fileSize` capture landed have `fileSize=null` and contribute 0 to the sum — the total is therefore a **lower bound** until those rows are re-uploaded. Null when scope=user (the byte totals are not per-user). Open-shape: future kinds (`timeseriesBytes`, `structuredDataBytes`) get added as separate keys without changing the schema.
     * @type {{ [key: string]: number; }}
     * @memberof ProvenanceStats
     */
    byteTotals?: { [key: string]: number; } | null;
}

/**
 * Check if a given object implements the ProvenanceStats interface.
 */
export function instanceOfProvenanceStats(value: object): value is ProvenanceStats {
    if (!('scope' in value) || value['scope'] === undefined) return false;
    if (!('sinceMillis' in value) || value['sinceMillis'] === undefined) return false;
    if (!('untilMillis' in value) || value['untilMillis'] === undefined) return false;
    if (!('bucketMillis' in value) || value['bucketMillis'] === undefined) return false;
    if (!('totalCount' in value) || value['totalCount'] === undefined) return false;
    if (!('distinctAgents' in value) || value['distinctAgents'] === undefined) return false;
    if (!('totalsByActionKind' in value) || value['totalsByActionKind'] === undefined) return false;
    if (!('buckets' in value) || value['buckets'] === undefined) return false;
    if (!('cumulative' in value) || value['cumulative'] === undefined) return false;
    return true;
}

export function ProvenanceStatsFromJSON(json: any): ProvenanceStats {
    return ProvenanceStatsFromJSONTyped(json, false);
}

export function ProvenanceStatsFromJSONTyped(json: any, ignoreDiscriminator: boolean): ProvenanceStats {
    if (json == null) {
        return json;
    }
    return {
        
        'scope': json['scope'],
        'id': json['id'] == null ? undefined : json['id'],
        'sinceMillis': json['sinceMillis'],
        'untilMillis': json['untilMillis'],
        'bucketMillis': json['bucketMillis'],
        'totalCount': json['totalCount'],
        'distinctAgents': json['distinctAgents'],
        'totalsByActionKind': json['totalsByActionKind'],
        'buckets': json['buckets'],
        'cumulative': json['cumulative'],
        'contentCensus': json['contentCensus'] == null ? undefined : json['contentCensus'],
        'byteTotals': json['byteTotals'] == null ? undefined : json['byteTotals'],
    };
}

export function ProvenanceStatsToJSON(value?: ProvenanceStats | null): any {
    if (value == null) {
        return value;
    }
    return {
        
        'scope': value['scope'],
        'id': value['id'],
        'sinceMillis': value['sinceMillis'],
        'untilMillis': value['untilMillis'],
        'bucketMillis': value['bucketMillis'],
        'totalCount': value['totalCount'],
        'distinctAgents': value['distinctAgents'],
        'totalsByActionKind': value['totalsByActionKind'],
        'buckets': value['buckets'],
        'cumulative': value['cumulative'],
        'contentCensus': value['contentCensus'],
        'byteTotals': value['byteTotals'],
    };
}

